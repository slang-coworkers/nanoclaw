---
author_agent_group: ag-1783611156430-vvj8oi
author_session: sess-1787610434034-9mjefw
written_at: 2026-08-25T00:30:15.926Z
---

# [approver/human-disagreement] False-abstain on #12593: merged unchanged at my exact decided head — my CHALLENGER_CONCERN over-weighted a "no harness to verify" doubt my own investigation had already refuted

**Outcome join (verified vs live GitHub):** shader-slang/slang#12593 merged at `fb32456a905712bc18bad36eff3b4af9656b33c1` — my *exact* decided head — by tangent-vector at 2026-08-25T00:29:10Z, unchanged (no follow-up commits after my decision; head never moved past fb32456). Human verdict = APPROVED-equivalent. My decision = ABSTAIN_POLICY / CHALLENGER_CONCERN. **Mismatch: false-abstain.**

**What my decision got right, and where it still went wrong.** My challenger investigation had already concluded Devin's 🔴 (removed else-reset → NVAPI prelude persists on the shared session) was spurious: slang-test has the same no-reset *by design* (`slang-test-main.cpp:2302-2323`, comment "session can be shared, prelude overwritten by the renderer"), the NVAPI install branch is Windows+SDK-gated (unreachable on the Linux guard-test lanes), CI was green on the exact un-quarantined `nvapi-guard-*` tests, and a human had approved at the head. Every one of those signals pointed to "safe." I then abstained anyway, on the residual "I have no Windows+NVAPI+GPU harness to definitively exercise the cross-test ordering." The merge-unchanged outcome shows that residual doubt carried no real risk.

**Root cause — the anti-pattern:** treating "I personally cannot run the environment that would exercise this path" as a standing reason to abstain, *even after* the available evidence (sibling-tool parity + platform gating + green CI on the very tests at issue + human approval) has already answered the question. "No harness to verify X" is only an abstain-worthy gap when X is *otherwise unresolved*. When convergent independent evidence already resolves X toward safe, the missing harness adds no information — abstaining on it is over-caution that scores as a false-abstain against the falsifiable reading ("material enough not to merge as-is"), which a clean merge-unchanged refutes.

**How to catch it next time (challenger discipline for fallback-tier Devin 🔴 I believe is spurious):** Before defaulting to ABSTAIN on "can't verify in my env," ask: *has the available evidence already resolved the concern?* Tally the independent signals — (a) does the sibling/reference impl the PR matches behave the same by design? (b) is the flagged branch platform/SDK-gated away from the lanes at risk? (c) is CI green on the specific tests the concern targets? (d) did a trusted human approve at this head? When (a)-(d) all hold and my own reconciliation says "spurious," the missing harness is not a genuine open question — the honest call trends toward clearing the 🔴, not abstaining on an already-answered doubt. The procedural bar on *upgrading a doc's 🔴 to WOULD_APPROVE via investigation* is real and I still can't emit WOULD_APPROVE here — but I should log the abstain explicitly as "conservative / procedure-forced, expect APPROVED on join," not carry it as a substantive CHALLENGER_CONCERN. The false-abstain signal is: my challenger narrative said "spurious" while my reason_code said "CONCERN." When those two disagree, the reason_code is the over-cautious one.

**Transferable prior for Step-0 recall:** render-test / shared-session prelude PRs of this shape (remove an incidental reset to match slang-test; platform-gated NVAPI branch; green on the affected tests) are safe merges. A Devin leak-forward 🔴 on them is a hygiene false-positive.
