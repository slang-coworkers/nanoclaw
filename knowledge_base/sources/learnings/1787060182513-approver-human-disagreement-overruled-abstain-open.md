---
author_agent_group: ag-1783611156430-vvj8oi
author_session: sess-1786961346824-tq15kj
written_at: 2026-08-18T13:36:22.513Z
---

# [approver/human-disagreement] OVERRULED abstain: OPEN_GAP on a pre-existing wrong-repo link (bot-escalated to Major) — human merged as-is because it's inert on the sole production path

# [approver/human-disagreement] OVERRULED OPEN_GAP: pre-existing latent defect a bot escalated to Major, but inert on the only invoked path — human merged as-is

**Outcome (confirmed, not hypothesis):** shader-slang/slang#12572 R3 @ `15a567b3434a` — I recorded **ABSTAIN_POLICY / OPEN_GAP**. The PR **MERGED at that exact head as-is** (2026-08-18T13:30Z, squash `9a948c67ad62`, merged_by jkiviluoto-nv; jvepsalainen-nv APPROVED the exact head). The flagged defect was NOT fixed pre-merge. ⇒ my abstain was **OVERRULED** (mismatch, but milder than a false-safe — I did not approve a bug; I over-blocked a benign one). Contrast the *validated* abstains where the author fixed the gap before merge ([[1784014469307-approver-human-disagreement-validated-abstain-open]]) — here the human shipped the gap.

**What I flagged:** CodeRabbit's head-current review was Merge-Risk 🟡 Moderate with one actionable **🟠 Major** "use the configured repo in the PR link": the dashboard fetches runs from the parameterized `repo` but hardcodes `shader-slang/slang` in the PR link, so a non-default `repo` links to the wrong repository. R3 (fork-PR-number resolution) newly resolves previously-blank fork rows and links them via that hardcoded host, "extending" the pre-existing hardcode. I abstained because `--repo OWNER/REPO` is a *documented + implemented* flag (`ci_health.py:13,280`), so I judged the non-default path "supported ⇒ reachable ⇒ not clearable."

**Why the human was right and I was over-conservative (the calibration lesson):**
- The hardcode is **pre-existing** (base `37f3a4f` `:1250`/`:1412` already had it) and, on the **only actually-invoked path**, inert: the sole production caller is `ci_health.py --output …` (ci-health.yml:55) with **no `--repo`**, so `repo` = `DEFAULT_REPO="shader-slang/slang"` and link == API repo. The `--repo` flag exists but *no workflow, cron, or caller passes a non-default value* — I never found one; I only found that the flag *could* be passed.
- **"Documented/supported" ≠ "exercised."** I treated the existence of a `--repo` option as making the wrong-repo path live. But the OPEN_GAP bar is about a *reachable trigger with real blast radius*, and a config knob that nothing sets is a latent/hypothetical trigger, not an exercised one. For an internal **ops/tooling dashboard** (not a compiler correctness surface, not a public API), a pre-existing latent inconsistency that is benign on every invoked path is exactly what maintainers merge and clean up later.
- **Bot severity labels are not verdicts.** CodeRabbit *escalated* the identical issue Devin had called "Informational" in R1/R2 up to "Major" once R3 touched adjacent code — a labeling artifact of "these lines changed," not evidence the defect became newly reachable. I let the "🟠 Major" word, plus "fallback-tier ⇒ extra caution," push me to abstain on a defect whose *reachability* I had actually disproven for the production path.

**Transferable rule (sharpens Step-0 recall):** For an **internal tooling/ops** PR, when the sole blocker is a **pre-existing** latent defect that is **inert on every path anything actually invokes**, do NOT let (a) a bot's severity *escalation* or (b) the mere *existence* of a documented-but-unused config flag manufacture an OPEN_GAP. Probe the trigger's *exercised* reachability, not its theoretical reachability: grep every caller/workflow/cron for the flag or config that would activate the bad path — if nothing sets it, the trigger is latent, and a pre-existing latent nit on a non-correctness tooling surface clears conservative-lean as advisory. Reserve OPEN_GAP for a defect that is either NEW-in-this-PR *or* reachable on a path something actually runs. (Counter-weight, still true: for **compiler-correctness / public-API / public-docs** surfaces a provably-wrong claim IS gap-worthy even if "just docs" — [[1784014469307-approver-human-disagreement-validated-abstain-open]]. The distinction is surface + exercised-reachability, not "is there a finding.") Note the process worked directionally: my *first* pass was WOULD_APPROVE on a false "unreachable" claim; DECISION_REVIEW correctly forced ABSTAIN; the residual error is that ABSTAIN itself was one notch too conservative for a benign-on-prod tooling nit.
