# [approver/calibration] CORRECTION to #12119 merged-join note — the PtrCast peel-set change was a LOAD-BEARING fix (R3 correctly ABSTAINED on it), NOT a safe-direction refinement; "walker miss is failure-direction-safe" is chain-AS-dependent, not universal

**Corrects my own earlier learning** `[approver/human-agreement] #12119 shape-independent OptiX SBT __ldg exclusion MERGED…`, which I wrote from an INCOMPLETE view (I only had my own session's R1/R2 rows and diffed R2→merged; I did not know PR #12119 had gone through R3/R4 in parallel sessions). The fuller ledger/memory shows the arc was **R1/R2 WOULD_APPROVE → R3 ABSTAIN_POLICY(OPEN_GAP) → R4 WOULD_APPROVE** — and the R3 ABSTAIN is exactly about the change I mischaracterized.

**What I got wrong:** In the merged-join note I said the walker's `NodeOutputRecordGetElementPtr`→`PtrCast` swap was "in the safe direction… the op-set can legitimately be refined post-approval… without invalidating a shape-independent-property approval." That framing is WRONG and dangerous:
- **Adding `kIROp_PtrCast` was a LOAD-BEARING correctness fix, not an advisory refinement.** A parallel session's R3 review (@f4fa4942590d) raised the missing `PtrCast` as a NEW gap R1/R2's reviews had not, and R3 correctly **ABSTAINED (OPEN_GAP)** on it rather than carrying the WOULD_APPROVE. The author then landed exactly `+case kIROp_PtrCast:` and it merged APPROVED — so R3's ABSTAIN was VINDICATED as a genuine catch.
- **The failure direction for a walker-miss on the SBT chain is the DANGEROUS one, not the safe one.** `GetOptiXSbtDataPtr` is `ConstantBuffer`-typed → `AddressSpace::Uniform` (lower-buffer-element-type.cpp:2570), and `Uniform` IS in `isPointerToImmutableLocation`'s immutable-AS set (util.cpp:3150). So if a forwarding op on the SBT chain is NOT peeled, the walker returns false (not-SBT), control falls through, the Uniform AS matches → returns TRUE (immutable) → `__ldg` re-emitted → the #10188 stale-read bug. codex DECISION_REVIEW in the R3 session explicitly PROVED this, overturning an initial "failure-direction-safe" draft — the SAME false-safe class as #11152.

**Why my R1/R2 Add-path reasoning was still locally correct (the subtle distinction):** my R1/R2 "walker miss returns false/mutable → safe" analysis was specifically about the `BitCast→Add→BitCast` path, where the outer BitCast carries `AddressSpace::UserPointer` — and UserPointer is NOT in the immutable-AS set, so THAT miss is genuinely safe. The error was **overgeneralizing** that one chain's safe direction into "walker misses are failure-direction-safe" in general. Failure direction is **per-chain, determined by the address space the un-peeled outer op carries**: UserPointer→safe(mutable), Uniform/ConstantBuffer→dangerous(immutable→__ldg). The PtrCast-on-SBT chain lands in the dangerous bucket.

**Transferable rule (the real lesson):**
1. When judging whether a peel-walker's op-set omission is a gap, do NOT ask "is a miss failure-direction-safe?" as a blanket question. Ask it **per the specific address space the missed chain would carry at fall-through**. For an SBT/ConstantBuffer/Uniform-rooted chain the immutable fall-through is the BUG direction — any plausibly-reachable un-peeled forwarding op there is a load-bearing OPEN_GAP (ABSTAIN), matching #11152/R3, not an advisory clear.
2. **Do not write calibration learnings from a partial revision view.** #12119 was decided across ≥4 sessions; diffing only my-head→merged and not knowing R3 existed led me to misread a vindicated correctness ABSTAIN as an "advisory the author tightened." Before generalizing from a merge outcome, check the memory/ledger for OTHER revision rows on the same PR (this PR's memory file lists all four) so you attribute each follow-up to whether a reviewer/approver treated it as advisory vs load-bearing.
3. The CHECK-NOT-bracketing follow-up WAS genuinely advisory (correctly cleared at R1/R2/R3) — that part of the earlier note stands. Only the PtrCast/op-set framing is corrected here.
