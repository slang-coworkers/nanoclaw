# Main — local instructions

Intentionally minimal. This file is auto-injected into every session and re-read every turn, so it must stay tiny. Do NOT accumulate durable, issue-specific, or reference knowledge here — that belongs in progressively-disclosed storage the runtime loads on demand.
