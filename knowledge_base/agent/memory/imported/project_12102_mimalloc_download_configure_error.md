---
name: project_12102_mimalloc_download_configure_error
description: "slang#12102: external/CMakeLists.txt silently git-clones mimalloc on a mutable unpinned tag that post-#12036 supplies the whole-DLL allocator; fix = vendor as a SHA-pinned submodule, delete the fetch, FATAL_ERROR on missing source. TERMINAL: PR #12107 MERGED 2026-07-15 (c5d4d76e67), issue auto-closed; approver WOULD_APPROVE at the exact merged head. ⛔ Pin churned four times — v2.1.7 -> v3.3.2 -> v2.3.2 (spirv-tools DEPS) -> FINAL v2.1.7 8c532c32 after pdeayton reported perf regressions in 2.3.2/3.3.2; earlier pins and the 'two mimalloc versions' coupling worry are superseded/disproven. ⛔ Also: `file(DOWNLOAD ... EXPECTED_HASH)` is DEFERRED-fatal (error raised, execution continues, configure still exits 1) — the 'fatals AT the call so the handler is unreachable' reading was falsified by job log + A/B."
metadata: 
  node_type: memory
  type: project
  originSessionId: 3533e4f7-af97-4361-b17c-8711d1ca2b2e
---

**✅ TERMINAL — MERGED (07-15 02:46:38Z).** PR #12107 merged by jkwak-work as **`c5d4d76e67`** on master; issue #12102 auto-closed (COMPLETED, 02:46:40Z) via `Closes #12102`. Independently verified (gh + merge commit on origin/master). Shipped: vendored `external/mimalloc` submodule @ v2.1.7 `8c532c32`, deleted silent `git clone … ERROR_QUIET` fetch, `FATAL_ERROR` on missing source (fast_float precedent), `.gitignore`/`.gitmodules` updated, 4 files +11/−24. Closes mutable-tag integrity risk by exact-commit pin (live post-#12036 `ad69c2e9f8` whole-DLL allocator). jkwak formal APPROVED 23:57Z @ HEAD `aa84370058` then merged. **No further action — chain closed.** Pin may be revisited after pdeayton-nv's perf investigation (2.1.7 chosen over regressed 2.3.2/3.3.2). Everything below = history.

---

shader-slang/slang#12102 — "Make missing mimalloc source a configure error instead of silently downloading it." Filed by nv-slang-bot at @jkwak-work's request off his review on PR #12036.

**Ask:** `external/CMakeLists.txt` (~L218–230) silently `git clone`s mimalloc on a mutable, unpinned tag (`v2.1.7`, `ERROR_QUIET`) when source is missing; post-#12036 that checkout supplies `mimalloc-static` linked into `slang` (whole-DLL `operator new`/`delete`), so a moved/compromised tag → arbitrary code in the shipped allocator. jkwak wants it promoted to `FATAL_ERROR`.

**Scoping (triager-verified against master 3eeda847c + #12036 head):** NOT a standalone one-liner. `external/mimalloc` is **not a submodule** (download-on-demand, no `.gitmodules`); `SLANG_ENABLE_SPIRV_TOOLS_MIMALLOC` defaults ON on Windows, so a bare `FATAL_ERROR` without vendoring breaks fresh Windows clones (pdeayton-nv precondition real). #12036 head already has a *partial* FATAL_ERROR (download-failure only) that does NOT close the mutable-tag hole. Recommended **Approach A** = vendor as SHA-pinned submodule, delete fetch, error on missing source (one PR).

**State:** TRIAGED → **HELD**. jkwak-work self-assigned his own hardening request; SHA pin / submodule structure is his call. Maintainer-self-assigned hold pattern (cf. [[project_11806_cmake_options_maintainer_selffix]], [[project_12035_overload_diag_reasons]], [[project_12083_licenses_dir_release_packages]], [[project_12095_autodiff_promotion_placement_loops]]). Fixer briefed with Approach-A memo but **not building** — no unsolicited draft.

**Provenance (07-14, jkwak asked "which PR introduced the download"; triager answered on issue comment 4974407648):** mimalloc download-at-configure-time introduced by **PR #8419 "Enable static mimalloc in Spirv-Tools"** (gtong-nv), merge commit `8ad0ae17880480abe587617c997ab28b23abc146`, merged 2025-09-16. Verified `git log -S "mimalloc.git" -- external/CMakeLists.txt` → exactly one commit. Follow-ups: PR #8460 added `SLANG_OVERRIDE_MIMALLOC_PATH` (2025-09-17); PR #12036 (open) expanded blast radius by linking `mimalloc-static` into `slang`. NOT #12036 — pre-dates it.

**Re-engage trigger:** jkwak comments asking for an implementation, or a "go" signal → triager re-dispatches fixer on canonical thread `gh-issue-shader-slang/slang-12102`. Do NOT auto-close on a re-webhook. jkwak's 07-14 provenance question was factual, not a go-signal — chain stayed HELD.

**07-14 (comment 4974495138):** jkwak stated the design — "drop the download logic and make the mimalloc repo a permanent submodule of slang in `external/mimalloc`." = confirms Approach A. But this is design-articulation on his SELF-ASSIGNED issue, NOT an explicit "make the PR" (he uses "make a pr" when he wants a coworker PR, cf. [[project_12097_ser_spirv14_vs_12099]]). Did NOT build. Triager posted a brief GitHub reply confirming alignment + surfacing the one maintainer decision (which commit SHA to pin — natural default = commit `v2.1.7` tag points to) + offering to draft if he wants a coworker to take it, else deferring. Chain still HELD pending his explicit yes/no on the offer. Do NOT dispatch fixer until he says go.

**Pin SHA (superseded — see GO below):** triager first resolved `v2.1.7` (annotated tag `7c5c43f5…`) → mimalloc commit `8c532c32c3c96e5ba1f2283e032f69ead8add00f`, offered as default. jkwak overrode it.

**GO (07-14, comment 4974520699):** jkwak replied "Let's go with release version 3.3.2" — read as go-signal + pin override to **mimalloc release v3.3.2** (a NEWER major line, 3.x not 2.x). NOT v2.1.7. Chain moves HELD → **BUILDING (draft)**. Fixer re-dispatched via triager on canonical thread. Fixer task: (1) resolve `v3.3.2` tag → commit SHA; (2) add `external/mimalloc` submodule pinned at that commit; (3) drop the `execute_process(git clone … v2.1.7 …)` fetch; (4) `FATAL_ERROR` on missing source; (5) flag any mimalloc-3.x-vs-2.x build concern with the #12036 `mimalloc-static` integration rather than silently working around — jkwak picked 3.x, but surface issues. **DRAFT only** (drafts-only guardrail; go=draft, jkwak drives ready/merge on his own issue). Ack stated the interpretation explicitly so jkwak can redirect if he meant "I'll take it." Fixer MUST call report_pr_created on PR open.

**v3.3.2 pin (SUPERSEDED — see PIN CHANGE #2 below):** `v3.3.2` annotated tag → mimalloc commit `30b2d9d89099bee08e9f67a1ffb3e12e7ba45227` (tagger 2026-04-29). Was the pin for ~5 min until jkwak changed his mind.

**PIN CHANGE #2 (07-14, comment 4974630122):** jkwak: "On my second thought, let's use whichever commit that spirv-tools repo is using." → re-pin the `external/mimalloc` submodule to whatever mimalloc commit `external/spirv-tools` references (rationale: mimalloc was added *for* SPIRV-Tools per PR #8419, so match its version to avoid two mimalloc versions in one build). Still GO / still building — this is a pin correction, not a re-open. Routed to triager (correction to in-flight fixer, before it finishes a draft on v3.3.2). **Resolution task:** find the mimalloc commit spirv-tools pins. If clean → re-pin + ack. If not pinned/ambiguous → flag back, don't guess.

**PIN #3 RESOLVED (07-14, triager, comment 4974657517) — THE ACTIVE PIN:** `external/spirv-tools/DEPS` has `mimalloc_revision': 'fef6b0dd70f9d7fa0750b0d0b9fbb471203b94cd'`. Verified authoritatively at slang's recorded spirv-tools gitlink `b707790a` (working tree matches, no drift). **Submodule `external/mimalloc` pins at `fef6b0dd70f9d7fa0750b0d0b9fbb471203b94cd`.** Fixer hold-pinged to pause the pin step, then handed the corrected commit; everything else unchanged, DRAFT.

**VERSION DELTA flagged to jkwak (material, surfaced not papered):** `fef6b0dd…` is mimalloc **v2.3.2** (`MI_MALLOC_VERSION 20302`) = the **2.x** line, NOT the v3.3.2 (3.x) he named one message earlier. His "use whichever spirv-tools uses" supersedes → 2.x is correct-per-latest-instruction, AND 2.x is *lower* integration risk (same major line as the original 2.1.7 block), so the earlier 3.x-vs-2.x concern is largely moot. Triager ack named the commit + version delta so jkwak can redirect if he specifically wanted 3.x. Pin history: v2.1.7`8c532c32`(default, offered) → v3.3.2`30b2d9d8`(jkwak 1st pick) → v2.3.2`fef6b0dd`(spirv-tools DEPS) → **PIN #4 conflict, see below**.

**PIN #4 — CROSS-MAINTAINER CONFLICT (07-14, comment 4974692781):** pdeayton-nv (DIFFERENT maintainer, @-mentioned the bot): "please use mimalloc v2.1.7, I detected a regression in performance with 2.3.2 and 3.3.2." Directly contradicts jkwak's "match spirv-tools" (=2.3.2, the ACTIVE pin) — jkwak's alignment heuristic landed on exactly the version pdeayton says is regressed. NOT auto-flipping: pinning `external/mimalloc` to 2.1.7 while `external/spirv-tools/DEPS` still names mimalloc 2.3.2 could reintroduce jkwak's "two mimalloc versions" concern (build-coupling Q — don't guess). **Action:** triager (1) holds fixer PIN step (don't finalize 2.3.2, don't auto-flip 2.1.7), fixer proceeds on pin-independent scaffolding (delete fetch, FATAL_ERROR, .gitmodules structure); (2) read-only checks whether slang's external/mimalloc feeds spirv-tools or spirv-tools fetches its own per DEPS (determines if 2.1.7-here vs 2.3.2-there is a real mismatch); (3) posts GitHub reply — thanks pdeayton for the material perf finding, surfaces the jkwak-vs-pdeayton tension, tags @jkwak-work, proposes 2.1.7 (perf-safe + original pre-#12036 version, commit `8c532c32c3c96e5ba1f2283e032f69ead8add00f`) with alignment caveat, asks jkwak to confirm. HOLD pin until maintainers reconcile. This is a GitHub maintainer decision, NOT operator escalation. If jkwak concurs 2.1.7 → pin `8c532c32`. Chain: BUILDING (pin held pending reconcile).

**COUPLING CHECK RESOLVED — NON-ISSUE (07-14, triager, from source):** When SPIRV-Tools builds inside Slang it does NOT use its own `DEPS mimalloc_revision`. Slang sets `mimalloc_SOURCE_DIR`→its own `external/mimalloc` (`external/CMakeLists.txt:250`) before `add_subdirectory(spirv-tools)`; SPIRV-Tools' `external/CMakeLists.txt:29-31,64` consumes it (`if(DEFINED mimalloc_SOURCE_DIR)→add_subdirectory(${MIMALLOC_DIR})`). Both link the SAME single `mimalloc-static` from Slang's submodule. DEPS is a gclient/gn field, never a CMake input here. ⇒ pinning `external/mimalloc` to 2.1.7 while DEPS says 2.3.2 is NOT a two-versions mismatch — one version, whatever we pin. jkwak's "one version" worry satisfied by 2.1.7 too. This removes the only technical objection to pdeayton's 2.1.7 request. Reconciliation reply posted comment 4974727408, @jkwak-work tagged. Pin HELD pending jkwak's yes/no on 2.1.7.

**#12036 MERGED (07-14 22:32Z, merge commit `ad69c2e9f8`, triager-verified via gh pr view):** `SLANG_ENABLE_MIMALLOC` (mimalloc linked into `slang`, replaces global new/delete for whole compiler DLL) now LIVE on master — blast radius is real, not hypothetical; strengthens the fix case. #12036 restructured the block (`SLANG_BUILD_MIMALLOC`/`SLANG_MIMALLOC_AVAILABLE`) but KEPT the silent clone — survives on master at `external/CMakeLists.txt:242` (`git clone --depth 1 --branch v2.1.7`). Fixer re-targeted onto new structure: branch **`fix/issue-12102 @ 6b035ca489`**, tree clean, 4 files +12/−24, gersemi clean, `cmake -P` sanity passes both paths. Pin-independent work DONE. Branch NOT pushed, NO PR opened (contested pin never lands publicly). Both candidate SHAs verified fetchable (2.3.2 `fef6b0dd`, 2.1.7 `8c532c32`). Re-pin = one-liner on reconciled SHA. Awaiting jkwak's reply to reconciliation post (comment 4974727408) — no reply yet. On confirm → SHA to fixer → checkout pin → push → DRAFT PR → report_pr_created → reviewer → [Fix Report].

**PIN DECIDED (07-14, comment 4974871767):** jkwak "Let's start with 2.1.7." → reconciles conflict, concurs with pdeayton. **FINAL PIN = mimalloc 2.1.7 = commit `8c532c32c3c96e5ba1f2283e032f69ead8add00f`** (original pre-#12036 version; coupling check already cleared two-versions worry). "start with" hints he may revisit pin later once perf regression understood — forward-looking, 2.1.7 is the pin now. Fixer unblocked: re-pin `external/mimalloc`@`8c532c32` (one-liner on branch `fix/issue-12102 @ 6b035ca489`) → push → open **DRAFT** PR (base master, `Closes #12102`) → `report_pr_created` → reviewer → [Fix Report]. DRAFT-only; jkwak drives ready/merge. Chain: BUILDING → finalizing draft.

**RESOLVED → PR #12107 OPEN, NON-DRAFT (07-14 23:47Z triager [Triage Resolution]):** https://github.com/shader-slang/slang/pull/12107 — Closes #12102. Fixer opened as DRAFT; **jkwak-work (assignee) flipped it ready himself at 23:27Z** (maintainer-flipped = sanctioned; drafts-only guardrail honored). Vendors `external/mimalloc` submodule @ v2.1.7 `8c532c32`, deletes silent `git clone … ERROR_QUIET` fetch, collapses #12036's tri-branch into single `FATAL_ERROR`-on-missing (mirrors fast_float precedent). HEAD `aa84370058`, 4 files +11/−24. reviewDecision=REVIEW_REQUIRED, mergeable=MERGEABLE, mergeStateStatus=**BLOCKED** pending review. jkwak "Looks good to me" + 2 inline comments; fixer responded to both threads (reworded stale comment; confirmed slang-llvm/slang-glslang not vendored) — **both now RESOLVED by jkwak (07-14 ~23:57Z)**, no open review-thread blocker. **Review requested from pdeayton-nv.** CI: human-priority yield CLEARED (`wait-for-human-priority=SUCCESS`), real builds live+several green (corrects fixer's earlier stale "cosmetic yield/builds skipped"). GitHub footprint = non-draft PR description (no separate issue 5-bullet, per posting policy). **Routing VERIFIED (07-14 23:49Z):** `report_pr_created({repo:"shader-slang/slang",pr_number:12107})` fired on PR open (returned "PR #12107 mapped to this session"). Independent evidence it's LIVE (not just claimed): every PR webhook since — both `ci_failed`, jkwak's `pr_review`, both `pr_mention` inline comments — landed on the fixer's session. pdeayton's pending review + CI + comments route to fixer, no orphaning. Routing question CLOSED.

**jkwak APPROVED #12107 (07-14 ~23:58Z, per fixer [Fix Update]):** `reviewDecision=APPROVED`, approval `commit_id=aa843700` = current HEAD (not stale). jkwak resolved both his own inline threads (.gitignore, external/CMakeLists.txt). **Still `mergeState=BLOCKED`** — pdeayton-nv remains a requested reviewer (expected, he raised the 2.1.7 perf point); slang-reviewer A/B/C combined verdict also pending. Fixer FROZEN (no push — a commit would auto-dismiss jkwak's approval; not marking ready/merge — merge is team's call). Holding webhook-driven for pdeayton review / reviewer verdict / merge. NOTE: this [Fix Update] arrived fixer→Main DIRECT (tier-skip; fixer's parent is triager). Did NOT reply (favorable non-actionable status + avoid reinforcing tier-skip edge). Held state unchanged (approval is progress within "held pending pdeayton+reviewer"). If fixer keeps reporting direct, have triager reinforce the edge. Next human action: pdeayton-nv review → jkwak merges (his issue). DRAFT/ready + merge = maintainer-owned. **3.x-vs-2.x pre-check (triager):** the `mimalloc-static` STATIC target name is UNCHANGED between v2.1.7 and v3.3.2, so #12036's `target_link_libraries(slang PRIVATE mimalloc-static)` still resolves; fixer to verify SPIRV-Tools allocator/version path at build time and flag anything else. State: HELD→**BUILDING (draft)**. Awaiting fixer [Fix Report] + PR#; triager forwards up + posts draft-PR footprint on issue on open. Coordinate with open PR #12036 (touches same block).

**APPROVER VERDICT (07-15 ~00:13Z) — WOULD_APPROVE (CLEAN) @ `aa8437005881`, ledger-only (shadow):** slang-pr-approver ran `/slang-pr-approve`. A `synchronize` landed mid-review (`a868769e`→`aa8437005881`, an amend — build-logic-inert: only a CMake comment + FATAL_ERROR message string changed); re-pinned to settled head. **Tier = Devin-only** (bot-authored PR ⇒ production claude-code-action correctly skips, harvest exit 20; no CodeRabbit). Devin timed out first fetch (exit 3), retried at 18-min window → **0 bugs / 0 flags / 2 informational-advisory**. **Clauses 6/6 pass** under mounted `v0-shadow-relaxed` policy (note: `external/CMakeLists.txt` + `external/` are NOT protected under the mount — only `.github/**` + `slang-tag-version.h`; recall-agent ABSTAIN warning was against the bundled conservative default the mount overrides). **Challenger CLEAN:** pin `8c532c32`=mimalloc v2.1.7 annotated tag; approach+pin maintainer-blessed (pdeayton perf regr on 2.3.2/3.3.2, jkwak "start with 2.1.7"); default Linux/macOS unaffected (`SLANG_BUILD_MIMALLOC` OFF ⇒ FATAL_ERROR unreachable); Windows CI `submodules: recursive` ⇒ new hard-fail never fires (`check-submodules`=success). **Critique gate:** DECISION_REVIEW=approve, OUTPUT_REVIEW=approve; codex caught wrong mechanism in reasoning (no-branch submodule pin verifies against DEFAULT BRANCH, not `refs/tags/v2.1.7`) — corrected to `8c532c32` is-ancestor-of-mimalloc-`main`. **jkwak-work (COLLABORATOR) APPROVED at exact head → clean human/shadow agreement.** Recorded to ledger; NOTHING posted to GitHub (shadow). Next human action still: pdeayton-nv review → jkwak merges (his issue). Merge = maintainer-owned.

**✅ TERMINAL — MERGED (07-15 02:46:38Z, merge commit `c5d4d76e`):** jkwak-work merged PR #12107 at the **exact approver decision head `aa8437005881`, unchanged, zero follow-up commits** — ideal calibration case (merged-unchanged-at-decision-head, not a false-safe). Shadow-mode join recorded to ledger (`human_verdict=APPROVED` on both review-approve join and merge join); nothing posted to GitHub. Chain fully terminal — issue #12102 closed via `Closes #12102`. No new abstract lesson (confirmed-safe shape already captured in a prior approver learning).

**GitHub:** triage 5-bullet live on issue (comment 4972592220), edited in place to HELD ("a coworker draft is available on request; not opening one unsolicited"). Related: PR #12036, [[project_11925_mimalloc_core_parked]].
