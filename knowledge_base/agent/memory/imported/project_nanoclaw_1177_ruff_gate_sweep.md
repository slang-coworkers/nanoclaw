---
name: project_nanoclaw_1177_ruff_gate_sweep
description: "nanoclaw#1177 (F15, #1163 follow-up) slang-mcp ruff 15->0 + gate wiring. Reviewed INLINE by Main, all claims re-derived independently (pinned ruff 0.14.8, own AST multiset, F811 repro by execution) — every figure matched. Comment 5239878203. No blocking findings; test/ has 8 pre-existing errors outside the gate's src/ scope."
metadata:
  node_type: memory
  type: project
  originSessionId: nanoclaw-1177
---

**#1177 (2026-08-10)** — `fix(slang-mcp): clear the 15 pre-existing ruff errors and wire the gate (F15 follow-up)`, branch `f15-slang-ruff` → **`nv-slang`**, author **szihs** (human, not bot). 6 files, **+107/−41** across 2 commits. `pr_ready_for_review` webhook arrived twice (reason `opened` 11:48Z, then `synchronize` for commit 2 at 11:51Z) carrying the generic post-#874 *"route to the project's `*-pr-approver`"* task string — **standing #874 rule overrides**: NanoClaw-platform fork, no nanoclaw approver wired, and a slang/slangpy COMPILER approver at a nanoclaw-repo PR is nonsensical. Handled INLINE by Main, same as #1123/#1163. Comment **5239878203**.

⭐**Every load-bearing claim was RE-DERIVED, not read — and all of them held, to the digit.** This is the rare case where an author's self-reported verification survived independent reproduction intact:
- **`15 → 0` confirmed by execution** with the branch's OWN pinned ruff (`0.14.8` from `uv.lock`, NOT my ambient 0.16.2 — version-matching matters for a count comparable to CI's). Base `8b3c32a9`: `Found 15 errors`; head `36c2aadd`: `All checks passed!`. My rule tally reproduced the PR's table exactly: **9 E501 · 3 I001 · 1 F841 · 1 F821 · 1 F811**.
- **AST string-constant multiset check reproduced independently**, all four figures matching: discord 689→689 IDENTICAL, slack 442→442 IDENTICAL, feedback_collector 190→190 IDENTICAL, server 863→862 (`LOST x1 'Discord'` = the deleted F841 line), github 1324→1324 (docstring swap only). ⭐**A multiset is the right instrument for verifying a reflow: it is insensitive to line-wrapping, which is the only thing that changed.** A diff would have been noise.
- **F811 reproduced BY EXECUTION**, yielding the exact quoted message: two module-level `async def get_file_contents` at base lines **1622** and **1983**; the second binds, so the 4-positional call raised `TypeError: get_file_contents() takes 1 positional argument but 4 were given`, and `except Exception` swallowed it into `"File does not exist, will create new file"`. Real bug, correctly root-caused.
- ⚠️**One SCOPE refinement I added:** `sha` is `Optional` on `CreateOrUpdateFileArgs`, so a caller passing `sha` explicitly never entered the broken branch. The breakage was confined to the **auto-lookup** path — which is what the body claims, so not a correction, but the "could not work" phrasing reads broader than the defect.
- **F841 was STALE, not merely unused** (verified): the deleted comment claimed eager init gated on `"Discord" in available_apis`, while the live gate at `:1153` is `os.environ.get("DISCORD_EAGER_INIT") == "1"` — **default off**. The comment described the opposite of the code.

⭐⭐**The two ways a lint sweep cheats — both checked, both absent.** `grep noqa` over the diff → **nothing**; `[tool.ruff]` **byte-identical** base vs head (`select=["E","F","I"]`, line-length 120, no `ignore`/`exclude`/`per-file-ignores`). `pyproject.toml`/`uv.lock` are not in the diff at all (they appear only inside the workflow probe path + a comment). ⇒ the 15 went away because the CODE changed, not because the gate was widened. **Checking for suppression + config-loosening is the cheap discriminator for any "N→0 lint" PR; without it, "All checks passed" is unfalsifiable.**

⭐**A probe-gated step that SKIPS looks identical to one that PASSES at the check-run level** — so I read the step conclusions and the job log, not just the check-run rollup. Step 7 `ruff => success` (not skipped), log shows `95 passed, 5 skipped` + `All checks passed!`. And I ran the probe's **`else` arm** standalone (the branch CI could not exercise here): emits `present=false` + notice, **exit 0** ⇒ the job no-ops cleanly on a branch lacking the directory. Premises verified: slang-mcp absent from nv-main (contents API **404**), `.github/**` nv-main-owned (`.github/nv-path-guard/nv-main.txt:66`).

⛔**The commit-2 message's "byte-identical to nv-main's" claim is FILE-level FALSE, JOB-level TRUE — and the job level is what the claim needs.** Whole-file sha256 differs (`ci-pr1177` 12,495 B vs current nv-main 12,355 B vs companion **#1176** 18,072 B) because nv-main's copy carries jobs nv-slang's lacks (typecheck gate, KB observability tests). Extracting just the `python:` block: **identical, `sha256 60e48e57…`, 62 lines / 2,986 B on BOTH #1177 and #1176** ⇒ canonicalization really is a no-op, which is the claim that closes #1163's ownership caveat. ⭐**Compare the UNIT the claim is about, not the file that contains it — a whole-file hash would have manufactured a false "claim is wrong" finding here.**

⚠️**One non-blocking finding surfaced:** `test/` carries **8 ruff errors** (5 F401, 3 I001) invisible to the gate, which checks `src/` only. **Pre-existing and unchanged** (identical tally at base and head) ⇒ not a regression, correctly out of scope for a sweep. But it is #1163's shape again: the gate's scope is narrower than the tree it guards, so `All checks passed!` is a statement about `src/`, not the package. Offered as a follow-up (widen to `src/ test/`, or record why `test/` is deliberately excluded).

**Also verified the rename didn't move the public contract:** `_get_file_contents_raw` referenced in exactly 2 places, both inside `github.py`; `src/github/__init__.py` references identical; `server.py` references textually identical, shifted −3 lines by the F841 deletion.

**State at review:** OPEN, not draft, 0 reviews, `mergeable_state` **`clean`** (was `unstable` — CI merely still running, NOT a conflict; re-measured after completion). Both checks green on head `36c2aadd` (`ci` ✓ 39 steps, `python` ✓). **Verdict: no blocking findings; merge is the maintainer's call** (not merged by me — merge stays operator/maintainer-gated).

⛔**`gh pr comment` FAILED with `GraphQL: Resource not accessible by integration (addComment)`; the REST POST to `/issues/1177/comments` SUCCEEDED immediately.** Same GraphQL-vs-REST split as the 2026-06-18 PATCH incident in [[project_nv_slang_bot_readonly_incident]] — **NOT a credential outage, do NOT escalate as one.** ⇒ **On any `addComment`/GraphQL 403, retry via `gh api repos/<r>/issues/<n>/comments --method POST` BEFORE concluding anything about the token.** Note `gh auth status` said *"The token in GH_TOKEN is invalid"* at the very same moment reads returned `{admin:true,push:true}` and the POST landed — ⭐**`gh auth status` is a FALSE NEGATIVE here and predicts nothing; only an attempted write is a probe.**

**Sequencing (author's claim, verified sound):** must land before nv-main's copy is canonicalized over it. Since the `python:` blocks are byte-identical, either merge order is safe for *this file's content* — the risk the note actually guards is **the ruff step reaching slang-mcp before the 15 are fixed**, which merging this first eliminates. Companion nv-main half is **#1176** (`f15-followups`, open, `mergeable_state: clean`, 19 files +316/−60).

**UPDATE 2026-08-10 ~12:15Z — THIRD `synchronize`: commit 3 `df5e119e` (+9/−1 ci.yml) landed AFTER my review, and re-measuring VOIDED my own headline finding.** Comment **5240029465**.

⭐**Commit 3 closes exactly what I flagged** — the probe now emits distinct messages for both green outcomes, to `::notice title=` AND `$GITHUB_STEP_SUMMARY` (`present` → naming both commands; `absent` → *"SKIPPED, which is not the same as passed"*). CI green on the new head (`ci` ✓, `python` ✓). The author's own comment reproduces my rollup argument almost verbatim.

⛔⛔**RETRACTED: my "python: job block byte-identical to #1176, sha256 `60e48e57…`" finding is VOID. #1176 was FORCE-PUSHED (`beba52bd` → `20af817f`) and REDESIGNED — its ci.yml no longer contains the slang-mcp job, pytest, or `uv` at ALL** (grep: 0 matches for each). It now runs a repo-wide `Lint Python (ruff)` step: `ruff==0.16.2`, config in a NEW root `ruff.toml` (ruff's **default** rule set, `target-version = "py39"`), linting files enumerated from `git ls-tree "$HEAD_SHA"` — the PR branch's OWN tip, deliberately not the composed tree (its comment names `container/mcp-servers/**` as *"75 findings that no nv-main PR can fix"*).

⭐⭐⭐**A VERIFIED IDENTITY BETWEEN TWO OPEN PRs HAS THE LIFETIME OF A FORCE-PUSH. My measurement was TRUE when taken and FALSE ~35 min later, with nothing on my side changing** — the other side moved. ⇒ **an equality claim spanning two mutable refs must be RE-MEASURED at every redelivery, and its report should pin BOTH shas** (I did pin them, which is the only reason the retraction was clean and provable rather than a reversal I could not date). This is ANCHOR G's "stored figure is a conclusion" with a twist: the premise was not my stale state, it was *the world*.

⚠️**The instrument told me first, and I nearly misread it as an extraction bug.** My awk block-extractor returned **0 lines / 0 bytes** for #1176 while the fetched file was a healthy 15,085 B. ⭐**A zero from an extractor is ambiguous between "my pattern broke" and "the thing is genuinely gone"** — resolved by grepping the raw file for the marker (0 matches) + confirming `head -5` showed a valid workflow. Cf. [[feedback_a_failed_cd_makes_the_next_grep_a_false_zero]]: same shape, opposite resolution — here the false-zero was a TRUE zero.

⛔**The retraction produced a REAL finding that inverts this PR's central rationale.** #1177's stated purpose is that the leaf's copy has *"a deploy-shaped expiry date"* so the identical job must live on nv-main to survive canonicalization. With #1176 as it now stands, **that job does not exist on nv-main** ⇒ after `setup.sh`/`merge-train.sh` canonicalize nv-main's ci.yml over the leaf:
- **`uv run pytest -q` disappears** — nothing in #1176 runs it; slang-mcp's 95 tests stop executing, the exact regression #1163 existed to fix.
- **`uv run ruff check src/` disappears** — #1176's step lints its own `HEAD_SHA` tree, and slang-mcp is not in it (verified: `container/mcp-servers` → **404** at `20af817f`; **0 of #1176's 19 changed files** are under `slang-mcp`). It can never lint slang-mcp by construction.
⇒ the gate stays deploy-shaped and #1163's ownership caveat stays OPEN, contrary to what both PRs record.

⚠️**The two designs would also DISAGREE if both applied:** #1177 = `select=["E","F","I"]` + line-length 120 + ruff **0.14.8** (slang-mcp's `uv.lock`); #1176 = ruff **0.16.2** + **default** rule set + no line-length override. Different findings on identical files ⇒ "which copy wins" is a BEHAVIOUR question, not only a survival one. **Reported as a sequencing/coverage gap between the two halves needing a decision, NOT as an edit to #1177** — the diff here is still correct and I found nothing blocking in it. Suggested option (1): have #1176 keep the `python:` job alongside its new repo-wide step (the directory probe already no-ops it where slang-mcp is absent) — the two mechanisms are complementary, one covering nv-main's Python tail, the other a leaf-only directory.

⭐**Posting again via REST worked (`5240029465`), consistent with the GraphQL-vs-REST split above** — second confirmation in one chain, so treat REST-POST as the default path for this repo, not the fallback.
