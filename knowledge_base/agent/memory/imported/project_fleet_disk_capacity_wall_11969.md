---
name: project_fleet_disk_capacity_wall_11969
description: "2026-07-08 /dev/vdb hard capacity wall — merged reap exhausted (0G), P1"
metadata: 
  node_type: memory
  type: project
  originSessionId: 62aa630d-2cf2-4171-b501-95bd015c1719
---

**2026-07-08:** Fleet shared build volume `/dev/vdb` hit a **hard capacity wall** — 251G, 100% used, 988M free. The standing merged-only worktree reap is **fully exhausted**: fixer re-verified all 41 non-active worktrees via fresh `gh pr view --json state,mergedAt` → **0 merged, 0 bytes reclaimable** (30 OPEN PRs isolation-protected, 11 no-PR, 4 active chains + main clone). This is a **capacity** problem, not hygiene — the reap can't keep pace with 40+ multi-GB worktrees on a full 251G volume.

**Blocked work:** P1 #11969 (Metal fragment out-param ICE) — fix committed + codex-approved on `fix/issue-11969@20bc7d0125` (5.5G partial, build died at 828/1170 on ENOSPC, needs ~6.7G to finish incrementally). Instantly resumable; disk is the sole gate. Held staged, NOT thrashing against a full disk.

**Two operator-only levers (neither is Main's to pull):**
1. Expand `/dev/vdb` — structural fix; merged reap won't keep pace otherwise.
2. Operator-authorized pruning of genuinely-stale **no-PR** branches. Candidate set (needs fixer per-branch safe-and-recoverable confirmation first): `rebase-tot`, `rebase-11591`, `ci/issue-11926-sanitizer`, `pr9085-takeover`, `slangpy-1014`.

**⚠️ DO NOT PRUNE — live triaged→fixer chains (fixer wrongly listed as "abandoned"):** `11981`, `11982`, `11983`, `11984`, `6557`, `11952`. #11981 is #11969's sibling. These hold staged/pending work. [[project_11981_metal_export_out_param_addrspace]] [[project_11969_metal_out_param_addrspace]]

**Rules held:** Main never git-worktree-removes directly — reaps/prunes run via the owning fixer ([[feedback_always_reap_merged_worktrees]]). Pruning open-PR worktrees is forbidden (isolation). Operator capacity-lever ask timed out again (600s this round, 300s prior) — treated as async-pending, NOT denial ([[feedback_push_not_away]]); P1-with-fallback doesn't justify a blocking timeout:0 re-ask. Post-de-escalation posture = **STABLE HOLD, not acute-blocked**: Main deliberately did NOT re-fire a blocking gate for a now-non-urgent structural issue; surface the lever non-urgently on the operator's next touchpoint instead. Relates to [[project_fleet_build_thundering_herd]].

**Resume trigger:** the moment ≥6.7G frees by any lever → fixer resumes build → repro + tests/metal/fragment-out-param.slang → push → draft PR (Closes #11969) → report_pr_created → triager rolls up.

**2026-07-08 11:50 — RESOLVED (disk gate cleared, no operator prune needed).** Prod update to build 2.1.39 rebuilt/restarted containers; during update /ephemeral was 100% so regenerable `build/` output was cleared from git worktrees. Source/commits/uncommitted TRACKED changes untouched — only gitignored CMake/compiler output. Net: `/dev/vdb` dropped 100%(308MB) → **36% used, 154G free** (verified by Main via `df -h`). Capacity wall gone; 6 live chains all intact (nothing pruned). **#11969 fix commit `20bc7d0125` intact — but the 5.5G partial build (828/1170) is cleared → fixer must REBUILD FRESH (CMake reconfigure + full build), not resume incrementally.** Resume signal relayed to triager (msg149): re-check branch state → rebuild → repro+tests → push → draft PR → report_pr_created → triager rolls up. #11981 build output likewise cleared (source intact); re-check + rebuild if needed. Structural capacity issue (19G clone + ~40 worktrees on 251G) still real long-term but no longer acute.

**2026-07-10 19:18 — WALL REFILLED (the 07-08 "resolved" was incidental, not durable).** `/dev/vdb` back to **100% used, 2.1G free** (Main-verified via read-only mount `df -h`; matches slang-fixer's "2.4G free" report on the #9062 Approach-A build). Now **54 worktrees / 124G** of `wt-slang-*`. Confirmed capacity, not hygiene: **autonomous MERGED/CLOSED reap grant is EXHAUSTED again** — the only terminal worktrees (`fix/issue-7406`→#12013 MERGED 07-09; `fix/issue-12004`→#12027 CLOSED) already have build dirs cleared (4K `.log` stubs only) → 0 reclaimable. ~30 worktrees are OPEN-PR (isolation-protected); rest are live no-PR chains. **Reclaim candidate — CORRECTED by fixer 07-10 19:36 (my relay was wrong on both worktrees):** `wt-slang-11917-p3` branch is `fix/issue-11917-matrix` which **HAS OPEN PR #11987 → isolation-protected, NOT reclaimable** (my categorization queried by the `fix/issue-<n>` convention and missed the variant branch's own PR — see the branch-variant caveat now in [[feedback_always_reap_merged_worktrees]]). `wt-slang-11917-asafe` (branch `fix/issue-11917-asafe`, no PR) held an **unpushed commit a97110a** (clean tree, origin lacks it) → full worktree-remove NOT strictly safe. **What the fixer actually did (correct safe lever):** cleared only the regenerable `build/` dir from dormant `-asafe` → **+3.8G (688M→4.4G free, disk 99%)**; source checkout + unpushed commit + branch all intact. This is the same build-output-clear lever that worked 07-08, applied by fixer judgment within its own volume. **Lever that actually worked 07-08 = clearing regenerable `build/` output from dormant/awaiting-review worktrees** (source+commits intact, rebuild on resume) — a fixer judgment within its own volume, NOT a Main directive to nuke specific dirs. Posture per 07-08: slang-fixer flagged EARLY ("no action needed yet, not blocked") → **STABLE HOLD, no premature blocking operator gate**; surface capacity lever (expand /dev/vdb, or authorize stale no-PR prune) only if fixer escalates to genuinely `blocked` on ENOSPC. #9062 build ~1.6G so far, proceeding. **Refreshed protect-list (live, DO NOT prune):** 9062, 11952, 11967, 11970, 11981(#12014), 11982(#12034), 11983, 11984, 11985, 12051, 6557 + all OPEN-PR worktrees.

**2026-07-15 20:33 — WALL REFILLED AGAIN, now blocking #12120 (Main-verified via read-only mount: `/dev/vdb` 251G / 237G used / 1.2G avail / 100%).** Matches slang-fixer's [Fix Report] measurement. **Blocked work now = #12120** (jkwak-requested CLI-gated opt-in revert of image-as-index, the [[project_12051_descriptor_reuse_pinning]] second fix chain): code WRITTEN + COMMITTED (WIP 0415197042 on fix/issue-12120, +40/4 files+test, DIAGNOSIS+PLAN+soundness gates green), but CANNOT BUILD — a fresh worktree needs DXC-from-source ~15-20GB (system GLIBC 2.36 < required 2.38) which won't fit. Fixer freed its own incomplete build/ (+1.3G, insufficient), correctly WON'T touch siblings (standing order; ~50 siblings hold in-flight PRs incl. #12111). **No work lost — resume scripted at `/workspace/agent/active-work/slang-12120/RESUME.md`, ~30-40 min after unblock.** #12111 untouched (separate worktree, held for operator merge). **Escalated to operator 07-15** (same two levers: grow /dev/vdb, or human-authorized prune of stale sibling `build/` dirs — Main/operator's call which are safe, NOT fixer's; the build-output-clear lever that worked 07-08 & 07-10 is the low-risk option). Resume trigger: space frees → Main signals triager → re-dispatch fixer to build + open draft PR + report_pr_created. **STABLE HOLD** posture (code committed, not thrashing) — surface non-urgently, no blocking timeout:0.

**2026-07-17 — RESOLVED, volume RELOCATED (structural, not incidental clear).** jkwak's PR #12111 comment + fixer both confirm the volume was relocated: **865G free** (vs the 251G wall). This is lever 1 (grow/relocate) applied by operator/infra, not a build-output clear — should durably end the recurring wall. #12120 unblocked, resumable. Main closed the loop with operator on the dashboard escalation (msg 147). Prior 251G-wall episodes (07-08/07-10/07-15) were the old undersized volume; 865G is the new normal — do NOT re-read those as active.

**2026-07-15 ~20:51 — SECOND container hits disk wall, distinct volume (slangpy-pr-approver overlay `/`).** During its #1065 approval run, `slangpy-pr-approver` reported its **container overlay root `/` hit 100%** mid-run — Bash couldn't create its own working dir until the Write tool pre-created it; worked around by routing all artifacts to `/workspace`. This is a **different filesystem** than the fixer fleet's shared `/dev/vdb` (worktree/build volume) — it's the approver container's own overlay root. Net signal: disk pressure is **not** confined to fixer worktrees; at least one other coworker container's overlay is also saturating. When the operator does the pending disk check (from the 20:33 `/dev/vdb` escalation), scope it fleet-wide, not just `/dev/vdb`. Not separately escalated (folded into the existing open escalation; approver self-recovered, decision was recorded, no work lost).

**08:52–08:56 acute-drain episode — RESOLVED, do not re-read as active emergency:** free space briefly fell 988M→17MB (~88M/min); diagnosed (read-only via fixer) as an **external container** writing to shared /dev/vdb, NOT our fixer (0 build processes, #11969 partial idle not thrashing). It **self-flattened** (recovered to ~308MB, stable). No fleet-zero action was needed. **Scope correction:** the draining volume is `/dev/vdb` (shared worktree/build volume, fixer fleet) — NOT all containers. Daily-report agent is on `/dev/vda1` (separate, 11G free), unaffected. My initial "every container's session-DB writes fail" framing was overstated; true blast radius = work/containers on /dev/vdb. Structural saturation source: 19G base clone `/workspace/agent/slang` + ~40 worktrees @7–7.7G. Standing watch: if drain restarts at that rate, re-sample + fingerprint the external writer.
