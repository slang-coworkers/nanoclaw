---
name: project_12223_debug_build_og_debuggability
description: "#12223 -Og in Debug build breaks debugging — design-gated PARK"
metadata: 
  node_type: memory
  type: project
  originSessionId: 86f18437-0686-4a2b-bae5-c4f763fc0025
---

# 🏁 TERMINAL 2026-08-04 — CHAIN FULLY CLOSED. NOTHING RESUMES. NO ACTION ON ANY TRIGGER BELOW.

**Main-VERIFIED at 14:35Z, all four close-out conditions met:**
- **#12324 MERGED** 14:32:59Z by `skiminki-nv` (the author) at exactly the reviewed head `e53dc1d38dfd`; merge commit `ca76f8781acd`, **0 follow-up commits**.
- **#12223 AUTO-CLOSED** 14:33:01Z, `state: closed`, `state_reason: completed` ⇒ the `Fixes #12223` link worked. **No manual close was ever needed** (the typo had been fixed 08-03).
- **Branch `fix/issue-12223` REAPED** — `git/ref/heads/fix/issue-12223` → **404**, against a live control (`master` → `refs/heads/master`). Housekeeping done; nothing to clean.
- **Approver join = AGREEMENT:** `WOULD_APPROVE` @ `e53dc1d38dfd` vs human `APPROVED` (`jkiviluoto-nv` 11:27:36Z) + merge; `record_human_verdict` stamped on both triggers.

⛔**Every "RESUME", "tripwire", "flag", and "housekeeping" instruction in this file is now SPENT. Read below for history only.** ⭐**Outcome: our fix was NOT taken (our #12234 closed unmerged); the maintainer wrote his own at a deeper layer. Direction adopted, shape rejected** — and our feasibility work still framed it.

⭐⭐**The join method matters and this row proves it:** `merge_commit_sha` `ca76f8781acd` is a **NEW commit with no ancestry link** to the reviewed head (slang is squash-only) ⇒ **`merge-base --is-ancestor` returns a confident FALSE NEGATIVE on a fully-merged PR.** Compare `pulls/N` → `head.sha` against the decided sha instead. (Independently, my clone is shallow ⇒ local ancestry is unusable for a second, unrelated reason — scope the rule to the SIGNATURE, not whichever cause you met first.) See [[feedback_shallow_clone_makes_your_head_the_graft_root]].

⭐⭐**Calibration datum worth keeping:** all three approver advisories **shipped unaddressed** — the 🟡 docs wording at `:96` shipped byte-identical (sha256 `0d7600f10c29`), no configure-level regression guard was added (4 modified / 0 added files), and the env-var claim stayed in non-shipped prose. **The judgment held:** for a build-flag *relocation* with a real CI positive control (Debug legs building the tree with warnings fatal), an author-rebutted docs 🟡 plus an absent future-proofing guard do **not** warrant `OPEN_GAP`. Second vindication of that shape after #12140; contrast slang-rhi#802 where an *abstain* was vindicated because held-on tests later actually failed. ⭐**The discriminator is the FAILURE DIRECTION and whether a green could have been red — not the count of open findings.**

---

✅**DISCHARGED 2026-08-04 (Main-VERIFIED) — THE `Fixes #12233` TYPO TRIPWIRE IS CLOSED. Do not re-flag it.** skiminki-nv corrected the PR body 08-03 15:29:15Z (#12324 comment [5168407059](https://github.com/shader-slang/slang/pull/12324#issuecomment-5168407059), *"PR description fixed."*). **Authoritative check, not a body read:** `gh api graphql … closingIssuesReferences` on #12324 → `[{number: 12223, state: OPEN}]` ⇒ the link is live and **#12223 WILL auto-close when #12324 merges**. Every "flag the typo" instruction below is a **past position, superseded**. ⭐**A tripwire must be discharged at the TOP, where a reader lands — an appended note leaves the stale instruction standing in the RESUME slot.**

⛔⭐⭐**THE TRIPWIRE WAS REDUNDANT THE MOMENT IT WAS ARMED — we had ALREADY posted the flag.** Our `nv-slang-bot` flagged the typo on issue #12223 at **14:33Z** (comment `5167730436`); the "flag it when #12324 merges" note was written ~**15:07Z**, 34 min LATER. `github-actions[bot]` review `4845259301` (14:38Z) flagged it too, and skiminki's fix quotes that wording. ⇒ had it fired, it would have **duplicated our own public post**, and **issue-comment edits `403` for this token ⇒ permanently.** ⭐**Before storing a "flag X later" trigger, check whether the fleet already flagged X** — a note recording an intent, written after the act, reads as un-acted-upon forever. ⚠️I also briefly published *"the author fixed it himself, unprompted"* here — a tidy causal story, **refuted by one API call**; see [[feedback_correction_must_sweep_whole_file]] postscript.

✅⭐⭐⭐**APPROVER DECISION 08-04: WOULD_APPROVE @`e53dc1d38dfd`** (mode `live_late`, policy `v0-shadow-relaxed`, tier PRIMARY = `github-actions[bot]`, clauses 6/6, diff_hash `a49272d404de`; 0🔴/1🟡 with the 🟡 cleared as author-rebutted docs phrasing). CI: 58 check-runs completed (48 success/10 skipped, 0 fail), **22 workflow runs all `run_attempt: 1`** ⇒ no latest-attempt-wins green. `reviewDecision: APPROVED`; `mergeStateStatus: BLOCKED` recorded **without inferring a cause**. Merge stays maintainer-gated; approver never posts.

⛔⭐⭐⭐**OUR ENV-VAR FINDING WAS TRUE WHEN POSTED AND THE REFUTATION MEASURED A DIFFERENT HEAD — I verified this myself, and it inverts the approver's "location-refuted" framing.** The approver read `docs/building.md:98` at `e53dc1d38dfd` ("env vars set **the base flags**" ⇒ slot 1 ⇒ no `-O`-override claim ⇒ advisory-not-gap). But we posted at **14:37:55Z on 08-03**, when HEAD was `25cc0718ac73`, and that head's line 98 read: *"The `CXXFLAGS`, `CFLAGS` and `LDFLAGS` environment variables **can also be used**, but only when a build directory is first configured"* — **unscoped**, in a section whose surrounding lines are all about overriding the Debug `-O` level. The scoping words **"to set up the base flags"** were ADDED in `e53dc1d38dfd` ("Minor fixes", 08-04 09:12Z) — i.e. **the refuting text did not exist when we made the claim.** ⇒ **the finding was accurate against its own head; only its CURRENCY expired.** ⭐⭐**A refutation is a measurement with a timestamp too — "your claim is wrong" and "your claim has been fixed" are opposite conclusions from the same read** ([[feedback_a_live_artifact_read_is_a_measurement_with_a_timestamp]]).

⚠️**DO NOT claim we caused it.** The added clause is *exactly* the one-clause docs fix we recommended, and it landed after our post — but there is **NO acknowledgment**: zero replies to comment `5167783319`, no mention in the commit. Temporally consistent ≠ causal. ⭐**I made precisely this error earlier in the same session** (inventing "the author fixed the typo unprompted"), so the rule is load-bearing here: **an unacknowledged upstream change that matches your advice is UNATTRIBUTABLE — record the coincidence, never the credit.** Same commit also switched `set(...)` → `string(PREPEND ...)` on the `_INIT` seeds to preserve toolchain-supplied values.

🟢⭐⭐⭐**HUMAN APPROVAL LANDED — #12324 is APPROVED and merge-ready, gated only on someone pressing merge (08-04, Main-verified via FOUR instruments).** `jkiviluoto-nv` submitted **APPROVED** at **11:27:36Z** on review `4853681015`, at the pinned head `e53dc1d38dfd` (empty body). Corroborated by: `gh api pulls/12324/reviews` (11 rows, `APPROVED` count = 1), `mcp__slang-mcp__github_get_pull_request_reviews` (same row), and GraphQL `reviewDecision: APPROVED` + `latestReviews` → `jkiviluoto-nv: APPROVED`. `mergeStateStatus: BLOCKED`, `state: OPEN`, `mergedAt: null` — **BLOCKED with an APPROVED decision ⇒ the block is a branch-protection/queue condition, NOT a missing review.** ⛔Cause NOT inferred beyond that.

⛔⭐⭐⭐**MY OWN INSTRUMENT DEFECT, and I published the wrong claim from it.** In my 12:20Z dispatch I wrote that #12324 had *"`juliusikkala`… note that review shows as `DISMISSED`"* and characterized the review state with **no APPROVED review present** — but jkiviluoto's approval had existed since **11:27:36Z, i.e. ~53 minutes BEFORE my read.** My `github_get_pull_request_comments` call returns `issue_comments` + `review_comments` and **does NOT return review STATE rows**, so an `APPROVED` with an empty body is **structurally invisible** to it. I then treated that silence as evidence of absence. ⭐⭐**REVIEW *STATE* AND REVIEW *COMMENTS* ARE DIFFERENT ENDPOINTS — an approval carries no comment, so a comments-endpoint read can never see it.** ✅Always confirm approval state via `pulls/N/reviews` **or** GraphQL `reviewDecision`; never from a comments listing. Extends the endpoint-split rule in [[slang-routing-lessons-index]] (`pulls/{n}/reviews` alone is blind to issue comments) — **it cuts BOTH ways, and I had only internalized one direction.**

⭐⭐**The approver caught it, not me — and I doubted it first.** Its 13:16Z note reported *"no new review since `jkiviluoto-nv`'s 11:27:36Z approval"*; my instinct was that this contradicted my own read and needed challenging. It was **right and I was wrong**; the challenge is what surfaced my defective instrument. ⭐⭐⭐**A fresh measurement contradicting mine ⇒ AUDIT MY INSTRUMENT BEFORE DISPUTING** — recency and authorship aren't evidence, and here the peer's tool was simply the correct one for the claim.

⏳**#12324 (was) OPEN/UNMERGED @head `e53dc1d38dfd` (08-04 11:22Z, non-draft, 4 commits, +76/−24, 4 files, `mergeable_state: blocked`).** Went ready-for-review 08-04 → routed to **slang-pr-approver** on thread `gh-pr-shader-slang/slang-12324`. Live: 2 `github-actions[bot]` doc-accuracy gaps **REBUTTED by skiminki** (`3711841902`, `3711845934` — he holds `-Og` names the opt level and `-g` is a toolchain default). Remaining RESUME = **merge** → confirm #12223 auto-closed (now expected to work) + reap `fix/issue-12223`.

shader-slang/slang#12223 — "Degraded debugging experience in debug builds." Reporter juliusikkala (core MEMBER), GCC 15.3.0: `<optimized out>` locals + single-step jumping across functions. Reverting #12140 fixes it.

**Root cause (VERIFIED @HEAD 5281ccc66, source inspection):** PR #12140 (merged 07-17, commit d9c9fa4, author skiminki-nv) added in `cmake/CompilerFlags.cmake:195` (inside `set_default_compile_options`):
`target_compile_options(${target} PRIVATE $<$<CONFIG:Debug>:-Og>)` guarded by `GNU|Clang AND NOT MSVC`. `-Og` is appended AFTER `CMAKE_CXX_FLAGS_DEBUG` (`-O0 -g`) → last-`-O`-wins → Debug compiles at `-Og`. No opt-out short of editing source.

**Classification:** regression / medium / P2 / build-system(CMake,CI). Dev-experience only, no shipped-binary impact. `regression` label applied; NOT `reproduced` (this box GCC 12, severity is GCC-15-scaled hypothesis, cause confirmed). Type "Build" already set.

**Solution space (mechanism SETTLED, default UNSETTLED):** gate `-Og` behind a `SLANG_ENABLE_*` option (matches SLANG_ENABLE_RELEASE_LTO convention, ~1-line guard). Default is the design call:
- A: opt-OUT, default ON — keeps #12140's ~4× Debug-build speedup (95m→21m slang-test) for CI; debuggers pass `-DSLANG_ENABLE_DEBUG_OPTIMIZATION=OFF`.
- B: opt-IN, default OFF — "Debug" debuggable by default (RelWithDebInfo already = fast+debug-info); opt in for speed. Triager's lean.
- C: straight revert — NOT recommended (discards benchmarked win).

**State: PARKED-at-triaged (design gate).** Default is a values tradeoff between two core MEMBERs (juliusikkala vs skiminki-nv). Verdict + blocker posted on issue (comment 5074128097) inviting maintainer decision. NOT dispatching fixer — would bake in unresolved default; avoids taskless-fixer session. RELEASE = maintainer/skiminki-nv picks default → then dispatch slang-fixer with chosen approach. Memo held by slang-triager; fixer holding do-not-implement.

**Decision-support (fixer investigate-only, re-verified by triager @HEAD 5281ccc66) — SHARPENED RECOMMENDATION:** default **OFF (opt-in) + pin ON in CI's Debug legs** — humans get debuggable Debug by default, CI keeps ~4× speedup. Load-bearing facts:
1. RelWithDebInfo is NOT assertion-preserving — `SLANG_ASSERT` is `#ifdef _DEBUG` (slang-common.h:363), `_DEBUG` defined only for Debug config (CompilerFlags.cmake:231); RwDI carries NDEBUG → strips all SLANG_ASSERT. Weakens the "RwDI already = fast+debuggable" argument; reinforces "Debug should be debuggable+assertion-carrying by default."
2. CI builds Debug on 21 legs (vs 32 release / 3 RwDI) → default OFF has real CI wall-clock cost → hence the CI-pin.
3. `SLANG_ENABLE_*` defaults are MIXED (ASAN/COVERAGE/TIME_TRACE/LTO=OFF; PCH/RELEASE_DEBUG_INFO/SPLIT_DEBUG_INFO=ON) → convention alone doesn't settle default.
Implementation when released ≈ ~2-line CMake gate + CI Debug-leg pin (Approach B shape). Recommendation NOT a blocker — still a maintainer values call. No new GitHub post (bot shouldn't push the values call publicly; ask already live).

**RESUME 07-27 — skiminki-nv (author/maintainer) commented (5089258348):** RESOLVED against the earlier fixer/triager "lean default-OFF/`-O0`" — skiminki says `-O0` default is "basically a showstopper" (full test suite prohibitively slow); prefers release builds over `-O0`. Two reasonable paths, he LEANS **option 2**:
- Opt 1: gate `-Og` behind `SLANG_ENABLE_*` (bot's suggestion).
- Opt 2 (his lean): provide a way to OVERRIDE optimization flags so those who find `-Og` problematic supply their own (e.g. `-O0 -g3`); bonus = trivial `-march=...` experimentation. Notes `CXXFLAGS='-O0 -g3' cmake --preset default` "doesn't work in Slang" today.
Root of why opt2 doesn't work today (hypothesis, for fixer to confirm): `set_default_compile_options` appends `-Og` via `target_compile_options` AFTER `CMAKE_CXX_FLAGS_DEBUG`/user `CXXFLAGS` → last-`-O`-wins clobbers user override. Opt 2 fix ≈ suppress `-Og` when user supplied an opt level, or add a Slang cache-var hook.
State: chain RESUMED → forwarded to slang-triager (07-27) to have fixer investigate opt-2 feasibility (still investigate-only/decision-support — "might suggest", not a firm "make the PR"; no juliusikkala concurrence yet). Firm opt1-vs-opt2 decision remains the dispatch gate. Fixer still do-not-implement until locked.

**FEASIBILITY FINDING 07-27 (fixer + triager independently re-reproduced @HEAD a4168d47c6; both empirical probes):**
- Q1 CONFIRMED: `-Og` is a target-level PRIVATE append (CompilerFlags.cmake:196); compile-line order = `CMAKE_CXX_FLAGS`(←env CXXFLAGS) → `CMAKE_CXX_FLAGS_DEBUG` → target opts (last) → `-Og` wins last-`-O`-wins → user `-O0` clobbered. Env CXXFLAGS structurally can NEVER win. Correction: CompilerFlags.cmake:180 comment says `CMAKE_CXX_FLAGS_DEBUG=-O0 -g` but cache value is really just `-g` (`-O0`=GCC implicit default); conclusion unchanged.
- Q2: **shape (a) "conditional-skip"** (inject `-Og` only if user gave no `-O`) ✅ minimal (~+3/−1, one file), reproduced 3 cases (none→`-g -Og`; env `-O0 -g3`→user wins; `-DCMAKE_CXX_FLAGS_DEBUG` override→user wins); honors skiminki's literal workflow, avoids check_cxx_compiler_flag genexpr trap. Shape (b) "-Og as CMAKE_CXX_FLAGS_DEBUG default" ❌ env still loses. Shape (c) named var `SLANG_DEBUG_OPTIMIZATION_FLAG` = most discoverable + `-march=` but doesn't honor literal env workflow.
- **Opt-2 FUNCTIONALLY SUBSUMES opt-1** (shape (a) covers "force -O0" + arbitrary flags); loses only DISCOVERABILITY (juliusikkala's exact gap) → mitigate w/ docs note or layer (c). They compose.

**DECISION 07-27 (Main): REPLY on GitHub, nudge opt-2 shape (a) + docs note.** Earlier "no post/values-call" steer LIFTED — opt-2 shape(a)+docs satisfies BOTH maintainers (no longer zero-sum), skiminki referenced the bot + host routed as pr_mention (posting authorized). Authorized slang-triager (closest-to-state; owns issue footprint) to post: findings as FACT, direction as OFFER (not locked), invite @juliusikkala concurrence, offer to open PR on maintainer go-ahead. Fixer STILL do-not-implement until an explicit "make the PR". Dispatch gate = maintainer picks + says go.

**POSTED 07-27 (nv-slang-bot):** GitHub reply live at issue comment [5089425319](https://github.com/shader-slang/slang/issues/12223#issuecomment-5089425319) — Q1 mechanism + shape-(a) 3 cases + subsumption as fact; opt-2 as offer; @skiminki-nv (mechanism/`-march=`) + @juliusikkala (discoverability) both mentioned; "happy to open the PR — just confirm". State HELD. ⚠️**SUPERSEDED trigger — do not act on it:** *"substantive reply from skiminki-nv or juliusikkala → triager forwards → fixer implements shape (a)"*. That was the **pre-resolution** condition; skiminki's **#12324** now carries the direction and our **#12234 closed unmerged**, so waiting on a reply would wait forever.

⚠️**SUPERSEDED RESUME (2026-08-03) — the typo half is DISCHARGED, see the top banner; do NOT flag it:** ~~"#12324 merges → re-read the merged diff, then flag to a maintainer that `Fixes #12233` is a one-digit TYPO for #12223, so this issue will NOT auto-close."~~ **Still live:** #12324 merges → re-read the merged diff → confirm #12223 auto-closed. ⛔Do not close #12223 myself — a human owns the close. Housekeeping: reap branch `fix/issue-12223`. ⭐**direction adopted ≠ our SHAPE accepted.**

⭐ **Why this needed fixing:** a trigger written pre-resolution goes **stale silently** — the row still reads "HELD with a resume condition," so nothing looks wrong, but the named event can no longer occur. Same class as a self-expiring note that doesn't expire itself: **when the resolution path changes, rewrite the trigger, don't just append the new state.**

**GO-AHEAD 07-27 — skiminki-nv (maintainer/author): "@nv-slang-bot Create a PR for Option 2" (comment 5091273388).** Explicit "make the PR" — dispatch gate RELEASED. UNPARKED → active fix. Routed via slang-triager to slang-fixer (07-27) to IMPLEMENT opt-2 shape (a): inject `-Og` only when user supplied no `-O` level, in `cmake/CompilerFlags.cmake` (~+3/−1, guard the :196 target_compile_options append) + docs note in `docs/building.md` (the `CXXFLAGS='-O0 -g3'` / `-march=` override now works). Default `-Og` UNCHANGED (CI keeps speedup). Draft PR w/ `Fixes #12223` + 5-bullet, report_pr_created, `<github-post-authorized />` (maintainer-requested via bot mention). Merge OPERATOR-gated. juliusikkala discoverability closed by docs note.

**DRAFT PR #12234 OPENED + VERIFIED 07-27** (https://github.com/shader-slang/slang/pull/12234) — OPEN draft, base master, head `fix/issue-12223`, label `pr: non-breaking`, `Closes #12223` (authoritative closingIssuesReferences=[12223]), report_pr_created done. Diff: `cmake/CompilerFlags.cmake` (+34/−14) inject `-Og` only if user chose no `-O` (whole-`-O`-token match on CMAKE_CXX_FLAGS+_DEBUG); default STAYS `-Og` (CI/#12140 speedup unchanged); `docs/building.md` (+33/0) new "Optimization level of Debug builds" section. No `tests/*.slang` (build-system-only; verified via Ninja-MC compile_commands.json probe + `cmake -P` regex: default→`-g -Og`; `CXXFLAGS='-O0 -g3'`/`-DCMAKE_CXX_FLAGS_DEBUG`→user wins; `-O4/-Os/-Ofast`/bare `-O`→suppress; `-ObjC/-Onone/-march=native`→keep). codex PLAN+CODE+OUTPUT approve; gersemi clean. CI red = benign priority-yield (wait-for-human-priority + check-ci; build/test skipped; CI Retry Yielded Bot reruns) — NOT a regression. Reviewer routed by Main (triager has no reviewer edge). Merge operator-gated. Triager deferring issue-verdict refresh to merge close-out; Main posting brief PR-up ack on issue to close skiminki's "@nv-slang-bot Create a PR" loop.

**2026-07-27 13:14 — reviewer IN FLIGHT, verified (Main).** PR #12234 head `9898dc30c2`, draft, under active review — **3 slang-reviewer sessions live on thread `gh-issue-shader-slang/slang-12223`** (last-active 13:12–13:14Z, Main-verified). Confirms Main's earlier reviewer dispatch landed (fixer msg 69598 "reviewer-routing handled through parent" = already-routed, verdict returns to fixer — NOT a gap to close). Fixer correctly silent-holding; nothing owed by Main. **✅ slang-reviewer ADDRESSABLE AGAIN → clears the #12210 "not addressable" WATCH** ([[project_12210_autodiff_property_getter_frontend_crash]]) — that was transient/session-specific (07-26), NOT a persistent reviewer outage; no operator re-auth needed. Next: reviewer verdict → fixer; merge OPERATOR-gated.

**2026-07-27 ~13:49 — PR #12234 assigned to skiminki-nv by jkwak-work** (benign chain-state advance; no reply/no GitHub action owed — bots don't touch assignees/reviewers; fixer confirmed needs-nothing-back). Sharpens resume trigger: skiminki-nv is now de-facto reviewer/assignee → his review or comment is the expected next substantive event (alongside slang-reviewer's consolidated A/B/C verdict, still in flight — B+indep-CMake clean, A re-run churning, C pending).

**2026-08-03 SUPERSEDED — our PR #12234 CLOSED UNMERGED by skiminki-nv** (comment 5167659240: "Closing this one. It doesn't quite seem to be the right fix. PR #12324 is the next attempt."). He then posted on the issue "Fix candidate: PR #12324" (5167666849). Chain outcome = **our fix NOT taken; maintainer wrote his own**. Issue #12223 still OPEN.
- **His #12324** ("Make the Debug `-Og` default user-overridable", OPEN non-draft, self-assigned, branch `12223-user-overridable-optimization-flags`): different mechanism — seeds `CMAKE_C_FLAGS_DEBUG_INIT`/`CMAKE_CXX_FLAGS_DEBUG_INIT` **before `enable_language()`**; `cmake_initialize_per_config_variable` sets cache vars without FORCE so user/preset/toolchain value replaces default → env `CXXFLAGS`/`CFLAGS` honored *globally*. Guard `NOT WIN32` (not compiler-ID — ID unset before `enable_language()`) ⇒ **MinGW loses `-Og`**; and `CMAKE_<LANG>_FLAGS_DEBUG` being global means **in-tree deps now build `-Og` in Debug too** (both tradeoffs he self-documents). Also docs/building.md + gitignore `CMakeUserPresets.json`.
- **Ours (#12234) vs his:** ours = conditional-skip of the target-level append (per-target, compiler-ID-guarded, token-matched). His = fix the *initialization* layer so nothing needs suppressing. His is the more root-level fix (env vars work generally, not just `-O`) — consistent with "doesn't quite seem to be the right fix."
- ✅**FIXED BY THE AUTHOR 08-03 15:29Z — historical record only, do not act.** ⚠️ **(was) VERIFIED DEFECT in his PR body: `Fixes #12233` is a ONE-DIGIT TYPO** (#12233 = jkwak's closed PCH *PR*, not our issue). Closing keywords only auto-close **issues**, so this links NOTHING. Confirmed via #12223 timeline: only cross-ref is from our #12234, **no `connected` event from #12324** ⇒ **#12223 will NOT auto-close when #12324 merges.** Branch name proves intent. → flag politely.
- **Lesson:** a maintainer asking "create a PR for option 2" does NOT mean our *shape* of option 2 is accepted — he re-implemented at a deeper layer. Our feasibility work still framed the fix (he adopted the override *intent* + docs note); shape rejected, direction adopted.
**08-03 REVIEWER FINDING vs #12324 — VERIFIED, offered as decision-support:** his **env-var path can't override the Debug `-O` level even on a clean FIRST configure** (≠ the "ignored on reconfigure" caveat he documents). `CXXFLAGS='-O0 -g3'` seeds `CMAKE_CXX_FLAGS` (all-config) = `-O0 -g3`; his seeded `CMAKE_CXX_FLAGS_DEBUG` = `-Og -g` is emitted AFTER ⇒ `-Og` last ⇒ wins. **Same last-`-O`-wins mechanism as #12223, different variable pair.** Main independently confirmed w/o rerunning cmake: (a) read his docs patch verbatim — does advertise `CXXFLAGS`/`CFLAGS`/`LDFLAGS`, reconfigure-caveat only; (b) his diff deletes the target-level `-Og` (CompilerFlags.cmake 0/−24) + seeds per-config; (c) **this chain's OWN original probe `-O0 -g3 -g -Og` already proves emission order** CMAKE_CXX_FLAGS→CMAKE_CXX_FLAGS_DEBUG→target ⇒ moving `-Og` into the per-config slot keeps it last. Hits the EXACT spelling the reporter AND skiminki used (`CXXFLAGS='-O0 -g3'`). Severity = docs-accuracy/partial, NOT a build break (his primary `-D` recipe works → #12223 genuinely fixed for anyone following the docs). Cheapest fix = one docs clause; explicitly do NOT gate the seed (reintroduces the complexity his design sheds — and would look like re-arguing our shape). Reviewer's 2 other checks: his `NOT WIN32` reasoning genuinely forced + global-var/in-tree-deps consequence exactly as he describes → nothing to add. Posted on ISSUE #12223 (our live footprint, natural continuation of the sanity-check offer) NOT on #12324 (his PR, not routed to us — stay consistent w/ that rule).
- Actions: reviewer STOOD DOWN on #12234 (closed — don't post a verdict on a closed PR; hand any edge-case findings up as decision-support only); triager to refresh issue footprint + flag the typo; fixer to reap `fix/issue-12223` worktree/branch (closed unmerged). Merge of #12324 is skiminki's own, operator/maintainer-owned. RESUME = #12324 merges → confirm #12223 closed (manually if typo unfixed).

**✅ CHAIN CLOSED ON COWORKER SIDE 08-03 15:07Z (Main-VERIFIED via GitHub API).** Confirmed: #12223 **still OPEN** (state_reason null, assignee skiminki-nv, milestone Q3-2026); our #12234 **closed unmerged** 14:26:41Z; **his #12324 still OPEN/UNMERGED** (so the env-var finding remains upstream-actionable, and the `Fixes #12233` typo is still uncorrected → #12223 won't auto-close).
- **Both bot posts VERIFIED live on #12223** (published by a concurrent session sharing `nv-slang-bot[bot]`, NOT by the reporting fixer): typo-flag + edge-case offer = comment **5167730436** (14:33Z); env-`CXXFLAGS` finding = comment **5167783319** (14:37:55Z). The env-var post is well-framed — explicitly *"a docs point, not an objection to the design"*, gives the `-O`-token-order evidence, correctly distinguishes it from the already-documented reconfigure caveat, notes it hits the reporter's exact spelling, and recommends the one-clause docs fix over gating the seed (which would reintroduce the complexity his design sheds). Nothing owed.
- **⭐ The reporting fixer's restraint was CORRECT and is the durable lesson:** it never posted its env-var finding, because a concurrent same-identity session had already published it 4 min after triage's close-out. Since **issue-comment edits 403 for this token**, a duplicate would have been PERMANENT. Standing rule captured: **read the thread tail before posting; surfacing a finding upward is NOT authorization to post it.**
- **Fixer self-assessment (worth preserving — the useful part):** (1) rejected the defaults-based direction after probing exactly ONE bad variant (`set(... CACHE STRING "" FORCE)`), never tried `_INIT`-before-`enable_language()` — the one that works — then built the more complex detection fix; codex gates all passed *because gates validate the shape you chose, not whether a better mechanism exists*. (2) Then repeated the error in reverse: praised his fix by repeating his PR body rather than testing it; on probing, env `CXXFLAGS` still loses to `-Og`. (3) Walked back two overstatements under review ("no GitHub write at all" — deleting the remote branch was one; and a stale CI reading — attempt 2 passed 36-green, so the close was purely an approach call, NOT CI). → captured as shared learning **"Review gates validate the shape you chose, not whether a better mechanism exists — breadth-of-mechanism is yours to check first."**
- **Cleanup verified by fixer:** worktree, local + remote branch, sentinel, scratch file all gone; issue left OPEN for a human. **RESUME (typo clause SUPERSEDED 08-04 — see top banner) = #12324 merges → confirm #12223 closed;** ~~"manually if the `Fixes #12233` typo is still unfixed"~~ — the link is fixed, so auto-close is expected. No Main action.

See [[feedback_dont_close_open_proposals]], [[feedback_reopen_not_release_parked_feature]], [[project_taskless_fixer_review_cc_loop]], [[feedback_always_reap_merged_worktrees]].
