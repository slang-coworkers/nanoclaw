---
name: project_10584_svbarycentric_capability_check
description: "slang#10584 SV_Barycentrics missing fragmentshaderbarycentric capability check — triaged/parked; human draft"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1ec77e7d-0b24-47cf-9691-8d55a647b40c
---

slang#10584: `SV_Barycentrics` emits `OpCapability FragmentBarycentricKHR` + `SPV_KHR_fragment_shader_barycentric`, but its `core.meta.slang:5374` decl only requires stage atom `[require(fragment)]`, never the feature atom `fragmentshaderbarycentric` → profile-upgrade warning 41012 (`profile-implicitly-upgraded`) never fires. bug (missing diagnostic) / low / P2. REPRODUCED on ToT (0 diagnostics under `-profile spirv_1_6` and `spirv_1_3`). Not a dup.

**State: TRIAGED + PARKED (07-18).** jkwak-work asked only "can you triage this?" — triage + 5-bullet verdict posted (comment 5011388106); `reproduced` label applied. Main authorized option (a): verdict-on-issue only, **no fixer dispatched**.

**Why no fixer:** assignee @jhelferty-nv (human CONTRIBUTOR) already owns **draft PR #10666** (`Fixes #10584`) implementing the correct fix (adds require + propagation for direct-param & struct-field cases + diag test). Stalled: CI red on `test-slang` (linux/macos/windows-gpu) + `check-ci`, unaddressed coderabbit/claude-review, last touched 2026-05-05 — likely new requirement trips existing barycentric tests. Dispatching slang-fixer would create a competing PR against a live human draft → held per [[feedback_drafts_only_guardrail]].

**If re-pinged:** don't re-triage, don't open competing fix. Options if maintainer escalates: (b) authorize fixer to *unblock #10666* only (green CI / address review), or (c) ping @jhelferty-nv.

**🔄 TAKEOVER → bot PR #12194 (07-23 02:35, fixer msg 57332; canonical thread `gh-issue-shader-slang/slang-10666`).** The stalled human draft #10666 was taken over by the bot (maintainer-directed — Main wasn't looped on the dispatch itself; webhook-originated to the fixer session). New PR **#12194** "SV_Barycentrics takeover", draft, head **`ca314db538`**. **slang-reviewer 3-reviewer pass → APPROVE_WITH_NITS** (0 bugs; 3 doc/test-coverage gaps; no correctness/memory/cross-backend/capability defect across 6 reviewer agents + independent trace). Fixer applied all 3 nits + a profile-parity nit: restored a corrected doc comment on the dual-purpose recursive validator, reworded the collector comment, added a type-mismatch test (int : SV_Barycentrics → only the type error, NO spurious capability upgrade), aligned the negative-case profile. **Declined one advisory** (removing defensive null/Invalid-stage guards) — coderabbit had explicitly REQUESTED that null guard on the original #10666, so removing it would undo prior review. Verify: focused 6/6, full diagnostics 708/708; codex PLAN/CODE/OUTPUT approve. Draft CI red = benign priority-yield. **Held draft — ready-flip + close-#10584/#10666 stay with jhelferty (drafts-only guardrail).** Webhook-driven to the fixer session. Await maintainer review → ready-flip → merge. NOTE: the old #10666 human draft is presumably superseded by #12194; verify #10666's disposition (close/superseded) when #12194 lands. Do NOT relay "approved" — it's APPROVE_WITH_NITS peer (shadow), not a maintainer approval yet.

**✅ 2026-07-23 19:31 — REAL CI FULLY GREEN + maintainer moving to approve (fixer msg 58314).** Aging mechanism force-ran the yielded draft CI (suite 81375802170) → **ALL 30 jobs PASSED** (every build Linux/macOS/Windows/wasm, test-slang, rhi, sanitizer, sm80, falcor-perf, materialx, check-cmdline-ref, check-capability-atoms-ref). A `ci_failed` webhook fired but STALE: only 2 reds at head = benign `check-ci` priority-yield (earlier manual dispatch, builds skipped) + a stale `test-falcor` from the old suite (infra flake; Falcor passed fresh). No real code failure. **jhelferty-nv moving to APPROVE #12194** + doing the SV_* capability sweep as a SEPARATE follow-up issue (the plan from the fixer's audit); pinged jkwak-work for a pre-approval sanity check. **Both comments maintainer-to-maintainer, NOT addressed to bot → fixer correctly HOLDING (not injecting into their approval discussion).** Fixer posted the 12-semantic capability-gap checklist on #12194 (cmt 5062004715) + offered to take the follow-up PR pending scope confirm; did NOT auto-implement (maintainer-owned). Drafts-only still binds the bot (not jhelferty) — his flip/approve/merge. Webhook-driven. **Next:** jhelferty approve → ready-flip → merge; then #10584/#10666 disposition + the SV_* sweep follow-up (bot offered, maintainer-scoped).

**⚠️ 2026-07-24 19:17 — ARCHITECT `changes_requested`; scope expands to an EntryPoint-capability refactor (fixer msg 60264).** Architect **tangent-vector** left `changes_requested` on #12194 with a clear architectural directive (NOT a mere edit list — fixer read the body, per [[feedback_changes_requested_read_body]]): the local-join approach is directionally right (don't mutate the FuncDecl's caps; compute at the entry-point boundary) but INCOMPLETE. Correct model: (1) leave `FuncDecl::inferredCapabilityRequirements` as phase-1-computed; (2) compute the entry point's inferred caps in phase 2 (as fixer does) but **STORE that set on the `EntryPoint` object as the source of truth** — not just for the local diagnostic; (3) make downstream entry-point consumers read the EntryPoint's caps, not the FuncDecl's. **This is a real refactor** — `EntryPoint` currently has NO capability field; adds one + migrates downstream consumers (target/profile check, late IR require-capability, reflection), well beyond the barycentrics one-liner. Fixer acked on-thread (r3647645572), confirmed understanding, and **surfaced a scope question to the architect**: plumb the EntryPoint-capability field in THIS PR or split to a follow-up (defaulting to here). **In progress:** auditing downstream capability consumers to find exactly which read the FuncDecl set for an entry point → complete migration, not half. Non-draft; fixer won't manual-dispatch CI; webhook-driven. **So the earlier "jhelferty moving to approve" is SUPERSEDED** — a second reviewer (architect tangent-vector) requested a deeper change; the SV_* sweep follow-up + jhelferty approve now sit behind this refactor. Chain OPEN, fixer reworking. Do NOT relay "approving/near-merge" — changes_requested is the current state.

**✅ 2026-07-24 19:55 — architect's refactor IMPLEMENTED + pushed @`4a0e6eba78` (fixer msg 60272).** tangent-vector's directive done: entry-point inferred caps now stored on the `EntryPoint` object (new `m_inferredCapabilityRequirements` field + getter/setter, finalized in `validateEntryPoint`); `FuncDecl`'s stage-agnostic set left untouched. **Migrated BOTH downstream entry-point consumers** in `slang-lower-to-ir.cpp` — the `[Shader64BitIndexing]` decoration + the per-entry-point require-capability version-atom loop — each with a FuncDecl fallback. **codex caught the 2nd consumer the fixer initially missed** → migration now complete (not half). Verified @`4a0e6eba78`: barycentrics + all 6 `shader-64bit-indexing` pass; full `tests/diagnostics`+`tests/spirv` **1240/1240**; codex CODE/PLAN/OUTPUT approve. Replied to architect (r3647776401); jkwak's 3 threads answered earlier (SLANG_ASSERT + no-default-param applied; layer question defended then superseded by tangent-vector's cleaner model). Kept scratch design/reply .md out of the commit (staged explicit source paths only). **State:** non-draft, `reviewDecision: CHANGES_REQUESTED` (tangent-vector's — the push does NOT dismiss it; awaiting his re-review). Correct: push ≠ approval-dismissal for a changes_requested state; fixer won't flip ready/merge. **Next:** tangent-vector re-review → (if satisfied) approve → jhelferty ready/merge; then SV_* sweep follow-up. Webhook-driven. Chain OPEN, fixer owns.

**➡️ 2026-07-24 18:19 — PR #12194 flipped NON-DRAFT by maintainer; jkwak review round addressed (fixer msg 60260/60262).** Maintainer (his call — drafts-only binds the bot, not him) flipped #12194 non-draft; real `pull_request` CI now running. **jkwak-work posted a 3-finding review, all handled at head `5b5ab65eb9`** (was ca314db538): (1) `SLANG_ASSERT(outCaps)` replacing `if(!outCaps)return`; (2) removed `=nullptr` default on `outInferredCaps` params, explicit at all call sites (fixer saved the no-default-param rule to memory — jkwak notes he repeats it often); (3) answered "is the local `entryPointInferredCaps` join a workaround?" with an evidence-backed defense (generic-struct requirements already join into the same local set at the entry-point boundary, not written to the decl; accessor match needs the entry-point stage while the decl's caps are frozen in the earlier CapabilityChecked phase) → posted reply r3647333012 + strengthened the code comment, offered a broader refactor as a follow-up left to jkwak. Tests: focused 6/6, diagnostics 708/708; codex CODE/PLAN/OUTPUT re-approved. **reviewDecision=REVIEW_REQUIRED (no approval at head → push dismissed nothing).** Fixer self-corrected a reflexive manual `gh workflow run` on the now-non-draft PR (cosmetic priority-yield red, suite 81615633153, NOT a real failure) — logged "re-check isDraft before dispatch"; the real `pull_request` run is the signal. **Blocker: none. Webhook-driven** — awaiting jkwak's layer-question reply, real CI, or approval. Future push will first check reviewDecision/approval-at-head (never-push-after-approval). Fixer HOLDING correctly; no Main action.

**SV_* audit follow-up (07-23 18:34, fixer msg 58308/58310).** jhelferty-nv @-mentioned on #12194 asking to audit other SV_* semantics for the same silent-missing-capability gap. Fixer ran EMPIRICAL audit (compiled minimal shader per candidate under weak profile + read spirv-asm, cross-checked slang-capabilities.capdef), posted report as comment **5062004715** with tracking checklist. Result: **all 12** candidates (InnerCoverage, StencilRef, SampleIndex, VulkanSamplePosition, ViewID, ShadingRate, FragSize, FragInvocationCount, DeviceIndex, DrawIndex, ViewportArrayIndex, RenderTargetArrayIndex) silently compile under weak profile yet emit a SPIR-V capability beyond `Shader` unenforced by front-end — same class as SV_Barycentrics; **none currently upgrades**. No bulk changes (jhelferty said not to; #12194 stays barycentrics-only). Only SV_DeviceIndex has a ready compound alias (`GL_EXT_device_group`); rest need new capdef alias authored; SV_RenderTargetArrayIndex is version-conditional (`ShaderLayer`/`Geometry`) design call. Fixer OFFERED dedicated follow-up PR + asked scope (one PR vs split) — **HOLDING for jhelferty direction, not auto-implementing** (maintainer-owned). codex OUTPUT_REVIEW gate caught+fixed a ShaderLayer SPIR-V-version error pre-post. Both #12194 review + audit-scope answer are webhook-driven to the fixer session.
