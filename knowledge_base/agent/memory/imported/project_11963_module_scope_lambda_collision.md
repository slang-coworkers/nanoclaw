---
name: project_11963_module_scope_lambda_collision
description: "IN-FLIGHT/STALLED — slang#11963 module-scope lambda name collision (_slang_Lambda_ bare name, no counter). Root-caused, fix in DRAFT PR #11965 with 0 reviews for a month. 08-06: maintainer jkwak-work reassigned to zangold-nv 'to triage' — draft invisibility is the failure mode."
metadata: 
  node_type: memory
  type: project
  originSessionId: 8b31b10e-fa48-4afa-bc9a-17cecc3d48d3
---

**shader-slang/slang#11963** — "Attribute with an IFunc member can not be declared more than once." Filed by ShiinaManatsu (external), v2026.12.

**Bug (verified, not attribute-specific):** any two lambdas at MODULE/global scope collide on the shared synthesized struct name `_slang_Lambda_` → `error[E30200]` conflicting declaration. The `[When(()=>true)]` user-attribute is incidental; minimal repro `IFunc<bool> g1=()=>true; IFunc<bool> g2=()=>true;` also fails. Reproduced on TOT `55b2530c6`, GPU-free, deterministic.

**Root cause:** `SemanticsExprVisitor::visitLambdaExpr`, `source/slang/slang-check-expr.cpp:7854-7867`. Func-scope branch (`m_parentFunc` set) appends `<funcName>_<counter>` disambiguator; global-scope `else` branch adds the `LambdaDecl` to the module container with the BARE name — no counter. Name-keyed redeclaration check (`slang-check-decl.cpp:13727/13765`) then flags the collision.

**Classification:** Bug / medium / frontend (semantic checker) / P2.

**Chain state (2026-07-07):** slang-triager reproduced, root-caused, posted verified verdict to GitHub (comment 4900977721), set `reproduced` label + Issue Type=Bug, and **handed off to slang-fixer** (peer-wired) on canonical thread `gh-issue-shader-slang/slang-11963` with memo `triage-11963.md`. Recommended **Approach A**: append a counter in the global-scope branch, mirroring the func-scope path (smallest change, fixes the producer). Approaches B (dedicated monotonic counter) / C (exempt synthesized decls from redecl check) documented as alternatives.

**FIX LANDED (07-07 ~07:38):** slang-fixer implemented Approach A — draft **PR #11965** (https://github.com/shader-slang/slang/pull/11965), labeled `pr: non-breaking`, 2 files +23/−0. Global-scope branch of `visitLambdaExpr` now appends `getDirectMemberDeclCount()` to `_slang_Lambda_`. New test `tests/language-feature/lambda/lambda-global-scope.slang` (two module-scope lambdas). Revert-drill verified (revert → E30200 reproduces); lambda suite 24/24; formatting clean; codex PLAN/CODE/OUTPUT all approve. Fixer dispatched slang-reviewer (Mode: pr) — a2a edge not live, routed via nanoclaw send. CI: manual workflow_dispatch run RED = benign priority-yield (only wait-for-human-priority + check-ci fail, builds skipped); `retry-yielded-bot-ci` reruns for real signal — see [[project_bot_pr_priority_yield_red_run]].

**Main's role:** do NOT double-dispatch to fixer (triager owns the peer-wire). 07-07 items both CLOSED: (a) triager refreshed GitHub verdict comment 4900977721 in place to reference draft #11965 (draft-held observability [MUST] satisfied); (b) `report_pr_created({repo, 11965})` CONFIRMED fired + proven live (a `github.ci_failed` webhook for #11965 already routed into the fixer's session — that's how the priority-yield was triaged). Awaiting reviewer verdict + real CI, both of which the fixer owns on the branch via webhook. Ready-flip + merge remain operator-gated; NEVER auto-close. See [[feedback_no_double_dispatch_peer_wired]] [[feedback_verify_report_pr_created]] [[feedback_drafts_only_guardrail]] [[feedback_github_comment_hygiene]].

**CHAIN RE-OPENED 2026-08-06 00:50Z** — webhook `github.issue_comment` on #11963: **jkwak-work (MEMBER)**: *"Assigning to @zangold-nv to triage."* Assignee on the ISSUE is now `zangold-nv`. Measured at re-open:
- PR **#11965 still `draft: true`**, `merged_at: null`, **reviews = `[]` (ZERO)**, issue+review comments = **0**, `updated_at` still 2026-07-07T07:36:45Z — i.e. **30 days with no review activity of any kind** despite `requested_reviewers: [expipiplus1, csyonghe]` and `assignees: [expipiplus1]`.
- Issue #11963 has label `reproduced`, milestone **Q4 2026 (Fall)**, and my triage comment 4900977721 naming the root cause + draft #11965 sits as the FIRST comment.

⇒ ⭐⭐ **The maintainer asking for triage on an already-triaged, already-root-caused, already-patched issue is the observable cost of holding the fix as a DRAFT.** A draft PR does not appear in review queues, its `Closes #11963` does not surface on the issue timeline prominently, and requested reviewers get no actionable signal — so from a maintainer's view the issue looks untouched. The 5-bullet issue comment (which the [MUST] draft-observability rule required, and which WAS posted) was necessary but **not sufficient** to prevent re-triage a month later.

**Routing decision (Main, 08-06):** forwarded to **slang-triager** on canonical thread `gh-issue-shader-slang/slang-11963` — triager owns this chain's peer-wire to slang-fixer (do NOT double-dispatch to fixer). Ask: reply to zangold-nv on GitHub pointing at the existing root cause + draft #11965, and state plainly that the fix is ready and awaiting a ready-flip/review decision. **Ready-flip and merge stay operator-gated — the bot must not flip the draft itself.**

**TRIAGER CLOSED THE LOOP (08-06 01:10Z) — I re-verified every load-bearing claim myself with `gh` (the `mcp__slang-mcp__*` tools went missing mid-session; fell back to `gh api`):**
- **cmt 5199246567** exists, `nv-slang-bot[bot]`, 3128 chars, issue comments **2→3** (fresh comment, NOT an edit — correct call: last commenter was human jkwak-work, so an edit would have notified nobody). July verdict 4900977721 `updated_at` still 2026-07-07T07:40:28Z ⇒ **not re-edited**, as claimed.
- **PR #11965 unmutated:** `draft=true`, `merged=false`, `state=open`, `updated_at` still frozen 2026-07-07T07:36:45Z, **reviews=0**. Triager did not flip ready and did not merge. ✅
- ⭐⭐ **Triager supplied a STRONGER instrument than my "0 reviews" inference, and I confirmed it:** PR timeline has **2 `review_requested`** (both by `jhelferty-nv`, for csyonghe + expipiplus1, at 2026-07-07T07:36:45Z) and **ZERO `ready_for_review`**. Requesting a reviewer on a *draft* never queues it ⇒ reviewers were "requested" with nobody notified in a way that acts. That is structural, not an attention guess.
- Repro **still fails on current master**, per triager's two-state differential on identically-built binaries (pristine ⇒ exit 255/E30200 on all 3 shapes incl. OP's original attribute form; +patch ⇒ exit 0; one-lambda CONTROL ⇒ 0 in both). It rebuilt pristine *after* restoring, so "still fails" does not rest on the patched build. Root cause unchanged; **only line numbers drifted** — `visitLambdaExpr` `:7854`→**`:7904`** (block **`:7916-7928`**), `checkForRedeclaration` `:13765`→**`:13842`**. ⚠️ The line cites in this leaf's body above are the STALE July ones; trust the drifted values.
- Patch applies clean: `behind 193 / ahead 1, diverged, NOT conflicting`; `merge-tree` clean **with a guilty control** (synthetic same-line conflict ⇒ exit 1 + CONFLICT, so the clean result is meaningful); suffix confirmed landing in the **`else`** branch of the merged tree. **Rebase before merge** so CI runs against current master, not a 193-commit-stale base.

**RESUME PATH ARMED (08-06 01:17Z) — `i11963-draft-gate-ecaa`, `*/30 * * * *`, script-gated `/workspace/agent/guard-11965.sh`.** This chain's defect was a gate with **no trigger of its own**; the 5-bullet was posted on time and did NOT prevent re-triage. Guard baselines at cmt 5199246567 and wakes only on a real transition: ready-flip · MERGED · close · `reviews>0` · any **non-bot** comment newer than baseline (bot filter is load-bearing — else it wakes on its own output). ⭐**Validated with a GUILTY CONTROL**, not just a clean baseline: baseline rewound to 4900977721 ⇒ `{"wakeAgent":true,"why":"issue-comment-from:jkwak-work"}`; at true baseline ⇒ `wakeAgent:false`. A script that only ever prints `false` is indistinguishable from a broken one.

⇒ Generalizable lesson shared via `append_learning`: **a public footprint makes work AUDITABLE, not ROUTED — different jobs.** On a draft-held chain the 5-bullet is necessary and not sufficient; expect re-triage, and escalate the HOLD ITSELF to the operator as a decision rather than as status. See [[feedback_a_gate_on_someone_elses_reply_needs_its_own_resume_path]] [[feedback_a_gate_on_someone_elses_reply_needs_its_own_resume_path]].

**Still open / operator-gated:** ready-flip of #11965. Asked orchestrator-dashboard 08-06; **no answer yet** — the guard covers the maintainer-side path meanwhile.
