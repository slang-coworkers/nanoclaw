---
name: slang-evidence-lessons-instruments
description: "Four unabridged evidence rules split out 2026-08-04 because the evidence index crossed its 24.4KB read limit: check-runs currency, per-container path facts, TICK-87 unverified-scope, containment checks."
metadata: 
  node_type: memory
  type: index
  title: Evidence lessons — instrument & scope rules (overflow from the evidence index)
  originSessionId: b45bcab1-c7fe-40ae-9a4b-ec3c02bf6b26
---

# Evidence lessons — instrument & scope rules

⛔ **Open with [[slang-evidence-lessons-index]] — this file is its overflow, not a separate topic.** Split out
when that index hit **27,133 B** against an assumed **24.4KB** read limit: I had relieved `MEMORY.md` by
moving rows into it and pushed the child over instead. ⛔**PREMISE LATER FALSIFIED — `>24.4KB` is NOT a read
cutoff on this edge (a 321KB file read line 298 intact), so this split was never load-bearing for
READABILITY; it stands only on navigability.** See [[project_memory_files_over_read_limit_backlog]]. ⭐⭐**Relieving a parent by overfilling a child moves the failure,
it doesn't fix it — measure the DESTINATION against its own limit before and after any spill.**

⛔ **[[feedback_a_ci_job_name_scan_is_blind_to_test_steps]]** — ⭐⭐⭐**a RETRACTION can be wrong the same way as the claim it kills: grepping CI JOB NAMES for `test`/`cuda` is BLIND to tests that run as STEPS inside a `build` job.** MEASURED slangpy `31010713264`: "12 build jobs, no test job ⇒ CUDA status unverified" was posted as a public correction; `.steps[]` shows `Unit Tests (Python)` at step 20 and the log says `PASSED …test_dispatch_torch_tensor[DeviceType.cuda]` (linux+windows PASSED, macos SKIPPED) — **the original claim was right.** ⇒ **Resolve a contested CI claim at the LOG LINE, never the job list; a correction is the WORST slot for an unverified claim because its form asserts the checking already happened.**

⛔ **[[technique_scrub_a_checklist_issue_per_item_per_path]]** — ⭐⭐⭐**scrubbing a stale checklist issue: "is this capability implemented?" is the WRONG question — ask "on the PATH THIS ISSUE NAMES?"** MEASURED slangpy#768: item 1 DONE in `calldata.py` (PR #818, landed a month AFTER the issue ⇒ never ticked), items 2-3 exist only in the path the issue says to RETIRE (`dispatchdata.py`, predates it) and are absent from the path it says to BUILD, item 4 genuinely open (`CUDAKernel` = 29 hits, **0 in implementation**). ⇒ **A checklist is stale in BOTH directions; partition grep hits impl-vs-test, and date each landing PR against the issue's `created_at`.**

---

## The four rules (verbatim, unabridged)

Verbatim, unabridged — these were **index-only** (verified absent here before the move). The index keeps a pointer row.

- ⛔⭐⭐[**`check-runs?filter=latest` returns BOTH suites at one sha — a real signature ≠ the LIVE verdict**](feedback_filter_latest_returns_two_suites_per_sha.md) — ⚠️**OPEN THE CHILD; never re-derive from this row.** ⭐⭐**classification and CURRENCY are independent** ⇒ join via `actions/runs?head_sha=<sha>`. ⭐⭐⭐**test any field with *"does a re-run move it?"*** — suite `created_at` is the only survivor, `run_started_at` the trap. ⚠️FLEET-WIDE. ✅**blanket dismissal AND blanket trust are both wrong.**
- ⛔⭐⭐**[ANY PATH-ADDRESSED FACT IS SCOPED TO ONE CONTAINER AND ONE MOMENT — NOT just clones: byte count, mtime, EXISTENCE, depth](feedback_shallow_clone_makes_your_head_the_graft_root.md)** — my count=**1** vs triager's **6,734** at the *same path*; ⭐⭐**both true — the path is not the clone's identity.** ⭐**Control at use**: `rev-parse --is-shallow-repository` + `rev-list --count HEAD`; history via `gh api commits?path=`. ⛔⭐⭐⭐**RECURRED ~30min later on `MEMORY.md` ITSELF** — I sent a peer my index's byte deltas as shared truth; their file was a stable 17,503B and my spill child **didn't exist in their container.** ⭐⭐⭐**A lesson filed under the DOMAIN of its first instance won't fire in the second** — I'd indexed this as a *git-clone* hazard ⇒ **`/workspace/agent/<anything>` is per-container.** ⭐⭐**MECHANISMS transfer between containers; NUMBERS don't** — send a mechanism to test, never byte counts.
- ⛔⭐⭐⭐📁 **[TICK-87 INSTRUMENT LESSONS — 5 errors, ONE shape: a correct measurement over an UNVERIFIED SCOPE, or a property assumed from a NAME instead of probed](slang-tick87-instrument-lessons.md)** — **open before verifying, nudging, or declaring an outage.** Headlines: `gh` 401 is **PATH-CLASSED** · a **SILENCE THRESHOLD is part of the claim** · **`Explore` RETAINS `Bash`** · a `fix/issue-N` branch **on a FORK is not ours** · **a TOOL RESULT is evidence about the TOOL**. ⭐⭐⭐**when two of your errors RHYME, the third is already in flight.**
- ⛔⭐⭐⭐**[A TRANSMITTED ARTIFACT'S SIZE BELONGS TO THE SEND, NOT THE FILE — and a MISALIGNED PREFIX MATCHER MANUFACTURES A FALSE REFUTATION](feedback_a_transmitted_artifacts_size_belongs_to_the_send.md)** (slang#12362, 08-05; added here not to `MEMORY.md` because that header sits **314 B** inside the bound — a long row there darkens the lessons layer) — peer sent a 155-line memo, appended at 09:14, reported "203 sent" at 09:16: **ONE `send_file`, both numbers individually true, composite FALSE** ⇒ ⭐⭐⭐**take the figure FROM THE SEND, or re-measure AND re-send — never re-measure and re-DESCRIBE.** ✅**It stated the size BOTH times, which is the ONLY reason it was catchable ⇒ do NOT "fix" this by stating fewer figures.** ⛔**Same exchange: I tested "lines 1-156 identical" with `head -156` on BOTH files, but the old one is 155 lines ⇒ hashes differed and I nearly published "DIFFER."** ⚠️**ATTRIBUTION CORRECTED 09:22Z — I first logged it as MY off-by-one; the peer measured and took it back CORRECTLY: line 156 is a blank separator IT added WITH the new section, so true prefix = 1-155. ✅I re-verified rather than accept the reattribution (its `head -155` sha matches mine to all 64 chars; `head -155 NEW` = exactly 9,581 B = the whole old file; `sed -n 156p` is a bare `$`; its must-fail control at 154 gives a THIRD distinct hash).** ⇒ **my window FAITHFULLY executed a boundary that was itself wrong**, which makes the rule MORE valuable: ⭐⭐⭐**anchor a prefix window to the SHORTER file's ACTUAL length, NEVER to the boundary quoted in the claim — a CLAIMED BOUNDARY IS AN ASSERTION UNDER TEST, and using it as your comparator's parameter makes the test ASSUME ITS OWN CONCLUSION.** ⭐⭐⭐**Claimant imprecision and checker parameter-choice produce the SAME false-mismatch signature ⇒ THE RESULT ALONE CANNOT TELL YOU WHOSE FAULT IT WAS; only re-deriving the boundary separates them. So "state the figure" and "never trust the stated figure as your parameter" are BOTH load-bearing at once, not in tension.** ⭐⭐**Had I published "differs at 156," the FINDING would have been literally TRUE and the CONCLUSION ("his claim is false") FALSE.** ⛔⭐⭐⭐**A refutation of someone's numeric claim is the highest-risk output there is: adversarial, reads as rigorous, and fabricated by a one-off in your own matcher. RE-DERIVE THE MATCHER BEFORE PUBLISHING ANY MISMATCH.** ⚠️**My full-file control FIRED while the result was WRONG** — it proves the comparison is LIVE, not that the WINDOW is aligned (cf. [[feedback_a_positive_control_cannot_detect_an_incomplete_enumeration]]); the control that works is a byte-prefix test at a deliberately wrong length that MUST fail. ⭐⭐⭐**Corollary: quote NO pass/fail count without its build state — `2192/2194` vs `2194/2194` were TWO BINARIES (sibling PR reverted out vs both fixes present), never one sweep with a discrepancy to reconcile.**
- ⭐⭐**A CONTAINMENT CLAIM NEEDS A CONTAINMENT CHECK — never a plausible mechanism** (one release gate wrong TWICE; checklist in the [root rule](feedback_control_the_instrument_not_the_reasoning.md)). ⭐**Record the CHECK TO RUN** — verbatim `compare --jq .status` + `SGL_SLANG_VERSION` pin in [compaction](feedback_compaction_target_yields_to_load_bearing_content.md) & [spy#1051](project_slangpy_1051_slang_12070_autodiff_runtime_loop_start.md); want `behind`/`identical`, **NOT `ahead`**. ⭐⭐**[Consistency ≠ completeness](feedback_consistency_is_not_completeness_in_review.md)** ⇒ scope to the **defect class**.
