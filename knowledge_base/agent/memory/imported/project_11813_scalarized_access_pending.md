---
name: DEFERRED for maintainer team discussion — #11813 scalarized / memory-trait axis
description: slang #11813 feature request for a memory/access-trait axis; jkwak kept it OPEN and will add a label to discuss the suggestion at the maintainer team next week (~week of 2026-07-06); fixer never dispatched, awaiting team design decision
type: project
originSessionId: 060d8b0c-aa6d-4af4-8290-0c878ba98814
---

**DEFERRED for team discussion (2026-06-29/30):** despite the author conceding "fine to close," maintainer @jkwak-work decided NOT to close — he will *"add a label to discuss about the suggestion next week"* (issuecomment-4848571867), i.e. ~week of 2026-07-06. The `Scalarized<T>` wrapper / memory-trait-axis idea (option B / Kokkos-style) is now under active maintainer-team consideration. **Awaiting the team's design decision from that discussion.** No bot post (jkwak narrating his own process plan), no fixer dispatch (no decision yet), issue stays OPEN (jkwak's explicit choice — do NOT auto-close). Note: this vindicates not auto-closing on the author's earlier consent — the maintainer wanted it open.

**How to apply:** Do NOT nudge before the team discussion (~2026-07-06) — a reconvening point is set. Do NOT dispatch the fixer until a concrete maintainer decision emerges (approach + mechanism). Re-engage on: a post-discussion maintainer design decision, a related label/PR webhook, or a clear stall well past next week. Portable workaround stands: `ByteAddressBuffer.Load<float3>(12*i)`.

---

**CONVERGED → CLOSE (2026-06-29):** author j8asic clarified the `Atomic<T>` reference was just an *analogy* for the `Scalarized<float3>` wrapper idea (our option B — a wrapper type telling the compiler to emit 3× scalar loads, Kokkos-style, to keep buffers strongly-typed while controlling access layout), not a request for atomic loads. Then conceded (issuecomment-4840984283): *"If the Slang team prefers that users stick to ByteAddressBuffer for this kind of custom layout control, I am fine with that and we can go ahead and close the issue."* No open proposal remains — author accepted maintainer @jkwak-work's ByteAddressBuffer position. **Awaiting the physical close by jkwak (or the author).** Bot does NOT auto-close (standing rule) even with author consent; jkwak is active on the thread. No bot post (jkwak owns the close; author addressed "the Slang team", not the bot), no fixer dispatch. If it stalls open, a gentle maintainer nudge is a candidate — not a bot close.

---

**SPECIFIC-CASE ANSWER (2026-06-29):** maintainer @jkwak-work told the author (issuecomment-4835439835): *"as the bot said, I think you should use ByteAddressBuffer … That is exactly what `ByteAddressBuffer.Load<float3>(12*i)` does today."* He declined to add the proposed new language axis (A) / `Scalarized<T>` wrapper (B) / extend the ByteAddressBuffer cluster (C) for the float3 portability case.

**AUTHOR FOLLOW-UP / current state (2026-06-29):** j8asic accepted that solution but re-raised a broader *direction* question (issuecomment-4839935214): Slang already has `Atomic<T>` as a high-level typed load/store mechanism — why not extend that model to "tight load" and future memory traits (Kokkos-style)? Explicitly deferred the call to maintainers: *"If this is not a direction slang should take, we can close this issue."* This is a substantive design-direction point, but a pure **maintainer language-direction call** — not bot- or fixer-actionable. No bot post (bot has no standing to rule on language direction; A/B/C analysis already on the issue as the verdict comment), no fixer dispatch, issue NOT auto-closed. **Held for maintainer direction** — jkwak/szihs are active on the thread and notified.

**Live exchange (last seen 2026-06-29):** jkwak-work asked j8asic to clarify the Atomic<T> point (issuecomment-4840394936): *"can you describe your question more about atomic loads? … Do you want to have an atomic type in StructuredBuffer?"* Ball is in the **author's** court to explain the proposal — discussion is live, NOT stalled on the maintainer. No bot/coworker action; do not nudge.

**How to apply:** Do NOT dispatch the fixer or post a bot ruling. If a maintainer says "yes, take the Atomic<T>-extension direction" with a concrete design → release fixer via slang-triager on the canonical thread. If a maintainer (or the author) closes / says no → terminal, no action. If it stalls with the author's open direction question unanswered, that's a candidate for a gentle maintainer nudge, not a bot decision.
shader-slang/slang#11813 (author j8asic) — requests a typed *scalarized* access trait: keep the typed `float3` element + 12-byte tight layout but emit 3× scalar loads/stores. Proposed as a 3rd generic axis on `StructuredBuffer` orthogonal to the existing `IBufferDataLayout` axis (e.g. `StructuredBuffer<float3, ScalarDataLayout, ScalarizedAccess>`). Triaged at HEAD `51959e21f` (2026-06-29): verified NOT a bug, Issue Type set to Feature. Verdict posted: https://github.com/shader-slang/slang/issues/11813#issuecomment-4830574062. Severity medium / P3 / frontend (core-module language axis) + target-emit. **Chain closed → PARKED:** slang-fixer read the memo and concurred with the HOLD (not fixer-actionable, no PR, no build) — handoff completed, no dangling fixer session. Triage memo at /workspace/inbox/a2a-1782722699378-wq0w36/triage-11813.md.

Three approaches in the memo: (A) new generic axis, (B) `Scalarized<T>` wrapper, (C) steer into / extend the in-flight ByteAddressBuffer-alignment cluster #11545/#11591/#11592/#11593.

**Why:** Permanent language-surface decision — needs maintainer buy-in before any implementation. Load-bearing subtlety: a naive "scalarize loads only" does NOT achieve portability — the declared `ArrayStride 12` on a vec3 array forces `scalarBlockLayout` at pipeline creation regardless of load codegen. Portability requires declaring the *storage element as a scalar array* and synthesizing the typed float3 — exactly what `ByteAddressBuffer.Load<float3>` already does today.

**Maintainer engagement:** 2026-06-29 — szihs pinged @jkwak-work on the issue (issuecomment-4832334305): "Should slang support the proposed approach for accessing memory traits?" This is szihs *asking* jkwak for the design call, NOT a decision — no convergence on a direction yet. Chain stays parked, fixer still held. Did not post a bot reply (maintainers deliberating among themselves; bot wasn't addressed; triager's verdict comment already carries the A/B/C analysis).

**How to apply:** Do NOT dispatch the fixer until a maintainer chooses a direction (A vs C) — a maintainer ping/question re-opens the discussion but does NOT release the fixer (re-open ≠ release on a parked feature chain). Release only when maintainers converge on a concrete design (the approach + mechanism), not on alternative-floating. Portable workaround exists now: `ByteAddressBuffer.Load<float3>(12*i)`. Re-engage on jkwak's (or another maintainer's) actual decision, or a related PR webhook, on this canonical thread.
