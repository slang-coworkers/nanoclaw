---
name: project_12239_switch_case_nested_block_uniqueness
description: "slang#12239 — case labels nested in blocks/if escape switch uniqueness diagnostics; distinct from"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8f5bd6a1-b914-4421-a088-a8bd564cdbba
---

# slang#12239 — case labels in nested blocks escape switch uniqueness diagnostics

**Filed** 2026-07-27 by **skiminki-nv** (MEMBER; self-files + self-defers the switch-diagnostic series). bug / **medium** / **P2** / frontend semantic-checker + IR-lowering. Triaged + VERIFIED @HEAD `70462843c` (compile-path, no GPU). Verdict posted **comment 5095052154**; `reproduced` label applied.

**Two facets, both reproduced:**
- **Facet 1** — `case` nested in a `{ }` block duplicating a sibling label → duplicate `case int(0):` emitted in HLSL, **exit 0, NO Slang diagnostic** (silent wrong-code; DXC rejects the dup).
- **Facet 2** — `case` inside an `if` → **E99997 ICE** (`SLANG_UNEXPECTED "case/default not under switch"`), exit 255.

**Root cause — traversal-depth mismatch (DISTINCT root SITE from the rest of the family):**
- `validateCaseStmts` (slang-check-stmt.cpp:360-399) scans ONLY direct children of the switch body → nested dup never reaches the uniqueness HashSet (E30605 never fires).
- `lowerSwitchCases` (slang-lower-to-ir.cpp:9221-9324) RECURSES BlockStmt/SeqStmt and hoists the nested arm anyway, but does NOT unwrap `IfStmt` → case-in-if reaches `visitCaseStmtBase` (:8077-8079) → the ICE.

**Recommended fix = Approach A** — make `validateCaseStmts` recurse and REJECT any case/default not directly under the switch body with a new 306xx diagnostic; producer-side (checker) fix closes BOTH facets, matches documented rule + reporter's expected behavior. (B: implement lowering's "general case" TODO — over-scoped, rejected. C: guard the ICE assert only — band-aid, pair with A at most.)

**Status: RESUMED 2026-07-27 21:52:59Z** — maintainer **jkwak-work** commented `@nv-slang-bot Make a PR` (comment 5097218572; verified). Hold released; slang-fixer dispatched Approach A with `<github-post-authorized />`. Draft/HELD posture per drafts-only guardrail; PR to carry 5-bullet + `Fixes #12239` + `report_pr_created`. Awaiting fixer [Fix Report] + PR number.

**Fixer-plan refinements folded in:** stop recursion at nested `SwitchStmt`; proposed diag# 30606/30607 — ⚠ E30606 contention with #9999/#12238; don't touch `TargetCaseStmt`; likely `pr: breaking change`.

**PIVOT 2026-07-28 — author filed own fix; bot stood down.** skiminki-nv (author) commented `Candidate fix PR #12252. Likely a different approach than the bots` (comment 5103424710, informational, NO bot mention). Triager verified via git/gh: **NO bot PR/branch pushed to origin** — nothing to retract. **#12252** is the sole PR (OPEN, non-draft, `Fixes #12239`). Triager read full diff: author's approach is **more upstream than our Approach A** — moves placement check into the **parser** (threads `AllowCaseDefaultStatements` through `parseBlockStatement`; only switch body allows case/default), rejecting BOTH facets at parse time before malformed AST; reuses existing `CaseOutsideSwitch`/`DefaultOutsideSwitch` diagnostics (**#12238 E30606 contention now MOOT**); simplifies `visitCaseStmt`/`visitDefaultStmt` (`FindOuterStmt`→immediate-parent). 4 files (+75/-36), test `bad-case-placement.slang`. Our Approach-A work now redundant. **Chain pivots to REVIEW-ONLY:** Main greenlit LOCAL independent review of #12252 (verify triager's diff read); **NO GitHub post** (no mention, maintainer owns series). Awaiting reviewer verdict → then close (author's PR owns fix) or reconsider posting only if a real defect surfaces (operator-gated).

**LOCAL REVIEW GREENLIT 2026-07-28 11:23 (fixer msg 70762 → Main).** Fixer confirmed stand-down clean: **NO push** (`git ls-remote origin fix/issue-12239` empty; branch at master HEAD; changes are uncommitted worktree edits only — no bot PR/branch/remote for #12239). Debug build green; its Approach A verified against fresh slangc (F1 dup-case-in-block: silent→E30606; F2 case-in-if: E99997 ICE→E30606; new test `switch-case-not-directly-in-body.slang` 3/3; `tests/diagnostics` 709/709; all legal-switch suites pass — recursion doesn't false-flag legal inner switches). Approach A shape: producer-side reject in `validateCaseStmts` via recursive `diagnoseMisplacedSwitchCases`, stops at nested `SwitchStmt`/`TargetSwitchStmt`, `TargetCaseStmt` untouched; FE-only, breaking. Worktree `/workspace/agent/wt-slang-12239` preserved.
- **Main instruction:** proceed with the LOCAL review of #12252 empirically — check out #12252's branch in the preserved worktree, rebuild, run F1 + F2 + the legal-inner-switch negative control + broader legal-switch suite against #12252's slangc; use Approach A only as a **coverage-comparison baseline** (does #12252's parser-level approach close BOTH facets and not false-flag legal nested switches?). **NO GitHub post** (author skiminki owns the series, maintainer reviews; no bot mention). Report the verdict to Main. **Escalate to Main only if a REAL defect surfaces** (missed facet, legal-switch false-flag, ICE-not-eliminated) — any GitHub posting stays operator-gated. If #12252 is clean, chain closes (author's PR owns the fix).

**⚠️ DOUBLE-REVIEW DEDUP 2026-07-28 11:34 (triager msg 70768).** Triager ALSO spun up an empirical rebuild+test pass on #12252 (its codex critique already returned full APPROVE on the diff) — collided with the fixer's empirical pass I dispatched (msg 70762→my greenlight). Two slangc rebuilds + two receipts = waste. **Resolution: FIXER owns the single empirical pass** (its preserved worktree has Approach A built as a coverage baseline → strictly more informative than a fresh empirical run: can directly answer "does #12252 cover everything Approach A did"). **Triager STANDS DOWN its empirical rebuild**, contributes its codex-APPROVE verdict to Main to fold in. Main consolidates (REVIEW-ONLY, no GitHub post, author owns PR). Class: [[feedback_no_double_dispatch_peer_wired]] — two of my direct edges landed on the same task; caught + deduped before both finished.

**✅ TRIAGER VERDICT on #12252 = FULL APPROVE, no correctness defect (msg 70770, codex-critique + independent static read, both agree).** All 5 points CONFIRMED with file:line (head `d9743ffda1`):
- **Q1 parser-flag threading:** `Allow` introduced only by ordinary `switch` (parser.cpp:6589), passed only to switch-body direct children (:7224); nested `{}` drops it via default-`Disallow` `parseBlockStatement()` (:6928); every other body (if/if-let, for, while, do, catch, defer, labels) resets to `Disallow`; nested ordinary switch re-grants only to its own body. **No leak path.**
- **Q2 both facets closed at parse:** under `Disallow`, case/default emit **E39999 at parse** (:6968/:6975), errors not warnings (diagnostics.lua:4008/4015); frontend halts after parse on any error (compile-request.cpp:598) → neither F1 nor F2 reaches the old ICE (lower-to-ir.cpp:8077 SLANG_UNEXPECTED). No residual malformed-AST→lowering path. **(Corrects earlier "reuses CaseOutsideSwitch/DefaultOutsideSwitch" read — actual reused diag is E39999; no NEW diagnostic added; #12238 E30606 contention stays MOOT.)**
- **Q3 checker simplification sound:** `visitBlockStmt`/`visitSeqStmt` don't push `m_outerStmts` (:82/:121); only control/labeled stmts do → legal direct switch-body case sees SwitchStmt as immediate parent (`FindOuterStmt`→immediate-parent equivalent-for-legal, stricter-for-illegal). No regression on legal switches incl. nested legal switches + block/if/loop case bodies.
- **Q4 target/stage switch unaffected:** `__target_switch`/`__stage_switch` parse labels in own loop (:6626)→`TargetCaseStmt`, bodies default-`Disallow`; checker on separate `FindOuterStmt<TargetSwitchStmt>` (:486). "Don't touch TargetCaseStmt" holds by construction.
- **Q5 recovery/IDs safe:** `SLANG_RELEASE_ASSERT(statement)` safe (Parse{Case,Default} always non-null); illegal nodes keep invalid `targetOuterStmtID` but never reach checking/lowering (frontend halts); `TryRecover` (:7229) stable.
- **One NIT (non-blocking, author's call):** test `bad-case-placement.slang` covers F1 (nested-block case+default) but has NO dedicated F2 (case-in-`if`) line; both close through the identical parser `Disallow` path so coverage is functionally equivalent, but an explicit F2 line would document the ICE regression.
- **Bottom line (triager):** #12252 correctly + completely fixes both facets at a cleaner layer than Approach A (parser-time rejection, no new diagnostic, reuses E39999). **Approach A redundant — retire.** No GitHub post made. **Awaiting fixer's empirical "does #12252 cover everything Approach A did" to seal the consolidated receipt.**

**✅ CHAIN CLOSED 2026-07-28 11:37 — #12252 CLEAN, both reviews agree, Approach A retired (fixer msg 70772 empirical + triager msg 70770 codex/static).** Fixer rebuilt #12252 head `d9743ffda1` in its worktree (BUILD_EXIT=0) and ran against its slangc — all 3 questions YES:
- **(a) both facets closed:** F1 (dup case in `{}`) → `error[E39999] 'case' is not allowed outside a 'switch' body`, exit 255, **0 duplicate cases** (was silent double `case int(0):` exit 0); F2 (case in `if`) → same E39999, exit 255.
- **(b) ICE eliminated:** F2 no longer E99997/internal-error — clean parse-time diagnostic.
- **(c) no legal-switch false-flag:** legal nested-switch control clean; broad suite green (nested-switch, fallthrough-nested-switch, multi-level-break, compute/switch-stmt, autodiff/reverse-switch-case, enum-switch, switch-continue variants, sccp-switch-case-removal, + its own `bad-case-placement.slang`); **`tests/diagnostics` 707/707**.
- **3 adversarial edges (beyond ask, all correct):** case-in-for-loop-in-switch → E39999 (flag doesn't leak into loop bodies); case entirely outside switch → E39999 (pre-existing path intact); legal case whose body IS a `{}` block → clean compile (correctly distinguishes "block is the case body" legal vs "case nested in a block" illegal — the subtle boundary #12252 gets right).
- **Consolidated verdict:** #12252 correctly + completely fixes both facets at a cleaner, more-upstream layer (parser-time `AllowCaseDefaultStatements` flag, reuses E39999, no new diagnostic, checker simplified to immediate-outer-stmt). **Our Approach A RETIRED** (saved as local baseline patch `patches/approach-a-12239.patch`, never pushed — no remote artifact). **NO GitHub post** (author skiminki owns the series, maintainer reviews; no bot mention). **TERMINAL — #12252 owns the fix; chain closes.** Reopen only if #12252 stalls/regresses or a human raises a new point (operator-gated for any posting). One non-blocking nit noted (no dedicated F2 test line in `bad-case-placement.slang`; both facets close through the identical `Disallow` path so coverage is functionally equivalent — author's call, not surfaced).

Not a dup of [[project_9999_switch_without_cases_diagnostic_fork]].

Switch-diagnostic family (all distinct root sites): #9999 (`lowerSwitchCases` :9305 pre-label drop) · [[project_12236_switch_pre_case_unreachable]] (dedup→#9999) · [[project_12237_bool_switch_spirv_assert]] (`processSwitch`) · #12238 (condition coercion) · #12239 (this — `validateCaseStmts` scan depth + `IfStmt` unwrap gap).
