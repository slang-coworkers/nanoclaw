---
name: project_12260_enum_bool_switch_e39999
description: "#12260 switch on 'enum : bool' fails E39999 could-not-extract-value; front-end case-value extraction gap, target-independent"
metadata: 
  node_type: memory
  type: project
  originSessionId: 32aee429-ff2d-4bb4-800a-6c9dd80c6122
---

**#12260** — `switch` on `enum E : bool` fails: `error[E39999]: could not extract value from integer constant` at each `case` label. Target-independent (repro'd -target spirv/hlsl/cpp), so a **front-end/lowering** bug in `case`-value handling for `bool`-tagged enums, NOT a code-emission gap. Exit 255. Confirmed on master at `6dba5d2120`.

Filed 07-29 by nv-slang-bot[bot] at **@skiminki-nv's request** on the #12254 thread (PR #12254 comment 5115702155). This is the "ADJACENT LEAD" the fixer surfaced while reviewing the [switch-diag cluster / #12254 bool-switch fix](switch-diag cluster) — the fixer source-verified it FAILS ON MASTER independently of #12254 (which fixes *plain* `bool` switch conditions for Khronos targets; a plain `bool b; switch(b){case true:…}` now works post-#12254). The `enum : bool` case fails EARLIER, during case-value extraction — distinct root cause.

**TRIAGE COMPLETE (07-29, slang-triager):** bug / **medium / P2 / frontend** (semantic checker → constant folding). `pr: non-breaking`. Labeled `reproduced`, Type=Bug. **Verdict posted** on issue (comment **5115941279**). NOT a duplicate. Reproduced **authoritatively @HEAD `6dba5d212`** (fresh Debug slangc), target-indep (spirv/hlsl/cpp).

**Triager CORRECTED the fixer's framing (independent verify):** the two failures are genuinely distinct paths, not the same bug —
- **plain `bool`** switch → **E99997** `getIntTypeWidth` in SPIR-V *legalize* (the #12237/#12254 path, SPIR-V-only, code-emission).
- **`enum : bool`** switch (#12260) → **E39999** earlier, in the **front-end checker**, target-independent.
`enum : bool` is a supported/tested feature (tests `enum-bool-lowering.slang`, `11043`), so it SHOULD compile — not be rejected.

**Root cause (traced, subagent-confirmed):** `TypeCastIntVal::tryFoldImpl` (`slang-ast-val.cpp:2107`) — its `convertValue` lambda switch (`:2116-2146`) has **no `BaseType::Bool` case** → `default: return false` → unfolded `TypeCastIntVal` survives → `as<ConstantIntVal>` fails at `slang-check-modifier.cpp:43`, emitting E39999.

**Recommended fix — Approach A** (triager memo, 3 approaches): one-line producer-side fix in `convertValue`, add `case BaseType::Bool:` + regression test.

**⚠️ FIX-ARM CORRECTED (07-29 10:17Z) — arm is `!= 0`, NOT `& 1`.** Triager's first verdict said `resultValue = resultValue & 1` and claimed `& 1` was load-bearing / a C++ `(bool)` cast would regress. **That was backwards** — the FIXER challenged it, triager re-verified on HEAD `6dba5d212` and confirmed the fixer:
- `& 1` is NOT load-bearing for tests/bugs/`11043` — it passes at HEAD with no Bool arm at all (bool enum-tag wraparound enforced upstream in `_incrementEnumerator`, significantBits=1). Earlier "11043 needs & 1" was a **misread SIGPIPE exit code**, not a real failure.
- `convertValue` is the **SHARED** cast-to-bool fold path → `& 1` would REGRESS direct casts (`(bool)2`/`(bool)4` are `true` at HEAD and must stay `true`).
- **Correct arm: `resultValue = (resultValue != 0);`** (C bool cast, matches sibling cases; identical to `& 1` only for the {0,1} values this bug involves).
Triager PATCHED the public verdict (comment 5115941279) in place + updated memo + shared learning. Root cause / classification / severity UNCHANGED — only the one-liner's operator.

**SCOPE EXPANDED → two-layer fix (07-29 10:50Z, triager Option A + L2a):** L1-only (front-end fold) is NOT enough — triager verified at source on post-#12254 master that after L1 folds the case values, the issue's headline `-target spirv` path then *asserts* in `legalizeBoolSwitch` (an over-narrow `IRBoolLit` assert = the L2 cascade). So the fixer bundles into ONE PR:
- **L1:** add `case BaseType::Bool:` (`!= 0`) to `convertValue` in `TypeCastIntVal::tryFoldImpl` — fixes the E39999 front-end fold.
- **L2a:** widen `legalizeBoolSwitch`'s over-narrow `IRBoolLit` assert so the now-folded values pass SPIR-V legalize.
Rationale: full spirv/hlsl/cpp/cuda coverage; L1-only would leave `-target spirv` (the reported target) asserting. Fixer caught triager's `getIntVal` mis-suggestion — correct API is `as<IRConstant>->value.intVal`. Fixer building + adding a `-target spirv` regression check.

**Routing state:** triager forwarded briefing + memo to **slang-fixer** on canonical thread `gh-issue-shader-slang/slang-12260`; will forward the [Fix Report] up when it lands. Main does NOT dispatch fixer directly (triager owns that edge). Expect **DRAFT PR** under drafts-only guardrail; ready-flip/merge OP+maintainer-gated (cf. sibling #12237 held-draft → merged pattern). Bot-filed at @skiminki-nv's request off #12254 — skiminki wants it fixed. Chain stays OPEN until triager verifies + forwards resolution up.

Expected disposition per issue body: `enum : bool` switch compiles like any other enum switch (triager confirms this is the right outcome — it's a supported feature). See [[project_12237_bool_switch_spirv_assert]] (#12254) and the switch-diag cluster (#12236/#12238/#12239/#12240).

**RESUME (07-30 00:24Z, fixer confirmed to supervisor):** STILL OURS, not handed to skiminki (#12254 is the separate merged plain-bool fix). Both layers implemented + fully verified locally — L1 `BaseType::Bool` fold in `TypeCastIntVal::tryFoldImpl`; L2 widened `legalizeBoolSwitch` to accept bool-typed IntLit case labels. Tests green: repro 3/3, #12254 test 6/6, 11043 2/2, spirv/interfaces/glsl/bugs sweeps clean; codex PLAN+CODE+OUTPUT all approve. Session was torn down mid-finalization; fixer resuming — force-pushing final commit + opening DRAFT PR (drafts-only guardrail; ready-flip/merge OP+maintainer-gated). PR link = resumable artifact, incoming. Supervisor nudge tick111 resolved.

**✅ PR #12275 OPEN (draft) 07-30 00:32Z** — branch `fix/issue-12260`, `Closes #12260`, `pr: non-breaking`, head `3592824a872a`, assignee skiminki-nv, reviewers csyonghe+skiminki-nv. +68/−4, 3 files (slang-ast-val.cpp L1 + slang-ir-glsl-legalize.cpp L2 + new test `enum-bool-switch.slang`). Matches the two-layer plan exactly.

**3-reviewer verdict 07-30 01:27Z: APPROVE_WITH_NITS — 0 bugs, 1 🟡 gap** @ head 3592824a872a, diff sha 69c5084bfdac. A: 0🔴/1🟡; B/Devin clean 0/0/0; C clarity 2 advisory (FG001 med: document the compound release-assert 3-part invariant + "by role" comment/opcode-guard mismatch; C001 low: signal the `!= 0`-not-`&1` choice) — "nothing blocks merge". B+C independently corroborated the `!= 0` reasoning + dual IRBoolLit/bool-typed-IRIntLit legitimacy. Report: `/workspace/inbox/a2a-1785374868627-lq3rd1/combined-review.md`.
- 🟡 **THE GAP (A, source-verified conf-85):** L1 `BaseType::Bool` fold is TARGET-AGNOSTIC → `switch(enum:bool)` now reaches codegen on ALL backends, but only Khronos legalizes a bool switch selector→int (`slang-emit.cpp:2202-2227`); **WGSL has the same i32/u32-selector requirement + NO equivalent legalization** (its `legalizeSwitch` `slang-ir-wgsl-legalize.cpp:126-152` only synthesizes a missing `default:`). So `-target wgsl` would emit a bool-selector switch naga/wgpu reject. **Test-robustness, NOT a correctness bug in the diff.** Underlying `switch(bool)` WGSL reachability PREDATES this PR (via #12254); this PR makes the `enum:bool` FORM reach WGSL emit. Fix options: (a) generalize bool-switch legalization to WGSL, or (b) `-target wgsl` filecheck documenting actual WGSL behavior; at minimum add WGSL coverage.
- **ROUTING:** reviewer sent verdict to **PARENT ONLY** (no fixer webhook — fix-chain relay, no REQUEST_CHANGES; no `<github-post-authorized/>`, draft). So the WGSL-gap disposition is MAIN's to route. **Main relayed verdict+gap to slang-triager (chain owner, holds fixer edge) on canonical thread `gh-issue-shader-slang/slang-12260`** for the fixer to decide: close gap before ready-flip vs document-and-accept. Main did NOT dispatch fixer directly (triager owns that edge). Await triager/fixer disposition.

**✅ WGSL GAP RESOLVED via OPTION (a) — fixer generalized to WGSL 07-30 03:40Z. NEW HEAD `37ab185dbf1ea2a12a2f183aa386668c3c5a0715`** (was 3592824a872a; 7 files, +136/−11). Fixer took the thorough path (not document-and-accept): **FOUR layers now** —
- L1: `BaseType::Bool` `!= 0` fold in `TypeCastIntVal::tryFoldImpl` (slang-ast-val.cpp) — the E39999 root.
- L2: widen `legalizeBoolSwitch` case-value read for bool-typed IRIntLit (slang-ir-glsl-legalize.cpp).
- **L3 (NEW): generalize the Khronos-only pass to WGSL + repo-wide rename → `legalizeBoolSwitchForTargetsRequiringIntSwitch`** (slang-emit.cpp/.h) — closes the reviewer's WGSL gap (L1 newly let enum:bool switches reach WGSL emit + crash).
- **L4 (NEW): WGSL emitter renders bool→int cast (scalar+vector) as `select(0,1,cond)`** (slang-emit-wgsl.cpp) — codebase idiom, not base `i32(bool)`.
Tests: enum-bool-switch 4/4 (cpu+spirv-asm+wgsl+cuda-variant); NEW `tests/wgsl/bool-to-int-cast.slang` (bool-vector); #12254 6/6; 11043 2/2; sweeps wgsl 56/spirv 558/glsl 339/language-feature 2187 clean; codex re-verified. Prior reviewer language-feature segfault did NOT reproduce (2187/2187 clean rerun = flaky/env, not this change).
- ⚠️ **WGSL naga-validation NOT run locally** (no naga in-tree; FileCheck text-match ≠ target-language validation) — fixer flagged a naga-capable reviewer/CI should confirm the emitted `select` form validates.
- **ROUTING:** scope grew materially → fixer requested FULL 3-reviewer re-run on head 37ab185dbf; routed through Main (fixer lacks reliable reviewer edge). **Main relayed re-review to slang-reviewer on canonical thread @ 37ab185dbf** + informed triager the WGSL-gap disposition is already RESOLVED by fixer (option a) so triager does NOT re-dispatch (no-double-dispatch — fixer resolved before/independent of triager relay). Await re-review verdict (returns via Main → fixer). Draft stays OP/maintainer-gated.

**R2 VERDICT 07-30 04:19Z (head 37ab185dbf, diff sha 339ef6b16ceb): APPROVE_WITH_NITS — 0 bugs, 2 gaps (both non-blocking, both NEW on L3/L4 surface).** A 0🔴/2🟡; B/Devin clean 0/0 (analyzed new head, named L3/L4 + the concrete pre-fix WGSL crash `E99997: 8 bit integer value emitted`); C clarity 0-bug/5 comment-accuracy nits. A/B/C converge, zero disagreement. R1 WGSL gap CLOSED (option a), R1's 2 nits resolved in-source. Report: `/workspace/inbox/a2a-1785385164831-1rd9af/combined-review-r2.md`.
- **Main VERIFIED-CLEAN (diff-hash check, 04:19Z):** live `gh pr diff 12275` sha256 = `339ef6b16cebef53...` == report's claimed `339ef6b16ceb` EXACTLY. Head 37ab185dbf, file set = all 7 #12260 files (slang-ast-val/emit-wgsl/emit/ir-glsl-legalize.cpp+.h + 2 tests), ZERO #11917 contamination. **INTEGRITY-FAIL = verified FALSE-POSITIVE** (concurrent #11917-batch3 review `pr-20260730T035154Z` clobbered shared tmp/pr-diff.patch; Reviewer A used live `gh pr diff` = safe path, hash matches). Verdict trusted.
- **Reviewer cleared BOTH Main-flagged focus points at source:** L3 rename blast radius CLEAN (`legalizeBoolSwitchForKhronos` was exactly 3 sites; all moved; no stragglers; WGSL call correctly placed BEFORE `legalizeIRForWGSL`). L4 `select(0,1,cond)` CORRECT scalar+vector (linkage coherent: `emitCast(int,boolCond)`→kIROp_IntCast which L4 intercepts; guard matches scalar+boolN; mirrors shipping And/Or select idiom). **naga limitation confirmed: no naga in-tree → WGSL FileCheck text-matches only, does NOT run naga/wgpu → CI/human-to-confirm** (reviewer high-confidence via documented builtin overload + structural identity to shipping And/Or, but not locally proven).
- **The 2 gaps (fixer/maintainer's call before ready-flip; NOT blockers, NOT REQUEST_CHANGES):**
  1. 🟡 **Canonical-rep smell:** L2 accommodates a bool-typed `IRIntLit` at the CONSUMER rather than canonicalizing at the producer (`lowerEnumType`). C-family emitter `emitSimpleValueImpl` kIROp_IntLit handler has NO BaseType::Bool case → HLSL/Metal/CUDA `switch(enum:bool)` emits `case int8_t(1):`/`case int8_t(0):` not `case true:`. **FUNCTIONALLY CORRECT** (int8 selector compiles + right values; CPU test passes for this reason) — cosmetic/architectural per CLAUDE.md "one canonical representation," NOT a miscompile. Fix options: canonicalize in `getIntValue`/`lowerEnumType`, or add Bool arm to C-family IntLit emitter. Consumer-side approach is consistent w/ #12254's original design.
  2. 🟡 **Test coverage:** new tests only -cpu/spirv-asm/wgsl; HLSL/Metal/CUDA/GLSL case-label spelling + plain-bool WGSL switch newly-reachable but unasserted.
- **ROUTING:** reviewer sent verdict to **parent + slang-fixer DIRECTLY** (reviewer→fixer edge works; only fixer→reviewer unreliable) → loop closed by reviewer's direct edge, Main did NOT re-dispatch. Fixer holds gap disposition (its webhook path: APPROVE/0-critical → chain effectively done; gaps are discretionary). Main fed triager (chain owner) the verdict facts for merge-time footprint (naga caveat + C-family int8-case-label behavior + 2 nits). Draft stays OP/maintainer-gated.

**MAIN DECISION 07-30 04:25Z — SHIP AS-IS, HOLD NITS (fixer asked "on your word"):** fixer's triage endorsed. 0 bugs → no correctness pressure; per-push cost is a FULL 3-reviewer re-run (~$28/round + shared-tmp collision risk) so churning an approved gated draft for discretionary nits is NOT warranted unilaterally. Disposition:
- **Gap1 (canonical-rep):** → **FOLLOW-UP ISSUE, not this PR** (producer-side canonicalize in `getIntValue`/`lowerEnumType` = module-wide enum:bool blast radius, beyond #12260 scope). ⚠️ **OWED FOLLOW-UP (captured, NOT filed yet — premature pre-merge):** file after #12275 merges, or when maintainer decides. Consumer-side approach shipped is consistent w/ #12254 original design.
- **Gap2 (coverage) + C002 (comment-only):** HELD. Fixer will fold BOTH in ONE revision IFF a maintainer asks at ready-flip (Gap2 coupled to Gap1 — asserting the hlsl int8 form would BLESS the smell, so don't add that coverage until Gap1 decided). No unilateral churn.
- PR ships as-is: draft, ready-flip + merge OP/maintainer-gated (assignee skiminki-nv; reviewers csyonghe+skiminki-nv). naga verification point (L4 select) = CI/human-confirm, in triager's merge footprint. Chain otherwise TERMINAL pending human ready-flip+merge. Fixer ready to execute fold-nits path on maintainer word.

**✅ MAINTAINER APPROVED + FLIPPED READY 07-30 08:32Z (Main-VERIFIED via GitHub API):** skiminki-nv **APPROVED** PR #12275 @ `commit_id 37ab185dbf...` == current head EXACTLY (not stale/dismissed), submitted 08:32:08Z. PR now `draft:false` (maintainer flipped ready — NOT the bot; guardrail intact), `mergeable:true`, state open, `review_decision:null` (single APPROVE, no formal gate count but human approval on record). **Maintainer approved the SHIP-AS-IS decision — nits NOT required** (skiminki reviewed with all 2 gaps + 5 clarity nits present and said LGTM). Fixer correctly holding: no push (would dismiss approval), no merge (maintainer-gated). **Chain TERMINAL pending maintainer MERGE press.** Open item unchanged: WGSL `select` naga-unvalidated locally → CI/human confirm. **⚠️ Gap1 follow-up (canonical-rep, producer-side IntLit→BoolLit) now DUE SOON — file once #12275 merges** (per Main's ship-as-is decision: defer follow-up to post-merge). Triager armed for merge-time footprint refresh (naga caveat + C-family int8 case-label cosmetic).

**✅✅✅ MERGED 07-30 18:01:42Z by skiminki-nv (Main-VERIFIED via GitHub API) — CHAIN TERMINAL.** merge_commit `111f1ff71549944399e359754cefdff247e8681f`, merged head = skiminki-approved head `37ab185dbf` (approved 08:32Z at that exact SHA → clean approval-to-merge, no drift), issue #12260 auto-closed via `Closes #12260`. Full 4-layer fix shipped (L1 fold + L2 guard + L3 pass-generalize/rename `legalizeBoolSwitchForTargetsRequiringIntSwitch` + L4 WGSL select). NB: the L3 rename landing on master is what caused the concurrent [[project_11709_groupshared_byref]] merge conflict (both inserted a pass into the same WGSL target-switch spot; #11709 fixer kept both, independent, correct order).
- **🔔 Gap1 FOLLOW-UP NOW DUE (was deferred to post-merge — merge has landed):** file the canonical-rep issue — C-family emitter `emitSimpleValueImpl` kIROp_IntLit handler has no BaseType::Bool arm → HLSL/Metal/CUDA `switch(enum:bool)` emits `case int8_t(1):` not `case true:` (functionally correct, cosmetic). Producer-side canonicalize in `getIntValue`/`lowerEnumType` = module-wide enum:bool blast radius. Also worth noting the 2 accepted-declined coverage gaps (F2 non-CPU case-label spelling, plain-bool WGSL). ✅ **Gap1 follow-up ALREADY FILED as issue #12298** (per MEMORY.md index line 40 "Gap1→#12298" — filed by another session). So the deferred follow-up did NOT drop — it's tracked. No Main action owed. naga-validation of L4 select was CI/human-confirm — CI green through merge so effectively confirmed. **CHAIN FULLY TERMINAL** (merged + follow-up filed).

**⚠️ WGSL-GAP CORRECTED → CRASH, not invalid text (07-30 01:39Z, FIXER re-verified):** Fixer completed nits FG001/C001, rebased on master, and found the WGSL gap is an actual **crash E99997 "8 bit integer value emitted"**, NOT the textually-invalid-WGSL output the triager/reviewer-A framed. This makes path (b) document-the-output **infeasible** (can't document a crash). Fixer surfaced the corrected finding to the triager (bidirectional edge — triager had asked fixer to "bounce back before falling to the producer route") and recommends **path (a): wire the already-generic `legalizeBoolSwitchForKhronos` into WGSL + rename**. Fixer holding for triager's scope decision; will then rebuild, re-verify all 4 targets, re-run codex gates, force-push, refresh PR body, send updated [Fix Report]. Chain healthy (draft PR #12275 = resumable artifact; ready-flip/merge OP+maintainer-gated). Main nudged triager on canonical thread to make the scope call (deadlock guard — both sides were "holding"). Fixer tier-skipped to Main with this status; Main did NOT dispatch fixer, only nudged the chain owner (triager).

**✅ DISPOSITION RESOLVED → path (a) IN THIS PR (07-30 01:43Z, triager re-verified + flipped):** Triager traced into `emitSimpleValueImpl` (slang-emit-wgsl.cpp:1037-1057): no `BaseType::Bool` arm for `kIROp_IntLit` → default→Int8/UInt8→`SLANG_UNEXPECTED` = E99997 ICE. Confirmed fixer: it's a CRASH not invalid-text; and it's NEW not pre-existing (pre-#12254 binary: plain `switch(bool)` -target wgsl compiles exit 0; enum:bool dies E39999 in front-end, never reaches WGSL emit — L1 makes it reach + ICE). Path (b) infeasible (no output to document); shipping as-is would convert a clean front-end error into a backend ICE on WGSL. **NEW PLAN:** wire target-agnostic `legalizeBoolSwitchForKhronos` into WGSL emit block + rename off "Khronos" (~3-line wiring + rename + `-target wgsl` regression test). Triager asked fixer to VERIFY (not assert) the "also fixes pre-existing plain-bool WGSL hole" bonus — triager's run suggests plain-bool WGSL already compiled, so real delta is selector representation. SCOPE now +slang-emit.cpp + pass rename (extra file); public verdict NOT refreshed for intermediate state (no-churn).
- **MAIN'S CALL (07-30 01:43Z): RE-RUN full 3-reviewer pass on the FINAL diff** (after fixer force-pushes + [Fix Report] lands), NOT a targeted re-verify. Rationale: material scope change — new WGSL codegen legalization path + repo-wide rename of `legalizeBoolSwitchForKhronos` touching all callsites + extra file. Reviewer A (correctness) to check rename callsite completeness + WGSL wiring soundness. One pass on the finalized diff (preserves no-churn). Ready-flip/merge stays OP+maintainer-gated. Relayed to triager on canonical thread; triager holds fixer edge.

**⚠️ L3 SNAG → invalid-text, not clean (07-30 02:11Z, codex CODE_REVIEW + fixer corroborated):** The naive L3 wiring (route shared bool-switch legalize into WGSL emit) emits `emitCast(intType, boolCondition)` → WGSL renders `switch(i32(m_0))` where `m_0: bool` = **`i32(bool)`, INVALID WGSL** (spec disallows bool→numeric value-constructor; Slang's own WGSL emitter uses `select(...)` for bool-conditioned values). So naive L3 converts the enum:bool WGSL *crash* → *invalid text* — improvement, not shippable. tests/wgsl 55/55 green but those are FileCheck text-match, NOT naga-validating, so can't catch invalid-but-emitted WGSL. Fixer surfaced 2 options to triager (chain owner): **(1, fixer rec)** add a small WGSL-emitter fix so bool→int renders valid `select(0,1,cond)` (emitter already has the `select` idiom) — principled completion, ALSO fixes the pre-existing plain-`switch(bool)` invalid-text hole; **(2)** drop WGSL wiring entirely, but enum:bool WGSL stays E99997 crash (triager already said won't sign off shipping a crash). Note: L1 fold is target-agnostic (front-end, pre-target), so enum:bool WILL reach WGSL emit once L1 lands → no clean "defer WGSL" path without a WGSL-specific guard; options are effectively fix-emit vs ship-crash. Nothing pushed (remote still pre-L3 head). Fixer HOLDING for triager's scope pick; will then implement, one clean force-push, full re-verify, re-run codex gates, then [Fix Report].
- **MAIN (07-30 02:11Z):** scope-authority to triager confirmed BOTH directions (absorb the WGSL-emitter fix into this PR, OR scope WGSL to a tracked follow-up — whichever is the cleaner non-regressing path; do NOT ship a new crash). Full 3-reviewer re-pass on finalized diff stands regardless of which path grows the diff (option 1 now touches WGSL emitter cast-rendering = broader review surface, esp. that `select` change doesn't alter other bool→int WGSL emissions). Ready-flip/merge OP+maintainer-gated. Did NOT dispatch fixer (triager owns edge); did NOT reply to fixer's tier-skip (topology). Scope count: L1 fold + L2 legalizeBoolSwitch widen + L3 WGSL wiring + (opt-1) WGSL-emitter select fix.

**✅ SCOPE DECISION: option 1 / L4 (07-30 02:15Z, triager picked + relayed to fixer msg63).** Final PR = **L1** front-end fold (`BaseType::Bool` in `TypeCastIntVal::tryFoldImpl`) + **L2** widened `legalizeBoolSwitch` guard + **L3** pass-generalize `legalizeBoolSwitchForKhronos`→WGSL + rename off "Khronos" + **L4** WGSL-emitter `select(0,1,cond)` for bool→int cast. Triager source-confirmed the emitter is the correct layer: base c-like `kIROp_IntCast` (slang-emit-c-like.cpp:2437) emits `emitType(dataType)`+args → `i32(m_0)`; WGSL `tryEmitInstExprImpl` (slang-emit-wgsl.cpp:1463) has no IntCast override (only BitCast) → falls through to base type-constructor form; `select` is already the emitter's bool-conditioned idiom (:37 prelude, :1491-1513 And/Or). ~10-15 line select fix < a WGSL front-end guard, and complete not partial. **Justification correction (triager→fixer, method not decision):** PR body must NOT rest on "i32(bool) is invalid WGSL" — neither can run naga, and tests/wgsl are FileCheck text-match (why 55/55 missed it); rest instead on "`select(0,1,cond)` is the codebase's established known-correct bool→int idiom, right regardless of whether i32(bool) parses." PR body explicitly flags **naga-validation not run locally → naga-capable reviewer/CI must confirm grammar validity.** Both Main constraints carried: (1) full 3-reviewer re-pass on finalized head, Reviewer A confirms the select change doesn't alter OTHER bool→int WGSL emissions; (2) naga text-match gap noted in PR body. Fixer to: one force-push + [Fix Report] → triager triggers re-pass + relays. Ready-flip+merge OP+maintainer-gated. **HOLDING for fixer's finalized push.**

**In-flight refinements (07-30 02:41–02:59Z, fixer, pre-push):** (1) codex asked for a **vector-cast regression test** — L4 select fix extends to bool2→int2 emitting `select(vec2<i32>(0),vec2<i32>(1),boolVec)`; fixer adding a vector test + re-running WGSL sweep + codex CODE_REVIEW. (2) ⚠️ **OPEN SEGFAULT QUESTION (watch):** codex hit a segfault on a fresh `language-feature` run; fixer's earlier subagent sweep got `language-feature 2187/2187` clean on the L4 scalar binary. Fixer treating with correct caution — narrowed PR-body claims to only sweeps it can stand behind (wgsl/spirv/glsl), will re-run a **targeted language-feature check post-rebuild** to determine if the segfault reproduces / is attributable to the IntCast→select + rename change (its hypothesis: likely flaky parallel test-server crash — language-feature is mostly CPU/spirv, few WGSL, change is narrow). NOT yet confirmed regression vs flake. **If it reproduces + is attributable, it blocks finalization** — await fixer's targeted-check result in the [Fix Report].

**✅✅ CHAIN TERMINAL — MERGED 07-30 18:21Z (fixer-reported, id 78454).** skiminki-nv **MERGED PR #12275** (merge commit `111f1ff715`); issue #12260 auto-closed COMPLETED via `Closes #12260`. Final shipped shape = L1 front-end `BaseType::Bool` fold + L2 widened `legalizeBoolSwitch` guard + L3 pass-generalize→WGSL (`legalizeBoolSwitchForTargetsRequiringIntSwitch`) + L4 WGSL `select(0,1,cond)` bool→int cast (scalar+vector). Shipped SHIP-AS-IS w/ 2 discretionary nits held (maintainer approved with them present).
- **OWED FOLLOW-UP DISCHARGED:** Gap1 (canonical-rep smell — C-family emitters print `case int8_t(1)` not `case true:` for switch(enum:bool); producer-side `lowerEnumType` canonicalization vs consumer-side `BaseType::Bool` arm + coupled test coverage) filed as **issue #12298** post-merge (no longer premature). This was the tracking issue owed since the R2 ship-as-is decision.
- Worktree `wt-slang-12260` + active-work sentinel removed (base clone intact; no sibling worktrees touched).
- **WGSL naga-unvalidated point (L4 `select` form) = MOOT for #12260** — merged & on master; any naga/wgpu issue surfaces via normal CI/downstream and is captured in #12298's orbit. **Chain closed. Re-engage only on a fresh human comment on #12260 / #12275 / #12298.**
