---
name: "#11778 loop-phi uninit-diagnostic — RESOLVED by merged maintainer PR #11810"
description: New issue #11778 (loop-carried uninitialized E41016 gap) was triaged + fixer dispatched, then superseded by a maintainer's merged PR #11810; bot stood down, no bot PR
type: project
originSessionId: f83f3ba4-6d74-40cc-ac2a-ec3c702ce2ac
---
shader-slang/slang#11778 — "Uninitialized variable not diagnosed (E41016) when accumulated across a loop (loop-carried)". Bot-filed spinoff from the #11763/PR#11764 chain (yonatan's sharper loop-carried repro). Separate root cause: undefined value escapes through an SSA **loop phi**; the `loop` terminator's phi-arg use is classified `None` via the literal `// TODO: Ignore branches for now` at `slang-ir-use-uninitialized-values.cpp:262-265` — orthogonal to #11764's store-of-undef path.

**RESOLVED 2026-06-30 — superseded by MERGED #11810 (maintainer @expipiplus1).** Chain closed, no bot PR.

Timeline: parked 2026-06-26 (HOLD, low sev), later un-parked — triager re-verified at HEAD `0583a0e33` (rebuilt fresh Debug slangc; cached binary was 3wks stale), posted verified 5-bullet verdict (issuecomment-4808791418) pinning the loop-phi root cause, forwarded to slang-fixer (Approach A: propagate undefined through phi block-params, full slang-test gate). Fixer implemented loop-phi-following + `tests/diagnostics/uninitialized-loop-carried.slang`, survived a build/-only disk wipe, rebased clean onto `51959e21ff`. Before it opened a PR, maintainer @expipiplus1 commented "Fixed by #11810" — **MERGED** 2026-06-30T09:06:20Z, branch `push-tkrnwtwvwlpn`, base master. #11810 uses the **identical approach** (new `getBranchArgPhiParam` mapping branch/loop arg use → target block param; `getAliasableInstructions` follows the phi edge, guarded by a `visited` set; same new test filename `uninitialized-loop-carried.slang`; also fixes a cascading conditional case in `uninitialized-local-variables.slang`; 486/486 diagnostics pass). Line-for-line match to our triage verdict → strongly suggests the maintainer was informed by our comment 4808791418.

**Outcome / lesson:** The "if a maintainer self-assigns, stand down" trigger fired — maintainer fixed it directly. Main verified #11810 (merged, human-authored, identical approach) and dispatched a stand-down to the fixer via the triager on canonical thread `gh-issue-shader-slang/slang-11778`. Did NOT auto-close the issue (maintainer's call). Clean example of triage→maintainer-fix: our verified verdict enabled a maintainer's own merge; the fixer's parallel branch became pure duplicate work — caught before a duplicate PR was opened.
