---
name: project_11919_remove_ox_optins_parked
description: "slang#11919 remove -OX opt-ins from slang-test — Phase 1 SHIPPED draft PR #11923; Phase 2 held pending maintainer scope signal"
metadata: 
  node_type: memory
  type: project
  originSessionId: adec0fca-5d0e-4e58-9409-9de2afcda755
---

**UPDATE 2026-07-08 (PHASE 1 MERGED — TERMINAL for Phase 1):** ✅ PR #11923 MERGED to master. Verified independently: `state:MERGED`, merge commit `d8e8e1a9e7bf3f766958760e646cdd366bbd917e`, merged by **pdeayton-nv** 18:43:59Z (maintainer-performed — bot did NOT self-merge, guardrail held). 109 `-O0` removals / 92 files; kept `-O0` on 11 maintainer-named files (5 `CHECK_O0` + 6 documented-condition), byte-identical to master. **#11919 stays OPEN** (verified) — `Addresses` not `Closes`; now tracks **Phase 2 ONLY** (opt-sensitive `-O1/-O2/-O3` tail, ~93 lines/65 files), HELD pending maintainer direction (mostly keep-with-doc since #11805 deliberately added those opt-ins; do NOT dispatch fixer speculatively). Fixer cleaned merged worktree + stood down. Fleet-disk closed (52%/115G). Journey: triaged→parked on #11805→auto-resumed on merge→re-surveyed (found #11805 ADDED 61 opt-ins)→draft→2 maintainer rounds→flaky-CI rerun green→approved→merged, every gated action by maintainer. Chain reopens ONLY on Phase-2 greenlight or fresh human comment.

**UPDATE 2026-07-08 (CI GREEN, all merge gates satisfied):** Transient CI red on #11923 (SIGSEGV exit-139 linux-cpu + exit-255 macos on `tests/library`/`gfx-unit-test`/macOS paths the 92-file directive-only diff never touches; merge-base `33f9ed0cea` green; same jobs flaking on ≥2 unrelated PRs) = infra flake, cleared by no-code rerun. Verified independently at head `a8e1a821ec`: check-runs rollup 29 success / 1 skipped, ZERO failing/cancelled/timed-out; `mergeable:MERGEABLE`, reviewDecision:APPROVED, isDraft:false, mergeStateStatus:BLOCKED (= awaiting merge, not a failing gate). Every merge gate satisfied (approved + non-draft + CI-green + accurate desc). **Sole remaining step = maintainer MERGE** (operator-gated, bot won't merge). Chain reopens on merge / maintainer comment / Phase-2 greenlight.

**UPDATE 2026-07-08 (APPROVED + ready):** #11923 flipped ready-for-review + maintainer-APPROVED. Verified who-flipped independently (guardrail): `ready_for_review` actor = **pdeayton-nv** (04:48:07Z) — MAINTAINER-driven, NOT bot self-flip → non-draft is not a breach (drafts-only exception: verify who). Sole review = pdeayton-nv APPROVED (4650990206, 04:48:27Z). Live: OPEN / isDraft:false / reviewDecision:APPROVED / mergeStateStatus:BLOCKED, head `a8e1a821ec`, +109/−109 / 92 files, `Addresses #11919` (not Closes — Phase 2 remains). Fixer rewrote PR description to accurate rev-2 scope (109/92 + kept-`-O0` note) at pdeayton's request; codex approved. Rests at **awaiting maintainer MERGE** (BLOCKED pending merge/CI — maintainer action, bot won't merge). Ready-flip MOOTS local-re-verify for #11923 (real-infra CI on ready PR supersedes blocked local run). **DISK RESOLVED 07-08:** re-escalation task-1783486133371-ndv3vg fired 12:46Z + self-completed (guard saw recovered volume, no wake); `/dev/vdb` now 52% / 115G free (operator freed host space — amber card worked). See [[project_fleet_disk_capacity_wall_11969]] (same incident, RESOLVED). No armed task lingering; fleet builds unblocked. Phase 2 still held pending maintainer direction. Issue trail comment 4870747030 refreshed. Chain reopens on merge or Phase-2 greenlight.

**UPDATE 2026-07-08 (rev 2):** Maintainer @pdeayton-nv reviewed draft #11923; fixer addressed + rebased. Triager independently verified against live GitHub: head `fix/issue-11919 @ a8e1a821ec`, base master, **+109/−109 / 92 files** (was 121/103 — 12 `-O0` restored to keep `CHECK_O0` tests symmetric with `CHECK_OPT` + 6 named comment-called-out files; all 9 named files byte-identical to master). Rebase clean (master `33f9ed0cea` ancestor, 0 behind). codex 3-stage approves rev 2. `report_pr_created` mapping CONFIRMED live (a `ci_failed` webhook for head 36319f33cb already routed into fixer session, classified benign draft priority-yield). Draft CI = all `skipping` (expected; builds run only on ready-flip/merge, operator-gated). **DISK BLOCKER:** fixer's local 92-test re-run blocked — shared `/dev/vdb` 251G overlay (backs fixer+triager workspaces) is 100% full / 32M free, host-wide not one worktree. Main's own workspace on separate `/dev/vda1` (12G free) — can't free coworkers' volume from mine. Local re-verify is nice-to-have NOT blocking: rev-1 157/157 stands + rev-2 delta is byte-identical-to-master restorations + clean rebase → GitHub CI on ready-flip is authoritative. Fixer correctly did NOT reap sibling `wt-*` (isolation). Escalated fleet-disk to operator (amber card 04:46 UTC — TIMED OUT, operator away). Merged-worktree reap grant EXECUTED + EXHAUSTED: only `wt-rhi-cmdquery` (slang-rhi #775 merged 06-16) reapable, freed ~8M; audit = 1 of 46 (all others open/draft/no-PR/dirty, rails held). `/dev/vdb` stays 100% — mass is in-flight `build/` dirs neither Main nor fixers may reap. Host resize / operator-approved build-dir GC is the SOLE remaining lever. Armed guarded one-shot re-escalation (12:46 UTC, fires only if `/dev/vdb` still ≥95%; guard reads overlay at `/` in my container). Next: maintainer ready-flip/merge #11923 (gated) + free `/dev/vdb`. Related: [[project_fleet_build_thundering_herd]], [[feedback_always_reap_merged_worktrees]].

**UPDATE 2026-07-03:** #11805 MERGED 00:27:54Z → auto-resume gate task-1783029120180-i4oxwt fired once + self-cancelled (verified gone from list_tasks). Triager re-surveyed at post-merge HEAD 99c3b77bd: #11805 *added* 61 opt-in lines (0 removed), so redundant-`-O0` held at 118 while opt-sensitive `-O1/-O2/-O3` tail nearly tripled (~32→93 lines/65 files). **Phase 1 SHIPPED** as draft PR #11923 (nv-slang-bot, `fix/issue-11919 @ 36319f33cb`, base master, +121/−121 / 103 files, `Addresses #11919` NOT Closes, `pr: non-breaking`): dropped explicit `-O0` from 121 directives (112 bare, 8 `-Xslang -O0` pairs, 1 preserving `-g`); all `-O1/-O2/-O3` left for Phase 2. Verified 157/157 PASS on merged master (GPU present, `-vk` render forms ran) → output-neutral confirmed. codex PLAN/CODE/OUTPUT approve; Reviewer-A pipeline declined as disproportionate for directive-only mechanical change (I concur). Issue comment 4870747030 refreshed for the trail. **Phase 2 HELD** (~93 lines/65 files): 2b core is 56 output-pinned (44 freshly added by #11805) = mostly keep-with-doc not removal; recommend maintainer signal on how far to push "remove all" before fixer effort; 2a exec-test drops (37, minus `performance-profile.slang`) = safe optional P3. Next human action: maintainer review + ready-flip/merge of #11923 (operator-gated). Original parked-state context below.

---

shader-slang/slang#11919 (bot-filed follow-up, @jkwak-work requested off PR #11805): remove ALL explicit `-O0..-O3` / `-Xslang -OX` / `-compile-arg -OX` opt-ins from `tests/**`, relying on slang-test's new `-O0` default.

**HARD BLOCKER — PR #11805 ("Default slang-test compiler invocations to -O0", Fixes #11804) is OPEN, not merged** (APPROVED, mergeable_state=blocked, head 3e6ebdf1, branch disable-spv-opt-in-slang-test). The `-O0`-default machinery (`tools/slang-test/slang-test-optimization-options.h`) is ABSENT at master HEAD 973274da9. Dropping explicit `-O0` before #11805 lands makes tests run at slangc default (spirv-opt ON) → breaks expected output. So removal work is strictly gated on #11805 merging first.

Triaged 2026-07-02, PARKED at triaged (NOT dispatched to fixer — dispatching into a hard blocker = dangling session or regressing PR; precedent #11903/#11519). Verified verdict posted: issuecomment-4870747030. Memo: inbox/msg-1783028971492-ietwwl/triage-11919.md.

Survey at HEAD: 119 files / 150 directive lines (`-O0`×118, `-O1`×5, `-O2`×11, `-O3`×16). Bucket 1 (~118 redundant `-O0`, 111 direct `-target spirv … -O0` FileCheck SIMPLE + 7 `-Xslang -O0` render forms) = clean bulk removal, output-neutral, GPU-free verify → Phase 1 single PR. Bucket 2 (~32 opt-sensitive `-O1/-O2/-O3`) = P3 tail; 2a `-compile-arg` COMPARE_COMPUTE_EX likely drop, 2b deliberate optimized-output (`debug-struct-member-values*`, `gh-9263`, `abort-after-switch`, cuda copy-elision) keep+document/rework, KEEP `performance-profile.slang` → Phase 2 separate PR.

**Auto-resume:** scheduled task task-1783029120180-i4oxwt (4-hourly, guard polls #11805 `.merged`) wakes the chain when #11805 merges → re-survey at new HEAD (#11805 edits ~42 files) then dispatch fixer with phased plan; task self-cancels after firing. Do NOT dispatch fixer before then. Related: [[project_autoroute_hook_pressures_parks]] (defend the park against auto-route-hook nudges).
