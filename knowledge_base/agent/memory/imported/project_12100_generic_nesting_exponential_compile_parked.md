---
name: project_12100_generic_nesting_exponential_compile_parked
description: "#12100 exponential compile time w/ generic nesting depth + >80x regression — CLOSED 08-05 by assignee (fixed by #12106); 2 sub-findings ORPHANED, see bottom"
metadata: 
  node_type: memory
  type: project
  originSessionId: 23ce319d-851b-45f8-a8ca-ea8189e575ec
---

shader-slang/slang#12100 — "Exponential compile time with generic nesting depth; expression-built nested generics regressed >80x since v2026.5". Reporter/assignee: jvepsalainen-nv (NVIDIA CONTRIBUTOR, self-assigned).

**REPRODUCED** by slang-triager on ToT `3eeda847c` (debug). Labeled `reproduced`+`regression`, Type=Performance. Verdict posted to GitHub (comment 4972584693) 2026-07-14.

Two-part perf bug:
1. **Exponential compile (~2-3x/level) in ALL versions** — missing memoization on substitution/conformance path. ResolveInvoke→inferGenericArguments→trySolveGenericArguments→buildSubstDeclRef re-descends the whole `Wrapper<Wrapper<…>>` tree per level; DeclRefType/GenericAppDeclRef substitution uncached, interning pointer-keyed so identical substituted types miss the subtype-witness cache. Fix = high-blast-radius core substitution refactor (Track 1).
2. **>80x v2026.5→v2026.8 regression** — HYPOTHESIS: #9808 autodiff refactor (45ccce9a3, verified in-window) from diff size + commit bodies, NOT a profiled bisect.

Repro (debug): depth 8≈3.1s→12≈11.7s→14≈72s→16 timeout; plain `f(f(…))` → E39997 at ~200 deep. Related shapes (Pair typealias chain, assoc-type chain) exponential back to v2025.14, no recent regression.

**DECISION (Main, 2026-07-14): PARKED — HOLD-for-assignee.** NO fixer dispatched. Mirrors #12096 park for same reporter: self-assigned + owns the #12086 compile-perf workstream this feeds; core-substitution fix too high-blast-radius for independent fixer. Triager kept branch/verdict; re-engage only if assignee opens a PR to review or explicitly asks for help. See [[project_12096_metal4_oscap_macos26_rhi795]] pattern, [[feedback_deadpromise_check_assignee_before_rewake]].

Tracking counterpart: #12086 (this reporter's compile-perf sweep PR) carries `generic_nesting`/`generic_nesting_eval`/`interface_depth` microbenchmarks — a fix collapses their top-rung times. See [[project_12082_compileperf_readme_superseded_12086]].

**UPDATE 2026-07-14 (comment 4972718926, jvepsalainen-nv [Agent] survey):** assignee actively driving — posted follow-up survey with 3 more findings. Chain STAYS parked (their own work). New deltas beyond original memo:
- Balanced-tree `typealias T_i = Pair<T_{i-1}, T_{i-1}>` (DAG, only `depth` unique types) still exponential (depth12=5.9s, depth16 timeout) — cleanest memoization canary; long-standing (not a regression).
- **NEW regression window v2026.12→v2026.13**: conditional conformance via `extension<F,S> Pair<F,S> : IExtra`, calling extension-provided method through depth-16 tower: 0.25s→1.67s→2.28s(master). Mechanism = extension-provided inheritance witness. Repro `cond-16.slang` in the comment.
- **Second ongoing regression post-v2026.13** on inference path: operator-built `Sum<A,B>` chain + the `wrap` repro both ~doubled after v2026.13.
- Clean: value-generic `let N`, built-in `Tuple`, nested init-lists.
Forwarded to triager as record-only (no re-triage, no GitHub post — assignee posts their own). Reviewer picture for eventual PR now includes 3 regression windows, not 1.

**CLOSED 2026-08-05 10:06Z by jvepsalainen-nv** (comment 5190409281): *"This issue is fixed with #12106. Compile time of this case is tracked in `shader-slang.org/slang-compile-perf/workloads/generic_nesting.html`. Closing issue."* `state_reason=completed`. Park ends — the assignee resolved it himself, exactly the RESUME condition. #12106 = saipraveenb25's "Memoize shared Val and type DAG traversals" (merged 07-16), i.e. Track 1 (the missing-memoization root cause) landed.

⚠️**THE CLOSE COVERS ONE OF THREE SHAPES — verified against the live manifest, not inferred.** The cited tracking page is `generic_nesting`, the **typealias-declared chain**. `tools/compile-perf/lib/manifest.py` `WORKLOADS` (single registration site, checked: no `+=`/`append`/`extend` anywhere in `lib/`) carries exactly 3 depth workloads — `generic_nesting`, `generic_nesting_eval`, `interface_depth` — and **none of them is the inference-built `wrap(wrap(...))` shape this issue's TITLE is about** (the >80x v2026.5→v2026.8 regression). Confirmed by reading the generators in `lib/workloads.py:446-533`: all three build the chain by `typealias`, none calls a generic function to infer the nesting.
⇒ Two findings from the 07-14 survey are now **ORPHANED — filed nowhere, tracked by nothing:**
1. **inference-built `wrap(wrap(...))`** — the >80x window plus a further ~2x v2026.13→master (assignee's own table: depth16 v2026.5 0.36s → v2026.8 2.70s → master 4.64s).
2. **conditional conformance via `extension<F,S> Pair<F,S> : IExtra`** — the one-release v2026.12→v2026.13 window (0.25→1.67→2.28s at depth 16), a DIFFERENT mechanism (extension-provided inheritance witness), which a Val/type-DAG memoization has no obvious reason to fix.

The guard for both was proposed in **#12103** (still OPEN, self-assigned, 5 workloads: `generic_nesting_expr`, `cond_conformance_chain`, `generic_nesting_tree`, `assoc_chain`, `op_built_chain`) — **none has been implemented**: code-search across the repo returns 0 hits for every one of the 5 names, no PR references #12103, and the perf site 404s on `generic_nesting_expr.html` / `cond_conformance_chain.html` (`generic_nesting.html` = 200, so the site is up — the pages are genuinely absent, not the probe failing). ⇒ **"tracked on the perf site" is true for the closed shape and false for the two orphans.**
⭐**The lesson worth keeping: a maintainer's close names the shape THEY measured; a multi-shape issue can be legitimately closed while most of its findings go dark.** The check is mechanical — enumerate the workloads at the REGISTRATION SITE and read each generator's source, never trust a shape name to mean what it sounds like (`generic_nesting_eval` sounds like the eval/inference shape; it is the typealias chain with a method call).

**RESOLVED 08-05 — guarding-only gap, no surviving regression on the two measured shapes. Comment posted on #12103 (`5191303144`, `nv-slang-bot[bot]`, 5654 chars); #12100 NOT reopened, #12103 NOT closed.** Audited the public artifact against the log: every figure reconciles, `-O0` + three-platform caveats both disclosed, the `cond` null framed as platform/pipeline-specific rather than a refutation, and the `generic_nesting_tree` flattening explicitly labelled a prediction, not a result. 
**ALL THREE SHAPES NOW MEASURED — #12106 removed the depth dependence on every one; `op_built_chain` closed 08-05, nothing further posted.** `op-16` (generator byte-identical to the published shader): v2026.13.1 d16 **16.5s** → master d16 **0.256s**; d20 v2026.13.1 **TIMEOUT >180s** vs master 0.293s (≥614x, where the timeout IS the finding); 8 post-fix cells span 0.275–0.368s (**33.8% — do NOT reuse the 5.9% band phrasing from wrap/cond**), trend DECREASING with depth ⇒ "no depth-dependent growth" as a trend, not a band.
⛔**STORED FRAMING — do not restate as "the reported master degradation does not reproduce."** The survey's "current master" (07-14T18:40Z) is **PRE-fix**; #12106 merged 07-16T03:28Z. The honest analogue is `v2026.13.1` (pre-fix, fix 38 commits ahead), where the degradation **reproduces WORSE than reported** (16.5s vs his 8.5s). ⇒ **corroborated pre-fix, erased by #12106** — no contradiction of the maintainer. Only the *intermediate* v2026.12→v2026.13 step fails to reproduce (d12 0.512→0.482), same signature as the `cond` window null ⇒ plausibly the same platform/pipeline specificity. Full mechanism + my own 68-vs-101 error: [[feedback_a_claim_about_master_is_a_timestamp_not_a_version]].
`assoc_chain` deliberately unmeasured (long-standing, no regression window claimed).

⚠️**Superseded coverage note, kept for the reasoning trail: at dispatch time only 2 of 3 were measured.** `op_built_chain` (survey finding #3 — operator-built `Sum<A,B>` 16 deep: 0.22s v2026.5 → 4.4 v2026.12 → 4.5 v2026.13 → **8.5s master**, i.e. the claimed *post-v2026.13 second ongoing regression on the inference path*) was **NOT measured** — and it is the only one of the three whose reported signal is a degradation **in current master**, i.e. the one #12106 would not be expected to have fixed. The shader is published (`op-16.slang`, #12103 cmt `4972924157`), so this is a ~5-minute measurement on the existing harness. `assoc_chain` also unmeasured (long-standing, no regression window claimed ⇒ lower value).
⇒ ⭐⭐**"no regression survives" is true of what was measured and the comment says so, but the shape most likely to still regress is the unmeasured one.** RESUME: measure `op-16` at depths 12–24 on v2026.13.1 vs v2026.14.1 vs master; if the master doubling reproduces, that is a LIVE regression needing its own issue, not a guard gap.

**MEASURED 08-05 by slang-triager (official Linux release binaries via `tools/compile-perf/fetch_releases.py`, min-of-3, `slangc -target spirv`, depth 16, idle):**
- **`wrap` (inference-built, the >80x shape) — #12106 FIXED IT.** v2026.5 0.274s → v2026.8 **8.826s** → v2026.12.2 8.602 → v2026.13.1 8.533 → **v2026.14.1 0.273s** (exactly v2026.5 class). The harness **reproduces the known v2026.5→v2026.8 regression (32x) as a positive control**, which is what makes the collapse a result rather than a harness null. Fix boundary confirmed independently: `c8d02ae59` is **absent in v2026.13.1, present in v2026.14** ⇒ measurable in released binaries, no build needed.
- **`cond` (conditional conformance) — DOES NOT REPRODUCE ON LINUX.** Assignee measured 0.19→**1.67s** at v2026.13 (~8.8x) on macOS arm64; Linux shows **no step at that boundary at all** (v2026.5 0.376 → v2026.12 0.565 → v2026.13 0.533 → v2026.14.1 0.208). Exact **minor** tags fetched (not patch tags) to kill the obvious confound; generator byte-identical to the #12103 artifact. ⇒ **PLATFORM-DEPENDENT, NOT refuted** — and per [[technique_compile_perf_three_platforms_and_v_staleness]] the guard would run on **Windows**, where the regression's existence is unmeasured by anyone. That is an argument FOR `cond_conformance_chain`, not against the report.
- ⚠️**The two cond shaders differ trivially** (`outBuf` declaration order, `numthreads(1,1,1)` spacing) between #12100 and #12103 — I diffed them; **no semantic difference**, both call `useExtra(v)` through the depth-16 tower. Fidelity is genuine.
- **The close's own evidence is sound, and I confound-checked it rather than accepting it:** the perf page's 96.8% Jul15→16 step lands on #12106's 07-16T03:28Z merge, **no `manifest.py` change between the two data points**, and `gen_generic_nesting` **byte-identical** across the window. The runner-pool migration (#11950, 07-07) is ~8 days OUTSIDE the window ⇒ **it does not confound this step; don't hedge for it.** Note the page's history is BACKFILLED (`generic_nesting` didn't exist in the manifest before #12086 on 07-15) — dates are slang-commit dates, not measurement dates.
- **#12103's stated blocker is discharged:** it had to stack after #12086, which merged 07-15T10:22Z ⇒ unblocked, unimplemented, 0 referencing PRs.

Follow-up state (08-05, all verified live): **#12139 OPEN** — the shallow-generic tax #12106 itself introduced, [[project_12139_shallow_generic_compile_regression_12106]] · **#12103 OPEN** (guard workloads, unimplemented) · **#12112 / #12113 OPEN** · **#12106 merged/closed 07-16**. #12100 is the only one of the group now closed.
