---
name: project_10668_fvk_bind_globals_set_binding_conflict
description: "#10668 -fvk-bind-globals set!=0 descriptor binding conflict — triaged+posted, awaiting maintainer greenlight for fix"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9ce31258-bb91-4c46-bc95-7d04c1f29bca
---

**shader-slang/slang#10668** — "Binding conflict when using `-fvk-bind-globals` with set != 0"

- **State (2026-07-28): FIX AUTHORIZED → REOPENED → DISPATCHED-FIXER.** Maintainer jhelferty-nv (assignee, MEMBER) authorized via GitHub cmt 5098253562: *"Implement DXC Example 3 semantics — flag moves only $Globals; split-out resources stay in set 0 — not same-set-as-globals with an incremented binding."* This chose **Approach C** (the DXC-set-0 divergence) and **RESOLVES** mbechard's open design point. Dispatched slang-fixer on canonical thread `gh-issue-shader-slang/slang-10668` with the corrected memo attached; scope = Approach C only, DRAFT PR, merge operator-gated.
- ⚠️ **Approach C SUPERSEDES the earlier Approach A relative-offset fix.** Do NOT implement same-set/next-binding. New behavior: `-fvk-bind-globals <b> <set>` moves ONLY `$Globals` to (set,binding); split-out samplers/textures STAY in set 0 (normal auto-binding). Different sets → conflict inherently gone. This is a SEMANTICS change to documented behavior (Slang's prior intended model was same-set/next-binding); docs may need updating. Regression test under tests/bindings/ must assert split-out resource in set 0, distinct from `$Globals` set (verify `0 1`: $Globals→set1/b0, sampler→set0; `3 2`: $Globals→set2/b3, sampler→set0).
- **Prior state (07-22):** TRIAGED + REPRODUCED on ToT (build g3649fb982, source @ d384b77e6). Verified 5-bullet posted to GitHub (comment 5051542260); `reproduced` label added. Human-set Type=Bug + {Dev Reviewed, spirv_vulkan, bug} left untouched. GitHub verdict comment needs no edit (stated correct outcome, not mechanism).
- **Bug:** SPIR-V/Vulkan param-binding. `-fvk-bind-globals <b> <set>` with set!=0 places a resource split out of the module-scope `uniform` globals struct at the SAME (set,binding) as the `$Globals` UBO instead of binding+1 → descriptor CONFLICT. Empirical: `0 1`→both (set1,b0); `3 2`→both (set2,b3); default & `0 0`→sampler correctly bumps to b1. Trigger = the non-default-set reservation path, NOT set==1. Severity medium / P2 / no dup.
- **Recommended fix (Approach A — CORRECTED memo 09:26Z):** make split-out globals resources start at **RELATIVE descriptor offset 1** (not 0) in the global-scope aggregate, so composition with the `-fvk-bind-globals` container base yields base+1 — mirroring the default-path bump. ⚠️ **NOT an absolute binding = base+1** — that double-composes (base 3 → binding 7). Proof: `-fvk-bind-globals 3 2` gives sampler binding 3 = relative-offset-0 + container-base-3; relative-offset-1 → binding 4 (correct), absolute base+1 → 7 (wrong). Fix target = the RELATIVE offset / composition layer, reached from globals-binding setup (`slang-parameter-binding.cpp:4607-4776`) through the ScopeLayoutBuilder/type-layout composition. Fixer must IR-dump/trace the composition site (where relative offset is set + combined with container binding). Add `-target spirv-asm` FileCheck regression under tests/bindings/ — ZERO `-fvk-bind-globals` coverage today (no GPU); test SHOULD assert BOTH base 0 (sampler binding 1) AND base 3 (sampler binding 4, not 7) to guard the double-compose pitfall.
- **OPEN DESIGN POINT (do not fold into fix):** reporter mbechard asks whether split-out samplers should instead go to set 0 (DXC SPIR-V example 3). Slang's intended model = same-set/next-binding; DXC's split-to-set-0 is a SEPARATE maintainer semantics decision. Flagged on issue, held for maintainer.
- **PR #12262 OPEN (draft) — head `04764dd7a2`, branch `fix/issue-10668`, `Fixes #10668`, assignee jhelferty-nv, reviewers jhelferty-nv+jkiviluoto-nv.** Approach C implemented in `slang-ir-collect-global-uniforms.cpp` (mark `$Globals` wrapper param w/ SynthesizedParameterGroupDecoration as discriminator) + `slang-ir-legalize-types.cpp` (drop root `$Globals` container link from the resource/special branch of the param-group `pair`, keep for ordinary branch) + options help + docs + 3 tests (`fvk-bind-globals-set/scope/entrypoint.slang`). tests/bindings 27/27, tests/reflection 42/42 green locally. CI `check-cmdline-ref` RED on prior head 7ec23ee03d (fixer hand-stripped trailing space; generator emits it) → fixed verbatim + force-pushed 04764dd7a2; remaining `check-ci` RED = benign draft priority-yield (builds skipped).
- **⚠️ REVIEW-RELAY DROP (07-29 ~14:24Z, Main-caught):** fixer believed its review request was "sent" (its seq-13 scratchpad 12:59Z) and was awaiting a verdict — but NO slang-reviewer session for #10668/#12262 ever existed (1h24m elapsed; a delivered request spawns a session in <1min, cf. #12219). Relay never reached reviewer = dropped. Model is fixer→parent(Main)→reviewer (the fixer→reviewer direct edge is for the reviewer's verdict RETURN, not the request). Main re-dispatched the `/slang-pr-review` request to slang-reviewer directly on thread `gh-issue-shader-slang/slang-10668`, head 04764dd7a2, reconstructed from PR body. **Re-relay CONFIRMED LANDED 14:55Z** — reviewer running A+C against the 8-file diff, B(Devin) done clean; monitor armed to wake on completion → will merge to combined-review + relay verdict to Main+fixer. Review genuinely in-flight (was never before). Await A/C settle → REQUEST_CHANGES/APPROVE verdict.
- **VERDICT 07-29 15:30Z: APPROVE_WITH_NITS** @ head 04764dd7a2 — 0 bugs, 1 🟡 gap, 0 questions; A 0🔴/1🟡/2🔵, B/Devin clean 0/0/0, C 8 raw candidates (pipeline exhausted budget before canonical-write; recovered drift-free). IR var-chain surgery in `declareVars` traced memory-safe + correctly scoped (pointer-identity invariant, implicitDeref/pair/tuple recursion, SynthesizedParameterGroupDecoration keying — entry-point groups decorate element struct not param, correctly excluded). Nits (fixer's to address or maintainer to waive; NON-blocking): 🟡 WGSL reachable via `-fvk-bind-globals -target wgsl`, silently fixed but no test/doc (add `tests/wgsl/fvk-bind-globals-set.slang` or declare out-of-scope); C001+A both want a `SLANG_ASSERT` on the load-bearing pointer-identity gate; C002 asks why type-legalization is the right layer vs `slang-parameter-binding.cpp`'s `hasGlobalsBinding()` (answer in process report — #10668 triage pointed there); FG003 verify hand-edited cmdline-ref byte-identical to `slangc -help` (already the fixer's fixed CI item). Report: `/workspace/inbox/a2a-1785339024893-2kg1cr/combined-review.md`. Reviewer sent to parent+fixer; loop closed by direct edge; Main did NOT re-dispatch.
- **ALL NITS APPLIED 07-29 15:48Z — new head `0713426634` (force-push over 04764dd7a2, draft-held, CI re-dispatched).** Verdict was 0-bug terminal so no REQUEST_CHANGES round needed; nits are advisory. Fixer applied: `SLANG_ASSERT` for pointer-identity invariant (codex corrected an overclaim about what it catches), consolidated/cross-ref'd comments, doc reword, layer-justification (C002) in process report. **A's 🟡 WGSL gap RESOLVED as evidence-backed out-of-scope** (pre-existing master crash, flag-independent — not introduced by this PR). Re-verified green: repro SPIR-V+GLSL, bindings 27/27, reflection 42/42, 6/6 new; codex CODE+OUTPUT approve. PR refined; **awaiting maintainer ready+merge (operator-gated; fixer won't flip).** Fixer armed monitor (real non-cosmetic CI only) + handles pr_review/pr_mention webhooks. No re-review needed (nits-only on a 0-bug verdict).
- **diff_hash check RESOLVED (Main, 15:31Z): CLEAN — no reverse cross-contamination.** Committed check `result-block diff_hash == gh pr diff 12262` did NOT byte-match (`gh pr diff`=`665e32dd…` vs report `f6b86a4d6a29…`) — but that's a benign **serialization-format** difference, NOT contamination: reviewer disclosed GH token invalid → reviewed via local-git three-dot `gh` shim. Verified the reviewer's premise: **head 04764dd7a2's parent == `1eeb3b29d8` == PR base_sha (master tip)** → head is a direct child of master → three-dot diff content byte-identical to PR diff. File set = all 8 #10668/`-fvk-bind-globals` files, zero #12263/SCCP content. Footer `reviewed: 04764dd7a2` + `+199/-2, 8 files` self-consistent. Verdict trusted.
- ⚠️ **Reviewer env infra note:** reviewer reported its GH token invalid + OneCLI GitHub disconnected (worked around: local-git diff + anonymous Devin scrape; degraded gracefully, review completed correctly). Main's own `gh` works → scoped to reviewer's container, not global. Reviewer never posts to GitHub anyway (wrapping workflow owns posting), so low-urgency. WATCH: if reviewer's NEXT review also reports GH-down = pattern → escalate to operator (OneCLI re-login). Single graceful-degrade instance = not yet operator-actionable.
- Same failure family as #11860/#11871 (single-kind exclusion guards, resource-kind-bucket mismatch). Reporter mbechard (CONTRIBUTOR); assignee/requester jhelferty-nv (MEMBER).
