---
name: project_12004_image_sampler_param_asymmetry
description: "#12004 SPIR-V image-vs-sampler param asymmetry — Approach A REJECTED by jkwak, PR #12027 CLOSED, issue Unplanned/parked"
metadata: 
  node_type: memory
  type: project
  originSessionId: dbfa379e-06ac-416c-9542-c54b194845ec
---

**⛔ RESOLVED 07-09 22:18Z — MAINTAINER WON'T-FIX (Approach A rejected):** jkwak-work **closed PR #12027 unmerged** (state=closed, merged=false, 22:18:54Z) and is marking issue #12004 **"Unplanned"** (PR comment 4930059216 + issue comment 4930073768). Reasoning (Yong/csyonghe via jkwak, Main-verified): "we don't want to make changes to the SamplerState side. Passing the sampler-state as descriptors should be considered **downgrading**." The desired eventual direction is **Approach B** (revert the texture/image descriptor-index workaround → both by-value) OR stay as-is — the texture workaround exists for old graphics-driver bugs that may now be fixed. BUT "we currently don't have a plan to remove the workaround"; will schedule removal only if certain it's safe, else wait for driver stability. → **A withdrawn, B NOT authorized (explicit no-plan), issue parked.** Bot does NOT re-open with B unilaterally (relaxing #3252 is maintainer-owned + explicitly deferred). Issue labels now `Dev Reviewed`+`reproduced`, state open (Unplanned reason). fixer to STAND DOWN. Re-engage ONLY on a maintainer directive to prototype B (webhook). TERMINAL for our active work.

**✅ CHAIN CLOSED 07-09 22:31Z (all artifacts triager-verified vs GitHub):** worktree `wt-slang-12004` reaped (0 remain); origin branch `fix/issue-12004`@`30b048f7ad` (with repro test) **preserved** as offered to jkwak (for future B prototyping). Closing ack posted on PR #12027 (nv-slang-bot comment 4930068941, 22:20Z) — non-defensive, agrees downgrade framing, confirms stand-down, offers B if scheduled. B NOT re-opened. Terminal state re-verified: PR #12027 CLOSED unmerged (mergedAt=null); issue #12004 open + `Dev Reviewed` (Unplanned). Durable learning logged (non-obvious: sampler-by-value was the DESIRED form; texture-by-index is the workaround → principled direction is B "lift the bad leg", not A which downgrades the good leg — memo under-weighted that A is a quality downgrade when recommending it for low risk). Edge idle. (fixer's own upstream-confirm bounced on reaped parent sessions — expected; roll-up is triager's edge.)

**Update 07-09 (READY FOR REVIEW):** @jkwak-work (maintainer) flipped **#12027 draft→ready_for_review** at 17:47:20Z — verified via timeline actor. NOT a fixer self-flip → drafts-only guardrail intact. Host auto-routed the `pr_ready_for_review` webhook **directly** to slang-reviewer (session `sess-1783619242225-eo49lw`, thread `gh-pr-shader-slang/slang-12027`, minted 17:47:22Z, active+running). Direct-routing HIT confirms `report_pr_created(#12027)` WAS called (resolves prior ⚠️). Main did NOT re-dispatch (would double-dispatch onto reviewer's live session). Reviewer processing autonomously via webhook skill. Awaiting [Review Report]. Gated: NO ready-flip already done by maintainer; merge still operator-gated.

**#12004** (shader-slang/slang) — external reporter maxime-modulopi. Passing `Texture2D<float>` + `SamplerState` (both `.Handle` from bindless cbuffer) into a `[noinline]` fn: SPIR-V asymmetric — sampler loaded at callsite & passed as `OpTypeSampler`; image passed as `uint32_t` bindless index, re-loaded inside callee.

**Verdict (triager, source-verified @ HEAD bfe6a7f14):** Bug / low / P3. Root cause `slang-ir-specialize-resources.cpp:1371-1379` — `isIllegalSPIRVParameterType` flags EVERY `Texture2D` param for specialization (→ by-index) but flags `SamplerState` only when ARRAY (line 1373), so scalar sampler stays by-value. Valid SPIR-V; **consistency bug, not miscompile**. Not a dup. `reproduced` label + Issue Type=Bug applied; 5-bullet posted (comment 4916795143).

**Fix path:** Approach A recommended = specialize scalar samplers like textures (both by-index, Slang's documented default; ~one-line + FileCheck/spirv-val test). Approach B (make image by-value too, reporter's literal preference) touches old "always specialize textures" invariant → high-risk, fixer to bounce up as maintainer design call.

**Chain:** webhook → Main → slang-triager → slang-fixer (triager owns fixer edge — do NOT double-dispatch). Thread `gh-issue-shader-slang/slang-12004`. Drafts-only guardrail applies.

**Update 07-09:** @jkwak-work (maintainer) pinged "how is the PR fix going?" (comment 4927384063). Fixer answered on GitHub (comment 4927415593, 16:37Z). **Approach A implemented** on branch `fix/issue-12004` — `isIllegalSPIRVParameterType` now flags scalar `SamplerState` for specialization like textures (both by-index, symmetric; dead `isArray` param removed across 3 sites). Fixer flagged A-vs-B fork to jkwak in the comment (A=both by-index/low-risk=shipping; B=both by-descriptor=reporter's lean, weakens #3252 texture invariant + risks #3546 compile-time blow-up → maintainer's call). Build finishing (infra restart interrupted, resuming); NO PR yet. Draft PR ETA ~30-40 min (build → FileCheck + spirv-val + descriptor-handle suite). Awaiting fixer [Fix Report] / draft PR link.

**Update 07-09 (PR opened):** **Draft PR #12027** open — https://github.com/shader-slang/slang/pull/12027 — `Closes #12004`, head `fix/issue-12004`→master, label `pr: non-breaking`, author `nv-slang-bot[bot]`, requested reviewers jkwak-work + juliusikkala (jkwak assignee). Verified live via github_get_pull_request (draft=true, state=open). +52/−13, 3 files. Tests: repro PASS under spirv-val (SLANG_RUN_SPIRV_VALIDATION=1); revert-drill 0/1 pre-fix; descriptor-handle 52/52; tests/spirv 501/501 — no regressions. codex critique PLAN/CODE/OUTPUT all APPROVE. slang-reviewer dispatched (Reviewer A pipeline). PR body carries full root-cause writeup + A-vs-B design note for jkwak. CI dispatched. Awaiting reviewer verdict + CI via webhook. Gated: merge operator-gated. **✅ report_pr_created(#12027) CONFIRMED** (fixer stated + direct-routing hit of the ready webhook to reviewer proves the mapping exists). **✅ Ready-flip actor = jkwak-work (maintainer), NOT the bot** — Main-verified via `gh api issues/12027/events` (actor jkwak-work, 17:47:20Z); `draft=false`. NOT a drafts-only breach (maintainer flip is fine). Fixer's #24 "no ready-flip by us" is correct — they opened draft 17:44Z, jkwak flipped 17:47Z. Issue footprint: edit-in-place is 403 for this bot token (standing finding), so fixer posted a FRESH 5-bullet footprint (comment 4928013179) linking #12027 + A-vs-B note; `Closes #12004` cross-ref also present. Reviewer auto-routed by host to slang-reviewer (session sess-1783619242225-eo49lw, thread gh-pr-shader-slang/slang-12027) — Main did NOT re-dispatch (would double-dispatch onto reviewer's live session). Awaiting [Review Report].
