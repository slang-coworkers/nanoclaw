---
name: project_12119_ldg_optix_sbt_exclusion
description: "slang#12119 __ldg OptiX SBT exclusion — TERMINAL: MERGED b593edc 07-21, jkwak-work APPROVED; R4 WOULD_APPROVE=agreement, R3 ABSTAIN vindicated"
metadata: 
  node_type: memory
  type: project
  originSessionId: a7377421-d5b3-4f58-8dd7-be5b3ddbf7bd
---

shader-slang/slang#12119 (author szihs) — "Fix #10188: shape-independent `__ldg` exclusion for OptiX SBT loads". **Supersedes prior PR #11152** for the same issue #10188.

slang-pr-approver verdict — three revision heads, shadow mode (NOT posted to GitHub). **R3 did NOT carry R1/R2's WOULD_APPROVE:**
- **R1 @ cab4543af5fa** — WOULD_APPROVE (CLEAN); 6/6 clauses; PRIMARY github-actions[bot] 0🔴/3🟡; Devin 0/0.
- **R2 @ 8de9683706f8** (2026-07-15, diff_hash 4928bb761a88): second `synchronize`, delta R1→R2 comment/format-only, zero logic change. WOULD_APPROVE (CLEAN). Waited out in-progress `Claude PR Review` (~30 min, success 14:28Z), re-harvested PRIMARY — 🟡 2 gaps/1 question/0 bugs. Devin 0/0.
- **R3 @ f4fa4942590d** (2026-07-16) — **ABSTAIN_POLICY (OPEN_GAP).** Source byte-identical to R2; delta test-only (defensive `CHECK-NOT: __ldg` bracketing, closes R2 Gap 1) → on that alone R2 would have carried. **BUT R3's primary review surfaced a NEW load-bearing gap:** the SBT walker omits **`kIROp_PtrCast`**. Approver first cleared it "failure-direction-safe"; codex DECISION_REVIEW returned **must-fix and proved that wrong** — `GetOptiXSbtDataPtr` is ConstantBuffer→`Uniform` AS, which the predicate treats as immutable, so a *missed* forwarding op falls through to **TRUE → __ldg → stale read** = the exact #10188/#11152 mechanism (walker miss returns the BUG direction here, unlike the R2 Add-path which returned the safe direction). Reachability LOW (no `kIROp_PtrCast` producer in source) → **not BLOCK**; can't prove unreachable to #11152's bar → **not WOULD_APPROVE**. **One-line fix flips it: add `case kIROp_PtrCast:` to the walker.** All ledger rows stand per revision-chain rule.
- **R4 @ 7d12d5a8224b** (2026-07-16) — **WOULD_APPROVE (CLEAN). R3 hold VINDICATED: author landed exactly the named one-line fix.** Delta R3→R4 = single net-zero walker line: **`+kIROp_PtrCast`** (closes R3 gap) and **`−kIROp_NodeOutputRecordGetElementPtr`** (verified inert — work-graph Node-stage op, mutually exclusive from OptiX RT, never on an SBT chain). Tier wrinkle: production github-actions[bot] primary **did NOT post at R4** (its `review` check-run went success in ~4 min vs ~17-29 min R1-R3, no comment in ~20 min — NOT the exit-22 pending race), so decided from head-current **fallback tier**: CodeRabbit (exit 0 clean) + Devin 0/0 + deep source verification. A scout subagent raised a `CastStorageToLogical` residual-cast bug → **refuted against source** (`materializeStorageToLogicalCasts()` eliminates those casts before the `__ldg` pass; matrix/array lowering emits only peeled ops; sole `Add`-path UserPointer-gated); codex adjudicated. Walker peel set complete for CUDA SBT path. Survived a mid-task container restart cleanly (resumed from persisted `work/` + `workflow-state.json`; recorded once).

R1/R2 challenger note (still valid for those heads): new `isAddressIntoOptiXShaderBindingTable` peel-set was a strict superset of `getRootAddr` for the ops then examined. The `lowerBufferElementTypeToStorageType` `BitCast→Add→BitCast` unpeeled `kIROp_Add` was resolved unreachable for SBT + failure-direction-safe. **R3's lesson: op-set completeness must include `kIROp_PtrCast`, and the AS-immutability of the SBT ptr chain means a walker miss is the DANGEROUS direction — not the safe one the R1/R2 challenger assumed for the Add path.**

Challenger ran the exact op-set completeness probe the **#11152 false-safe failed**: new `isAddressIntoOptiXShaderBindingTable` peel-set is a strict superset of `getRootAddr`, covers every SBT-reachable forwarding op. `lowerBufferElementTypeToStorageType` emits a `BitCast→Add→BitCast` where `kIROp_Add` is NOT peeled, but resolved (a) unreachable for SBT uniforms (that path is `AddressSpace::UserPointer`+unsized-array-gated; SBT is default-AS `ConstantBuffer` `FieldAddress` chain) and (b) failure-direction-safe (walker miss → returns `false`/mutable → no `__ldg`, opposite of #11152). Sole producer of `kIROp_CUDALDG` is this pass; failure direction cannot miscompile (removes `__ldg`, observable at FileCheck). Correct producer-layer fix, vindicated by #11152 postmortem — cf. [[project_11323_casttovoid_closed_wronglayer]].

**TERMINAL — MERGED 2026-07-21T22:21Z** (mergeCommit b593edc384, merged by szihs). human_verdict=**APPROVED** (jkwak-work APPROVED, reviewDecision=APPROVED — genuine independent maintainer approval, not self-merge) stamped against R4 (7d12d5a8224b) and R3 (f4fa4942590d).

**SHA-verify-first caught a non-clean join:** merged head is **e5c7b14f3209, NOT R4 head 7d12d5a8224b** (5 days + rebase-onto-master later; R4→merged = +34/−1, almost all `.github/**` CI churn). Approver did NOT blind-stamp R4's SHA as merged — verified the load-bearing SBT walker `isAddressIntoOptiXShaderBindingTable` byte-identical at merged head to R4 (same peel set incl. `kIROp_PtrCast`), so R4's decision content is exactly what merged. Only content deltas vs R4: doc-comment rewordings (CodeRabbit nit) + one unrelated test (`cuda-prelude-vec1-make.cu`, scope-adjacent nvcc vec1-make regression, not SBT). Stamped verdict against R4/R3 (rows whose content merged), not the never-decided e5c7b14f.

**Outcome:** R4 WOULD_APPROVE = calibration agreement (HIT). R3 ABSTAIN(OPEN_GAP) VINDICATED — author landed exactly the `+kIROp_PtrCast` one-liner R3 named, then merged APPROVED. The R2→R3-ABSTAIN→R4-fix→merge-APPROVED arc = #11152 discipline paid off end-to-end. All 4 ledger rows stand with human verdicts on R3/R4. Shadow throughout — nothing posted either tier. Chain terminal.
