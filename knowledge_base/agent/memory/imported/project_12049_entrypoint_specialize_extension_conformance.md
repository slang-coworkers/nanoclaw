---
name: project_12049_entrypoint_specialize_extension_conformance
description: "#12049 entry-point -specialize can't see primary-TU extension conformances — SHIPPED (PR#12052 merged 07-16)"
metadata: 
  node_type: memory
  type: project
  originSessionId: dbd1957c-3d1d-4960-b4ee-23473451083a
---

**shader-slang/slang#12049** — Compiler cannot validate conformance of a type specializing a *generic entry point* when the conformance is provided by an `extension` in the **primary** source file. `slangc -entry shader -specialize float` → `error[E38029]: type argument doesn't conform to interface`.

- **State (07-10):** triaged (P2 / medium / frontend) → fixer dispatched, Approach A, DRAFT-only. Repro CONFIRMED on ToT 85d79c676; `reproduced` label applied; triage verdict on GitHub = comment 4937485580. Canonical thread `gh-issue-shader-slang/slang-12049`.
- **OWNERSHIP:** **slang-triager owns the fixer dispatch** (peer-wired; triager's msg id 9). It forwards the fixer's [Fix Report] upstream to me. Main double-dispatched by mistake (direct send_file to slang-fixer) — CAUGHT: both landed in the SAME fixer session `sess-1783701595318-cn6xdd` (parent edge = triager, minted at birth; my message is a redundant later edge). No parallel session, no dup-PR risk. **Main must NOT contact slang-fixer again on this chain** — route everything through the triager ([[feedback_no_double_dispatch_peer_wired]]).
- **Root cause (high confidence):** `EntryPoint::_validateSpecializationArgsImpl` builds `SharedSemanticsContext(linkage, nullptr, sink)` (slang-check-shader.cpp:3430, `m_module==nullptr`). Ad-hoc mode pulls extensions only from `Linkage::loadedModulesList` (slang-check-decl.cpp:17291) — but the **primary command-line TU module is never in that list** (only imported/serialized modules are). Primary TU's extension conformances invisible → witness not found → E38029.
- **Decisive discriminator (triage):** moving the same interface+extensions into an *imported* module makes E38029 disappear. Only the import-vs-primary-TU variable changed.
- **Approach A (recommended):** scope the specialize checking context to the entry point's owning module (`getModule()`, slang-entry-point.cpp:61) and make its own extensions visible; verify at seam whether ctor-module route suffices or the :17267 `m_module` branch also needs the own-module add (mirror DeclAssociations sibling :17469-17476). Sibling nullptr contexts at :3663/:3708. **Approach C (add primary TU to loadedModulesList) REJECTED — high blast radius.**
- **Test:** issue primary repro under tests/language-feature/generics or tests/front-end; `-target spirv-asm -entry shader -specialize float -skip-spirv-validation`, CHECK-NOT E38029.
- Reporter EvgeniyPeshkov (new to Slang, assoc NONE). Related but distinct: [[project_9580_glsl_legalize_layout_mismatch]] (other entry-point-specialize boundary divergence).
- **Guardrails:** DRAFT PR only; drafts-only per [[feedback_drafts_only_guardrail]]; ready-flip + merge operator-gated.
- **RESOLVED-to-draft (07-10 ~17:50):** Fixer shipped Approach A — **draft PR #12052** (`fix/issue-12049` → master, `Closes #12049`, `pr: non-breaking`, author nv-slang-bot[bot]). 1 file +33/−1 (slang-check-shader.cpp): `_validateSpecializationArgsImpl` now scoped to `getModule()` + `importedModulesList` seeded from `getModuleDependencies()` (self-includes owning module via `addModuleDependency(this)`). Tests: repro 2/2 (float + uint32_t), front-end 49/49, generics 249/249, 0 regressions, clang-format clean. codex-critique PLAN/CODE/OUTPUT approve. `report_pr_created` done (fixer-confirmed). Issue footprint = issuecomment-4938000213. Reviewers requested: jkwak-work + jkiviluoto-nv; assignee jkwak-work. Draft CI = benign priority-yield red (workflow_dispatch skips) — NOT a failure.
- **IN REVIEW (07-10 ~23:42):** PR #12052 **flipped NON-draft by jkwak-work** (maintainer/assignee) at 23:23 — Main-verified via timeline `ready_for_review` actor=jkwak-work (NOT bot → NO guardrail breach per [[feedback_drafts_only_guardrail]]). HEAD ad5855b46e; `mergeable_state: behind` (trails master — fixer's rebase call, not a CI action). jkwak review points: (1) drop `-skip-spirv-validation` → DONE, jkwak RESOLVED thread; (2) design Q — use `SemanticsVisitor::importModuleIntoScope()` instead? fixer replied w/ rationale (seam needs flat extension list not a lookup Scope; API over-reaches + needs Scope* we lack; `__exported` recursion subsumed by dep closure), offered to switch, **jkwak's call — no code change pending**. Repro now runs FULL validated SPIR-V path. One benign self-inflicted priority-yield CI red (own `gh workflow run` on non-draft) — cosmetic, no action.
- **APPROVED (07-11 00:57):** PR #12052 **APPROVED by jkwak-work** (maintainer) — "Looks good to me", `pullrequestreview-4675922976` @ 2026-07-11T00:56:47Z. Main-verified via GitHub reviews API. Both review points closed: (1) `-skip-spirv-validation` dropped, thread resolved; (2) `importModuleIntoScope()` design Q — jkwak RESOLVED the source thread = direct-seeding approach ACCEPTED, no code change. Human review gate satisfied. HEAD later bumped ad5855b46e → **c4c7d1837d94** (fixer-reported: jkwak's own merge-master-in to un-BEHIND the branch; fix files untouched → re-approval not stale). Triager independently verified + holds.
- **SHIPPED (07-16 01:59):** PR #12052 **MERGED into master by jkwak-work** — merge commit `89443da3660e9b9791a9c2b006840b8f57430489` @ 2026-07-16T01:59:42Z. Issue #12049 **auto-closed COMPLETED** @ 01:59:43Z via `Closes #12049`. Main-verified via GitHub PR + issue APIs. Regression test `tests/front-end/specialize-entry-point-extension-conformance.slang` in master (passed Windows GPU CI). Pre-merge `static-const-vector-array.slang.3 syn (llvm)` red = unrelated flake, jkwak merged over it. Fixer cleaned up worktree + active-work sentinel.
- **CHAIN CLOSED.** No further action. Terminal.
