---
name: "#11837 Metal half-float literal suffix — SHIPPED (#11838) + CLOSED by maintainer 2026-07-07"
description: shader-slang/slang#11837 Metal FP-literal-suffix bug; finite fix MERGED via #11838 (jkwak-work 07-01); skiminki-nv CLOSED the issue 07-07 as resolved; NaN/Inf gap documented+subsumed under his #11276/#11783 fp-literal series
type: project
originSessionId: 869d7f28-2134-474f-af2d-7d2e8250fbc8
---
**CLOSED (2026-07-07) by skiminki-nv.** He reviewed #11838, confirmed it "does what it's supposed to do," re-affirmed the syntax answer (`61440.hf` valid, `61440.h` equivalent; side note: Slang float literals always require a `.` or exponent — a suffix alone is not sufficient, as in C/C++), and closed the issue himself (issuecomment-4903028970). The remaining NaN/Inf half/float gap was NOT split out as a separate follow-up on #11837 — it's subsumed under his own frontend fp-literal series (#11276 / #11783), which is where he's tracking that class of work. Chain fully terminal; our staged emitter sketch (@1140/1145/1150) was never posted and is now moot unless #11276/#11783 explicitly surfaces the Metal-emit NaN/Inf piece and invites the bot. Triager stood down.

**RESOLVED / SHIPPED.** shader-slang/slang#11837 — `half`/`float` literals emitted to the Metal target with no MSL type suffix (`half h = 61440.hf` → bare `61440.0`, read as 64-bit double → `as_type<ushort>(61440.0)` is a 64→16-bit bit-cast MSL rejects). Pure emit-layer gap: `MetalSourceEmitter::emitSimpleValueImpl` ignored the (correct) IR literal type. Fix appends `h`/`f` for finite half/float literals (mirrors the WGSL emitter); double/untyped stay bare. **PR #11838 MERGED to master by maintainer jkwak-work on 2026-07-01** — he merged it himself, so no bot-initiated (operator-gated) merge was involved. Regression test `tests/metal/float-literal-suffix.slang` + 3 baseline updates landed; worktree/sentinel cleaned up.

**Remaining known gap (documented, out-of-scope) — SCOPE PREPARED + HELD 2026-07-02:** `half`/`float` NaN/±Inf literals still emit the bare double-typed form — a separate pre-existing path. #11838 used `Addresses` (not `Fixes`), so the merge did NOT auto-close #11837; the NaN/Inf gap stays tracked. jhelferty-nv asked skiminki-nv on the issue (issuecomment-4869557536) to scope what's left so they can close it — maintainer-to-maintainer, **no @nv-slang-bot mention** → we PREPARED the scope but did NOT post (don't insert the bot into a maintainer thread uninvited).

**Sequenced behind #11276 (2026-07-05, skiminki issuecomment-4902116709):** skiminki replied to jhelferty "be aware that #11276 has not yet been merged" — a sequencing signal. **#11276** = skiminki's own frontend issue "Incorrect handling of floating point literals" (lexer `getFloatingPointLiteralValue` value-extraction / `0#INF` dispatch / decimal precision narrowing; assigned skiminki, milestone Q2 2026, `reproduced`); it's the PARENT of the float-literal series that **#11783** belongs to (per skiminki's own comment on #11276: "#11783 fixes the gap"). Our #11837 NaN/Inf gap is EMITTER-side (`MetalSourceEmitter`), a DIFFERENT layer from #11276's FRONTEND lexer work — logically independent files — but skiminki is sequencing the whole float-literal cleanup, and #11276/#11783 could change how non-finite half literals reach the emitter (IRFloatLit representation). **Revised release condition:** hold posting/implementing UNTIL (skiminki pulls the bot in OR the thread stalls) AND #11276/#11783 have landed — then re-verify the staged sketch against the post-#11276 frontend before offering. Don't offer the emitter fix while the frontend series is still in flight.

Prepared scope (triager, confirmed at HEAD 803dff915 = #11838 merged as 8d919cb64; matches documented characterization exactly): the non-finite switch in `MetalSourceEmitter::emitSimpleValueImpl` (source/slang/slang-emit-metal.cpp) runs BEFORE #11838's finite-suffix code and emits bare double forms w/ early return — `NaN→"(0.0 / 0.0)"` @1140, `+Inf→"(1.0 / 0.0)"` @1145, `-Inf→"(-1.0 / 0.0)"` @1150 → half/float NaN/Inf becomes 64-bit double → `as_type<ushort>(...)` 64→16-bit reinterpret MSL rejects (same defect class as the finite bug). **Fix sketch:** hoist base-type→suffix (Half→"h", Float→"f", else nullptr) into a shared helper used by both paths; make the three non-finite branches type-aware (`(1.0h / 0.0h)` etc., `f` for float; bare for double/non-basic). Consumer-side only, IRFloatLit already correctly typed. Test: extend `tests/metal/float-literal-suffix.slang` w/ half ±inf/NaN + metallib compile — NB NaN literal spellings `1#INDhf`/`1#QNANhf`/`1#SNANhf` do NOT parse (E39999); use `(0.0h/0.0h)` const-fold for a half NaN.

**Suffix-validity Q (resolved, don't re-litigate):** jkwak-work asked (issuecomment-4848438922) whether `61440.hf` is valid / should be just `h`. Verified at HEAD 6d355565c: `h`/`H`/`hf`/`HF`/`fh`/`FH` all = 16-bit `half` literal (`h` ≡ `hf`; `hf` is the language-reference example form); no-suffix=float, `l`/`lf`=double, unknown suffix→E39999. Sources: parser `parseFloatingPointLiteralExpr()` (slang-parser.cpp:8906-8921), lexer (slang-lexer.cpp:974-1049), docs expressions-literal.md.
