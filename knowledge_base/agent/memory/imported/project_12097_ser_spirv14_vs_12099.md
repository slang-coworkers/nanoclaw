---
name: project_12097_ser_spirv14_vs_12099
description: SHIPPED #12097 SER retain SPIR-V 1.4 — PR #12115 MERGED 07-15 21:40Z; sibling #12099 still maintainer's design call
metadata: 
  node_type: memory
  type: project
  originSessionId: 0c896180-ff45-4841-97a8-b1e68087b6b1
---

**✅✅ SHIPPED — PR #12115 MERGED by szihs 07-15 21:40:44Z (merge commit `8e3f9163`), issue #12097 auto-closed COMPLETED 21:40:45Z via `Closes #12097` (both verified via GitHub).** Fixer reaped worktree + sentinel. Chain terminal. **Sibling #12099 (opposite resolution — reject conflicting -profile/-capability combo) remains the maintainer's design call; #12115 shipped #12097's preferred allow-1.4 path — if #12099 activity surfaces, note #12115 already landed the 1.4 route.**

---

**shader-slang/slang#12097** — SER (shader invocation reorder) extensions unconditionally upgrade a requested `spirv_1_4` profile to 1.5. Author + explicit "make a pr" authorization: jkwak-work (core maintainer). Repro CONFIRMED on ToT (HEAD 3eeda847c, deterministic, no GPU): `-profile spirv_1_4 -capability spvShaderInvocationReorderNV` → header `; Version: 1.5` (control `-profile spirv_1_4` alone → 1.4). Verdict posted https://github.com/shader-slang/slang/issues/12097#issuecomment-4972430785. Confirmed bug / low severity / P2. Not a regression. `reproduced` label applied; type `DevRel` human-set, left untouched.

**Root cause (verified):** `source/slang/slang-capabilities.capdef:624` `def SPV_EXT_shader_invocation_reorder : _spirv_1_5 + SPV_KHR_ray_tracing;` (NV + motion aliases inherit) pulls `_spirv_1_5` into target caps → determineSpirvVersion() (spirv-legalize.cpp:2482) → header at emit-spirv.cpp:537. Author's "capdef edit alone insufficient" CONFIRMED: 1.4 path must ALSO emit `OpExtension "SPV_KHR_physical_storage_buffer"` (today only emitted via PhysicalStorageBuffer64 addressing, which SER doesn't trigger) or spirv-val fails.

**Fix approach (A, dispatched to slang-fixer via slang-triager on canonical thread `gh-issue-shader-slang/slang-12097`):** capdef floor to 1.4 + SER emit declares PSB ext on <1.5 path; NV/EXT/motion inherit; 1.5+ profiles keep core 1.5 (no downgrade); version-dependency test on `tests/bugs/gh-11082.slang` (header stays 1.4, required OpExtensions, NV+EXT variants, spirv-val). **DRAFT PR, held until maintainer flips to ready.**

**✅ 07-15 ~08:06Z: FIX COMPLETE — DRAFT PR #12115 open (`Closes #12097`, `pr: non-breaking`).** Fixer auth recovered ~07:06Z, picked up queued handoff, implemented Approach A. Final shape (after codex CODE review caught a consistency gap — the `ReorderThread()` spirv_asm path bypassed the initial HitObject-only PSB helper): PSB dependency centralized into `requireSPIRVCapability` — the single chokepoint all SER paths (HitObject + all hlsl.meta.slang spirv_asm blocks) funnel through — declaring `SPV_KHR_physical_storage_buffer` on the <1.5 path via `ensureExtensionDeclarationBeforeSpv15` (plain OpExtension, memory model stays Logical GLSL450, no addressing switch). 5 files +165/−56: capdef floor `_spirv_1_5`→`_spirv_1_4`, `requireShaderInvocationReorderExtension()` helper, regenerated capability-atoms doc, 2 new tests. **Tests:** repro PASS; `tests/spirv/` 515/515 (12 ignored); SER-adjacent 50/50; spirv-val EXIT=0 for NV@1.4/EXT@1.4/ReorderThread@1.4/NV@1.5; NV/EXT@1.5 keep Version 1.5 + no PSB (floor preserved). codex CODE+PLAN+OUTPUT all approved. Reasoning log fixer-side `memory/fix-12097.md`.

**✅ 07-15 11:15Z: PR #12115 flipped DRAFT→READY by human maintainer `szihs`** (verified via timeline: `ready_for_review` actor=szihs @11:15:00Z; NOT our bot, fixer did NOT run `gh pr ready`). Guardrail SATISFIED by the maintainer's own hand — do NOT flip back. `jkwak-work` is requested reviewer + assignee (jhelferty-nv requested jkwak @08:02:33Z right after PR open). reviewDecision=`REVIEW_REQUIRED`. **Maintainer review is now the real merge gate**; our internal slang-reviewer (A/B/C) is advisory only. Real `pull_request` CI now runs (non-draft triggers it; supersedes earlier cosmetic workflow_dispatch yields). One CI red: `test-linux-release-gcc-x86_64-cpu / test-slang` SIGSEGV in `tests/compute/static-const-matrix-array.slang` — fixer triaged as infra flake (SPIR-V-emit-only change, CPU compute test has zero SER content, passes 8/8 locally, all 9 builds GREEN, matches #12059 pattern), ran `gh run rerun --failed` (attempt 1/≤3). No code change. report_pr_created(12115) CONFIRMED (live mapping — ci webhook routed to fixer session).

**⏳ 07-15 ~12:58Z: MAINTAINER REVIEW — open design question from jkwak (COMMENTED, NOT REQUEST_CHANGES).** jkwak asked why the capability system can't model the PSB relationship directly, suggesting `def SPV_EXT_shader_invocation_reorder : _spirv_1_4 + SPV_KHR_ray_tracing + SPV_KHR_physical_storage_buffer;`. Fixer replied on PR discussion (r3587408739, codex OUTPUT-approved): capdef-only insufficient because (1) `SPV_KHR_physical_storage_buffer` is NOT a capdef atom (only `SPV_EXT_physical_storage_buffer : _spirv_1_3` exists; KHR is emit-string-only) and (2) no generic atom→OpExtension mechanism — every ext emitted by explicit C++ (`ensureExtensionDeclaration`), so an atom documents but emits nothing. Fixer OFFERED a hybrid: additionally add the real `SPV_EXT_physical_storage_buffer` atom (floor _spirv_1_3, won't raise version) to the capdef dep for self-documentation, keeping the guarded <1.5 C++ emission. **Asked jkwak his preference; made NO code change yet — correctly deferring the design call to the maintainer.** This is the fixer's OWN technical position on its OWN PR (not relayed by me; between fixer↔jkwak). See [[feedback_dont_close_open_proposals]].

**✅ 07-15 ~14:05Z: jkwak CHOSE the hybrid → fixer applied + pushed (commit `8230c81991`, EXT atom — SUPERSEDED, see next).** First hybrid used `+ SPV_EXT_physical_storage_buffer`.

**✅ 07-15 ~14:48Z: jkwak SELF-CORRECTED EXT→KHR → fixer re-applied (commit `36d187dd53`, FINAL capdef shape).** jkwak clarified SER's PSB dep is on **KHR** not EXT and asked for a capdef atom. Fixer verified SER spec (disjunction: SPV_EXT_physical_storage_buffer / SPV_KHR_physical_storage_buffer / core 1.5) and: added NEW atom `def SPV_KHR_physical_storage_buffer : _spirv_1_3;` (floor deliberately 1.3 NOT 1.5 — 1.5 would raise version + break the 1.4 goal), switched SER dep to `... + SPV_KHR_physical_storage_buffer`. **FINAL atom = KHR, floor _spirv_1_3.** Emission unchanged (KHR is the name the emitter already declares via requireSPIRVCapability guarded <1.5). Regenerated BOTH generated docs (capability-atoms + command-line-slangc-reference — `check-cmdline-ref` CI verifies the latter). Verified: NV/EXT@1.4→1.4+PSB+Logical GLSL450; @1.5→1.5 no PSB; spirv-val clean; tests/spirv 515/515. Pushed `36d187dd53`; replied jkwak r3588260069 (offered fuller spec-disjunction top-level `|` def if preferred, implemented the simpler single-KHR dep he asked). codex CODE+OUTPUT re-approved. **All maintainer feedback addressed → held on jkwak re-review/merge.**

**✅ 07-15 16:46Z: MAINTAINER APPROVED + ENQUEUED (verified via GitHub, not just fixer claim).** szihs APPROVED ("Looks good to me") at `36d187dd53` = current headRefOid (on HEAD, NOT stale); `reviewDecision:APPROVED`; `added_to_merge_queue` by szihs @16:46:21Z. jkwak's 3 earlier reviews were all COMMENTED (never REQUEST_CHANGES), resolved by the 2 capdef iterations. **NOT YET MERGED** — state OPEN, mergeStateStatus BLOCKED (normal queue-wait), no `merged` event. Fixer standing down correctly (won't push — would dismiss approval + be queue-excluded; won't merge — maintainers own it). **PENDING close-out:** when #12115 MERGES → `Closes #12097` auto-closes issue; slang-triager edits comment 4972430785 → "fixed, merged in #12115" + forwards final [Triage Resolution]. Merge webhook routes to FIXER session (report_pr_created mapping); triager tracks the fixer's report stream so should catch it. If merge lands and triager doesn't close out, relay merge→triager. Then dashboard close-out note (hold until actual merge — one clean "shipped" ping, not "enqueued" now + "merged" later).

**✅ 07-15 ~18:19Z: slang-pr-approver settled SHADOW verdict — WOULD_APPROVE (CLEAN) @ `36d187dd53d7` (R3), recorded to ledger, nothing posted (shadow mode).** PR revised 3× via `synchronize` during the approver session (R1 `34ab263a` → R2 `8230c819` EXT → R3 `36d187dd` KHR); approver re-anchored + re-ran full procedure each time, recorded only settled head R3. Devin-only tier (harvest exit 20 — bot-authored PR, production review legitimately skips), Devin 0 bugs/0 flags/4 info notes; 6/6 clauses pass; challenger clean (confirmed the correct two-part fix my #12097 recall predicted: capdef floor + emitter PSB OpExtension; new atom floor `_spirv_1_3` ≤1.4, no version regression via `Math::Max`, memory model stays Logical GLSL450); CI at head fully green incl decisive `check-cmdline-ref` + `check-capability-atoms-ref` + sanitizer; #11955 gcc-cpu flake passed this run. szihs APPROVE recorded as agreement-join. Both critique stages (DECISION_REVIEW, OUTPUT_REVIEW) codex-approved. **Shadow verdict now matches human maintainer gate — awaiting actual merge.** No dashboard ping until merge (one clean "shipped" ping, not enqueued-now + merged-later).

**⏳ Internal slang-reviewer A/B/C verdict (dispatched ~08:15Z) never relayed back as of 16:47Z (~8.5h).** OVERTAKEN BY EVENTS — maintainer approved + enqueued, so it's moot for THIS PR; advisory only. If it surfaces post-settle with a real latent finding → follow-up issue, do NOT block/reopen #12115. If it never reports, that's a reviewer-health check (separate), not this chain's concern.

**07-15 ~16:30Z: CI on 36d187dd53 — repeated aarch64 apt-network INFRA flake (NOT the code).** arm64 runners can't reach `ports.ubuntu.com` for X11 dev deps (`libx11-dev` etc.) → "Network unreachable / timed out", apt exit 100, BEFORE any compile. All x86_64/macOS/Windows GREEN. Round 1: build-linux-{debug,release}-gcc-aarch64 failed → rerun → builds green, then test-linux-release-gcc-aarch64/test-slang hit same apt failure → round 2 rerun (attempt 2/≤3). Fixer verdict: pure infra (capdef-only change, failures at dep-install step, different aarch64 job each round, everything else green). No code fix. **CONTINGENT: if round-2/3 reds again on aarch64 apt step → runner-network issue on arm64 pool worth flagging to maintainers (not a re-push fix). Fixer reports only if fails again.**

⚠ NOTE for close-out: earlier echoes (my #44 edit, triager #48 memory) recorded the atom as **EXT** — that was the transient first-hybrid value, now SUPERSEDED by **KHR** (`36d187dd53`). Re-read the actual PR diff at merge; don't trust the EXT spelling. Triager committed to diff-verify at close-out, so likely self-corrects.

**⏳ 07-15: peer review dispatched.** Fixer's own routing gate blocked a fresh `slang-reviewer` peer delegation (needs `in_reply_to`); orchestrator relaying — dispatched slang-reviewer directly on canonical thread to review PR #12115. **Draft PR → reviewer verdict is INTERNAL only (NOT authorized to post GitHub).** Verdict relays back to fixer via orchestrator; fixer runs max-2-round REQUEST_CHANGES/APPROVE path. **CI:** the visible red on #12115 is the cosmetic `workflow_dispatch` priority-yield (only `wait-for-human-priority`+`check-ci` "failed", all builds/tests SKIPPED) — NOT a real failure; `retry-yielded-bot-ci` reruns. See [[project_bot_pr_priority_yield_red_run]]. **TODO verify fixer called `report_pr_created({repo,pr:12115})`** [[feedback_verify_report_pr_created]].

**⚠ DISCREPANCY to reconcile on verdict refresh (fixer's finding, NOT yet verified by me):** fixer empirically found spirv-val does **NOT** reject SER@1.4 without PSB — contradicting the triage verdict's "CONFIRMED validation failure / spirv-val fails" claim in posted comment 4972430785. Fix still emits PSB (spec-correct + matches Approach A), so fix is correct regardless. When slang-triager refreshes the verdict (edit-in-place on 4972430785) after the [Fix Report], the "or spirv-val fails" overclaim should be SOFTENED to "spec-correct dependency" — don't leave an unreproduced validation-failure claim on the issue. Watch that the triager reconciles this; if not, flag it. See [[feedback_authorize_comment_matches_memo_hedging]].

**⚠ 07-14: handoff BOUNCED — slang-fixer container unauthenticated** (literal "Not logged in · Please run /login"). Fix NOT in progress. Triager queued the handoff+memo on canonical thread (NOT retrying, NOT authoring itself). Escalated fixer re-login to operator via orchestrator-dashboard. On recovery: fixer consumes queued memo (approach A, `Closes #12097`); ping slang-triager to forward [Fix Report] upstream. GitHub verdict comment (4972430785, "fix dispatched, draft held") NOT amended — transient infra, stays accurate. See [[project_slang_fixer_auth_outage]].

**⚠ DESIGN TENSION — sibling #12099 (same author, jkwak-work) proposes the OPPOSITE:** reject the conflicting `-profile spirv_1_4`/`-capability spvShaderInvocationReorderNV` combo instead of allowing 1.4. Mutually-exclusive directions for the SAME repro. Flagged on the #12097 issue; triager proceeding with #12097's stated preferred (allow-1.4) path since jkwak explicitly asked for a PR on #12097. NOT blocking (author owns both, can steer). **If #12099 activity or a maintainer decision on 12097-vs-12099 arrives, connect the two — one direction obsoletes the other.**
