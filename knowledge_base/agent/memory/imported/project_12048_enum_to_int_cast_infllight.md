---
name: project_12048_enum_to_int_cast_infllight
description: MERGED 07-13 — PR #12050 (skiminki-nv); approver WOULD_APPROVE matched human
metadata: 
  node_type: memory
  type: project
  originSessionId: cfcf4a4e-5244-42d9-83b6-81819372a1ae
---

IN-FLIGHT — shader-slang/slang#12048 "Enumeration to integer cast is broken" (skiminki-nv, Dev Opened).

**Confirmed root cause (triager, local build + DeepWiki):** `calcRequiredLoweringPassSet` (slang-emit.cpp:436-437) flags the `enumType` lowering pass ONLY on `kIROp_EnumType`, never on enum-*cast* opcodes. When an enum-typed local holds a compile-time const (`XYZ xyz = XYZ.One`), SSA promotion + const-folding delete the local AND the last `IREnumType`, stranding a degenerate `CastEnumToInt(1:UInt)`. Flag stays false → `lowerEnumType` (:1276-1277) skipped → cast survives to code emit → `E99999 unexpected IR opcode`.

**Scope:** all 7 targets (target-agnostic pre-scan defect). Discriminator = enum-typed local var holding a const; direct `(uint)XYZ.One` and runtime `(XYZ)tid.x` both work. bug / high / P2 / IR back-end pass gating. NOT a dup of #7908 or #12046 cluster.

**Fix A (recommended):** add `CastEnumToInt`/`CastIntToEnum`/`EnumCast` (+Constexpr variants) to the switch so `enumType` flags on any surviving enum op — mirrors `taggedUnion` precedent (:561-568). B (SCCP fold) + C (emitter patch) rejected as symptom/consumer-side.

**FIXED — draft PR #12050** (07-10). https://github.com/shader-slang/slang/pull/12050 — OPEN, isDraft=true, branch fix/issue-12048, head 576c5a9305, `Closes #12048`, `pr: non-breaking`. Fixer narrowed Approach A: EXCLUDED the `Constexpr*` cast variants (no case in `lowerEnumType::processInst`; SCCP-folded) to avoid a false-coverage gate — good tightening. Tests: revert-drill (un-patched → HLSL+GLSL direct-emit FAIL E99999; patched → all 4 pass); new `tests/language-feature/enums/enum-to-int-cast-local.slang` (SIMPLE hlsl+glsl + CPU COMPARE_COMPUTE); 37/37 enums green; SPIR-V validation passes; codex PLAN/CODE/OUTPUT all-approve. Triager verified all artifacts vs live GitHub. Issue footprint: fixer posted issuecomment-4937856675 (draft-PR trail on #12048) ✓.

**Draft CI red = cosmetic** — `workflow_dispatch` run 29111014977: only `wait-for-human-priority`+`check-ci` "failed", all build/test SKIPPED. Matches [[project_bot_pr_priority_yield_red_run]]. Not a functional blocker.

**TERMINAL (bot-state) 07-10 — PEER-REVIEWED ✅ APPROVE, awaiting operator ready-flip.** `report_pr_created(shader-slang/slang, 12050)` CONFIRMED (host: "#12050 mapped to this session" — no orphan risk) ✓. Bot peer review (slang-reviewer, read real source @ then-head 576c5a9305): single round, APPROVE, 0 critical/high. Both focus points confirmed: (a) opcode set COMPLETE — {CastEnumToInt, CastIntToEnum, EnumCast} == exactly what `lowerEnumType::processInst` handles; any *live* enum value keeps its IREnumType alive so the pre-existing `kIROp_EnumType` arm fires — #12048's folded-away-type-with-residual-cast is the only residual case, precisely what the PR adds. (b) `Constexpr*` exclusion SAFE — those arise only in const IntVal contexts (emitConstexprCast←visitTypeCastIntVal), never runtime, no `lowerEnumType` case; reviewer ENDORSED rejecting triage's "+for completeness." 2 advisory non-blocking: (1) comment/PR-body SCCP wording imprecise → FIXED (reworded to IntVal-provenance); (2) compile-only CastIntToEnum/EnumCast leg → DECLINED w/ IR-dump probe (int→enum→int fully folds, no residual cast — fold-away leg can't be built non-vacuously). PR amended to head 4c507cb94c (comment-only, byte-identical logic), verified live still isDraft=true, `Closes #12048`, `pr: non-breaking`. codex CODE_REVIEW re-approved amended diff; regression 4/4 green.

**UPDATE 07-13 09:41 — #12050 NON-DRAFT + APPROVED + CI running.** skiminki-nv (human, the issue REPORTER, author_association=CONTRIBUTOR — NOT collaborator/maintainer) flipped it ready-for-review at 09:41:19Z. **Verified via timeline `ready_for_review` actor = skiminki-nv, NOT nv-slang-bot → drafts-only guardrail INTACT** (legit human flip, not bot self-promote [[feedback_drafts_only_guardrail]]). reviewDecision=APPROVED (review 4683515549 by skiminki-nv, "LGTM if CI passes", self-resolved own inline). PR live: isDraft=false, mergeable=true, mergeable_state=**BEHIND** (needs update-branch before merge), head 4c507cb94c (= reviewed/approved commit), author nv-slang-bot[bot]. Real CI now RUNNING (stale draft workflow_dispatch replaced): several PASS (linux-aarch64 build+test-slang, macos-release build, board-sync), rest pending — green so far.

**Carries bot-reviewer APPROVE + contributor APPROVE.** Next: let CI finish → update-branch (BEHIND) → merge. Update-branch is a code push on fix/issue-12050, fixer's job (owns its branch [[feedback_let_fixer_own_single_session]]), route via triager when CI greens — NOT premature (mid-flight rebase just churns CI). Merge = operator/maintainer + CODEOWNERS gated (contributor LGTM ≠ CODEOWNERS approval). I have NOT flipped/merged/rebased. Chain bot-side complete; PR follow-up webhook-driven to fixer's session.

**TERMINAL — MERGED 07-13 15:39:28Z.** PR #12050 merged to master by skiminki-nv (human owned ready-flip AND merge — guardrails held end-to-end, bot never flipped/merged), merge commit `340a191c5`. Issue #12048 auto-closed `completed` 15:39:30Z via `Closes #12048` (verified: state=closed, state_reason=completed). CI went fully green 46/0 — the Windows-GPU red WAS the #12059-signature infra flake (cleared on auto-retry, never code, blast-radius excluded the enum gate — my flake read confirmed). Fix on origin/master: `340a191c5 Fix #12048: run lowerEnumType when only an enum cast survives (#12050)`. Merged head 4c507cb94c. Merged PR = persistent artifact (auto-closed issue); NO further issue comment needed. Worktree reap: fix/issue-12050 → owning fixer's job [[feedback_always_reap_merged_worktrees]] (fresh fixer session on #12050 processing merge webhook, likely self-reaps). CHAIN CLOSED.

**UPDATE 07-13 11:05 — CI NOT green (1 red leg) [historical, resolved by flake-retry above].** `gh pr checks 12050`: `test-windows-debug-cl-x86_64-gpu / test-slang` FAIL (37m), `check-ci` rollup FAIL (4s, reflects the red leg). ALL other legs PASS — every other test-slang platform (linux debug/release/aarch64/sm80/rhi, macos debug/rhi, cpu), builds (win/mac/linux/wasm), falcor, sanitizer, compile-regression, formatting, SlangPy tests. Single Windows-debug-GPU leg red while all-else-green = classic FLAKE signature (Windows debug GPU runner known-flaky), BUT flake-vs-real is the reviewer/CI-health call, NOT concluded here. **This red leg — not the BEHIND rebase — is the actual gating item before merge.** skiminki-nv's "LGTM if CI passes" is not yet satisfied. head 4c507cb94c.

**UPDATE 07-13 12:37 — pr_ready_for_review webhook → slang-pr-approver → WOULD_APPROVE (shadow, CLEAN).** Draft→ready flip (07-13 09:41 by skiminki-nv) fired a `pr_ready_for_review` webhook; routed to slang-pr-approver per policy (never reviewer/fixer). Approver ran full pipeline @ head **4c507cb94ca3**: production claude-code-action review SKIPS bot-authored `fix/issue-12048` branch (harvest exit 20), no CodeRabbit → **Devin-only tier**, mode live_late. Verdict **WOULD_APPROVE / CLEAN**: all 6 eligibility clauses pass; review-doc APPROVE (0 bugs/gaps/questions); challenger independently confirmed the fix in source (purely-additive stale-FALSE gate closure — `calcRequiredLoweringPassSet` adds {CastEnumToInt,CastIntToEnum,EnumCast} to the `enumType` arm so gate exactly matches `lowerEnumType`'s handled set; degenerate `CastEnumToInt(1:UInt)` lowers via `processEnumCast` to `outputBuffer[0]=1U`, no E99999; Constexpr* correctly excluded); both critique gates (DECISION_REVIEW, OUTPUT_REVIEW) approved. Decision **recorded to ledger** (shadow — nothing posts to GitHub; human skiminki-nv already APPROVED). Devin head-current ("Analysis is up to date"), 0 bugs/0 flags. Agreement join stamps on merge/close. **Merge still gated on:** Windows-debug-GPU red leg (flake-vs-real, CI-health/fixer call) + BEHIND rebase + CODEOWNERS/maintainer merge — all fixer/maintainer-owned, webhook-driven. Nothing further orchestrator-side.

**TERMINAL — MERGED 07-13 15:41.** `github.pr_merged` webhook: PR #12050 merged by **skiminki-nv** at merged head **4c507cb94ca3** — the *exact* approver decision commit, ZERO follow-up commits (no read/ship delta; the Windows-debug-GPU red leg cleared as flake/re-run before merge). Approver ran calibration: human APPROVED-equivalent stamped onto ledger row `shader-slang/slang#12050@4c507cb94ca3`; **agreement ✅ WOULD_APPROVE matched human merge**. #12048 RESOLVED. **Worktree reap dispatched to slang-fixer** (branch fix/issue-12048 — always reap MERGED via owning fixer [[feedback_always_reap_merged_worktrees]]; fleet disk pressure [[project_fleet_disk_capacity_wall_11969]]). Chain fully closed.
