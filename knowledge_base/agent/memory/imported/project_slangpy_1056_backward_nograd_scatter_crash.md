---
name: project-slangpy-1056-backward-nograd-scatter-crash
description: "slangpy#1056 CUDA illegal-address abort in backward w/ mixed requires_grad; sibling of"
metadata: 
  node_type: memory
  type: project
  originSessionId: f8cd49c6-d5cf-409f-902e-b6109909227f
---

**STATE (07-29 10:21Z): MAINTAINER DESIGN QUESTION OPEN — approach may pivot. Draft PR #1057 held; NOT yet promotable.** @ccummingsNV objected on #1057 (comment 5116171829): "Adding a potentially significant memory allocation to solve a null pointer seems overkill. Is there no way to deal with the null pointer correctly?" — valid pushback on the throwaway-zeroed-buffer fix (one primal-sized alloc wasted every backward). Fixer replied on GitHub (comment 5116413916, NO code change — maintainer's call): explained two-form fault (CUDA dangling `T*` scatter vs non-CUDA generation-time rejection at `has_grad_out and d_out is None`), conceded cost is real, and OFFERED an alloc-free alternative: **bind no-grad `IDiffTensor` inputs as `PrimalTensor`** (conforms to IDiffTensor, no `_grad_out`, grad-writes compile to no-ops) — fixes BOTH backends with no allocation. Caveat: not trivial — `requires_grad` isn't in the torch kernel signature (`[Dn,Sm]`), so backward kernels would need to specialize per grad-config (touches the #1054 cache-signature territory). Awaiting maintainer's approach choice; fixer webhook-driven, will act on reply. Main-verified: PR open/draft @ 10:20:51Z, assignee `szihs`, requested reviewers `szihs`+`NV-xiaoyongs` (human-review track engaged per jhelferty-nv routing). **Fixer owns this via triager peer-wire — do NOT double-dispatch/authorize past that edge; nothing owed from Main.**

--- prior (07-12 ~20:06Z): FIXED, on-GPU verified, PEER-APPROVED (APPROVE_WITH_NITS) — draft PR #1057 (`Closes #1056`); was awaiting promotion before ccummingsNV's design objection reopened the approach question. --- slangpy-reviewer returned ✅ APPROVE_WITH_NITS (0 bugs, 0 blocking gaps): 1 clarity nit fixed (a541eeb — docstring distinguishes CUDA abort vs non-CUDA TypeError pre-fix); 2 non-blocking Qs declined w/ justification (1D form = equivalent input-pair coverage; pooled-scratch = deferred Approach-B), reviewer concurred. **NOTE:** this is the internal COWORKER review verdict, NOT a GitHub review object — `get_pull_request_reviews` returns `[]` (drafts-only: reviewer produces verdict, doesn't APPROVE on GitHub). Main-verified PR #1057 via MCP: open, `draft:true`, updated 20:03Z (matches a541eeb nit commit), author `nv-slang-bot[bot]`, head `dev/slangpy-fixer/1056` (prod-instance → branch-fallback webhook routing to slangpy-fixer), PR body fix rationale matches triager report.

**CI on #1057 @ a541eeb: 1 red check = VERIFIED PRE-EXISTING FLAKE, NOT the fix.** `test_pytorch.py::test_tensor_interfaces[d3d12]` grad-deviation assertion (5120 passed / 1 failed, d3d12-only, build succeeded). Triager verified causal exclusion against source: failing assertion is on `weights.grad`/`biases.grad`/forward `y` (both params `requires_grad=True` → the path the fix does NOT touch; fix only alters the no-grad else-branch; sole no-grad input `x` not among asserted quantities). Same test green on CUDA+Vulkan same run + green on main @ base 2c8afea. Bot CAN'T `gh run rerun` (no repo admin) — clears on promotion's CI re-trigger or maintainer rerun. **When a human promotes #1057, the red d3d12 check is safe to disregard.** (Unverified side-note relayed by fixer: `test_tensor_interfaces` may lack the GFX-skip its sibling `test_tensor_arguments` carries — out-of-scope maintainer-awareness item, non-load-bearing.) CI note added to issuecomment-4952299566.

**Human-review track ENGAGED (07-12 ~20:xxZ):** maintainer `jhelferty-nv` commented on #1056 (comment 5049311904) "@szihs Can you review the bot PR?" — routing human review of #1057 to `szihs`. Bot NOT mentioned; human-to-human coordination, no bot action/GitHub-write owed (not a re-open — human-gated step proceeding as expected). If `szihs` later posts a **GitHub** review it WILL show in `get_pull_request_reviews` (unlike the internal coworker APPROVE_WITH_NITS verdict, which is not a GitHub review object) → that would be a real substantive signal to route to slangpy-fixer/reviewer. Fix built + reproduced on-GPU (L40S): unfixed tree aborts on yes/no & no/yes; post-fix all 3 matrix cases green on CUDA+Vulkan, native+fallback bridge (12/12); full test_torchintegration.py 342 passed. **Approach A is UNCONDITIONAL, NOT CUDA-gated** — codex advised an `is_cuda` gate but it regressed Vulkan (non-CUDA type-resolution *requires* a bound output-grad buffer, tensorcommon.py:153-161), so both backends need the scratch bind for opposite reasons. GitHub verdict PATCHed in place (issuecomment-4952299566). No further action owed from Main; resurfaces on reviewer verdict / new human comment / merge.

---
ORIGINAL TRIAGE (retained):
IN-FLIGHT chain: shader-slang/slangpy#1056 (tekintatar). `[Differentiable]` fn with ≥2 `IDiffTensor` params where only SOME torch inputs have `requires_grad=True` → `CUDA_ERROR_ILLEGAL_ADDRESS` process abort during `.backward()`. Matrix: yes/yes=OK, yes/no & no/yes=abort. Async fault surfaces at next stream sync (author first hit it in Adam `optimizer.step()`).

Root cause (triager-confirmed against source FOUR ways: own read + 2 subagents + fixer's independent pass; Main-verified issue open + 0 prior comments via MCP read): compiled backward kernel scatters `_grad_out` UNCONDITIONALLY per `IDiffTensor` param (difftensor.slang:130-147; CUDA atomic target = raw `T*`, atomics.slang:106-109), but backward dispatch binds a grad buffer ONLY for `requires_grad=True` inputs (`autograd_backward` sets `pair->grad=None`, slangpy.cpp:568-577 → `has_grad=false`, grad_out fields left unwritten, slangpytorchtensor.cpp:297-301,397-407). No-grad param's `_grad_out` = dangling/zero device ptr → atomic scatter faults. Python guard is CUDA-gated OFF (tensorcommon.py:153). Key verification anchors (survived fixer's DeepWiki-based challenge that a no-grad input binds a grad-less PrimalTensor — that describes the PRIMAL pass, not backward): `IDiffTensor→DiffTensor` resolution is keyed on CallMode ONLY (tensorcommon.py:169-173) so in backward EVERY IDiffTensor → DiffTensor w/ `_grad_out`; `has_grad_fields` set solely from Slang param type name = per-PARAMETER not per-tensor (slangpytensor.cpp:161-205). **RESIDUAL — CLOSED:** on-GPU shader-dump confirmed the no-grad input is typed `DiffTensor` flowing through `bwd_diff` with an unbound `_grad_out` (hypothesis→fact). Scope: torch-only is correct — plain-`Tensor`/`DiffTensorView` path confirmed non-vulnerable (`SGL_THROW`s loudly on missing grad_out, no fault); un-gating tensorcommon.py:153 CUDA guard = optional Python-ergonomics follow-up, NOT #1056 exposure.

VERIFIED DISTINCT from [[project_slangpy_1052_autograd_cache_grad_bit]]: #1052 = silent hook-drop (no crash); PR #1054 = routing-only cache-key `[Dn,Sm]→[Dn,Sm,Gk]` (kernel/binding UNCHANGED). #1056 crashes on FIRST call in fresh process → no cache entry to mis-key → #1054 leaves path exposed. Do NOT close as dup; do NOT mark fixed-by #1054.

Classification: bug/critical/P1/torch-integration (native torch-bridge)/CUDA.

**Why:** common "frozen input + learnable param" pattern crashes by default; author gave clean repro + workaround (`t.detach().requires_grad_(True)`).

**How to apply:** Recommended fix = Approach A (bind zeroed scratch `_grad_out` for no-grad `IDiffTensor` inputs at backward dispatch — internal equivalent of workaround; still report `None` to torch for that leaf). No kernel/ABI change. Regression test all 3 matrix cases on CUDA via `torch_bridge_mode` fixture (dual-bridge, per [[project_slangpy_1052_autograd_cache_grad_bit]]). Triager→fixer peer-wired: do NOT double-dispatch to fixer, do NOT authorize past triager ([[feedback_no_double_dispatch_peer_wired]], [[feedback_route_authorizations_through_dispatch_owner]]). Drafts-only ([[feedback_drafts_only_guardrail]]). GitHub artifact: issuecomment-4952299566 (nv-slang-bot triage verdict). Awaiting fixer [Fix Report]; triager forwards [Triage Resolution] upstream.
