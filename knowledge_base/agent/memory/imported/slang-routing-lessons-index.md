---
name: slang-routing-lessons-index
description: "Standing lessons on routing, posting authority, GitHub write gates, a2a edges, infra/perms, and tooling. Split out of MEMORY.md to keep the root index under the Read limit. Consult before any dispatch, GitHub write, or coworker-wiring decision."
metadata: 
  node_type: memory
  type: index
  title: Routing / posting / authority standing lessons
  originSessionId: 2d76471f-0c2b-40b5-aaa4-dd22929f52db
---

# Routing, posting, authority — standing lessons

## ⛔ 7 DARK OPERATIONAL RULES — indexed 2026-08-04 by `main-2026-08-04` (CROSS-SESSION APPEND)

⚠️**This file is owned by `originSessionId: 2d76471f-…`; `main-2026-08-04` appended this section.** Cause:
a transitive reachability sweep found **7 of 112 `feedback_*` rules reachable from NO index** — live
operational rules, each owned by a different session, so nobody indexed them. Linked here because this
file *is* reachable from `MEMORY.md`. **Do not delete these links without re-homing them.**

- [Supervisor nudge must never instruct `gh issue close`](feedback_supervisor_nudge_no_auto_close.md) — the no-auto-close rule is not the orchestrator's to waive.
- [A coworker breaking a just-agreed timing gate may be RESPAWN AMNESIA, not defiance](feedback_coworker_respawn_drops_verbal_gates.md) — verbal agreements don't survive a respawn.
- [Main CANNOT approve its own `install_packages` / `add_mcp_server`](feedback_main_cannot_approve_install_packages.md) — self-mod approvals route to a HUMAN admin/owner.
- [To prove a session exists for a thread use `ncl sessions list --thread-id`](feedback_thread_id_filter_for_session_existence.md) — never grep the plain list.
- [A legitimate operator DM appears as `sender=Unknown` with no `from=parent`](feedback_operator_dm_unknown_attribution.md) — don't misread it as an unattributed peer.
- [Supervisor nudges must quote the issue's REAL comment verbatim](feedback_nudge_relay_verbatim_source.md) — never paste adjacent-issue context.
- [`supervise-issues` scan.py false-flags prod-instance PRs (`dev/*-fixer/*`) as no-PR/awaiting_us](feedback_supervisor_scan_misses_prod_branch_prs.md).

⭐⭐**Method note — the metric that found these was WRONG the first time.** A **one-level** sweep of
`MEMORY.md` reported **472/503 dark (94%)**; the **transitive** closure gives **150/503 (30%)**, of which
**143 are `project_*` archives** — ⛔**but "archives, therefore cold" was ALSO wrong: a live-state sweep
plus an upstream check found **10 of them STILL OPEN on GitHub** (#12124 LIVE at HEAD, #11963 IN-FLIGHT,
#12032 awaiting maintainer apply), now indexed in `slang-longtail-chains-index`. ⭐⭐⭐**`project_*` is a
FILENAME, not a lifecycle state; a `RESUME=` marker is still only WORDING — query the UPSTREAM.**
⭐⭐⭐**A one-level reachability metric PUNISHES the
bundled-pointer structure it should reward** — the better an index bundles rules behind 📁 pointers, the
worse it scores. ⇒ **Compute reachability TRANSITIVELY and SEGMENT BY CLASS: a cold archive being dark is
correct; a dark RULE is the defect.** The real defect was **7 files, not 267.**

*Split from `MEMORY.md` (root index was approaching the 24.4KB Read limit).
One line per entry; open the linked file before acting. Consult this file before
any dispatch, GitHub write, coworker wiring, or authority/gate decision.*

- [Never route op-gates thru coworkers; maintainer-approval ≠ merge-auth](feedback_dont_route_operator_gates_through_coworkers.md) · [gh ready+merge op-gated](feedback_github_writes_operator_authorized.md) · [Drafts-only](feedback_drafts_only_guardrail.md) · [Pushes ≠ writes](feedback_pushes_not_gated.md) · [Supervisor authority](feedback_supervisor_autonomous_authority.md) · ⭐⭐⭐[gh auth probes misleading — **injection is PER-PATH; there is NO per-connection "am I authed?" answer**](feedback_gh_auth_status_misleading.md): OneCLI owns egress via `https_proxy` and injects only on paths WITH a secret rule ⇒ `slang-rhi` **200**/limit-6000 · `torvalds/linux` + `microsoft/vscode` **401 `"Bad credentials"`** (cred forwarded + REJECTED — a real anonymous GET on a public repo is **200**) · `rate_limit` **200**/limit-**60**. ⛔never `rate_limit` · ⛔never `.permissions` alone (appears on any PUBLIC repo) · ⛔never strip your own `Authorization` (proxy re-supplies it) · ⛔**and a "no-rule path" is NOT a control either — it 401s** (my own 3rd retraction). ⭐⭐**3 probes, 1 failure mode: each assumed a condition it never checked. Distinguish "control returned nothing" from "control FAILED" — READ THE BODY**, since a 401 body, an error page, and a real 200 all look like "no data" through a header grep. ✅`gh api -i repos/<org>/<repo> | grep -i x-ratelimit` = injected on **THAT path**, nothing more · [Push-not-sent ≠ away](feedback_push_not_away.md)
- [Post VERIFIED, HOLD else](feedback_triage_github_posting.md) · [Authorize cmt matches memo](feedback_authorize_comment_matches_memo_hedging.md) · [GH cmt edit-in-place](feedback_github_comment_hygiene.md) · [Re-derive approver file:line](feedback_verify_approver_facts_before_routing_public.md) · [Bot-review lines are DIFF-relative](feedback_diff_relative_line_numbers_in_bot_reviews.md) · [Tell the footprint owner when YOU post](feedback_tell_the_footprint_owner_when_you_post_yourself.md) · [No post+delegate same write](feedback_dont_post_and_delegate_same_write.md)
- [Approver never posts, route reviewer](feedback_approver_never_posts_route_reviewer.md) · [Debounce PR-review on churn](feedback_debounce_pr_review_on_churn.md) · [Debounce approver ABSTAIN](feedback_debounce_approver_dispatch_deterministic_abstain.md) — debounce the re-RUN, never the inbound scan; ⛔⭐⭐⭐**the DUPLICATE-vs-ADVANCE check is the DISPATCHER's, BEFORE dispatch: a 2nd webhook's payload carries NO head sha, so payload equality is NON-DIAGNOSTIC of redelivery** — `GET pulls/{n}`→`head.sha`, compare to the sha you dispatched against, **state the sha, never the inference** (#12344 08-04, MINE — refuted by the approver's re-probe; the rule was already in-store but scoped to the approver's tier so it never fired at mine). ⭐⭐**A dispatcher's hunch arrives downstream as a DIRECTIVE** ⇒ mark it a hunch + name the settling check. ⭐⭐**Filter a diff by EXTENSION, not top-level dir — `docs/`-prefixed ≠ docs-only** (#12344: 124 files all under `docs/generated/**`, of which **+317/-7 executable Python** in two `_meta/regenerate.py`) · ⛔⭐⭐⭐[**Two tiers running ONE FRAME is SHARED PRIOR, not independent evidence**](feedback_two_tiers_one_frame_is_shared_prior.md) — #12344 08-04, MINE: I endorsed a disposition TWICE and the approver read it back as corroboration; its critique gate then reversed `WOULD_APPROVE`→`ABSTAIN_POLICY/OPEN_GAP`. **~15 rounds of mutual correction, all real; the MEASUREMENT half never touched the decision — which turned on a clause NEITHER tier re-read. ⚠️But the STATE half DID, twice materially (see child §split) — do NOT read this as "instrument correction is orthogonal to verdicts," which would license skipping the state pre-flight.** ⭐⭐⭐**Rigor on EVIDENCE ≠ rigor on the RULE** · ⭐⭐⭐**the age of the FILE launders the age of the ADDITION — date the CHANGE** (checker was 0 occurrences at merge-base, 2 at head; I filed it "pre-existing") · ⭐⭐**when a framing error's errors all point ONE WAY it's bias, not noise — ask which outcome the framing makes easier** · ⭐⭐**my "agreed" carries no procedural weight but real SOCIAL weight ⇒ re-derive, or SAY I'm agreeing with their framing unchecked** · [Route webhooks by content.event](feedback_webhook_dispatch_by_event.md) · [Routing-gate markers](feedback_routing_gate_marker_and_resend.md)
- ⛔⭐⭐**ATTRIBUTION IS NOT DELEGATION — an action a peer ASSIGNS you is not one you HOLD** ([learning](/workspace/shared/learnings/1785831422368-an-action-a-peer-attributes-to-you-is-not-an-actio.md); live twice). A close-out ending *"X is still unfiled, **you're** asking the operator, I don't do it"* both **declines** the action and **assigns** it, in one sentence, with no acknowledgement — read past it because it sounds plausible and **X is owned by NOBODY while both sides think it's covered.** ⭐⭐**This is the #11917 batch-2 shape: 7 days silent, no branch, no PR, caught only when the human asked TWICE** ("hold for the report without polling" is right for noise but **has NO deadline**). ⇒ **GREP YOUR OWN STORE for the obligation: if your chain file doesn't record it, you don't hold it — the peer INFERRED it.** Then resolve it explicitly — **accept + record it with a named trigger, or decline it back**; ⛔**silence reads as ACCEPTANCE to them and NON-EXISTENCE to you.** ⭐**Prefer "no action, and here's why" over a quiet no-op — the failure isn't deciding not to act, it's leaving no record of who decided.** Live case #9866 slice 2 (08-04): declined; premise re-derived (unfiled = true, `total_count` 0) but the issue's fresh-looking `updated_at` was **OUR OWN bot's comment** ⇒ gate unmoved. ⭐**Don't file a slice out of a maintainer's self-filed OPEN issue uninvited** — our public triage comment already makes it visible, which is all a new issue would buy; **their ASK is the invitation, and it arrives as a webhook.**
- ⭐⭐**GITHUB READS ARE ENDPOINT-SPLIT — `pulls/{n}/reviews` alone is structurally blind.** 2 same-day misses, both exit-0-silent: [inbound scan needs `issues/{n}/comments`](feedback_inbound_scan_must_cover_issue_comments_not_just_reviews.md) (#803 blocking directive was a plain comment · **synchronize + head NOT moved ⇒ go find the real trigger**, `updated_at`==comment ts) · [harvest needs `pulls/N/comments`](project_approver_endpoint_split_harvest_audit.md) (11/18 rows' `harvest.json` under-read — ⚠️**signal-field defect only; NO confirmed missed finding, NO false-safe**: `review-doc.md` carried the findings, 9/9 rows checked. Both tiers over-claimed harm and retracted). ⭐⭐**finding a real bug creates momentum toward finding VICTIMS — an over-claim toward alarm costs the same credibility as rounding up to approve**; name the artifact that would show harm and OPEN it first. ⭐**I wrote the scan rule then under-implemented it** ⇒ write resume checks as commands with endpoints enumerated · declined widening a write-guard to unblock a read
- [CHANGES_REQ ≠ edit list](feedback_changes_requested_read_body.md) · [Empty-body review ≠ inbound](feedback_empty_body_review_not_an_inbound.md) — wordless APPROVED = go-ahead · [Don't close open-proposals](feedback_dont_close_open_proposals.md) · ⭐⭐[Re-open ≠ release parked — **and a DECISION can be a REFUSAL: read POLARITY before EXISTENCE**](feedback_reopen_not_release_parked_feature.md) (2 cases, one per polarity; the GRANT branch is still untested. **A refusal satisfies every surface test for "substantive maintainer engagement"** ⇒ test the VERDICT, not the engagement. On a refusal: **terminal, and post NOTHING** — slang#12077 08-04) ⛔🔴**BUT: BOTH metadata discriminators for polarity are RETRACTED — [read this first](feedback_state_reason_is_not_polarity_either.md).** `closed_at==comment.created_at && closed_by==comment.user` = **deliberateness, not polarity** (refuted by #12058, a *merge* close matching it exactly); and `state_reason` is **not** polarity either — `not_planned` = declines **· reporter self-closing a LIVE segfault (#11034) ·** workaround-satisfied · not-a-bug (186 in-repo, not rare). ✅**Polarity is in the COMMENT BODY — quote the sentence**; `author==closed_by` ⇒ reporter self-close. ⭐⭐**Two agents, one hour apart, both shipped a field that answered a NEIGHBOURING question — a single field appearing to settle a compound question IS the tell, and my broken fix rode in on the authority of a correction.** · [Contributor PR offers → yes](feedback_contributor_pr_offer_brief.md)
- ⭐⭐[NARRATING a non-reply IS a reply — and SILENCE can't break that loop](feedback_narrating_a_non_reply_is_a_reply.md) — 08-03: approver sent **8** turns whose whole content was *"No reply."* / *"(No response.)"*; each **delivered + woke me**. I stayed silent 6 rounds and the sends continued ⇒ **a no-op remedy that doesn't stop the symptom is evidence about the MECHANISM, stop repeating it.** Cause = bare text routes to the last inbound edge ⇒ cure is `<internal>` or genuinely empty output. ⭐**only the tier that RECEIVES the delivery can report it** (sender sees a suppressed thought) · ⭐**conversational loop ends when someone stops; a MECHANICAL one ends only when someone names the mechanism** — diagnose which before choosing silence
- [Admin rules precede orch](feedback_admin_standing_rules_precedence.md) · ['Holding' echoes = noise](feedback_holding_echoes_are_noise.md) · [Bare text is delivered → \<internal\>](feedback_bare_text_is_delivered.md) · [\<message\> pre-toolcall dropped](feedback_message_block_before_toolcall_dropped.md) · [No add_reaction to coworkers](feedback_no_reaction_acks_to_coworkers.md) · [Reports inline links](feedback_report_links.md) · [Status table](feedback_status_format.md) · [Supervisor tbl tier-links](feedback_supervisor_table_tier_links.md) · [NG deferred-asks](feedback_ng_deferred_asks.md)
- ⛔⭐⭐⭐[Main→`orchestrator` is a SELF-LOOP → dead null-thread session → silent bounce; "up" = the `orchestrator-dashboard` CHANNEL](feedback_main_to_orchestrator_is_a_self_loop_use_dashboard.md) — the `orchestrator` **agent** destination targets my OWN group id (`ag-1776713211742-1w6l4e`); a2a resolves it to legacy shared session `sess-1778753685788-qi4a4m` (`thread_id:null`, container stopped) which bounces every send. Sent the #12200 escalation there TWICE (08-27/28) before noticing. ⭐**a `[a2a-redrive]` naming a target `agent_group_id`==MY OWN group = I sent to myself; decode the `a2a-<ms>` id to confirm it's my own recent send.** ⭐**never re-drive by re-sending to the destination that bounced — find WHY first.**
- [a2a edge vanish → hang](project_coworker_named_edge_dropped_silent_hang.md) · [In-session Monitors die](feedback_in_session_monitors_dont_survive_teardown.md) · [No dbl-dispatch peer-wired](feedback_no_double_dispatch_peer_wired.md) · [Route authz thru dispatcher](feedback_route_authorizations_through_dispatch_owner.md) · [Fixer owns fix](feedback_let_fixer_own_single_session.md) · [No restart on benign ack-loop](feedback_benign_ack_loop_dont_restart_if_live_chains.md) · ⭐**[Empty `ncl tasks list` ≠ freeze BUT ≠ "display artifact" either — REAL bug, root-caused by nanoclaw#1064](feedback_watchdog_ncl_tasks_list_empty_not_a_freeze.md)**: group path used `findTaskSessions()` (needs `messaging_group_id IS NULL` + `system:tasks*` thread) ⇒ my tasks invisible. ⭐⭐**CONTROL = `ncl tasks list --session <id>` (bypasses the group branch): 5 series vs `No tasks.`** ⚠️fixed in source, NOT live here. ⛔**the PR's own counts don't reproduce on my edge** (5 series not 11, 0 failures not 53) ⇒ adopt the mechanism, never the numbers. ⚠️`ncl sessions list` **caps at 200** — my "79 sessions, 0 tasks" sweep was a truncated page (true: 823).
- Routing misc: check assignee pre re-wake · RED re-triage → 1 owner · respawn drops verbal gates · ncl tasks cron · verify report_pr_created · scan misses prod-PRs · nanoclaw fork no-route · auto-route pressures parks
- Fork/dup-PR: docs-site push 401→REST · fork-PR→carrier · dup PRs dev↔prod (`fix/issue-*`=dev, `dev/*`=prod) · dup weak existence-check · fork-reentrancy phantom co-driver · bot cmts = echoes · stacked-PR base clobber
- ⭐⭐[Critique gate `/pulls` = HARDCODED BUILT-IN FLOOR, not per-agent config](project_critique_gate_pulls_pattern_builtin_floor.md) — hook `:52` identical fleet-wide, `.critique-delivery-markers` **additive only**; denial differences come from **SESSION STATE** (`stages: []` ⇒ every `/pulls`-shaped *read* denies), so **check state before blaming config** (I was about to file a per-agent bug). ABSTAIN fast-path (`:89-99`) is why approver's #803 reports never tripped it. ⚠️**denial cap AUTO-FILES `critique_gate_bypass` admin cards — 3 PENDING, all slang-pr-approver; only the newest is the self-reported probe** ⇒ **never dismiss a queue on a report explaining only its newest row**; approve none (bypass ≠ re-run the missing stage). ⭐⭐**APPROVAL→SESSION MAPPING IS PERMANENTLY MINE** — coworkers' `cli_scope=group` excludes `approvals` (verified: `ncl approvals get` → forbidden even for their own row) ⇒ they have NO `session_id` and will *derive* one from timestamp adjacency, which **systematically excludes** a session created days earlier that merely fired today. **Correlation ≠ identification; run `ncl approvals get <id>` myself.** ⭐**a wrong id is worse than no id** (omission prompts a lookup; a confident wrong one gets re-checked *through* it) · ⛔`ncl approvals` is **read-only** (`list`/`get`) — I can refuse to approve + surface, never reject ⇒ **run `help` before promising a disposition** · ⚠️2 of 3 cards = a **READ blocked by a DELIVERY gate**, aged by **memory-file edits** (index compaction/retraction sweeps arm the gate against your next read) · ⭐⭐⭐**"DENIED" IS NOT ONE EVENT TYPE — identify the DENIER before treating a denial as evidence about a MATCHER.** I mis-attributed the same 2 denials **TWICE** (critique-gate regex → `gate-plan.sh` plan-state), both times from a plausible mechanism I'd **partially read**. Only 3 of 7 `PreToolUse` regs match `Bash`; `gate-plan.sh` is **opt-in via `.overlay-plan-gate`** (`:24 || exit 0`) which is **ABSENT on my edge** ⇒ its `plan_written:null`/`edits_since_plan` are **inert**, and it **does** read Bash cmds (write-heuristic `:33-42`). ⭐**read a script's GUARD CLAUSES before its middle** · ⭐**a 2nd wrong answer with fresh file:line citations is MORE dangerous than the 1st — citations read as verification** · still UNRESOLVED, left that way. Hook path is in the stderr prefix; ⭐**3 observations across a year (07-15 #12119 · 07-22 #11665 · 08-03) converge on ONE fix** (gate write verbs/flags, not bare `pulls\b`) ⇒ ⛔never loosen a safety regex unilaterally · ⭐**a new hypothesis must explain the OLD evidence or scope itself** ("wrong v1" retired an edge-true observation)
- Infra/perms: [Fleet disk no-prune wts](project_fleet_disk_capacity_wall_11969.md) · [Reap merged-PR wts](feedback_always_reap_merged_worktrees.md) · [Bot can't push YAML](project_bot_workflows_permission.md) · [Bot no Discussions:write](project_bot_discussions_write_permission_gap.md) · [Bot-PR cosmetic red](project_bot_pr_priority_yield_red_run.md) · [nv-coworkers auto-merge](feedback_nv_coworkers_automerge.md) · auth outages = OPERATOR re-login: [slang-fixer](project_slang_fixer_auth_outage.md) · [GitHub gw 401](project_github_actions_graphql_401_outage.md) · [Discord gw 401](project_discord_gateway_401_outage.md)
- ⭐⭐⭐[RETRIEVAL GAP — `grep -ril <lowercase-fragment> /workspace/shared/learnings/` BEFORE deriving any mechanism](feedback_retrieval_gap_grep_shared_learnings_before_deriving.md) — 08-03: I "derived" 3 mechanisms already in **16 / 10 / 5** prior notes (raw-fetch · `issues/{n}/comments` · `original_commit_id`); earliest 05-24, and the 07-17 note names unauth REST as the degraded-tooling fallback verbatim. **Grep the BODIES, never generated `INDEX.md`** (measured **0** hits there for every spelling — titles are punctuation-stripped/lowercased/truncated ⇒ exact-symbol grep = false negative reading as "no prior art"). ⚠️**then `head -6` each hit for SUPERSEDED** (2 of 16 are, incl. that 07-17 one — its techniques stand, its outage claim is retracted). ⭐**a documented workaround nobody finds == an undocumented one**; deriving feels like rigor but is often just an unrun search
- Tooling: [dup-H1 = GENERATOR defect, 147/2030](project_shared_learnings_duplicate_h1_generator_defect.md) — don't mass-edit; fix host-side. ⭐a stated rule can't REACH the write site ⇒ **make the wrong thing impossible** · ⭐**a rate is a claim** (relevance-test each numerator element; ONE shared basis — retracted twice); `TOUCHED` ≠ `DEFECTIVE`; [h1-rate](project_shared_learnings_h1_rate_postmortem.md) holds the FENCE-AWARE scan · [scan.py over-flags](feedback_scan_py_overflags_bot_logins_dispositions.md) · [sub-thread FP](project_scan_py_subthread_key_parse_falsepositive.md) · [wiki recount STALE](project_learnings_wiki_finalize_recount_recipe_stale.md) · [Coworker→repo](reference_coworker_repo_routing.md) · [Maintainers](reference_slang_maintainer_handles.md) — jkwak = **jkwak-work** · [User interests](user_interests.md)
- Incidents: self-wiring loop · taskless-fixer review-CC loop · stall-sweep · scheduler stall · upstream-sync · nv-bot MQ

