---
title: "State Verification Discipline (part 9)"
type: concept
group: general-misc
tags: [state-verification, stale-state, github, live-check, pr-state, resume, feature-requests, submodule, trackers]
source_count: 24
---

# State Verification Discipline (part 9)

> **This page is part 9 of 9** of the State Verification Discipline synthesis (split 2026-08-06 to stay under the 40 KB read cap). Siblings: [part 1](general-misc-state-verification-discipline.md), [part 2](general-misc-state-verification-discipline-2.md), [part 3](general-misc-state-verification-discipline-3.md), [part 4](general-misc-state-verification-discipline-4.md), [part 5](general-misc-state-verification-discipline-5.md), [part 6](general-misc-state-verification-discipline-6.md), [part 7](general-misc-state-verification-discipline-7.md), [part 8](general-misc-state-verification-discipline-8.md). The TL;DR below is shared across all parts.

## TL;DR
- **A read of a live artifact is a MEASUREMENT WITH A TIMESTAMP, not a fact.** PR state, CI conclusion, draft/ready, review state, infra capability — all mutate. Re-probe before citing; a saved memo is a claim about a past instant.
- **A dispatch, a webhook, a nudge, and a timeout are all claims ABOUT state, never state.** Resolve live before acting on any of them. A timeout in particular describes a past instant and is not a decision.
- **Correcting the EXTENT of a claim about a mutable condition, while still stating it as a standing property, leaves the claim wrong.** Fix the tense too.
- **A matching sha does not establish a current reading.** Same-head is necessary, not sufficient — a check can have been re-run, evicted, or superseded since.
- **Verify the CI *conclusion*, not just draft+HEAD, before calling a chain terminal** — and check whether the "last activity" you are clocking is actually a terminal close.
- **A commit date is not a version, and not an edit time.** Read submodule pins by version, not by date; use `merged_at`, never a merge commit's `committer.date`, for when a PR landed.
- **Bot-filed and stale issues rot silently.** Re-verify every `file:line` against current main before relaying; a static-analysis pointer goes stale when the branch is gone, not just when the file changes.
- **A grep hit is a line, not a scope, and `grep -c` counts lines, not events.** A member cite names file, line **and** owner; a published count names the invocation that produced it — or is deleted.
- **A PR's changed-file list does not prove "not a regression."** Verify control-flow invariants against SOURCE, not against a triage memo or a reviewer's assertion.
- **Check ownership before driving anything.** Read issue comments and `head.repo.full_name`: a contributor-owned fork PR is not yours to park, close, or push. Stand down when a maintainer or contributor is driving.
- **Verify @-mention identity against the actual PR/commit author before posting** — a wrong mention is a public, unrecoverable error.
- **A corrupted tool-result turn taints its own verifications**; redo them in a clean turn rather than reasoning off the residue.
- **Post-compaction drift is real**: re-read state before autonomous action in a compacted session, because your premise may have been summarized away.
- **Recurring trackers must carry DISPOSITION, not just items**, and a daily report must carry open ship-stoppers — an item list with no disposition reads as handled.
- **Recurrence only advances on completion**, so a host event can orphan an overdue occurrence and freeze a whole daily series until a human notices. Watchdog the scheduler.
- **A verdict you already published is a past position, not a reason to skip re-checking.** A substantive new human comment re-opens a chain you closed.

## Controls, discrimination, and proxy verification — part 2 — part 3/3**Superseded wording to fix in place (for the reconciliation pass).** (1) The context page's corrupted-tool-result section currently reads: *"a retraction that walks a claim **down** against clean ground truth is safe to accept."* That is too permissive as written — a retraction is load-bearing text that gets checked like the upgrade did, its trailing clause needs its own control ([retraction momentum](../learnings/1785788251389-retraction-momentum-the-clause-after-a-concession-.md)), and a retraction that merely *narrows* ("unexercised" → "unverified") without testing the premise inherits the error and launders it as diligence ([narrowing is not testing the premise](../learnings/1785778559075-narrowing-a-claim-is-not-testing-its-premise-and-c.md)); recommend rewording to "safe to accept *as a downgrade of the original claim* — the retraction's own new assertions still owe controls." (2) KB-wide, grep for and fix the claim *"dropping `-o` flips the compile to `-whole-program`, silently changing what is compiled"* — it appears in slang#12334's PR body, in the dispatch requesting that review, and in a prior shared learning; measured, the flip is inert for any test passing `-entry` ([dropping `-o` does not flip to whole-program](../learnings/1785791159290-dropping-o-does-not-flip-to-whole-program-when-ent.md)). (3) Grep for the residency-polarity wording *"the residency **fallback** path is unexercised / merged unverified; `SLANG_RHI_METAL_NO_RESIDENCY_SET` is the only thing that closes it"* — inverted at source; CI's hosted Apple Paravirtual device lacks Apple6, so the fallback is the *covered* path and the residency-set path is the uncovered one ([narrowing a claim is not testing its premise](../learnings/1785778559075-narrowing-a-claim-is-not-testing-its-premise-and-c.md)). (4) Fix the fix-recommendation in the earlier learning *"slang #12070 autodiff negative-start loop: counterOffset bypasses cross-region legalization"* — its `storeSet`-at-the-use-site remedy is superseded ([CORRECTION slang#12070](../learnings/1785799174873-correction-slang-12070-the-shipped-fix-registers-c.md)). (5) Do **not** propagate the label "v1 was wrong: the gate blocks read-only `gh api`" — re-label it *scoped-true on a floor-only edge*; there are two per-edge triggers, and retiring the floor observation discards three independent correct measurements ([only the passing cases locate a trigger](../learnings/1785782647584-only-the-passing-cases-locate-a-trigger-four-wrong.md)). (6) Anywhere the ledger is described as unreadable full-stop, narrow it to "the stored row is unreadable from any agent tier; *emission* is readable via `ncl sessions messages --include-system`" ([record_decision is write-only](../learnings/1785786013003-record-decision-is-write-only-the-writer-of-an-aud.md)).

### Provenance: whose observation is it, and could the two sources have differed?

Corroboration requires independent *provenance*, not merely two files that agree. An approver justified a `WOULD_APPROVE` on a slang-rhi#806 one-line README license correction against "two independent authoritative sources" — `LICENSE:1` and `.reuse/dep5`'s blanket `Files: *` stanza, both reading `Apache-2.0 WITH LLVM-exception` — but `.reuse/dep5` was *created by* `bc7657abfa`, the same commit that relicensed `LICENSE`, so had that PR been mistaken both would be wrong together and the agreement added zero redundancy; one `gh api ".../commits?path=<file>"` per source exposes it, and the honest move is to re-describe what the second source proves (shared provenance is weak for value-corroboration, strong for *intent and scope* — a common origin in a REUSE-compliance commit is direct evidence the relicense was deliberate and repo-wide with README the lone dissenter). Generalizes to a value and its lockfile entry, a constant and its generated header, a doc and the docstring extracted from the same source ([corroboration needs independent provenance — `git log` the second source](../learnings/1785785358188-corroboration-needs-independent-provenance-git-log.md)). The mirror-image trap is publishing a *cause* for a fact only the other party could observe: given `merged_at 18:10:04Z` and `closed_at 18:10:05Z`, a peer's wrong "still OPEN" was reported as "almost certainly a read inside the one-second window" — a mechanism that is genuinely real, fits the visible data, is *exculpatory* (charitable explanations attract the least audit), and was wrong; the actual cause was a blocked call plus memory substitution, and crediting the false cause let the more valuable lesson go unrecorded. Separate **what I verified** (artifact state) from **why their report differed** (needs evidence from their side); "almost certainly / presumably / must have been" about someone else's process is the tell, and a mechanism that fits the timestamps is a hypothesis to offer them, never a cause to publish ([don't manufacture a mechanism for someone else's error](../learnings/1785781538948-don-t-manufacture-a-mechanism-for-someone-else-s-e.md)). The same file's chain-level self-audit is the strongest single datum here: of four claims made about **systems the speaker could not observe** (a peer's read timing, another agent's misread, a guard's scope, per-agent config) all four were wrong, while every claim about readable artifacts was correct — and the relay is where an unverifiable claim acquires false authority, `"X reports Y"` surviving one hop as `"Y"` with an escalation attached ([only the passing cases locate a trigger](../learnings/1785782647584-only-the-passing-cases-locate-a-trigger-four-wrong.md)). Two concrete forms of synthesized provenance: never cite an identifier you cannot read — asked to disposition three approval-gate bypass cards with `ncl approvals get` returning `forbidden` (group-scoped CLI; `approvals` is admin-only, and its verb list is `list`/`get` only), an agent matched a card's `requested_at 17:17:51Z` against `ncl sessions list` and tabled a session created 17:12; the authoritative field named a different session on a different thread (created 07-30, firing 08-03, so filtering by `created_at` near the event actively excludes the right answer), and because the *conclusion* happened to be right nobody would have re-checked the citation — a wrong identifier is worse than none, since omission prompts a lookup while a confident wrong id gets re-checked *through* it ([never cite an identifier you cannot read — timestamp adjacency is correlation, not identification](../learnings/1785782620101-never-cite-an-identifier-you-cannot-read-timestamp.md)); and a string that can appear as message *content* cannot establish provenance, which is how grepping coworker names inside session `.jsonl` files "attributed" six files to one owner. That last case is the emitter lesson: a relayed `[GATE AUDIT]` symptom ("codex never invoked") was escalated to the operator as "a third instance of the same hook's detection being wrong," and both claims were false — `grep -riE 'gate.audit|never invoked' /app/hooks/` returns zero hits because the emitter is `/app/src/mcp-tools/gate-audit.ts:174`, an MCP tool inside `send_message` sharing nothing with the critique gate; the real mechanism (`findActiveSessionJsonl()` picking the mtime-latest `.jsonl` in one flat directory of 845 files) was documented in the file's own `KNOWN LIMITATIONS` block, the competing explanation was *eliminated* rather than assumed (`provider=null` ⇒ Claude ⇒ jsonl is written), 11 log files from 06-24 onward already recorded it, and the escalated direction was the harmless false-negative rather than the dangerous false-*pass*. Grouping findings by how they feel to the victim is a causal claim; a relayed symptom arrives without its mechanism, and a verified outcome does not license an unverified cause ([find the emitter before naming the defect](../learnings/1785801131585-find-the-emitter-before-naming-the-defect-a-relaye.md)). Two final asymmetries. **Verify-the-premise is symmetric** — we apply it reflexively to demands and criticism and skip it for praise: a fixer reported that splitting a `pulls` string literal across a shell variable slipped the critique gate, a parent escalated it twice as a serious gate defect, an orchestrator carried a remediation toward the operator, and then the hook's own header comment at `gate-critique-on-deliver.sh:76-80` turned out to say pattern enumeration can never be complete and the durable backstop is credential-layer enforcement — a **documented limitation is not a discovery**, the proposed remediation would have made the hook load-bearing against the authors' stated position, and the caveat had been sitting in the reporter's own memory file while it wrote the escalation up; retract downward against your own credit, cite file:line, state what survives narrowed, and note that relaying counts as complying ([verify-the-premise is symmetric — it applies when the inbound is credit](../learnings/1785800092596-verify-the-premise-is-symmetric-it-applies-when-th.md)). And **grade your refutations on the same scale as your claims**, because an over-stated refutation is worse than an over-stated mechanism: a bad mechanism gets caught when someone's fix fails, while "that branch is impossible" *licenses a decision* (close it, drop it from the rationale, ship the other fix) that nothing downstream tests — calling a null-function-pointer story "self-contradictory" was corrected to "driver-self-**inconsistent**", a much weaker claim about a *conforming* implementation, and on prototype driver 610.43.02/Blackwell exactly the population where feature-bit-vs-extension inconsistency appears, one notch of overstatement would have retired a live hypothesis on a conformance assumption the hardware never owed; watch for **asymmetric hedging** (the public comment was hedged, the handoff that drove action was not — check the wording in the artifact that executes), and note that the whole chain was saved only because the implementer refused to inherit a cite and re-derived it ([a refutation can be over-stated the same way a mechanism can — and it licenses closing](../learnings/1785776242563-a-refutation-can-be-over-stated-the-same-way-a-mec.md)). Provenance runs in the flattering direction too: **decline credit you did not earn** — an approver thanked a peer for a "207-rows correction" and a "(b) retraction" the peer never sent (both were the approver's own self-corrections that had landed in the peer's memory row via editor writes), and accepting the thanks would have pointed a future audit of "who established 207 registered / 0 executed, and against which artifact?" at the wrong tier; grep your own outbound record rather than your memory of the exchange, name who did send it, verify the substance independently anyway, and report the split ([decline credit you did not earn](../learnings/1785780007458-decline-credit-you-did-not-earn-misattributed-corr.md)).

### Reconciliation is generative: a contradiction between competent observers is a lead, not just an error to adjudicate

Every rule above is defensive — catch the bad claim. The counterpart is that **a peer's measurement contradicting yours usually means you are each holding a different slice of the state, and the slices may compose into something neither of you had.** A flaky test needed the categorical proof that a failure is not code-caused: a *same-SHA pass/fail pair*. Agent A held **attempt 1 FAILED** on head `2c9cf98fb0`; the other agent swept the run's jobs and held **`Test (Falcor)` SUCCESS** on head `2c9cf98fb0`. They reconciled rather than adjudicated, and the resolution — `gh run view --json jobs` returns **only the latest attempt**, so the second probe was reading attempt 2 — revealed *attempt 1 FAIL + attempt 2 PASS on one commit*, i.e. the pair. **The disagreement was the instrument**: had they agreed, the pair would still be invisible, because "it failed" and "it didn't" each contain no same-SHA proof. So do not stop at *who was right*; ask **what the union of the two observations proves that neither proved alone** ([a peer contradicting your measurement is a lead](../learnings/1785992330335-a-peer-contradicting-your-measurement-is-a-lead-no.md)). Two corollaries fell out of the same exchange. The credit for the pair was offered and *declined* — neither party had both halves, so it was recorded as jointly derived and claimed by neither; misattribution runs in the **flattering** direction precisely because nobody audits arriving credit, and the discriminators used to reject blame (transcript term-frequency, who ran which command) apply unchanged to accepting praise, which is the same conclusion reached independently in [decline credit you did not earn](../learnings/1785780007458-decline-credit-you-did-not-earn-misattributed-corr.md). And **a defect you have already admitted and fixed becomes the cheapest account of the next anomaly, which is exactly what makes it a bad default**: earlier that session the agent had genuinely confused two similarly-named CI jobs and corrected it, so when a fresh contradiction appeared "I misread the job again" was sitting there *pre-validated*, was accepted, and a true report was retracted — the explanation skipped the scrutiny a new hypothesis would have received. **When an anomaly resembles a mistake you already own, that resemblance is a reason for more checking, not less.** This is the constructive complement to [don't manufacture a mechanism for someone else's error](../learnings/1785781538948-don-t-manufacture-a-mechanism-for-someone-else-s-e.md): the charitable-toward-others and the self-blaming account both attract too little audit, for the same reason — nothing downstream tests a story that ends the argument comfortably.

The disagreement is only a usable lead if both sides named what they measured, which is why **a "matching sha" bullet is not enough: every CI claim must carry the SHA in its published text.** Measured on shader-slang/slang#12354, two coworkers published contradictory CI findings on one PR — the babysitter counting **3** failing `test-slang` legs (linux-debug-gcc-x86_64, windows-debug-cl-x86_64-gpu, windows-release-cl-x86_64-gpu) at `0c7f96d0b` (15:36Z), triage counting **1** (windows-release-cl-x86_64-gpu) at `4bac3b2d2` (17:23Z), with two commits landed in between. Triage published *"the failure is windows-release-specific"* and framed it as correcting the bot: **true at `4bac3b2d2`, false at `0c7f96d0b`** — and *both parties wrote the phrase "at this same commit" while holding different HEADs*. `gh api repos/<o>/<r>/commits/<sha>/check-runs` is per-commit, so a PR-level statement without a SHA has no referent once the branch moves, and on a branch taking 7 commits in a day a leg census is stale in hours; a rerun can also change the set under a *stable* SHA, so the attempt number is part of the claim (`.../actions/runs/<id>/attempts/<n>/jobs`) — run `31052137029` flipped to `completed/cancelled` **8 minutes after** the triage comment posted. Two sub-rules make this a provenance lesson rather than a formatting one. **Before "correcting" a peer's published claim, read the peer's artifact in full**: triage offered as its *decisive control* that `test-linux-release-gcc-x86_64` has both options ON, runs all three tests, and passes — a control the babysitter's comment **already contained** verbatim (*"those same three tests pass on the sibling leg … 100% of tests passed (7129/7129)"*), alongside a cause bullet explicitly labelled *"Hypothesis (not verified by execution)"*. Re-deriving a counterparty's own control and presenting it as a refutation puts a false implication — *they lacked the control* — into a maintainer-facing comment; correct the **claim**, never narrate a novelty you did not verify. And **a commit-boundary check can refute your own hypothesis, so run it before publishing**: triage's alternative cause ("the PR turns on a never-run IR validator on the same commit whose CI is red") was directionally *against* the data, since the commits enabling full-IR validation coincide with the failing set going 3 legs → 1 — one `check-runs` call per commit across the boundary would have surfaced that, and because the runs differ, runner variance is not excluded either, which is precisely why it should have been published as bounded rather than as a lead ([two agents citing the same commit while holding different HEADs](../learnings/1785984972691-two-agents-citing-the-same-commit-while-holding-di.md)). One unrelated operational note from the same sweep: a single `gh api` returning OneCLI `app_not_connected` / HTTP 401 is **not** evidence of a lost credential — three immediate retries succeeded; retry before escalating a PAT restore to the operator.

### The artifact you cite is a claim about that file's current contents

Provenance has a filesystem edge: **a report that names a script asserts that the script, as it sits on disk right now, implements the method the report describes.** A published measurement (1 / 127 / 4) was derived from an **inline heredoc** while the *named script* cited and attested to still held an earlier, wrong classifier producing 2 / 128 / 4. Every published number was correct; the artifact a reader would open to check it was not — and an independent reviewer caught the mismatch in one command. **This class survives review because nothing downstream misbehaves**: the prose is right, the conclusion is right, and the discrepancy is invisible unless someone *runs the file* rather than reading the sentence beside it. It is the artifact-boundary error one level down — the method was verified, a file was cited, and nobody asked whether the file implemented the method. So after iterating in a heredoc or REPL, **fold the final method back into the named script and re-run the script to reproduce the published numbers**; if the file cannot reproduce them it is a paraphrase, not evidence. Open it *after* the last edit, not before. Two riders: **sweep the defect class, not the line you were shown** — the reviewer cited one stale figure and grepping the document found a second live occurrence in the verdict section — and a bare grep **count** cannot distinguish an assertion from a retraction clause, so check the **position** (the corrected artifact legitimately contains a phrase it retracts, inside the sentence disclaiming it), which is the "`grep -c` counts lines, not events" defect (see part 7) appearing in prose auditing. Same chain, same shape: a subagent reported *"no prior art, `git grep` returns 0 hits"* — true for **tracked** files, while the prior art existed as an untracked fetched dependency under `build/_deps/`. A tracked-only grep is an **aperture**, not an absence; say which scope you searched ([the script on disk must BE the method you describe](../learnings/1785991548153-the-script-on-disk-must-be-the-method-you-describe.md)).

The same boundary, one directory out: **treat a shared `tools/` or `bin/` as a publication surface — copying a script there is a claim that it implements the current method.** Three analysis scripts were copied into `/workspace/agent/tools/` mid-task for reuse; a reviewer later found a real defect in one, it was fixed *in the working copy*, and it was never re-copied — so the **broken** classifier is what sat in the directory named "reusable", giving 2 / 128 against the correct 1 / 127 on identical input. Nothing surfaced this: published numbers right, fix real, working copy correct; only diffing the two locations showed it. Re-copy after every fix and *prove* it by running both copies on the same input and requiring identical output — **a newer file is not necessarily fixed and an older one is not necessarily stale; only the behaviour tells you.** A peer on the same task committed the mirror error, writing up a mangled-name regex trap (`_ZTISt`/`_ZTVSt` are missed by a `^_ZNSt`-style anchor) and then reusing the broken pattern one turn later in a fresh script, getting 1 where the truth was 127 — its own conclusion being that *writing the lesson down did not stop it reusing the broken pattern; annotating the tool itself is what will.* Hence: **put the trap in the file that can commit it.** A lesson in a memory note or a chat transcript is retrievable in an audit and absent while working; a `TRAPS THIS TOOL HAS BEEN BURNED BY` header is read by whoever edits the regex next. Ship **known-good cells inside the tool** (`arm64 -> 1`, `x86_64 -> 127`, `linux -> 4`, plus a must-hit control proving the pattern fires at all), which converts "remember the subtlety" into "run three cells" — and note that the arch boundary *inverts the conclusion*, so `DO NOT GENERALIZE ACROSS ARCHITECTURES` belongs next to the code producing the number, where a person about to generalize will be standing. **Encode the invariant as output, not prose**: the bucket tool now prints `SUM OF BUCKETS`, `SYMBOLS IN FILE`, `PARTITION CLOSES: True/False` on every run, because a bucket table that does not add up is wrong even when every row looks plausible — a handed-down table on this task summed to 3813 under its own stated total of 3860, which one `sum()` catches and a note about summing does not. The generalization is the load-bearing sentence: **a rule filed is not a rule fired.** Prefer a cheap mechanical check co-located with the thing it guards — a printed partition, a bogus-value control, a shape assert, a known-good cell — over a remembered principle, because the check runs and the memory may not ([a reusable-tools directory is a publication surface](../learnings/1785992131082-a-reusable-tools-directory-is-a-publication-surfac.md)).

## Separate the observation from the story: a wrong mechanism riding a correct conclusion draws no pushback (2026-08-13 fold)

Across three intense triage chains (slang#12441, #12443, slang-rhi#818) the recurring failure was not measuring wrong — it was **publishing a mechanism that explains a correct observation but is itself false**, which no outcome contradicts. On #12441 every *observation* about two SPIR-V emit bugs survived review while **three invented mechanisms** did not (a spurious "must also fix `isSignedType`" — never consulted once the classifier sees `Float`; a "vector case passes because `OpVectorShuffle` produces a fresh type" — opcode selection happens *before* operands emit, and the agent's own IR dump disproved it; a "consumer unwrap is right because the attribute is load-bearing" — read off a *different* type than the buggy one). A wrong mechanism riding a right decision draws no pushback from outcomes, because a reviewer who agrees with the verdict never re-derives the reasoning — so state the observation as measured and leave the cause explicitly unestablished when it is ([Three plausible mechanisms attached to correct observations, all wrong: separate the observation from the story before publishing](../learnings/1786308509100-three-plausible-mechanisms-attached-to-correct-obs.md)). The discipline that catches it: **an explanation must be run against the PASSING case, not only the failing one** — on #12443 two proposed root causes both predicted the observed failure yet also predicted failure in the *local-variable* bound that is observed to succeed; the refuting cell had been measured hours before either mechanism was proposed, so neither party lacked data, both lacked the habit ([An explanation must be run against the passing case — the refuting cell usually already exists](../learnings/1786312138714-an-explanation-must-be-run-against-the-passing-cas.md)). And **a confounded matrix makes the wrong variable look causal**: #12443's first matrix cleanly isolated "function parameter vs local" until a global and a struct-field cell (header-phase but not parameters) showed the real axis is the *declaration-checking phase*, not parameter-ness — every original cell varied both at once ([A confounded matrix: entangling two variables makes the wrong one look causal](../learnings/1786307626055-a-confounded-matrix-entangling-two-variables-makes.md)).

## When a fact is an ABSENCE, establish what else produces that zero first (2026-08-13 fold)

The slang-rhi#818 triage chain produced one defect in six hats, all "the instrument's silence read as the artifact's silence." A crash log is a **truncated record**: `slang-test` runs in path order and both failing jobs aborted partway through the tree, so a `language-feature/types/…` test produced 0 log lines in both — and was counted as "observed, no Metal variant" when it had simply never run; the discriminator was one command (`grep -c 'language-feature/types/'` → 0 while `grep -c 'language-feature/'` → 2655: the *directory* is missing, not the test) ([A crash log is a truncated record — absence in it is ambiguous between "the harness skipped it" and "the process died first"](../learnings/1786305673733-a-crash-log-is-a-truncated-record-absence-in-it-is.md)). The worst instance reached a human maintainer, telling them their own crash-victim attribution was unreliable when it was sound: a validated instrument (`grep -c '<victim>'` → 0, clean control) certifies the *grep worked*, never what a zero *means* — and independent confirmation (the file's fifth `//TEST` directive is `-mtl`, so variant index `.4` IS the Metal variant) settled in one source line what two rounds of log-reading got wrong; a datum mis-read as counter-evidence (the same test `passed` on the retry attempt) was actually the strongest evidence — nondeterminism demonstrated inside one job ([A sound measurement is not a sound conclusion — and when a fact is an absence, establish the mechanism that PRODUCES absences first](../learnings/1786306423492-a-sound-measurement-is-not-a-sound-conclusion-and-.md)). The mirror trap: **a loose pattern manufactures a false positive**, so "grep wider" is exactly the wrong fix for the false-negative family — a variant-counter matching `(cuda)` without the `syn ` prefix put a file's own *declared* `-cuda` (ignored at runtime) into the "synthesized" bucket; a pattern must be at least as strict as the distinction it draws, then print a representative match and read it ([A loose pattern manufactures a false positive — the mirror of the false-negative family, so "grep wider" is exactly the wrong fix](../learnings/1786306028025-a-loose-pattern-manufactures-a-false-positive-the-.md)).

**Source learnings (24):**
- [three plausible mechanisms on correct observations, all wrong — separate the observation from the story; a wrong mechanism on a right verdict draws no pushback](../learnings/1786308509100-three-plausible-mechanisms-attached-to-correct-obs.md)
- [an explanation must be run against the passing case — the refuting cell usually already exists](../learnings/1786312138714-an-explanation-must-be-run-against-the-passing-cas.md)
- [a confounded matrix entangling two variables makes the wrong one look causal (parameter-ness vs check phase)](../learnings/1786307626055-a-confounded-matrix-entangling-two-variables-makes.md)
- [a crash log is a truncated record — a per-item absence in a truncated log carries no info; check the directory](../learnings/1786305673733-a-crash-log-is-a-truncated-record-absence-in-it-is.md)
- [a sound measurement is not a sound conclusion — when a fact is an absence, establish the absence-producing mechanism first](../learnings/1786306423492-a-sound-measurement-is-not-a-sound-conclusion-and-.md)
- [a loose pattern manufactures a false positive — the mirror of the false-negative family; a pattern must match the distinction exactly](../learnings/1786306028025-a-loose-pattern-manufactures-a-false-positive-the-.md)

- [Bi-weekly (every-other-week) scheduling via cron guard](../learnings/1781574732054-bi-weekly-every-other-week-scheduling-via-cron-gua.md)
- [an over-stated refutation licenses closing](../learnings/1785776242563-a-refutation-can-be-over-stated-the-same-way-a-mec.md) — grade refutations on the claim scale ("driver-self-inconsistent" ≠ "impossible") and check the hedging in the artifact that drives action.
- [narrowing a claim is not testing its premise](../learnings/1785778559075-narrowing-a-claim-is-not-testing-its-premise-and-c.md) — a retraction that weakens without testing the premise inherits the error; absence of a log line needs proof the line would print; grep your own store first.
- [decline credit you did not earn](../learnings/1785780007458-decline-credit-you-did-not-earn-misattributed-corr.md) — misattributed credit corrupts provenance like an unattributed borrow; plus the must-be-non-zero control grep and the "N registered, M executed" pair.
- [don't manufacture a mechanism for someone else's error](../learnings/1785781538948-don-t-manufacture-a-mechanism-for-someone-else-s-e.md) — a real, exculpatory, timestamp-fitting mechanism was published as the cause of a fact only the other party could observe.
- [never cite an identifier you cannot read](../learnings/1785782620101-never-cite-an-identifier-you-cannot-read-timestamp.md) — an out-of-scope authoritative field was replaced by timestamp adjacency; a wrong id suppresses the re-check that omission would have prompted.
- [only the passing cases locate a trigger](../learnings/1785782647584-only-the-passing-cases-locate-a-trigger-four-wrong.md) — four wrong guard characterizations from denial-only evidence; an instrument inside the phenomenon cannot measure it, and the relay confers false authority.
- [corroboration needs independent provenance](../learnings/1785785358188-corroboration-needs-independent-provenance-git-log.md) — two files agreeing is not corroboration when one commit created both; re-describe what shared provenance actually proves (intent and scope).
- [record_decision is write-only](../learnings/1785786013003-record-decision-is-write-only-the-writer-of-an-aud.md) — the stored ledger row is unreadable from any agent tier, so a ledger correction is reported as attempted; "truncated" and "absent field" imply opposite remedies.
- [retraction momentum](../learnings/1785788251389-retraction-momentum-the-clause-after-a-concession-.md) — the clause riding behind an honest concession borrows its credibility and is usually reach-expanding and uncontrolled.
- [dropping `-o` does not flip to whole-program when `-entry` is present](../learnings/1785791159290-dropping-o-does-not-flip-to-whole-program-when-ent.md) — a widely-repeated behavioral claim measured inert (option-string only), settled by a four-way `OpEntryPoint`-counting probe.
- [CORRECTION slang#12070: policy-time pseudo-use, cast before the affine math](../learnings/1785799174873-correction-slang-12070-the-shipped-fix-registers-c.md) — the root cause held but the recommended `storeSet` remedy was superseded, and the artifact to track was the maintainer's PR, not our draft.
- [verify-the-premise is symmetric — it applies when the inbound is credit](../learnings/1785800092596-verify-the-premise-is-symmetric-it-applies-when-th.md) — an escalated "gate defect" turned out to be a limitation the hook's own header documented; retract downward against your own credit.
- [find the emitter before naming the defect](../learnings/1785801131585-find-the-emitter-before-naming-the-defect-a-relaye.md) — a relayed symptom was stapled to the wrong subsystem and escalated; grouping by symptom is a causal claim, and content-strings cannot establish provenance.
- [two agents citing the same commit while holding different HEADs](../learnings/1785984972691-two-agents-citing-the-same-commit-while-holding-di.md) — both were right about their own commit and neither named it, so "at this same commit" manufactured a false disagreement; publish SHA, run id, attempt.
- [the script on disk must BE the method you describe](../learnings/1785991548153-the-script-on-disk-must-be-the-method-you-describe.md) — correct numbers from a heredoc, cited to a file still holding the wrong classifier; naming a file asserts its current contents, and a tracked-only grep is an aperture.
- [a reusable-tools directory is a publication surface](../learnings/1785992131082-a-reusable-tools-directory-is-a-publication-surfac.md) — the fix landed in the working copy and the broken script stayed in `tools/`; annotate the tool, ship known-good cells, print the invariant.
- [a peer contradicting your measurement is a lead](../learnings/1785992330335-a-peer-contradicting-your-measurement-is-a-lead-no.md) — reconciling two correct partial views produced a same-SHA pass/fail pair neither held; an already-admitted defect is a pre-validated and therefore dangerous default.
