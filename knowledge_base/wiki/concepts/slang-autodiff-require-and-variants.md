---
title: "Slang Autodiff: Capability [require] Threading and Derivative Variants"
type: concept
group: slang-autodiff-ir
tags: [autodiff, differentiation, require, capability, prefer-recompute, ivector, subscript, transitive]
source_count: 5
---

# Slang Autodiff: Capability [require] Threading and Derivative Variants

Companion to [[wiki/concepts/slang-autodiff-ir-autodiff-differentiation.md]] (internals and design rules). This page collects the capability-`[require]` threading rules for derivatives (where the requirement rides, why the use-site hook under-constrains transitive differentiation, and the four-cell inconsistency of primal-`[require]` propagation), the derivative-variant purity gate's `[PreferRecompute]` equivalence, and the IVector differentiable-subscript placement.

## TL;DR

- **Derivative `[require]` must ride the differentiation USE, not the primal.** The gate (presence of `[require]`) was correct; #11859 shows the *placement* was wrong — over-propagating onto the primal makes non-AD callers of the primal inherit a capability they never asked for.
- **BUT the use-site hook under-constrains TRANSITIVE differentiation.** Slang capability checking is AST-only; synthesized IR derivatives are never capability-checked. An AST syntactic hook that fires only on a *direct* `fwd_diff(p)`/`bwd_diff(p)` cannot see transitive differentiation — `bwd_diff(g)` where `g` calls a `testC` carrying `[require(spirv)]` never joins the requirement. The OLD primal-side model caught this for free via call-graph inference. Reviewer rule: when a requirement moves from a callee/primal to a "use-site," ask whether the old location rode call-graph inference and whether the new hook does.
- **Primal-`[require]` propagation is INCONSISTENT across the 4 (fwd/bwd × user/synthesized) cells** — not a uniform gap. Both directions go through the same rewrite (`convertHigherOrderExprToLookup`), so a single stated root cause is INCOMPLETE. Run the full behavior matrix (direction × derivative-kind × call-form) before accepting one cause; a shared path with divergent results means the cause is incomplete. HOLD — it also raises a genuine semantic design question (maintainer call).
- **Derivative-variant purity gate must accept `[PreferRecompute]`.** The `isInstCarryingOverDiff` readNone carry-gate must treat `[PreferRecompute]` as purity-equivalent to `[__readNone]`, else a `[PreferRecompute]` derivative variant is wrongly rejected (#11374, follow-up to #11286).
- **The differentiable-subscript requirement lives on `IVector`, inherited (non-`[Differentiable]`) from internal `IArrayAccessor`.** Strengthen it on IVector, NOT IArrayAccessor (whose other conformers are raw storage handles). Gates the InlineVector default variant too, not just the wave variant.

## Derivative `[require]` Must Ride the Differentiation Use, Not the Primal

Follow-up to the "gate derivative→primal capability propagation on explicit `[require]`" learning: that gate (presence of `[require]`) was correct, but #11859 shows the *placement* is wrong. The derivative's `[require]` capability must ride the **differentiation use**, not be propagated onto the primal function — over-propagating to the primal makes non-AD callers of the primal inherit a capability they never asked for (an over-propagation regression) ([Slang autodiff: derivative [require] must ride the differentiation use, not the primal (over-propagation regression #11859)](../learnings/1782864820466-slang-autodiff-derivative-require-must-ride-the-di.md)).

### The use-site hook under-constrains transitive differentiation (#11872)

But the *layer* the use-site hook lives at has a soundness gap. Reviewing PR #11872 (the #11859 fix) surfaced that Slang capability checking is **AST-only** — the synthesized IR derivative is never capability-checked (`grep -rlnE "[Cc]apabilit" source/slang/slang-ir-autodiff*.cpp` is empty; `slang-ir-late-require-capability.cpp` only handles explicit `__requireCapability`). So a `CapabilityDeclReferenceVisitor` use-site hook that fires only on a *direct* syntactic `fwd_diff(p)`/`bwd_diff(p)` cannot see **transitive** differentiation: `bwd_diff(g)` where `g` calls a `testC` carrying a `[require(spirv)]` user-defined derivative never joins `testCBwd`'s requirement — a silent false-negative that compiles clean on `-target hlsl`. The OLD primal-side model caught this for free because the requirement sat on `testC` and rode ordinary call-graph capability inference (`visitReferencedDecls` → `inferredCapabilityRequirements`), which is exactly what handles transitivity; an AST syntactic hook only sees the *outermost* operator. Reviewer rule: when a capability/requirement moves from a callee/primal to a "use-site," ask whether the old location rode call-graph inference and whether the new hook does. Note also DeepWiki claimed transitive propagation works and the IR derivative is capability-checked — it conflated *differentiability* checking (`CheckDifferentiabilityPassContext`) with *capability* checking (`SemanticsDeclCapabilityVisitor` / E36107); verify pipeline claims against source ([Slang: use-site propagation of user-defined derivative [require] under-constrains transitive differentiation](../learnings/1782882850345-slang-use-site-propagation-of-user-defined-derivat.md)).

### Primal `[require]` propagation is inconsistent across four cells (#11882)

Deeper still (#11882, HEAD `7f79b923f`): propagation of a primal's OWN `[require(spirv)]` to a differentiation entry point is **inconsistent across four cells**, not a uniform gap — for a user-defined derivative, `fwd_diff` propagates (E36107) but `bwd_diff` drops it; for a synthesized derivative, `fwd_diff` drops but `bwd_diff` propagates (plain calls always propagate). Since both directions go through the SAME rewrite (`convertHigherOrderExprToLookup`, where `as<DifferentiateExpr>` matches both), a single stated root cause like the `visitMemberExpr`-vs-`visitStaticMemberExpr` base-visitor asymmetry is INCOMPLETE: `ConstructDeclRefExpr` yields a StaticMemberExpr (base dropped) OR a plain MemberExpr (base traversed) depending on the member's static-ness, and that form decides visitation per cell. Lesson: when a bug report gives a clean single root cause for "X is dropped," run the full behavior matrix (direction × derivative-kind × call-form) before accepting it — a shared path producing divergent results means the cause is incomplete. This also surfaces a genuine semantic design question (should a primal's own `[require]` gate fwd/bwd_diff when a user-defined derivative means the primal body may never run?), making it a maintainer call — HOLD given the over-propagation abort history (#11551) ([Slang primal [require] propagation at fwd_diff/bwd_diff is inconsistent across 4 cells (not a uniform gap)](../learnings/1782906093613-slang-primal-require-propagation-at-fwd-diff-bwd-d.md)).

## Derivative-Variant Purity Gate Accepts `[PreferRecompute]`

The `isInstCarryingOverDiff` readNone carry-gate in `slang-ir-check-differentiability.cpp` must treat `[PreferRecompute]` as purity-equivalent to `[__readNone]` — a `[PreferRecompute]` derivative variant is otherwise wrongly rejected by the differentiability purity check (#11374, follow-up to #11286) ([Slang autodiff: derivative-variant purity check must accept [PreferRecompute], not just [__readNone]](../learnings/1780312221839-slang-autodiff-derivative-variant-purity-check-mus.md)).

## Differentiable Subscript Requirement Lives on IVector, Inherited from Internal IArrayAccessor (#12025/#12026)

`IVector`'s element-access subscript is NOT declared on `IVector` itself — `public interface IVector<T> : IDifferentiable, IArrayAccessor<T>` (`source/standard-modules/neural/ivector.slang`) inherits `__subscript(int)->T { get; set; }` from the `internal interface IArrayAccessor<T>` (`vectorized-reader.slang`), and that inherited requirement is NOT `[Differentiable]`. So autodiff can't differentiate element access (issue #12025: `WaveTangledVector`'s subscript lacks `[Differentiable]` while `InlineVector`'s have it). The fix must strengthen the requirement on **IVector**, NOT on IArrayAccessor — IArrayAccessor's other conformers are raw storage handles (`Ptr<T>`, `Array<T,N>`, `RWStructuredBuffer<T>.Handle`) that must stay non-differentiable. The open question for a fixer: does Slang let IVector *refine/strengthen* an inherited requirement's differentiability attribute? The precedent tests (`tests/autodiff/subscript.slang`, `generic-accessors.slang`) declare `__subscript { [BackwardDifferentiable] get; }` in a *standalone* interface, not as a refinement of an inherited one — so build-verify before committing to that scope; if unsupported, shadow/redeclare on IVector or mark the concrete accessors + a shared contract test. Because `FFLayer.eval` is generic over `IVector`, this requirement gates the **InlineVector (default) variant too**, not only the wave variant — PR bodies often claim wave-only, so check the interface requirement, not just the concrete type. The real deliverable is the missing shared contract test applying fwd+bwd autodiff to the getter/setter for *every* IVector conformer (only InlineVector + WaveTangledVector in-tree) — its absence is why the drift went unnoticed ([slang IVector differentiable subscript drift (#12025): requirement is inherited from internal IArrayAccessor](../learnings/1783618355299-slang-ivector-differentiable-subscript-drift-12025.md)).

---
**Source learnings (5):**
- [Autodiff: derivative [require] must ride the differentiation use, not the primal (over-propagation #11859)](../learnings/1782864820466-slang-autodiff-derivative-require-must-ride-the-di.md)
- [Use-site propagation of user-defined derivative [require] under-constrains transitive differentiation (PR #11872)](../learnings/1782882850345-slang-use-site-propagation-of-user-defined-derivat.md)
- [Primal [require] propagation at fwd_diff/bwd_diff is inconsistent across 4 cells (#11882)](../learnings/1782906093613-slang-primal-require-propagation-at-fwd-diff-bwd-d.md)
- [Derivative-variant purity check must accept [PreferRecompute], not just [__readNone] (#11374)](../learnings/1780312221839-slang-autodiff-derivative-variant-purity-check-mus.md)
- [slang IVector differentiable subscript drift (#12025): requirement inherited from internal IArrayAccessor](../learnings/1783618355299-slang-ivector-differentiable-subscript-drift-12025.md)
_Catalog: [[wiki/index.md]]_
