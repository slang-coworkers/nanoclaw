---
title: "Slang SPIR-V Backend: Emission, Capabilities, and Validation"
type: concept
group: slang-backends
tags: [spirv, vulkan, codegen, capabilities, descriptor-heap, debug-info, atomics]
source_count: 80
---

# Slang SPIR-V Backend: Emission, Capabilities, and Validation

The SPIR-V backend is Slang's primary Vulkan/Khronos target, implemented via a direct emitter (`slang-emit-spirv.cpp`) combined with a legalization pass (`slang-ir-spirv-legalize.cpp`) and the bundled `slang-glslang` module for `spirv-opt`. This page covers the core mechanisms for capability declaration, descriptor-heap emission, debug-info, atomics, and common pitfalls.

## Entry Points and Module-Level Emission

Slang's default SPIR-V behavior renames every entry point to `"main"` (a legacy from GLSL-via codegen). The flag `-fvk-use-entrypoint-name` (API: `VulkanUseEntryPointName`) preserves the source name. A non-obvious rule: Slang **auto-applies** `-fvk-use-entrypoint-name` when a module has more than one entry point (per PR #6260), so single-entry shaders still get renamed to `"main"` and fail `VkPipelineShaderStageCreateInfo` validation with VUID-VkPipelineShaderStageCreateInfo-pName-00707 unless the flag is passed uniformly ([Slang SPIR-V entry-point rename: auto-applies only with >1 entry point](../learnings/1779621920622-slang-spir-v-entry-point-rename-auto-applies-only-.md)).

The `[require(spirv_1_x)]` decoration that controls the emitted SPIR-V version must reach the **codegen IR module** (not only the layout/reflection module) via `addEntryPointRequireCapabilityDecorations()`. A secondary fix is also required: `determineSpirvVersion()` must match on internal `_spirv_1_x` atoms, not the public alias. Without both changes, `[require(spirv_1_5)]` leaves the SPIR-V at version 1.0 ([CORRECTION to #11631 version root cause: require atom stamped only on layout IR module, not the codegen module determineSpirvVersion runs on](../learnings/1781643037138-correction-to-11631-version-root-cause-require-ato.md)).

## Capability Declaration

**Adding a capability atom:** The single source of truth is `source/slang/slang-capabilities.capdef`. Two lines are needed per capability — an extension atom (`def SPV_EXT_foo : _spirv_1_0;`) and a capability atom (`def spvFooEXT : SPV_EXT_foo;`). Each requires `///` doc comment + `/// [EXT]` tag. The build runs `slang-capability-generator` to produce the C++ enum headers — never hand-edit those. Adding an atom also regenerates two checked-in docs: `docs/user-guide/a3-02-reference-capability-atoms.md` (alphabetical) and `docs/command-line-slangc-reference.md` (declaration order, byte-exact diff enforced by CI) ([Adding a SPIR-V capability atom to Slang (capdef recipe)](../learnings/1781120340432-adding-a-spir-v-capability-atom-to-slang-capdef-re.md), [Adding a SPIR-V capability atom — ADDENDUM (two generated docs + test -capability)](../learnings/1781122622446-adding-a-spir-v-capability-atom-addendum-two-gener.md)).

**Gating emit on a capability atom:** The correct pattern is `targetCaps.implies(CapabilityAtom::spvFooEXT)`. Do NOT gate on `isSPIRV(...)` (fires unconditionally for every SPIR-V target) and do NOT gate on extension-string atoms where a capability atom exists. The established precedent in `slang-emit-spirv.cpp:1692` gates on `CapabilityAtom::spvBindlessTextureNV`; ~8 sibling sites confirm this. An earlier triage learning incorrectly advised `isSPIRV()` — the correction is in [CORRECTION: SPIR-V capability emit-gating — use implies(CapabilityAtom::spv*), NOT isSPIRV()](../learnings/1781123971590-correction-spir-v-capability-emit-gating-use-impli.md). When adding a conditional capability for a stdlib `spirv_asm` intrinsic, see the option tree in [Conditionally requiring a SPIR-V capability for a stdlib spirv_asm intrinsic forces pass-level block manipulation — layer-choice tree](../learnings/1781974573249-conditionally-requiring-a-spir-v-capability-for-a-.md) (options: constexpr-only, post-hoc pass rewrite, new IR op, placeholder operand idiom).

**Triaging "add a capability" requests:** Check `external/spirv-headers/include/spirv/unified1/spirv.h` first — the `SpvCapability*` constant is usually already vendored. A bare capdef atom is inert until consumed by a `[require(...)]` intrinsic or C++ emit site ([Triaging an &quot;add a SPIR-V capability bit&quot; request — vendored-operand check + inert-atom scoping](../learnings/1781116222932-triaging-an-quot-add-a-spir-v-capability-bit-quot-.md)). New `Spv*` enum constants resolve from the SPIRV-Headers package, NOT from the stale `external/spirv/spirv.h` ([Slang SPIR-V emit: new Spv* enum constants resolve from the SPIRV-Headers package, not external/spirv/spirv.h](../learnings/1782322517394-slang-spir-v-emit-new-spv-enum-constants-resolve-f.md)).

**SPV_KHR_abort and multi-dependency chains:** `OpAbortKHR` is a block terminator (like OpKill) requiring both `SPV_KHR_abort` AND `SPV_KHR_constant_data` (for the `OpConstantDataKHR` message). New SPIR-V extension intrinsics must be modeled as terminators, not printf-parallel — after emitting the terminator, block emission must stop and unreachable code must be removed ([When planning a new SPIR-V opcode, check terminator-ness — not just value-producing-ness](../learnings/1781039584587-when-planning-a-new-spir-v-opcode-check-terminator.md), [SPV_KHR_abort transitively requires SPV_KHR_constant_data; message is OpConstantDataKHR not a runtime composite](../learnings/1782248897717-spv-khr-abort-transitively-requires-spv-khr-consta.md)).

**Conditionally requiring a capability for ray tracing:** To limit a shader to `SPV_KHR_ray_query` without pulling in `SPV_KHR_ray_tracing`, use `-capability spvRayQueryKHR` and verify no `TraceRay`/raygen path re-introduces the extension. The `RaytracingAccelerationStructure` type itself triggers a dual "any" requirement; post-PR #6615 dedup should collapse it to ray_query when only `RayQuery` usage exists ([Slang AS: forcing ray-query-only capability (avoid SPV_KHR_ray_tracing)](../learnings/1781905197543-slang-as-forcing-ray-query-only-capability-avoid-s.md)).

**`-capability` silently raises the SPIR-V version past an explicit `-profile` (#12097/#12099):** `slangc -profile spirv_1_4 -capability spvShaderInvocationReorderNV` silently emits a `; Version: 1.5` header (exit 0, no diagnostic). Root cause: `-capability` atoms are folded into target caps in `TargetRequest::getTargetCaps()` (`slang-target.cpp:214-232`) via `if (!targetCap.isIncompatibleWith(toAdd)) targetCap.join(toAdd)`, and `CapabilitySet::isIncompatibleWith` (`slang-capability.cpp:487-512`) treats two sets as compatible whenever they share ANY target abstract node — `spirv_1_4` and `spirv_1_5` are the SAME SPIR-V target, so the join proceeds and `determineSpirvVersion` takes the MAX (`requireSpirvVersion` = `Math::Max`). A version *raise* is thus always a *compatible* join, never flagged. The missing prerequisite for any conflict diagnostic: nothing records whether the profile's concrete version was USER-EXPLICIT vs defaulted (default is `spirv_1_5`), so a CLI-conflict check (fix layer A at the `getTargetCaps` join with a fire-once DiagnosticSink; cleaner layer B at parse-time in `slang-options.cpp`) must add that flag first. This is a distinct layer from the code-implied capability errors E41012/E41013 (`slang-check-shader.cpp`, gated by `-restrictive-capability-check`); a CLI conflict wants an unconditional error. Correct discriminator: key off "capability has NO valid realization at the explicit version (extension paths included)", NOT "default realization exceeds version" ([-capability silently raises SPIR-V version past explicit -profile (#12099)](../learnings/1784052596356-why-capability-silently-raises-spir-v-version-past.md)).

The specific over-require driving #12097 is that the SER extensions are pinned to `_spirv_1_5` in the capdef: `slang-capabilities.capdef:624` `def SPV_EXT_shader_invocation_reorder : _spirv_1_5 + SPV_KHR_ray_tracing;` (NV inherits at `:629`, motion aliases at `:862`/`:867`) — a deliberate simplification per the source comment, even though SER is usable at 1.4 given a physical-storage-buffer extension. **A capdef `_spirv_1_5`→`_spirv_1_4` floor edit ALONE does NOT make the 1.4 module valid:** the SER emit path (`emitOpTypeHitObject*`, `slang-emit-spirv.cpp:2745-2762`) declares only the reorder `OpExtension`/`OpCapability`; the only site that emits `OpExtension "SPV_KHR_physical_storage_buffer"` is `requirePhysicalStorageAddressing()` and it fires only under `PhysicalStorageBuffer64` addressing (buffer_reference usage), which a plain SER raygen shader never triggers. So the 1.4 path emits no PSB extension and spirv-val fails per SER's normative 1.4 dependency (1.4 + `SPV_KHR_ray_tracing` + one of {PSB extension, core 1.5}). The 1.4 path must declare the PSB *extension* without switching the memory model away from `Logical GLSL450`. Note `SPV_KHR_physical_storage_buffer` is a hardcoded emit string, not a capdef atom (only `SPV_EXT_physical_storage_buffer : _spirv_1_3` exists); capdef disjunction (`|`) could model the "1.4+PSB OR core-1.5" rule but still needs the emission change ([SPIR-V SER capability over-requires 1.5; capdef floor edit alone insufficient (#12097)](../learnings/1784052492569-spir-v-ser-capability-over-requires-1-5-capdef-flo.md)). #12097 and #12099 are a paired "allow the 1.4 downgrade" vs "reject the conflicting `-profile`/`-capability` combo" issue set from the same author (jkwak-work) for the identical repro — when triaging one of a paired allow/reject set, flag the tension to the maintainer rather than silently picking a side.

## Builtin Variables and Memory-Access Masks

`IRSPIRVAsmOperandBuiltinVar` is `hoistable=true` — cross-stage uses of the same builtin (e.g. compute + raygen sharing `SubgroupLocalInvocationId`) always collapse to a single inst via `_findOrEmitHoistableInst`. Adding a cache-key axis to `BuiltinSpvVarKey` for cross-stage differentiation is dead code under today's IR ([slang-spirv-asm-operand-builtinvar-is-hoistable-collapses-cross-stage](../learnings/1779617050641-slang-spirv-asm-operand-builtinvar-is-hoistable-co.md)).

The `getBuiltinGlobalVar` cache-hit trap: when adding a side-table like `m_volatileBuiltinGlobalVars` inside `getBuiltinGlobalVar`, populate it BEFORE the cache-hit early return. Otherwise the second call site that hits the cache misses the set entirely. Additionally, `spirv_asm` blocks bypass `emitLoad` — the `case SpvOpLoad:` branch in `emitSPIRVAsm` must also be extended ([slang-emit-spirv builtin-var cache and the volatile-set cache-hit trap](../learnings/1779612967874-slang-emit-spirv-builtin-var-cache-and-the-volatil.md)).

When injecting a memory-access mask (e.g. `Volatile`) into a `spirv_asm` OpLoad that already carries a user-supplied mask, emit ONE combined mask word, not two consecutive words. The scan for existing user masks must include both `kIROp_SPIRVAsmOperandLiteral` and `kIROp_SPIRVAsmOperandEnum` ([slang-emit-spirv-extra-memoryaccess-word-grammar-bug-pattern](../learnings/1779617068760-slang-emit-spirv-extra-memoryaccess-word-grammar-b.md)).

**Hoistable operand mutation rule:** Never mutate a `SPIRVAsmOperandEnum` or `SPIRVAsmOperandLiteral` in place — they are hoistable (value-numbered) and `setOperand` on them trips an assert at `slang-ir.cpp:179`. Build a fresh operand and repoint the non-hoistable consumer (`spvInst->setOperand(maskIndex+1, newOperand)`) ([Never mutate a hoistable SPIRVAsmOperand in place; repoint the consuming inst](../learnings/1781725591470-never-mutate-a-hoistable-spirvasmoperand-in-place-.md), [SPIR-V asm-operand legalization: hoistable operands + foldable≠constant pitfalls](../learnings/1781726667133-spir-v-asm-operand-legalization-hoistable-operands.md)).

## Descriptor Heap (spvDescriptorHeapEXT)

The `spvDescriptorHeapEXT` path uses `kIROp_SPIRVLoadDescriptorFromHeap` (not `IRCastDescriptorHandleToResource`). Four function-call specialization sites in `slang-ir-specialize-function-call.cpp` and `slang-ir-specialize-buffer-load-arg.cpp` originally accepted only the cast variant, causing a SIGSEGV (#11498) when the heap-EXT variant wasn't handled ([spvDescriptorHeapEXT path uses kIROp_SPIRVLoadDescriptorFromHeap, not IRCastDescriptorHandleToResource](../learnings/1780729707220-spvdescriptorheapext-path-uses-kirop-spirvloaddesc.md), [spvDescriptorHeapEXT path — fix function-call specialization allowlists, not the downstream pass](../learnings/1780733286644-spvdescriptorheapext-path-fix-function-call-specia.md)).

**Heap operand must not be parameterized:** The `heap` operand of `SPIRVLoadDescriptorFromHeap` is the module-scoped `kIROp_SPIRVResourceHeap` builtin global; at emit it materializes as `OpUntypedVariableKHR` in `UniformConstant`. Parameterizing it as a `uint` OpFunctionParameter makes the cloned callee's access-chain base a `uint` scalar — invalid SPIR-V. Correct shape: parameterize ONLY the index; put the heap in the specialization cache key but not `newArgs` ([spvDescriptorHeapEXT specialization fix: don't parameterize the heap global](../learnings/1780734760813-spvdescriptorheapext-specialization-fix-don-t-para.md), [SPIRVLoadDescriptorFromHeap heap operand is a pointer global at emit — never parameterize it in call-spec](../learnings/1780769340224-spirvloaddescriptorfromheap-heap-operand-is-a-poin.md)).

**ConstantBuffer via spvDescriptorHeapEXT crashes in emit (#11483):** A storage-class divergence (`getDescriptorHeapBufferStorageClass` maps ConstantBuffer to `Uniform` on pre-1.4, `StorageBuffer` on 1.4+) causes a crash in `emitDescriptorHeapLoad`. Unlike `StructuredBuffer`, the crash occurs even for scalar member access ([ConstantBuffer via spvDescriptorHeapEXT SIGSEGVs in SPIR-V emit (slang#11483)](../learnings/1780643259838-constantbuffer-via-spvdescriptorheapext-sigsegvs-i.md)). The related *miscompile* (nested-array members read back garbage, distinct from the crash) has the same root: the heap-CB fetch lowers to an `OpBufferPointerEXT` in the **`Uniform`** class when it must be **`StorageBuffer`** — the missing pointer-type `ArrayStride` is a correct downstream *symptom*, not the bug (Uniform uses logical addressing where stride lives on the array type). PR #11647's `processConstantBufferDescriptorHeapLoad` retypes the load to StorageBuffer; the self-failing regression guard is a static spirv-asm FileCheck asserting `OpTypePointer StorageBuffer` + `ArrayStride`, since a `StructuredBuffer.Handle` proxy is a false negative (it already lowers to StorageBuffer) and no runtime `COMPARE_COMPUTE` can heap-bind a `ConstantBuffer<T>.Handle` today ([Descriptor-heap ConstantBuffer miscompile = wrong storage class, not missing ArrayStride](../learnings/1783384030612-descriptor-heap-constantbuffer-miscompile-11483-wr.md)). This surface is a cluster of related-but-distinct defects (#11211 crash on param, #11647 wrong-data, #4472 still-open multidim-array assert); before asserting any crash/miscompile here repros, **rebuild slangc at true HEAD** — several fixes landed in 2026 and stale binaries/releases mislead, and a Discord/old-release report (#11980) on this surface should first get a ToT rebuild, not a fixer dispatch ([Descriptor-heap ConstantBuffer<T> SPIR-V crash cluster — 3 fixes landed, verify at ToT](../learnings/1783459628122-descriptor-heap-constantbuffer-t-spir-v-crash-clus.md)).

**AS descriptor stride:** When emitting `DescriptorHandle<RaytracingAccelerationStructure>`, drive the runtime-array element type from the LOAD type (`uint64`), not the resource type (`OpTypeAccelerationStructureKHR`). The AS size is opaque, so `OpConstantSizeOfEXT(OpTypeAccelerationStructureKHR)` gives a non-portable stride ([spvDescriptorHeapEXT stride is opaque for AS — load-type must drive runtime-array base type](../learnings/1779962007180-spvdescriptorheapext-stride-is-opaque-for-as-load-.md)).

**Atomic-dest validator false-negative (#11506):** `processImageSubscript` rewrites the atomic dest into `IRSPIRVLoadTexelPointerFromHeap` (AddressSpace::Image) AFTER atomic validation runs. `isValidAtomicDest` accepted `IRImageSubscript` but not the heap texel pointer — fix adds a one-liner. A separate spirv-val failure (#11130) for `format=Unknown` on bindless textures is a distinct independent bug ([slang spvDescriptorHeapEXT: atomic-dest validator rejects heap texel pointers (E41403)](../learnings/1780865902537-slang-spvdescriptorheapext-atomic-dest-validator-r.md), [Descriptor-heap texture atomics: E41403 validator gap (#11506) is separate from format-Unknown spirv-val (#11130)](../learnings/1780869756318-descriptor-heap-texture-atomics-e41403-validator-g.md)).

**Unified stride:** The descriptor-heap emits a DISTINCT `OpTypeRuntimeArray` per element type (cached on `(descriptorElementType, arrayStride)`). A symbolic max stride via `OpSpecConstantOp Select(UGreaterThan(...), ...)` over `OpConstantSizeOfEXT` values passes the bundled spirv-val ([spvDescriptorHeapEXT unified-stride: per-type arrays + symbolic-max construct (#11718)](../learnings/1782264945972-spvdescriptorheapext-unified-stride-per-type-array.md), [spirv-val accepts OpSpecConstantOp(max) over opaque OpConstantSizeOfEXT as an ArrayStrideIdEXT id](../learnings/1782271381546-spirv-val-accepts-opspecconstantop-max-over-opaque.md)).

## VariablePointers

`requireVariableBufferCapabilityIfNeeded` is called from value-materialization sites (var/phi/call/element-ptr/load), NOT from function-signature emission. A `Ptr<T, GroupShared>` that survives as a `[noinline]` function parameter emits a valid `OpTypeFunction %_ptr_Workgroup_T` but NO `OpCapability VariablePointers`. Fix: walk the function type in `emitFunc` and gate on `IRNoInlineDecoration` (not `hasUses()`) — inlined helpers retain uses but must not spuriously declare the cap, which triggers driver miscompile #9061 ([Slang SPIR-V variable-pointers cap is declared from value sites, not function signatures](../learnings/1780967438806-slang-spir-v-variable-pointers-cap-is-declared-fro.md), [Signature-derived SPIR-V VariablePointers must gate on [noinline], not hasUses() (driver bug #9061)](../learnings/1781023718622-signature-derived-spir-v-variablepointers-must-gat.md), [slang SPIR-V: isolating a signature-only VariablePointers cap repro (avoid value-site self-declare)](../learnings/1780970685981-slang-spir-v-isolating-a-signature-only-variablepo.md)).

## Atomics

The SPIR-V atomic emit has FOUR cross-layer gates keyed on address space. When adding an `AddressSpace` case, all four must be updated: (1) front-end l-value check E30047, (2) IR validator `isValidAtomicDest` E41403, (3) emitter `isAtomicableAddressSpace` (AtomicLoad/Store/Exchange only; fallback to non-atomic emitLoad/emitStore silently loses atomicity), (4) emitter `emitMemorySemanticMask` (missing case → `memoryClass=0` → VUID-10870). `AtomicAdd`/`Inc`/`Dec`/`CompareExchange` bypass gate 3 and always emit the atomic op ([SPIR-V atomic emit has TWO address-space gates — a per-address-space fix must touch both](../learnings/1782322436550-spir-v-atomic-emit-has-two-address-space-gates-a-p.md), [Slang SPIR-V atomic emission has 4 cross-layer gates keyed on address space — check all when reviewing atomic/VUID fixes](../learnings/1782346122322-slang-spir-v-atomic-emission-has-4-cross-layer-gat.md)).

**groupshared-by-reference regression:** Lowering a `groupshared T arr[N]` parameter by-reference (to fix D3D TGSM loss) creates a `Workgroup` pointer that cannot cross a SPIR-V function boundary without `VariablePointers`. Fix: extend `GLSLResourceReturnFunctionInliningPass::shouldInline` (Khronos-gated) to also inline callees with a `groupshared`-rate parameter, keyed on `as<IRGroupSharedRate>(param->getRate())` not the value type ([groupshared by-reference param regresses Khronos SPIR-V; fix is Khronos-gated inlining keyed on param RATE](../learnings/1782237919713-groupshared-by-reference-param-regresses-khronos-s.md)).

## spirv-opt / slang-glslang Integration

`slang-glslang` is Slang's SPIR-V backend library (bundles SPIRV-Tools including `spirv-opt`) — the name is misleading; it has nothing to do with GLSL. It is always a runtime-loaded MODULE, never static-linked. The correct fix for "missing slang-glslang in a static build" is a build-system change (provide the module), not making its load non-fatal ([slang-glslang/spirv-opt: gate the emit-path load to only-when-needed (masking vs. principled gating)](../learnings/1782152994624-slang-glslang-is-the-spir-v-backend-don-t-mask-its.md)).

`slangc -target spirv-asm` at default optimization (`OptimizationLevel::Default`) DOES run `spirv-opt` (specifically `CreatePrivateToLocalPass` et al.) — this is not the same as `-O0` ([slangc -target spirv-asm at default opt DOES run spirv-opt (private-to-local)](../learnings/1781714495665-slangc-target-spirv-asm-at-default-opt-does-run-sp.md)). The `private-to-local` pass demotes any `Private` variable used in exactly one function to `Function` storage, which breaks cross-call persistence for function-`static` locals. Fix: remove the two live registrations of `CreatePrivateToLocalPass` ([SPIR-V function-static state loss = SPIRV-Tools private-to-local (and how to CLI-repro)](../learnings/1781713084820-spir-v-function-static-state-loss-spirv-tools-priv.md)).

**spirv-opt load is fatal via real sink:** `createArtifactFromIR` unconditionally calls `getOrLoadDownstreamCompiler(SpirvOpt, getSink())`. If `slang-glslang` is absent, the sink is poisoned with E00100 before any output guard. Deliberately-optional callers pass `nullptr` sink ([slang downstream-compiler load: optional spirv-opt is fatal via real sink + half-wired GLSLANG gate](../learnings/1781799089141-slang-downstream-compiler-load-optional-spirv-opt-.md), [Slang downstream-compiler absence (spirv-opt/slang-glslang) IS unit-testable via setSharedLibraryLoader](../learnings/1781804734767-slang-downstream-compiler-absence-spirv-opt-slang-.md)). For opt-out via CMake escape-hatch, follow the `SLANG_ENABLE_SPIRV_OPT_MERGE_RETURN` pattern: `advanced_option`, compile definition wired through `CompilerFlags.cmake`, and `#if ... #endif` guards around ALL registrations including dead `#if 0` blocks ([slang-glslang: add an opt-out via dedicated CMake escape-hatch, not by deleting the pass](../learnings/1781975592365-slang-glslang-add-an-opt-out-via-dedicated-cmake-e.md)).

**fp8 scalar float constants abort in spirv-tools:** `GetWordsFromScalarFloatConstant` in `external/spirv-tools/source/opt/folding_rules.cpp` asserts `width == 16 || 32 || 64`; width-8 (fp8) is missing. The integer sibling handles width-8 correctly. Tracked as issue #11766 for an upstream fix; Slang-side workaround is expected-failure (#11744) ([fp8 scalar float constants abort in spirv-tools constant folding (width-8 gap)](../learnings/1782449605671-fp8-scalar-float-constants-abort-in-spirv-tools-co.md)).

## Debug Information

**-g0 does not zero all debug info:** `OpSource` is emitted unconditionally at `slang-emit-spirv.cpp:11838` without consulting the debug level. `OpName`/`OpMemberName` are emitted from `kIROp_NameHintDecoration` which also survives at g0. Neither is gated on `m_targetProgram->getOptionSet().getDebugInfoLevel()` ([slang -g0 doesn't zero SPIR-V debug info: OpSource/OpName bypass the IRDebug gating path](../learnings/1782145409789-slang-g0-doesn-t-zero-spir-v-debug-info-opsource-o.md)).

**Synthesized `$init` constructors get full debug info** (issue #11550): synthesized ctors inherit the struct's source loc and their member-store stmts inherit field locs — both are lowered like user functions, giving them `DebugFunction`/`DebugScope` entries. Suppressing the function-level scope without also suppressing body `DebugLine`s leaves invalid NonSemantic debug SPIR-V. Gate on AST flavor (`SynthesizedDefault | SynthesizedMemberInit`) during lowering ([Slang: synthesized $init ctors get full source-level SPIR-V debug info (no synthesized-func filter)](../learnings/1781168370965-slang-synthesized-init-ctors-get-full-source-level.md)).

**User-defined `__init` lacks `this` in debug info** (issue #11565): a constructor's `this` is a synthesized local var (not an IRParam), so the debug-var pass doesn't pick it up. Fix: after emitting `thisVar` at `slang-lower-to-ir.cpp:13839`, add a "this" name hint + `IRDebugLocationDecoration` so the local-var loop emits it ([Slang: user-defined __init lacks `this` in SPIR-V debug info — root cause + fix site](../learnings/1781206942083-slang-user-defined-init-lacks-this-in-spir-v-debug.md)).

**Extension method debug names:** `ExtensionDecl` is anonymous (`getName()` returns null), so extension method name hints are emitted unqualified. Fix: redirect the `getNameForNameHint` recursion through the extended type's decl. Note: the name hint also feeds C-like emitter identifiers (HLSL/CUDA), not just SPIR-V debug names ([getNameForNameHint feeds C-like emitter identifiers (not just SPIR-V debug); ExtensionDecl is anonymous](../learnings/1781267563910-getnamefornamehint-feeds-c-like-emitter-identifier.md)).

**DebugScope variable-arity rule:** `IRDebugScope` must be built with 1 or 2 operands (1 when `inlinedAt` is absent) — never with 2 operands where operand 1 is null. A null operand crashes `buildEntryPointReferenceGraph` at `slang-ir-call-graph.cpp:~85` which iterates operands unguarded ([SPIR-V DebugScope must be variable-arity (null IR operand crashes buildEntryPointReferenceGraph)](../learnings/1781568128097-spir-v-debugscope-must-be-variable-arity-null-ir-o.md)).

**`-g2` test self-match trap:** At `-g2`, the full source is embedded as an `OpString`. A `CHECK-NOT` pattern whose text appears in a `//CHECK` directive line will falsely fail by matching the embedded copy. Use real quotes or `@LINE`-relative patterns to avoid this ([slang -g2 spirv-asm FileCheck tests: embedded-source self-match trap + local env gotchas](../learnings/1781176200581-slang-g2-spirv-asm-filecheck-tests-embedded-source.md)).

**`-g2` + `import`/`#line` debug-info cluster (all producer-side, all in `slang-lower-to-ir.cpp`):** three distinct 2026-07 defects (#11982/#11983/#11984, same reporter/subsystem, not dups) share a debug-info emission surface where the *consumer* (`slang-emit-spirv.cpp` around :2136) is innocent — fix the producer. (1) **DebugFunction scope** for imported functions gets pinned to the entry-point's `DebugCompilationUnit` because `IRDebugFunction` carries no scope operand and a post-pass (PR #10907) unconditionally forces the one module-global scope slot to the entry-point source; fix builds a `DebugSource → CU` map at emit and resolves scope per-file ([SPIR-V DebugFunction scope uses one module-global slot pinned to entry-point CU (#11983)](../learnings/1783465931766-slang-spir-v-debugfunction-scope-uses-one-module-g.md)). (2) **Duplicate DebugSource** for one physical file arises because two producers spell the filename operand differently (`getMostUniqueIdentity()` relative vs `getName()` absolute for `PathInfo::Type::Normal`), defeating the `hoistable=true` auto-dedup — for a hoistable IR inst, two producers MUST construct identical operands or dedup silently fails, a string/path operand being the classic divergence point ([duplicate SPIR-V DebugSource from divergent path spelling defeating hoistable-dedup (#11982)](../learnings/1783466061131-slang-11982-duplicate-spir-v-debugsource-from-dive.md)). (3) **BOM in DebugSource** for a `#line`-named file: the fresh-read fallback `getOrEmitDebugSource` wraps the raw blob bytes as content, bypassing `SourceFile::setContents`'s canonical BOM strip (`CharEncoding::determineEncoding`), so a UTF-8 BOM lands in the `OpString` while line/col data stays BOM-free — decode/BOM-strip in the fallback ([-g2 DebugSource for #line files bypasses canonical BOM strip (fresh-read fallback) (#11984)](../learnings/1783468265984-slang-11984-g2-debugsource-for-line-files-bypasses.md)). Repro note for all three: the disassembly path needs slang-glslang, present only in `build/Debug/lib`, so use `LD_LIBRARY_PATH=build/Debug/lib ... -target spirv-asm` or emit binary `-emit-spirv-directly -o out.spv` and grep raw bytes; `-dump-ir` is authoritative for "does dedup actually happen" (DeepWiki reasons from symbol names and confidently got the DebugSource dedup wrong).

## Depth Output Execution Modes

**Direct SPIR-V path** emits both `DepthReplacing` AND `DepthGreater`/`DepthLess` for `SV_DepthGreaterEqual`/`SV_DepthLessEqual`. When two depth-affecting builtins coexist (e.g. `SV_Depth` + `SV_DepthGreaterEqual`), `maybeEmitEntryPointDepthReplacingExecutionMode` collapses to `DepthReplacing` only via the `else if (mode != thisMode)` conflict branch ([Slang SV_Depth{Greater,Less}Equal: direct SPIR-V correct, GLSL/via-GLSL drops directional mode](../learnings/1782165817624-slang-sv-depth-greater-less-equal-direct-spir-v-co.md), [Slang direct-SPIR-V depth mode ALSO dropped via conflict-branch (dual depth-affecting vars) — not just GLSL](../learnings/1782211031715-slang-direct-spir-v-depth-mode-also-dropped-via-co.md)).

**Output topology (hull/domain):** The `case kIROp_OutputTopologyDecoration:` hull/domain arm handles only TriangleCW/TriangleCCW/Point; `Line` falls through to the mesh-shader fallback arm emitting `OutputLinesEXT` — invalid on tess stages. For isoline, `Line` should be a no-op (the `OpExecutionMode Isolines` from `[domain("isoline")]` already implies it). The fallback must be stage-guarded ([Slang SPIR-V output-topology: hull/domain arm only handles a subset of OutputTopologyType — Line falls through to mesh OutputLinesEXT](../learnings/1780253722562-slang-spir-v-output-topology-hull-domain-arm-only-.md)).

## Validation and Testing

**spirv-val must be run explicitly:** A clean `slangc` exit is not sufficient. The compiler can emit structurally invalid SPIR-V that only `spirv-val` detects ([SPIR-V issues require spirv-val, not just slangc exit code](../learnings/dashboard_slang-triage-1776263007885.md)). Under CI, `SLANG_RUN_SPIRV_VALIDATION=1` is set — a unit test asserting the absence of a validation-related diagnostic will pass locally and fail in CI ([CI runs slang-test with SLANG_RUN_SPIRV_VALIDATION enabled — unit tests must not assume validation is off](../learnings/1781825083619-ci-runs-slang-test-with-slang-run-spirv-validation.md)).

**Text FileCheck cannot catch malformed access-chain bases:** `//TEST:SIMPLE` does NOT run SPIR-V validation. For descriptor-heap / access-chain fixes, run `SLANG_RUN_SPIRV_VALIDATION=1` out-of-band or write FileCheck that pins the base to the heap builtin global ([SPIRVLoadDescriptorFromHeap heap operand is a pointer global at emit — never parameterize it in call-spec](../learnings/1780769340224-spirvloaddescriptorfromheap-heap-operand-is-a-poin.md)).

**Triage discriminator — embedded spirv-tools vs system validation layer:** At HEAD ~2026-06 the embedded spirv-tools is v2026.2. A VUID that fires only in a maintainer's system layer (Vulkan SDK 1.4.328+) is validator version-skew, not a master regression ([Triage discriminator: Slang embedded spirv-tools vs system Vulkan validation layer (version-skew)](../learnings/1782267495019-triage-discriminator-slang-embedded-spirv-tools-vs.md)).

**Local verify LD_LIBRARY_PATH trap:** Always put `build/Debug/lib` FIRST in `LD_LIBRARY_PATH` when verifying fixes locally. The package-staging dir (`build/slang-<ver>-linux-x86_64/lib/`) holds a stale `libslang-compiler.so` that silently overrides the fresh build ([slang local spirv-asm verify: put build/Debug/lib FIRST in LD_LIBRARY_PATH or you test a stale compiler](../learnings/1782733787106-slang-local-spirv-asm-verify-put-build-debug-lib-f.md)).

**Vulkan build dependencies:** Building any RHI-dependent target on Linux requires `libx11-dev` (header `X11/Xlib.h`), even headless. `slang-rhi` CMakeLists.txt fetches Vulkan-Headers via FetchContent; the bundled headers unconditionally include `X11/Xlib.h` ([CONSOLIDATED: slang-rhi Vulkan build needs libx11-dev (X11/Xlib.h)](../learnings/1780330247261-CONSOLIDATED-slang-rhi-vulkan-build-needs-libx11-dev.md)). The GPU environment may have NVIDIA hardware but no Vulkan loader — installing `libvulkan1` gives only software llvmpipe; real-GPU Vulkan needs the container to be provisioned with graphics device injection ([This environment HAS an NVIDIA GPU (nvidia-smi works) despite CLAUDE.md saying 'no GPU' — but no Vulkan loader, so Vulkan execution still unavailable](../learnings/1781698311286-this-environment-has-an-nvidia-gpu-nvidia-smi-work.md), [Correction: installing libvulkan1 does NOT enable NVIDIA-GPU Vulkan in these containers — only software llvmpipe; NVIDIA Vulkan ICD fails (ERROR_INCOMPATIBLE_DRIVER, NVIDIA_VISIBLE_DEVICES=void)](../learnings/1781699613539-correction-installing-libvulkan1-does-not-enable-n.md)).

**Runtime tests for new extensions:** A `COMPARE_COMPUTE` slang-test for a Vulkan extension requires a `SLANG_RHI_FEATURES` entry in slang-rhi. The `SLANG_RHI_FEATURES` X-macro is the bridge: one line auto-generates the `rhi::Feature` enum entry AND makes `-render-feature <name>` valid in slang-test. Cross-repo sequence: slang-rhi gate PR → bump slang-rhi pin → slang test-re-enable PR ([Slang RHI feature gate + disabled Vulkan runtime test: the SLANG_RHI_FEATURES X-macro is the cross-repo bridge](../learnings/1780969782467-slang-rhi-feature-gate-disabled-vulkan-runtime-tes.md), [slang-rhi NV-extension feature gate (fp16-vector atomics) + Vulkan-Headers v1.4.318 retires bump caveat](../learnings/1780970406624-slang-rhi-nv-extension-feature-gate-fp16-vector-at.md), [Runtime slang-test for a new Vulkan extension is gated on slang-rhi harness support (abort/VK_KHR_shader_abort #11790)](../learnings/1782515089370-runtime-slang-test-for-a-new-vulkan-extension-is-g.md)).

## Miscellaneous SPIR-V Pitfalls

- **VulkanBindShift C++ API:** `HLSLToVulkanLayoutOptions::Kind` encoding: u=0, s=1, t=2, b=3. Pack: `intValue0 = (kind << 24) | (set & 0xFFFFFF)`, `intValue1 = shift` ([Slang VulkanBindShift C++ API kind encoding (u=0,s=1,t=2,b=3)](../learnings/1780993682325-slang-vulkanbindshift-c-api-kind-encoding-u-0-s-1-.md)).
- **SlangPy kernel abstraction:** Fully collapses to zero overhead in Slang SPIR-V at `-O3` — 0 `OpFunctionCall`, byte-identical to a hand-rolled kernel. Runtime index arithmetic (1 sdiv + 1 srem + strided loads) is design-inherent, not a codegen defect ([SlangPy generated-kernel abstraction fully folds in slang SPIR-V (-O3), but is deferred to nvrtc for CUDA target](../learnings/1781017004302-slangpy-generated-kernel-abstraction-fully-folds-i.md), [Measure SlangPy generated-kernel codegen quality GPU-free via slangc SPIR-V opcode census](../learnings/1781050949927-measure-slangpy-generated-kernel-codegen-quality-g.md)).
- **wgpu VUID errors come from Dawn/tint, not Slang:** VUID errors during `wgpu` variant tests are emitted by Dawn's internal tint regenerating SPIR-V from WGSL, not from Slang's emitter. Fingerprint: tint uses `uint` array-length constants; Slang uses `int` ([slang-test wgpu COMPARE_COMPUTE VUID errors come from Dawn's internal tint, not Slang's SPIR-V emitter](../learnings/1782499611532-slang-test-wgpu-compare-compute-vuid-errors-come-f.md)).
- **Conditional<T,b> ICE:** `Conditional<T,b>` with a non-literal `hasValue` param survives `lowerConditionalType` and reaches emit as `makeConditionalValue` → ICE. Root cause is an upstream producer (autodiff, dynamic dispatch, or higher-order params) leaking an unresolved generic value param ([Conditional&lt;T,b&gt; ICE: non-literal flag survives lowerConditionalType to spirv-emit](../learnings/1782485307814-conditional-lt-t-b-gt-ice-non-literal-flag-survive.md)).
- **Stale prebuilt slangc:** Check `slangc -v`'s `<sha>` against `git rev-parse HEAD` before trusting compiled output. Function-static SPIR-V storage bugs are masked by inlining and NOT reproducible via `slangc -target spirv-asm` at default opt ([Stale prebuilt slangc + inlining mask SPIR-V static-local bugs (verify-at-HEAD pitfalls)](../learnings/1781708255374-stale-prebuilt-slangc-inlining-mask-spir-v-static-.md)).
- **`OpCompositeConstruct` of logical-pointer array (#9062):** SPIR-V logical addressing forbids logical (StorageBuffer/Uniform/Workgroup) pointers as operands to `OpCompositeConstruct`. The resource-legalization passes that eliminate arrays-of-resources only inspect the array's *direct* element type (they `unwrapArray()` through nested arrays and check whether the unwrapped type is *directly* a resource) — they do **not** recurse into struct fields, so a `struct { RWStructuredBuffer buf; }` used as `MyPtr[2]` is invisible while a bare `RWStructuredBuffer[2]` is caught. Autodiff reliably materializes this shape (`Array<T,N> : IDifferentiablePtrType` → whole array wrapped as one `DifferentialPtrPair` → lowered to a struct), which is why the maintainer couldn't repro without autodiff; pass ordering is fine (resource legalization runs after autodiff) — the escape is *structural blindness*, not a timing gap ([OpCompositeConstruct of logical-pointer array escapes resource legalization (struct-field blindness)](../learnings/1783524098198-slang-9062-spir-v-opcompositeconstruct-of-logical-.md)).
- **`spirv_asm` named registers leak into `OpName` (#12002):** the emitter emits an `OpName` for *every* named `%id` register in a `spirv_asm` block, unconditionally (not gated on debug-info level). Texture-sample intrinsics in `hlsl.meta.slang` name their raw-sample register `%sampled`, so every texture sample emits `OpName ... "sampled"` regardless of caller variable names. For any "debug name on the wrong inst" claim, the decisive discriminator is a **rename drill**: rename the user's variable and recompile — if the disputed `OpName` follows the rename it's real propagation, if it stays it's an emitter-baked intrinsic name and the match was coincidence ([spirv_asm named registers leak into OpName; texture-sample intrinsic uses %sampled](../learnings/1783525064110-slang-12002-spirv-asm-named-registers-leak-into-op.md)).
- **Sampler-vs-texture `[noinline]` param asymmetry (#12004):** `isIllegalSPIRVParameterType` always specializes textures but only specializes samplers when they are *arrays* (a legacy compile-time-blowup guard, #3546), so a scalar `SamplerState` param stays by-value `OpTypeSampler` while the texture is passed by index and reloaded in the callee. Emission is symmetric — the by-value-vs-by-index location is decided entirely by the specialize step, not emit, so don't chase it in the emitter; fixes here MUST `spirv-val`, not just FileCheck ([sampler-vs-texture noinline SPIR-V param asymmetry — isIllegalSPIRVParameterType isArray gate](../learnings/1783527024741-slang-12004-sampler-vs-texture-noinline-spir-v-par.md)).
- **`double→bool` cast emits a 1-word `OpConstant` (#12019):** `emitFloatToIntCast`'s float→bool path calls `builder.getIntValue(fromType, 0)` with the *double* type, minting a `kIROp_IntLit` stamped with a float type; at emit `emitIntConstant` switches on the type, double hits `default:` → one 32-bit word, but a float64 `OpConstant` needs two → "says it has 4 words, found 5". Only double is structurally invalid (for float/half the 1-word path accidentally matches the IEEE bit pattern). Fix at the producing cast (`getFloatValue(fromType, 0.0)`, mirroring the correct bool→float sibling), not by special-casing `emitIntConstant` — an int-lit-with-float-type should never exist ([double→bool SPIR-V: getIntValue(floatType,0) mints int-lit with float type → 1-word OpConstant](../learnings/1783587817471-slang-12019-double-bool-spir-v-getintvalue-floatty.md)).
- **`spirv-opt --strip-debug` deletes `DebugBuildIdentifier` (#8681):** you cannot just replace the hand-rolled `stripDbgSpirvFromArtifact` with spirv-opt's strip passes — that function is the `-separate-debug-info` splitter and hand-rolls the strip precisely to keep the DBI ext-inst (the hash linking the stripped main module to its `.dbg.spv`). Both `CreateStripDebugInfoPass` and `CreateStripNonSemanticInfoPass` unconditionally kill the DBI; the correct shape is a hybrid where spirv-opt owns the bulk strip but a thin Slang shim exempts/re-injects the DBI + its OpString + the NonSemantic import — the DBI exemption is irreducible ([spirv-opt strip-debug deletes DebugBuildIdentifier — separate-debug-info needs a DBI shim](../learnings/1783556986544-slang-8681-spirv-opt-strip-debug-deletes-debugbuil.md)).
- **64-bit buffer indexing bifurcates (#11967):** direct concrete-buffer subscript (`RWStructuredBuffer<int>[uint64_t idx]`) preserves `UInt64` to `OpAccessChain` and works, but the interface-constrained `IArray<T>`/`IRWArray<T>.__subscript(int index)` path (core.meta.slang, hard `int`) silently truncates a `uint64_t` index to 32-bit (front-end `warning E30081`). The fix is to widen the IArray/IRWArray subscript *requirement* to a generic `TIndex`, mirroring the buffer/Ptr subscripts — with the caveat that widening an interface requirement can force `Array`/`vector`/`matrix` conformances to re-satisfy. A >2^32-element execution test is impractical (>8 GB buffer), so the E2E guard is necessarily a codegen FileCheck for `OpTypeInt 64` on `OpAccessChain` ([64-bit indexing works on direct subscript but IArray/IRWArray truncates to 32-bit](../learnings/1783415716330-slang-64-bit-buffer-indexing-works-on-direct-subsc.md)).
- **vec3 `OpConstantComposite` over-count is a downstream spirv-opt fold, NOT Slang emit (#12104):** `output[0] = (value<eps) ? 0 : (value/value)` for a runtime `float3` emits `OpConstantComposite %v3float %float_1 ×4` (4 constituents on a vec3 → spirv-val rejects). The maintainer's (jkwak) leads pointed at Slang's own `processConstructor`/`emitCompositeConstruct`/`MakeVector` — those are WRONG. Layer-isolation drill (reusable for any "invalid SPIR-V only at optimization" bug): (1) `-dump-ir` shows `div(%value,%value)` surviving intact through every pass → Slang IR isn't folding it; (2) `-O0` emits correct `OpFDiv` while default/`-O1/2/3` emits the malformed constant — the only pipeline delta is `slang-emit.cpp` routing SPIR-V through `PassThroughMode::SpirvOpt` (vendored `external/spirv-tools` spirv-opt @ v2026.3); (3) `-fp-mode precise` → clean `OpFDiv ; NoContraction`, `-fp-mode fast` → malformed, confirming an IEEE-unsafe `x/x→1` fold (0/0=NaN); (4) width sweep shows a HARDCODED 4 constituents (float2→4, float3→4, float4→4), NOT a vec4→vec3 result-type shrink — the vec2-with-4 case is the smoking gun. Slang's own peephole folds `v-v` correctly to a 3-constituent zero-vector and leaves `v*v` as `OpFMul`; only `v/v` (no `x/x→1` rule in slang-ir-peephole/sccp) reaches the vulnerable downstream fold. Fix options: (A, true root) fix `external/spirv-tools/source/opt/folding_rules.cpp` to use the result type's component count (upstream + submodule bump); (B, in-tree actionable now) add a fast-math-gated `x/x→1` Slang peephole fold producing a correctly-sized ones vector so spirv-opt never sees the vulnerable `OpFDiv %a %a`. An emit-time constituent-count assert in `emitOpConstantComposite` (which has no `constituent-count==element-count` check today, `slang-emit-spirv-ops.h:1417-1425`) is a useful general net that converts silent-invalid-output into a loud failure, but does NOT catch this bug — the malformed constant is minted post-emit in spirv-opt ([#12104 vec3 OpConstantComposite 4-constituent bug is in downstream spirv-opt, not Slang (x/x fold)](../learnings/1784072018251-slang-12104-vec3-opconstantcomposite-4-constituent.md)). An earlier repro-blocked triage of the same issue (jkwak's `float3(float2(1,1),1)` minimal case validates fine; ~11 minimal shapes on near-HEAD all emit a correct 3-constituent composite) correctly ruled out the sub-vector UNDER-count path (`processConstructor`'s flatten at `slang-ir-spirv-legalize.cpp:1795-1810` already handles that) and flagged a post-legalize fold (`simplifyIRForSpirvLegalization`) as the suspect — since superseded by the spirv-opt root cause above, but the standing `emitOpConstantComposite` count-assert recommendation still holds ([vec3 OpConstantComposite over-count — repro-blocked, post-legalize fold suspect (#12104)](../learnings/1784069141044-slang-spir-v-vec3-opconstantcomposite-over-count-r.md)).

## Contradictions / Supersessions

- [Triaging an &quot;add a SPIR-V capability bit&quot; request — vendored-operand check + inert-atom scoping](../learnings/1781116222932-triaging-an-quot-add-a-spir-v-capability-bit-quot-.md) originally advised gating on `isSPIRV(...)`, but this is incorrect — [CORRECTION: SPIR-V capability emit-gating — use implies(CapabilityAtom::spv*), NOT isSPIRV()](../learnings/1781123971590-correction-spir-v-capability-emit-gating-use-impli.md) supersedes it: gate on `targetCaps.implies(CapabilityAtom::spvFooEXT)`.
- [CORRECTION to #11631 version root cause: require atom stamped only on layout IR module, not the codegen module determineSpirvVersion runs on](../learnings/1781643037138-correction-to-11631-version-root-cause-require-ato.md) supersedes an earlier root-cause analysis of `[require(spirv_1_5)]` not raising the SPIR-V version, identifying the primary cause as the decoration only reaching the layout IR module.
- [GLSL legalize: per-entry-point system-value decorations must gate on VaryingOutput (inout double-attach)](../learnings/1782175454099-glsl-legalize-per-entry-point-system-value-decorat.md) corrects [GLSL legalize: per-entry-point system-value decorations must gate on VaryingOutput (inout double-attach)](../learnings/1782173862922-glsl-legalize-per-entry-point-system-value-decorat.md): the inout double-attach is unreachable for depth specifically because depth semantics are output-only; both still recommend VaryingOutput gating as defense-in-depth.

## Capability/validation fixes: declare OpCapability inline, test with -target spirv

A `[require]` capability atom does **not** auto-emit `OpCapability` for `spirv_asm` builtin-loads — e.g. `RayCurrentTime()` emitted `BuiltIn CurrentRayTimeNV` but no `OpCapability RayTracingMotionBlurNV`, so spirv-val rejected the module (VUID-...-pCode-08737). Declare the capability **inline** in the `spirv_asm` block ([[require] capability atom does NOT auto-emit OpCapability for spirv_asm builtin-loads — declare it inline](../learnings/1782828754941-require-capability-atom-does-not-auto-emit-opcapab.md)). When writing the regression test for a SPIR-V *validation* fix (missing `OpCapability`/`OpExtension` that spirv-val or `vkCreateShaderModule` rejects), the test must use a real **`-target spirv`** directive so the module is actually validated — a `spirv_asm`-only or `-target spirv-asm` check can pass without exercising validation ([SPIR-V capability/validation fixes need a -target spirv test directive, not just spirv-asm](../learnings/1782833127380-spir-v-capability-validation-fixes-need-a-target-s.md)).

## Signature-derived VariablePointers capability must gate on [noinline], not hasUses()

When declaring a SPIR-V capability from a function's *signature type* (rather than a value/op site), gate it on the function surviving inlining — check `[noinline]` via `findDecoration`, not `hasUses()` — or you over-declare `VariablePointers` for a function that gets inlined away (driver bug #9061) ([Signature-derived SPIR-V VariablePointers must gate on [noinline], not hasUses() (driver bug #9061)](../learnings/1781023718622-signature-derived-spir-v-variablepointers-must-gat.md)).

## [require] atoms don't reach the codegen capability set (SS-class silent divergence)

Entry-point `[require(...)]` capability atoms do **not** update the SPIR-V codegen capability set, so source-level `[require]` silently diverges from what codegen decides — the version path has a partial compensation, bindless has none (#11631, commit 890f8bef5) ([Slang [require] capabilities never reach codegen cap set; version path has a partial compensation, bindless has none](../learnings/1781638061693-slang-require-capabilities-never-reach-codegen-cap.md)). Severity refinement: the `[require]`-drop is a **silent runtime-divergence (SS-class)** that only bites codegen-DECISION paths, not a wrong/invalid-SPIR-V emit ([slang#11631 severity: [require]-drop is a SILENT runtime-divergence (SS-class), and only hits codegen-DECISION caps](../learnings/1781686744418-slang-11631-severity-require-drop-is-a-silent-runt.md)). A `[require]` atom also does not auto-emit `OpCapability` for `spirv_asm` builtin-loads — declare it inline ([[require] capability atom does NOT auto-emit OpCapability for spirv_asm builtin-loads — declare it inline](../learnings/1782828754941-require-capability-atom-does-not-auto-emit-opcapab.md)).

## HitObject SM6.9+NVAPI: verify the premise before fixing (implies()-inert was WRONG)

A cautionary correction chain. First read: on `sm_6_9` + NVAPI SER, `HitObject` codegen emits an invalid mix because `CapabilitySet.implies()` is inert on disjunctive/abstract target caps ([Slang HitObject SM6.9+NVAPI invalid mix — CapabilitySet.implies() is inert on disjunctive/abstract target caps](../learnings/1782986638349-slang-hitobject-sm6-9-nvapi-invalid-mix-capability.md)). A HEAD revert-drill then showed that premise was **empirically false** ([Verify a triage's predicate premise at HEAD (revert-drill) before fixing on top of it — #11889 HitObject NVAPI premise was empirically false](../learnings/1782991917563-verify-a-triage-s-predicate-premise-at-head-revert.md)); the real bug is a **2-arg Invoke ABI gap**, and the original wrong read came from a stale binary ([CORRECTION: Slang HitObject SM6.9+NVAPI — refutes my earlier "implies() inert" learning; real bug is 2-arg Invoke ABI gap; stale-binary meta-lesson](../learnings/1782992753685-correction-slang-hitobject-sm6-9-nvapi-refutes-my-.md)). Meta-lesson: revert-drill the triage predicate at HEAD before building a fix on top of it.

## Direct SPIR-V emitter never emitted NoContraction (fp-mode precise was a no-op)

Slang's *direct* SPIR-V backend (`slang-emit-spirv.cpp`) never emitted the `NoContraction` decoration, so `-fp-mode precise` / `precise` were no-ops on `-target spirv` (#11933) ([Slang direct SPIR-V emitter never emits NoContraction — fp-mode/precise are no-ops there](../learnings/1783064216848-slang-direct-spir-v-emitter-never-emits-nocontract.md)). Fix: emit `NoContraction` at the `emitArithmetic` choke point for the precise fp-mode (PR #11935) ([Slang direct SPIR-V: emit NoContraction for -fp-mode precise at the emitArithmetic choke point (issue #11933)](../learnings/1783067747847-slang-direct-spir-v-emit-nocontraction-for-fp-mode.md)).

## spirv-opt crash suppressions are often droppable via the -O0 default

A test suppressed because it ABORTS inside `spirv-opt` can often have its expected-failure entry dropped simply because slang-test now defaults to `-O0` (PR #11805) — the crash never triggers at O0 — independent of any upstream spirv-tools fix ([spirv-opt crash suppressed via expected-failure list is often droppable via the -O0 default (PR #11805), independent of the upstream fix](../learnings/1783036168133-spirv-opt-crash-suppressed-via-expected-failure-li.md)).


## Recent operational learnings (incremental fold 2026-07-17)

**CORRECTION: SPIR-V SER at 1.4 without PSB — bundled spirv-val ACCEPTS it (triage claim overturned)** — **Correction to prior learning "SPIR-V SER capability over-requires 1.5; [CORRECTION: SPIR-V SER at 1.4 without PSB — bundled spirv-val ACCEPTS it (triage claim overturned)](../learnings/1784102979347-correction-spir-v-ser-at-1-4-without-psb-bundled-s.md)

**Slang capdef atoms never auto-emit an OpExtension — emission is always explicit C++** — **Context:** slang#12097 — maintainer jkwak-work asked why a SPIR-V extension dependency couldn't be modeled purely in `slang-capabilities.capdef` (e.g. [Slang capdef atoms never auto-emit an OpExtension — emission is always explicit C++](../learnings/1784120354355-slang-capdef-atoms-never-auto-emit-an-opextension-.md)

**[approver/confirmed] SPIR-V UniformConstant load-coalescing is a decoration-neutral safe shape** — **Symptom / context:** PR #12111 (fixes #12051), our own fixer's bot-authored PR, coalesces redundant per-use `OpLoad`s of a read-only `UniformConstant` resource-element address in `insertLoadAtLatestLocation` (slang-ir-spirv-legalize.cpp). [[approver/confirmed] SPIR-V UniformConstant load-coalescing is a decoration-neutral safe shape](../learnings/1784126649253-approver-confirmed-spir-v-uniformconstant-load-coa.md)

**CORRECTION slang#12051: 'orthogonal to #12027 workaround' was verified on the WRONG shape — [noinline] changes the answer; #12111 DOES coalesce caller-side resource loads across call boundaries** — **A public GitHub answer I posted ("Option A and the #12027 descriptor-index workaround are cleanly orthogonal, A can't reach across the function boundary") was WRONG for the `[noinline]` case — because both the fixer and I verified it on the FULLY-INLINED shape.** The reporter pushed back with a `[noinline]` repro; [CORRECTION slang#12051: "orthogonal to #12027 workaround" was verified on the WRONG shape — [noinline] changes the answer; #12111 DOES coalesce caller-side resource loads across call boundaries](../learnings/1784127324690-correction-slang-12051-orthogonal-to-12027-workaro.md)

**slang#12051/#12111 — DescriptorHandle load-coalescing interacts with [noinline] + #12027 image-as-index workaround (benign)** — When reasoning about how a SPIR-V load-coalescing change (PR #12111, coalesce dominating read-only UniformConstant resource-element loads in insertLoadAtLatestLocation) interacts with function boundaries, the INLINED case and the [noinline] case behave differently — analyze both: [slang#12051/#12111 — DescriptorHandle load-coalescing interacts with [noinline] + #12027 image-as-index workaround (benign)](../learnings/1784128384354-slang-12051-12111-descriptorhandle-load-coalescing.md)

**slang bindless [noinline]: image passes as uint INDEX (workaround), sampler as real descriptor — resolve SPIR-V operand types from OpTypeFunction, never label by source position** — **On slang#12051/#12111 I mislabeled which operand was the texture vs the sampler across TWO turns of public GitHub answers, and had to correct it.** The reporter caught it. [slang bindless [noinline]: image passes as uint INDEX (workaround), sampler as real descriptor — resolve SPIR-V operand types from OpTypeFunction, never label by source position](../learnings/1784128407283-slang-bindless-noinline-image-passes-as-uint-index.md)

**[approver/confirmed-safe] SER SPIR-V 1.4 fix (12115) — capdef floor + PSB OpExtension both present = the correct two-part fix; recall precedent held** — **PR:** shader-slang/slang#12115 "Allow shader invocation reorder extensions with SPIR-V 1.4" (fixes #12097), bot-authored fix/issue-12097. [[approver/confirmed-safe] SER SPIR-V 1.4 fix (12115) — capdef floor + PSB OpExtension both present = the correct two-part fix; recall precedent held](../learnings/1784139502358-approver-confirmed-safe-ser-spir-v-1-4-fix-12115-c.md)

**[approver/clause-gap] New capdef ATOM (not just floor edit) trips BOTH check-cmdline-ref AND check-capability-atoms-ref — wait for them green** — **Context:** slang#12115 revised 3× via synchronize. [[approver/clause-gap] New capdef ATOM (not just floor edit) trips BOTH check-cmdline-ref AND check-capability-atoms-ref — wait for them green](../learnings/1784139517684-approver-clause-gap-new-capdef-atom-not-just-floor.md)

**CORRECTION: emitted SPIR-V version comes from getTargetCaps, NOT from used functions' [require] caps** — **This RETRACTS the earlier learning "HitObject::TraceRay forces SPIR-V ≥1.5 (SER) regardless of -profile" — that learning is FACTUALLY WRONG. [CORRECTION: emitted SPIR-V version comes from getTargetCaps, NOT from used functions' [require] caps](../learnings/1784153531683-correction-emitted-spir-v-version-comes-from-getta.md)

**Entry-point [require] caps do NOT drive the emitted SPIR-V version header** — When reasoning about what SPIR-V version Slang emits (`; [Entry-point [require] caps do NOT drive the emitted SPIR-V version header](../learnings/1784157894453-entry-point-require-caps-do-not-drive-the-emitted-.md)

**A GLSL-family -profile does not PIN a SPIR-V output version (only implies a floor)** — When deciding whether an explicit `-profile` pins a concrete SPIR-V/Metal OUTPUT version, use the profile's EXPLICIT family+version (`profile.getFamily()` ∈ {ProfileFamily::SPIRV, METAL} and `profile.getVersion()`), gated by the target's format family (`isSPIRV(format)` for SPIR-V — NOT `isKhronosTarget`, which also matches the GLSL text target that emits no SPIR-V version header; [A GLSL-family -profile does not PIN a SPIR-V output version (only implies a floor)](../learnings/1784157903315-a-glsl-family-profile-does-not-pin-a-spir-v-output.md)

**Conditional SPIR-V capability by operand constness → new backend IR op, NOT spirv_asm-block iteration (jkwak, #9382)** — **Context:** shader-slang/slang#9382 — `Gather(s, uv, int2(2,1))` (compile-time-constant offset) wrongly emitted `OpImageGather ... [Conditional SPIR-V capability by operand constness → new backend IR op, NOT spirv_asm-block iteration (jkwak, #9382)](../learnings/1784164953340-conditional-spir-v-capability-by-operand-constness.md)

**slang#12108 triage: spirv_asm __-prefix cleanup — scope spans hlsl AND glsl meta.slang** — **Issue #12108** (bot-opened follow-up to #12002/#12053, assignee+driver jkwak-work): prefix all internal `spirv_asm` result registers in `*.meta.slang` with `__` so the emitter's auto-`OpName` doesn't leak misleading user-facing-looking names into SPIR-V, plus a build assert to prevent regressions. [slang#12108 triage: spirv_asm __-prefix cleanup — scope spans hlsl AND glsl meta.slang](../learnings/1784248363961-slang-12108-triage-spirv-asm-prefix-cleanup-scope-.md)

---

## #12002 OpName Register Leak: Shipped (refute-then-fix)

shader-slang/slang#12002 shipped as PR #12053 (MERGED 2026-07-11, jkwak-work, merge commit 4d91d47bf3). Approach A (narrow, producer-side): renamed the internal spirv_asm result register `%sampled`->`%__sampled` in `hlsl.meta.slang` -- the fixer found 94 whole-token sites (triage grep undercounted at ~20; the register spans OpImageSample{Implicit,Explicit}Lod + OpImageFetch/Read + sparse OpCompositeExtract + the `$(spvLoadInstName)` splice), plus a regression test asserting the OpName is `"__sampled"` and `CHECK-NOT: "sampled"`. Triage lessons that held: the rename-drill discriminator is decisive for "debug name on the wrong inst" claims (rename the user's var, see whether the OpName follows -> real propagation, or stays -> intrinsic-baked name); refuting the reporter's stated hypothesis did NOT mean "not a bug" -- lead the public verdict with the refutation, then the real defect. Approach B (gating the auto-emit-OpName loop) correctly left maintainer-scope: it's a documented/tested feature and gating would make asm names inconsistent with ungated user OpNames ([slang#12002 SHIPPED: refute-then-fix triage of a 'misattributed OpName' that was a coincidental stdlib name collision](../learnings/1783746988745-slang-12002-shipped-refute-then-fix-triage-of-a-mi.md)).

<!-- fold-20260711 -->

On slang#12051, disassembled SPIR-V at HEAD `4d91d47b` settled the open design question: the per-use descriptor reload is an EMIT-TIME ARTIFACT, not a SPIR-V requirement. A `DescriptorHandle<T>` hoisted into a local (`Texture2D tex = texH;`) emits three `OpLoad` from the same `OpAccessChain` on the SPIR-V path (the user's local store is not honored), while HLSL loads once — and the reporter's `pinDescriptor()` (`OpCopyObject`) collapses it to a single `OpLoad` feeding all samples, proving one load is valid SPIR-V. The mechanism is `shouldDuplicateInstAtUseSite` unconditionally duplicating `CastDescriptorHandleToResource` at use sites (`slang-ir-util.cpp:2638`); the cleanest fix (surfaced to jkwak/csyonghe) is to make an explicitly-stored descriptor local "stick" when its def dominates the uses. Tooling note: `-target spirv-asm` needs the glslang disassembler downstream lib a bare Debug build lacks — put a RELEASE `libslang-glslang-*.so` on `LD_LIBRARY_PATH`; `-dump-ir` shows a single shared load (duplication is downstream at emit), so you must disassemble to see the true per-use count ([slang#12051 SPIR-V descriptor reload is an emit-time artifact, proven by OpCopyObject pin](../learnings/1783759451138-slang-12051-spir-v-descriptor-reload-is-an-emit-ti.md)). Separately, for the builtin-var Flat gate (slang#12064): to clear a "skips the Input/BuiltinInput address-space gate" concern on `needFlatDecorationForBuiltinVar`, prove else-branch reachability by tracing CALLERS and their type-asserts, not IR-type intuition — `getBuiltinGlobalVar` asserts a pointer type, so the non-ptr `else` branch is reachable only for `SPIRVAsmOperandBuiltinVar` (always BuiltinInput); note `IRGlobalParam` CAN be non-pointer-typed (only `IRGlobalVar` is always ptr), so the tempting "globals are always ptr" clear is wrong ([SPIR-V builtin-var Flat gate: prove reachability via getBuiltinGlobalVar's ptr-assert](../learnings/1783767618309-approver-challenger-spir-v-builtin-var-flat-gate-p.md)).

<!-- fold-20260712 -->

## slang#12051 DescriptorHandle NonUniform marker (an evolving investigation)

The #12051 perf work (coalesce per-use SPIR-V descriptor loads) turned on whether the coalescing pass needs a divergence guard, and the mechanism was corrected several times before landing. Two distinct clarifications preceded it. First, the per-use descriptor reload it targets is an **EMIT-TIME artifact, not a `shouldDuplicateInstAtUseSite` force-dup** as originally attributed: on the default SPIR-V path the `CastDescriptorHandleToResource` has already lowered to a single `getElement(__slang_resource_heap, index)` in the generic IR, and the triplication is introduced *during SPIR-V emission* (`legalizeIRForSPIRV` + the emit walk, `emitSPIRVFromIR` at `slang-emit-spirv.cpp:11913`) which runs AFTER the final `-dump-ir` snapshot — so the emit realizes one shared `OpAccessChain` (index math emitted once) plus an `OpLoad` re-emitted per use (measured 2 access-chains, 6 loads for 3 samples); `pinDescriptor()`/`OpCopyObject` collapses it to one `OpLoad`, proving the reload is emit strategy, not a validity requirement. The lever is thus in the emit/legalize path (materialize one `OpLoad` when the descriptor-load def dominates its uses), not the generic duplication helper ([CORRECTION #12051: descriptor reload is an EMIT-time OpLoad-per-use, verify emit not just IR](../learnings/1784075115135-correction-slang-12051-spir-v-descriptor-reload-is.md)).

The bigger correction chain concerns the **NonUniform divergence guard** the plan mandated ("dominance + INVARIANCE — don't coalesce divergent/`NonUniformResourceIndex` loads"). That guard turned out to be UNIMPLEMENTABLE and UNNECESSARY: at the coalescing site (`insertLoadAtLatestLocation`, `slang-ir-spirv-legalize.cpp`) uniform and divergent IR are byte-identical because the heap `getElement`'s index carries no `NonUniformResourceIndex` wrapper and no `IRSPIRVNonUniformResourceDecoration` — the `isNonUniform` signal the plan borrowed lives on a DIFFERENT path (the raw resource-array-param path at `:690`), so the guard would be dead code reading an absent signal. Coalescing divergent-hoisted loads is actually CORRECT, not a tolerated bug: per-LANE divergence (SIMD lanes read different descriptors — one load per lane is fine) is not per-ITERATION/temporal variance, a hoisted loop-invariant conversion is one `getElement` per lane correctly reused across N samples, and the already-shipping `spvDescriptorHeapEXT` path does exactly this (divergent-hoisted → 2 loads, 0 NonUniform decoration, valid spirv-val). The missing NonUniform *hint* is a pre-existing, documented-accepted gap (`tests/language-feature/descriptor-handle/desc-heap-nonuniform.slang:8-12`), so the fix is decoration-neutral; NonUniform-preservation on the DescriptorHandle path is a separate maintainer-owned follow-up (issue #12110, PR #12111) ([#12051 option-a: the invariance guard was over-specified](../learnings/1784081353064-slang-12051-option-a-the-invariance-guard-was-over.md), [#12051 DescriptorHandle divergent NonUniform marker stripped before SPIR-V legalize](../learnings/1784081063099-slang-12051-descriptorhandle-divergent-nonuniform-.md)).

**Contradictions / supersessions (within this narrative):** the earliest reading — "the `NonUniformResourceIndex` inst is essentially *never present* on this path; 0 insts / 0 decorations anywhere in `-dump-ir`; specialized away in specialize-function-call" — is WRONG as stated, though its *conclusion* (guard-drop is safe) held. The precise mechanism: the marker DOES exist early, on the pre-handle uint value (`%34 = nonUniformResourceIndex(%33)`), and is then **dropped by the `DescriptorHandle` construction round-trip** — `CastUInt2ToDescriptorHandle` ↔ `CastDescriptorHandleToUInt2` re-extracts the index as a plain swizzle (`%38`) that feeds the heap `getElement` UNMARKED. So it's "present early, dropped by the Cast round-trip," not "never there." Two methodology traps produced the sloppy earlier evidence: (1) a whole-dump `grep -c` for `nonUniformResourceIndex` is inflated by the capability-atom NAME colliding with the inst mnemonic in every function's capabilitySet decoration (166 hits mostly noise) — check the SPECIFIC operand (the `getElement` index def chain), not a raw grep; (2) `-dump-ir` starts its first snapshot too late (`### AFTER validateAndRemoveAssumeAddress`) to see an inst created and dropped in early AST→IR lowering, so "0 occurrences in `-dump-ir`" proves absence only from the first dumped pass onward. Combined with the emit-time lesson above, `-dump-ir` is blind on BOTH ends — very-early insts dropped before the first snapshot AND very-late emit/legalize mutations after the last — so a dump grep is a middle-of-pipeline view only; disassemble the output for late/emit questions and dump a specific early pass (or trace ops) for early-lowering questions ([#12051 CORRECTION — inst survives, round-trip drops it from heap index](../learnings/1784083614756-slang-12051-correction-descriptorhandle-nonuniform.md), [REFINEMENT #12051 — marker exists early then dropped by Cast round-trip; -dump-ir starts too late](../learnings/1784083913871-refinement-slang-12051-the-nonuniform-marker-exist.md)).

**Source learnings (118):**
- [slang-emit-spirv builtin-var cache and the volatile-set cache-hit trap](../learnings/1779612967874-slang-emit-spirv-builtin-var-cache-and-the-volatil.md)
- [IRSPIRVAsmOperandBuiltinVar is hoistable — cross-stage builtin refs always collapse to one inst](../learnings/1779617050641-slang-spirv-asm-operand-builtinvar-is-hoistable-co.md)
- [emitOperand(extraMask) after a user-supplied MemoryAccess word emits invalid SPIR-V](../learnings/1779617068760-slang-emit-spirv-extra-memoryaccess-word-grammar-b.md)
- [Slang SPIR-V entry-point rename: auto-applies only with >1 entry point](../learnings/1779621920622-slang-spir-v-entry-point-rename-auto-applies-only-.md)
- [spvDescriptorHeapEXT stride is opaque for AS — load-type must drive runtime-array base type](../learnings/1779962007180-spvdescriptorheapext-stride-is-opaque-for-as-load-.md)
- [Slang SPIR-V output-topology: hull/domain arm only handles a subset of OutputTopologyType](../learnings/1780253722562-slang-spir-v-output-topology-hull-domain-arm-only-.md)
- [slang-rhi builds headless (no Vulkan SDK/GPU) via CMake FetchContent](../learnings/1780310225210-slang-rhi-builds-headless-no-vulkan-sdk-gpu-via-cm.md)
- [CONSOLIDATED: slang-rhi Vulkan build needs libx11-dev](../learnings/1780330247261-CONSOLIDATED-slang-rhi-vulkan-build-needs-libx11-dev.md)
- [Auditing whether a Vulkan VUID bug propagates to the CUDA backend](../learnings/1780475494503-auditing-whether-a-vulkan-vuid-bug-propagates-to-t.md)
- [ConstantBuffer via spvDescriptorHeapEXT SIGSEGVs in SPIR-V emit](../learnings/1780643259838-constantbuffer-via-spvdescriptorheapext-sigsegvs-i.md)
- [Slang CI: a test-slang-rhi Windows-GPU failure is unrelated to compiler/test-only PRs](../learnings/1780663679121-slang-ci-a-test-slang-rhi-windows-gpu-failure-e-g-.md)
- [spvDescriptorHeapEXT path uses kIROp_SPIRVLoadDescriptorFromHeap, not IRCastDescriptorHandleToResource](../learnings/1780729707220-spvdescriptorheapext-path-uses-kirop-spirvloaddesc.md)
- [spvDescriptorHeapEXT path — fix function-call specialization allowlists](../learnings/1780733286644-spvdescriptorheapext-path-fix-function-call-specia.md)
- [spvDescriptorHeapEXT specialization fix: don't parameterize the heap global](../learnings/1780734760813-spvdescriptorheapext-specialization-fix-don-t-para.md)
- [Slang #11496 SPIR-V SIGSEGV — static getFormatInst() triage hypothesis was wrong](../learnings/1780768927407-slang-11496-spir-v-sigsegv-static-getformatinst-tr.md)
- [SPIRVLoadDescriptorFromHeap heap operand is a pointer global at emit](../learnings/1780769340224-spirvloaddescriptorfromheap-heap-operand-is-a-poin.md)
- [slang spvDescriptorHeapEXT: atomic-dest validator rejects heap texel pointers (E41403)](../learnings/1780865902537-slang-spvdescriptorheapext-atomic-dest-validator-r.md)
- [Descriptor-heap texture atomics: E41403 validator gap (#11506) is separate from format-Unknown spirv-val (#11130)](../learnings/1780869756318-descriptor-heap-texture-atomics-e41403-validator-g.md)
- [Slang SPIR-V variable-pointers cap is declared from value sites, not function signatures](../learnings/1780967438806-slang-spir-v-variable-pointers-cap-is-declared-fro.md)
- [Slang RHI feature gate + disabled Vulkan runtime test: the SLANG_RHI_FEATURES X-macro](../learnings/1780969782467-slang-rhi-feature-gate-disabled-vulkan-runtime-tes.md)
- [slang-rhi NV-extension feature gate (fp16-vector atomics) + Vulkan-Headers v1.4.318 retires bump caveat](../learnings/1780970406624-slang-rhi-nv-extension-feature-gate-fp16-vector-at.md)
- [slang SPIR-V: isolating a signature-only VariablePointers cap repro](../learnings/1780970685981-slang-spir-v-isolating-a-signature-only-variablepo.md)
- [Slang VulkanBindShift C++ API kind encoding (u=0,s=1,t=2,b=3)](../learnings/1780993682325-slang-vulkanbindshift-c-api-kind-encoding-u-0-s-1-.md)
- [SlangPy generated-kernel abstraction fully folds in slang SPIR-V (-O3)](../learnings/1781017004302-slangpy-generated-kernel-abstraction-fully-folds-i.md)
- [Signature-derived SPIR-V VariablePointers must gate on [noinline], not hasUses()](../learnings/1781023718622-signature-derived-spir-v-variablepointers-must-gat.md)
- [When planning a new SPIR-V opcode, check terminator-ness](../learnings/1781039584587-when-planning-a-new-spir-v-opcode-check-terminator.md)
- [Measure SlangPy generated-kernel codegen quality GPU-free via slangc SPIR-V opcode census](../learnings/1781050949927-measure-slangpy-generated-kernel-codegen-quality-g.md)
- [Slang windows-Vulkan LargeBuffer unit-test crash is a flake, not a regression](../learnings/1781114968928-slang-windows-vulkan-largebuffer-unit-test-crash-i.md)
- [Triaging an "add a SPIR-V capability bit" request](../learnings/1781116222932-triaging-an-quot-add-a-spir-v-capability-bit-quot-.md)
- [Adding a SPIR-V capability atom to Slang (capdef recipe)](../learnings/1781120340432-adding-a-spir-v-capability-atom-to-slang-capdef-re.md)
- [Adding a SPIR-V capability atom — ADDENDUM (two generated docs + test -capability)](../learnings/1781122622446-adding-a-spir-v-capability-atom-addendum-two-gener.md)
- [CORRECTION: SPIR-V capability emit-gating — use implies(CapabilityAtom::spv*), NOT isSPIRV()](../learnings/1781123971590-correction-spir-v-capability-emit-gating-use-impli.md)
- [Slang CI: windows falcor "Unknown VCS root" exit 1 is a Vulkan GPU crash artifact](../learnings/1781129282277-slang-ci-windows-falcor-unknown-vcs-root-exit-1-is.md)
- [Slang: synthesized $init ctors get full source-level SPIR-V debug info](../learnings/1781168370965-slang-synthesized-init-ctors-get-full-source-level.md)
- [slang -g2 spirv-asm FileCheck tests: embedded-source self-match trap](../learnings/1781176200581-slang-g2-spirv-asm-filecheck-tests-embedded-source.md)
- [Slang: user-defined __init lacks `this` in SPIR-V debug info](../learnings/1781206942083-slang-user-defined-init-lacks-this-in-spir-v-debug.md)
- [getNameForNameHint feeds C-like emitter identifiers; ExtensionDecl is anonymous](../learnings/1781267563910-getnamefornamehint-feeds-c-like-emitter-identifier.md)
- [SplitBuffer ByteBuffer Vulkan GPU-hang flake (Falcor)](../learnings/1781338076804-splitbuffer-bytebuffer-vulkan-gpu-hang-flake-falco.md)
- [SPIR-V DebugScope must be variable-arity (null IR operand crashes buildEntryPointReferenceGraph)](../learnings/1781568128097-spir-v-debugscope-must-be-variable-arity-null-ir-o.md)
- [CORRECTION to #11631 version root cause: require atom stamped only on layout IR module](../learnings/1781643037138-correction-to-11631-version-root-cause-require-ato.md)
- [This environment HAS an NVIDIA GPU despite CLAUDE.md saying "no GPU"](../learnings/1781698311286-this-environment-has-an-nvidia-gpu-nvidia-smi-work.md)
- [Correction: installing libvulkan1 does NOT enable NVIDIA-GPU Vulkan](../learnings/1781699613539-correction-installing-libvulkan1-does-not-enable-n.md)
- [Stale prebuilt slangc + inlining mask SPIR-V static-local bugs](../learnings/1781708255374-stale-prebuilt-slangc-inlining-mask-spir-v-static-.md)
- [SPIR-V function-static state loss = SPIRV-Tools private-to-local](../learnings/1781713084820-spir-v-function-static-state-loss-spirv-tools-priv.md)
- [slangc -target spirv-asm at default opt DOES run spirv-opt (private-to-local)](../learnings/1781714495665-slangc-target-spirv-asm-at-default-opt-does-run-sp.md)
- [Never mutate a hoistable SPIRVAsmOperand in place](../learnings/1781725591470-never-mutate-a-hoistable-spirvasmoperand-in-place-.md)
- [SPIR-V asm-operand legalization: hoistable operands + foldable≠constant pitfalls](../learnings/1781726667133-spir-v-asm-operand-legalization-hoistable-operands.md)
- [slang downstream-compiler load: optional spirv-opt is fatal via real sink](../learnings/1781799089141-slang-downstream-compiler-load-optional-spirv-opt-.md)
- [Slang downstream-compiler absence (spirv-opt/slang-glslang) IS unit-testable](../learnings/1781804734767-slang-downstream-compiler-absence-spirv-opt-slang-.md)
- [CI runs slang-test with SLANG_RUN_SPIRV_VALIDATION enabled](../learnings/1781825083619-ci-runs-slang-test-with-slang-run-spirv-validation.md)
- [Slang AS: forcing ray-query-only capability (avoid SPV_KHR_ray_tracing)](../learnings/1781905197543-slang-as-forcing-ray-query-only-capability-avoid-s.md)
- [Conditionally requiring a SPIR-V capability for a stdlib spirv_asm intrinsic](../learnings/1781974573249-conditionally-requiring-a-spir-v-capability-for-a-.md)
- [slang -g0 doesn't zero SPIR-V debug info: OpSource/OpName bypass the IRDebug gating path](../learnings/1782145409789-slang-g0-doesn-t-zero-spir-v-debug-info-opsource-o.md)
- [slang-glslang is the SPIR-V backend — don't mask its load as optional](../learnings/1782152994624-slang-glslang-is-the-spir-v-backend-don-t-mask-its.md)
- [Slang SV_Depth{Greater,Less}Equal: direct SPIR-V correct, GLSL/via-GLSL drops directional mode](../learnings/1782165817624-slang-sv-depth-greater-less-equal-direct-spir-v-co.md)
- [Slang direct-SPIR-V depth mode ALSO dropped via conflict-branch (dual depth-affecting vars)](../learnings/1782211031715-slang-direct-spir-v-depth-mode-also-dropped-via-co.md)
- [groupshared by-reference param regresses Khronos SPIR-V](../learnings/1782237919713-groupshared-by-reference-param-regresses-khronos-s.md)
- [SPV_KHR_abort transitively requires SPV_KHR_constant_data](../learnings/1782248897717-spv-khr-abort-transitively-requires-spv-khr-consta.md)
- [spvDescriptorHeapEXT unified-stride: per-type arrays + symbolic-max construct](../learnings/1782264945972-spvdescriptorheapext-unified-stride-per-type-array.md)
- [Triage discriminator: Slang embedded spirv-tools vs system Vulkan validation layer](../learnings/1782267495019-triage-discriminator-slang-embedded-spirv-tools-vs.md)
- [spirv-val accepts OpSpecConstantOp(max) over opaque OpConstantSizeOfEXT as an ArrayStrideIdEXT id](../learnings/1782271381546-spirv-val-accepts-opspecconstantop-max-over-opaque.md)
- [SPIR-V atomic emit has TWO address-space gates — a per-address-space fix must touch both](../learnings/1782322436550-spir-v-atomic-emit-has-two-address-space-gates-a-p.md)
- [Slang SPIR-V emit: new Spv* enum constants resolve from the SPIRV-Headers package](../learnings/1782322517394-slang-spir-v-emit-new-spv-enum-constants-resolve-f.md)
- [Slang SPIR-V atomic emission has 4 cross-layer gates keyed on address space](../learnings/1782346122322-slang-spir-v-atomic-emission-has-4-cross-layer-gat.md)
- [fp8 scalar float constants abort in spirv-tools constant folding (width-8 gap)](../learnings/1782449605671-fp8-scalar-float-constants-abort-in-spirv-tools-co.md)
- [Conditional<T,b> ICE: non-literal flag survives lowerConditionalType to spirv-emit](../learnings/1782485307814-conditional-lt-t-b-gt-ice-non-literal-flag-survive.md)
- [slang-test wgpu COMPARE_COMPUTE VUID errors come from Dawn's internal tint, not Slang's SPIR-V emitter](../learnings/1782499611532-slang-test-wgpu-compare-compute-vuid-errors-come-f.md)
- [Runtime slang-test for a new Vulkan extension is gated on slang-rhi harness support](../learnings/1782515089370-runtime-slang-test-for-a-new-vulkan-extension-is-g.md)
- [slang local spirv-asm verify: put build/Debug/lib FIRST in LD_LIBRARY_PATH](../learnings/1782733787106-slang-local-spirv-asm-verify-put-build-debug-lib-f.md)
- [SPIR-V issues require spirv-val, not just slangc exit code](../learnings/dashboard_slang-triage-1776263007885.md)
- [[require] capability atom does NOT auto-emit OpCapability for spirv_asm builtin-loads — declare inline](../learnings/1782828754941-require-capability-atom-does-not-auto-emit-opcapab.md)
- [SPIR-V capability/validation fixes need a -target spirv test directive, not just spirv-asm](../learnings/1782833127380-spir-v-capability-validation-fixes-need-a-target-s.md)
- [Signature-derived SPIR-V VariablePointers must gate on [noinline], not hasUses() (#9061)](../learnings/1781023718622-signature-derived-spir-v-variablepointers-must-gat.md)
- [[require] capabilities never reach codegen cap set; version path partially compensates, bindless doesn't (#11631)](../learnings/1781638061693-slang-require-capabilities-never-reach-codegen-cap.md)
- [#11631 severity: [require]-drop is a silent runtime-divergence (SS-class), hits codegen-DECISION only](../learnings/1781686744418-slang-11631-severity-require-drop-is-a-silent-runt.md)
- [[require] atom does NOT auto-emit OpCapability for spirv_asm builtin-loads — declare inline](../learnings/1782828754941-require-capability-atom-does-not-auto-emit-opcapab.md)
- [HitObject SM6.9+NVAPI invalid mix — CapabilitySet.implies() inert (LATER REFUTED)](../learnings/1782986638349-slang-hitobject-sm6-9-nvapi-invalid-mix-capability.md)
- [Verify a triage's predicate premise at HEAD (revert-drill) before fixing — #11889 premise was false](../learnings/1782991917563-verify-a-triage-s-predicate-premise-at-head-revert.md)
- [CORRECTION: HitObject SM6.9+NVAPI real bug is 2-arg Invoke ABI gap; stale-binary meta-lesson](../learnings/1782992753685-correction-slang-hitobject-sm6-9-nvapi-refutes-my-.md)
- [Direct SPIR-V emitter never emits NoContraction — fp-mode/precise are no-ops (#11933)](../learnings/1783064216848-slang-direct-spir-v-emitter-never-emits-nocontract.md)
- [Direct SPIR-V: emit NoContraction for -fp-mode precise at emitArithmetic choke point (PR #11935)](../learnings/1783067747847-slang-direct-spir-v-emit-nocontraction-for-fp-mode.md)
- [spirv-opt crash suppressed via expected-failure list is often droppable via the -O0 default (PR #11805)](../learnings/1783036168133-spirv-opt-crash-suppressed-via-expected-failure-li.md)
- [Descriptor-heap ConstantBuffer miscompile (#11483) = wrong storage class (Uniform not StorageBuffer), NOT missing ArrayStride](../learnings/1783384030612-descriptor-heap-constantbuffer-miscompile-11483-wr.md)
- [Descriptor-heap ConstantBuffer<T> SPIR-V crash cluster — 3 fixes landed, verify at ToT](../learnings/1783459628122-descriptor-heap-constantbuffer-t-spir-v-crash-clus.md)
- [slang SPIR-V DebugFunction scope uses one module-global slot pinned to entry-point CU (#11983)](../learnings/1783465931766-slang-spir-v-debugfunction-scope-uses-one-module-g.md)
- [slang#11982: duplicate SPIR-V DebugSource from divergent path spelling defeating hoistable-dedup](../learnings/1783466061131-slang-11982-duplicate-spir-v-debugsource-from-dive.md)
- [slang #11984: -g2 DebugSource for #line files bypasses canonical BOM strip (fresh-read fallback)](../learnings/1783468265984-slang-11984-g2-debugsource-for-line-files-bypasses.md)
- [slang 64-bit buffer indexing works on direct subscript but IArray/IRWArray interface truncates to 32-bit (#11967)](../learnings/1783415716330-slang-64-bit-buffer-indexing-works-on-direct-subsc.md)
- [slang#9062 SPIR-V OpCompositeConstruct of logical-pointer array escapes resource legalization (struct-field blindness)](../learnings/1783524098198-slang-9062-spir-v-opcompositeconstruct-of-logical-.md)
- [slang#12002: spirv_asm named registers leak into OpName; texture-sample intrinsic uses %sampled](../learnings/1783525064110-slang-12002-spirv-asm-named-registers-leak-into-op.md)
- [slang#12004 sampler-vs-texture noinline SPIR-V param asymmetry — isIllegalSPIRVParameterType isArray gate](../learnings/1783527024741-slang-12004-sampler-vs-texture-noinline-spir-v-par.md)
- [slang#8681 spirv-opt strip-debug deletes DebugBuildIdentifier — separate-debug-info needs a DBI shim](../learnings/1783556986544-slang-8681-spirv-opt-strip-debug-deletes-debugbuil.md)
- [slang#12019 double→bool SPIR-V: getIntValue(floatType,0) mints int-lit with float type → 1-word OpConstant](../learnings/1783587817471-slang-12019-double-bool-spir-v-getintvalue-floatty.md)
- [slang#12002 SHIPPED: refute-then-fix triage of a 'misattributed OpName' coincidental stdlib name collision](../learnings/1783746988745-slang-12002-shipped-refute-then-fix-triage-of-a-mi.md)
- [slang#12051 SPIR-V descriptor reload is an emit-time ARTIFACT not a spec requirement — proven by OpCopyObject pin](../learnings/1783759451138-slang-12051-spir-v-descriptor-reload-is-an-emit-ti.md)
- [SPIR-V builtin-var Flat gate: prove else-branch reachability via getBuiltinGlobalVar's ptr-assert, not IR-type intuition](../learnings/1783767618309-approver-challenger-spir-v-builtin-var-flat-gate-p.md)
- [SPIR-V SER capability over-requires 1.5; capdef floor edit alone is insufficient (#12097)](../learnings/1784052492569-spir-v-ser-capability-over-requires-1-5-capdef-flo.md)
- [Why -capability silently raises SPIR-V version past explicit -profile (#12099)](../learnings/1784052596356-why-capability-silently-raises-spir-v-version-past.md)
- [#12104 vec3 OpConstantComposite over-count — repro-blocked, post-legalize fold suspect](../learnings/1784069141044-slang-spir-v-vec3-opconstantcomposite-over-count-r.md)
- [#12104 vec3 OpConstantComposite 4-constituent bug is in downstream spirv-opt, NOT Slang (x/x fold)](../learnings/1784072018251-slang-12104-vec3-opconstantcomposite-4-constituent.md)
- [CORRECTION #12051: SPIR-V descriptor reload is an EMIT-time OpLoad-per-use — verify emit, not just IR](../learnings/1784075115135-correction-slang-12051-spir-v-descriptor-reload-is.md)
- [#12051 DescriptorHandle divergent NonUniform marker stripped before SPIR-V legalize](../learnings/1784081063099-slang-12051-descriptorhandle-divergent-nonuniform-.md)
- [#12051 option-a: the invariance guard was over-specified — NonUniform marker stripped before SPIR-V legalize](../learnings/1784081353064-slang-12051-option-a-the-invariance-guard-was-over.md)
- [#12051 CORRECTION — NonUniform marker: inst survives, round-trip drops it from heap index](../learnings/1784083614756-slang-12051-correction-descriptorhandle-nonuniform.md)
- [REFINEMENT #12051: NonUniform marker exists early then dropped by Cast round-trip; -dump-ir starts too late](../learnings/1784083913871-refinement-slang-12051-the-nonuniform-marker-exist.md)
- [CORRECTION: SPIR-V SER at 1.4 without PSB — bundled spirv-val ACCEPTS it (triage claim overturned)](../learnings/1784102979347-correction-spir-v-ser-at-1-4-without-psb-bundled-s.md)
- [Slang capdef atoms never auto-emit an OpExtension — emission is always explicit C++](../learnings/1784120354355-slang-capdef-atoms-never-auto-emit-an-opextension-.md)
- [[approver/confirmed] SPIR-V UniformConstant load-coalescing is a decoration-neutral safe shape](../learnings/1784126649253-approver-confirmed-spir-v-uniformconstant-load-coa.md)
- [CORRECTION slang#12051: 'orthogonal to #12027 workaround' was verified on the WRONG shape — [noinline] changes the answer; #12111 DOES coalesce caller-side resource loads across call boundaries](../learnings/1784127324690-correction-slang-12051-orthogonal-to-12027-workaro.md)
- [slang#12051/#12111 — DescriptorHandle load-coalescing interacts with [noinline] + #12027 image-as-index workaround (benign)](../learnings/1784128384354-slang-12051-12111-descriptorhandle-load-coalescing.md)
- [slang bindless [noinline]: image passes as uint INDEX (workaround), sampler as real descriptor — resolve SPIR-V operand types from OpTypeFunction, never label by source position](../learnings/1784128407283-slang-bindless-noinline-image-passes-as-uint-index.md)
- [[approver/confirmed-safe] SER SPIR-V 1.4 fix (12115) — capdef floor + PSB OpExtension both present = the correct two-part fix; recall precedent held](../learnings/1784139502358-approver-confirmed-safe-ser-spir-v-1-4-fix-12115-c.md)
- [[approver/clause-gap] New capdef ATOM (not just floor edit) trips BOTH check-cmdline-ref AND check-capability-atoms-ref — wait for them green](../learnings/1784139517684-approver-clause-gap-new-capdef-atom-not-just-floor.md)
- [CORRECTION: emitted SPIR-V version comes from getTargetCaps, NOT from used functions' [require] caps](../learnings/1784153531683-correction-emitted-spir-v-version-comes-from-getta.md)
- [Entry-point [require] caps do NOT drive the emitted SPIR-V version header](../learnings/1784157894453-entry-point-require-caps-do-not-drive-the-emitted-.md)
- [A GLSL-family -profile does not PIN a SPIR-V output version (only implies a floor)](../learnings/1784157903315-a-glsl-family-profile-does-not-pin-a-spir-v-output.md)
- [Conditional SPIR-V capability by operand constness → new backend IR op, NOT spirv_asm-block iteration (jkwak, #9382)](../learnings/1784164953340-conditional-spir-v-capability-by-operand-constness.md)
- [slang#12108 triage: spirv_asm __-prefix cleanup — scope spans hlsl AND glsl meta.slang](../learnings/1784248363961-slang-12108-triage-spirv-asm-prefix-cleanup-scope-.md)
_Catalog: [[wiki/index.md]]_
