---
title: "Supervisor Operations, Sweep Suppression, and scan.py Health"
type: concept
group: agent-infra
tags: [supervisor, sweep, nudge, scan, watchdog, ci-health, dashboard, artifact-check, staleness, dropped-task]
source_count: 34
---

# Supervisor Operations, Sweep Suppression, and scan.py Health

> **Split 2026-08-12** out of the over-cap *Multi-Session Coordination* page. Sibling: [Multi-Session Coordination, A2A Routing, and Session Handoff](agent-infra-multi-session-coordination.md). This page owns the periodic-supervisor / sweep-coworker / `scan.py` machinery: nudge discipline, artifact checks, staleness triggers, watchdog no-op controls, and dashboard/CI-health reporting. The coordination sibling owns A2A edges, dedup, empty-ack loops, and session handoff.

## TL;DR
- **A supervisor/cron/tooling nudge states CLAIMS, not facts.** Verify its premises against live GitHub state before acting — and always before an outward-facing or irreversible write. ~89% of artifact-less flags measured false; a false flag pressures a coworker into spamming a maintainer's issue with an un-retractable comment.
- **Putting "→ NUDGE" on a board is not a nudge** — actually dispatch. Coworker-nudge ONLY when a chain is stuck *inside the pipeline*: assigned session silent ≥~60min AND ZERO GitHub artifact. If ANY artifact exists, do not coworker-nudge.
- **"No artifact" ≠ "no artifact":** absent/None in a monitoring payload usually means *not populated*, not *does not exist*. **Recency ≠ presence:** a chain silent 17 days can be fully documented — ask "is the public record adequate?", not "did we speak recently?".
- **A watchdog's no-op branch is its dangerous branch** — "nothing to do" is indistinguishable from a blind instrument. Bake a known-positive control (create-then-delete a row) into the check; `--help` output and a well-formed error are NOT capability probes.
- **Staleness triggers are ETA-relative, not uniform.** Builds: 48h (ninja resumes incrementally). Reviews: ~2× ETA (~1h). A review "complete" = a hash-matched verdict against *current* head, not merely "a verdict returned."
- **A dispatched task's silence is not evidence of health** — the completion signal is a terminal artifact, never the absence of bad news. Blown-ETA silence on a bot deliverable = dropped-task 🔶 (verify session alive), NOT a maintainer nudge.
- **Don't assume FIFO with two dispatches to one child** — a later task completing is positive evidence the child is alive, which makes the earlier one's silence *more* suspicious. A repeat status question about the same artifact is a starvation alarm.
- **A superseded bot PR closed-unmerged is often a WIN, not a dropped deliverable** — resolve against the issue's actual close reason (a human PR may have superseded it) before re-escalating.
- **scan.py over-flags revert on every skill re-sync** — the durable home for `parse_ts` tz-crash, narrow `bot_logins`, and the `HUMAN_OWNED` gate lift is an upstream PR, not a per-tick hand-patch.
- **A non-draft PR's authoritative CI run is the `pull_request` run on HEAD**, not a stale `workflow_dispatch` run from when it was a draft. Re-confirm latest-run + draft-status before acting on a stored CI cell.
- **Sweep coworkers stay silent on re-confirmation** — ping the parent only on a delta (new signature, state change, actionable item). Recurring diagnostic tasks re-derive from scratch each fire (`new_session:true`) — anchor on a canonical report file.
- **Dashboard channels render markdown** — every ref is a clickable `[label](url)`; inline the body AND attach the `.md`; Unicode emoji, not shortcodes; URL-encode search queries.

## Sweep Coworkers: Suppress Re-Confirmation Reports

For periodic-sweep / babysitter coworkers: do NOT send the parent a report when a sweep merely re-confirms already-known/already-escalated state. Stay silent and continue local work (update trackers, append durable logs) when the sweep only re-surfaces known reds ([Sweep coworkers: suppress re-confirmation reports, alert only on deltas](../learnings/1781352352298-sweep-coworkers-suppress-re-confirmation-reports-a.md)).

Ping the parent only on a delta: a NEW signature, a state change (fix merges, flake recurs/escalates, cap hit, merge-queue eviction), or anything actionable on the parent's side. This extends the "all-green → go internal" rule to also cover re-confirmation of KNOWN reds.

## Supervisor Operations (nudge, deliver, concurrency)

Putting "→ NUDGE" in the supervision board is NOT a nudge — the supervisor must actually dispatch a message. Coworker-nudge ONLY when a chain is stuck inside the pipeline: the assigned coworker session went silent ≥~60min AND the chain has produced ZERO GitHub artifact ([CONSOLIDATED: /supervise-issues operations (nudge, deliver, format, concurrency)](../learnings/1780558152383-CONSOLIDATED-supervisor-operations.md)).

If ANY GitHub artifact exists → do NOT coworker-nudge. For a PR open but review stale: post a ONE-TIME GitHub ping to the assignee; if no assignee, post a "standing down for maintainer to drive" comment.

Nudge mechanics: find the stuck session with `ncl sessions list --agent-group <coworker-ag> --limit 2000 | grep 'gh-issue-...'`; dispatch into the EXISTING session using `send_message` with `target_session_id`. Track `nudgedAt`; max 2 nudges then escalate.

A scheduled/cron session has no default reply target — bare `send_message` fails "multiple destinations." Always specify `to="orchestrator"`. Concurrent manual `/supervise-issues` fan-outs create multiple orchestrator sessions that each rewrite `supervisor-state.json`; live `gh` is the single source of truth, not state files.

## Scheduled Tasks: Anchor on Canonical Reports

A recurring scheduled task that diagnoses a persistent broken state (default `new_session: true`) re-derives the diagnosis from scratch on every fire with no memory of prior fires, producing divergent and sometimes wrong conclusions ([Scheduled diagnostic tasks re-diagnose persistent state inconsistently across fresh sessions](../learnings/1780350138352-scheduled-diagnostic-tasks-re-diagnose-persistent-.md)).

Remediation: (1) have the task write findings to a canonical report file and read-anchor on it each fire; (2) when underlying state is known-broken and blocked, pause the task (`pause_task`) rather than letting it fire daily; (3) as supervisor, treat a coworker's same-symptom re-diagnosis as their current finding, not fact — especially when it contradicts a verified report.

## Dashboard Formatting

The `slang-maintainer-dashboard` channel renders markdown. Every issue, PR, discussion, search query, and external reference must be a clickable `[label](url)` markdown link — never a bare `#N` or plain search query ([Dashboard channels render markdown — always include hyperlinks for issue/PR/discussion refs](../learnings/1778835191236-dashboard-channels-render-markdown-always-include-.md)). Inline the full markdown body AND attach the `.md` file via `send_file`. Use Unicode emoji (🚨 ⚠️ ✅), not Slack/GitHub shortcodes. URL-encode search queries (`>` as `%3E`, `=` as `%3D`, `:` as `%3A`, spaces as `+`).

## CI Health Monitoring

A single common-suite flake can functionally stall the Slang merge queue without hard-jamming it — batches still run and individual PR checks look fine; the symptom is only visible as "master HEAD hasn't advanced in N hours." Always check `gh api repos/<o>/<r>/commits/master --jq .commit.committer.date` to detect this ([A single common-suite flake can functionally stall the Slang merge queue](../learnings/1782226186227-a-single-common-suite-flake-can-functionally-stall.md)).

For timeout/hang signatures: a true deterministic hang kills every run. If most PRs are green and only a few get hit, the cause is intermittent — the correct response is normal rerun + requeue, NOT a maintainer ask. Escalate a dominant-evictor signature to the maintainer only after it persists across TWO consecutive sweeps once confounding noise is gone ([CI: flake-class vs deterministic-hang, and the two-sweep escalation threshold](../learnings/1782346148219-ci-flake-class-vs-deterministic-hang-and-the-two-s.md)).

## supervise-issues pull-universe.sh argv overflow at ~170+ chains

A `/supervise-issues` tick stalls and the final assembly dies with `python3: Argument list too long` at `scripts/pull-universe.sh` (~line 242) once the chain count reaches ~170+. The per-chain fetch completes fine; the overflow is passing all chains as argv to the assembly step. Fix by piping via stdin/a temp file instead of argv when the universe is large ([supervise-issues pull-universe.sh argv-overflow at ~170+ chains](../learnings/1782867269290-supervise-issues-pull-universe-sh-argv-overflow-at.md)).

## Supervisor Artifact-Check for No-PR Chains

Before flagging a no-PR chain as "no GitHub artifact / owed PR", check for **issue comments** by the bot (triage 5-bullets), not just a PR or a `pr_session_mappings` row — `scan.py`'s `github_artifact` is null whenever there's no PR, but a stood-down/upstream-blocked chain's artifact is its triage comment on the issue ([supervisor artifact-check misses issue-comment artifacts for no-PR chains](../learnings/1783950814878-supervisor-artifact-check-misses-issue-comment-art.md)).

**Blast radius measured (2026-08-04): ~89% of artifact-less flags were false, and the nudge's suggested remedy was strictly worse than silence.** A supervisor nudge on slang#10027 said *"this chain has no GitHub artifact of ours either… post the public 5-bullet so a human landing on the issue can see where it stands."* The recipient checked before posting: the issue already had **six** bot comments, including the exact code-path trace the maintainer had asked for — the correct action was to **refuse the write and correct the premise**. Root cause confirmed the 07-14 finding and sized it: `scan.py` derived the artifact URL from `chain["pr"]` and otherwise required a caller-supplied `github_artifact_url` the feeder never populated, so **every no-PR chain reported `github_artifact: None` regardless of how much public trail it had**. **105 chains flagged artifact-less, 93 of them false negatives (~89% of the rows carrying the board's most-repeated warning)**; now fixed (105→12) with presence-not-recency as the discriminator and 202 artifact URLs persisted. The governing rule: **a supervisor/cron/tooling nudge states claims, not facts** — verify its premises against live state before acting, and especially before an outward-facing write; the check is one call, `gh api repos/<o>/<r>/issues/<n>/comments --paginate --jq '.[] | "\(.created_at)  \(.user.login)  #\(.id)  \(.body|split("\n")[0][0:100])"'`. This matters more than a wrong dashboard cell because a false flag **pressures a coworker into spamming a maintainer's issue**, and for the `nv-slang-bot` App token issue-comment PATCH/DELETE is **403** — a 7th duplicate comment would have been permanent. Generalizable discriminators: **"no artifact" ≠ "no artifact"**; **recency ≠ presence**; **a maintainer deferring their own assigned issue is ack-class input** with zero public output owed; and **irreversibility raises the verification bar** — when the requested action can't be undone with your token, the burden of proof sits on the request, not on your refusal ([supervisor artifact-less flags were ~89% false — verify a nudge's premises before complying with a public write](../learnings/1785761398778-supervisor-artifact-less-flags-were-89-false-verif.md)).

**A superseded bot PR closed-unmerged is often a WIN, not a dropped deliverable.** A bot-authored draft PR that ends up **closed without merging** looks identical, on a raw GitHub read, to a dropped/abandoned deliverable — and if you track it on a watch list as "fix in flight" the natural (wrong) conclusion is "our fix died — re-escalate." Concrete case (2026-08-03, shader-slang/slang): issue **#12070** (bwd_diff runtime induction-start crash, P1) had bot draft PR **#12072**; on 08-03 the issue **closed** — but via **PR #12299** by the human assignee (saipraveenb25), which superseded the bot draft. The bot PR closing unmerged was the *correct terminal state*, not a loss. Before re-escalating a closed-unmerged bot PR, read the linked issue's close reason and check for a superseding human PR ([a superseded bot PR closed-unmerged is often a win, not a dropped deliverable](../learnings/1785831626664-a-superseded-bot-pr-closed-unmerged-is-often-a-win.md)).

## Reconciling Near-Terminal Approver Teardown + scan.py Disposition Suppression

Two supervisor/orchestrator reconciliation lessons. **(1) An approver torn down one CI-leg from recording loses its verdict with no ledger row** — observed twice on slang#12111. The approver stages its full decision but gates `record_decision` on settled-head CI; a container restart / monitor "stopped" / crash at that boundary looks identical to "still waiting on CI" from the orchestrator's side, and the decision evaporates. On any teardown signal after an approver reports it is near the record step, do NOT assume it recorded: independently verify CI terminal state via `gh`, confirm the head hasn't moved, then explicitly ask "did you `record_decision` on `<sha>`? NO→record now, gate is clear." The approver's gate is correct; the fix is orchestrator reconciliation on resume ([approver teardown at near-terminal loses unrecorded verdict](../learnings/1784445113353-approver-teardown-at-near-terminal-loses-unrecorde.md)). **(2) scan.py disposition suppression must apply on ALL ball branches** — `classify` only consulted the human-owned disposition on the `ball=="human"` path, so `ball=="ours"`/`"none"` branches re-nudged every parked chain each tick. Fix: hoist a single `human_owns_disposition(chain)` guard to the top of `classify()` (returns `awaiting_human`, `needs_nudge=False`) before the ball dispatch, and widen the token set. Two upstream gaps remain that the agent must backfill each tick: pull-universe mis-tags `github-actions`/`coderabbitai` as `is_bot:false`, and emits naive space-separated timestamps that crash scan.py's aware `max()` ([scan.py disposition suppression must apply on all ball branches](../learnings/1784464263683-scan-py-disposition-suppression-must-apply-on-all-.md)).

## Blown-ETA Silence on a Dispatched Bot Deliverable = Dropped-Task, Not "Still Holding"

Sharply distinguish two watch-list dispositions that look identical on GitHub. **"Maintainer-gated":** a fix/verdict EXISTS and only a human ready-flip/merge/alignment-reply remains → genuine 🟡, a real maintainer nudge. **"Deliverable dropped":** no PR/verdict was ever produced by the owning bot session → a chain-liveness problem, NEVER a maintainer nudge. The trap is that a dropped chain (fixer/reviewer work that died on session teardown, losing the completion notification) looks *identical* to one calmly progressing — open issue, no PR/verdict, quiet. On 2026-07-20 the daily report carried #11877 (a fixer's mid-build PR) and #12116 (a reviewer's re-run) as "PR overdue → soft nudge to maintainer" and "draft awaiting ready-flip"; both were actually dropped-on-teardown with nothing ever produced, and had been dark 5 days across a compaction gap. Rule: any in-flight bot deliverable silent >48h (no PR, no verdict, `updated_at` frozen, no monitor ping) → flag 🔶 "verify not dropped — re-confirm the owning session is alive" and surface to parent so it can re-wake the owning session; only after the session is confirmed alive does it return to 🟡-held ([Watch-list: blown-ETA silence on a dispatched bot deliverable = dropped-task signal, not "still holding"](../learnings/1784535643816-watch-list-blown-eta-silence-on-a-dispatched-bot-d.md)).

Track dispatched-work ETAs; a blown ETA is a dropped-task signal, not a reason to keep holding ([Track dispatched-work ETAs; a blown ETA is a dropped-task signal, not a reason to keep holding](../learnings/1784154058484-track-dispatched-work-etas-a-blown-eta-is-a-droppe.md)). And **verify a claimed A2A handoff actually fired** — when a coworker says "I sent X to Y" and Y is waiting, verify the send actually left the sender's session before treating the chain as advanced; a stale critique gate, marker, or auth can silently block a send while the sender believes it went. Ask the sender to *confirm* the dispatch fired (not blind re-send, which double-dispatches) ([Verify a claimed A2A handoff actually fired — a stale critique gate can silently block the send](../learnings/1784882173077-verify-a-claimed-a2a-handoff-actually-fired-a-stal.md)).

## ETA-Relative Review Staleness + Recurring scan.py Over-Flag

**A review deliverable needs an ETA-relative staleness trigger, not the uniform 48h dropped-deliverable window.** A review (correctness/clarity verdict, ~20-30 min ETA) silent past ~2× its ETA (~1h) with no `running` owning reviewer session is *stranded*, not running → flag 🔶 immediately; 48h is right only for builds (ninja resumes incrementally) and owed-answers. Also 🔶-flag a review when the PR head moved after dispatch, or a returned verdict's reviewed-diff-hash ≠ current head — "review complete" means a hash-matched verdict against current head, not merely "a verdict returned." 🔶 on a review means the correctness gate has NOT completed → the next action is re-dispatch foreground/in-turn (completion = tool result, not a teardown-prone monitor), owned by parent/reviewer, not a maintainer nudge ([review deliverables need an ETA-relative staleness trigger, not the 48h dropped-deliverable window](../learnings/1784622307704-review-deliverables-need-an-eta-relative-staleness.md)). A fresh bot-authored draft fix PR with 0 comments looks *identical on GitHub* to a review calmly in progress and to one that died on teardown — so a daily/maintainer report must NOT default such a PR to 🟡; the disposition is driven by whether a hash-matched verdict actually landed against current head ([Daily-report: a fresh draft PR with 0 reviews is NOT proof the review is progressing — check for a landed verdict](../learnings/1784881773024-daily-report-a-fresh-draft-pr-with-0-reviews-is-no.md)).

**The `/supervise-issues` scan.py over-flag reverts on every skill re-sync.** Three recurring input/gate bugs owed as durable upstream fixes: (1) `scan.py::parse_ts` crashes on naive session-DB timestamps — force `tzinfo=utc` when `dt.tzinfo is None`; (2) `pull-universe.sh` only knows `nv-slang-bot` as a bot, so `github-actions`/`coderabbitai`/`CLAassistant`/`slangbot` reviews read as "human spoke last" — correct `is_bot` on the comment objects and OVERWRITE (not fill-when-absent) the disposition; (3) the HUMAN_OWNED gate must sit at the TOP of `classify()` (returning `awaiting_human`) so a maintainer's own comment on a `maintainer-driving`/`pr_open` chain doesn't false-flip — but deliberately do NOT add `awaiting_human`/`pr_open` tokens, so a fresh substantive @bot ask still overrides a stale self-label ([scan.py over-flag: 3 input/gate bugs revert on skill re-sync](../learnings/1784638375681-supervise-issues-scan-py-over-flag-3-input-gate-bu.md), [tick 97 — scan.py over-flag reverted again (skill re-sync); raw 15→0](../learnings/1784680848889-supervisor-tick-97-scan-py-over-flag-reverted-agai.md)).

## Supervisor CI Cell Goes Stale When a PR Flips Draft→Non-Draft

The supervisor's §2b CI read stores a per-chain CI cell, but the *authoritative* CI signal depends on the PR's draft status — and a stored cell from a prior tick can be stale if that status changed since. Fix PRs normally sit in **draft** and drive CI by `workflow_dispatch`, so §2b takes "latest non-skipped `workflow_dispatch` run for `fix/issue-<num>`" as the signal. But once a PR flips to **non-draft**, the `wait-for-human-priority` gate no longer yields — yet a stale `workflow_dispatch` run dispatched *while it was still a draft* stays the "latest workflow_dispatch" and can carry a `failure` conclusion from flaky jobs. On a non-draft PR the authoritative run is the **`pull_request`** run on the same HEAD. Incident (Tick 102, slang #12120 / PR #12195): a stored `❌` (workflow_dispatch, draft-era) fired a rebase nudge — but the `pull_request` run on the same HEAD was green, and the PR was `BLOCKED`/`REVIEW_REQUIRED` on human review, NOT CI. Apply: (1) when non-draft, prefer the latest non-skipped `pull_request` run's conclusion; (2) don't act on a stored CI cell without re-confirming the run is still latest AND the draft-status hasn't changed; (3) a rebase nudge on a `BLOCKED`/`REVIEW_REQUIRED` PR whose real CI is green is noise ([Supervisor CI cell goes stale when a PR flips draft→non-draft](../learnings/1784982853601-supervisor-ci-cell-goes-stale-when-pr-flips-draft-.md)).

## The Chronic 70+ Over-Flag Had Two Root Causes in scan.py, Both Revert on Skill Re-Sync

The supervisor's `supervise-issues/scripts/scan.py` over-flagged ~70 chains as `needs_nudge` for 5+ consecutive ticks, each tick re-diagnosing the same two bugs by hand because the fix reverts on every skill re-sync. **(1) `parse_ts` timezone crash (fatal EVERY tick):** `pull-universe.sh` emits `our_last_outbound` as naive SQLite `YYYY-MM-DD HH:MM:SS` (no `Z`) for ~59 chains; `datetime.fromisoformat` returns a naive datetime → `TypeError: can't compare offset-naive and offset-aware` in the `max()` inside `compute_last_activity_by_us`, producing ZERO output and a dead tick. Fix: in `parse_ts`, `if dt.tzinfo is None: dt = dt.replace(tzinfo=timezone.utc)`. **(2) `HUMAN_OWNED` disposition gate too deep:** the gate lived only inside `we_owe_next_step` (the bot-last `ball=='human'` path), so `ball=='ours'`/`ball=='none'` *parked* chains leaked into `needs_nudge`. Fix: lift the gate to the very top of `classify()`. Also widen `HUMAN_OWNED_DISPOSITION` with the descriptive tokens the state file actually uses (`maintainer-owned`, `design-gated`, `parked`, `superseded`, `held for`) but NOT bare `pr_open`/`awaiting_human` prefixes. Result: raw `must_nudge` 70 → 1 honestly, + 3 regression tests. **Both scan.py edits AND the still-owed `pull-universe.sh` fixes revert on skill re-sync — the durable home is an upstream PR to the supervise-issues skill** ([supervise scan.py: durable fix for chronic 70+ over-flag (parse_ts tz + HUMAN_OWNED gate lift)](../learnings/1785112718566-supervise-scan-py-durable-fix-for-chronic-70-over-.md)).

**A fourth input bug in the same feeder: bare numeric transient signatures (502/503/504) false-positive on GitHub comment IDs.** `scripts/pull-universe.sh::classify_error_text` lists bare `"502"`, `"503"`, `"504"` in `_TRANSIENT_SIGNATURES`, and the `is_errorish` guard is **or**-ed with the signature scan, so any outbound text containing those three digits *anywhere* (including inside a URL or comment id) is classified `transient`. Observed at tick 87: `gh-issue-shader-slang/slang-12219` was flagged `transient` on the match `504` inside comment id `5150492632` — while the outbound was a normal `[Loop closed]` report on a **terminal** chain. `last_outbound_error_class ∈ (transient|unknown)` is the **bounce limb** of `scan.py`, so a false positive manufactures a phantom nudge row on a finished chain; since GitHub comment ids are 10 digits, ~a third of all links contain one of these substrings by chance. Fix: require numeric signatures to co-occur with an errorish marker (`and`, not `or`), or match `\b(HTTP|status)\s*50[234]\b`. **A substring signature for an error class must be anchored to error syntax, never bare digits** — when a classifier's cheapest signature is also the most common accidental substring, it will mostly report accidents ([supervise-issues: bare numeric transient signatures (502/503/504) false-positive on GitHub comment IDs](../learnings/1785802540595-supervise-issues-bare-numeric-transient-signatures.md)).

## A Watchdog's No-Op Branch Is Its Dangerous Branch — Control the Empty Result

Two failure modes at the same seam: a supervisor that concludes "nothing to do" and exits silently, where "nothing to do" is indistinguishable from a broken instrument or a dropped handoff.

**An empty scheduler task list needs a create-probe control before you call it a true empty.** The scheduler-watchdog task says: call `list_tasks`, find any task whose next-fire `at=` is >10 min in the past, re-arm it. On 2026-08-04 the list came back `No tasks.` fleet-wide, across every filter — while **two scheduled tasks had just fired to create the very session doing the reading**. "Zero tasks exist" and "two tasks just fired" cannot both be true. Probes that did NOT discriminate — all returned `No tasks.`/not-found: `ncl tasks list`/`--all`/`--status pending`/`--json`; `--group <id>`; `ncl tasks get --id <derived>`. The discriminating probe is a **positive control that plants a row you know exists**:

```bash
ncl tasks create --name "watchdog-control-probe" \
  --prompt "Control probe — delete me." --process-after "2027-01-01T00:00:00Z"
ncl tasks list --all          # → shows the probe, "in 150d", pending
ncl tasks delete --id watchdog-control-probe-00f1
```

The list saw it instantly ⇒ the instrument works ⇒ the empty list is a TRUE empty. Generalized: for any watchdog whose trigger is "the list is empty / the count is zero / nothing is overdue", **the no-op branch is the dangerous branch** — it is indistinguishable from a broken query and exits silently, so a blind instrument issues a clean bill of health forever. Bake a create-then-delete (or any known-positive) control into the check itself. **`--help` output and a well-formed error are not capability probes; only a round-trip on data you planted is** ([an empty scheduler task list needs a create-probe control before you call it a true empty](../learnings/1785824186159-an-empty-scheduler-task-list-needs-a-create-probe-.md)).

**When two dispatches are in flight to one child, explicitly check for starvation — the later one can overtake the earlier.** On the slang#11917 pass-gating epic two batches went to the same child two days apart: **batch-2** (07-28) → no branch, no PR, 7 days later; **batch-3** (07-30 01:12) → PR #12281 opened ~2h later, approved 08-03. The issue author asked about batch-2 **twice** before anyone noticed. Every tier behaved "correctly": the child reported "fixer building draft PR" (true at the time), that was relayed upward without verification, and the "hold without polling" discipline took over — right for suppressing noise, but **it has no deadline**, so a dropped handoff and healthy in-progress work emit the same signal: nothing. Rules: do **not** assume FIFO — ask which dispatch produced an artifact, because **a later task completing is positive evidence the child is alive, which makes the earlier one's silence more suspicious, not less**; **a repeat question about the same artifact is a starvation alarm**; **pair every silent-hold with a staleness bound**; and **verify a child's in-progress claim before relaying it upward**. Ground-truth calls:

```bash
# Does the branch exist at all? (REST — works when `gh pr list` GraphQL 401s)
gh api "repos/O/R/git/matching-refs/heads/fix/issue-<N>" --jq '.[] | "\(.ref)  \(.object.sha)"'
# Any PR in ANY state referencing the issue?  → MCP github_search_issues q="repo:O/R is:pr <N> in:title"
# Is a found branch real work or a stale leftover?
gh api "repos/O/R/compare/master...<sha>" --jq '"status=\(.status) ahead=\(.ahead_by) behind=\(.behind_by)"'
```

**Matching the name prefix is not enough** — `fix/issue-11917` looked like a hit but was the merged #11920 slice from a month earlier, `diverged` and 208 commits behind master ([two dispatches to one child: check for starvation, because silence reads identically to progress](../learnings/1785824876739-two-dispatches-to-one-child-check-for-starvation-b.md)).

## Monitoring a repair workflow: alert on the blocker's identity, not on red (2026-08-13 fold)

Monitoring the hourly `ci-retry-yielded-bot` repair job exposed a family of monitoring traps. **A watchdog that correctly declines reports `success`** — the workflow's job is to *not* rerun bot CI while human CI is active, so every fire logs `success` with "CI is still active … not rerunning"; a monitor keyed on red conclusion sees a healthy-looking histogram over a genuinely stuck condition, so alert on the *declines*, not on failures ([A watchdog that correctly declines reports success — alert on declines, not on red](../learnings/1786296254567-a-watchdog-that-correctly-declines-reports-success.md)). But **a raw decline streak cannot separate two diseases**: an early "streak grew 16→44, worsening" claim was three defects at once — the two endpoints counted different predicates (one trigger arm silently dropped), both were page-bounded floors (the true streak was 277, found only by paginating), and — the finding no count would have caught — the conclusion field was *constant while the mechanism changed underneath it*: 40h-earlier declines were ordinary CI contention (`still active (8 run)`), today's were wedged on an approval gate holding zero runners (`(2 run)`). Same field, same verb, different disease; a growth claim needs both endpoints on one predicate, and the alarm must key on the blocker's *identity*, not the streak length ([A constant conclusion field can hide a changed mechanism: 277 "declines" were two different diseases](../learnings/1786296545684-a-constant-conclusion-field-can-hide-a-changed-mec.md), [A streak count cannot separate two diseases — alarm on the blocker's identity](../learnings/1786296763575-a-streak-count-cannot-separate-two-diseases-alarm-.md)). And when you find the real defect, fix the *reachable* gate: an earlier note blamed the retry script's `--lookback-hours 16` hard-drop (66 runs stranded 53 days) — but that code is **unreachable behind an earlier `ACTIVE_STATUSES`/`any_active_ci` gate** that returns first, so the lookback fix would have changed nothing; trace which gate returns first before proposing a fix ([CORRECTION to the yielded-bot lookback finding: the lookback is UNREACHABLE behind an earlier gate — fix ACTIVE_STATUSES first](../learnings/1786300033438-correction-to-the-yielded-bot-lookback-finding-the.md)).

## Building guards for an append-only ledger: the guard, its population, and its residue (2026-08-13 fold)

Instrumenting the CI-babysitter's append-only rerun ledger produced a set of guard-construction rules. **A recorded policy limit that only binds when recalled is as reliable as an unenforced schema — none:** a "don't recommend discarding a unit whose value this data can't see" rule lived in a README no script reads, and the next sweep reproduced the shape one layer up (auto-close of idle PRs); move policy onto a consumer the actor cannot avoid, and name the *shape* it guards ([A scope boundary stored as prose is unenforced — put policy on the same code path as mechanism](../learnings/1786314434522-a-scope-boundary-stored-as-prose-is-unenforced-put.md)). **Test a prescribed guard against the population it must catch before adopting it:** `audit_summary_rows`, named as "the only mechanism that can see a bypassed row," filters to `result=="sweep_summary"` (0.7% of rows) while the bypassed rows carried `result:"left"` — a detector on the wrong population returns a clean report, worse than none because it retires the question ([Test a prescribed guard against the population it must catch — "run it unconditionally" is worthless if it can't see the defect](../learnings/1786314802358-test-a-prescribed-guard-against-the-population-it-.md)). **A live-fire control belongs on a temp copy:** proving a guard fires means writing input that trips it, and in an append-only artifact that input can never be withdrawn — planted rows became permanent findings, and *future-dated* ones scheduled a wedge with none of the reasoning in context; point the path constant at a tempfile for the control ([A live-fire control belongs on a temp copy — proving a guard fires leaves permanent residue in an append-only artifact](../learnings/1786315108736-a-live-fire-control-belongs-on-a-temp-copy-proving.md)). **A guard defined against "now" must read the clock once per run and report the resolved value** — a per-row clock read makes one file return two answers, and a future-dated row pushes a *currency* metric forward (stale looks current), failing toward reassurance; report `newest_real_row` excluding future stamps ([A guard defined against "now" must read the clock once per run and report it — per-row reads make the same file return two answers](../learnings/1786316206459-a-guard-defined-against-now-must-read-the-clock-on.md)). Two schema traps: **a `pop()` of the wrong key prints a benign `False`, not an error** — a multi-key schema plus a single-key deletion silently leaves a stale mark (delete via the key tuple) ([A pop() of the wrong key prints a benign False, not an error — verify which mark key an entry actually carries](../learnings/1786306718517-a-pop-of-the-wrong-key-prints-a-benign-false-not-a.md)); and **a ledger can enforce `labels[]` and still have zero labelled rows** — `[]` passes a "field present" check while carrying no information, so all 40 reruns ranked over an empty vocabulary and the only signature route was free-text (which once turned "52 rows/11 PRs" into a true count of 3); require *non-empty* where the field feeds a ranking ([A rerun ledger can enforce labels[] and still have zero labelled reruns — check the population your ranking describes](../learnings/1786292075724-a-rerun-ledger-can-enforce-labels-and-still-have-z.md)).

**Source learnings (34):**
- [a watchdog that correctly declines reports `success` — alert on declines, not on red](../learnings/1786296254567-a-watchdog-that-correctly-declines-reports-success.md)
- [a constant conclusion field can hide a changed mechanism — 277 declines were two different diseases; endpoints need one predicate](../learnings/1786296545684-a-constant-conclusion-field-can-hide-a-changed-mec.md)
- [a streak count cannot separate two diseases — alarm on the blocker's identity](../learnings/1786296763575-a-streak-count-cannot-separate-two-diseases-alarm-.md)
- [CORRECTION: the yielded-bot lookback is UNREACHABLE behind an earlier ACTIVE_STATUSES gate — fix the gate that returns first (supersedes the hard-drop fix-order note)](../learnings/1786300033438-correction-to-the-yielded-bot-lookback-finding-the.md)
- [a scope boundary stored as prose is unenforced — put policy on the same code path as the mechanism, name the shape](../learnings/1786314434522-a-scope-boundary-stored-as-prose-is-unenforced-put.md)
- [test a prescribed guard against the population it must catch — a detector on the wrong population returns a clean, question-retiring report](../learnings/1786314802358-test-a-prescribed-guard-against-the-population-it-.md)
- [a live-fire control belongs on a temp copy — proving a guard fires leaves permanent residue in an append-only artifact](../learnings/1786315108736-a-live-fire-control-belongs-on-a-temp-copy-proving.md)
- [a guard defined against 'now' must read the clock once per run and report it; future-dated rows fail a currency metric toward reassurance](../learnings/1786316206459-a-guard-defined-against-now-must-read-the-clock-on.md)
- [a pop() of the wrong key prints a benign False, not an error — delete via the key tuple](../learnings/1786306718517-a-pop-of-the-wrong-key-prints-a-benign-false-not-a.md)
- [a rerun ledger can enforce labels[] and still have zero labelled rows — []  passes 'present'; require non-empty where it feeds a ranking](../learnings/1786292075724-a-rerun-ledger-can-enforce-labels-and-still-have-z.md)

- [an empty scheduler task list needs a create-probe control — `--help` and a well-formed error are not capability probes; a watchdog's no-op branch is its dangerous branch](../learnings/1785824186159-an-empty-scheduler-task-list-needs-a-create-probe-.md)
- [two dispatches to one child: check for starvation (batch-3 shipped in 2h while batch-2 produced nothing in 7 days) — a repeat status question is the alarm; silent-hold needs a staleness bound](../learnings/1785824876739-two-dispatches-to-one-child-check-for-starvation-b.md)
- [supervisor artifact-less flags were ~89% false (105 flagged / 93 false) — a cron nudge states claims, not facts; verify before an irreversible public write](../learnings/1785761398778-supervisor-artifact-less-flags-were-89-false-verif.md)
- [a superseded bot PR closed-unmerged is often a win, not a dropped deliverable — resolve against the issue's close reason first](../learnings/1785831626664-a-superseded-bot-pr-closed-unmerged-is-often-a-win.md)
- [supervise-issues: bare numeric transient signatures (502/503/504) match GitHub comment ids and forge `transient` on terminal chains via the bounce limb](../learnings/1785802540595-supervise-issues-bare-numeric-transient-signatures.md)
- [supervise scan.py chronic 70+ over-flag: two roots — parse_ts naive-tz crash (dead tick) + HUMAN_OWNED gate too deep; both revert on skill re-sync, needs upstream PR](../learnings/1785112718566-supervise-scan-py-durable-fix-for-chronic-70-over-.md)
- [supervisor §2b CI cell goes stale on draft→non-draft flip — a non-draft PR's authoritative run is the pull_request run on HEAD, not the stale workflow_dispatch one (#12120/#12195)](../learnings/1784982853601-supervisor-ci-cell-goes-stale-when-pr-flips-draft-.md)
- [daily-report: a fresh draft PR with 0 reviews is NOT proof review is progressing — check for a landed hash-matched verdict](../learnings/1784881773024-daily-report-a-fresh-draft-pr-with-0-reviews-is-no.md)
- [verify a claimed A2A handoff actually fired; a stale critique gate can silently block the send (confirm, don't re-send)](../learnings/1784882173077-verify-a-claimed-a2a-handoff-actually-fired-a-stal.md)
- [blown-ETA silence on a dispatched bot deliverable (>48h) = dropped-task 🔶, verify session alive; NOT a maintainer nudge](../learnings/1784535643816-watch-list-blown-eta-silence-on-a-dispatched-bot-d.md)
- [approver teardown one CI-leg from recording loses the verdict (no ledger row); orchestrator must reconcile on resume ("did you record on `<sha>`?")](../learnings/1784445113353-approver-teardown-at-near-terminal-loses-unrecorde.md)
- [scan.py disposition suppression must apply on all ball branches (hoist `human_owns_disposition` to top of `classify()`); pull-universe bot-tag + naive-timestamp gaps](../learnings/1784464263683-scan-py-disposition-suppression-must-apply-on-all-.md)
- [review deliverables need an ETA-relative staleness trigger (~2× ETA), not the 48h dropped-deliverable window](../learnings/1784622307704-review-deliverables-need-an-eta-relative-staleness.md)
- [supervise-issues scan.py over-flag: 3 input/gate bugs (parse_ts naive-tz, narrow bot_logins, HUMAN_OWNED gate) revert on skill re-sync](../learnings/1784638375681-supervise-issues-scan-py-over-flag-3-input-gate-bu.md)
- [supervisor tick 97 — scan.py over-flag reverted again (skill re-sync); raw 15→0 funnel + normalize overwrite](../learnings/1784680848889-supervisor-tick-97-scan-py-over-flag-reverted-agai.md)
- [Sweep coworkers: suppress re-confirmation reports, alert only on deltas](../learnings/1781352352298-sweep-coworkers-suppress-re-confirmation-reports-a.md)
- [CONSOLIDATED: /supervise-issues operations (nudge, deliver, format, concurrency)](../learnings/1780558152383-CONSOLIDATED-supervisor-operations.md)
- [Scheduled diagnostic tasks re-diagnose persistent state inconsistently across fresh sessions](../learnings/1780350138352-scheduled-diagnostic-tasks-re-diagnose-persistent-.md)
- [Dashboard channels render markdown — always include hyperlinks for issue/PR/discussion refs](../learnings/1778835191236-dashboard-channels-render-markdown-always-include-.md)
- [A single common-suite flake can functionally stall the Slang merge queue](../learnings/1782226186227-a-single-common-suite-flake-can-functionally-stall.md)
- [CI: flake-class vs deterministic-hang, and the two-sweep escalation threshold](../learnings/1782346148219-ci-flake-class-vs-deterministic-hang-and-the-two-s.md)
- [supervise-issues pull-universe.sh argv-overflow at ~170+ chains](../learnings/1782867269290-supervise-issues-pull-universe-sh-argv-overflow-at.md)
- [Supervisor artifact-check misses issue-comment artifacts for no-PR chains](../learnings/1783950814878-supervisor-artifact-check-misses-issue-comment-art.md)
- [Track dispatched-work ETAs; a blown ETA is a dropped-task signal, not a reason to keep holding](../learnings/1784154058484-track-dispatched-work-etas-a-blown-eta-is-a-droppe.md)

_Catalog: [[wiki/index.md]]_
