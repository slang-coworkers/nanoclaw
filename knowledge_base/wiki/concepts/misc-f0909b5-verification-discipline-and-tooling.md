---
title: Verification discipline — controls, tests, build environment, and memory tooling
type: concept
group: misc
tags: [verification, controls, filecheck, testing, build, grep, discriminator, okf-synthesis, methodology]
source_count: 17
---

## TL;DR

A cross-cutting discipline: a control (test, assertion, grep, self-check, classifier) that stays green no matter what the code does certifies nothing — it must be PROVEN to discriminate the thing it guards.

- **Arm-test your controls.** Deliberately reintroduce the regression a control guards in a `/tmp` throwaway and confirm it goes RED. Green on first run is also the state you'd see if the control were inert. For Slang bug fixes, a Debug pass does NOT verify the Release path (`SLANG_ASSERT`→`SLANG_ASSUME`/UB); use a four-leg control (fix passes, revert-drill confirms the bug, base binary crashes, restore). "Executable-code-unchanged ≠ fresh build" — prove binding with `nm | grep <new-symbol>`.
- **FileCheck traps:** `//TEST:SIMPLE(filecheck=...)` is silently IGNORED (`0/0`, exit 0) when `slang-llvm`/`libslang-llvm` isn't built — a vacuous null, not a pass; run a shipping FileCheck test as a positive control. A `CHECK-NOT` only discriminates relative to its bounding positive `CHECK`s — re-run the must-fail control after ANY layout change. `CHECK-NOT: for(` is whitespace-sensitive; prefer `for{{[[:space:]]*}}(`.
- **Test-input gotchas:** Slang `L` literals are 32-bit (`7L << 32` = 0); use `LL` to build a nonzero high word or the split/reassembly test passes vacuously.
- **Baselines & inventories are claims:** re-grep a quoted code inventory against current source before republishing; a "current-master" grep is the wrong baseline for a cleanup that follows an open PR; state provenance precisely (PR-head vs squash-merge commit). A whole-file grep for a status token matches inner streaming content — parse the terminal `type==result` event.
- **Blockers are often surmountable:** a "disk-full, can't build" is usually an incremental build on a cached-deps checkout; a docs-only prose edit must skip the auto-formatter (it churns unrelated lines on non-conformant `.md`).
- **Crashpad** captures faults/aborts, not graceful nonzero exits — a null minidump is informative, not a failure.
- **okf-synthesis:** most reported backlog is a frontmatter-convention mismatch, not rot; run a proposed classifier change yourself (a reclass pushes files into another class); frontmatter regexes must be line-anchored and must not cross newlines.

## Make every control actually discriminate

The core principle: a control that stays green regardless of the code is a "dark control" — it certifies the ABSENCE of the mechanism it mimics, and the cheap decisive proof is to deliberately reintroduce the exact regression it guards in a `/tmp` throwaway copy and confirm it goes RED, then delete the copy ([Prove a control arms by breaking the logic in a throwaway copy](../learnings/1787226695062-prove-a-control-arms-by-breaking-the-logic-in-a-th.md)). This applies with force to Slang fixes whose pre-fix failure is `SLANG_ASSERT` in Debug but SIGSEGV in Release: a Debug-only run does NOT verify the shipping config, so the decisive verification is a four-leg control — build at head with `SLANG_ASSERT=release-assert-only` and observe the ACTUAL printed values, revert only the fixed file and grep-confirm the fix is gone, run the input on the base binary and confirm SIGSEGV (which also defeats the stale-binary false pass), then restore — overlaying the single changed `.cpp` into the WARM shared Release build is far cheaper than a cold worktree ([Release crash-reproduction is a distinct control from a Debug pass](../learnings/1787500349746-release-crash-reproduction-is-a-distinct-control-f.md)). And a Monitor/grep control whose timing is impossible for the real event is itself the tell: greping a `stream.jsonl` for `"is_error":true` fired within seconds (the real crash took 120s) because a raw whole-file grep matches an `is_error` field in some inner streaming payload, NOT the run's terminal `{"type":"result",...}` event — parse the LAST `type==result` event, or key on a terminal-artifact floor ([A whole-file grep for a status token matches inner streaming content](../learnings/1787323802416-a-whole-file-grep-for-a-status-token-matches-inner.md)).

## FileCheck and build-environment traps

`slang-test tests/foo.slang` on a `//TEST:SIMPLE(filecheck=CHECK):` test can print `0% of tests passed (0/0), N tests ignored` and exit 0 — a VACUOUS null, because FileCheck (`IFileCheck`) lives in the `slang-llvm`/`libslang-llvm` library, and a build configured without LLVM (missing git tags in a shallow/worktree clone) returns null so every filecheck test is `Ignored`; the positive control is to run a shipping FileCheck test from the tree, and if IT also reports `0/0` then FileCheck is simply unavailable locally and your test is fine ([FileCheck SIMPLE tests are silently ignored locally when slang-llvm is not built](../learnings/1787276807546-filecheck-simple-tests-are-silently-ignored-locall.md), [slang-test FileCheck tests are silently Ignored without libslang-llvm](../learnings/1787343027940-slang-test-filecheck-tests-are-silently-ignored-wi.md)). A `CHECK-NOT` only means something relative to its bounding positive `CHECK`s: restructuring to a leading `CHECK-NOT` + `CHECK-DAG` changed the scan region so the leaked token escaped and the test passed on the BUGGY binary too — after ANY layout change re-run the must-fail control against a known-buggy binary; "passes on the fixed build" is necessary but never sufficient ([FileCheck CHECK-NOT layout: re-run the must-fail control after restructuring](../learnings/1787368063094-filecheck-check-not-layout-re-run-the-must-fail-co.md)). Whitespace matters too: `CHECK-NOT: for(` does NOT match `for (` (with space, as Slang's CPU emitter formats grid loops), so prefer the whitespace-insensitive `for{{[[:space:]]*}}(` idiom and verify it stays non-vacuous ([Reducing a PR to test-only: rebase mechanic + FileCheck whitespace trap](../learnings/1787552911744-reducing-a-pr-to-test-only-after-the-source-fix-me.md)). This last atom also carries a rebase mechanic: `git reset --soft origin/master` is the WRONG tool to rebase an old-based branch (it stages the entire old→new master delta) — back up the file(s) outside the tree, `git reset --hard origin/master`, `git submodule update` any moved pointer, then restore and commit. A pure test-input gotcha in the same family: Slang treats an `L`-suffixed literal as 32-bit, so `(7L << 32)` overflows to 0 and an AnyValue 64-bit split/reassembly test passes vacuously — use `LL` to build a nonzero high word ([Slang int64 shift literals need LL suffix, not L](../learnings/1787337737957-slang-int64-shift-literals-need-ll-suffix-not-l-l-.md)).

## Baselines, inventories, and surmountable blockers

An inventory (file:line lists, "N producers", "doc is stale") is a claim carrying the same staleness risk as memory even when it's your own — re-grep it against current master before republishing a tracking issue, since a quoted PublicDecoration inventory was materially wrong (missed a second producer, "refuted" correct prose, ±1–46 line drift) ([Re-verify a quoted code inventory against source before republishing it](../learnings/1787264346872-re-verify-a-quoted-code-inventory-against-source-b.md)). For a cleanup that follows an open PR, a raw "current-master" grep is the wrong baseline: verify the footprint against the state the cleanup STARTS from (`gh pr diff <N>` to see what the PR itself deletes), watch the decoration-spelling trap (`[export("...")]` = ExportDecoration ≠ `[hlslExport]`), and state provenance precisely — a built worktree at a PR head is NOT the squash-merge commit ([A "current-master" grep is the wrong baseline for a cleanup that follows an open PR](../learnings/1787269228520-a-current-master-grep-is-the-wrong-baseline-for-a-.md)). A "can't build — disk full" blocker is often surmountable: the obstacle is a *from-scratch* build in a *fresh* worktree, but a shared checkout with `external/` already fetched and a populated `build/` needs only an incremental rebuild that fits in a few GB — and prove-of-binding (`nm -C ... | grep <new-symbol>`) plus a non-vacuity control (run the compiler on a NEGATIVE input) defeats the stale-binary false pass; give a concurrent read-only Reviewer A its own worktree so a mid-read edit doesn't desync its line citations ([Disk-full "can't build" is often surmountable via incremental build on a cached-deps checkout](../learnings/1787318166939-disk-full-can-t-build-is-often-surmountable-via-in.md)). For a docs-only prose edit, NEVER run `./extras/formatting.sh --md`: the pristine `docs/*.md` are not prettier-conformant even under the CI-pinned version (CI skips draft PRs), so the formatter reflows ~40 unrelated lines — instead restore pristine (`git checkout HEAD~1 -- docs/file.md`), re-apply only your prose paragraphs, and verify your specific lines are clean without reformatting the whole file ([Docs-only edit: never run the auto-formatter on docs/*.md](../learnings/1787236112272-docs-only-edit-never-run-the-auto-formatter-on-doc.md)). And Crashpad only produces a minidump on a hardware fault or `abort()` — it does NOT capture a graceful nonzero exit, so a null minidump on a "nonzero exit after green summary" flake is informative (the teardown fault is a clean return, needing exit-code/stdout analysis), not a sign the arming failed ([Crashpad in sgl_tests captures faults/aborts, not graceful nonzero exits](../learnings/1787174413870-crashpad-in-sgl-tests-captures-faults-aborts-not-g.md)).

## okf-synthesis memory-scanner tooling

Running `/okf-synthesis` on a slang-fixer-style OKF tree, most reported backlog is NOT foldable rot: NO-FRONTMATTER is over-counted because the scanner only recognizes a top-level `type:` while the NanoClaw auto-memory writer emits nested `metadata:\n  type:` (263 of ~503 files), live operational aggregates (`active-holds.md`) legitimately ESCALATE, per-issue `fix-<n>.md` logs are one concept each, and the one clean win is deleting stale generated index monoliths — verify before fragmenting anything ([okf-synthesis backlog is inflated by frontmatter-convention mismatch, not rot](../learnings/1787373318278-okf-synthesis-backlog-is-inflated-by-frontmatter-c.md)). The fixes shipped three heuristic changes — anchor `_has_type` to `^\s*type:\s*\S` (recognizing the nested dialect, with a `node_type:`-only negative test), gate the DOSSIER heuristics on `not _has_type`, and add an `okf_synth: exempt` flag for genuinely-live aggregates — but SKILL.md is the source of truth (the container rewrites the tool each run) and there is NO repo source-of-truth, so a local edit doesn't propagate ([okf-synthesis: nested metadata.type recognition + self-declared exempt flag](../learnings/1787375075036-okf-synthesis-nested-metadata-type-recognition-sel.md)). Two follow-on discipline lessons: a codex critique found the round-1 regexes cross newlines (`\s` in `re.M` matches `\n`, so `type:\n<nextkey>` reads as typed and a block-scalar body can silence an arbitrarily large file) — for "value on the SAME line as the key" always use `[ \t]`, never `\s`, and walk block-scalars as YAML keys ([okf-synthesis round-2: frontmatter regexes must not cross newlines](../learnings/1787376438827-okf-synthesis-round-2-frontmatter-regexes-must-not.md)). And, from the authorizing side: before signing off on a heuristic/classifier change, RUN the proposed logic yourself against real data — a change that clears one offender class frequently pushes the same files into another (here DOSSIER→OVERSIZE, ESCALATE unchanged), so the fix's headline outcome can be false even when its stated mechanism is true; reproduce the baseline first, simulate the proposed classifier end-to-end, dump the full top-N offender list, and distrust aggregate "phantom→real" headlines ([Run a proposed classifier change yourself — a reclassification is not an exemption](../learnings/1787374271043-run-a-proposed-classifier-change-yourself-a-reclas.md)).

**Source learnings (17):**
- [Prove a control arms by breaking the logic in a throwaway copy](../learnings/1787226695062-prove-a-control-arms-by-breaking-the-logic-in-a-th.md) — a dark control stays green regardless of the code; reintroduce the guarded regression in /tmp and confirm RED; green on first run is not evidence it works.
- [Release crash-reproduction is a distinct control from a Debug pass](../learnings/1787500349746-release-crash-reproduction-is-a-distinct-control-f.md) — SLANG_ASSERT→UB in Release; four-leg control (fix passes / revert-drill / base crashes / restore); overlay the changed .cpp into the warm shared build.
- [A whole-file grep for a status token matches inner streaming content](../learnings/1787323802416-a-whole-file-grep-for-a-status-token-matches-inner.md) — parse the LAST type==result event, not a substring grep over the whole stream; an impossibly-fast fire is the tell the instrument matches the wrong thing.
- [FileCheck SIMPLE tests are silently ignored locally when slang-llvm is not built](../learnings/1787276807546-filecheck-simple-tests-are-silently-ignored-locall.md) — 0/0 + exit 0 is a vacuous null; run a shipping FileCheck test as a positive control; verify by hand and note CI-gated lanes.
- [slang-test FileCheck tests are silently Ignored without libslang-llvm](../learnings/1787343027940-slang-test-filecheck-tests-are-silently-ignored-wi.md) — IFileCheck lives in libslang-llvm (a heavy target); build slang-llvm to exercise FileCheck locally, else verify patterns by hand; don't report 0/0-ignored as passing.
- [FileCheck CHECK-NOT layout: re-run the must-fail control after restructuring](../learnings/1787368063094-filecheck-check-not-layout-re-run-the-must-fail-co.md) — a CHECK-NOT discriminates only relative to its bounding positive CHECKs; a "more robust" leading CHECK-NOT + CHECK-DAG passed on the buggy binary; a proven layout beats a theoretical one.
- [Reducing a PR to test-only: rebase mechanic + FileCheck whitespace trap](../learnings/1787552911744-reducing-a-pr-to-test-only-after-the-source-fix-me.md) — `git reset --soft` is wrong for rebasing an old branch (hard-reset + restore instead); `CHECK-NOT: for(` is whitespace-sensitive; keep cross-PR narrative out of test comments.
- [Slang int64 shift literals need LL suffix, not L](../learnings/1787337737957-slang-int64-shift-literals-need-ll-suffix-not-l-l-.md) — `L` is 32-bit so `(7L<<32)`=0; use `LL` to build a nonzero high word or the split/reassembly test passes vacuously; a test-authoring gotcha, not a compiler bug.
- [Re-verify a quoted code inventory against source before republishing it](../learnings/1787264346872-re-verify-a-quoted-code-inventory-against-source-b.md) — an inventory is a claim with memory-grade staleness even when your own; re-grep before filing a tracking issue; delegating the re-verify keeps grep volume out of context.
- [A "current-master" grep is the wrong baseline for a cleanup that follows an open PR](../learnings/1787269228520-a-current-master-grep-is-the-wrong-baseline-for-a-.md) — measure the footprint at the state the cleanup starts from (gh pr diff); decoration-spelling and PR-head-vs-squash-merge provenance traps; state provenance precisely.
- [Disk-full "can't build" is often surmountable via incremental build on a cached-deps checkout](../learnings/1787318166939-disk-full-can-t-build-is-often-surmountable-via-in.md) — a shared checkout with external/ + build/ needs only an incremental rebuild; nm-grep proof-of-binding + negative-input non-vacuity control; isolate a concurrent reviewer's worktree.
- [Docs-only edit: never run the auto-formatter on docs/*.md](../learnings/1787236112272-docs-only-edit-never-run-the-auto-formatter-on-doc.md) — pristine docs aren't prettier-conformant (CI skips draft PRs), so the formatter churns ~40 unrelated lines; restore pristine and re-apply only your prose.
- [Crashpad in sgl_tests captures faults/aborts, not graceful nonzero exits](../learnings/1787174413870-crashpad-in-sgl-tests-captures-faults-aborts-not-g.md) — a null minidump on a graceful-exit flake is informative not a failure; arming Crashpad is still the correct first diagnostic; SGL_HAS_CRASHPAD needs the config.h include.
- [okf-synthesis backlog is inflated by frontmatter-convention mismatch, not rot](../learnings/1787373318278-okf-synthesis-backlog-is-inflated-by-frontmatter-c.md) — NO-FRONTMATTER over-counted (nested metadata.type), live aggregates legitimately ESCALATE, per-issue logs are one concept; only stale index monoliths are a clean delete.
- [Run a proposed classifier change yourself — a reclassification is not an exemption](../learnings/1787374271043-run-a-proposed-classifier-change-yourself-a-reclas.md) — a class-clearing change often pushes files into another class (DOSSIER→OVERSIZE); reproduce baseline, simulate end-to-end, dump the full offender list, distrust phantom→real headlines.
- [okf-synthesis: nested metadata.type recognition + self-declared exempt flag](../learnings/1787375075036-okf-synthesis-nested-metadata-type-recognition-sel.md) — anchor `_has_type`, gate DOSSIER on not-typed, add `okf_synth: exempt`; SKILL.md is the source of truth and there's no repo propagation.
- [okf-synthesis round-2: frontmatter regexes must not cross newlines](../learnings/1787376438827-okf-synthesis-round-2-frontmatter-regexes-must-not.md) — `\s` in re.M crosses newlines (block-scalar abuse silences files); use `[ \t]` for same-line values and walk block-scalars as YAML keys; raw docstrings for `\n` examples.
