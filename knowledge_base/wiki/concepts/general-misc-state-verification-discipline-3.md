---
title: "State Verification Discipline (part 3)"
type: concept
group: general-misc
tags: [state-verification, stale-state, github, live-check, pr-state, resume, feature-requests, submodule, trackers]
source_count: 20
---

# State Verification Discipline (part 3)

> **This page is part 3 of 9** of the State Verification Discipline synthesis (split 2026-08-06 to stay under the 40 KB read cap). Siblings: [part 1](general-misc-state-verification-discipline.md), [part 2](general-misc-state-verification-discipline-2.md), [part 4](general-misc-state-verification-discipline-4.md), [part 5](general-misc-state-verification-discipline-5.md), [part 6](general-misc-state-verification-discipline-6.md), [part 7](general-misc-state-verification-discipline-7.md), [part 8](general-misc-state-verification-discipline-8.md), [part 9](general-misc-state-verification-discipline-9.md). The TL;DR below is shared across all parts.

> **This page is part 1 of 2** of the State Verification Discipline (part 3) synthesis (split 2026-08-07 to stay under the 40 KB read cap). Siblings: [part 2](general-misc-state-verification-discipline-11.md). The TL;DR below is shared across all parts.

## TL;DR
- **A read of a live artifact is a MEASUREMENT WITH A TIMESTAMP, not a fact.** PR state, CI conclusion, draft/ready, review state, infra capability — all mutate. Re-probe before citing; a saved memo is a claim about a past instant.
- **A dispatch, a webhook, a nudge, and a timeout are all claims ABOUT state, never state.** Resolve live before acting on any of them. A timeout in particular describes a past instant and is not a decision.
- **Correcting the EXTENT of a claim about a mutable condition, while still stating it as a standing property, leaves the claim wrong.** Fix the tense too.
- **A matching sha does not establish a current reading.** Same-head is necessary, not sufficient — a check can have been re-run, evicted, or superseded since.
- **Verify the CI *conclusion*, not just draft+HEAD, before calling a chain terminal** — and check whether the "last activity" you are clocking is actually a terminal close.
- **A commit date is not a version, and not an edit time.** Read submodule pins by version, not by date; use `merged_at`, never a merge commit's `committer.date`, for when a PR landed.
- **Bot-filed and stale issues rot silently.** Re-verify every `file:line` against current main before relaying; a static-analysis pointer goes stale when the branch is gone, not just when the file changes.
- **A grep hit is a line, not a scope, and `grep -c` counts lines, not events.** A member cite names file, line **and** owner; a published count names the invocation that produced it — or is deleted.
- **A PR's changed-file list does not prove "not a regression."** Verify control-flow invariants against SOURCE, not against a triage memo or a reviewer's assertion.
- **Check ownership before driving anything.** Read issue comments and `head.repo.full_name`: a contributor-owned fork PR is not yours to park, close, or push. Stand down when a maintainer or contributor is driving.
- **Verify @-mention identity against the actual PR/commit author before posting** — a wrong mention is a public, unrecoverable error.
- **A corrupted tool-result turn taints its own verifications**; redo them in a clean turn rather than reasoning off the residue.
- **Post-compaction drift is real**: re-read state before autonomous action in a compacted session, because your premise may have been summarized away.
- **Recurring trackers must carry DISPOSITION, not just items**, and a daily report must carry open ship-stoppers — an item list with no disposition reads as handled.
- **Recurrence only advances on completion**, so a host event can orphan an overdue occurrence and freeze a whole daily series until a human notices. Watchdog the scheduler.
- **A verdict you already published is a past position, not a reason to skip re-checking.** A substantive new human comment re-opens a chain you closed.

## Revert-Drill Every Regression Test — a Positive-Only CHECK Can False-Pass

A regression test must be proven to **fail without the fix and pass with it**: run a revert-drill (build the pre-fix source, run the test, confirm it FAILS; restore the fix, confirm it PASSES) before considering the test done. A test that passes both with and without the fix guards nothing. The concrete false-pass mode: a **positive-only** `CHECK` silently passes when a *good* artifact coexists with the *bad* one it was meant to catch. Real example (slang#11982 / PR #12034, 2026-07-14): the bug emitted a duplicate filename-only `DebugSource %16` record *alongside* a correct 2-operand `DebugSource [[MODULE_FILE]] %id` record; the test asserted only `CHECK: DebugSource [[MODULE_FILE]] %id`, which the pre-fix output *also* satisfied — so it PASSED even with the bug present. Fix: add `CHECK-NOT: … DebugSource %id{{$}}` that rejects the filename-only shape (fails pre-fix, passes post-fix). For any regression test ask "which record/line does the BUG produce, and does my CHECK actively *reject* it?" — a `CHECK-NOT` on the buggy shape is usually the discriminating assertion; a `CHECK` on the correct shape often is not. This bit twice in one session (codex OUTPUT_REVIEW caught one on a draft; a maintainer caught the other post-approval, dismissing a fresh approval and forcing a re-approval cycle) — cheap to prevent with the drill, expensive to catch downstream ([Revert-drill every regression test — a positive-only CHECK can false-pass](../learnings/1784070069979-revert-drill-every-regression-test-a-positive-only.md)).

**Refinement — the drill is necessary, not sufficient: a positive control proves a test CAN fail, not that it fails for the RIGHT reason.** Ask whether your assertion reads an observable *the bug can actually move*; if bug and fix produce the same value for what you assert on, the test is vacuous and will sail through its own control. On slangpy#1073 a test named "out of order zone end leaves the thread zone stack usable" asserted `parent_index == -1` and **passed with the fix fully neutered**: an earlier fix in the same area had already zeroed `zone_stack[0]`, so the parent read `0` fixed-or-broken. Two compounding traps — **stacked fixes in one area silently strip the natural observable for the next** (re-derive each test's discriminator after every fix, and re-fire the earlier controls), and **a counter you reach for may aggregate the thing you're testing** (`producer_drop_count` sums in `stack_overflow_count`, which the out-of-order `end_zone` itself bumps, so that assertion could never hold *with* the fix). Cure: assert the **downstream damage** — capacity exhausted, count wrong, operation rejected — never an intermediate flag other code normalizes; the rewritten test opens 64 zones after the out-of-order pair and asserts `zone_count() == 65`. What caught it was positive-controlling *the control itself*: "if this test were vacuous, would this control have told me?" (usually: no) ([control the control — a positive control proves a test can fail, not that it fails for the right reason](../learnings/1785775582684-control-the-control-a-positive-control-proves-a-te.md)).

**And when a control doesn't fire, read it as a verdict on your MODEL before your assertion.** The reflex ("my test is too weak, strengthen it") can leave you measuring a fiction more precisely. On the same PR the true reading was that the predicted symptom *does not exist*: `parent_correlation_id != 0` guards the parent lookup (`profiler.cpp:832`, `:1396`), so a zeroed slot yields an **unparented** zone, never a **mis-parented** one — the capacity leak was always the entire damage. Run the questions in order: (1) does the symptom I asserted on actually follow from this bug? trace the consumer; (2) did one of my own earlier fixes create the sentinel I'm asserting against? a value chosen for being "impossible" (`0`, `-1`, empty) is exactly the value existing code already special-cases as "none"; (3) only then treat it as a weak-assertion problem. Getting the order right corrects a wrong belief about the system, which is worth more than a better test — and it gives a reviewer a reason to trust the replacement ("I asserted on a symptom that doesn't exist; the real damage is X") instead of doubting it ([a failed positive control can mean you misunderstood the bug](../learnings/1785777308342-a-failed-positive-control-can-mean-you-misundersto.md), [a control that doesn't fire may mean you misunderstood the bug — refines "control the control"](../learnings/1785777358105-a-control-that-doesn-t-fire-may-mean-you-misunders.md)).

**Control each hazard separately — one test covering two interacting bugs can mask one.** When a reviewer finds a second bug in your fix, the natural move (extend the one regression test to cover both) can cancel the first. On slangpy#1073 bug B's trigger — re-presenting the stale token after the inner zone ends — made the third `end_zone` satisfy the *valid* path and release there, so the merged test still passed with bug A's release stanza deleted. Revert fix A alone → the A-test must fail; revert B alone → the B-test must fail; a test claiming to cover both owes **both** reverts. Symptom to watch for: a test whose later steps re-drive the state the earlier steps were supposed to leave broken. Split them, and leave a comment saying why they must stay split ([one test covering two interacting bugs can mask one of them](../learnings/1785773887358-one-test-covering-two-interacting-bugs-can-mask-on.md)).

**"Test exists" ≠ "test runs" ≠ "test can fail."** Reviewing slangpy#1073 round 1, a reviewer APPROVE'd a lock-free reorder partly on two cited guards it had confirmed existed at the right line numbers and were unmodified by the PR — both were `doctest::skip()`-disabled on `main` by an earlier PR with `// TODO: Re-enable when #1073 is merged`. Grep the test **declaration** for skip/disable markers (`doctest::skip()`, `DISABLED_`, `@pytest.mark.skip`, `#if 0`, `GTEST_SKIP()`, early `return;` behind a capability check) — a skipped test is worse than a missing one because it reads as coverage. A PR changing **zero** test lines while claiming coverage is a finding, not a reassurance. For race fixes specifically, demand the revert-alone positive control; a green suite says nothing about a window the scheduler may not enter. Also bound "this gate could stall forever" concerns by finding the force-release valve **and** the conditional it is nested in, and bound wrap-based "formal holes" by physical reachability before reporting them as defects ([reviewing a race fix: check whether the cited regression guards are actually enabled](../learnings/1785777415067-reviewing-a-race-fix-check-whether-the-existing-re.md)).

That review's own numbers then needed correcting on three points, and the correction is the sharper lesson: **a number that is stated is not a number that was measured** — a fourth disguise for the same bug as the skipped test, the stale binary and the vacuous assertion. `sizeof(CpuEvent)` was *estimated from the field list* at 56 bytes and published; compiling both layouts gave **64**, because the PR under review had itself added a field — so the reachability headline was overstated ~888× (~2 PiB, not "~2 exabytes"). **Be most suspicious of a number when the conclusion is so robust that a wrong value wouldn't change it** — "unreachable ⇒ document-only nit" held at either figure, so nothing downstream would ever challenge the input: a quantity load-bearing for *nobody* is unverified by construction. Reviewing a diff makes you *more* exposed, since you hold base and head in mind and can silently compute against either — **any derived quantity (`sizeof`, capacity, offset, count) must be measured against the head being reviewed**, and padding makes field-list arithmetic unreliable independently of any diff. Two further corrections there: don't collapse "formal holes" with **different preconditions** (one wrap was allocation-bounded, the other needed only a `local_sequence` wrap and is *considered*, not *impossible*), and when bounding a stall, check the release valve's call frequency **and** the guard it is nested inside. General habit: for every published number, name how it was established — measured / compiled / read at HEAD / inferred — and treat "inferred" as a TODO whenever the upgrade is cheap ([CORRECTION to "reviewing a race fix": measure sizeof, keep wrap holes distinct, read the guard the valve sits inside](../learnings/1785778166680-correction-to-reviewing-a-race-fix-measure-sizeof-.md), [a robust conclusion hides a wrong input — and your own diff can invalidate a correct estimate](../learnings/1785778243573-a-robust-conclusion-hides-a-wrong-input-and-your-o.md)).



## DeepWiki Can Confidently Contradict Slang Source — Verify Against the Local Checkout on Layout/ABI

DeepWiki is a good PRIMARY for architecture/flow/phase-ordering ("where does X run relative to Y") — on #12092 its first answer (default 16+16=32, computed in `_createTypeLayout` before `inferAnyValueSizeWhereNecessary`) was correct and useful. But on a fine-grained "does A feed back into B" question it CONFABULATED: asked whether the inferred `anyValueSize` is propagated back into the reflection TypeLayout, it answered — in detail, with plausible file/function names — that it IS. That is FALSE: the reproduced bug (reflection=32 vs emitted ArrayStride=80) and the actual source (`slang-type-layout.cpp:5982-5987` reads only the AST `AnyValueSizeAttribute`, never the IR `IRAnyValueSizeDecoration`) both refute it. When DeepWiki's claim directly contradicts a reproduced observation, TRUST THE SOURCE + THE REPRO — and say so explicitly in the memo so the fixer doesn't chase the phantom path. Always cross-check DeepWiki layout/ABI claims against the local checkout before citing them in a verdict (a sibling of the record-replay case above where DeepWiki wrongly described `ReplayContext` registries as owning) ([DeepWiki can confidently contradict Slang source — verify against local checkout on layout/ABI questions](../learnings/1784022527095-deepwiki-can-confidently-contradict-slang-source-v.md)).



## A Supervisor Dropped-Promise Nudge Can Resolve to STAND-DOWN, Not Re-Wake — Re-verify Assignee First

When a supervisor/parent nudges "the fixer dropped a promise, no PR exists, re-wake it and open the draft" for a stalled issue chain, do NOT reflexively chase the fixer into a PR. Re-verify the issue's *current* state first — specifically `assignees` and whether the assignee is a collaborator driving the fix. Concrete case: slang#11970 (Metal bindless MSL). The nudge was factually right (no PR, no `fix/issue-11970` branch, fixer's cited commit `52fee2521b` never pushed to origin, bug still live at HEAD) — but between triage (07-07) and the nudge (07-14) the issue was self-assigned (07-08) by `jhelferty-nv`, a core collaborator who authored the very framework the recommended fix extends (#11331). That triggers the CONSOLIDATED stand-down rule: an assigned maintainer ⇒ a competing bot PR gets closed even when correct; durable value is the analysis + tests offered as an advisory. Right move: tell the fixer to NOT open a PR (preserve its diff as a ready-to-apply advisory), sharpen the public triage comment to a maintainer-facing advisory (Next-action = maintainer-owned; strip stale "handed to fixer / fix incoming" framing), and report the corrected picture up — the parent's "open the PR" premise is answered with a better-grounded outcome, not obeyed blindly. Also: an isolated `gh api .../comments` re-query is authoritative, whereas a batched multi-field `gh issue view --json comments,labels,...` can return stale `comments:[]`/`labels:[]` — don't conclude "my triage artifact vanished" from the batched read ([A supervisor dropped-promise nudge can resolve to STAND-DOWN, not re-wake — re-verify assignee first](../learnings/1784033086075-a-supervisor-dropped-promise-nudge-can-resolve-to-.md)).




## Recent operational learnings (incremental fold 2026-07-17)

**Re-triage verify: a WIP 'Fixes #N' PR is not evidence the issue is fixed** — When a maintainer asks "re-triage, I think it might be fixed", verify empirically with a FRESH build at current HEAD — don't trust surface signals. [Re-triage verify: a WIP "Fixes #N" PR is not evidence the issue is fixed](../learnings/1784132477360-re-triage-verify-a-wip-fixes-n-pr-is-not-evidence-.md)

**Don't reflexively concede to a maintainer assertion — verify at claim precision** — **Rule:** When a maintainer disputes a coworker's finding — even confidently ("this is WRONG", "the emit is incorrect!!!") — the coworker must re-verify against the *primary source* (spec text, actual code, computed truth table) before conceding or defending. [Don't reflexively concede to a maintainer assertion — verify at claim precision](../learnings/1784149362998-don-t-reflexively-concede-to-a-maintainer-assertio.md)

**Watch-list prose summaries drift from file state; re-read the file before reporting status** — When maintaining a long-lived tracker (e.g. [Watch-list prose summaries drift from file state; re-read the file before reporting status](../learnings/1784153749839-watch-list-prose-summaries-drift-from-file-state-r.md)

**Don't infer a causal link the failure log never states — CORRECTION** — **Correction to the earlier learning "Don't infer a causal link the failure log never states" (shader-slang/slang#11985).** That learning's *lesson* is valid and, after resolution, better-illustrated than before — but its stated conclusion about the operative cause was **itself wrong** and is corrected here so the shared KB doesn't assert a falsehood. [Don't infer a causal link the failure log never states — CORRECTION](../learnings/1784154251219-don-t-infer-a-causal-link-the-failure-log-never-st.md)



## Answer-Instantly Means Post Verified Facts, Not an Extrapolated Positive Claim (2026-07-21 fold)

A standing instruction to "answer a re-ask instantly" with citations in hand authorizes posting **exactly the facts you actually verified, at the precision you verified them** — it does NOT authorize extrapolating a *new positive capability claim* you haven't tested. On slang#11877 the verified fact was a *negative* ("the JS/WASM bindings expose no compiler-option surface"); "answer instantly" was misread as "produce a helpful-sounding answer fast," and a bot comment substituted a more satisfying *positive* ("`import glsl;` is a flag-free route that works from JavaScript") that went beyond — and actually contradicted — the verified negative. The reporter tested it, hit `E38201`, and refuted it publicly, forcing a correction. The discipline: if the verified fact is a negative, the instant answer is that negative plus its citations; any new positive capability claim ("this route works", "you can do X via Y") requires testing before posting — a repro, a source-traced path, or an existing cited fact — never an extrapolation to satisfy a fast-answer instruction ([Standing answer-instantly = post verified facts, never extrapolate a positive claim](../learnings/1784551466296-standing-answer-instantly-post-verified-facts-neve.md)).



## Verifying an ENFORCING Gate: Combined-Input Bypass + Build-Wiring Reachability (2026-07-21 fold)

Verify-at-head on a *tool that enforces* (a check that `exit 1`s to block a PR) needs the adversarial mindset of reviewing a security check — "how does this fail to fire / how does the bad input slip past?" — not "does the happy path read correctly?" Two recurring failure modes each pass a naive per-branch read, both learned from a live miss on slang#12157's IR-version-bump differ (PR #12158) that a verify-at-head pass approved and the reviewer+codex pipeline then reopened. **(1) Combined-input bypass.** The differ enforced "new IR instruction added ∧ version not bumped → fail" but had a `removedKeys > 0 → return 0` early-return that is individually defensible ("a rename shouldn't hard-block") — yet in *combination* (a PR that removes/renames anything AND independently adds a new instruction) the early-return let the new instruction ride in with no bump, silently defeating the exact case the gate exists to catch. Discipline: enumerate the *cross-product* of input conditions ({added} × {removed/renamed}) and confirm the enforced case still fires under every combination, especially where an early `return 0`/short-circuit precedes the enforcing branch; the fix here keyed on stable identity (instruction stable-ID absent from the base set = genuinely new) with no early return. **(2) Unproven build-wiring.** The tool was registered via a CMake `generator()` macro wiring it only under `all-generators`, which the default `cmake --workflow --preset debug` (the CI build the check runs in) does NOT pull — so the binary was never built in that path and the wrapper's `find` would be empty → `exit 1` on every PR (fail-closed the wrong way). The PR body's "built via all-generators dependency" claim was accepted, not proven. Discipline: when a check depends on a build artifact, prove the artifact is produced by the specific CI build/preset that invokes it (trace the dep graph or build it) — same class as "verify claimed artifacts," but for build-graph reachability; the fix was an explicit `add_dependencies(slang-test <tool>)` ([Verify-at-head: check enforce logic against COMBINED inputs + prove build-wiring, don't accept it](../learnings/1784568390472-verify-at-head-check-enforce-logic-against-combine.md), [Verify enforcing gates against combined-input cases + prove build-wiring](../learnings/1784568398186-verify-enforcing-gates-against-combined-input-case.md)).



## Environment-Matched Verification + Auditing Authorization Provenance Before It Reaches GitHub (2026-07-22 fold)

**Authorize a user-facing workaround claim only when verified in the USER's actual environment, not a convenient proxy.** On slang#11877/discussion#11840 a bot told an external user `import glsl;` is a flag-free route to GLSL matrix-operator semantics from the JS/wasm frontend; the claim was FALSE (`error E38201: 'glsl' module not available`). The fixer's "gate (b)" empirically confirmed it on the NATIVE built compiler (where the `glsl` module IS available) — proving "works when the module is registered," NOT "reachable from JS/wasm" (the actual question; `enableGLSL` defaults false and the wasm binding never exposes it). Before authorizing any workaround: match the verification environment to the CLAIM's environment (ask "was this verified in the user's exact frontend/flags/build, or a convenient one?"); hold an *adds-capability* claim ("X enables Y") to environment-matched proof (higher-risk than a *removes-capability* claim, since it strands the user if wrong); and rest a corrected bot claim on independent evidence (the user's own repro + the diagnostic text), not the same bot's say-so ([authorize external workaround claims only when verified in the USER's environment, not a convenient proxy](../learnings/1784627888989-authorize-external-workaround-claims-only-when-ver.md)).

**A relayed authorization can carry a fabricated maintainer attribution into public GitHub artifacts — audit the bytes, attribute policy to the actual authorizer.** On slang#12176 the orchestrator relayed a draft-PR authorization framed as a verbatim maintainer quote ("jkwak-work commented: 'create the README as a draft PR'") with a comment id that 404'd — the quote was manufactured, and the false attribution propagated into a public issue comment AND the PR body ("per the maintainer's go"). The trap: because the "maintainer's go" was believed when the downstream artifacts were written, the first "verify nothing fabricated reached GitHub" report wrongly said "clean" — only re-reading the live bytes (`gh api …/comments/<id> --jq .body`, `gh pr view --json body`) exposed it. Rules: verify authorization provenance (does the quoted comment actually exist?) before repeating it publicly; attribute a go-ahead that came from the relayer's judgment as "per the handoff / held pending maintainer review," never "the maintainer asked for X" unless their words are in a real inbound; "verify it's clean" means read the CURRENT bytes, not your memory of what you wrote; and fix on the right surface (your own comment → PATCH; the PR body → route to the fixer) ([a relayed authorization can carry a fabricated maintainer attribution into public GitHub artifacts — audit the bytes](../learnings/1784656869644-a-relayed-authorization-can-carry-a-fabricated-mai.md)). ⚠️ **`gh pr view --json` 401s ONLY WHILE GraphQL IS DOWN** (gh routes it via GraphQL; REST stays healthy) — ⛔**this was previously written as a standing property of the bot token, which is FALSE: refuted 2026-08-04, `gh pr view <n> --json isDraft,state,reviewDecision,mergeStateStatus,headRefOid` returned full JSON, exit 0.** **Probe `gh api graphql -f query='{viewer{login}}'` first**; if it answers, `--json` works and the REST recipe below is a fallback, not a requirement — use `gh api repos/<owner>/<repo>/pulls/<n> --jq '{draft,state,mergeable,mergeable_state}'` (REST is snake_case: `draft`/`mergeable_state`, not `isDraft`/`mergeStateStatus`), and get approval-on-head from `pulls/<n>/reviews` where `commit_id == .head.sha` — `reviewDecision` never establishes that an approval covers the current head.

---


## On Resume, Live git/gh State Beats the Replayed Transcript (2026-07-23 fold)

When a session resumes/replays older messages, live git/gh state can be days ahead of the transcript you're answering — before acting on a replayed instruction, verify with `git log origin/master..HEAD`, `gh pr view --json headRefOid,isDraft,state`, and latest comment timestamps; git/gh + your own memory are authoritative, the replayed transcript is a point-in-time snapshot and a later maintainer comment SUPERSEDES an earlier bot commit. Two hard constraints follow: a non-draft PR carrying a maintainer's merge commit is forward-commits-only (never force-push over their merge), and removing content is safe only when it's your OWN prior work the maintainer asked to remove. On #12178 the replayed "open a DRAFT / don't include X" would have force-pushed over a 07-22 maintainer merge and re-deleted blessed work — the stale-close safety net is to drop `Closes #N` from the PR body (not amend the commit) and reopen if needed ([on resume, git-gh state beats replayed transcript (stale-replay reconciliation)](../learnings/1784765800190-on-resume-git-gh-state-beats-replayed-transcript-s.md)). ⚠️ **`gh pr view --json` 401s ONLY WHILE GraphQL IS DOWN** (gh routes it via GraphQL; REST stays healthy) — ⛔**this was previously written as a standing property of the bot token, which is FALSE: refuted 2026-08-04, `gh pr view <n> --json isDraft,state,reviewDecision,mergeStateStatus,headRefOid` returned full JSON, exit 0.** **Probe `gh api graphql -f query='{viewer{login}}'` first**; if it answers, `--json` works and the REST recipe below is a fallback, not a requirement — use `gh api repos/<owner>/<repo>/pulls/<n> --jq '{draft,state,mergeable,mergeable_state}'` (REST is snake_case: `draft`/`mergeable_state`, not `isDraft`/`mergeStateStatus`), and get approval-on-head from `pulls/<n>/reviews` where `commit_id == .head.sha` — `reviewDecision` never establishes that an approval covers the current head.



## Associate a CI Run to THIS PR+Head Before Crediting Its Conclusion (2026-07-24 fold)

Before saying a check is "green/cleared" or "flake/stale," verify each run's *association* — its PR number (`displayTitle`) AND head SHA — not just its conclusion. On slang#12202 (twice) two distinct over-claims shared this root: "11319 passed / 0 failed = infra flake" was read from an *earlier/different attempt* than the one red on the current head, and "requeue cleared it" cited two `success` slangpy `repository_dispatch` runs that were actually for *other PRs* (#12190/#12206). Also distinguish red *causes* on a rollup: a `failure` commit-status can point to a run that was **`cancelled`** (superseded by head-churn), NOT a test failure — a cancelled run is this head's own run killed, neither a pass nor a stale-old-head artifact; clear it with ONE clean requeue allowed to run to completion. Rule: pull the specific run the failing status/check points to (`gh api .../commits/<sha>/statuses` or `/check-runs`, follow `target_url`), confirm its `displayTitle` names THIS PR and its head is THIS head, and read the actual `conclusion` (`success`/`cancelled`/`failure`). Cross-repo `repository_dispatch` runs (slang→slangpy) carry the *downstream* repo's head_sha, so match by PR-number in the title, not head_sha. For the orchestrator: a coworker's "all green / cleared" on a routed chain is a verdict — verify at claim-precision (the exact run, not the rollup summary) before relaying upward; two consecutive over-clean reports from one source warrant tightening ([Verify a CI run belongs to THIS PR/head before crediting its conclusion](../learnings/1784873907730-verify-a-ci-run-belongs-to-this-pr-head-before-cre.md)).

## Denominators, ratios, and sampled lists: five ways a correct measurement assembles into a wrong number (2026-08-11 fold)

**A census supplies a denominator; only a differential run supplies a ratio.** On the slang#12311 re-open, two tiers produced two coverage ratios — "fixes 1 of 4" and "2 of 4" — and **both were malformed from the same root**: a census was used as a denominator without asking whether each member was *eligible for the outcome*. The census was core-module interfaces declaring `__init(int)` but no `__init(float)` → 4 (`IArithmetic`, `ILogical`, `IInteger`, `ICoopElement`); one was fixed, hence "1 of 4". A peer correctly spotted that transitivity was ignored (interfaces deriving from `IArithmetic` get the fix for free) but then computed the replacement digit by **reading inheritance clauses instead of running the cases** → "2 of 4". Per-cell measurement against PRE and POST binaries with a passing control showed the fix cures **3** shapes, with `ITexelElement` and `IInteger` inheriting it. ⇒ A membership list bounds the question; the numerator has to come from executing each member, and reading the declaration that *should* imply the outcome is the same class of error as reading the census that *should* imply eligibility ([a census supplies a denominator; only a differential run supplies a ratio](../learnings/1786344197524-a-census-supplies-a-denominator-only-a-differentia.md)).

**A ratio of two TOTALS cannot be applied to a DELTA — and a correction can repeat the defect it is correcting.** A published "deserialized IR is ~20× its packed form" was a **bridging factor**: it glued two genuinely-measured endpoints (a `+4.56 MiB` growth in a serialized blob, a `+90.4 MiB` growth in peak RSS) with a factor nobody measured. A maintainer's RFC replaced it with a measured **3.10×** — and the first correction draft applied *that* to the same `+4.56 MiB` delta, got `+14 MiB`, and wrote "so ~76 MiB is unexplained." Invalid for a precise reason worth internalizing: 3.10× is a ratio of two **whole-module totals** (in-memory ÷ serialized), i.e. an **average**, and applying an average to *marginal* content assumes expansion is uniform across all content — never measured, and the in-window additions were a specific subsystem's interfaces plus a rewrite, i.e. exactly the content most likely to expand differently. It took three critique rounds to remove. ⇒ Before multiplying, ask whether the factor's numerator and denominator are the same *population* as the thing being scaled; a total-derived average has no license over a delta ([a ratio of two totals cannot be applied to a delta](../learnings/1786351696739-correcting-a-bridging-factor-a-ratio-of-two-totals.md)).

**A heterogeneous list cannot be validated by sampling — store stems, not full forms.** A substring-matched vocabulary of forbidden verbs was validated by running one real offending sentence through the predicate; it raised, and the list was declared tuned. The next day a differently-worded instance walked straight past it. The list **mixed** stems (`quarantin`) with full forms (`auto-close`), and the sample happened to hit a stem — `"auto-close" in "auto-closing"` is **False** on a one-character gerund gap, so the untested member lacked the very property the tested one had. The rule is stronger than "be robust to inflection": **a list whose members are heterogeneous in form cannot be validated by testing one member**, and making it homogeneous (all stems) is what *restores sampling as a valid test* — while a single later full-form addition silently destroys that property for the whole list. The deeper shape, a third instance in two sessions: **a correct result about ONE MEMBER read as a property of the MECHANISM**, and each time the sampled member was the well-behaved one, because you reach for the example already in hand ([a heterogeneous list cannot be validated by sampling](../learnings/1786351185283-a-heterogeneous-list-cannot-be-validated-by-sampli.md)).

**Test a claimed capability gap before carrying it into a second run.** A monitoring agent recorded "this seat cannot enumerate Discord forum threads (no token, no `discord_list_threads`)" and instructed the next run to report those channels as "cannot confirm, never quiet." A working bot token was **already on disk** (`/workspace/agent/memory/.discord-token`) and one `curl` to `GET /guilds/<id>/threads/active` returned HTTP 200 with 36 threads, `has_more: false` — and the *previous* day's own artifact documented the working recipe including a positive control. So the capability was recorded working on day N−2 and missing on day N−1, and **the pessimistic note won purely because it was newer**. Cost: two support threads unreported, one a user hitting a compiler bug whose fix PR had sat one approval short for 11 days. ⇒ Re-test any capability gap before carrying it forward (10 seconds), grep your own prior artifacts for a working recipe first, and treat a capability that **downgrades** between two consecutive notes as a claim to re-derive rather than inherit ([test a claimed capability gap before carrying it into a second run](../learnings/1786350645999-test-a-claimed-capability-gap-before-carrying-it-i.md)).

**Two operational shapes from the same discipline.** *Triaging a maintainer's architectural RFC* (slang#12447, lazy deserialization of core-module IR, 5 explicit open design questions + a DRAFT prototype): a triage draft verified every source anchor correctly and still had to be rewritten, because correct verification assembled into the **wrong genre** — sentences like "the assumption looks well-founded" / "so this is not a counterexample" are **verdicts on questions the maintainer reserved for himself**, and codex judged the comment "functions as an architectural mini-review." On an RFC, supply **source anchors, not answers**, and check whether the maintainer already published the number you are about to lecture them about ([triaging a maintainer's architectural RFC: source anchors, not answers](../learnings/1786350576341-triaging-a-maintainer-s-architectural-rfc-source-a.md)). *Splitting the daily PR report for Discord* (~9.5 KB body vs a 2000-char cap): the exit-code contract is load-bearing — run `pr_report.py 2>/dev/null` and branch on `$?` (`10` = report due, post it; `0` = quiet day, do **not** post; anything else = transient, do not post, next fire retries), with stderr excluded because it is per-repo progress chatter and the body starts after the `--- report ---` marker. Chunk at assignee-bullet boundaries (`- **`), knowing one assignee block can itself exceed the budget, and **verify by line-set comparison** rather than eyeballing ([splitting the Slang PR report for Discord: chunk at assignee boundaries and verify by line-set](../learnings/1786338436692-splitting-the-slang-pr-report-for-discord-chunk-at.md)).

**Source learnings (20):**

- [verify a CI run belongs to THIS PR/head (displayTitle + head SHA) before crediting its conclusion; cancelled ≠ pass ≠ stale](../learnings/1784873907730-verify-a-ci-run-belongs-to-this-pr-head-before-cre.md)
- ["answer instantly" = post verified facts at their precision; never extrapolate a new positive claim to sound helpful](../learnings/1784551466296-standing-answer-instantly-post-verified-facts-neve.md)
- [verify-at-head of an enforcing gate: check against COMBINED inputs + prove build-wiring, don't accept the PR body's claim](../learnings/1784568390472-verify-at-head-check-enforce-logic-against-combine.md)
- [two failure modes that pass a naive read of an enforcing gate: combined-input bypass, unproven build-wiring](../learnings/1784568398186-verify-enforcing-gates-against-combined-input-case.md)
- [Revert-drill every regression test — a positive-only CHECK can false-pass](../learnings/1784070069979-revert-drill-every-regression-test-a-positive-only.md)
- [A supervisor dropped-promise nudge can resolve to STAND-DOWN, not re-wake — re-verify assignee first](../learnings/1784033086075-a-supervisor-dropped-promise-nudge-can-resolve-to-.md)
- [DeepWiki can confidently contradict Slang source — verify against local checkout on layout/ABI questions](../learnings/1784022527095-deepwiki-can-confidently-contradict-slang-source-v.md)
- [Re-triage verify: a WIP 'Fixes #N' PR is not evidence the issue is fixed](../learnings/1784132477360-re-triage-verify-a-wip-fixes-n-pr-is-not-evidence-.md)
- [Don't reflexively concede to a maintainer assertion — verify at claim precision](../learnings/1784149362998-don-t-reflexively-concede-to-a-maintainer-assertio.md)
- [Watch-list prose summaries drift from file state; re-read the file before reporting status](../learnings/1784153749839-watch-list-prose-summaries-drift-from-file-state-r.md)
- [Don't infer a causal link the failure log never states — CORRECTION](../learnings/1784154251219-don-t-infer-a-causal-link-the-failure-log-never-st.md)
- [authorize external workaround claims only when verified in the USER's environment, not a convenient proxy (import glsl; JS/wasm E38201)](../learnings/1784627888989-authorize-external-workaround-claims-only-when-ver.md)
- [a relayed authorization can carry a fabricated maintainer attribution into public GitHub artifacts — audit the live bytes, attribute policy to the real authorizer](../learnings/1784656869644-a-relayed-authorization-can-carry-a-fabricated-mai.md)
- [on resume, verify live git/gh state before acting on a replayed instruction; a maintainer's merge/comment supersedes an earlier bot commit — forward-commits-only, never force-push over it](../learnings/1784765800190-on-resume-git-gh-state-beats-replayed-transcript-s.md)
- [a census supplies a denominator; only a differential run supplies a ratio — 'fixes 1 of 4' and '2 of 4' both malformed; measured 3 (slang#12311)](../learnings/1786344197524-a-census-supplies-a-denominator-only-a-differentia.md)
- [a ratio of two totals cannot be applied to a delta — the ~20x bridging factor, and the correction repeated the defect it corrected](../learnings/1786351696739-correcting-a-bridging-factor-a-ratio-of-two-totals.md)
- [a heterogeneous list cannot be validated by sampling — store stems, not full forms ('auto-close' misses 'auto-closing')](../learnings/1786351185283-a-heterogeneous-list-cannot-be-validated-by-sampli.md)
- [test a claimed capability gap before carrying it into a second run — a working Discord token was already on disk; the pessimistic note won by being newer](../learnings/1786350645999-test-a-claimed-capability-gap-before-carrying-it-i.md)
- [triaging a maintainer's architectural RFC: source anchors, not answers — correct verification can assemble into the wrong genre (slang#12447)](../learnings/1786350576341-triaging-a-maintainer-s-architectural-rfc-source-a.md)
- [splitting the Slang PR report for Discord: exit 10 = post / 0 = quiet day, chunk at assignee boundaries, verify by line-set](../learnings/1786338436692-splitting-the-slang-pr-report-for-discord-chunk-at.md)
