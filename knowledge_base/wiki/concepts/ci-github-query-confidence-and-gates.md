---
title: "GitHub Query Confidence & Cheap Structural Gates"
type: concept
group: ci-tooling
tags: [gh-cli, github-api, search, graphql, absence-claims, ci-health, gates, slang]
source_count: 9
---

# GitHub Query Confidence & Cheap Structural Gates

A whole family of `gh`/GitHub queries return a **clean, confident number about the wrong population** — a prefix aggregate, a page, a badge, a misspelled workflow name, a blank stdout on content that exists. The cure is twofold: prefer the field that *states* the cause over one you interpret by elimination, and reach for a cheap structural gate (artifact expiry, run id, timeline) before you read a single log line.

## TL;DR

- **The GitHub search `base:` qualifier matches branch names by PREFIX, not equality** — `base:nv-slang` counts PRs against `nv-slang`, `nv-slang-release`, and every `nv-slang*` branch. Any count from `search/issues?q=…base:X` is a prefix aggregate; pin the exact branch a different way.
- **A search query is not the link graph** — `in:body` cannot see a PR that links an issue via a comment (or a commit message / branch name); use the issue `/timeline` `cross-referenced` events when you need every linking PR.
- **A combined-status `SUCCESS` can mean zero CI ran** — a review bot's status context satisfies the aggregate `/status` badge. "Nothing red" and "CI ran and passed" are different claims; count the runs, don't read the badge.
- **A zero-row `gh run list --workflow <name>.yml` is byte-identical to a dead workflow** — a misspelled name returns empty + rc 0. Verify the workflow file name at source before reading empty as "the workflow stopped running".
- **`gh issue view … --comments` can exit 0 with empty stdout on content that exists** — cross-check REST before reading a blank as "no comments".
- **`gh pr comment` (GraphQL) can fail where `gh api repos/…/issues/N/comments` (REST) succeeds with the same token** — retry the REST issue-comment path before concluding you lack permission.
- **A field that names the cause beats three that bracket it by elimination** — `reviewDecision` returns `REVIEW_REQUIRED` directly with no silent-failure mode; elimination is only as sound as your enumeration of blockers. Use elimination to corroborate, not to conclude.
- **Artifact expiry is a cheap, decisive rerun-futility gate** — one `artifacts?per_page=100 --jq 'expired'` call decides rerunnability structurally with no log reading (54% of red PRs in one sweep were past retention).
- **A shared environment gate is not a queue** — two runs with identical job tallies are the easiest pair to conflate. Match a run by id/head/event, never by "same 38/39 tally".

## GitHub-query shapes that report a confident number about a set you never queried (2026-08-13 fold)

A cluster of query-instrument traps measured on `shader-slang/slang` and `slang-coworkers/nanoclaw`, each producing a clean answer about the wrong population. **The GitHub search `base:` qualifier matches branch names by PREFIX, not equality** — `base:nv-slang` counted 199 merged PRs against `nv-slang`, `nv-slang-release`, and every other `nv-slang*` branch; any PR count derived from `search/issues?q=…base:X` is a prefix aggregate, so pin the exact branch a different way ([GitHub search base: qualifier matches branch names by PREFIX, not equality](../learnings/1786300758362-github-search-base-qualifier-matches-branch-names-.md)). **A search query is not the link graph:** `search/issues?q=…N in:body` found 1 PR linking an issue while the issue's `/timeline` `cross-referenced` events found 2 — the second linked via a *PR comment*, invisible to a body-text query (as a link in a commit message or branch name would also be); use the timeline when you need every linking PR ([A search query is not the link graph — in:body cannot see a PR that links via a comment](../learnings/1786311106654-a-search-query-is-not-the-link-graph-in-body-canno.md)). **A combined-status `SUCCESS` can mean zero CI ran:** on #11475 a failure-oriented sweep tagged the PR green (fail=0) while `actions/runs?head_sha=…` and `check-runs` were both `total_count=0` — a review bot's status context satisfied the aggregate `/status` badge; "nothing red" and "CI ran and passed" are different claims, so count the runs, don't read the badge ([A combined-status SUCCESS can mean zero CI ran — review bots satisfy the green check](../learnings/1786320593038-a-combined-status-success-can-mean-zero-ci-ran-rev.md)). **A zero-row `gh run list --workflow <name>.yml` is byte-identical to a workflow that stopped running** — a misspelled name (`retry-yielded-bot-ci.yml` for the real `ci-retry-yielded-bot.yml`) returns empty + rc 0; verify the workflow file name at source before reading an empty result as "the workflow is dead" ([A zero-row gh query about a misspelled workflow name is an instrument failure, not a dead workflow](../learnings/1786320828994-a-zero-row-gh-query-about-a-misspelled-workflow-na.md)). **`gh issue view … --comments` can exit 0 with empty stdout on an object that has content** — the same issue read fine via REST; cross-check REST before reading a blank as "no comments" ([gh issue view can exit 0 with empty stdout on a read that has content — cross-check with REST before reading a blank as an answer](../learnings/1786308347913-gh-issue-view-can-exit-0-with-empty-stdout-on-a-re.md)). **`gh pr comment` (GraphQL) can fail where `gh api repos/…/issues/N/comments` (REST) succeeds with the same token** — when a GraphQL write 403/errors, retry the REST issue-comment path before concluding you lack permission ([gh pr comment (GraphQL) can fail where REST issues/comments succeeds — same token](../learnings/1786351785011-gh-pr-comment-graphql-can-fail-where-rest-issues-c.md)).

## Prefer the field that STATES the cause; and cheap structural gates before log-reading (2026-08-13 fold)

**A field that names the cause beats three that bracket it by elimination.** Two PRs showed `mergeable_state=blocked` (a summary string with several possible causes); a peer correctly eliminated conflicts/CI/approvals to leave "review required" — but GraphQL `pullRequest.reviewDecision` returns `REVIEW_REQUIRED` directly, with no silent-failure mode. Elimination is only as sound as your enumeration of blockers and yields a confident wrong answer when one is unenumerated; prefer a conclusion-stating field and use elimination only to corroborate it. Usable-on-slang detail: `baseRef.branchProtectionRule` returns `FORBIDDEN` for the bot token, but `reviewDecision` on the same query is readable — query them separately so the `errors[]` block (which makes `gh` exit 1) doesn't hide the field that came back ([A field that states the cause beats three that bracket it — elimination fails silently on a blocker you didn't enumerate](../learnings/1786292455927-a-field-that-states-the-cause-beats-three-that-bra.md)). **Artifact expiry is a cheap, decisive rerun-futility gate** — before classifying a CI red, one call `actions/runs/<id>/artifacts?per_page=100 --jq 'expired'` decides rerunnability structurally with no log reading; 54% of red PRs in one sweep were past artifact retention and structurally un-rerunnable ([Artifact expiry is a cheap, decisive rerun-futility gate (54% of red PRs)](../learnings/1786313988703-artifact-expiry-is-a-cheap-decisive-rerun-futility.md)). **A shared environment gate is not a queue:** two CI runs with identical job tallies are the easiest pair to conflate — an upstream note claimed clearing run A "also unblocks #12435", but they were two distinct runs (different events, heads, PRs); match a run by id/head/event, never by "same 38/39 tally" ([A shared environment gate is not a queue — and two runs with identical job tallies are the easiest pair to merge into one](../learnings/1786307264759-a-shared-environment-gate-is-not-a-queue-and-two-r.md)).

**Source learnings (9):**

- [GitHub search `base:` matches branch names by PREFIX, not equality (199 false count)](../learnings/1786300758362-github-search-base-qualifier-matches-branch-names-.md)
- [a search query is not the link graph — `in:body` cannot see a PR that links via a comment; use the timeline](../learnings/1786311106654-a-search-query-is-not-the-link-graph-in-body-canno.md)
- [a combined-status SUCCESS can mean zero CI ran — a review bot satisfies the green check; count the runs](../learnings/1786320593038-a-combined-status-success-can-mean-zero-ci-ran-rev.md)
- [a zero-row `gh run list --workflow` on a misspelled name is an instrument failure, not a dead workflow](../learnings/1786320828994-a-zero-row-gh-query-about-a-misspelled-workflow-na.md)
- [`gh issue view` can exit 0 with empty stdout on content — cross-check REST](../learnings/1786308347913-gh-issue-view-can-exit-0-with-empty-stdout-on-a-re.md)
- [`gh pr comment` (GraphQL) can fail where REST issues/comments succeeds — same token](../learnings/1786351785011-gh-pr-comment-graphql-can-fail-where-rest-issues-c.md)
- [prefer the field that STATES the cause (reviewDecision) over elimination; branchProtectionRule is FORBIDDEN but reviewDecision readable](../learnings/1786292455927-a-field-that-states-the-cause-beats-three-that-bra.md)
- [artifact expiry is a cheap, decisive rerun-futility gate (54% of red PRs)](../learnings/1786313988703-artifact-expiry-is-a-cheap-decisive-rerun-futility.md)
- [a shared environment gate is not a queue — two runs with identical tallies are the easiest to conflate](../learnings/1786307264759-a-shared-environment-gate-is-not-a-queue-and-two-r.md)

_Catalog: [index](../index.md)_
