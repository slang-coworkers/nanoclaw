---
title: "PR Review Practices (part 9) (part 2)"
type: concept
group: review-process
tags: [pr-review, slang-reviewer, devin, reviewer-a, reviewer-b, reviewer-c, github, draft-pr, convergence, false-positives, a2a-review, fleet-contention, repo-root-isolation, pr-approver, shadow-mode, clause-gap, challenger, critique-gate, abstain-infra, head-pinning, human-calibration, memoization-safety]
source_count: 3
---

# PR Review Practices (part 9) (part 2)

> **This page is part 2 of 2** of the PR Review Practices (part 9) synthesis (split 2026-08-07 to stay under the 40 KB read cap). Siblings: [part 1](review-pr-practices-9.md). The TL;DR below is shared across all parts.

## TL;DR
- **Every reviewer fails toward CLEAN.** Devin exit 0 is not a signal; Reviewer A can emit a 0-byte or budget-capped output; Reviewer C can skip environmentally. **Verify each reviewer produced findings before treating "no findings" as a result** — an absent reviewer and a clean reviewer are byte-identical downstream.
- **Watch the ARTIFACT, not the subagent.** A Devin subagent returns before `devin-fetch.sh` finishes; a detached reviewer's completion is proven by its exit marker, never by streamed JSON. Anchor monitors to exit markers.
- **Read the full page dump, not the summary file.** `devin-flags.md` can have an EMPTY `## Flags` section while `devin-page.txt` from the same run shows real findings — synthesizing from the summary records a false "0 findings" and carries a false-clean prior into the challenger.
- **Reviewer A needs an isolated checkout.** Fleet contention on a shared repo root corrupts runs; `$30` is too low a budget cap and produces silent truncation. Use fresh per-run out-dirs to avoid `/tmp` collisions.
- **Two bots agreeing measures shared priors, not correctness.** Independent bots flagging the same item is weak corroboration; read the human review thread before scoring any structure/maintainability gap.
- **A bot finding a named maintainer already overruled ON THIS PR is not an open gap** — but **lead with the technical grounds**, never the maintainer's preference. Maintainer preference is supporting context; a repo guideline is not cleared by taste alone.
- **A refuted 🔴 does not become WOULD_APPROVE**, and a standing human `CHANGES_REQUESTED` caps the verdict at ABSTAIN. Never round up.
- **A mid-iteration `diff_hash` mismatch is expected under debounce**, not a bug. Pin the head, byte-prove reviews against it, and re-verify a stale secondary reviewer's flags at the current head.
- **Re-review scope: redesign gaps and self-recommended nits do not reset the pipeline.** Additive nit rounds are fine; a full re-run is not.
- **The review signal is endpoint-split across three surfaces** (`pulls/N/reviews`, `pulls/N/comments`, `issues/N/comments`) and the harvest reads one. That split is a property of GitHub's data model, so **every consumer inherits the blind spot independently — fixing one instance is not fixing the class.**
- **A GitHub post gates on the literal marker, not on prose.** Post only in COMMENT state; never APPROVE/REQUEST_CHANGES from a runner.
- **Empirical probes can test the wrong sub-case.** A probe that varies the intended factor while holding the confound constant reads as decisive and proves nothing — name the factor and the confound before citing a control.
- **In an artifact where claims cite `file:line`, the UNCITED SENTENCE IS THE DEFECT** — in a document whose norm is citation, the citation outlier is where an inference got restated as an observation. Run that self-check mechanically over your own draft.
- **Recording is not routing.** Documenting a blocking infra defect any number of times never reaches a file only an operator can edit. The record serves the next agent; the escalation serves the fix — do both.
- **A stale webhook dispatch on an already-decided PR is a no-op plus a report, never a re-decision.** `record_decision` is idempotent per `(repo, pr, sha)`, so re-deciding CLOBBERS a human-joined calibration row with a verdict contaminated by post-hoc information.
- **Read approval state from `latestOpinionatedReviews`, never `latestReviews`.** `latestReviews` is each reviewer's most-recent review *of any kind*, so a later COMMENTED from the same author makes a live APPROVED vanish. When an API offers two similarly-named collections, one is "most recent event" and the other "current state" — pick the one whose *definition* answers your question, and distrust any field whose name describes recency when you need state. Then check the verdict's bound commit: an APPROVED at an older commit is push-invalidated; a DISMISSED at a stale commit is not a dismissal at head.
- **Name the system a status belongs to before asserting the status.** "Approved" is meaningless without "approved *in what*". Internal review pipelines, codex stages, and peer coworker verdicts are real approvals and are **zero GitHub reviews** — a PR can carry eleven internal rounds and still read `reviewCount: 0 / reviewDecision: REVIEW_REQUIRED` to the maintainer whose queue it lands in. The honest split form ("3 internal pipelines approve; 0 GitHub reviews") is longer and strictly stronger, because the compressed form reads as overstated the instant anyone runs the query.
- **Local build + green tests are not delivery.** The heavier the local verification, the more it masquerades as shipping — a 20-minute compile produces a sense of completion a `git push` does not. "Corrected locally but undelivered" and "never corrected" are indistinguishable from outside your container. Discriminate with `git show HEAD:<path> | grep -c` vs `grep -c <path>`, then verify against the **remote object** after pushing.
- **A local grep, a clean `git status`, and a successful checkout all fail toward the answer you want.** Ask what the output would look like if the work had *not* landed; if it looks the same, you have not measured. A checkout of the wrong ref exits 0, and "0 files changed" is the expected output of both "wrong ref" and "already up to date" — so verify content arrived with a grep for a revision-unique marker whose expected count is non-zero.
- **Before merging or rebasing into a branch you authored, fast-forward the remote tip first.** A maintainer with write access can push to a bot branch, and a stale worktree gives no signal: clean status, your commit at HEAD, your branch name. Prove `git merge-base --is-ancestor HEAD origin/<branch>`, `--ff-only` the remote, and only then merge master. Compare the **index** (`git diff --cached origin/master --stat`), not the worktree, so stale submodule pointers don't ride in as accidental reverts.
- **"Nobody touched it in N days" is an inference from the wrong instrument.** `updatedAt` and your local `git log` can agree with each other and both be wrong about who last wrote to the branch. `git log -1 --format='%an' origin/<branch>` can invert "close as superseded" into "engaged maintainer, needs a merge".

## Local Verification Is Not Delivery: the Remote Blob, the Named Ref, and Who Owns Your Branch HEAD (2026-08-07 fold)

Three incidents, one generator: **a measurement made inside your own container says nothing about the artifact anyone else reads.** Each failed at a different layer — the working tree, the ref you fetched into, the branch tip you assumed you owned — and each produced a clean, confident, wrong report after an expensive build.

**The working tree: verify the remote blob, not your checkout.** A session adopted a reviewer's proposed change, rebuilt the compiler, ran the full suite, measured every pre-registered criterion, and reported "adopted — all 8 checks pass." The change was an **uncommitted working-tree edit**; the reviewer fetched the remote and found the PR head still carrying the old form, plus a 404 on a test file reported as passing 2/2. Every measurement was true, and none of it was true of the artifact anyone else could read. This slips past a rule you already hold — *verify the published copy, not your working copy* — because it gets applied to the PR body and issue comments and skipped for the code: **building and testing feels like delivery.** A 20-minute compile plus a green suite produces a sense of completion that `git push` does not, and the heavier the local verification, the more convincingly it masquerades as shipping. The discriminator costs two seconds: `git show HEAD:path | grep -c '<change>'` returning 0 while `grep -c '<change>' path` returns 1 **is the entire bug**. After pushing, verify against the remote object — `git fetch origin <branch>:refs/remotes/origin/verify -f` then `git show refs/remotes/origin/verify:path | grep -c` — and do **not** use `gh api .../contents/<path>` for this on a large file: it returns HTTP success with a zero-length body past its inline size cap, which reads exactly like "the change is absent." Generalized: "corrected locally but undelivered" and "never corrected" are **indistinguishable from outside your container**, the same class as an absent reviewer being byte-identical to a clean one. Corollary for reviewers, and the reason this was caught at all: **keep re-reporting a delivery gap until the remote shows the change** — a gap mentioned once and dropped reads as retracted, and dropping this one would have meant dropping a correct finding [Building and testing feels like delivering — verify the REMOTE blob, because a local grep cannot distinguish "corrected but unpushed" from "never corrected"](../learnings/1786073630147-building-and-testing-feels-like-delivering-verify-.md).

**The ref: `FETCH_HEAD` is mutable, and a checkout of the wrong ref fails silently.** Verifying the head of #12417, a session fetched the PR, then later ran `git checkout FETCH_HEAD -- <files>`. By then `FETCH_HEAD` pointed at **master** — every `git fetch` overwrites it, including one run by a **sibling session sharing the same clone**, which is normal since `/workspace/agent` is per-agent-group, not per-session. The checkout restored master content, **exited clean, and produced zero modified files**; a ~25-minute build then ran against pristine master under the belief it was testing the patch. **A checkout of the wrong ref is not an error: it succeeds, and "0 files changed" is the expected output of both "wrong ref" and "already up to date."** Remedy: fetch into a named ref you own (`git fetch <remote> pull/<N>/head:refs/pr/<N>`), check out *that*, and then **verify the content arrived with a grep for a revision-unique marker whose expected count is non-zero** — here the new loop conjunct and `kCoreModule_MaxVectorElementCount`. That grep is what caught this; nothing else did. Sibling trap from the same session: `pkill -f 'cmake --build --preset release'` **killed the caller's own shell** (exit 144) because `-f` matched the wrapper's own command line — use `pgrep -cx ninja`, and never `-f` a pattern that appears in your own argv [FETCH_HEAD is mutable and a checkout of the wrong ref fails silently](../learnings/1786081698988-fetch-head-is-mutable-and-a-checkout-of-the-wrong-.md).

**The branch tip: a maintainer may own the HEAD of your own `fix/` branch.** Before merging or rebasing into a `fix/issue-<n>` branch you authored, prove your local tip is an ancestor of the remote tip. A maintainer with write access can push to a bot branch, and a stale worktree gives **no signal whatsoever**: `git status` clean, `git log -1` shows *your* commit, the branch name is yours. On #11981 / draft PR #12014 the worktree sat at its own commit `d1141f42d6`, 29 days stale, while the real PR head was `2e8c12db84` — *"Merge branch 'master' into fix/issue-11981"*, **authored by the maintainer on a bot-authored branch**; he had already done the catch-up merge. Merging master into the local HEAD and pushing would have produced a history **omitting the maintainer's merge**, deliverable only by `git push --force` — forbidden on a PR under review — stranding the work at the final step *after* a 20-minute build. The fast-forward-first ordering costs one command: fetch the branch's own refspec explicitly, `git merge-base --is-ancestor HEAD origin/fix/issue-<n>`, `git merge --ff-only origin/fix/issue-<n>` to adopt the real PR head, and only then `git merge origin/master`. Two independently fatal halves live inside this: (1) **`origin/fix/issue-<n>` may not exist locally at all** — the clone's fetch refspec was `+refs/heads/master:refs/remotes/origin/master`, master only, so `git log origin/fix/issue-11981` died with `fatal: ambiguous argument` and 29 days of `git fetch origin master` had silently refreshed *nothing* about the session's own branch; **a plain `git fetch` does not give you your branch's remote state — name the refspec.** (2) **"Nobody touched it in N days" is an inference from the wrong instrument** — a supervisor read 29 days of idleness as *the bot sitting on the PR*, when the same 29 days were *the maintainer having already handled it*; `updatedAt` and the local `git log` agreed with each other and were both wrong about who last wrote to the branch, and one `git log -1 --format='%an' origin/fix/issue-<n>` inverted the recommendation from "close as superseded" to "engaged maintainer, just needs the merge." Companion: **compare the INDEX, not the worktree.** `git diff --cached origin/master --stat` should name only your own files; the same worktree had `external/lz4` `-dirty` and three other submodules (`spirv-headers`, `spirv-tools`, `vulkan`) at wrong commits on disk, so the worktree diff showed 3 submodule pointer changes while the index diff showed only the 4 fix files — compare the index, or stale submodule pointers ride into the merge commit as accidental reverts of someone else's dependency bumps (the same lz4 had 41 tracked files under `build/` deleted in the working tree, breaking cmake configure with `add_subdirectory given source "lz4/build/cmake" which is not an existing directory`; fix is `git checkout -- build` inside the submodule) [A maintainer may own the HEAD of your own fix/ branch — --ff-only the remote before merging master](../learnings/1786075880249-a-maintainer-may-own-the-head-of-your-own-fix-bran.md).

The unifying check across all three, to run before any "done" report: **ask what this output would look like if the thing were absent; if the answer is "the same," it is not a measurement.** A clean `git status`, a zero-exit checkout, a green suite, and a local `grep` hit all satisfy that condition — so each must be paired with a read of the remote object, a revision-unique marker, or an ancestry proof before it is evidence about the PR.

**Source learnings (3):**

- [a maintainer may own the HEAD of your own `fix/` branch: fetch the explicit refspec, `--ff-only` the remote before merging master, and diff the index not the worktree](../learnings/1786075880249-a-maintainer-may-own-the-head-of-your-own-fix-bran.md)
- [`FETCH_HEAD` is overwritten by sibling sessions sharing the clone; fetch into `refs/pr/<N>` and prove content arrived with a non-zero-count grep for a revision-unique marker](../learnings/1786081698988-fetch-head-is-mutable-and-a-checkout-of-the-wrong-.md)
- [a local build + green suite is not delivery: verify the remote blob, because a local grep cannot distinguish "corrected but unpushed" from "never corrected"](../learnings/1786073630147-building-and-testing-feels-like-delivering-verify-.md)
