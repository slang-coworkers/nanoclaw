---
author_agent_group: ag-1783611156430-vvj8oi
author_session: sess-1786614920076-jgix5q
written_at: 2026-08-17T14:17:29.687Z
---

# [approver/human-disagreement] ABSTAIN:OPEN_GAP on test-infra coverage nits was overruled — maintainer approved+merged unchanged at the exact decided head

**Decision vs outcome.** shader-slang/slang#12471 @ e8ffb7782c4d: I decided **ABSTAIN_POLICY:OPEN_GAP** (2026-08-13 10:22Z). Human outcome: **merged at e8ffb7782c4d** (my exact decided head, author jvepsalainen-nv) with a **live APPROVED review from jkiviluoto-nv at that same commit** (14:59Z). **No interval commits** — decided head == final commit == merged head, so my three "open gaps" were merged completely unchanged. Merged ⇒ APPROVED-equivalent; scored against the falsifiable reading ("gaps material enough not to merge as-is"), a clean approve + merge-unchanged REFUTES it ⇒ my abstain was **overruled** (not the tautological "a human looked, so the abstain succeeded" — that framing is retracted).

**The gaps I abstained on:** (1) `_reportExitReason` stderr-visibility (PR's stated motivation) untested; (2) `isSourceEnd()`/`HTTPPacketConnection::update()` EOF-fix positive path untested — core-lib, multi-consumer; (3) `SendFailed`/`ProtocolError` consumer branches unrigged. All three are **test-coverage/clarity nits** — the primary review itself said **0 bugs**, and Devin was clean. None described a plausible false-green or unmitigated false-red in *shipped behavior*; they were "more tests would be nice" on new internal test-tooling.

**Root cause of the miss.** My conservative-lean ("uncertainty ⇒ ABSTAIN; a 🟡 clears only if clearly inconsequential") over-fired on a **test-infrastructure PR authored by a maintainer**. For such PRs, a maintainer's merge bar treats *missing-positive-test* / *untested-non-verdict-branch* gaps as advisory, not blocking — especially when the reviewer found 0 bugs and the change's core behaviors (here: the false-red retry and false-green abort-exit) each already have a driving test. Note the irony: DECISION_REVIEW correctly caught my *over-clear* of gap 2 (a REACH over-claim) and pushed me to ABSTAIN — the right move against shipping a false WOULD_APPROVE — but the human then judged even that gap non-blocking. So the critique gate and I were both "correct" on rigor yet the calibrated answer was closer to approve.

**How to catch it / transferable rule.** When every open gap is a *coverage/clarity* nit (no 🔴; no gap that could hide a wrong pass/fail verdict in shipped behavior) on a **test-tooling or test-infra PR from a trusted author**, weigh that the maintainer merge bar is "does the shipped behavior verify correctly", not "is every new branch positively tested". Distinguish: a coverage gap that could **mask a wrong verdict** (blocking → ABSTAIN) vs. one that is purely **"add a test for completeness"** (advisory → lean toward the reviewer's own APPROVE_WITH_NITS, not abstain). Cross-check the reviewer's own verdict: here it was 🟡 "Has issues" but **0 bugs** — an APPROVE_WITH_NITS shape, which the maintainer treated as mergeable. An abstain is cheap (a human glance), so this is a low-cost miss — but the pattern (abstain-on-nits for maintainer test-infra PRs) recurs and should shift the lean. Also: both gaps that drove my *prior* c9898ad abstain (expected-failure abort false-red; untested SIGKILL/SIGSEGV) were genuinely resolved at this head — so the author WAS responsive to substantive gaps; the residual set was nit-only, which is exactly the set to weigh as advisory.
