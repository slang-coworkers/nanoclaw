---
name: project_9085_glsl_samplecmp_takeover
description: "#9085 GLSL SampleCmpBias/Grad — Copilot draft takeover COMPLETE, held draft, awaiting maintainer ready/merge"
metadata: 
  node_type: memory
  type: project
  originSessionId: d121aed2-2953-4ba3-81f4-b3f8111b7852
---

PR #9085 "Add GLSL support for SampleCmpBias and SampleCmpGrad" (Fixes #9038). Copilot-authored draft on same-repo branch `copilot/add-glsl-samplecmp-support`. Maintainer @jhelferty-nv asked nv-slang-bot to take it over (2026-07-07 webhook, comment 4909016932) — rebase + **finish** the issue, not just resolve conflicts.

slang-fixer drove it end-to-end on thread `gh-issue-shader-slang/slang-9085`: rebased onto master, all 7 maintainer items met, pushed, **held DRAFT** (not flipped ready — gate intact). Full 5-part write-up + test table lives in the PR description (durable artifact; ack comment PATCH-edit 403'd, description supersedes). Label corrected `pr: breaking change` → `pr: non-breaking`.

**Root technical fix:** Copilot draft leaked `GL_EXT_texture_shadow_lod` onto baseline shadow forms (E36104). Fixer redefined the `texture_shadowgrad` capability atom extension-free (`_sm_6_8 | _GLSL_150 | spirv_1_0`) and split master's merged `textureOffset(sampler2DShadow,…,bias=0.0)` overload, confining the ext to genuine 2DArray/CubeArray bias forms only — mirrors #11156's split. Core module compiled clean (no E36104); sample-cmp.slang 3/3 (CHECK_GLSL now live), no-ext regression 1/1, #11156 guard 1/1, texture sweep 19/19. codex CODE_REVIEW + OUTPUT_REVIEW approve.

**✅ 2026-07-27 22:34 — APPROVED @HEAD + CI-GREEN, awaiting maintainer merge (fixer msg 69638).** **jkwak-work APPROVED at HEAD `fae9572a17`** ("Looks good to me") → aggregate **`reviewDecision: APPROVED`** (davli's + jhelferty's earlier reviews dismissed by the intervening static_assert push; this is a genuine re-approval on the CURRENT commit — resolves the "REVIEW_REQUIRED after push" state). **CI green:** the Falcor red was the [[project_12145_gbufferrttexgrads_d3d12_access_violation]] flake (confirmed — `gh run rerun --failed` → test-falcor + Falcor Perf + check-ci all SUCCESS on same commit; was failing identically on unrelated #12204; GLSL-emit-only diff can't reach D3D12). **Only remaining: `mergeStateStatus: BLOCKED` = purely `BEHIND` master** — maintainer-driven "Update branch"/merge (won't dismiss approval; a bot rebase WOULD dismiss + wouldn't clear BEHIND → fixer correctly HOLDS, no push). Follow-up **issue #12244** filed per jkwak (shadow-compare capability-atom naming cleanup — deferred; the `SampleCmpGrad`/`SampleCmpBias`→`texture_shadowgrad` grouping stays as-is for this PR). Label `pr: breaking change` (correct). **Terminal-pending: maintainer merges (drives the BEHIND update).** Fixer reports merge → triager closes #9038 verdict. Do NOT bot-merge/rebase (operator/maintainer-gated). This resumed the long-stalled #9085 → approved+green.

**[historical] CI run 28907294856 cosmetic priority-yield; awaiting maintainer ready/merge (pre-jkwak-2nd-round).**

**⚠️ 2026-07-27 14:23 — 2nd-reviewer round: jkwak-work (invited by jhelferty) left 3 comments; fixer answered all, HOLDING for form+scope before push (fixer msg 69602; head still `db08b8d7b1`, non-draft, author Copilot).** All 3 replies OUTPUT_REVIEW-gated (codex caught + fixer corrected 2 shape-domain accuracy overstatements pre-post):
- **`$p`?** — answered: combined-sampler intrinsic placeholder (`docs/design/stdlib-intrinsics.md`) folding separate `Texture*`+`SamplerComparisonState` operands into GLSL's single combined `sampler*Shadow`; pre-existing mechanism.
- **⚠️ `static_assert` misuse — REAL correctness gap, not just style (fixer conceded):** the `static_assert(Shape.dimensions==3)` sits in the fallback `else`, which only proves "not 1D/2D" — but `__Shape3D` ALSO has dims==3, and the generic only constrains `Shape : __ITextureShape`, so a **3D shadow shape would pass the assert and be mistreated as cube** (latent; safe today ONLY because none is instantiated). **Family-wide pattern the fixer copied from the pre-existing `SampleCmpLevel` dispatch.** Fixer asked jkwak: (1) preferred form (explicit `Shape is __ShapeCube` + canonical unhandled-shape mechanism?), (2) scope (fix pre-existing `SampleCmpLevel` sites too, or follow-up?).
- **`texture_shadowgrad` naming (why not `…lod`)** — answered: pre-existing atom; both `SampleCmpBias`+`SampleCmpGrad` already used it on master (SM6.8 introduced both together); `texture_shadowlod == texture_sm_4_1` (no SM6.8) can't carry SampleCmpBias's SM6.8 floor. Offered follow-up rename rather than folding a cross-cutting rename here.
**✅ 2026-07-27 16:11 — jkwak's review ACTIONED + pushed `fae9572a17` (fixer msg 69606).** jkwak resolved the form+scope by explicitly commenting "remove this line" ×4 → fixer **removed the 4 misused `static_assert` lines** in the `SampleCmpBias` GLSL dispatch (they didn't prove the cube case; `__Shape3D` also dims==3). CODE_REVIEW-approved, 272/272 sweep, confirmed on his 4 threads. **Left the pre-existing `SampleCmpLevel` asserts for a FOLLOW-UP** (scope held tight). Answered his 3 questions on-thread, OUTPUT_REVIEW-gated (codex caught + fixer fixed a real error: `SampleCmpLevel` is HLSL **SM6.7**, not SM4.1).
- **`pr: breaking change` label — jkwak set it, fixer AGREED (correct):** the GLSL-source `texture(…,bias)` compat overloads moved `texture_shadowlod`→`texture_shadowgrad` because they now *apply* the bias (was dropping it on the HLSL path) → a build declaring only `texture_shadowlod` + calling bias fns will now ERROR. Genuine breaking change; label left as-is.
- **State change:** the maintainer-directed push **DISMISSED jhelferty's approval** + davli's newer review auto-dismissed → `reviewDecision: REVIEW_REQUIRED`; full CI build matrix (linux/macos/windows + sanitizer) running. Normal maintainer iteration — approved → re-review-required on jkwak's substantive round, now breaking-labeled. Push was expected/maintainer-directed, NOT a guardrail breach ([[feedback_pushes_not_gated]]).
- **Next:** CI outcome + next review verdict (jkwak/jhelferty) → fixer reports. Quiet hold; no push unless further substantive/human trigger or CI failure. Merge OPERATOR-gated. Follow-up owed: the pre-existing `SampleCmpLevel` static_assert family-wide pattern (fixer to file/note). See [[feedback_changes_requested_read_body]], [[feedback_drafts_only_guardrail]].

**2026-07-27 22:16 — jkwak `texture_shadowlod` follow-up ANSWERED (operator-flagged r3658760994 → Main routed to fixer → answered r3661286307).** jkwak asked why `texture_shadowlod` requires only SM4.1 (not SM6.8) + which HLSL SM4.1 fns map to it. Fixer answered all 3 with verified capdef citations: (1) SM4.1 basis correct — `texture_shadowlod` aliases `texture_sm_4_1` (capdef:2414→2368→1723); (2) stays SM4.1 because raising it would spuriously bump every plain SM4.1 shadow-compare caller — the SM6.8 floor lives on the SEPARATE atom `texture_shadowgrad = _sm_6_8|_GLSL_150|spirv_1_0` (capdef:2423), and the diff jkwak's looking at is exactly the move of the *biased* overloads (HLSL `default:`→`SampleCmpBias`, SM6.8) onto it; (3) `texture_shadowlod` gates plain `SampleCmp`/`SampleCmpLevelZero` (SM4.0/4.1). **SM-tier verified vs source** (MS docs + DXC `hctdb.py`: `SampleCmp`=ps_4_0/4_1, `SampleCmpLevel`=**SM6.7**, `SampleCmpBias`/`Grad`=**SM6.8**); honest caveat included that Slang's `SampleCmpLevel` gating is understated (SM4.1-aliased, should be SM6.7) but **pre-existing/unchanged by this PR**. OUTPUT_REVIEW caught 4 real errors pre-post (over-broad citations, bias-vs-LOD extension-boundary conflation, an imprecise "only touches bias/gradient forms" claim) — all corrected, every file:line re-checked live. No push (question, not change-request).

**⚠️ 2026-07-27 16:39 — BOT-vs-MAINTAINER conflict, fixer correctly deferred to human (fixer msg 69610).** 7th bot review (COMMENT-state) on `fae9572a17`: its 🔵 finding asks to **RE-ADD the `static_assert(Shape.dimensions==3)` lines** for symmetry with `SampleCmpLevel` — the **EXACT OPPOSITE of jkwak-work's explicit "remove this line" ×4** (done in `fae9572a17`). **Fixer deferred to the maintainer: did NOT re-add**, posted a reply on that thread explaining the conflict + why jkwak's call is correct. **Human instruction WINS over bot nit** — correct precedence; held per debounce, no push. Other 2 bot findings = test-hardening nits on already-covered paths (HLSL bias-fix regression assertion; separate-sampler per-form ext isolation) + they note the SM-tightening already captured by the `pr: breaking change` label → holding, add only if jkwak/jhelferty asks. CI matrix from the push still running. This is a clean instance worth remembering: **when a bot review contradicts an explicit maintainer instruction, follow the maintainer + explain the conflict on-thread; don't let a bot nit reintroduce what a human directed out.** Quiet hold continues.
