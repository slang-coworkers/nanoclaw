---
name: project_slangpy_1051_slang_12070_autodiff_runtime_loop_start
description: "slangpy#1051 = slang#12070; bwd_diff loop w/ runtime-non-const start crashes; root cause CONFIRMED; two-repo chain in-flight"
metadata: 
  node_type: memory
  type: project
  originSessionId: a74cbb92-efd6-42e1-9dd2-09c712b6b6bf
---

Cross-project chain. Origin **slangpy#1051** (reporter tekintatar, external): `.bwds()` SIGSEGV when a `[Differentiable]` fn has a `[MaxIters]` loop whose induction START is a runtime value (e.g. `for (int dx=-radius; ...)`). Forward correct; zero-based rewrite (`for i in 0..; int dx=i-radius`) gives correct grads. Root cause upstream in Slang autodiff, NOT SlangPy (SlangPy just emits `[Differentiable] _trampoline` + `bwd_diff(_trampoline)`, no loop handling).

**Upstream = shader-slang/slang#12070** (filed by slang-triager, cross-linked, labeled bug/Autodiff/reproduced). Reproduced in pure Slang @8f0c3515d.

**Root cause CONFIRMED (codex + repro + file:line — corrects the original DeepWiki OOB hypothesis):** reverse-mode reconstruction `applyToInst` (`slang-ir-autodiff-primal-hoist.cpp:1355-1361`) splices the loop's PRIMAL initial-value inst (counterOffset, :1034) into the reverse scope with **no remap and no constant-guard** → dangling cross-scope ref for a runtime offset → SPIR-V ICE `neg(<null>)`, CUDA/HLSL use-before-def → runtime SIGSEGV. Sibling exit-value path guards `!isIntegerConstantValue(counterOffset)` (:1153); reconstruction doesn't — **that asymmetry is the defect**. NOT a checkpoint-index/OOB bug (checkpoints decoupled via synthetic counter).

**Trigger is runtime-non-CONSTANT start, not "negative"** — const `-2` compiles fine. Refines the reporter's "negative" framing. SlangPy docs/xfail test must describe it as "runtime (non-constant) induction start."

**Fix:** A = materialize loop-invariant counterOffset in reverse scope (`Closes #12070`); B = optional diagnostic stopgap. **07-12 ~18:48: first Approach-A implementation went verified RED — repro still fires all 3 backends (vk `neg(<null>)` ICE, cuda/cpu counterOffset used-before-def in reverse scope). Fixer RECOVERABLE, nothing pushed/lost. RED symptom is CONSISTENT with the diagnosis (counterOffset not correctly materialized) → diagnosis likely stands, the fix MECHANISM (correct primal→reverse fetch) needs refinement. slang-triager doing read-only re-diagnosis to de-risk next cycle; recovery owned at its tier, not escalating.** **07-12 ~18:54: converged on refined mechanism — root cause = counterOffset bypasses cross-region legalization; fix = Option B: register the non-const `counterOffset` into `hoistInfo->storeSet` at the L1355 reconstruction site (keep `emitAdd`); storeSet chosen as safe universal default. Key correction: L1154 is const-ONLY, NOT a materialization template (this asymmetry likely caused the first RED). Fixer implementing; won't commit/push until `-cpu` green + vk ICE gone, then [Fix Report]; slang-triager verifies against this analysis.** **07-12 ~19:50: fix DONE (+15/-0 clean, matches Option B, test file written) but UNCOMMITTED — fixer hit 2nd autocompact hard-error, stuck in verify/report phase; NOTHING pushed (no branch/PR). slang-triager recovery = tell fixer to COMMIT LOCALLY FIRST (durable vs churn) → /clear → verify→push→draft PR→[Fix Report]. ESCALATION TRADE pre-flagged to Main: if commit-nudge fails + fixer wedged, fresh-dispatch fallback would LOSE the done +15/-0 work. MAIN STANCE (given in advance): PRESERVE the work — prefer wait/recover over fresh-dispatch UNLESS fixer confirmed dead; a `groups restart` is now EVEN MORE destructive (kills uncommitted worktree) → restart-rejection stands harder. slang-triager owns recovery at its tier; comes back to Main before choosing fresh-dispatch.**

**✅ RESOLVED UPSTREAM BY MAINTAINER — 08-03 (Main-verified live via REST).** saipraveenb25 commented on #1051 (cmt 5172733382): *"Should be fixed by slang#12299. Please re-open if not resolved after next release."* **slang PR#12299 MERGED 2026-08-03T21:28:38Z** (author saipraveenb25, branch `codex/fix-gh-12070-runtime-loop-start`, *"Preserve runtime induction values in reverse differentiation"*) — touches **the SAME file as our fix** (`source/slang/slang-ir-autodiff-primal-hoist.cpp`) + **4** autodiff tests (superset of our 1: scalar-widths, nested-runtime-start, runtime-start, strides). **slang#12070 CLOSED completed 21:28:39Z; slangpy#1051 CLOSED completed 23:14:57Z.**
**⚠️ TWO STALE ARTIFACTS (actioned by Main 08-03):** (1) **our draft slang PR#12072 is STILL OPEN and now SUPERSEDED** by merged #12299 (same file, same root cause) ⇒ must be CLOSED with a pointer to #12299 (not merged — don't try). (2) **slangpy PR#1053 still OPEN draft** — its DEFERRED regression test is now **DUE**: upstream fix landed ⇒ the runtime-start case returns as a **plain PASSING test** (no crashing-subprocess scaffold) once #12299 ships in a released wheel; ⛔**MY "gate on the WHEEL" WAS WRONG — it would have FIRED EARLY and failed for the old reason. The gate is the `SGL_SLANG_VERSION` PIN BUMP** (slangpy bundles Slang as a release **tarball** pinned at `external/CMakeLists.txt:85`, still **`2026.12`**, last bumped 06-30 #1037 = 5 Slang releases behind). `546ad18f` is **25 commits AHEAD of `v2026.14.1`** (newest tag) ⇒ in NO tarball. Chain: merge ✅ → Slang tag containing `546ad18f` ⏳ → slangpy pin bump ⏳ (no open PR does it). **A wheel cut today would still bundle 2026.12.** Gate check: `gh api .../slangpy/contents/external/CMakeLists.txt | base64 -d | grep SGL_SLANG_VERSION` + `compare/<tag>...546ad18f --jq .status` (want `behind`/`identical`, NOT `ahead`). One-command version at slangpy-triager's `/workspace/agent/NOTE-slangpy-1051-gate.md`.
⭐⭐**BEATS WAITING: `ci-latest-slang.yml` builds slangpy `main` against Slang **master** and ALREADY ran #12299 twice GREEN** (`repository_dispatch` runs `30836651948`, `30848628629`), **GPU `Unit Tests (Python)` included, ~13 min** ⇒ the fix is **already validated against slangpy with no tag needed**. ⚠️caveat: reduced Linux+Windows **Release** matrix; Debug/macOS come from nightly. ⭐⭐**GENERALIZES BEYOND THIS ISSUE: ANY "is this upstream Slang fix good against slangpy?" question is answerable TODAY via `ci-latest-slang.yml`'s per-PR `repository_dispatch` runs — no waiting on the tag→pin chain at all.** Reach for this before parking anything on a release gate.
⚠️**slangpy#1070 is CLOSED (07-29, "race condition in profiling") — NOT the open tracker I assumed — and its closing rationale FAILS a timeline check:** the profiler feature (`dac9e0e3`, merged 07-13T17:02Z) is **absent from 5 of the 6 wedged commits**, and merged #1076 only added `doctest::skip()` to two **C++** tests (cannot affect a Python step). **#1073 = the actual fix, still OPEN.** Flagged publicly, left to maintainers (not ours to re-litigate).
⚠️**The hang is UNEXERCISED, NOT FIXED** — 46 runs/14 branches since 07-29 show zero wedges (Python median 1.70min, max 7.00min), **but the signature only ever fired on `dev/slangpy-fixer/1051`, which hasn't run CI since 07-13 and whose file never reached `main`.** ⭐**"no recurrence on a PARKED branch" is not evidence of a fix.** **Structural gap still LIVE: no `timeout-minutes` on `Unit Tests (Python)` + `--maxprocesses=4` intact (#1024 closed unmerged) ⇒ a recurrence again burns 6h SILENTLY.**
✅**Crashing scaffold ALREADY GONE at `0fb3344`** (file = 2 constant-start controls only; SIGSEGV/subprocess machinery + strict-xfail removed before parking) ⇒ re-adding `runtime_start` is **purely additive**.
⛔**"#1053 is saipraveenb25's PR" WAS WRONG — WE author it (`nv-slang-bot[bot]`); **saipraveenb25 is the sole current ASSIGNEE**, and jhelferty-nv was the ACTOR who assigned him (timeline-verified: `actor=jhelferty-nv, assignee=saipraveenb25, 2026-07-17T16:42:30Z`, simultaneously unassigning his own 07-14 self-assignment) — ⚠️**two different roles in one sentence; never compress them, that ambiguity caused a mis-correction downstream**. Pushing is mechanically OURS; the coordination courtesy stands but it is not someone else's PR. Incremental comment: [5172859890](https://github.com/shader-slang/slangpy/pull/1053#issuecomment-5172859890). ⭐**Lesson: a maintainer can fix your issue via a DIFFERENT PR than your draft — always check the named PR, never assume your own PR is the fix path.**
**⭐⭐⭐ OUR FIX WAS INCOMPLETE — AND BOTH "CONFIRMED-CORRECT" REVIEW LENSES MISSED IT** (slang-triager read #12299's diff, +52/-10 vs our +15). Mechanism/file/layer were RIGHT; his is a superset on 2 axes: **(1)** he registers `UseOrPseudoUse(param, counterOffset)` at **POLICY** time (~L535) so the policy may **store OR RECOMPUTE** (remapping via `cloneEnv` in the recompute case) — we forced `storeSet` at the reconstruction site and **would have MISSED a recompute outcome**; **(2)** he moves the **type-cast BEFORE** the factor/offset arithmetic, fixing **narrow induction types** (`int16_t` += raw `int` count) — **a SEPARATE defect we never found**, covered by his `reverse-loop-induction-scalar-widths.slang`. ⇒ **CALIBRATION LESSON: our independent IR-correctness pass concluded "storeSet (not recomputeSet) is the correct set" and "both guards correct & complete — could construct neither false-negative nor false-positive." It was WRONG on precisely that. Both lenses (codex CODE+OUTPUT + IR-correctness) agreed and BOTH were incomplete.** The review answered *"is this storeSet registration correct?"* — it never asked *"is storeSet SUFFICIENT (could policy choose recompute?)"* nor *"are there OTHER defects on this reconstruction path?"* ⇒ **a thorough review of the patch you wrote does not test the patch you DIDN'T write; scope the review to the DEFECT CLASS, not the diff.** (Also: slang-triager filed a correction learning — its earlier shared learning had recommended the weaker storeSet-only fix.)
**[HISTORICAL] MAINTAINER HANDOFF — STAND DOWN (07-14, Main-verified live):** jhelferty-nv assigned **saipraveenb25** (Slang autodiff engineer, has GPU-runner access) to ALL THREE: slangpy#1051, slangpy PR#1053, slang#12070 (verified via `gh issue/pr view` — assignee=saipraveenb25 on each). This **supersedes the pending operator (a)/(b) escalation** (moot — the named maintainer can bisect the #1053 GPU-CI harness hang himself = path (a), or make the scope call). **Both chains → maintainer-owned; coworkers STAND DOWN** (don't drive/nudge/burn CI). slangpy-fixer stays HELD; #1053 stays draft. slang PR#12072 (review-confirmed draft, both lenses) is now saipraveenb25's to merge or supersede — he owns #12070. Follow-up trackers (tripwire re-add post-#12072; #1053 GPU-CI-harness-hang infra bug) still worth existing but maintainer drives. **FILED: GPU-CI-harness-hang → slangpy#1070 (https://github.com/shader-slang/slangpy/issues/1070 — factual, full ruled-out matrix, needs GPU-runner access, cross-links #1053/#1051/slang#12070/#12072, no labels [left for human triager], cross-linked from #1053 blocker note). Tripwire-re-add (plain passing test post-slang#12072) recorded in [Triage Resolution] + #1053 test docstring (passive note, no auto-detect).** Comment 5005440715 addressed saipraveenb25 NOT the bot → no bot-post authorized. Watch-only: re-engage only if maintainer pings the bot or a substantive new inbound lands.

**Chain state (as of 2026-07-12):**
- Slang side: **FIX SHIPPED (draft PR) 07-12 ~19:58.** **PR shader-slang/slang#12072** — OPEN/draft, branch `fix/issue-12070` @ `aafc733f45` (pushed+durable, verified via `git ls-remote`), `Closes #12070` + cross-links slangpy#1051. Fix = Option B verbatim: register non-const block-parented `counterOffset` into `hoistInfo->storeSet` → `ensurePrimalAvailability` legalizes reverse-region use into checkpoint load; constants/module-scope skipped (constant-loop IR byte-identical). +15 pass / +151 test (`tests/autodiff/loop-induction-runtime-start.slang`) = +166/−0. `slang-test tests/autodiff/` 577/577; test RED without fix (wrong grads + `-vk` orphan-global ICE). codex CODE+OUTPUT approve; `report_pr_created` done; CI run 29206829865. **Main-verified against live GitHub.** **07-12 ~20:15: independent IR-correctness review = CONFIRMED-CORRECT (not thin).** slang-triager ran its `ir-correctness-reviewer` subagent against PR-head diff (no `slang-reviewer` on its edges; Main confirmed subagent satisfies the ask, won't double-dispatch its own edge). Traced every claim vs on-disk source, 7/7 correctness Qs, NO SSA/use-def/type-legalization/pass-ordering violation. Key: storeSet (not recomputeSet) correct — bare-param case safe via ensureInstAvailable first-block-param store support (L2194-97), params can't be recomputed; both guards complete (no false-neg/pos — Slang IR has no module-scope RUNTIME values); constant-start → byte-identical IR. 2 cosmetic caveats (redundant `isIntegerConstantValue` fast-path; human-glance CI-green on both `-cpu`+`-vk` lines — buffer-loaded `radius` correctly defeats const-fold). Both lenses agree (codex CODE+OUTPUT + IR-correctness). Main fallback (heavier slang-reviewer 2nd opinion) does NOT fire. Verdict posted COMMENT-state on #12072. **Now awaiting ONLY human maintainer merge + CI green (draft/merge operator/maintainer-gated).** Recovered cleanly from 2 autocompact thrash errors via commit→PUSH→/clear→verify (push-before-verify reorder Main advised = why nothing was lost). — [HISTORY below] slang-fixer (group `ag-1780667166439-vmjrwe`) dispatched approach A, then hit an **autocompact THRASH loop** (~18:08-18:12, self-diagnosed "context refilled to limit within 3 turns, 3× in a row"). NOTHING pushed — local commit `2b2379d2` returns 422 (never landed). Thrash loops don't self-recover → slang-triager issued a scoped `ncl groups restart` (`appr-1783880179261-waqc5s`) with a RESUME-#12070 message — **but this was SUPERSEDED & REJECTED (07-12 ~18:44): the detached background BUILD lives inside the fixer container and survives session churn (autocompact resets only the driving loop, not the build); a `groups restart` would KILL the container + live build = definite work loss. Restart is also not the fallback — a truly-wedged fixer gets a FRESH dispatch (slang-triager owns, no restart needed). Operator told to reject both `appr-...waqc5s` (#12070 restart) and `appr-1783880131927-g38yqo` (stray `groups-restart help` probe). In-place recovery (nudge → resume-from-BUILD_EXIT-marker) is the live path; slang-triager owns recovery at its tier. NO #12070 branch/PR exists yet → thrash cost ZERO shipped work.** [historical intent of the restart message] (read on-disk memo → approach A + stopgap-B-diagnostic + `-cpu` COMPARE_COMPUTE test → ONE branch/PR `Closes #12070`). Only running fixer containers were the two #12070 sessions (`sess-...657771-1a2ft8` canonical + `sess-...030261-ay1kty` escalation), so restart is effectively scoped. **RESTART EXECUTED 07-13 09:22 (`restarted:1`, no rebuild) — but ~15h approval gap meant both #12070 fixer sessions had already self-stopped (last_active 07-12 17:03 / 19:51), and NEITHER woke post-restart.** The queued RESUME-#12070 `on_wake` message did NOT land on the #12070 thread (group-restart-with-message can't reliably target a specific stopped session). Lesson: after a long approval gap, a group restart-with-message is unreliable for resuming a specific stalled chain — the re-hand-off must be an explicit threaded dispatch from the edge owner (triager) on `gh-issue-shader-slang/slang-12070`. 07-13 09:22: asked slang-triager to re-hand-off approach A to fixer. **STALE PREMISE — triager verified live & corrected (msg #68): the fix was ALREADY DONE before the thrash.** Branch `fix/issue-12070 @ aafc733f45` on origin; **draft PR #12072 OPEN + MERGEABLE**, `Closes #12070`; approach A2 = +15 lines at reconstruction site (~L1355: when `counterOffset` non-const AND block-parented, `hoistInfo->storeSet.add(counterOffset)` so `ensurePrimalAvailability` legalizes cross-region use to a checkpoint load; const fast path preserved) + 151-line GPU-free regression test `tests/autodiff/loop-induction-runtime-start.slang` (`-cpu`+`-vk` COMPARE_COMPUTE, analytic derivs). PR created 07-12 **19:58Z**, BEFORE the thrash — thrash burned a session that had already delivered; nothing lost. My restart was unnecessary (harmless — no #12070 work in-flight to disrupt). MY ERROR: judged "nothing pushed" from one stale local SHA `2b2379d2` (422) instead of checking branch-convention + PR timeline → see [[feedback_verify_pushed_state_by_branch_not_sha]].

**[Triage Resolution] #12070 = FIXED (root-cause), draft PR #12072 MERGEABLE.** Review: nv-slang-bot ir-correctness review COMMENTED-state, recorded CONFIRMED-CORRECT, no blocking findings. Tests: local `tests/autodiff/` 577/577; CI draft-gated (build/test SKIPPED until ready-flip). **Next human action: maintainer flips #12072 ready → CI → merge (operator-gated; NOT flipping).** Blocker: none — awaiting maintainer ready/merge. State = HELD pending maintainer promote. Canonical thread `gh-issue-shader-slang/slang-12070`.
- SlangPy side: **NOT TERMINAL — RED/BLOCKED on a CI hang (verified 07-13 ~20:46; corrects the earlier "COMPLETE"/"tripwire armed" claims Main relayed upstream).** #1053 `ci` FAILS on EVERY commit incl. `bc4e5ba`: the "Unit Tests (Python)" GPU step hangs (macOS-aarch64 ~349min→cancelled ~6h timeout; linux/windows x86_64 killed ~128min mid-test). NOT fleet-wide — 11 other open PRs run the identical 12-job matrix (`--maxprocesses=4`) in ~12-17min, zero hangs. #1053 branch is **19 commits behind main**; passing PRs are on current main → stale-base is the leading hypothesis. **Strict-xfail TRIPWIRE DROPPED** (`bc4e5ba`): the deliberately-crashing runtime-start subprocess test wedged CI ~6h×4, so #1053 now ships **only 2 constant-start CONTROL tests + docs workaround** (xlinked slang#12070/#12072); crashing case is meant to return as a plain PASSING test once upstream ships → **LOSS of auto-detection; needs a manual follow-up mechanism (PARKED question for Main).** slangpy-triager steered fixer to REBASE onto current `origin/main` + re-run CI (cheapest stale-base-vs-infra discriminator; `tools/ci.py` edits out-of-scope). If STILL hangs post-rebase → fixer STOPs (no more 6h cycles), reports evidence → triager escalates to Main for operator decision. Main advance stance given. **07-14 ~02:51: STOP HIT — rebase did NOT fix it. Main-verified live: CI run `29283873237` on rebased HEAD `0fb3344` (0-behind `dac9e0e`) ran ~6h (20:48→02:49) = `failure`; GPU lanes (linux/win x86_64, macOS-aarch64) cancelled/failed on timeout, non-GPU linux-aarch64 succeeded. RULED OUT: crashing test (controls-only `bc4e5ba` hung identically), stale base (rebased, still hangs), fleet infra (11 sibling PRs pass same 12-job matrix ~15min), CUDA-OOM (macOS-aarch64=Metal/no-CUDA hangs too). Only 4 GPU lanes run the Python suite (self-hosted `nvrgfx-kernelvm-bridge`); every GPU lane hangs, every passing lane SKIPS Python. Delta vs passing siblings = fixer's ~144-line test file (2 constant-start `.bwds()` controls + docs) — passes locally + `CI=1 -n auto --maxprocesses=4` ~5s, but wedges the full 3218-test GPU suite. UNDIAGNOSABLE from fixer side (no runner access). ESCALATED TO OPERATOR (Main→dashboard) — 2 paths: (a) GPU-runner-access human bisects which full-suite test wedges w/ file present; (b) land #1053 DOCS-ONLY (drop test file), ship #1051 workaround, defer regression test until slang#12072 lands (→ plain passing test, no special handling). Main REC = (b)-now + file tracked follow-up for BOTH the test-return AND the trivial-file-hangs-GPU-CI infra mystery (itself a separate SlangPy infra bug). Fixer HOLDING (won't do b w/o say-so) via triager. Pre-rebase backup: local branch `backup/1051-pre-rebase-bc4e5ba`.** HISTORY (pre-correction): PR #1053 was reported COMPLETE (07-12).** PR **shader-slang/slangpy#1053** (OPEN, non-draft) — `test_diff_loop_runtime_start.py` (strict xfail, crashing bwds subprocess-isolated + numpy oracle + const-neg/zero-start controls) + docs workaround (`autodiff.rst`). Uses `Refs #1051` NOT `Closes` (issue stays open tracking upstream). 3 codex critique stages approved; `report_pr_created` registered; CODEOWNERS auto-assigned bmillsNV. Tests 4 pass/2 xfail exit 0. Strict xfail XPASS→fail when upstream lands, forcing marker removal. **Merge = human-gated (bmillsNV); NOT closed by #1053.** NOTE: opened non-draft = real drafts-only BREACH (born non-draft by bot, no maintainer flip — verified via timeline, no `ready_for_review` event). NO content carve-out for test+docs-only PRs. slangpy-triager directed slangpy-fixer to revert via `gh pr ready --undo` — **RESOLVED: `isDraft=true` verified live (convert_to_draft by bot @17:17:02Z, reviewer bmillsNV intact, footprint untouched); fixer recorded drafts-only rule to its memory so future PRs open `--draft`.** Merge strictly maintainer/operator-gated (bmillsNV CODEOWNERS) — never bot self-merge. (Caveat: non-draft open already fired the CODEOWNERS review-request to bmillsNV @17:03:39Z; revert restores guardrail but can't recall that notification.) **07-12: `pr_ready_for_review` webhook (opened) routed to slangpy-pr-approver → verdict WOULD_APPROVE @ `2b2379d276bc` (shadow mode, nothing posted to GitHub).** All 6 eligibility clauses pass (policy v0-shadow-relaxed); production review skipped bot-authored branch (harvest exit 20, documented skip not infra gap), Devin ran head-current 0 bugs/0 flags; challenger clean (test-only+docs, zero product code; strict-xfail well-mitigates masking). Devin confirmed via `SLANGPY_PRINT_GENERATED_SHADERS` SlangPy hands body verbatim to `bwd_diff` — corroborates "pin, don't fix" scope. Caveat noted: unit-test CI jobs pending at read (policy doesn't require CI green; Devin ran suite locally 4 pass/2 xfail, stable under -n2). Human still merges; row joins eventual human/merge outcome for calibration.
- Issue comment on slangpy#1051: [#issuecomment-4951890945](https://github.com/shader-slang/slangpy/issues/1051#issuecomment-4951890945) (slangpy-triager owns, edit-in-place).

**07-22 ~17:45 — slangpy#1070 maintainer ping (re-engaged watch-only).** jhelferty-nv pinged @nv-slang-bot on #1070 (cmt 5049385158): "more failures like this since? triage / any updated info?". Real human @-mention → GitHub-post-authorized; routed to slangpy-triager on thread `gh-issue-shader-slang/slangpy-1070` (issue, not PR). NOTE: the preceding `issue_opened` on #1070 was our OWN bot filing echoed back (correctly no-op'd per [[project_bot_comment_webhook_echo]]); THIS `pr_mention` is a distinct human inbound. Triager [Triage Resolution] (cmt [5049477701](https://github.com/shader-slang/slangpy/issues/1070#issuecomment-5049477701)):
  - **No recurrence** of the GPU "Unit Tests (Python)" 6h-wedge signature — read-only sweep ~60 `ci` runs 07-14→07-22, zero beyond original run 29283873237. Caveat surfaced to maintainer: #1053 branch hasn't re-run CI since 07-13 (parked draft) → "trigger not re-exercised" ≠ "hazard gone."
  - Flagged one UNRELATED in-window 6h timeout: run 29460256843, branch `dev/slangpy-fixer/1067`, 07-16, wedged on **Unit Tests (C++)** (different step) — possible distinct latent hazard, NOT a #1070 recurrence.
  - **Corrects dispatch premise:** slang#12072 still **OPEN DRAFT**, slang#12070 still **OPEN** (saipraveenb25). SlangPy still pins **Slang 2026.12** (unchanged since 06-30) → upstream fix definitively NOT in pinned Slang; nothing on resolution path changed since filing.
  - **Open offer (awaiting maintainer go/no-go):** small upstream-independent CI-hardening PR — step-level `timeout-minutes` (~30-45m) on the Python test step + `pytest-timeout` for per-test attribution; verified still absent on live `main` (ci.yml:218-220 no `timeout-minutes`, no `pytest-timeout` dep). Chain HOLDING for maintainer's answer. Triager memo `/workspace/agent/memory/triage-1070.md` (triager-local).

**07-22 ~later — #1070 CLOSED by maintainer.** jhelferty-nv (cmt [5121599085](https://github.com/shader-slang/slangpy/issues/1070#issuecomment-5121599085)): "From talking with Jay, it sounds like this is the race condition in profiling. We have a fix in the review awaiting @skallweitNV's review. Closing for now." → GPU "Unit Tests (Python)" 6h-wedge root-caused to a **profiling race** (NOT the autodiff bug), fix in review under @skallweitNV; #1070 closed. LIKELY = our [[project_slangpy_1072_profiler_drain_snapshot_race]] (profiler `drain()` snapshot-race, PR #1073) — but NOT confirmed as fact: my record had #1073 as *draft*/gated and jhelferty says "in review awaiting skallweitNV" (draft≠in-review) → #1073 may be flipped ready or it's a different profiler PR; slang​py-triager tasked to verify the exact PR before equating. CI-hardening offer = **RETIRED/superseded** (maintainer closed with a real fix in flight; no @nv-slang-bot mention + issue closed → no GitHub post). #1070 sub-chain TERMINAL pending triager's verified [Triage Resolution]. Residual watch (non-blocking): 07-16 C++-step 6h timeout on `dev/slangpy-fixer/1067` is a possibly-distinct hazard the profiler fix may not cover.

**07-22 phantom-build system-notification (ignored, not-actionable-by-me):** a system-notification claimed my container restarted mid-`ninja` build for slang#9038/PR#9085 and told me to resume the build. I am Main (orchestrator) — never run ninja builds, no #9038/#9085 chain/memory/destinations. Misrouted/injected; did NOT restart any build or fabricate a working tree. Per [[feedback_never_fabricate_events_between_turns]].

**Threads:** `gh-issue-shader-slang/slangpy-1051` (slangpy chain), `gh-issue-shader-slang/slangpy-1051/slang-escalation` (original slang chain). **07-12: fresh `issue_opened` webhook on #12070 arrived → host-stamped canonical thread `gh-issue-shader-slang/slang-12070`.** Routed to slang-triager, which re-verified from scratch (msgs #4/#6, all 3 variants @8f0c3515d). Triager had already forwarded to fixer on canonical thread before my dedup flag → took RECONCILE path: sent slang-fixer explicit DEDUP telling it this is the SAME work as the queued escalation-chain approach-A dispatch → converge to ONE branch/PR `Closes #12070`, report on canonical thread. Verdict posted as **cmt 4952016540**. **Priority = P2** (real crash + correctness impact but clean zero-based workaround + NO regression evidence; earlier escalation-session P1 was unbacked — I confirmed P2 stands). Canonical thread for all #12070 work = `gh-issue-shader-slang/slang-12070`. Awaiting single [Fix Report] from slang-fixer → triager forwards [Triage Resolution] to me.
