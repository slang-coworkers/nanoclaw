---
name: project_11568_descriptor_heap_direct_index
metadata: 
  node_type: memory
  type: project
  originSessionId: 5324107a-53f8-4242-9a16-17fbfc0a8f64
---

jkwak-work asked the bot (webhook, 2026-06-27) to base a PR on #11723 (which closed #11718). Triager correctly **redirected** — #11723 is backend SPIR-V stride only (emit-spirv/options/diagnostics/slang.h enum/docs), zero shared surface with #11568's front-end need. jkwak accepted the redirect (cmt 4819867103, "proceed with what Yong suggested").

Real blocker: generic `__subscript(uint) -> DescriptorHandle<T>` is uninferable (E39999) — OverloadResolveContext has no expectedType (slang-check-overload.cpp:3082). Implemented **csyonghe's settled design**: new builtins `UntypedResourceHandle`/`UntypedSamplerHandle` returned non-generically + implicit conversions reusing `kIROp_CastDescriptorHandleToResource`. Scope MEDIUM.

Draft PR **#11798** `Closes #11568`. 3 bot review rounds clean (REQUEST_CHANGES → APPROVE_WITH_NITS → APPROVE, 0 correctness bugs); codex caught+fixed a real Metal guard-bypass on Family-1 texture conversion (added `[require(glsl_hlsl_spirv_wgsl, descriptor_handle)]` on all 4 conversions). Tests 60/60 (HLSL/SPIR-V/GLSL/WGSL positive; E30019 heap-family-mismatch; E36107 Metal guard). Rebased onto master 33f9ed0cea (head 09ad277d1a) 2026-07-08, now MERGEABLE (was CONFLICTING); conflict resolutions append-only (stable-names 891–896, k_maxSupportedModuleVersion→25), feature diff unchanged.

**MAINTAINER REVIEW 2026-07-08:** jkwak ran a 9-comment review round (7 comment-trim in hlsl.meta.slang removing ad-hoc "Family-1/2" terminology — code byte-unchanged; 2 test-strengthening: `-target dxil` DXC-compile + GLSL via glslang `-emit-spirv-via-glsl`), then **"Looks good to me" + resolved all 9 threads**. Fixer fast-forwarded onto jkwak's own merge commit `1adba4cf8c` (did NOT force over it — merge preserved), head `5f4a206568`.

⚠️ **FIXER HARD-DOWN 2026-07-09 22:54Z (blocker, operator-owned).** Fixer session `sess-1781219496490-v720ku` (group ag-1780667166439-vmjrwe) died on autocompact thrash (~890K-token context, 3× refill). **BUT csyonghe's invariant edits are DONE on disk in wt-slang-11568** (seq 255 @ 18:51Z: lowerUntypedResourceHandleToUInt pass + 3 emit/layout asserts + AST comment + -O0 test; build was started). Thrash trigger = jkwak's 22:28Z "address Yong's comments" + thread-resolve webhooks refilling an already-huge context. Remaining work = verify-build → push → reply to csyonghe/jkwak threads (comment_ids: csyonghe r3553920251 + r3553924763; jkwak issue-cmt 4930113464). Restart ≠ fix (resume reloads bloated transcript → re-thrash); needs operator `/clear` on that session OR re-dispatch to a fresh session. ⚠️ Do NOT `ncl groups restart --id <fixer-group>` — group-scoped, would kill ~6 live sibling fixer sessions (#7406 awaiting-merge, #12032, #12004, #11954, #12029). NOTE: an accidental `restart help` misparse (07-10 01:30Z) bounced an orchestrator-group session (sess-1781218996321-ahx971), NOT the fixer — fixer still down. Escalated to operator.

**Design pivoted 2026-07-09 — park RELEASED on csyonghe directive.** The jkwak↔csyonghe discussion produced a concrete outcome: csyonghe (design author) gave an **invariant directive** — the untyped descriptor-heap handle must NEVER reach emit; lower it to `uint` in a legalization pass, and emit/layout sites become internal errors. This is a design shift from the original "handle rides existing DescriptorHandle<T> lowering through to emit" approach. Fixer is executing it (PR-owner webhook): new `lowerUntypedResourceHandleToUInt` pass + 3 emit/layout asserts + AST comment + -O0 regression test; rebuilding on branch fix/issue-11568 (worktree wt-slang-11568). Legitimate re-activation — a design-pivot directive is exactly the concrete signal the park was waiting for ([[feedback_reopen_not_release_parked_feature]]). Head still `5f4a206568` until this pass lands.

**Remaining gates (all external):** (1) jkwak↔csyonghe design discussion outcome; (2) `pull_request` CI green on `5f4a206568` — earlier run's 2 red jobs were GPU-`createComputePipeline` infra flake (text suite 5463/5463; master flips same jobs), fixer retried; (3) human merge. Ready-flip/merge stays operator-gated; fixer does not merge. Fixer idle + webhook-driven; PR follow-up routes to slang-fixer as PR owner. Issue verdict at cmt 4819877983.

Fleet-disk (which had blocked local compile-verify) is RESOLVED per [[project_fleet_disk_capacity_wall_11969]] — CI remained the build authority throughout regardless.

**STILL STALLED / OPERATOR-FLAGGED 2026-07-10 21:01Z — jkwak's "address Yong's comments" unanswered ~22.5h; re-driving via triager (Main-verified at HEAD).** Operator: "not getting response to my comment" → jkwak-work issue-cmt **4930113464** (07-09 22:28Z) "@nv-slang-bot, can you address the comments from Yong above?" Confirmed at HEAD: PR **head still `5f4a206568`** (07-08 21:08) — **the invariant work (`lowerUntypedResourceHandleToUInt` pass + 3 emit/layout asserts) my note said was done-on-disk in wt-slang-11568 NEVER PUSHED**; last bot comment 07-08 01:04; **no fixer session on the 11568 thread** (`ncl sessions list`). So the 22:54Z autocompact-thrash death was terminal — no restart since revived this chain, and the local seq-255 work is stranded unpushed. **Yong's 2 inline asks (both `pull_request` review COMMENTED 07-09 18:30-18:31), the exact content to address:** (1) `slang-emit-c-like.cpp:2587` — "invariant that UntypedResourceHandleType never survives to emit; explicitly lower to `uint` in legalization, not visible to emit"; (2) `slang-emit-spirv.cpp:2712` — "seeing kIROp_UntypedResourceHandleType here should be an INTERNAL ERROR, type should've been lowered by now." This is EXACTLY the `lowerUntypedResourceHandleToUInt` pass already written-not-pushed → recovery = get that work pushed + reply to both threads. **Re-dispatching via triager (owns fixer edge; NOT a group restart — would kill live sibling sessions #7406/#12032/#12004/#11954/#12029 per prior entry).** Fresh fixer session must: recover/rebuild the seq-255 invariant work from wt-slang-11568 (or redo if lost), verify build, push to fix/issue-11568, reply to csyonghe's 2 inline threads + jkwak's 4930113464 confirming the lower-to-uint invariant landed. Ready-flip/merge stay gated. Escalate to Main for targeted fresh-fixer wake if triager dispatch doesn't engage (same ladder as [[project_8125_empty_struct_cuda_infllight]] #10788 — targeted wake, not group restart).

**Triager dispatched fresh fixer 2026-07-10 21:06Z (msg 24614).** Two notes: (1) dispatched on append-only sub-thread `gh-issue-shader-slang/slang-11568/recovery` — NOT the bare canonical thread, because the dead thrashed session is still routing-active there and a dispatch there would re-wake/re-thrash it; the sub-thread mints a genuinely fresh session (correct application of the append-only canonical-thread rule to dodge the re-thrash trap). (2) **PR flipped MERGEABLE→CONFLICTING** since my 21:01Z read (master moved) — fixer must rebase onto origin/master before pushing; triager briefed it. Fixer tasked (as #11798 owner) to recover/rebuild the pass, rebase, verify, push, reply to both Yong inline threads + jkwak 4930113464; ready/merge gated; context-size watch. Awaiting "head past `5f4a206568` + replies posted" or triager's escalation-ping for the targeted wake.

**recovery-1 DIED (turn-1 infra "Internal server error", 0 work); Main did targeted wake → recovery-2 on `gh-issue-shader-slang/slang-11568/recovery-2` (sess-1783718449163-ndevs2, ~21:20Z, reports to MAIN not triager). API confirmed healthy (sibling fixer sessions 12056/12047/12031 running) — recovery-1 was a transient one-turn blip. recovery-2 emitted an ~865K compaction notice at 21:33Z WITHOUT pushing (head still `5f4a206568`, CONFLICTING) — working but churning; this chain has now died TWICE from autocompact thrash so watch closely, but 1 compaction 13min into rebase+rebuild = still productive range, do NOT interrupt.** ⚠️ **TOOLING GAP: `mcp__nanoclaw__schedule_task` is UNAVAILABLE this session** (errored "No such tool available" — likely dropped in the 06:2xZ instruction-update/restart; `list_tasks`/`update_task` still work). So I could NOT set my usual deterministic self-check. Fallback: handed a read-only GitHub-state watch to the triager (ping Main if head doesn't move past `5f4a206568` in ~40min OR session dies). If schedule_task returns, prefer it. Escalation on 3rd death = targeted wake on `.../recovery-3`, and tell operator the thrash-durability pattern needs host attention. NEVER group-restart the fixer (live siblings).

**2nd compaction 21:49Z (16min after the 1st), STILL head `5f4a206568`/CONFLICTING, no checkpoint pushed.** Session `sess-...ndevs2` still `running` (last_active frozen at 21:20 creation, but last_active LAGS — the compaction notices themselves prove active processing). **DECISION: do NOT wake recovery-3 yet.** Two 870K compactions/no-push is AMBIGUOUS — either (a) thrash-death (this chain's 2× prior pattern) OR (b) a legitimate long rebase+recover-pass+build+verify that front-loads ~20min before any push. Can't distinguish from outside yet. Waking a 3rd session while ndevs2 is genuinely running = co-driver/clobber trap ([[feedback_no_double_dispatch_peer_wired]], [[project_stacked_pr_shared_base_clobber]]). **Deferring to triager's armed watcher `bwz3elv08` (polls branch head every 90s, fires at 22:10Z deadline if head still `5f4a206568`).** By 22:10Z (~50min post-dispatch) "no push" is an unambiguous death signal, not a long build → THEN recovery-3 wake. Watcher is the decision instrument; don't pre-empt it.

**AMBIGUITY RESOLVED — recovery-2 is ALIVE + productively working (21:51Z substantive [Status] via `in_reply_to=2`, NOT thrash-death).** The 2× 870K compactions were case (b) legitimate long build, vindicating the don't-pre-empt decision. Progress: seq-255 `lowerUntypedResourceHandleToUInt` pass + emit/layout internal-error asserts RECOVERED + hardened; **codex CODE_REVIEW approve (r2 — r1 caught 4 cast-ops still passing through at 3 emit sites, now all `SLANG_UNEXPECTED` = exactly Yong's "not visible to emit logic")**; debug rebuild running (~480/1104 objs, DXC-from-source after CMake reconfigure — explains the long front-load, no push yet); head still `5f4a206568`; draft stays draft. Build-waiter `bnx78a580` armed; fixer ended turn to avoid idle churn; will report when head past `5f4a` + replies posted. ⚠️ **RECALIBRATED the triager's watcher `bwz3elv08`:** its "head still `5f4a` at 22:10Z → escalate for recovery-3" trigger is now STALE — a long DXC-from-source build may not have pushed by 22:10Z, so "no push" is NO LONGER a death signal (fixer confirmed alive). Told triager: keep SUCCESS detection (head moves → report), but DON'T fire the death-escalation at 22:10Z on head-unchanged-alone — only escalate if the session actually dies/errors. This closes an info gap the watcher didn't have (fixer reported liveness to Main, not triager) — prevents a false-positive recovery-3 wake that would clobber the live ndevs2 session.

**PUSHED 2026-07-10 22:12:44Z — invariant landed (Main-verified at HEAD).** Head `5f4a206568`→**`4465500b1e`** "Lower untyped descriptor-heap handles to uint before emit (#11568)" (+ rebase merge `f5f476c4b2` of origin/master); PR #11798 back to **MERGEABLE**, non-draft, reviewDecision=REVIEW_REQUIRED (untouched — bot did not flip). Commit title = exactly Yong's invariant. recovery-2 session `ndevs2` still `running`. ⚠️ **REPLIES GAP:** as of ~22:15Z, **0 bot replies** posted to Yong's 2 inline threads (emit-c-like.cpp:2587, emit-spirv.cpp:2712) OR jkwak's ask 4930113464 — the maintainer-watched "address Yong's comments" is still visibly unanswered even though the code landed. Push was ~2min ago and the fixer's stated sequence is push→reply, so it's likely mid-posting; do NOT jump in (fixer owns PR surface, mid-sequence — [[feedback_let_fixer_own_single_session]]). BUT this post-push/pre-comms spot is exactly where this chain died before, so NOT unwatched: armed triager read-only reply-watcher + will confirm fixer posts. If the fixer stalls on replies specifically (session dies before posting), the reply is the deliverable jkwak asked for — I post it (closest-to-the-state fallback since I own recovery-2) or targeted-wake. Triager holding issue-footprint refresh ("invariant landed 4465500b1e, in review") until replies confirmed.

**RECOVERY COMPLETE 2026-07-10 22:15Z — replies posted, chain fully recovered (Main-verified at HEAD).** All 3 bot replies exist + bot-authored: Yong's c-like inline thread (`discussion_r3562277256`, anchored `slang-emit-c-like.cpp:2583` — line drifted from :2587 via rebase, same thread), Yong's spirv inline thread (`discussion_r3562277322`, `:2712`), jkwak's ask (`issuecomment-4939959350`). So the maintainer-watched "address Yong's comments" gap is CLOSED. [Fix Report] summary: 9 files +164/−35, new unconditional `lowerUntypedResourceHandleToUInt` pass in `linkAndOptimizeIR` (after simplification, before emit/layout — covers the `-O0` path peephole misses; forwards the 4 Cast{UInt↔Untyped*Handle} ops to uint operand, rewrites the 2 nullary handle types), HLSL+SPIR-V+IR-layout sites now `SLANG_UNEXPECTED` on a survivor, new `-O0` regression `desc-heap-direct-index-o0.slang`. BUILD_EXIT=0 (1104 targets), -O0 2/2, descriptor-handle suite 64/64, pre-emit IR dump 0 untyped-handle refs, codex PLAN/CODE/OUTPUT approve. Non-draft = jkwak's 22:09 flip (maintainer, verified, NOT a breach); bot did not touch state. `workflow_dispatch` run 29127121064 red = benign priority-yield (builds skipped); real `pull_request` CI 29127083745 queued/green-on-label-checks. **Net: recovery-2 delivered end-to-end after recovery-1's infra death + 2 prior thrash deaths — 3 sessions total to land it. PR #11798 now non-draft/MERGEABLE/REVIEW_REQUIRED, awaiting Yong/jkwak re-review → merge (maintainer/operator-gated; bot does NOT merge/flip).** Triager can now refresh issue footprint to "invariant landed `4465500b1e`, replies posted, in review." Chain webhook-driven; terminal states = merge (→ reap worktree) or a re-review comment.

**✅ SHIPPED — MERGED & CLOSED 2026-07-11 00:43:33Z (Main-verified at HEAD).** jkwak-work **squash-merged PR #11798** (merge commit `84792eb15f`; squash → branch commit `7cb7fea5d2` is NOT an ancestor of the merge). Issue #11568 CLOSED/COMPLETED. Confirmed in merged master: stable-names 892–897 (Type.UntypedResourceHandle/SamplerHandle + 4 casts) present; the `lowerUntypedResourceHandleToUInt` pass gated behind `RequiredLoweringPassSet.untypedResourceHandle`. Merge maintainer-taken (jkwak), bot flipped/merged nothing. Fixer reaped worktree+sentinel. csyonghe's two invariants satisfied; 7 review threads left unresolved (human-originated, bot replied last, human closes per policy) — jkwak merged in that state. **The 3-session recovery (thrash death → infra-error death → recovery-2 landed) delivered end-to-end; operator's flagged unanswered-comment is fully resolved + shipped.**

**⚠️ OPEN ITEM — version-bump gap in MERGED master (operator decision surfaced 2026-07-11 ~01:05Z).** slangbot IR-version-check flagged: the 6 new serialized IR ops shipped while **`k_maxSupportedModuleVersion` stayed 25** (slang-ir.h:2261; in-code comment line 2254 mandates bumping MAX when ops are ADDED). The fixer's 25→26 bump was **uncommitted when the squash-merge landed → did NOT ship**, and can't be delivered via the now-merged branch. Main assessment: this is a **real forward-compat gap, not cosmetic** — `m_version = k_maxSupportedModuleVersion` (line 2292) stamps serialized modules, so a module using ops 892–897 is stamped v25 identical to one without them; a v25 slangc loading such a module won't correctly reject it as too-new → deserialize-unknown-op hazard. BUT: **jkwak merged with the advisory VISIBLE on the PR** (may be intentional / may self-fix — jkwak self-fixes, see [[project_11806_cmake_options_maintainer_selffix]]), and a bot follow-up PR bumping the shared counter risks the [[feedback... merge-hazard]] collapse if another in-flight PR also bumps. Fixer defaults to NO unsolicited follow-up PR (correct). **Surfaced to operator: authorize the 1-char 25→26 follow-up PR (fresh off master), OR check with jkwak first (he saw the advisory), OR leave it.** Awaiting operator pick; do NOT open the follow-up PR without it.

**RE-VERIFIED 2026-07-12 ~18:30Z (~41h post-merge): gap NOT self-resolved.** master `slang-ir.h:2261` still `k_maxSupportedModuleVersion = 25`; NO post-merge comment on #11798 re: the version bump; no PR touching the counter; jkwak did NOT self-fix. So the forward-compat hazard stands unaddressed in shipped master: a `.slang-module` using ops 892–897 stamps `m_version=25`, indistinguishable from a module without them, so a v25-max slangc accepts it as "not too new" but lacks the deserializers. My 07-11 operator ask sat ~41h with no reply. Re-surfaced ONCE (non-pushy) to operator 07-12 ~18:30Z with a firm recommendation (option 1: authorize the 1-char 25→26 follow-up PR, since jkwak clearly isn't self-fixing and no competing counter-bump is in flight → the #11541 collision risk that motivated option 2 is now low). Still will NOT open the PR without the go. If this re-surface also goes unanswered, escalate no further by re-ping — hold and note it's operator-declined-by-silence per [[feedback_ng_deferred_asks]].
