---
name: project_12051_descriptor_reuse_pinning
description: "#12051 re-use loaded descriptors — jkwak picked (a); mechanism is SPIR-V EMIT not IR dup; awaiting his go to prototype draft"
metadata: 
  node_type: memory
  type: project
  originSessionId: a4546982-e3c1-438d-9b81-9756edcca56a
---

**#12051 "Add a way to re-use loaded descriptors multiple times"** (shader-slang/slang, author maxime-modulopi). Feature request: `DescriptorHandle<T> → T` re-materializes at every use → redundant descriptor reload in hot loops (reporter saw 2.5% SSR/HiZ tracing win by pinning via `OpCopyObject` spirv_asm workaround).

**REPORTER ENDORSES A (07-15, maxime-modulopi issuecomment-4978892563):** *"I agree that option A seems to be the best fit, and is already how it works when targetting HLSL anyway."* Pure concurrence — no new design point, does NOT re-open. Triager posted brief author-facing thanks (issuecomment-4978920913) noting A live in draft PR #12111.

**⚠️ REPORTER SURFACES REAL LIMITATION — A doesn't deliver its texture benefit in the [noinline] case (07-15, maxime-modulopi issuecomment-4982202323):** *"So if the workaround is still present, then option A ... supposed to avoid loading the texture descriptor 3 times when sampling a `Texture2D` instead of a `Texture2D.Handle` doesn't work in this code snippet."* He's connecting the benign verdict to A's PURPOSE: because #12027 passes the image as a per-call bindless INDEX (loaded callee-side), the texture descriptor is STILL loaded 3× (once per `[noinline]` invocation) — precisely what A was meant to stop. #12111 coalesced only the SAMPLER (not the primary concern); the TEXTURE (A's reason for existing) is untouched in this shape. **NOT a regression — a genuine SCOPE LIMITATION: A's benefit and the #12027 image-as-index workaround are in direct TENSION across a function boundary.** A coalesces shared CALLER-side loads; the image workaround forces a per-call callee-side load through an index param that A can't reach. Reporter is CORRECT (his 3rd correct catch). Routed to triager: VERIFY the texture is loaded 3× dynamically on #12111 in his snippet (static 1 OpLoad in callee body × 3 OpFunctionCall = 3 dynamic), answer HONESTLY (A helps only when the texture load is caller-side+shared, i.e. the no-function-boundary loop case; covering the cross-boundary image case needs inlining OR lifting the #12027 workaround — driver-dependent, ties to jkwak's earlier "#11798"/"drivers in a better place" #12027 note). **This is a MAINTAINER DESIGN POINT on a PR under csyonghe/jkwak review → surface on GitHub for jkwak AND report up** (bears on what #12111 claims to deliver + possible follow-up scope). Do NOT spin as "A fixes his case" — that framing was the mislabel; A does NOT reduce texture loads here. Canonical thread.

**✅ RESOLVED → FILED FOLLOW-UP #12120 (07-15, triager; honest GitHub answer cmt 4982255036, credited reporter — his 3rd correct catch).** Verified (master + #12111 IDENTICAL for image): callee `%sample` has 1 STATIC image OpLoad (%41 via param0 uint index) × 3 OpFunctionCall → image descriptor loaded **3× dynamically**, ZERO caller-side image load. So the TEXTURE is still loaded 3× — exactly what A was meant to prevent; #12111 only coalesced the SAMPLER. Reporter right. **NOT a patch bug** — #12111 correctly does caller-side coalescing; the limitation is inherent to boundary + workaround interaction (A runs in SPIR-V legalize, after front-end decides image-as-index; A structurally can't reach a per-call callee-side load through a uint index param). A helps NO-BOUNDARY loop (original repro ✅) but NOT `[noinline]`+image-index shape ❌. **Follow-up #12120** "Descriptor-load reuse doesn't cross a [noinline] boundary when images are passed as a bindless index" — cc jkwak+csyonghe, cross-links #12051/#12111/#12027; two maintainer-call directions: (1) inline/hoist past boundary, or (2) revisit image-as-index workaround per jkwak's "drivers may be in a better place now" #12027 note (if images pass as resource-typed params like HLSL, A coalesces them automatically). Mirrors #12110 split. No #12111 scope creep unless jkwak asks.

**🧭 JKWAK DESIGN COMMENT on #12120 direction (07-15, issuecomment-4983603147) — parks cross-boundary fix with him, surfaces `[ForceInline]` as today-workaround:** jkwak: *"`[ForceInline]` is required to workaround the problem with the current approach. Ideally we want to revert the [#12027] workaround, but unclear if the driver can function without crashing if reverted. Alternative: revert behind a CLI arg keeping old behavior... but if drivers don't work, no benefit. The source of the problem is GLSL doesn't allow resource-type instances passed as a function parameter. SPIRV spec allows it but most drivers wouldn't work. HLSL→DXIL goes through different driver processing, so HLSL working doesn't convince me SPIRV will. I can locally experiment... but even if it works, this is a high-risk backward-compat-breaking change."* ⇒ **KEY TAKEAWAYS:** (1) **`[ForceInline]` on the reporter's `sample()` = usable workaround TODAY** — forces inlining → texture load becomes caller-side → A coalesces it. Triager to VERIFY empirically + offer to reporter (concrete win now). (2) The cross-boundary fix (#12120) is **HIGH-RISK, driver-dependent, MAINTAINER-OWNED — jkwak will experiment locally.** Bot does NOT prototype #12120; it's parked with him. (3) This is about REVERTING the #12027 workaround (a #12120 direction) — **#12111 itself does NOT revert anything** (caller-side coalescing only, low-risk, jkwak already readied it 14:17Z) → do NOT conflate; #12111 stands on its own. Routed to triager: verify+offer ForceInline, capture design constraints into #12120, respond judiciously (don't overstep into implementing the CLI flag), confirm #12111 merge picture unaffected.

**🚀 NEW FIX CHAIN — jkwak EXPLICITLY REQUESTED THE CLI-GATED PR (07-15, issuecomment-4984767878, direct @nv-slang-bot): "Please give me a PR as we discussed."** Converts the draft-only offer → real fix dispatch. **This is a SECOND, SEPARATE fix chain (#12120 direction), NOT #12111** (which stays independently held for operator merge). Dispatched via triager→fixer, canonical thread. **CORE SPEC + safety property:** revert the #12027 image-as-index workaround (pass resource-typed instances as fn params, enabling A to coalesce across boundaries) **ONLY behind a NEW command-line argument, DEFAULT OFF** — old image-as-index behavior stays the default; opt-in flips it. Backward-compat is preserved *because* the change is opt-in. **GUARDRAILS:** DRAFT only (jkwak asking for "a PR" does NOT authorize non-draft — drafts-only guardrail holds; jkwak/operator flips ready/merge); `report_pr_created` on open; PR body FOREGROUNDS the risk + that validation is **NVIDIA 596.75 ONLY** (Intel/AMD unverified — the whole reason it's opt-in/default-off). Tracking home = **#12120** (references #12051 origin). Branch fix/issue-12120 (triager/fixer resolve exact Fixes/branch). ON MY RADAR as new fix chain; triager reports [Fix Report] + PR# up.

**BUILD DISPATCHED (07-15, triager→fixer, fix site pinned + verified):** revert point = `isIllegalSPIRVParameterType` **slang-ir-specialize-resources.cpp:1371** (`as<IRTextureType>` returns true), already target-gated by `isKhronosTarget`:64 (HLSL/CPU untouched). Minimal: gate that texture branch on the new flag (option-set already in scope). CLI-flag pattern mirrors **GLSLForceScalarLayout** (append-only enum include/slang.h + slang-options.cpp table/switch + getBoolOption). Flag name provisional (jkwak underspecified) — fixer picks descriptive, notes provisional in PR. Safety (default-off/opt-in) marked non-negotiable to fixer. Risk framing (NVIDIA 596.75 only; Intel/AMD unverified) mandated in PR body. DRAFT-only, report_pr_created on branch fix/issue-12120, Fixes #12120 + ref #12051, reviewer looped. GitHub ack to jkwak posted (cmt 4984788026). Awaiting [Fix Report] + PR#.

**🧭 JKWAK #12120 EXPERIMENT WORKS + LEANS TO CLI-GATED FEATURE (07-15, issuecomment-4984742293):** *"From my local experiment, it appears to work with the NVIDIA graphics driver, 596.75. I am tempted to add this feature and enable it only when the user explicitly request it with a new command-line argument."* ⇒ positive data point for the #12120 revert direction (NVIDIA 596.75 OK) + jkwak leaning toward implementing the CLI-gated opt-in revert. **This is #12120 territory, NOT #12111** (#12111 unchanged, still held for operator merge). **STANCE: maintainer-owned, jkwak "tempted" (not asking the bot, not yet self-assigned) → do NOT prototype/build; capture datapoint into #12120; light ack; at most a DEFERENTIAL draft-only offer to prototype the CLI-gated opt-in IF jkwak wants it — no presumption, no action absent explicit "yes."** High-risk/backward-compat-breaking/driver-dependent/user-facing-CLI-surface = his call. Watch: single-driver validation (NVIDIA only) ≠ the Intel/AMD robustness jkwak+reporter flagged — the CLI gate exists precisely because other drivers may still crash.

**✅ REPORTER SATISFIED / WIND-DOWN (07-15, maxime-modulopi issuecomment-4984218631):** *"having a function call in this case should be fine since optimizations will likely inline it. I was just demonstrating a worst-case where option A wouldn't work."* + commiserates on driver robustness (glslang SPIR-V dialect, BDA Intel/AMD crashes), optimistic on D3D12→SPIR-V. ⇒ **Concurrence/close, NOT a new design point — does NOT re-open.** The `[noinline]` case was a deliberate worst-case illustration, non-blocking; reporter accepts the inlining path. Light warm ack (human author), triager's call closest-to-the-state. #12111 merge picture UNCHANGED.

**✅ `[ForceInline]` WORKAROUND VERIFIED + OFFERED (07-15, triager, types resolved from master + fixer #12111 emit; posted issuecomment-4983644084):** `[ForceInline]` on the helper inlines it (`OpFunctionCall=0`), moves the texture conversion caller-side onto one shared UniformConstant access chain → #12111 coalesces to **1 image load** (vs 3 on master). **Reporter has a usable-today path.** #12120 design constraints recorded (issue #12120 comment 4983634247), parked with jkwak (no bot prototype). **#12111 merge picture UNCHANGED** — jkwak's high-risk framing is the #12120 revert direction, not the narrow low-risk PR; `[ForceInline]`-works reinforces #12111. No upstream needed.

**✅ #12111 MERGE-TRACK POSITIVE — jkwak "looks good to me" (07-15, PR cmt 4985130798, triager-verified verbatim):** *"It looks good to me but I will discuss with @csyonghe [before] merging it."* Maintainer APPROVES the change, NO changes requested → merge imminent-pending the csyonghe discussion, NOT blocked on any code issue. PR non-draft, reviewDecision REVIEW_REQUIRED. **No bot action (correct):** jkwak's comment asks nothing → no GitHub reply (noise); **fixer must NOT push to fix/issue-12051** — a commit auto-dismisses the positive review. The two conscious-review points (scope breadth; #12120 cross-boundary limitation) are the discussion-worthy items for csyonghe — both already in the PR narrative. Merge = csyonghe/jkwak's call (operator-gated on our side). Reviewer A verdict still separately pending.

**✅ jkwak FLIPPED #12111 DRAFT→READY (07-15 14:17Z, maintainer — NOT the bot; guardrail-checked via PR timeline, fixer's "I did not" truthful).** This is jkwak's review-time engagement (merge still operator-gated). **CI red = infra-flake, NOT regression:** the red `static-const-*-array .3 syn (llvm)` is the `-cpu -slang -compute` variant (no SPIR-V, no descriptors); #12111's diff is SPIR-V-legalize-only, gated off LLVM/D3D12 paths → a SPIR-V load-cache categorically can't touch a CPU-compute/D3D12 test (matches known #12059 flake pattern). Fixer reran (its lane, signed off "silent once green"). Still: non-draft (maintainer-readied), Reviewer A verdict pending, held for OPERATOR MERGE.

**⚠️ TWO THINGS FOR MY OPERATOR/JKWAK RELAY when decision ripens** (both = conscious maintainer review points, both foregrounded/tracked, neither blocks #12111 on its own merit): **(1) SCOPE GENERALIZATION** — A coalesces ALL UniformConstant resource-element loads, not just desc-heap (foregrounded in PR body; jkwak must ratify). **(2) SCOPE LIMITATION** — #12111 delivers the no-boundary loop case + sampler, but NOT the cross-`[noinline]`-boundary image case (tracked in follow-up #12120 as a driver-dependent design decision). #12111 stands on its own merit for what it claims.

**✅ REGRESSION FLAG RESOLVED → BENIGN, NOT a regression (07-15, triager verdict, 3 converging lines; corrected GitHub cmt 4982174429 supersedes 4981993774, credited reporter).** #12111 does NOT defeat the #12027 image-as-index workaround: (1) STRUCTURAL dispositive — diff = 72 lines load-coalescing in `insertLoadAtLatestLocation` + test file only; touches nothing that sets param type/signature/arg-passing/image-as-index specialization (front-end/pre-legalization decisions; #12111's pass runs after, only dedups shared-address loads). (2) MASTER + (3) #12111 raw emit (types triager-resolved): `%sample` param0(image)=`%uint` bindless INDEX on BOTH, unchanged; all 3 calls pass the index; image loaded callee-side. Only the SAMPLER (param1, real OpTypeSampler) has its 3 redundant caller-side loads coalesced 3→1 — same form, fewer loads = benign. Reporter's CHARACTERIZATION was right (image=index workaround, sampler=descriptor); coalescing lands on the sampler, never the image. **texture↔sampler mislabel (msg98/fixer msg90/cmt 4981993774) now CORRECTED.** NET: no regression, no code/scope/PR-body change, workaround preserved. #12111 stands as-is, non-draft, csyonghe+jkwak review.

**[superseded] REPORTER RAISED POSSIBLE REGRESSION — #12111 may DEFEAT the #12027 driver workaround for textures (07-15, maxime-modulopi issuecomment-4982055177):** *"The mentioned workaround is about IMAGE descriptors being passed as bindless index, not sampler descriptors... Samplers are already passed as the actual sampler descriptor. If the texture is now also passed as descriptor in my example, then the workaround is no longer getting applied."* ⇒ INVERTS the triager's characterization (which had texture=safe resource-value side, sampler=workaround path). Reporter says #12027 workaround exists specifically to pass IMAGE/texture descriptors as a bindless INDEX (avoids a driver bug with passing image descriptors directly). If A's coalescing causes the texture to be passed/materialized as an actual descriptor across the `[noinline]` boundary, the workaround is bypassed → **could reintroduce the driver bug the workaround avoids = real compatibility REGRESSION, not framing.** ⚠️ #12111 is NON-DRAFT under csyonghe/jkwak review NOW → if this holds it bears directly on the merge decision. Routed to triager: VERIFY against the workaround's RATIONALE, determine whether #12111 defeats it, and if real → answer on GitHub AND report up. Do NOT dismiss with the prior "orthogonal/strengthening" framing — reporter has corrected it twice; this is the third, sharper pass. Canonical thread.

**MASTER GROUND TRUTH ESTABLISHED (07-15, triager verified from full-type emit; Main flagged a texture↔sampler label contradiction between triager msg98 and msg104 → triager re-verified types ITSELF):** On master, reporter's `[noinline]` repro: `%sample` sig = OpTypeFunction %float **%uint** %19(OpTypeSampler) %v2float. **IMAGE = param0 = %uint (bindless INDEX)** — actual image loaded ONCE *inside* the callee (%41 OpLoad from image heap via index) = **#12027 workaround IS APPLIED to the image**. **SAMPLER = param1 = real DESCRIPTOR (%19)** — loaded caller-side **3×** (%25/%51/%56 from Sampler heap). ⇒ **The caller-side 3× load that coalesces is the SAMPLER, not the texture.** Reporter's framing is CORRECT: image=index(workaround), sampler=already-a-descriptor.

**🔴 CORRECTIONS PENDING (do not trust these until reissued):**
- **GitHub comment 4981993774 (triager) + msg98 + fixer msg90 all MISLABELED texture↔sampler** — the "TEXTURE caller-side 3→1" claim is actually the SAMPLER (%25 = OpLoad %19=Sampler). Triager will correct 4981993774 ONCE, completely, together with the regression verdict.
- **The "✅ settled orthogonal / A fixes his case / strengthening" entry below is SUPERSEDED on labels** — its mechanism (address-sharing coalescing) may still be structurally sound, but its texture/sampler identification is wrong; regression verdict is NOT yet in.

**REGRESSION VERDICT — pending #12111 raw emit (triager type-identifies itself, not trusting labels; asked fixer for raw signature + OpTypeFunction + call-arg lines):**
- **(a) BENIGN:** #12111 leaves image as index, only coalesces the SAMPLER's 3 caller-side descriptor loads 3→1 (sampler already a descriptor; coalescing redundant loads doesn't change passing form) → workaround intact.
- **(b) 🔴 REGRESSION:** #12111 flips image index→caller-side descriptor across the boundary → defeats #12027, could reintroduce the driver bug. On master the image is NOT loaded caller-side at all (it's an index), so for #12111 to "coalesce the texture caller-side" it would have to FIRST flip image→descriptor = the regression.
Triager reports up: regression → immediately + flag csyonghe/jkwak (PR non-draft under their review NOW); benign → GitHub only. GitHub correction held until verdict.

**STRUCTURAL PRIOR → BENIGN (07-15, triager, high confidence, pending empirical belt-and-suspenders):** #12111's entire diff = 72 lines in `slang-ir-spirv-legalize.cpp` (all load-coalescing in `insertLoadAtLatestLocation`; the lone grep hit is a "global-param" COMMENT, not signature code) + the test file. It touches NOTHING that sets a function param type, `OpTypeFunction` signature, image-as-index specialization, or arg-passing form — those are decided in front-end/specialization LONG BEFORE this SPIR-V-legalization pass. ⇒ #12111 **categorically cannot** flip image `uint` index→descriptor across the `[noinline]` boundary; `%sample` param0=`%uint` is byte-identical master vs #12111. It can only coalesce redundant loads of a SHARED ADDRESS = the sampler's 3 caller-side descriptor loads (already a descriptor → benign, no form change). **Verdict trending (a) BENIGN, #12027 image workaround fully intact.** Triager still confirming param0 type on the fixer's raw #12111 emit before the single complete GitHub correction (promised reporter a built answer). Structural argument alone dispositive.

**✅ [noinline] repro SETTLED EMPIRICALLY — reporter right they're RELATED, but NO conflict, A fixes his case (07-15, triager built on master + fixer on #12111, results agree; corrected GitHub issuecomment-4981993774, credited reporter):** the earlier "cleanly orthogonal" answer was TOO CLEAN for the `[noinline]` shape. Emit (`-target spirv-asm`, exact repro): `[noinline]` survives (3 OpFunctionCall). **TEXTURE** passed as a loaded resource value → its load lives CALLER-side on a shared dominating UniformConstant chain → master 3× (%25/%51/%56), **#12111 coalesces to 1** (%25 reused). **SAMPLER** crosses as a descriptor INDEX (uint2 handle), loaded once inside callee → unchanged (the #12027 workaround path). Answer to his either/or: **SINGLE texture OpLoad — A reaches his case** (prior "A can't reach across the boundary" held only for the sampler; the texture's dominating caller-side load IS exactly what A coalesces). **PR IMPACT = NONE (triager-verified, not relayed):** grep of PR #12111 body = 0 orthogonality/workaround/noinline claims; the "too clean" framing lived ONLY in a2a msgs + the one author comment now corrected. PR body states the GENERAL invariant ("read-only UniformConstant resource-element address that dominates all its uses") which ALREADY covers this cross-boundary case → no PR edit, no code/scope change. Strengthens the PR (demonstrably fixes the cross-`[noinline]`-boundary case). Nothing to flag to csyonghe/jkwak as a defect.

**REPORTER Q — A vs #12027 workaround: ANSWERED ORTHOGONAL (07-15, triager issuecomment-4981818769, verified 2 independent reads):** author asked how A (#12111) interacts with the #12027 descriptor-index-passing workaround. **Conclusion: cleanly orthogonal, NO conflict, mildly complementary.** PR-diff read: #12111's `cacheable` flag set only descending through `getElement` off a `UniformConstant` resource ARRAY; a `uint` descriptor-index crossing a call boundary never gets it. Fixer emit read: `loadCache` keyed by access-chain ADDRESS, collapses uses of ONE shared address only; workaround produces N distinct per-call-site chains → N loads untouched (emit shows texture-as-resource→1 coalesced, sampler-as-descriptor→3 preserved). Reverting the workaround later would only give A more to coalesce. (NOTE: a function-boundary regression test was attempted but **reverted** — hit the known slang-test server-vs-CLI disasm divergence; triager ruled land-as-is, PR untouched, 3/3 entries pass, core contract covered. The "bonus test added" framing was superseded.) No state change → handled inline, no upstream re-report.

**✅ [APPROVER SHADOW VERDICT] #12111 WOULD_APPROVE (CLEAN) (07-15, slang-pr-approver @4e78df9c4c5b, ledger-only):** Devin-only tier (production claude-pr-review.yml legitimately skipped bot-authored PR; harvest exit 20, no CodeRabbit). Devin exit 0: 0 bugs / 0 flags / 4 advisory. 6/6 eligibility clauses pass; challenger clean; DECISION_REVIEW+OUTPUT_REVIEW critique gate recorded. Nothing posted to GitHub (shadow). **PR NOW NON-DRAFT/READY (07-15, webhook ready_for_review):** state open draft=false, assignee jkwak-work, reviews requested csyonghe+jkwak-work, 0 human reviews yet, merged_at null. **Scope-generalization flag IS foregrounded in PR body** (dedicated "ratify the broadened scope" subsections + plain-array example + plainArrayMain test) → staged as conscious maintainer decision, requirement met. Awaiting human review + merge (maintainer/operator-gated; approver never posts, reviewer/human owns any post).

**✅ [FIX REPORT] DRAFT PR #12111 OPEN (07-15, triager-verified via gh):** https://github.com/shader-slang/slang/pull/12111 — title "Coalesce redundant UniformConstant resource-element loads on SPIR-V" (foregrounds broadened scope). OPEN **draft**, `Closes #12051`, branch fix/issue-12051, "pr: non-breaking", +181/−4 across 2 files (SPIR-V legalize fix `loadCache` in `insertLoadAtLatestLocation` + new 3-entry test). Codex DIAGNOSIS+PLAN+CODE+OUTPUT all approved. Tests: reporter's hoisted loop 6→2 loads; repro 3/3 (uniform/divergent/plain-array all coalesce to 2); descriptor-handle 69/69, spirv 503/503; valid SPIR-V; clang-format clean. CI: only cosmetic priority-yield (no real failure — expected for manual draft dispatch, see [[project_bot_pr_priority_yield_red_run]]). **Follow-up #12110** filed (orthogonal NonUniform-decoration gap, cross-linked, kept OUT of perf PR).

**⏳ HOLDING at draft — ready/merge decision NOT yet ripe.** Pending before operator engagement: (1) **slang-reviewer** peer review dispatched, ~20-30 min pending; (2) **jkwak maintainer review** not started — he must consciously **ratify the broadened scope** (all UniformConstant resource-element loads, not just desc-heap) at review. **✅ `report_pr_created(#12111)` CONFIRMED fired** (07-15, via fixer's session-mapping note) → chain fully webhook-driven; review comments + CI route back to the triager session. (Was the one open item; now closed.) **When reviewer verdict + jkwak review land → decision ripens → I relay to operator: present ready/merge AND explicitly flag the scope generalization as jkwak's review-decision point (see below).** DRAFT-only; ready-flip + merge OPERATOR-gated.

**✅ GO (07-15, jkwak issuecomment-4975558589): "Let's go with 'a'."** Maintainer bless received → triager planned (/slang-plan) + dispatched fixer to build (a) as a **DRAFT PR**. (b) pin() builtin REJECTED; (c) auto-LICM was fallback. **DRAFT ONLY — ready-flip + merge remain OPERATOR-gated** (drafts-only guardrail; jkwak's "go" authorizes building, not landing).

**⚠️ SCOPE GENERALIZED beyond jkwak's literal "go with a" — MUST FLAG AT RELAY (07-15, triager ruling):** the correct fix gates on `addressSpace==UniformConstant` at the heap getElement, which is NOT descriptor-heap-specific. A plain `Texture2D textures[4]` indexed via a hoisted uniform local **also** coalesces now (3 samples→1 load). Same mechanism, generalizes to all bindless/plain UniformConstant resource-array element loads. **Triager ruled KEEP BROAD** (Option A): narrowing to `__slang_resource_heap` globals would require an unprincipled magic-name check that CLAUDE.md red-flags; the invariant is general (UniformConstant = read-only opaque resource memory → dominating uniform-indexed load safe to reuse). Correctness verified (fixer+codex): plain-array per-lane-correct, spirv 503/503 + descriptor-handle 68/68 green, valid SPIR-V, no test broke. Mitigation: PR title/summary/Process-report FOREGROUND the breadth w/ a plain-array example + a plain-array pinning test, so jkwak **consciously ratifies or asks to narrow** (cheap draft iteration) at review. Still `Fixes #12051` (DescriptorHandle = motivating case). **→ When the draft PR lands, my relay to operator/jkwak MUST call out that scope generalized beyond the literal ask** — it's a review-decision point; draft/review-gated, nothing ships without ratification.

**⚠️ GUARD RETRACTED → Option A (07-15, triager ruling, verified HEAD a8874f6a1e):** the "mandatory dominance+INVARIANCE guard" (which Main also reinforced in the GO dispatch) rested on a WRONG premise from the pre-correction mechanism model. On the DescriptorHandle **default** SPIR-V path the `NonUniformResourceIndex` marker is **STRIPPED before legalization** (verified: 0 NonUniform insts/decorations in `-dump-ir`), so uniform vs divergent are **byte-identical at the coalescing site** — no guard there can distinguish them or read a real signal. **RULING: coalesce whenever the heap getElement is `UniformConstant`; DROP the dead guard.** Justification (sound, not expedient): (1) per-lane divergence ≠ per-iteration variance — one hoisted conversion = one per-lane descriptor, correctly reused; convert-in-loop still stays at 6 loads. (2) The already-shipping **EXT path does exactly this today** (divergent-hoisted→2 loads, 0 NonUniform, valid SPIR-V, accepted) — fix brings default path to parity. (3) Emits VALID SPIR-V → meets the "change NonUniform decoration only if divergent were INVALID" criterion; it's valid → follow-up. **Decoration-neutral** (removes no decoration that was present; desc-heap-nonuniform.slang:8-12 documents the pre-existing absence as accepted/orthogonal). **Orthogonal follow-up:** fixer to FILE a separate issue for NonUniform-preservation on the DescriptorHandle SPIR-V path, cross-linked to #12051, kept OUT of this perf PR. Results so far: uniform repro flips 6→2 loads; suites green (descriptor-handle 68/68, spirv 503/503); valid SPIR-V. Fixer finalizing A + an inert-test must-fix.

**FIX LOCATION (verified HEAD ad69c2e9f, triager):** the N loads are born in **SPIR-V LEGALIZATION — `slang-ir-spirv-legalize.cpp`**, specifically `insertLoadAtLatestLocation`:330/:656 (needLoad branch) + the getElement→access-chain rewrite :658-732 emit a fresh load per use. (This refines the earlier `slang-emit-spirv.cpp:11913` framing — legalize is the precise site.) **Recommended = Layer B:** share one load when def dominates + index uniform, SPIR-V-scoped. Invariance signal **`isNonUniform` is ALREADY computed at :690** — use it for the guard. Fallback **Layer A** (`isMovableInst`, slang-ir.cpp:10042) is broader/all-target blast radius — avoid unless B insufficient. Guard = dominance + INVARIANCE (won't collapse divergent `NonUniformResourceIndex`; preserve NonUniform decoration). Tests: single-OpLoad FileCheck (uniform hoisted repro) + divergent case proving guard doesn't over-fire + existing descriptor-handle/heap suites. Fixer must call `report_pr_created` on open (branch `fix/issue-12051`, no prefix-route fallback). NOT the generic IR dup pass 2638.

**⚠️ ROOT-CAUSE MECHANISM — CORRECTED (07-15, triager, verified HEAD ad69c2e9f via code + disasm):** the SPIR-V reload is **NOT** `shouldDuplicateInstAtUseSite` force-duplicating `CastDescriptorHandleToResource` in generic IR (that was our earlier framing across turns — WRONG). Verified: after hoisting, the descriptor is a **SINGLE shared value in generic IR** (one `getElement`/`SPIRVLoadDescriptorFromHeap`, visible in `-dump-ir`). The triplication is introduced **LATER, during SPIR-V EMISSION** — `legalizeIRForSPIRV`/emit walk at **`slang-emit-spirv.cpp:11913`** (runs AFTER the `-dump-ir` snapshot, which is why IR shows 1 load but disasm shows 3). At emit the descriptor realizes as a shared `OpAccessChain` (index math, once) + an `OpLoad` dereference **RE-EMITTED per use**. Disasm: 2 OpAccessChain, 6 OpLoad (3 tex + 3 samp) for 3 samples. **The fix lever is in SPIR-V emit/legalize, NOT the generic IR dup pass (slang-ir-util.cpp:2638).** Any PR must target the emit path.

**(a) as framed to jkwak:** when a descriptor load's def DOMINATES all uses AND pointer/index are INVARIANT (the hoisted-local case), emit ONE OpLoad and reuse its result-id instead of re-loading per use. Safe: pin()/OpCopyObject already emits exactly this (valid SPIR-V) — (a) automates the workaround. **Blast radius:** confined to SPIR-V emit (HLSL/others unaffected — no separate load step). Found NO pass asserting per-use reload is load-bearing, but did NOT exhaustively prove independence → ONE caveat flagged to jkwak: divergent/`NonUniformResourceIndex` indexing, so guard must be **dominance + INVARIANCE**, not dominance alone (uniform index like the repro is unambiguously fine).

**Historical note (SUPERSEDED framing):** earlier turns attributed the reload to IR-level `shouldDuplicateInstAtUseSite` force-dup of `CastDescriptorHandleToResource` (slang-ir-util.cpp:2638) — that mechanism claim is CORRECTED above; the emit-path finding is authoritative. NOT a dup of [[project_11568_descriptor_heap_direct_index]] (that's input syntax; this is codegen reuse).

**Classification:** Feature/enhancement, low sev, P3. Component: IR + SPIR-V emit + core-module. Type set to Feature on GitHub; verified verdict posted (issuecomment-4937951648).

**MAINTAINER DOWN-SELECT (07-15, jkwak issuecomment-4975321210, direct @nv-slang-bot):** *"option b is not what we want. option c sounds good but a sounds better. I don't fully understand what you are proposing with a. Please give me more details about option a."* ⇒ **(b) pin() builtin REJECTED. (a) [relax use-site dup when def dominates] is the front-runner, (c) auto-LICM the fallback.** Directive = explain (a) in detail. NOT yet a bless/go — he wants to understand (a) first. Triager to produce a detailed, VERIFIED explanation of (a): what `shouldDuplicateInstAtUseSite` (slang-ir-util.cpp:2638) does today for `CastDescriptorHandleToResource`/descriptor loads, the precise proposed change (stop force-cloning when the def dominates all uses / honor an explicit dominating store), why it's safe (pin variant already proved 1 OpLoad→N samples valid SPIR-V), blast radius (SPIR-V emit only? other targets unaffected since HLSL already sticks), and any risk (does any pass RELY on the duplication — e.g. legalization requiring per-use materialization). Don't over-promise the exact patch shape without reading the dup logic. Respond on GitHub (fresh CREATE — jkwak last commenter). Still design-gated (explaining, not building); no bot PR until he blesses (a). Routed through triager, canonical thread.

**REPORTER TARGET-SPLIT REPRO (07-11, issuecomment-4943954942):** maxime-modulopi posted a minimal repro with 3 Godbolt links isolating the problem to SPIR-V:
- **HLSL** (godbolt 74daKWbKP): hoist-into-local `Texture2D tex = texH;` → loads ONCE ✓ (matches triager finding).
- **SPIR-V** (godbolt za9vKEYE3): SAME hoist-into-local → descriptors STILL reloaded every loop iteration ✗ — this is the crux.
- **SPIR-V + `.pinDescriptor()`** (godbolt dxMn4fx8s, his `OpCopyObject` workaround): fixed ✓ — "but should ideally not be required."
⇒ Confirms the SPIR-V asymmetry: `CastDescriptorHandleToResource` is force-duplicated (slang-ir-util.cpp:2638) REGARDLESS of user hoisting, so a local doesn't stick on SPIR-V the way it does on HLSL.

**CRUX ANSWERED (07-11, triager, DISASSEMBLED SPIR-V at HEAD 4d91d47b, replied issuecomment-4944015739, fresh CREATE):** the SPIR-V reload is **NOT a hard SPIR-V requirement — it's an ARTIFACT.**
- HLSL + hoisted local → `ResourceDescriptorHeap[i]` 1×, reused ✓
- SPIR-V + hoisted local → descriptor `OpLoad` **3×** from SAME `OpAccessChain` (%32/%43/%52 ← %24) ✗ = reporter's exact complaint
- SPIR-V + `pinDescriptor()` → `OpLoad` **1×** (%25) feeding all 3 `OpImageSampleExplicitLod` ✓ — **PROVES one load feeding N samples is valid SPIR-V; nothing in spec forces the reload.**
The hoisted load is treated as re-materializable (kin to `shouldDuplicateInstAtUseSite` force-dup, slang-ir-util.cpp:2638) → OpLoad re-emitted per use → user's explicit local store not honored. pin() defeats it by forcing a materialized SSA value the duplicator won't re-clone. **Fix is clearly on our side and safe.**

**Design options framed for jkwak/csyonghe (triager offered to prototype any):**
- **(a) [LEADING]** relax use-site duplication for descriptor loads when the def dominates uses → local "sticks" on SPIR-V, matches HLSL, **NO new API surface**.
- (b) official `pin()`/`loadOnce()` builtin.
- (c) auto loop-invariant-load hoist pass.
Design-gated on WHICH approach; still **no bot PR (jkwak's surface)**; ready/merge operator-gated. Chain OPEN awaiting maintainer pick.

**ANSWERED jkwak (07-10, issuecomment-4940241238) — all 3 asks verified empirically at slangc 2026.12.2-60-g33f9ed0ce ≈HEAD, fresh CREATE (jkwak was last commenter):**
1. **Repro delivered + reporter is RIGHT:** `DescriptorHandle<Texture2D>` sampled 3× in a loop → per-use conversion emits `ResourceDescriptorHeap[i]` **3× (one reload/use)**. Hoisting the conversion into a local outside the loop → emits **1×, reused**. ⇒ "store in a local" **already works on HLSL today**, no new pin() needed there. This CORRECTS our earlier imprecise "no value to pin on HLSL" note.
2. **#11798 does NOT close #12051 (jkwak's 1st-comment guess checked & refuted):** #11798 is the [[project_11568_descriptor_heap_direct_index]] INPUT-SYNTAX feature (adds UntypedResourceHandle→uint lowering; peephole only collapses the uint↔handle round-trip). Changes how you SPELL a heap index, NOT how many times the descriptor loads. Complementary; does NOT by itself remove per-use reload. **Do not assume #11798 resolves this.**
3. **#12027 IS linked (same phenomenon):** #12027's cited comment (4930059216) = SAME reload at the function-param boundary (textures passed as descriptor INDICES, re-fetched far side — historical driver workaround). #12051 is the loop-local version. jkwak's "drivers may be in a better place now" directly enables auto-reuse here.

**Next: jkwak's design call** among — (a) document the "hoist into a local" idiom (works today, zero code), (b) opt-in reuse builtin, (c) automatic loop-invariant-load reuse pass. Triager offered to prototype whichever; **no PR yet (his surface)**. Chain OPEN, awaiting jkwak direction. ready/merge operator-gated regardless.

**MAINTAINER DIRECT-MENTION (07-10, 2nd comment):** jkwak-work `@nv-slang-bot`-mentioned (issuecomment-4940204817): *"I am not clear on what the problem is. Please give me a simple repro shader... also wondering if this is related to the workaround Slang has to use"* → links [#12027 comment 4930059216]. Answered as above.

**MAINTAINER ENGAGED (07-10):** jkwak-work commented (issuecomment-4940194035) responding to the author's HLSL-path question: *"this will be possible with the following PR: [#11798]"* — i.e. he links the descriptor-reuse/pin capability to the existing direct-index `ResourceDescriptorHeap` redesign ([[project_11568_descriptor_heap_direct_index]], draft #11798, "lower to uint in legalization" per csyonghe invariant). STRONG signal the fix path may be #11798, NOT a separate `pin()` builtin. Triager to verify what #11798 actually delivers re: reuse/CSE-ability, fold it against its own in-flight emit-datapoint check, and respond on GitHub. Routed through triager on canonical thread.

**RE-OPENED (07-10 later):** author maxime-modulopi replied (issuecomment-4938420177) challenging the "HLSL: no value to pin" scope note — points out HLSL *can* store `ResourceDescriptorHeap[i]` in a variable, so asks why `pin()` couldn't use that for the HLSL path. Triager verified at HEAD 4676e878e: reporter largely right, note imprecise — HLSL lowers via `kIROp_LoadResourceDescriptorFromHeap` (NOT force-cloned `CastDescriptorHandleToResource`), resource types storable in Slang IR (`canInstBeStored`=true) → pin() meaningful **all-target**, not SPIR-V-only. Was pulling fixer for one emit datapoint (per-use re-emit vs DXC CSE) when jkwak comment landed.

**STATE (07-10): HELD — no PR, design-gated.** Fixer correctly declined to open a bot draft on a maintainer-owned public API surface (precedent: #12027/#11657/#11655 all closed-unmerged for same reason). Root cause confirmed at HEAD 4676e878e by both triage AND fixer. Verified: no PR, no remote branch refs #12051; issue OPEN. GitHub triage comment (issuecomment-4937951648) updated IN PLACE to held state = live footprint. **Re-engages ONLY on a maintainer/human comment** (webhook → new inbound) → fixer prototypes Approach A as a DRAFT PR.

**Minimal-path lead (fixer):** internal `__copyObject<T>` (hlsl.meta.slang:13923, `[require(spirv)]`, `OpCopyObject $v`) already does the reporter's trick — exposing it publicly may be most of Approach A. The "no `shouldDuplicateInstAtUseSite` change needed" part is fixer *inference* (labeled as such in the public comment), NOT IR-verified.

**Next human action:** maintainer (@csyonghe/@jkwak-work) design decision on: spelling (pin/loadOnce), host (method vs free fn), scope (SPIR-V/GLSL-only vs all-target — "load once" is meaningless on HLSL, which lowers to `ResourceDescriptorHeap[i]`), and whether to also auto-hoist (Approach B). Not a dup of [[project_11568_descriptor_heap_direct_index]] (that's input syntax; this is codegen reuse).

**Routing:** triager owns fixer edge (do NOT double-dispatch). Canonical thread `gh-issue-shader-slang/slang-12051`.
