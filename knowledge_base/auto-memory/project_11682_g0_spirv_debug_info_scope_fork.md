---
name: project-11682-g0-spirv-debug-info-scope-fork
description: "#11682 slangc -g0 emits SPIR-V debug info — maintainer picked (b), but b1/b2 scope fork awaits jkwak steer"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9e3a655e-bbbe-4877-93f0-a652b1104daf
---

# #11682 — `slangc -g0` includes SPIR-V debug info (IN-FLIGHT, awaiting maintainer steer)

Bug/P3, `reproduced`. `-g0` (`DebugInfoLevel::None`) still emits `OpSource` (unconditional, `slang-emit-spirv.cpp:11824-11844`) and `OpName`/`OpMemberName` (from `IRNameHintDecoration`, not classified as debug info → bypasses the debug gate). Help text `Don't emit debug information at all.` over-promises.

**jkwak-work chose option (b)** (2026-07-18): gate names/source on `None`. BUT slang-fixer code-traced a scope fork the one-word steer didn't resolve:

- Default compile (no `-g`) resolves to the SAME `DebugInfoLevel::None` as explicit `-g0` — indistinguishable at emit site (`global-session.cpp:879` erases the distinction via Max(targets)).
- **b1 (literal):** gate on `None` → strips `OpName`/`OpSource` from EVERY default SPIR-V compile, not just `-g0`. User-visible default-output change + ~41 spirv/spirv-asm CHECK tests (friendly-name disassembly uses `SPV_BINARY_TO_TEXT_OPTION_FRIENDLY_NAMES` ← `OpName`). ~4-line emit change but big test sweep. Contradicts triage's "small fallout."
- **b2 (explicit -g0 only):** default preserved (~1 test, `debug-levels.slang`), matches issue title, but needs new plumbing to distinguish explicit-None from default-None — itself a small design call.

**State:** fixer posted b1/b2 clarification tagging @jkwak-work (comment 5011506198), recommends b2. **Holding build** until steer. jkwak's reply webhooks back to fixer's session directly — Main does NOT drive until then. Worktree `wt-slang-11682` @ base `aaa07fe296`; plan `/workspace/agent/reports/slang-11682.md` (fixer's fs). Adjacent SPIR-V debug work: #11550 / #11565.

**Triager independently re-verified (2026-07-18, HEAD `3292090b3`):** confirms default==`-g0`==`None` enum value 0 with no "was-explicitly-set" tracking; `stripDebugInfo()` doesn't strip `NameHintDecoration`. So literal fix changes default disassembly for everyone. **Caveat: fixer's "~41 tests" is an ESTIMATE** — triager confirmed the direction only + ~14-15 test files carry `CHECK.*OpName` (do NOT relay "41" as fact). Doesn't change the decision (hinges on "default output changes for everyone").

**jkwak steer #1 (comment 5013234042):** "default value for `-g` should be 1 (minimal, includes OpName+OpSource); `-g0` shouldn't include them." BUT fixer code-traced a mismatch between his mechanism and intent: Minimal (g1) = "line numbers," so flipping default→Minimal ALSO newly emits `OpString "<filename>"` (`IRDebugSource`, lowered only when level≠None, `slang-lower-to-ir.cpp:15425`→`slang-emit-spirv.cpp:2164`) + `OpLine` per statement (`maybeEmitDebugLine` early-returns at None, `:9908`). Today's default emits NEITHER — only OpName+OpSource. So literal-jkwak (A) turns on line tables for every default compile (~7 no-`-g` tests w/ body-level CHECK-NEXT break by OpLine adjacency).
- **(A) flip default→Minimal + gate names on None** (literal; accepts line tables on by default).
- **(B) keep default byte-identical to today, distinguish explicit `-g0`, gate names on explicit-None** (default unchanged, no line tables; `-g0`=nothing; ~1 test; small "explicitly-set" plumbing). Fixer recommends B; Main endorses B lean but it's jkwak's product call.

**jkwak steer #2 — RESOLVED (comment 5013397505, 2026-07-18):** picked **(A)**, verbatim: "We should go with 'A' and make the PR breaking-change. 'B' will mean that we introduce a new debug-level, which can be more confusing." So: flip default→Minimal + gate names on None, and the `OpLine`/`OpString` default-output change is an INTENDED breaking change (he explicitly rejected B). Build UNBLOCKED.

**State (2026-07-19):** fixer building (A): flip default None→Minimal (`global-session.cpp:879`); gate OpName/OpSource on level==None; refresh ~7-10 no-`-g` CHECK-NEXT golden tests broken by OpLine adjacency + invert `debug-levels.slang` CHECK_NONE; add breaking-change changelog note. → repro test → draft PR `Fixes #11682` → report_pr_created → review → `[Fix Report]`. Fixer owns E2E on this thread; Main does NOT drive unless flagged.

**Routing note (07-19):** fixer 11682 session = `sess-1782145314986-866vyp` (slang-fixer grp ag-1780667166439-vmjrwe). Its last processed inbound was 22:59 ("held one more round") — my prior-turn `<message>` (A)-steer dispatch may NOT have landed (container was stopped). jkwak then pinged bot for status (comment 5014568474). Re-sent (A)-GO + status-ping relay in ONE wake, pinned `target_session_id=sess-...866vyp` + `in_reply_to=52` (the fixer's last outbound) to guarantee it hits the worktree-bearing session, not a cold mint. Only ONE fixer 11682 session exists (no phantom double-session).

**IMPLEMENTED (07-19 05:57):** fixer built (A). Worktree wt-slang-11682 @ `a916653b70`. Patch 3 files +57/−24: `slang-compiler-options.cpp` getDefault(DebugInformation)→Minimal (verified Max(linkage,targets) at global-session.cpp:879 can't clobber explicit `-g0`); `slang-emit-spirv.cpp` new `shouldEmitDebugNames()` (!=None) gating 5 compiler name-hint OpName/OpMemberName sites + OpSource (USER spirv_asm OpName left ungated — different path, matches triage scope); tests `tests/spirv/g0-no-debug-info.slang` new + `debug-levels.slang` CHECK_NONE inverted. Status posted to jkwak (comment 5014581117).

**FULL-SUITE FALLOUT + 2ND FIX (07-19 06:40):** SPIR-V repro perfect, but full suite = 51 fail (not ~7-10): **(1) 19 INTERPRET/`slangi` VM crashes** — hard abort "unimplemented: VM bytecode gen for inst" = REAL functional regression; **(2) ~29 textual-target golden diffs** (HLSL/GLSL/Metal/CUDA `#line`+temporaries, SPIR-V id-renumber) = cosmetic. **Single root cause:** `slang-emit.cpp:1030` `stripDebugInfo` fires only at `None`, but comment `:1027` says strip "if target can't express debug info OR user specifies -g0" — only the `-g0` half was implemented; default==None masked it. Flip exposed it → debug insts survive to targets that can't consume them. Only SPIR-V + CPU/LLVM actually consume debug insts; textual no-op them; VM crashes.
**Main GREEN-LIT the root-cause fix (07-19):** extend `:1030` strip to also fire when target can't express debug info — finishes the comment's intent, NARROWS breaking surface to SPIR-V(+CPU) only, VM crash gone, ~48/51 failures vanish, jkwak intent preserved. In-scope (correctness fix to approved (A), not new design) so no jkwak pre-re-ask. **2 conditions:** (1) use existing target-capability query for "can express debug info", not a hardcoded list; (2) PR desc must document BOTH changes + full breaking surface (SPIR-V **and CPU/LLVM** change output — call CPU out explicitly). Fold finding into PR desc, not a separate interim comment. Rebuild → repro → confirm failures collapse to handful of SPIR-V goldens → draft PR (`pr: breaking change`, `Fixes #11682`) → report_pr_created → review → `[Fix Report]`.

**⚠️ STALL / DEAD-PROMISE (discovered 07-22 20:16Z):** fixer went dark 07-19 07:01 (seq 61) right after kicking off a BACKGROUND master-baseline subagent + "I'll act on its completion notification" + ending turn. Two instruction-update container restarts ~07:06-07:08 tore the container down → **background completion notification never survived teardown** (known failure mode: feedback_in_session_monitors_dont_survive_teardown) → fixer waited 3+ DAYS for a wake that never came. NO branch pushed (GitHub 404 fix/issue-11682), NO PR. jkwak pinged bot TWICE (07-19 + 07-22 comment 5051001261 "I don't see a PR yet"). **Main re-woke fixer session sess-1782145314986-866vyp (pinned target_session_id + in_reply_to=90, msg id 109)** with ordered resume: (1) post honest 3-day-delay status to jkwak; (2) recover worktree wt-slang-11682 or rebuild patch from reports/slang-11682.md; (3) re-run + master-baseline slangi attribution; (4) push + draft PR + report_pr_created.

**⚠️ SCOPE CORRECTION (triager, 07-22 21:45Z, comment-backed):** the 19 VM/`slangi`/INTERPRET crashes are a **GENUINE IN-SCOPE REGRESSION caused by this change** — NOT pre-existing/out-of-scope (earlier "prove pre-existing" plan is REFUTED). Authoritative baseline: `byte-code/hello`, `byte-code/composite`, `autodiff-custom-dadd-dzero`, `gh-6856` PASS on clean master `d384b77e66`, CRASH in worktree → change causes them → must be root-caused before PR. Mechanism NARROWED (triager, 07-22 22:09Z): it is **NOT** the `IRDebugLine → VM unimplemented-inst` default arm (hit 0×) — it's a **dictionary lookup miss post-strip** (stripping debug insts removes something a later VM dictionary lookup expects). **Magnitude corrected UP: ~111 INTERPRET tests affected, not 19** — materially bigger regression surface; PR firmly held.

**ROOT CAUSE = OPTIMIZATION-SHAPE CHANGE, NOT JUST DEBUG OUTPUT (triager verify + Main source-confirmed, 07-22 23:21Z):** the default→Minimal flip injects `IRDebugLine` insts that alter OPTIMIZATION SHAPE on every default (no-`-g`) TEXTUAL compile — NOT the VM-only issue, NOT cosmetic golden churn. Mechanism (Main verified in source `slang-ir-loop-inversion.cpp:22-35`): `isSmallBlock()` counts `getOrdinaryInsts()` against threshold 4 inside `isSuitableForInversion`; `IRDebugLine` is counted → tips a ≤4-inst loop-condition block over → loop inversion disqualified. `deferBufferLoad` similarly treats `IRDebugLine` as side-effecting → stops deferring loads. BOTH run BEFORE the None-only `stripDebugInfo` → strip can't undo. A/B-confirmed (worktree `-g0` == master byte-identical; default differs, ~34 goldens). **jkwak decided (A) framed as debug-OUTPUT change — did NOT have optimization-shape change in view.**

**Main GREENLIT fixer to post 3-option finding to jkwak (msg 141, 07-22 23:21Z):** #1 proceed with (A), accept as documented breaking + refresh ~34 optimization-shape goldens (likely jkwak lean); #2 reconsider scope → default=None, only explicit `-g0` strips (= original B; default output AND opt-shape byte-identical to today); #3 deeper/principled — exclude `IRDebugLine` from `isSmallBlock` count + `deferBufferLoad` side-effect check so debug info never affects optimization (right long-term fix + latent correctness bug regardless, but LIKELY SEPARATE PR/issue scope). Fixer recommends between #1/#2 (holds the proof); notes #3 out of scope for a bugfix. **Golden-refresh + PR HELD until jkwak picks.** Told triager: fixer posts (closest-to-state), single instruction path via Main-owned fixer edge; triager holds consolidated roll-up for resolved state.

**POSTED (fixer, 07-22 23:29Z, comment 5052676726):** fixer independently source-verified BOTH mechanisms before posting (not just relaying Main's read): (1) `isSmallBlock` counts `getOrdinaryInsts()` incl `IRDebugLine` (only params skipped) vs threshold 4; (2) `IRDebugLine.mightHaveSideEffects()==true` (no pure flag) + not in `deferBufferLoad` allow-list → hits `default: return false` (`slang-ir-defer-buffer-load.cpp:200`). Posted 3 options, recommended **#2** (delivers `-g0` fix, zero collateral) noting #1 consistent w/ jkwak's breaking-change call. **`SLANG_DBG2_11682` instrumentation ALREADY STRIPPED — clean diff: 4 source + 2 tests + 1 new test** (cleanliness gate satisfied pre-emptively). PR HELD until jkwak picks.

**jkwak PICK (comment 5059132569, 07-22 late):** verbatim "In that case, I think we should make `-g0` a default." **Main interpretation (fixer confirming):** REVERSAL — declines (A)'s Minimal flip AND #2's explicit-vs-default distinction. **Default debug level STAYS `-g0`/None; fix None to emit no names/source.** Net: default == `-g0` == None, all strip OpName/OpSource (default SPIR-V loses friendly-names = accepted breaking change, in view since cmt 5011506198). **NO Minimal flip → no IRDebugLine at default → opt-shape change AND ~111-test VM regression BOTH VANISH.** Patch COLLAPSES: DROP default→Minimal flip + VM root-cause work + `slang-emit.cpp:1030` strip-condition change (all only needed because flip injected debug insts); KEEP only gate OpName/OpSource on `level==None` (`shouldEmitDebugNames`) + refresh ~14-41 `CHECK.*OpName` no-`-g` goldens + invert `debug-levels.slang` CHECK_NONE; verify `-g1`/`-g2` still emit names (!=None). Still breaking → `pr: breaking change`.

**Main dispatched to fixer (msg 149, in_reply_to=144, pinned):** confirm interpretation matches (post 1 tight q to jkwak only if genuinely unsure — Main reads it clear) + RE-VERIFY consequence chain on worktree (with default=None + names gated on None, does ~111-VM-regression + opt-shape delta actually vanish? A/B: default==today except OpName/OpSource stripped — LOAD-BEARING, prove before PR). If both check out → build → repro → push → draft PR → report_pr_created → review → Fix Report. If VM regression does NOT vanish → STOP + flag (interpretation/model wrong).

**CONSEQUENCE CHAIN CONFIRMED + ~273 GOLDEN FALLOUT (fixer, 07-23 14:13Z):** Main interpretation VERIFIED — reverted to -g0-default (dropped Minimal flip + strip-condition + decoration fix + vector-member-atomic; kept ONLY `shouldEmitDebugNames()!=None` name/source gate). Repro perfect: `-g0`=0 names, default=0 (jkwak intent), `-g1`=4. **BOTH regressions VANISHED** (VM/INTERPRET byte-code+autodiff PASS 0-fail; opt-shape loop-inversion PASS) — load-bearing claim holds. **BUT full suite: 273 non-GPU `.slang`/`.hlsl` tests FAIL** (117 tests/spirv, 14 glsl, rest front-end/language/pipeline/bugs) — they CHECK `OpName`-friendly ids (`%main`,`%samplers`) with NO `-g` → default now strips OpName → ids become numeric `%N` → CHECK misses. All pass master, fail w/ change (A/B 8 samples). = INTENDED consequence of "-g0 default" (default loses friendly names for ALL users); true scale ~273 (first-escalation est was ~41). PR goes 1-src+2-test → **1 src + ~270 test-CHECK updates** (mechanical-but-error-prone: add `-g1`/`-g2` where CHECK needs named handle, else rework to numeric/pattern).

**Main GREENLIT fixer to surface ~273 to jkwak (msg 153, 07-23):** frame = "measured cost of the path you chose, confirm given this" NOT re-decide-from-scratch (he's decided twice; ~273 is ~8× the ~34 churn he picked -g0-default to AVOID → undercuts his rationale → he must see it). 2 options: #1 proceed (systematic ~270-test update, faithful to his repeated breaking-change calls); #2 explicit-`-g0`-only (the variant he called "confusing" — now measured at ~0 golden fallout, preserves default friendly names). Fixer leans #2 given cost. **ALL test edits + PR HELD until jkwak answers.**

**POSTED (fixer, 07-23 14:16Z, comment 5059474203):** ~273 measurement to jkwak, confirm-not-relitigate framing per Main outline, fixer lean=#2. All test edits + PR HELD.

**FINAL DECISION — DOCS-ONLY (jkwak, comment 5059691305, 07-23):** THIRD pivot, back to original triage option (a). Verbatim: "we shouldn't be making changes to the existing tests just for this... update the document saying something like: `0, none: Don't emit debug information except OpSource and OpName`." → Current behavior is ACCEPTABLE; the only defect is the over-promising help text. **Fix collapses to a HELP-TEXT correction:**
1. REVERT the code change entirely — drop the `shouldEmitDebugNames()` name/source gate, ALL of it. Zero behavior change, zero test churn. Worktree ends with ZERO source-logic + ZERO test-CHECK diff.
2. Fix ONLY the help text `Don't emit debug information at all.` at `source/core/slang-type-text-util.cpp:212` → accurate string per jkwak template (`0, none: Don't emit debug information except OpSource and OpName`).
3. ACCURACY GATE: confirm what None actually emits before writing — earlier trace said OpSource + OpName **+ OpMemberName**; if OpMemberName survives, use accurate form ("...except OpSource, OpName, and OpMemberName") — faithful to intent, no re-ask.
4. NOT a breaking change (behavior unchanged) — DROP `pr: breaking change` label.
**Main dispatched (msg 159, in_reply_to=144, pinned).** PR = single help-text line, `Fixes #11682`, desc cites jkwak decision + "no behavior change". Confirm worktree clean of ALL prior (A)/name-gating/VM/instrumentation work (git status = 1 line) before push.

**PR-review watch-items (triager, 07-23 14:48Z):** fix may involve **doc regen** beyond the single help-text line (help string possibly mirrored/generated); fixer showed "correct restraint on `include/slang.h`" → confirm PR has **NO ABI change** to the public header. Triager will refresh its verdict comment (4770490923) in place at Fix Report, NOT clobbering the fixer's jkwak-question comments; consolidated `[Triage Resolution]` folds "jkwak chose doc-only" at that point.

**✅✅✅ MERGED — TERMINAL (07-23 22:06:40Z, Main-verified via gh):** PR #12201 **MERGED by jkwak-work**, merge commit `b629210213`; issue #11682 **CLOSED/COMPLETED** via `Fixes #11682`. Landed docs-only, 3 files +5/−2, `pr: non-breaking`, ZERO behavior/test change: `-g0`/none help text corrected ("Don't emit debug information. This is the default. For SPIR-V, OpSource, OpName and OpMemberName are still emitted."), `getDefault(DebugInformation)`→None explicit (no-op), regen `command-line-slangc-reference.md`. Maintainer APPROVED after 1 round; peer review APPROVE_WITH_NITS/0-bugs; real CI green. Worktree `wt-slang-11682` removed, sentinel cleared. **Deferred non-blocking follow-up** (out of scope, mutually verified — future comment-only PR if anyone wants): 2 stale "at all" doc comments `include/slang.h:905` + mirror `tools/gfx/slang.slang:229` (`docs/user-guide/08-compiling.md:553` = phantom cite; `slang-downstream-compiler.h:190` = separate enum). Triager folds `[Triage Resolution]` + appends verified-facts learning now merged. **CHAIN COMPLETE — nothing routes through Main.**

---
_(prior milestone, superseded above:)_
**✅✅ APPROVED + MERGEABLE — PR #12201 (07-23 17:27Z, Main-verified via gh):** jkwak-work APPROVED ("Looks good to me", review-4766571745); `reviewDecision=APPROVED`, approval `commit==headRefOid==e095697dd8` (binds HEAD, not stale), non-draft, state OPEN, `mergeable=MERGEABLE`. Final = **3 files +5/−2, `pr: non-breaking`**: `-g0`/none help corrected+default-marked+SPIR-V-scoped, `getDefault(DebugInformation)` returns None explicitly, regen `command-line-slangc-reference.md`. Zero behavior/test change. jkwak's review asks (mark default+explicit-0 internal; don't state SPIR-V specifics as universal) both addressed in e095697dd8, re-reviewed+approved. **Fixer holding correctly: will NOT push (auto-dismisses approval), will NOT merge/flip (OPERATOR-gated).** Last `ci_failed` on e095697dd8 = benign draft-dispatch priority-yield (now non-draft → real `pull_request` CI is the signal). **Real `pull_request` CI GREEN** (fixer-triaged 07-23 18:09Z; red = documented priority-yield cosmetic pattern). Peer review closed APPROVE_WITH_NITS (0 bugs, nits resolved). So: APPROVED + MERGEABLE + CI-green — fully ready. **MERGE = operator's call.** Triager folds `[Triage Resolution]` + verified-facts learning at merge.

---
_(prior milestone, superseded above:)_
**✅ RESOLVED — DRAFT PR #12201 OPEN (fixer Fix Report, 07-23 14:59Z; Main-verified via gh).** Docs-only, `pr: non-breaking`, `Fixes #11682`, head `fix/issue-11682`, `report_pr_created` done. **2 files +4/−2:** `source/core/slang-type-text-util.cpp` (`-g0`/none help → "Don't emit debug information, except for SPIR-V OpSource, OpName and OpMemberName") + `docs/command-line-slangc-reference.md` (regen, CI diff-checked). ALL prior (A)/name-gate/VM/strip-condition/instrumentation work FULLY REVERTED — zero behavior/test change. Accuracy verified clean-master (`-g0` emits OpName/OpMemberName/OpSource, no OpLine — OpMemberName detail caught). codex-critique PLAN→CODE→OUTPUT approved. Watch-items resolved: doc-regen IN (CI diff-checks); `include/slang.h` same stale wording left as **out-of-scope public-ABI follow-up** per jkwak help-text-only scope (noted in PR). 2 `ci_failed` webhooks BENIGN (stale pre-regen force-pushed-past + draft priority-yield cosmetic-red on d7d9248685). Review dispatched to slang-reviewer (running); fixer owns PR through review per workflow. Triager folds `[Triage Resolution]` + verdict-refresh (cmt 4770490923, don't clobber fixer jkwak-questions) at close. **Main watchdog `byj4rra05` STOOD DOWN** (PR exists). Merge OPERATOR-gated per standing rule.

**UPDATE (07-23 16:39Z, Main-verified):** PR #12201 flipped **non-draft by jkwak-work himself** (timeline actor=jkwak-work 16:39:53Z) — NOT a fixer drafts-guardrail violation; maintainer's own action while reviewing. jkwak PR-review comment requests a code change: "explicitly make the default value to be 0 internally at the implementation" + mention 0 is default in help. **This getDefault change is MAINTAINER-REQUESTED = in scope** (triager initially worried scope-creep, self-corrected after reading PR reviews). It's a CLARITY change (make the already-effective None/0 default explicit in code) — does NOT flip to Minimal, so does NOT reintroduce OpName-stripping or the ~273 fallout; still non-breaking, still no test churn. Once pushed, PR grows from 2 docs files → +getDefault src change; the "zero code change" framing goes stale (triager will refresh verdict cmt 5060023366 against the committed diff, not intent). Fixer owns the push (jkwak review webhooks to its session).

**PUSHED + VERDICT REFRESHED (triager, 07-23 16:54Z, verified against diff):** getDefault change now committed — PR #12201 = **3 files, +7/−2**, the maintainer-requested `getDefault`→0 change described as behavior-neutral. Verdict cmt 5060023366 refreshed accurately. Chain rests correctly: fixer owns #12201 through jkwak re-review + Reviewer A verdict + merge (all webhook to its session). Triager's sole remaining trigger = **merge** → refresh verdict to "fixed, merged" + forward final resolution + append learning (verified compiler facts: stripDebugInfo dangling-decoration; IRDebugLine perturbing invertLoops/deferBufferLoad + meta-lesson: measuring behavior-fix cost steered maintainer to cheap doc fix).

**ARC (for the record):** triage (a)/(b) → jkwak "(b)" → fixer flags default==g0 → b1/b2 → jkwak "default→Minimal" → fixer flags Minimal adds OpLine/OpString → A/B → jkwak "(A) breaking" → build surfaces ~111-test VM regression (dictionary-miss post-strip) + opt-shape change (isSmallBlock threshold-4 counts IRDebugLine) → jkwak "make -g0 default" → fixer proves both regressions vanish BUT ~273-test friendly-name fallout → jkwak FINAL "don't edit tests, fix the doc" = original triage (a). 6 maintainer round-trips; each fixer escalation surfaced a real cost jkwak hadn't priced. Also recovered a 3-day dead-promise stall (background-notification-across-teardown; learning appended). Rest of suite clean (4805+/0; tests/spirv 520/526, 6 = `spirv-version-option` line-number goldens = intended). PR correctly HELD (not shipping until true residual + root cause). Branch rebased LOCALLY to `d384b77e66` (still 404 remote — not pushed). Fixer's honest delay note to jkwak = comment 5051042000 (≈20:19Z).

**PR-CLEANLINESS GATE:** fixer added `SLANG_DBG2_11682` debug instrumentation while root-causing the VM regression (triager flagged a "cleanup gate"). **When the PR lands, verify it is STRIPPED of all `SLANG_DBG2_11682` / temporary diagnostic instrumentation** before routing to review — instrumentation must not leak into the breaking-change PR. **LESSON: never let fixer end a turn waiting on a background-subagent completion notification — it dies on container teardown; use synchronous blocking Agent.** Main now actively driving; awaits status URL + PR number or worktree-lost flag.

**RECOVERED + RE-RUNNING (07-22 ~21:07Z):** fixer re-woke on msg 109. Confirmed: (1) posted honest 3-day-delay status to jkwak on #11682 at 20:19:04Z ✓; (2) worktree wt-slang-11682 intact, patch present, PR body drafted; (3) re-running via background bash (`bqri55kl3` full suite + `bboo2hoop` master-baseline) + ended turn — fixer argues run_in_background is correct single-completion pattern (it IS, for normal turn-end/resume; only a container TEARDOWN kills it, as happened 07-19). Progress live (triager: 70 tests/0 fail, master build 85/555). **Main armed own watchdog** (teardown-independent safety net vs fixer's in-container notification). Did NOT interrupt fixer mid-build. First watchdog `b1cgalq9f` FALSE-FIRED (jq guard bug: `.[0]` on empty array → literal "PR #null" passed non-empty check) — re-verified directly: still no PR, no branch (404) as of 07-22 ~21:10Z, suite still running. Re-armed corrected watchdog `batgbat1w` — but it was STOPPED by a container teardown (07-22 ~21:45, no completion record) before firing. Fixer session still running (last_active 20:17, root-causing VM regression). Re-armed 3rd watchdog after teardown. Next real event: PR/branch lands (→ verify report_pr_created → route to review) OR watchdog TIMEOUT (→ re-check sess-...866vyp + re-drive).
