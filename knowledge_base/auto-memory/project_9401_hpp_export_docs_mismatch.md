---
name: project_9401_hpp_export_docs_mismatch
description: "shader-slang/slang#9401 -target hpp export/docs mismatch — triaged P3, docs-only fix routing to fixer"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0574abae-931e-41dc-9682-6e980e9d381d
---

# #9401 `-target hpp` export requires entrypoint + docs mismatch — TRIAGED → fixer

Maintainer **jkwak-work** requested triage (2026-07-18, issuecomment-5011335733). Chain: orchestrator → slang-triager → slang-fixer (docs PR).

- **Classification:** bug (docs + behavior/UX) / **low / P3** / docs+CPU-C++-hpp-emit+frontend-visibility.
- **Two problems:** (1) `docs/cpu-target.md:216-225` sample malformed (missing `int` return type; uses `public`); (2) exported fns only appear in `-target hpp` header when marked `export` (not `public`) with ≥1 entry point.
- **Reproduced on ToT** (v2026.13.1 / aaa07fe29), 7 slangc runs. Key correction to report premise: **dummy entrypoint NOT actually required** — `-whole-program` already exports `export __extern_cpp` fns with no entry point. `public` never roots vs DCE (only `export`'s HLSLExportModifier adds KeepAlive).
- **Recommended fix = Approach A (docs-only):** add return type, use/explain `export`, document `-whole-program`. Zero compiler risk. Approaches B (`public` roots like `export`) and C (scope header emit-filter) flagged as **maintainer design calls — NOT auto-implemented**.
- **Files:** `docs/cpu-target.md:216-225`; `slang-lower-to-ir.cpp:1434-1442`; `slang-emit-c-like.cpp:5392-5396`.
- **GitHub:** verified 5-bullet posted (issuecomment-5011426913), `reproduced` label applied, Type=Bug (human-set). Triager owns triager→fixer handoff via direct edge; Main did NOT double-dispatch.
- **2026-07-18 maintainer comment (jkwak, issuecomment-5011458299):** "I think `__extern_cpp` should have `KeppAliveDecoration`" (typo for KeepAliveDecoration) — endorses a **compiler-side change** rooting `__extern_cpp` fns vs DCE so they emit in `-target hpp` without `export`/entry point. This IS the Approach B/C design call. Routed to triager (Main id 11) to assess + coordinate; do NOT message fixer directly (triager owns the fixer edge — two-parent topology risk).
- **Fixer plan (id 12):** docs-only Approach A is **self-sufficient** (docs describe *current* behavior); B/C flagged as PR-body follow-ups, not implemented. Verified master HEAD 3292090b36; DCE gate `slang-ir-dce.cpp:538` confirms only `export`'s KeepAlive roots host symbol. Building slangc (worktree wt-slang-9401, DXC-from-source off) to re-confirm incantations empirically; draft PR + peer review to follow.
- **Coherence:** jkwak's KeepAliveDecoration idea ≈ the compiler follow-up the fixer already plans to flag — complementary, not in conflict. Docs PR proceeds; compiler change is a separate tracked item the triager folds in.
- **2026-07-18 triager re-assessment (id 14, reply issuecomment-5011526623):** jkwak points at right layer but there are **two independent gates**. GATE 1 (public vs export asymmetry) = a **linking-root** gap: linker roots on `HLSLExportDecoration` (`slang-ir-link.cpp:2363`→`getHLSLExports`, `slang-ir.cpp:5000-5015`), NOT KeepAlive — `-dump-ir` shows `public __extern_cpp` vanishes at link, `export` persists. So KeepAlive alone necessary-but-not-sufficient; `__extern_cpp` would need `HLSLExportDecoration`+KeepAlive (mirror CUDA/DLL/Torch arms `slang-lower-to-ir.cpp:1455-1492`). GATE 2 (needs-entrypoint) = separate `generateOutput` gate (`slang-end-to-end-request.cpp:733-751`), decoration-independent; `-whole-program` already solves it.
- **BLOCKED on maintainer decision (jkwak), 2 scope questions posted on issue:** (1) should ANY `__extern_cpp` be a host-export root, or keep opt-in via export/public? (2) no-entrypoint — document `-whole-program` (works today) or auto-whole-program design change? Fixer held (do-not-ship-docs-only relayed); PR shape depends on answers.
- **Fixer Fix Report (id 20) — HELD, no PR, both gates independently re-verified @ master 3292090b36:** fixer built slangc + ran emission matrix itself (not secondhand). Sharpened GATE 1: `__global public __extern_cpp int myGlobal;` **alone → empty**; earlier apparent emit was transitive ride-in (an `export` fn referenced it). `public` self-roots NOTHING (fn or global); `export` alone emits. Confirms linking-root (not DCE) diagnosis. Link-root source `getHLSLExports()` (`slang-ir.cpp:5000-5015` @ `slang-ir-link.cpp:2363`) enrolls only HLSLExport/DownstreamModuleExport insts; KeepAlive is DCE-liveness, does NOT enroll. So ExternCpp arm needs `HLSLExportDecoration`+KeepAlive (mirrors DLL/CUDA-device/CUDA-kernel/Torch arms `slang-lower-to-ir.cpp:1458/1464/1481/1491`). **NB the CUDA-HOST arm (1468-1473) adds ExternCpp+KeepAlive but NOT HLSLExport = exactly the insufficient shape jkwak's literal wording would produce.** Fixer concurs on both gates.
- **Parked for resume:** worktree `wt-slang-9401` (branch `fix/issue-9401`, no tracked diff; untracked build logs only) + built slangc retained. Docs fix staged: `cpu-target.md:216-225` (missing `int` return type) + line-210 prose conflating cross-module `public` with host-emission rooting. Lands aligned to jkwak's pick. No GitHub footprint from fixer (no-PR-hold; triager's comment is issue surface).
- **2026-07-18 jkwak ANSWERED (issuecomment-5013267558) — UNBLOCKED:** "two problems... focus on the first one first for now. I agree there are already similar pattern in slang-lower-to-ir.cpp. I think we should follow the pattern an add both HLSLExportDecoration and KeepAliveDecoration to `__extern_cpp`." → (1) scope to **GATE 1 only**; GATE 2 (entrypoint/`-whole-program`) **deferred**. (2) GATE 1 fix = add BOTH `HLSLExportDecoration` + `KeepAliveDecoration` to `__extern_cpp` lowering arm, mirror DllExport/CudaDeviceExport/CudaKernel/TorchEntryPoint. Routed to triager (Main id 27, in_reply_to=24) to confirm reading + re-dispatch fixer to parked worktree wt-slang-9401.
- **Triager re-dispatch confirmed (id 28), re-verified @ HEAD 203065d66; GitHub ack issuecomment-5013276247:** fix = add BOTH `HLSLExportDecoration` + `KeepAliveDecoration` to the **`ExternCppModifier` arm (`slang-lower-to-ir.cpp:1443-1446`)**, mirror DllExport/CudaDeviceExport/CudaKernel/TorchEntryPoint (NOT CudaHost — lacks HLSLExport).
- **ALL-vs-gated RESOLVED:** that arm is an else-if over modifiers → fires for ANY `__extern_cpp` decl → roots ALL `__extern_cpp` (bare/public/export) as host-callable exports. Minimal change matching docs' original `public __extern_cpp` sample. Flagged to jkwak in ack comment + fixer to call out in PR body (jkwak can narrow to keep host-export behind export/public). Blast radius LOW: `__extern_cpp` is only the syntax decl in core.meta.slang, not applied to core-module symbols.
- **2026-07-19 00:08Z STATE-CHANGE — PR #12156 OPEN + jkwak APPROVED @ red head, aarch64 golden-fail (Main-verified):** the GATE-1 fix shipped as **PR #12156** "Root `__extern_cpp` symbols as host-callable exports (#9401)" (bot, `fix/issue-9401`, non-draft, base master, mergeable_state=**behind**). **jkwak-work APPROVED at 23:49:53Z on head `f52b991add`** (pushed 23:37Z). BUT that head is RED: `test-slang` FileCheck fail on `tests/headers/generate-cuh-header.slang` across **all 4 aarch64 + linux-cpu** platforms (babysitter 00:08Z). **Assessment: almost certainly an EXPECTED GOLDEN-UPDATE, not a regression** — the PR changes what `__extern_cpp` emits into the `.cuh` header (now HLSLExport+KeepAlive-rooted), so the header-emission FileCheck output legitimately shifts → the test's CHECK lines need regenerating. Fixer-loop work, not a design bug; plausibly what jkwak expects. jkwak approving-on-red is his informed call; merge is human-gated + `behind` (can't land without rebase, which re-runs CI) → no bad-merge risk.
- **Chain ACTIVELY OWNED (do NOT dispatch — Main verified sessions 23:55–23:56Z):** fixer PR-session (gh-pr-12156), reviewer session, + #9401 fixer/triager sessions all live. CI-red→fixer via PR webhook. Routing directly = double-dispatch vs triager's fixer-edge → HELD.
- **⚠️ MY "golden-update" CALL WAS WRONG — CORRECTED (Main, 07-19 04:05Z):** I told the babysitter the aarch64 `generate-cuh-header.slang` red was "almost certainly an expected golden-update, reds expected until golden regenerated." **It was a REAL deterministic regression.** Verified: the fix (head `81d88d5`) touched **`slang-lower-to-ir.cpp` (source)** + docs + the PR's own regression test — NOT `generate-cuh-header.slang`'s golden. So the HLSLExport+KeepAlive change had an unintended struct-emission effect in the `.cuh` header; fixer corrected it in the *lowering*, not by re-baselining output. Devin + shadow-approver BLOCK + babysitter all had it right as a regression; a sibling session already retracted my framing. No harm (babysitter correctly never reran — reruns clear neither a stale golden NOR a deterministic regression), but I over-confidently classified a red I hadn't verified at the time. Lesson [[feedback_signature_grep_passed_vs_failed]]-adjacent: don't hand a coworker a confident benign-classification of a red you haven't verified — check whether the failing test is a pure CHECK-line shift vs a wrong-output before calling "golden-update," especially when downstream reviewers may hold BLOCK evidence.
- **✅ RESOLVED + green @ `81d88d5` (Main-verified 29✓/1-skip/0-fail).** State now: **jkwak's approval DISMISSED by the push → REVIEW_REQUIRED, `mergeable_state=blocked`.** So NOT await-merge — **await RE-REVIEW → then human merge** (still `behind`/needs rebase too). Chain owned by fixer/reviewer; await re-review verdict.
- **Fixer implementing (id 32):** rebased wt-slang-9401 → 203065d66, ExternCppModifier arm unchanged; applied `addHLSLExportDecoration` + `addKeepAliveDecoration` to the arm. Regression test `tests/headers/extern-cpp-public-export-9401.slang` written + confirmed FAILS pre-fix (parked slangc: `public __extern_cpp` culled, `export` emits). Incremental rebuild running (only lower-to-ir.cpp touched). **E41017 cross-interaction flagged for PR body:** change makes bare-`__extern_cpp` uninitialized globals newly E41017-exempt (fixer's #12006 `isHostProvidedGlobal` gains HLSLExport on them) — defensible, being flagged.
- **[Fix Report] id 36 — DRAFT PR #12156 open** (https://github.com/shader-slang/slang/pull/12156, `pr: non-breaking`, report_pr_created done). 3 files +7/−1: `slang-lower-to-ir.cpp` ExternCppModifier arm += `addHLSLExportDecoration`+`addKeepAliveDecoration` (mirror DLL/CUDA-device/CUDA-kernel/Torch, NOT CUDA-host); `cpu-target.md` sample += `int` return type (kept `public __extern_cpp`); new test `tests/headers/extern-cpp-public-export-9401.slang`. Repro GREEN post-fix (both `public`/`export __extern_cpp` emit hpp+cpp; empty pre-fix). `generate-hpp-header.slang` + both #12006 E41017 tests unchanged. **E41017 correction:** codex (10 rounds) caught fixer's id-32 overclaim — bare `__extern_cpp` globals were ALREADY exempt via `Export`, NOT newly; corrected in PR body. PR body flags all-vs-gated interpretation for jkwak + documents GATE 2 deferral. CI dispatched on draft.
- **jkwak APPROVED (id 42, pullrequestreview-4729516629 "Looks good to me"):** verified reviewDecision=APPROVED @ headRefOid f52b991add == fixer HEAD (approval on current commit, not stale). jkwak flipped #12156 to **non-draft himself** (his call — drafts-only guardrail is on coworkers, not maintainer). Fixer correctly holding: will NOT push (auto-dismisses approval) and will NOT merge (operator-gated; jkwak merges). slang-reviewer A/B/C pipeline still running as independent record (no GitHub post-back — no @nv-slang-bot marker on chat-chain request).
- **2026-07-19 STATE-CHANGE — PR flipped ready_for_review → shadow-approver BLOCK (RED_BUG) @ f52b991add; MY PRIOR "benign golden-update" READ RETRACTED:** approver ran full pass, recorded shadow BLOCK. **Main independently re-verified the CI facts and my earlier framing (bullets 30-31) was WRONG:** the red is NOT aarch64-only golden-drift — `test-slang` fails IDENTICALLY on all **7 platforms** (linux x86_64+aarch64, macos aarch64, debug+release) = deterministic content change, not platform golden flake. Failing test = pre-existing `tests/headers/generate-cuh-header.slang` (CHECK@9:10 `struct Struct` not found; "possible intended match here" @actual 22:17). It exercises the **collateral `__extern_cpp struct`/field path** — the fix's ExternCppModifier arm also catches structs/fields + `-no-mangle` globals, wider than jkwak's GATE-1-functions scope. New intended test `extern-cpp-public-export-9401.slang` PASSES (fix works for functions). **Contested characterization (NOT Main-adjudicated):** Devin + shadow-approver call it a real regression (struct body dropped from `.cuh`); the "possible intended match" note hints the struct text may have merely moved (→ golden-stale). **Fixer must adjudicate correct-vs-broken output; must NOT blindly regen the golden (would bake in a regression if Devin right); do NOT merge red.** Correction re-sent to babysitter reversing bullet-31 "aarch64 reds EXPECTED/benign" reclassification.
- **2026-07-19 01:03Z ADJUDICATED + SCOPE-NARROWED @ new head `81d88d57040dd5e3fb129404429348410053a2eb` (fixer id 16, Main-verified diff+CI):** fixer determination = **SEMANTICALLY CORRECT, NOT broken.** The red `.cuh` had `struct Struct` fully present (all 3 fields well-formed) but emitted BEFORE `sharedMemory` (golden expects after) + a redundant `extern "C"{}` wrapper on the global — struct+global were ALREADY on the header path pre-change; the f52b991 fix merely rooted+**reordered** them, not a golden-regen case. **Chosen path (a) scope-narrow, faithful to jkwak's "functions":** the `HLSLExport`+`KeepAlive` adds in the ExternCppModifier arm are now gated `if (as<CallableDecl>(decl))`; `addExternCppDecoration` stays unconditional so `__extern_cpp` structs/fields/globals keep unmangled names + existing emission. Golden NOT regenerated (constraint #1 honored). Fixer rebuilt debug slangc: cuh golden CHECKs pass in-order (regression resolved), hpp unchanged, function test green, both #12006 E41017 tests unchanged, codex CODE+OUTPUT re-approved. **Authoritative failing count on old head = 8 test-slang configs** (my point-in-time "7" missed windows-release-gpu which failed later; fixer listed all 8 in PR comment). Fixer POSTED on PR (issuecomment-5013610076, closest-to-state) explaining struct-path regression + functions-only fix for jkwak re-review. Verification-miss (ran cuh for compile-errors only, FileCheck absent→SIMPLE skipped) logged to shared learnings.
- **State (2026-07-19 ~01:03Z):** new head `81d88d5704` pushed (force over fixer's own f52b991; no maintainer commit on branch). **jkwak's APPROVED auto-dismissed** → reviewDecision=REVIEW_REQUIRED (expected — fixed a red he approved over). CI on new head: 0 failing / 11 pending of 22 at push time; **`test-slang` not yet reported** (the jobs that held the regression). **Approver RE-DISPATCHED** (thread gh-issue-…-12156) to run a fresh shadow pass once CI settles / `test-slang` green — deliberately NOT forwarded the fixer's "correct-not-broken" conclusion so its adversarial pass stays independent. GATE 1 functions-only shipped; GATE 2 = documented follow-up. Chain OPEN — awaiting: (1) green CI on 81d88d5704, (2) jkwak re-review, (3) approver refreshed verdict, then human merge (operator/maintainer-gated). Canonical thread `gh-issue-shader-slang/slang-9401`.
