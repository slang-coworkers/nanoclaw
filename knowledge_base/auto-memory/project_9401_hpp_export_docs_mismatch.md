---
name: project_9401_hpp_export_docs_mismatch
description: "shader-slang/slang#9401 -target hpp export/docs mismatch — triaged P3, docs-only fix routing to fixer"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0574abae-931e-41dc-9682-6e980e9d381d
---

# #9401 `-target hpp` export requires entrypoint + docs mismatch — TRIAGED → fixer

Maintainer **jkwak-work** requested triage (2026-07-18, issuecomment-5011335733). Chain: orchestrator → slang-triager → slang-fixer (docs PR).

- **Classification:** bug (docs + behavior/UX) / **low / P3** / docs+CPU-C++-hpp-emit+frontend-visibility.
- **Two problems:** (1) `docs/cpu-target.md:216-225` sample malformed (missing `int` return type; uses `public`); (2) exported fns only appear in `-target hpp` header when marked `export` (not `public`) with ≥1 entry point.
- **Reproduced on ToT** (v2026.13.1 / aaa07fe29), 7 slangc runs. Key correction to report premise: **dummy entrypoint NOT actually required** — `-whole-program` already exports `export __extern_cpp` fns with no entry point. `public` never roots vs DCE (only `export`'s HLSLExportModifier adds KeepAlive).
- **Recommended fix = Approach A (docs-only):** add return type, use/explain `export`, document `-whole-program`. Zero compiler risk. Approaches B (`public` roots like `export`) and C (scope header emit-filter) flagged as **maintainer design calls — NOT auto-implemented**.
- **Files:** `docs/cpu-target.md:216-225`; `slang-lower-to-ir.cpp:1434-1442`; `slang-emit-c-like.cpp:5392-5396`.
- **GitHub:** verified 5-bullet posted (issuecomment-5011426913), `reproduced` label applied, Type=Bug (human-set). Triager owns triager→fixer handoff via direct edge; Main did NOT double-dispatch.
- **2026-07-18 maintainer comment (jkwak, issuecomment-5011458299):** "I think `__extern_cpp` should have `KeppAliveDecoration`" (typo for KeepAliveDecoration) — endorses a **compiler-side change** rooting `__extern_cpp` fns vs DCE so they emit in `-target hpp` without `export`/entry point. This IS the Approach B/C design call. Routed to triager (Main id 11) to assess + coordinate; do NOT message fixer directly (triager owns the fixer edge — two-parent topology risk).
- **Fixer plan (id 12):** docs-only Approach A is **self-sufficient** (docs describe *current* behavior); B/C flagged as PR-body follow-ups, not implemented. Verified master HEAD 3292090b36; DCE gate `slang-ir-dce.cpp:538` confirms only `export`'s KeepAlive roots host symbol. Building slangc (worktree wt-slang-9401, DXC-from-source off) to re-confirm incantations empirically; draft PR + peer review to follow.
- **Coherence:** jkwak's KeepAliveDecoration idea ≈ the compiler follow-up the fixer already plans to flag — complementary, not in conflict. Docs PR proceeds; compiler change is a separate tracked item the triager folds in.
- **2026-07-18 triager re-assessment (id 14, reply issuecomment-5011526623):** jkwak points at right layer but there are **two independent gates**. GATE 1 (public vs export asymmetry) = a **linking-root** gap: linker roots on `HLSLExportDecoration` (`slang-ir-link.cpp:2363`→`getHLSLExports`, `slang-ir.cpp:5000-5015`), NOT KeepAlive — `-dump-ir` shows `public __extern_cpp` vanishes at link, `export` persists. So KeepAlive alone necessary-but-not-sufficient; `__extern_cpp` would need `HLSLExportDecoration`+KeepAlive (mirror CUDA/DLL/Torch arms `slang-lower-to-ir.cpp:1455-1492`). GATE 2 (needs-entrypoint) = separate `generateOutput` gate (`slang-end-to-end-request.cpp:733-751`), decoration-independent; `-whole-program` already solves it.
- **BLOCKED on maintainer decision (jkwak), 2 scope questions posted on issue:** (1) should ANY `__extern_cpp` be a host-export root, or keep opt-in via export/public? (2) no-entrypoint — document `-whole-program` (works today) or auto-whole-program design change? Fixer held (do-not-ship-docs-only relayed); PR shape depends on answers.
- **Fixer Fix Report (id 20) — HELD, no PR, both gates independently re-verified @ master 3292090b36:** fixer built slangc + ran emission matrix itself (not secondhand). Sharpened GATE 1: `__global public __extern_cpp int myGlobal;` **alone → empty**; earlier apparent emit was transitive ride-in (an `export` fn referenced it). `public` self-roots NOTHING (fn or global); `export` alone emits. Confirms linking-root (not DCE) diagnosis. Link-root source `getHLSLExports()` (`slang-ir.cpp:5000-5015` @ `slang-ir-link.cpp:2363`) enrolls only HLSLExport/DownstreamModuleExport insts; KeepAlive is DCE-liveness, does NOT enroll. So ExternCpp arm needs `HLSLExportDecoration`+KeepAlive (mirrors DLL/CUDA-device/CUDA-kernel/Torch arms `slang-lower-to-ir.cpp:1458/1464/1481/1491`). **NB the CUDA-HOST arm (1468-1473) adds ExternCpp+KeepAlive but NOT HLSLExport = exactly the insufficient shape jkwak's literal wording would produce.** Fixer concurs on both gates.
- **Parked for resume:** worktree `wt-slang-9401` (branch `fix/issue-9401`, no tracked diff; untracked build logs only) + built slangc retained. Docs fix staged: `cpu-target.md:216-225` (missing `int` return type) + line-210 prose conflating cross-module `public` with host-emission rooting. Lands aligned to jkwak's pick. No GitHub footprint from fixer (no-PR-hold; triager's comment is issue surface).
- **2026-07-18 jkwak ANSWERED (issuecomment-5013267558) — UNBLOCKED:** "two problems... focus on the first one first for now. I agree there are already similar pattern in slang-lower-to-ir.cpp. I think we should follow the pattern an add both HLSLExportDecoration and KeepAliveDecoration to `__extern_cpp`." → (1) scope to **GATE 1 only**; GATE 2 (entrypoint/`-whole-program`) **deferred**. (2) GATE 1 fix = add BOTH `HLSLExportDecoration` + `KeepAliveDecoration` to `__extern_cpp` lowering arm, mirror DllExport/CudaDeviceExport/CudaKernel/TorchEntryPoint. Routed to triager (Main id 27, in_reply_to=24) to confirm reading + re-dispatch fixer to parked worktree wt-slang-9401.
- **Triager re-dispatch confirmed (id 28), re-verified @ HEAD 203065d66; GitHub ack issuecomment-5013276247:** fix = add BOTH `HLSLExportDecoration` + `KeepAliveDecoration` to the **`ExternCppModifier` arm (`slang-lower-to-ir.cpp:1443-1446`)**, mirror DllExport/CudaDeviceExport/CudaKernel/TorchEntryPoint (NOT CudaHost — lacks HLSLExport).
- **ALL-vs-gated RESOLVED:** that arm is an else-if over modifiers → fires for ANY `__extern_cpp` decl → roots ALL `__extern_cpp` (bare/public/export) as host-callable exports. Minimal change matching docs' original `public __extern_cpp` sample. Flagged to jkwak in ack comment + fixer to call out in PR body (jkwak can narrow to keep host-export behind export/public). Blast radius LOW: `__extern_cpp` is only the syntax decl in core.meta.slang, not applied to core-module symbols.
- **2026-07-19 00:08Z STATE-CHANGE — PR #12156 OPEN + jkwak APPROVED @ red head, aarch64 golden-fail (Main-verified):** the GATE-1 fix shipped as **PR #12156** "Root `__extern_cpp` symbols as host-callable exports (#9401)" (bot, `fix/issue-9401`, non-draft, base master, mergeable_state=**behind**). **jkwak-work APPROVED at 23:49:53Z on head `f52b991add`** (pushed 23:37Z). BUT that head is RED: `test-slang` FileCheck fail on `tests/headers/generate-cuh-header.slang` across **all 4 aarch64 + linux-cpu** platforms (babysitter 00:08Z). **Assessment: almost certainly an EXPECTED GOLDEN-UPDATE, not a regression** — the PR changes what `__extern_cpp` emits into the `.cuh` header (now HLSLExport+KeepAlive-rooted), so the header-emission FileCheck output legitimately shifts → the test's CHECK lines need regenerating. Fixer-loop work, not a design bug; plausibly what jkwak expects. jkwak approving-on-red is his informed call; merge is human-gated + `behind` (can't land without rebase, which re-runs CI) → no bad-merge risk.
- **Chain ACTIVELY OWNED (do NOT dispatch — Main verified sessions 23:55–23:56Z):** fixer PR-session (gh-pr-12156), reviewer session, + #9401 fixer/triager sessions all live. CI-red→fixer via PR webhook. Routing directly = double-dispatch vs triager's fixer-edge → HELD.
- **⚠️ MY "golden-update" CALL WAS WRONG — CORRECTED (Main, 07-19 04:05Z):** I told the babysitter the aarch64 `generate-cuh-header.slang` red was "almost certainly an expected golden-update, reds expected until golden regenerated." **It was a REAL deterministic regression.** Verified: the fix (head `81d88d5`) touched **`slang-lower-to-ir.cpp` (source)** + docs + the PR's own regression test — NOT `generate-cuh-header.slang`'s golden. So the HLSLExport+KeepAlive change had an unintended struct-emission effect in the `.cuh` header; fixer corrected it in the *lowering*, not by re-baselining output. Devin + shadow-approver BLOCK + babysitter all had it right as a regression; a sibling session already retracted my framing. No harm (babysitter correctly never reran — reruns clear neither a stale golden NOR a deterministic regression), but I over-confidently classified a red I hadn't verified at the time. Lesson [[feedback_signature_grep_passed_vs_failed]]-adjacent: don't hand a coworker a confident benign-classification of a red you haven't verified — check whether the failing test is a pure CHECK-line shift vs a wrong-output before calling "golden-update," especially when downstream reviewers may hold BLOCK evidence.
- **✅ RESOLVED + green @ `81d88d5` (Main-verified 29✓/1-skip/0-fail).** State now: **jkwak's approval DISMISSED by the push → REVIEW_REQUIRED, `mergeable_state=blocked`.** So NOT await-merge — **await RE-REVIEW → then human merge** (still `behind`/needs rebase too). Chain owned by fixer/reviewer; await re-review verdict.
- **Fixer implementing (id 32):** rebased wt-slang-9401 → 203065d66, ExternCppModifier arm unchanged; applied `addHLSLExportDecoration` + `addKeepAliveDecoration` to the arm. Regression test `tests/headers/extern-cpp-public-export-9401.slang` written + confirmed FAILS pre-fix (parked slangc: `public __extern_cpp` culled, `export` emits). Incremental rebuild running (only lower-to-ir.cpp touched). **E41017 cross-interaction flagged for PR body:** change makes bare-`__extern_cpp` uninitialized globals newly E41017-exempt (fixer's #12006 `isHostProvidedGlobal` gains HLSLExport on them) — defensible, being flagged.
- **[Fix Report] id 36 — DRAFT PR #12156 open** (https://github.com/shader-slang/slang/pull/12156, `pr: non-breaking`, report_pr_created done). 3 files +7/−1: `slang-lower-to-ir.cpp` ExternCppModifier arm += `addHLSLExportDecoration`+`addKeepAliveDecoration` (mirror DLL/CUDA-device/CUDA-kernel/Torch, NOT CUDA-host); `cpu-target.md` sample += `int` return type (kept `public __extern_cpp`); new test `tests/headers/extern-cpp-public-export-9401.slang`. Repro GREEN post-fix (both `public`/`export __extern_cpp` emit hpp+cpp; empty pre-fix). `generate-hpp-header.slang` + both #12006 E41017 tests unchanged. **E41017 correction:** codex (10 rounds) caught fixer's id-32 overclaim — bare `__extern_cpp` globals were ALREADY exempt via `Export`, NOT newly; corrected in PR body. PR body flags all-vs-gated interpretation for jkwak + documents GATE 2 deferral. CI dispatched on draft.
- **jkwak APPROVED (id 42, pullrequestreview-4729516629 "Looks good to me"):** verified reviewDecision=APPROVED @ headRefOid f52b991add == fixer HEAD (approval on current commit, not stale). jkwak flipped #12156 to **non-draft himself** (his call — drafts-only guardrail is on coworkers, not maintainer). Fixer correctly holding: will NOT push (auto-dismisses approval) and will NOT merge (operator-gated; jkwak merges). slang-reviewer A/B/C pipeline still running as independent record (no GitHub post-back — no @nv-slang-bot marker on chat-chain request).
- **2026-07-19 STATE-CHANGE — PR flipped ready_for_review → shadow-approver BLOCK (RED_BUG) @ f52b991add; MY PRIOR "benign golden-update" READ RETRACTED:** approver ran full pass, recorded shadow BLOCK. **Main independently re-verified the CI facts and my earlier framing (bullets 30-31) was WRONG:** the red is NOT aarch64-only golden-drift — `test-slang` fails IDENTICALLY on all **7 platforms** (linux x86_64+aarch64, macos aarch64, debug+release) = deterministic content change, not platform golden flake. Failing test = pre-existing `tests/headers/generate-cuh-header.slang` (CHECK@9:10 `struct Struct` not found; "possible intended match here" @actual 22:17). It exercises the **collateral `__extern_cpp struct`/field path** — the fix's ExternCppModifier arm also catches structs/fields + `-no-mangle` globals, wider than jkwak's GATE-1-functions scope. New intended test `extern-cpp-public-export-9401.slang` PASSES (fix works for functions). **Contested characterization (NOT Main-adjudicated):** Devin + shadow-approver call it a real regression (struct body dropped from `.cuh`); the "possible intended match" note hints the struct text may have merely moved (→ golden-stale). **Fixer must adjudicate correct-vs-broken output; must NOT blindly regen the golden (would bake in a regression if Devin right); do NOT merge red.** Correction re-sent to babysitter reversing bullet-31 "aarch64 reds EXPECTED/benign" reclassification.
- **2026-07-19 01:03Z ADJUDICATED + SCOPE-NARROWED @ new head `81d88d57040dd5e3fb129404429348410053a2eb` (fixer id 16, Main-verified diff+CI):** fixer determination = **SEMANTICALLY CORRECT, NOT broken.** The red `.cuh` had `struct Struct` fully present (all 3 fields well-formed) but emitted BEFORE `sharedMemory` (golden expects after) + a redundant `extern "C"{}` wrapper on the global — struct+global were ALREADY on the header path pre-change; the f52b991 fix merely rooted+**reordered** them, not a golden-regen case. **Chosen path (a) scope-narrow, faithful to jkwak's "functions":** the `HLSLExport`+`KeepAlive` adds in the ExternCppModifier arm are now gated `if (as<CallableDecl>(decl))`; `addExternCppDecoration` stays unconditional so `__extern_cpp` structs/fields/globals keep unmangled names + existing emission. Golden NOT regenerated (constraint #1 honored). Fixer rebuilt debug slangc: cuh golden CHECKs pass in-order (regression resolved), hpp unchanged, function test green, both #12006 E41017 tests unchanged, codex CODE+OUTPUT re-approved. **Authoritative failing count on old head = 8 test-slang configs** (my point-in-time "7" missed windows-release-gpu which failed later; fixer listed all 8 in PR comment). Fixer POSTED on PR (issuecomment-5013610076, closest-to-state) explaining struct-path regression + functions-only fix for jkwak re-review. Verification-miss (ran cuh for compile-errors only, FileCheck absent→SIMPLE skipped) logged to shared learnings.
- **State (2026-07-19 ~01:03Z):** new head `81d88d5704` pushed (force over fixer's own f52b991; no maintainer commit on branch). **jkwak's APPROVED auto-dismissed** → reviewDecision=REVIEW_REQUIRED (expected — fixed a red he approved over). CI on new head: 0 failing / 11 pending of 22 at push time; **`test-slang` not yet reported** (the jobs that held the regression). **Approver RE-DISPATCHED** (thread gh-issue-…-12156) to run a fresh shadow pass once CI settles / `test-slang` green — deliberately NOT forwarded the fixer's "correct-not-broken" conclusion so its adversarial pass stays independent. GATE 1 functions-only shipped; GATE 2 = documented follow-up. Chain OPEN — awaiting: (1) green CI on 81d88d5704, (2) jkwak re-review, (3) approver refreshed verdict, then human merge (operator/maintainer-gated). Canonical thread `gh-issue-shader-slang/slang-9401`.
- **2026-07-19 ~01:17Z — CI GREEN on `81d88d5704` (Main-verified): all 16 test-slang/test-slang-rhi legs SUCCESS** (linux/macos/windows × debug/release × x86_64/aarch64) → scope-narrow fix confirmed resolving the 8-platform regression; approver's R2 decision-hinge satisfied (its test-slang monitor should fire → finalize R2). reviewDecision=REVIEW_REQUIRED, mergeable.
- **2026-07-19 ~01:17Z — SUBSTANTIVE HUMAN QUESTION extends chain (pdeayton-nv, issuecomment-5025894976, real @nv-slang-bot mention on PR #12156):** *"does the PR introduce any regressions? GLSL output may be affected if a module has e.g. `public __extern_cpp int main(int)` with a compute shader using `void main()`. Shaders using `[__extern] __extern_cpp` also may generate an error now?"* Two blast-radius concerns for the fixer to answer EMPIRICALLY (NOT Main-adjudicated): (1) `__extern_cpp` fn named `main` now HLSLExport-rooted → GLSL entry-point `main` collision risk; (2) `[__extern] __extern_cpp` (body-less extern) now KeepAlive+HLSLExport → may error at link/emit ("no definition"). Routed to triager→fixer on canonical thread `gh-issue-…-9401`; post-back AUTHORIZED (genuine bot mention). Chain re-opened for this Q; not a close.
- **2026-07-20 ~19:20Z — pdeayton's 2 concerns RESOLVED empirically @ new head `e3a5e7027bbc9b0b7a8a23e9d2c417224b98c3f3` (fixer id 52, posted issuecomment-5026223001; Main-verified diff):** fixer built pre-change vs post-change slangc, `-target glsl -stage compute`. **(b) `[__extern] __extern_cpp` bodyless = REAL regression, NOW FIXED.** Pre-change clean exit 0; first-fix (f52b→81d88) `error E45001 unresolved external symbol` (exit 255) — the `as<CallableDecl>` gate did NOT exclude imported/extern decls, so bodyless imported fn got Import+UserExtern AND HLSLExport+KeepAlive (retained root, no local def → linker unresolved) — exactly triager's source-level lead. **Fix: gate now `as<CallableDecl>(decl) && !isImported`** (isImported hoisted from `isImportedDecl(...)`); post-fix clean exit 0; control `[__extern] export` still E45001 (separate arm, correct/unchanged). New regression test `tests/headers/extern-cpp-imported-9401.slang`. **(a) GLSL `main` collision = NOT a new failure mode.** `public __extern_cpp int main(int)` now emits alongside `void main()` BYTE-IDENTICAL (SHA-256) to what `export __extern_cpp int main` already emits on master — the change makes `public`≡`export` (intended #9401 fix), inheriting export's pre-existing GPU quirk. Honestly framed: pre-change public-main was culled so the collision IS newly surfaced for `public`, but it's export's pre-existing behavior, not a new mode; offered a tracking-issue, scope deferred to jkwak/pdeayton. No code change for (a). Post-fix verified: (b) test clean, function test green, cuh+hpp goldens unchanged, E41017 unchanged, codex CODE+OUTPUT re-approved.
- **State (2026-07-20 ~19:20Z):** new head `e3a5e7027b` pushed → auto-triggers pull_request CI (non-draft); jkwak approval already dismissed by prior push, re-review pending; mergeable/REVIEW_REQUIRED; not merging (jkwak's call). **Approver RE-DISPATCHED (R3)** on thread gh-issue-…-12156 to re-run once CI settles. Chain OPEN — awaiting: (1) green CI on e3a5e7027b, (2) approver R3 verdict, (3) jkwak re-review + pdeayton (a)-scope call, then human merge.
- **2026-07-20 CI GREEN on `e3a5e7027b` (Main-verified): all 16 test-slang/-rhi legs SUCCESS** (linux/macos/windows × debug/release × x86_64/aarch64) — approver R3 CI-hinge satisfied.
- **2026-07-20 — pdeayton-nv DESIGN OBJECTION (issuecomment-5038630211, genuine @nv-slang-bot mention) — deeper than a blast-radius Q; likely jkwak design call:** two parts. (1) **Empirical:** test with `-emit-spirv-via-glsl` (glslang path validates generated GLSL) — pdeayton believes the fix is *extending* the pre-existing invalid `export __extern_cpp` GLSL bug to `public __extern_cpp` (previously valid). (2) **Design critique (the weight):** the fix makes `__extern_cpp` a **target-neutral export modifier** (`HLSLExportDecoration` roots the symbol for EVERY target), but #9401 is specifically about **host C++/HPP** output. Proposed alt: keep `ExternCppDecoration` as naming metadata, select defined `public __extern_cpp` fns as additional roots **only during host-target linking** — fixes HPP/CPP without extending invalid export-GLSL to previously-valid `public`. **This CONTRADICTS the fixer's "(a) = intended public≡export unification of a pre-existing quirk" framing** — pdeayton's point is the quirk shouldn't be inherited; rooting should be host-target-scoped. Routed triager→fixer (empirical `-emit-spirv-via-glsl` test + evaluate host-target-scoped alt design) + alerted approver to NOT finalize WOULD_APPROVE over a live design objection (tilts ABSTAIN_POLICY = maintainer design decision pending). Post-back AUTHORIZED. Chain OPEN, re-opened for this objection.
- **2026-07-21 — APPROVER R3 VERDICT = ABSTAIN_POLICY (CHALLENGER_CONCERN) @ `e3a5e7027bbc`, ledger-only (supersedes R1 BLOCK + R2 undecided):** full arc — R1 (f52b991) BLOCK unconditional over-rooting → 3-platform generate-cuh regression; R2 (81d88d5) `as<CallableDecl>` gate fixed cuh but left E45001 on bodyless `[__extern] __extern_cpp` (undecided, session ended); R3 (e3a5e70) gate `as<CallableDecl>(decl) && !isImported` → both regressions fixed, all 16 test-slang green, Devin head-current clean. **Why ABSTAIN not APPROVE:** CI-green satisfied behavioral hinge BUT pdeayton-nv's live maintainer design objection (target-neutral HLSLExport rooting vs #9401's host-target-only need; extends latent export-GLSL quirk to previously-valid public) is a design acceptability call the shadow can neither clear nor refute → never rounds up over unresolved design objection; not BLOCK (no verified 🔴). Verdict pends jkwak design decision + fixer `-emit-spirv-via-glsl` result. **KEY CALIBRATION: shadow challenger MISSED R2's E45001** (cleared Devin's imported-decl flag as benign) — **human pdeayton-nv caught the real regression**; captured as challenger-miss learning. Critique gate caught a premature "CI green" (late x86_64 legs) → corrected; stale Devin fetches discarded. 4 learnings captured across chain.
- **State (2026-07-21):** pdeayton-nv now MEMBER (weightier voice). Approver R3=ABSTAIN_POLICY recorded ledger-only. Chain OPEN — awaiting: (1) fixer's empirical `-emit-spirv-via-glsl` PRE/POST result (does glslang path now FAIL for `public` where it didn't pre-change — would confirm regression, overturning fixer's earlier "(a) benign" framing which fixer already conceded); (2) fixer's host-target-scoped rooting recommendation (triager grounded feasible: linkIR `targetReq` @48/1718, `doesTargetAllowUnresolvedFuncSymbol` precedent incl HostCPPSource @1842, `getHLSLExports` @2363 runs inside linkIR); (3) jkwak design decision (owns target-neutral-vs-host-scoped call). If glsl prediction confirmed → current HLSLExport approach likely needs host-scoped redesign before merge. NO merge over open MEMBER design objection. Canonical thread `gh-issue-shader-slang/slang-9401`.
- **2026-07-21 20:56Z — GLSL REGRESSION CONFIRMED empirically; pdeayton RIGHT; fixer retracted (id 76, PR comment 5038963190 Main-verified @author nv-slang-bot[bot]):** fixer built slang-glslang, ran `-emit-spirv-via-glsl` (glslang validates emitted GLSL), PRE=master vs POST=e3a5e7027b. **`public __extern_cpp int main(int)`+compute: PRE glslang ACCEPTS (valid SPIR-V, main(int) culled) → POST glslang REJECTS "'main': function cannot take any parameter(s)".** `export __extern_cpp int main(int)`: glslang rejects BOTH pre+post (the pre-existing latent bug). **CONCLUSION: PR takes previously-VALID `public __extern_cpp` GLSL and makes it INVALID — extends export's latent GPU bug to public = REAL regression.** Fixer's earlier "(a) byte-identical/not-a-new-failure-mode" framing WRONG, retracted on PR. **DESIGN: pdeayton's host-target-scoped alternative = the right fix (fixer confirmed feasible + recommends):** root cause = HLSLExport added at lowering is TARGET-NEUTRAL → link-root collection slang-ir-link.cpp:2363 roots for EVERY target incl GLSL/SPIR-V. Fix = keep ExternCppDecoration naming-only, add defined `public __extern_cpp` CallableDecls as roots ONLY at host-target linking (targetReq available @2363 via context->getShared()->targetReq; precedent doesTargetAllowUnresolvedFuncSymbol @1842; reuse isCPUTarget @slang-target.h:89). Fixes HPP/CPP with ZERO GPU side-effect, more principled. **Fixer HELD for jkwak's design confirmation before rewriting** (root moves lowering→host-gated link = design-direction change; did NOT unilaterally rewrite per Main instruction). Worktree @ e3a5e7027b clean.
- **State (2026-07-21 ~20:56Z) — BLOCKED on jkwak design decision (external human):** chain OPEN, awaiting jkwak's direction on host-target-scoped rooting (surfaced on PR by fixer, closest-to-state — no action owed by Main; jkwak replies via webhook). If jkwak says go → fixer implements host-scoped rooting, codex-gates, re-verifies glslang PASS for public + goldens, re-pushes → new head → approver re-runs. **Chain vindicated:** R1 BLOCK + R3 ABSTAIN_POLICY + "don't round up over design objection / don't merge over it" all correct — a clean approve would have shipped this GLSL regression (human pdeayton caught what shadow challenger missed at R2). GATE 2 still documented follow-up. NO merge until jkwak resolves. Canonical thread `gh-issue-shader-slang/slang-9401`.
- **2026-07-22 01:44Z — pdeayton-nv (MEMBER) GREEN-LIGHTS host-scoped fix as NEW PR (issuecomment-5040893377, genuine mention):** *"could you create a new PR with the host-target-scoped fix?"* jkwak has NOT replied on design since 07-19 (thread verified: last jkwak-directed = bot's 07-19 re-dismissal heads-up), but pdeayton (repo MEMBER, objection author who empirically confirmed the regression) is directly directing the implementation → legitimate go-ahead on the host-scoped direction (the more conservative/correct fix both already agreed on). Routed triager→fixer to implement host-target-scoped rooting (keep ExternCppDecoration naming-only; add defined `public __extern_cpp` CallableDecls as roots ONLY at host-target linking — targetReq @slang-ir-link.cpp:2363, doesTargetAllowUnresolvedFuncSymbol @1842, isCPUTarget @slang-target.h:89) as a **NEW draft PR** (drafts-only guardrail), superseding #12156's target-neutral approach. Verify glslang PASS for `public` (the confirmed-fail case) + cuh/hpp goldens + E45001 bodyless + function test; codex-gate; report_pr_created; post-back authorized. **#12156 stays OPEN — do NOT auto-close** (human decides; new PR references it as superseding). Chain OPEN, resumed → implementation.
- **2026-07-22 — jhelferty-nv REASSIGNED #12156 + #9401 to pdeayton-nv (issuecomment-5072766335, "since you've been the most active on it"):** administrative/housekeeping — @-mentions pdeayton NOT our bot, asks nothing, doesn't change in-flight work. Formalizes pdeayton (MEMBER) as assignee/owner of both PR + issue = the review destination when the new host-scoped PR lands. No dispatch, no bot GitHub post, no interrupt of fixer's in-flight implementation. Context only. Fixer still building new host-scoped draft PR (~30-40min ETA from 01:49Z).
- **2026-07-27 — STALLED HANDOFF, fixer went dark; pdeayton chasing (issuecomment-5096768608 "do you have an updated PR with the host-target-scoped fix ready?"). Main-verified GitHub state:** NO host-scoped branch or PR exists — `fix/issue-9401` still @ old head `e3a5e7027b` (07-18), no branch matching 9401/host/extern/scoped. Fixer acked dispatch 07-22 01:49Z (~30-40min ETA, promised new PR#+glslang-PASS) then never reported back + never created the PR = ~5-day stall. #12156 still OPEN, assignee=pdeayton-nv. Per stalled-handoff discipline (I own the chase, no self-heal): re-dispatched triager→fixer with URGENCY to build the host-scoped fix + open the new draft PR now, and to post a brief holding ack to pdeayton on #12156 (closest-to-state) so the MEMBER reviewer isn't left waiting. If fixer stays dark → escalate (fixer-down history: project_slang_fixer_auth_outage). Chain OPEN, chasing.
- **2026-07-27 22:04Z — STALL RESOLVED, host-scoped PR #12242 UP (Main-verified GitHub + triager source-verified):** fixer recovered its 07-22 worktree (per-task drop, group was ALIVE per ncl sessions — NOT fixer-down), rebased to master, opened **PR #12242** "Root `public __extern_cpp` functions as host-callable exports at CPU-target link (#9401)" — DRAFT, OPEN, base master, branch `fix/issue-9401-hostscope`, head `85da5d8e7a42b78648a9bfc1ed380597dc825862`, `Closes #9401`, `pr: non-breaking`, supersedes #12156 explicitly, credits pdeayton. 7 files: slang-ir.{h,cpp} + slang-ir-link.cpp + docs/cpu-target.md + 3 tests. `report_pr_created` done (webhooks → this session). **Design shape (triager source-verified):** ExternCppModifier lowering arm now naming-ONLY (target-neutral HLSLExport/KeepAlive REMOVED); collection slang-ir.cpp:5033 adds only `isExternCpp && isPublic` funcs that are `IRFunc && isDefinition()`; rooting gated `isCPUTarget(targetReq)` at link slang-ir-link.cpp:2383 → host-scoped, zero GPU side-effect = pdeayton's design. **PREDICATE NARROWED (flagged, NOT silent):** roots only DEFINED `public __extern_cpp` funcs — NOT all-`__extern_cpp` (the interp jkwak green-lit for #12156). Bare `__extern_cpp` stays private; `export __extern_cpp` uses existing path; body-less `[__extern]` excluded (no E45001). Stated explicitly in PR body → jkwak/pdeayton confirm narrowing on review.
- **State (2026-07-27 ~22:06Z):** #12242 DRAFT, CI early (builds pending, NOT green). #12156 stays OPEN (human closes), assignee pdeayton-nv. **Fixer still owes (triager watching):** (1) post #12242 link as reply to pdeayton on #12156 (last comment there = triager's holding reply); (2) formal [Fix Report] with decisive glslang-PASS proof (`public __extern_cpp int main(int)`+compute PASSES `-emit-spirv-via-glsl` = the case that regressed on #12156). Then: CI-green verify → triager refreshes issue verdict → point at #12242 → approver runs on #12242 when flipped ready. Chain OPEN — stall resolved, awaiting fixer's pdeayton-reply + Fix Report + CI. Canonical thread `gh-issue-shader-slang/slang-9401`.
- **2026-07-27 ~22:07Z — FIX REPORT complete, both receipts Main-verified; chain PARKED healthy awaiting human review:** (1) **glslang-PASS proof:** fixer built slangc+slang-glslang, ran the exact #12156-regressed case `public __extern_cpp int main(int)`+compute `void main()` → `-emit-spirv-via-glsl` → **glslang ACCEPTS** (valid SPIR-V) — regression FIXED (#12156 rejected it). (2) **pdeayton reply verified** (issuecomment-5097321568, nv-slang-bot[bot] @22:06:23Z on #12156) — MEMBER reviewer has #12242 link. Full verify (master build): glslang PASS · public+export emit hpp · bare non-public `__extern_cpp` NOT emitted (public-gate = codex must-fix catch) · `export __extern_cpp` still rooted via getHLSLExports · `[__extern]` bodyless 0 E45001 · cuh/hpp goldens unchanged. Impl: new `m_hostExternCppFuncs` cache mirroring getHLSLExports pattern, rooted only when `isCPUTarget(targetReq)` in linkIR. codex CODE+OUTPUT approved. **CI "fail" = BENIGN:** only failing check on head 85da5d8e7 is `check-ci` (draft wait-for-human-priority gate yield = cosmetic bot-PR priority-yield red, per project_bot_pr_priority_yield_red_run); 29/30 non-failing, no real build/test fail. **State: #12242 DRAFT, verified-green(modulo cosmetic check-ci), awaiting jkwak/pdeayton review via webhook.** NOT merging (jkwak's), NOT closing #12156 (human's). Approver runs on #12242 when flipped ready. Predicate-narrowing (defined-public-only vs #12156's all-__extern_cpp) stated in PR body for maintainer confirm. Chain OPEN, healthy, human-review-gated. Nothing owed by Main/fixer until review verdicts or real CI arrive.
- **2026-07-27 — jkwak-work (MAINTAINER, design owner) asks for the regression repro (issuecomment-5097545210, genuine mention on #12156):** *"I am not sure what regression on GLSL we are talking about. Can you give me a simple example that demonstrate the regression?"* High-value — jkwak green-lit #12156's broader all-`__extern_cpp` approach; he needs to SEE the GLSL regression before ruling on target-neutral-vs-host-scoped. Routed triager→fixer to post a MINIMAL self-contained repro on #12156 (the thread jkwak asked on), post-back AUTHORIZED. Canonical repro (fixer has it): `public __extern_cpp int main(int){...}` + compute `[shader("compute")]/[numthreads]void main(){}`, compile `-target glsl -stage compute` (or `-emit-spirv-via-glsl` for glslang validation) → PRE-#12156 (master) glslang ACCEPTS (main(int) culled) → ON-#12156 head glslang REJECTS "'main': function cannot take any parameter(s)". Root: #12156's target-neutral HLSLExport roots `public __extern_cpp` for EVERY target incl GLSL → emits invalid GLSL that was previously valid (culled). #12242 host-scoped fix = the answer. Fixer should give exact source + cmdline + both outputs so jkwak can repro in one paste. Chain OPEN — maintainer engaged, this is the design-decision juncture.
- **2026-07-27 22:43Z — FIXER FLAGGED VERIFICATION SNAG (id 124), potential IN-PROGRESS RECANT of the e3a5e7027b-REJECT finding — NOT yet resolved, do NOT relay as fact:** fixer says on its current build of e3a5e7027b (narrowed #12156) it CANNOT reproduce the GLSL rejection — claims `public __extern_cpp` there "gets 0 hlslExport"; believes the regression was only on the ORIGINAL BROAD f52b991 (all-__extern_cpp), rebuilding f52b991 to reproduce authentically before posting to jkwak; will report honestly even if it doesn't repro. **Main CAUTION FLAGGED to fixer (id-124 edge):** "0 hlslExport on e3a5e7027b" CONTRADICTS (a) fixer's OWN msg-52 verify (HPP function test GREEN on e3a5e7027b → public __extern_cpp EMITS → REQUIRES rooting/HLSLExport) and (b) the gate logic (`CallableDecl && !isImported`=TRUE for defined public main; HLSLExport target-neutral → roots GLSL too → unmangled main collides → REJECT, = fixer's own msg-76). → strong signal fixer's CURRENT binary isn't actually e3a5e7027b / wrong flags. Also: **public PR comment issuecomment-5038963190 (07-21) claimed regression reproduced ON e3a5e7027b — jkwak has READ it**; if conclusion changes, that public claim needs correction so the record isn't self-contradictory. Design point for jkwak stands regardless (#12242 host-scoped rooting correct because target-neutral rooting emits public fns into GLSL). AWAITING fixer's authentic commit-pinned repro (ACCEPT@master vs REJECT@<which commit>). Verdict NOT in hand.
- **2026-07-27 23:27Z — pdeayton-nv TEST-COVERAGE feedback on #12242 (issuecomment-5097989268, posted on #12156 thread but about #12242 code):** real gap — #12242's test `importedFunc` isn't `public`, so `_build()` rejects it at the `isExternCpp && isPublic` gate BEFORE the new `isDefinition()` check runs → the definition condition is UNTESTED. pdeayton's fix: make it `[__extern] public __extern_cpp int importedFunc(int a);` (reaches definition path), + add a DEFINED non-public `__extern_cpp` fn, and use `CHECK-NOT` on BOTH names so visibility-gate and definition-gate are covered INDEPENDENTLY. Actionable, correct. Routed triager→fixer to update the #12242 test (draft, head 85da5d8e7), re-verify, push; post-back authorized. NOTE this is a SECOND concurrent fixer task on this chain alongside the jkwak-repro reconciliation (id 124/137) — both live, both on #12242/#12156. Chain OPEN, pre-merge test iteration on #12242.
- **2026-07-27 ~23:29Z — "0 hlslExport" SCARE RECONCILED (per triager relaying fixer; NOT yet Main-verified-in-hand):** fixer's fresh rebuild of e3a5e7027b CONFIRMS it REJECTS (GLSL regression IS on e3a5e7027b) — the id-124 "0 hlslExport" was a wrong/stale binary, NOT a recant. Consistent with public record (issuecomment-5038963190), fixer msg 52 (HPP green) + msg 76 (REJECT@e3a5e70), and triager's source-verify of the CallableDecl gate. My caution (contradiction-flag) was correct. Fixer posting a three-way repro (ACCEPT@master, REJECT@e3a5e7027b narrowed, + f52b991 broad) consistent with what jkwak read. Authoritative confirmation = the posted comment id (still pending). Two fixer reports awaited: (a) jkwak repro posted (comment id); (b) test-coverage push (new head SHA, both gates exercised + load-bearing CHECK-NOT). Then jkwak's design ruling (target-neutral #12156 vs host-scoped #12242).
- **2026-07-27 — #12242 test-coverage push landed, new head `c749424efb1b0369a6192e14c596919e8cc43f62` (was 85da5d8e7); `ci_failed` webhook = BENIGN draft priority-yield (Main-verified, NO dispatch):** only failing check = `check-ci`, all 29 others `skipped` = classic wait-for-human-priority gate yield on bot draft (project_bot_pr_priority_yield_red_run), NOT a build/test fail; real jobs not yet run (priority-pending). #12242 still DRAFT. Fixer pushed pdeayton's test-coverage change (imported→`[__extern] public`, defined-non-public case, CHECK-NOT both). Still awaiting fixer's two explicit reports: (a) jkwak repro comment id (Main will verify on GH), (b) formal test-coverage confirmation (this SHA + both gates exercised/load-bearing). Triager watching. No action on the benign red.
- **2026-07-27 23:43Z — BOTH fixer tasks DONE + Main-verified on GitHub; chain PARKED at maintainer design-decision juncture:** (1) **jkwak repro** (issuecomment-5098090993, nv-slang-bot[bot]) — minimal `repro.slang` (`public __extern_cpp int main(int)` + `[shader("compute")] void computeMain()`), cmd `slangc repro.slang -target spirv -emit-spirv-via-glsl -stage compute -entry computeMain`, 3-way: master ACCEPTS (main(int) DCE'd) / #12156 e3a5 REJECTS `'main': function cannot take any parameter(s)` (plain -target glsl shows BOTH void main() + int main(int) emitted) / #12242 ACCEPTS. Root cause + #12242 isCPUTarget-only fix stated. Consistent w/ prior public record. **"0 hlslExport" scare = confirmed STALE BINARY** (.o@20:42 < source@20:53), exactly Main's contradiction-flag — NOT a recant; e3a5 genuinely REJECTS. (2) **test-coverage** (issuecomment-5098091119, new head `c749424efb`) — pdeayton's gap closed: `importedFunc`→`[__extern] public __extern_cpp` (reaches+excluded by isDefinition), added defined non-public `nonPublicFunc` (exercises isPublic gate), CHECK-NOT on both BRACKETING computeMain anchor (codex: host fns emit BEFORE entry → trailing-only CHECK-NOT misses leak). Load-bearing confirmed; codex CODE+OUTPUT approved. **State: #12242 + #12156 both DRAFT/OPEN — NOT merging/flipping/closing (jkwak's + human's calls). Next = webhook-driven jkwak/pdeayton review + jkwak's design ruling (target-neutral #12156 vs host-scoped #12242).** jkwak now has a runnable repro. Nothing owed by Main/fixer until review verdicts / real CI / jkwak ruling arrive.
- **2026-07-28 00:06Z — jkwak-work REVIEWED #12242 (COMMENTED, pullrequestreview-4792502507) + flipped it NON-DRAFT himself (Main-verified isDraft=false; maintainer prerogative, guardrail is coworker-only; COMMENTED=no approval to dismiss, fixer can push freely). reviewDecision=REVIEW_REQUIRED.** Full review = 3 items (webhook trio complete): (1) **DESIGN Q (review body):** *"What is the exact definition of __extern_cpp? Shouldn't it include CUDA too?"* — challenges the CPU-only scope: should `public __extern_cpp` host-rooting also cover CUDA (host-adjacent/C++-ish target)? Directly affects the `isCPUTarget(targetReq)` predicate @slang-ir-link.cpp:2382. (2) **CLARITY slang-ir.cpp:5032** (discussion_r3661765020): "I am not sure what you mean by `rooted`" → reword the jargon (linking-root/kept-alive-vs-DCE in plain terms). (3) **CLARITY slang-ir-link.cpp:2382** (discussion_r3661782706): "Too verbose" → trim comment. Routed triager→fixer: answer the CUDA-scope design Q empirically (what CUDA host emit does today; whether __extern_cpp semantics should include CUDA host targets) + recommend, DEFER final scope call to jkwak; fix the 2 clarity nits; push + reply per-thread. Post-back authorized. Approver NOT dispatched — open maintainer design Q, don't finalize over it (tilts ABSTAIN_POLICY); hold approver until review convo settles. Chain OPEN, active maintainer review on #12242.
- **2026-07-28 00:11Z — jkwak CLARITY-QUESTION CLUSTER on #12156 (superseded PR), 3 inline comments, educational/non-blocking:** (1) discussion_r3661807873 @ slang-lower-to-ir.cpp:1453 — define *root / restrict to locally-defined functions / header path / imported `[__extern]` function* (the diff comment on #12156's target-neutral rooting — the exact code #12242 REMOVES); (2) discussion_r3661796202 @ tests/headers/extern-cpp-public-export-9401.slang:15 — *"difference between `export` and `public`? They sound similar"*; (3) discussion_r3661793404 @ tests/headers/extern-cpp-imported-9401.slang:9 — *"Do we have a document that describes `[__extern]`? unfamiliar w/ this keyword."* jkwak reviewing #12156 IN DEPTH (may be weighing keep-#12156-vs-#12242). Routed triager→fixer: answer all 3 per-thread on #12156 plainly (root=linking-root-survives-DCE; export vs public = visibility/linkage distinction; [__extern] = check core.meta.slang + honest "doc exists or not"), AND orient jkwak to #12242 where a #12156 comment is superseded (the :1453 target-neutral rationale is what #12242 drops). Post-back authorized. Non-blocking educational; no approver impact. Chain OPEN.
- **2026-07-28 00:16Z — ALL jkwak review items addressed, Main-verified on GitHub; chain PARKED at jkwak's CUDA-scope ruling:** #12242 head advanced `c749424`→`33d3ad1863da85f3d230983d2f2ca7b31f3a483a` (non-draft, COMMENTED, reviewDecision=REVIEW_REQUIRED). **(1) 2 nits fixed+pushed:** slang-ir.cpp "rooted" jargon reworded→"keep alive through DCE" (reply r3661825694); slang-ir-link.cpp trimmed to 2 lines (reply r3661825755). **(2) CUDA design Q answered (issuecomment-5098313334):** empirical — `public __extern_cpp` retained for `cpp` (1) NOT `cuda`/`cuh` (0), CUDA has the SAME gap CPU had. **KEY (codex-corrected fixer's initial error):** naively widening isCPUTarget→CUDA would keep the fn as `__device__` NOT host export (`slang-emit-cuda.cpp:443-453` defaults __device__; only CudaKernel→__global__, CudaHost→__host__) — closing CUDA correctly needs `CudaHostDecoration` wiring + CUDA-emit test + nvcc/CI (fixer can't run here). CUDA entry emits distinct name `computeMain` (not `main`) so no #12156-collision, but that alone ≠ widening-correct. **Fixer RECOMMENDS: keep #12242 CPU-only, CUDA as separate follow-up; scope decision DEFERRED to jkwak.** (3) #12156's 3 educational Qs answered per-thread (r3661862877 term-defs + #12242-supersedes note; r3661862940 export=linkage/keep-alive vs public=visibility; r3661863002 `[__extern]`=ExternAttribute core.meta.slang:4769, NO user-facing doc = genuine docs-gap follow-up, no fabricated link). codex CODE+OUTPUT approved both. **State: #12242 non-draft/COMMENTED @ 33d3ad1863, both PRs OPEN, NOT merging/closing. GATE = jkwak's CUDA-scope ruling (CPU-only ship #12242 as-is, OR broaden→CUDA follow-up w/ CudaHostDecoration+nvcc CI). Approver still HELD (open design Q).** Possible docs follow-up: `[__extern]` undocumented. Nothing owed until jkwak rules / review verdict / real CI.
- **2026-07-28 00:30Z — jkwak MUSES `[__extern]` may be deprecated (discussion_r3661885448, reply to r3661793404 on tests/headers/extern-cpp-imported-9401.slang:9):** *"Or maybe we are not supposed to use it; probably because it is outdated/deprecated."* **Main source-check: `[__extern]` NOT deprecated** — core.meta.slang:4769 `attribute_syntax [__extern] : ExternAttribute;` has NO `[deprecated]` marker (nearby deprecation hits unrelated: vector-init :2809/:2820, generic `[deprecated]` def :4905). MATTERS because **#12242's coverage test uses `[__extern] public __extern_cpp int importedFunc(int a);`** (added per pdeayton) — if deprecated, test would lean on deprecated kw; since NOT deprecated, test is fine. Routed triager→fixer: (1) confirm in source `[__extern]` is undeprecated + still parsed/functional (git-blame/usage recency); (2) reply honestly to jkwak — not marked deprecated, but if HE prefers, offer an ALT test formulation for the imported/bodyless-exclusion case that doesn't rely on `[__extern]` (e.g. a plain forward-declared/bodyless `public __extern_cpp` decl, or import from another module) so the test doesn't hinge on a keyword he's uneasy about. Defer the keep-`[__extern]`-vs-alt call to jkwak. Post-back authorized. Non-gating clarity/test-robustness. Chain OPEN.
- **2026-07-28 00:33Z — jkwak drills into "linking root" (discussion_r3661896697, reply to r3661807873 @ slang-lower-to-ir.cpp:1453):** *"Is `root` common compiler terminology? never heard of it. What does 'a linking root' mean? It sounds like it will have 'body' of the function, is it?"* His body-intuition is CORRECT + ties to #12242's `isDefinition()` gate: a linking root = a symbol the linker treats as an entry/reachability seed (like GC roots — starting points that survive DCE + everything they reference), NOT dropped even if nothing else in the module calls it. And YES it needs a body/definition to be resolvable — that's exactly why #12242 gates on `isDefinition()` && !isImported: rooting a bodyless/imported decl → linker demands a symbol with no definition → E45001 (the R2 regression this chain hit). Routed triager→fixer: answer on-thread — root = reachability seed/GC-root analogy, "linking root"=kept by linker as export entry-point vs DCE'd; confirm jkwak's body-intuition (root must be locally-defined, which is why the gate excludes imported/bodyless → else E45001). Post-back authorized. Non-gating educational. Chain OPEN.
- **2026-07-28 00:36Z — jkwak CHALLENGES the gate condition (discussion_r3661911672 @ slang-lower-to-ir.cpp:1453, on #12156):** *"I don't think we need `if (as<CallableDecl>(decl) && !isImported)`. Seems over conservative. Justify why necessary. Not sure how imported state matters — give me an example of why we care about 'imported' here."* **LOAD-BEARING — the `!isImported`/`isDefinition()` guard is EXACTLY what prevents the E45001 regression pdeayton caught** (bodyless imported `[__extern]` decl → no definition to root → linker demands unresolvable symbol → E45001; the R2 regression this chain hit+fixed). Fixer has the exact repro. Routed triager→fixer: answer on-thread w/ the CONCRETE E45001 example (bodyless `[__extern] public __extern_cpp int f(int);` → rooting it → `error E45001 unresolved external symbol` — the case pdeayton's own test now covers), justify NOT over-conservative but necessary-for-correctness. NUANCE: on #12156 the guard is `!isImported` at LOWERING; on #12242 it's `isDefinition()` at the CPU-gated LINK site (same intent — root must have a body). Fixer should justify the concept AND note #12242 expresses it more precisely → reinforces landing #12242 over #12156. Post-back authorized. If fixer can't justify → risk jkwak drops guard → reintroduces R2 bug. Chain OPEN, active gate-design review.
- **2026-07-28 ~00:39Z — jkwak RESOLVED both #12242 nit threads (housekeeping, non-actionable, no dispatch):** resolved slang-ir-link.cpp:2382 (":2382 too verbose"→trimmed) + slang-ir.cpp:5032 (":5032 rooted jargon"→reworded "keep alive through DCE") on #12242. Both clarity nits accepted → convergence signal toward #12242 (the PR that lands). Also resolved the export-vs-public educational thread on #12156 (r3661796202) earlier. Thread-resolves = maintainer satisfied; NOT routing events, no reply/post. Real gates still open: jkwak's !isImported guard challenge (r3661911672, load-bearing), CUDA scope ruling, [__extern] test-alt preference, approval loop.
- **2026-07-28 00:44Z — fixer answered all 3 jkwak follow-ups on #12156, Main-verified on GitHub; guard DEFENDED with real E45001:** (1) **LOAD-BEARING guard defense (reply r3661938105):** built the exact bodyless case `[__extern] public __extern_cpp int importedFunc(int a);`+compute on BROAD f52b991 (no guard, roots all __extern_cpp) → **`error[E45001]: unresolved external symbol 'importedFunc'`** exit 255; guarded e3a5 → clean exit 0. Real output pasted. Justification: host-export root must be locally-defined to resolve; guard removes a REAL failure (pdeayton's test pins it — dropping guard fails that test too); #12156 `!isImported`@lowering = #12242 `isDefinition()`@link (cleaner). (2) **"root" terminology (reply r3661939081):** GC/DCE-root analogy, "standard in DCE/linking not universal", confirmed jkwak's body-intuition (root needs definition → isDefinition gate). (3) **`[__extern]` not deprecated (reply r3661938034):** source-verified core.meta.slang:4769 no `[deprecated]`, actively used (library-entry-point.slang:6 + lower-to-ir/linkable/type-layout consumers); offered plain-bodyless `public __extern_cpp` test-alt (no `[__extern]`) that still exercises isDefinition gate (tested clean), keep-vs-rewrite DEFERRED to jkwak. codex-gated (caught+corrected: disabled-test miscite, ExternAttribute-vs-ExternModifier conflation, verbatim E45001 brackets). **State: all educational/challenge replies, NO code change, both PRs OPEN. Gates: jkwak's response to the guard defense + CUDA scope ruling + test-alt keep-vs-rewrite preference + approval. Approver HELD (open design Qs). Nothing owed until jkwak moves a gate.**
- **2026-07-28 ~00:46Z — jkwak RESOLVED the slang-lower-to-ir.cpp:1453 thread on #12156 = ACCEPTED the load-bearing guard defense (Main-verified thread path):** that thread carried the `!isImported` guard challenge (r3661911672) + the root/body-intuition subthreads (r3661807873/r3661896697). Maintainer resolving it AFTER the E45001-repro defense ⇒ he accepts the guard is necessary, NOT pushing to drop it → **load-bearing risk (guard removed → R2 E45001 regression reintroduced) RETIRED.** Still a thread-resolve (housekeeping), NOT an explicit ruling/approval — no dispatch/reply/post. Remaining gates: CUDA scope ruling, `[__extern]` keep-vs-rewrite test preference, approval loop. Convergence pattern strengthening (all #12242 nit threads + now #12156 guard thread resolved).
- **2026-07-28 — jkwak asks "Shouldn't we have `//CHECK-NOT: importedFunc`?" (discussion_r3661997946 @ tests/headers/extern-cpp-imported-9401.slang:11 on #12156):** he's RIGHT for the #12156 version — Main-verified that #12156's test uses `-target glsl` + `[__extern] __extern_cpp int importedFunc(int a);` + only `//CHECK: void main()`, NO CHECK-NOT. **BUT #12242 (head 33d3ad1863) ALREADY HAS IT + MORE** (Main-verified): `-target cpp`, `//CHECK-NOT: importedFunc` (L18,21) + `//CHECK-NOT: nonPublicFunc` (L19,22) BRACKETING `//CHECK: void computeMain` on both sides, using `[__extern] public __extern_cpp` (pdeayton's form). So jkwak is reviewing the STALE #12156 test; answer = "good catch, yes — and #12242 already does exactly this (both names, bracketed)". Another steer to land #12242 over #12156. Routed triager→fixer: reply on-thread affirming + pointing to #12242's existing CHECK-NOTs; no code change needed on #12242 (already present). Post-back authorized. Non-gating.
- **2026-07-28 — jkwak PIVOTAL PR-comparison Q (issuecomment-5098643098, top-level on #12156):** *"I am confused by two similar PRs: #12242 and #12156. What is the difference and why 12156 doesn't use `isCPUTarget()`?"* This is the ORIENT-THE-MAINTAINER moment — he's been reviewing superseded #12156 in depth, now realizes there are two. Answering cleanly steers him to ABANDON #12156, focus review/approval on #12242. Routed triager→fixer to post a crisp comparison on #12156 (post-back authorized): **#12156 = OLD target-neutral approach** (adds HLSLExport+KeepAlive at LOWERING via `!isImported` gate → roots for EVERY target incl GLSL/SPIR-V → the GLSL regression pdeayton caught: `public __extern_cpp int main(int)` emits invalid GLSL); **#12242 = NEW host-scoped fix** (ExternCpp stays naming-only; roots defined `public __extern_cpp` ONLY at CPU-target link via `isCPUTarget(targetReq)` → zero GPU side-effect). #12156 doesn't use isCPUTarget BECAUSE it roots at lowering (target-neutral, before target-specific linking) — that's the very design flaw #12242 fixes by moving rooting to the CPU-gated link site. **Recommendation: close #12156, review+land #12242** (fixer states it, jkwak decides). Fixer must be accurate + neutral (jkwak green-lit #12156's broad approach earlier — frame the shift as the review-driven regression finding, not a reversal-blame). Chain OPEN — pivotal clarification, likely unlocks the merge path.
- **2026-07-28 01:01Z — jkwak asks `[__extern]` vs `extern` vs `imported` distinction (discussion_r3662011528, reply to r3661793404 @ extern-cpp-imported-9401.slang:9):** ACCURACY-SENSITIVE — fixer earlier CONFLATED ExternAttribute vs ExternModifier (codex catch), so this must be source-grounded not guessed. Main-verified: `[__extern]`=`ExternAttribute` (attribute_syntax @core.meta.slang:4769); `__extern_cpp`=`ExternCppModifier` (@86, DIFFERENT — naming metadata). Distinct concepts: (1) `[__extern]` attribute = marks a decl as externally-provided/no-body-here (the serialization/cross-module linking attr); (2) `extern`/`ExternModifier` = separate language modifier (must verify its exact semantics in source); (3) `imported` = a decl STATE (`isImportedDecl()` true) meaning the definition lives in another module resolved at link — the property the #12242/#12156 guard tests. Routed triager→fixer: verify all three in source (don't guess, given prior conflation), reply on-thread with the precise distinctions + how `imported` (the state) relates to `[__extern]` (one way a decl becomes import-like). Post-back authorized. Non-gating educational.
- **2026-07-28 01:09Z — ALL #12156 review threads ANSWERED incl the PIVOTAL PR-comparison, Main-verified on GitHub:** (1) **PIVOTAL comparison (issuecomment-5098703023):** "#12242 is the proposed host-scoped replacement for #12156" — #12156 roots at LOWERING (slang-lower-to-ir.cpp, `!isImported` gate, no TargetRequest there) → target-neutral → GLSL regression; #12242 roots at CPU-gated LINK (linkIR has targetReq) via isCPUTarget. Answers "why no isCPUTarget on #12156" = because lowering has no targetReq; moving to link is #12242's change. Neutral/review-driven framing; recommends close #12156/land #12242, deferred to jkwak. (2) CHECK-NOT affirmed (r3662013900, #12242 already has it). (3) terminology (r3662038882) source-verified: `[__extern]`=ExternAttribute(:1610 Attribute), `extern`=ExternModifier(:100 Modifier, distinct), `imported`=state via isImportedDecl(:738, either modifier on decl/ancestor or non-main module). codex-gated 43 rounds (caught+corrected: over-broad public, overclaims, blame tone, isImportedDecl ancestor-check omission). **State: ALL jkwak #12156 threads answered; jkwak systematically resolving #12156 threads (convergence). Both PRs OPEN, no merge/close by us. Gates DOWN TO: jkwak's (a) close-#12156/land-#12242 decision, (b) CUDA scope ruling, (c) approval on #12242. Approver still HELD (open design Qs). Pivotal clarification delivered → likely unlocks merge path. Nothing owed until jkwak moves a gate.**
- **2026-07-28 ~01:11Z — jkwak CLOSED #12156 in favor of #12242 (Main-verified: #12156 state=CLOSED, merged=false per pr_closed webhook + closedAt set; comment issuecomment-5098712146 "Closing in a favor of #12242").** The design decision the chain circled for days is RESOLVED: **host-scoped (#12242) WINS over target-neutral (#12156).** #12242 now the SOLE live PR — OPEN, non-draft, MERGEABLE, reviewDecision=REVIEW_REQUIRED @ head 33d3ad1863. The pivotal PR-comparison answer (issuecomment-5098703023) did its job → maintainer abandoned #12156. **Remaining gates DOWN TO: (a) jkwak's CUDA scope ruling (his open review-body Q — CPU-only vs broaden; fixer recommended CPU-only + CUDA follow-up), (b) #12242 approval + merge (jkwak's).** Approver can run on #12242 once the CUDA design Q settles (still HELD until then). Documented follow-ups: GATE 2 (no-entry-point export), CUDA host-scope, `[__extern]` docs gap. Chain OPEN — #12156 closed, #12242 is the fix, awaiting jkwak CUDA ruling + approval/merge.
- **2026-07-28 01:26Z — APPROVER TERMINAL JOIN on #12156 = R3 ABSTAIN VINDICATED (id 254, ledger-only):** #12156 CLOSED UNMERGED by jkwak 01:11:35Z at exact R3 head `e3a5e7027bbc` (no R4 push); `human_verdict=CHANGES_REQUESTED` recorded vs R3 decision commit. **R3 ABSTAIN_POLICY (CHALLENGER_CONCERN) vindicated by terminal human outcome** — jkwak closed in favor of host-scoped #12242 (uses isCPUTarget) for exactly the abstain basis: #12156 roots target-neutrally at lowering → every target incl GLSL/SPIR-V → pdeayton's regression. **KEY CALIBRATION (whole-chain payoff): CI fully GREEN (16/16 test-slang) + both prior regressions fixed, YET a WOULD_APPROVE would've been a FALSE-APPROVE** — design-scope defect real + CI-invisible. Discipline confirmed: CI-green NEVER resolves a maintainer design-scope objection; ABSTAIN right (not BLOCK=no verified 🔴, not APPROVE). Learning captured [approver/human-agreement] R3-ABSTAIN-target-neutral-rooting-VINDICATED. #12156 chain TERMINAL; #12242 = live successor to watch.
- **2026-07-28 01:27Z — jkwak IMPLICIT-PUBLIC coverage/correctness Q (issuecomment-5098806009, top-level on #12242):** *"what if `__extern_cpp` is used without an explicit `public` keyword but implicitly its visibility is public? In that case, the function should be also root-ed; but did you say that case is already handled properly by other code path?"* REAL correctness Q — does #12242's gate test the LITERAL `public` keyword or EFFECTIVE/implicit visibility? If a `__extern_cpp` fn is implicitly-public (default visibility by scope/module rules) but lacks the keyword, is it rooted or does it fall through a hole? jkwak recalls fixer saying some case is "handled by another path" — wants it reconciled. Fixer MUST answer from SOURCE not memory (chain history of codex catching overclaims): (1) what does the `isPublic` check actually test — literal `PublicModifier`/keyword vs computed effective visibility (check the visibility resolution: does default visibility make a symbol effectively public without the keyword)? (2) if implicit-public exists + isn't caught by the gate → is it rooted via another path, or a genuine gap needing the gate widened to effective-visibility? (3) reconcile with whatever "other code path" the fixer referenced earlier. Routed triager→fixer, post-back authorized. Potentially GATING if it reveals a real coverage hole (implicit-public fn not rooted = same class as original #9401 bug). Chain OPEN.
- **2026-07-28 02:11Z — IMPLICIT-PUBLIC GAP CONFIRMED REAL (GATING); Main-verified in source + fixer correction posted:** gate misses implicit-public. **Main-verified slang-lower-to-ir.cpp:1432-1436:** `[public]` IR decoration added ONLY when literal `PublicModifier` in `decl->modifiers` — NEVER consults effective/computed visibility. So implicitly-public `__extern_cpp` fn (no literal keyword) → no `[public]` → gate isPublic (isExternCpp&&isPublic&&isDefinition @slang-ir.cpp:5034) MISSES it → dropped from hpp/cpp = same class as original #9401. Fixer repro: `public module m; __extern_cpp int f(int){...}` → IR has [externCpp]+[export] NO [public] → dropped; litmus `internal` return type → E30604 fires (IS effectively public) yet dropped. **TWO self-caught overturns (neither relayed as fact by Main):** (a) triager's earlier "effective-public materializes PublicModifier→covered" lead WRONG — verified every `addVisibilityModifier` site (slang-check-decl.cpp ctor:2991/assoc:3844/agg:3867/etc) is a SYNTHESIZED decl, never user fn; (b) fixer's earlier "other code path handles it" corrected (that path=normal DCE reachability, does NOT root implicit-public). Fixer posted honest correction to jkwak (issuecomment-5099120238). **Fixer did NOT push unilateral fix — put A/B design fork to jkwak:** (A) broaden [public] stamp at lowering (smallest diff but perturbs ~7 other [public] consumers incl GPU-emit/uninit-warn/pytorch — blast-radius risk); (B) dedicated CPU-link-only host-export marker (fully scoped, small IR-decoration add — FIXER'S REC, consistent w/ #12242 CPU-link design). **+SCOPE call:** in legacy AND `public module` (both default→Public), correct effective-vis gating would force-emit EVERY defined unmodified `__extern_cpp` (48 bare occurrences in tests/) — narrower only in a `2026` module w/o public. **GATING correctness item on #12242 before merge; held on jkwak's steer (A/B repr + is legacy everything-public force-emit intended); then fixer implements codex-gated + implicit-public regression test.** Joins jkwak csyonghe-consult approval-hold + CUDA scope. Approver stays HELD.
- **2026-07-28 — jkwak DESIGN STEER on implicit-public gap (issuecomment-5111380542):** *"I think we should ignore `public`-ness and treat `__extern_cpp` as public visibility by itself."* RESOLVES the scope question: **DROP the `isPublic` gate entirely — `__extern_cpp` on a defined fn implies host-export visibility by itself.** New gate → `isExternCpp && isDefinition` (no isPublic). Accepts the force-emit-all blast radius the fixer flagged (~48 bare `__extern_cpp` in tests/ now emit). **CRITICAL GUARDRAIL: dropping isPublic must NOT reintroduce the target-neutral GLSL regression — the `isCPUTarget(targetReq)`-at-link gate MUST stay** (that's the #12242 fix; jkwak's steer changes the visibility axis, NOT the target axis). Representation: jkwak's steer is about visibility SEMANTICS not IR repr — fixer keeps the CPU-link-scoped design (#12242 B-style), just removes isPublic from the predicate. Routed triager→fixer: implement (drop isPublic, keep isDefinition + isCPUTarget-at-link), MEASURE blast radius + update any golden outputs for the newly-emitted bare `__extern_cpp` cases, add implicit-public regression test, re-verify GLSL-regression case still PASSES (isCPUTarget intact), codex-gate, push. Post-back authorized. Still NOT merge (jkwak csyonghe-consult approval-hold + CUDA scope open). Chain OPEN — design steer received, implementation dispatched.
- **2026-07-29 ~01:25Z — jkwak's isPublic-drop IMPLEMENTED @ head `ad59212a39b4abdc4ef6e08747c96ca6b882a07f`, Main-verified both load-bearing points in source:** (1) **Gate change (slang-ir.cpp:5029-5032):** `if (isExternCpp) { if (as<IRFunc> && func->isDefinition()) m_hostExternCppFuncs.add }` — isPublic DROPPED, isDefinition KEPT, dead PublicDecoration tracking removed. Exactly jkwak's steer. Commit "Root __extern_cpp for host output regardless of visibility (#9401)". (2) **CRITICAL GUARDRAIL INTACT (slang-ir-link.cpp:2381):** `if (isCPUTarget(targetReq))` STILL gates getHostExternCppFuncs() — target axis untouched, did NOT revert to lowering/target-neutral → #12156 GLSL regression NOT reintroduced. This was the must-hold; it holds. **GLSL re-verify (fixer):** public+compute → 0 errors no leak; BARE __extern_cpp int main+compute → 0 errors (isCPUTarget keeps bare out of GPU); bare implicitFunc now emits hpp+cpp → GAP CLOSED. **Blast radius = ZERO golden churn** (Main-plausible: only defined bare __extern_cpp FUNCTIONS suite-wide = PR's 2 tests + tests/glsl-intrinsic/intrinsic-texture.slang which isCPUTarget excludes; other ~45 = structs/fields/globals non-IRFunc, bodyless fail isDefinition, or non-CPU targets). Tests added: bare-positive hpp+cpp, imported→isDefinition-negative, GPU-guard→bare main. **Follow-up FLAGGED not self-fixed (good scope discipline):** bare __extern_cpp emits cpp DEFINITION as `static` (internal linkage) — isPublicOrExportedFunc (slang-emit-cpp.cpp:955/992) doesn't include ExternCppDecoration; pre-existing emitter behavior; small separate emitter change offered if jkwak wants "public-by-itself" to extend to emission linkage. **State: gap CLOSED correctly, correctness line held. NOT a merge — jkwak approval held (csyonghe consult; CUDA scope + static-linkage follow-up may fold in). Approver stays HELD (open review, no approval). Chain OPEN, awaiting jkwak approval/merge/follow-up ruling.**
- **2026-07-30 20:04Z — csyonghe (the consult maintainer) CONTRADICTS jkwak's steer (issuecomment-5135718535):** *"it shouldn't be `public __extern_cpp`, it needs to be `export __extern_cpp` for it to be included in the generated code. `public` is a visibility modifier, it doesn't convey the semantics of 'this thing needs to be preserved for final library export'."* **DIRECT maintainer-to-maintainer DESIGN CONFLICT on the trigger for host-export** (exactly the consult jkwak invited before approving): jkwak (07-29) = `__extern_cpp` alone roots regardless of visibility (fixer IMPLEMENTED this @ad59212a39, dropped isPublic); csyonghe (07-30) = require `export __extern_cpp` (export conveys preserve-for-export; public is only visibility). csyonghe's position is PRINCIPLED — this chain already established `export` adds HLSLExport/KeepAlive (roots vs DCE), `public` does NOT (bullets 16/25). jkwak has NOT yet responded to csyonghe (verified: csyonghe's is the latest #12242 comment). **CRITICAL DISCIPLINE: fixer must NOT reflexively re-implement to export-gating** — it just implemented jkwak's opposite steer; flipping now = thrash, and if jkwak pushes back we flip again. Routed triager→fixer to (1) reply to csyonghe NEUTRALLY with the technical landscape (what export/public/bare __extern_cpp each do re DCE/keep-alive/host-emit; note current head implements jkwak's steer), (2) EXPLICITLY surface the jkwak-vs-csyonghe conflict + ask the two maintainers to converge on the trigger, (3) NOT change code until they reconcile — defer, don't pick a side. Post-back authorized. Chain OPEN — maintainer design conflict, held for jkwak↔csyonghe convergence. NO code thrash.
- **2026-07-30 20:13Z — fixer SURFACED the conflict neutrally, code HELD (Main-verified issuecomment-5135800296):** posted branch-verified (`ad59212a39` + local slangc) technical landscape, explicitly flagged it as an open jkwak↔csyonghe question, asked them to align BEFORE any code change — no side-picking. Code UNTOUCHED. **Source landscape (fixer -dump-ir verified):** `export __extern_cpp` gets [hlslExport]+[keepAlive] at LOWERING → rooted target-neutrally, PRE-EXISTING (already host-emitted before this PR); `public` + bare `__extern_cpp` get neither at lowering → rooted ONLY by this PR's CPU-link gate. So **csyonghe factually right that `export` already conveyed "preserve for export"** — BUT the docs sample AND original #9401 report both used `public __extern_cpp` not export. **MATERIAL FORK (fixer-surfaced, neutral):** under csyonghe's export-trigger view, #9401 largely reduces to a DOCS CORRECTION (sample public→export) + REVERTING this PR's public/bare gate; under jkwak's view, keep ad59212a39. Real outcome divergence — maintainers' call. **jkwak/csyonghe NOT yet responded (fixer's comment latest).** State: correctly parked, ball with 2 humans, no thrash. Triager owns went-quiet watch → will flag Main for nudge decision. Approver HELD. Chain OPEN — awaiting jkwak↔csyonghe convergence on trigger (bare vs export __extern_cpp).
- **2026-07-30 21:01Z — CONFLICT RESOLVED: jkwak CONVERGED with csyonghe → #12242 becomes DOCS-ONLY (issuecomment-5136229007, Main-verified latest):** *"revert all of the code changes in this PR because the current behavior is intended in a way that `export` is required to emit the function for C++ header files. But the document needs to be modified to use `export` instead of `public` because it can be confusing."* **VERDICT: `export __extern_cpp` is INTENDED/correct behavior — #9401 is a DOCS BUG, not a compiler bug.** Revert ALL compiler + test changes in #12242; keep ONLY the docs correction. Main-verified docs/cpu-target.md offenders: lines 162 (`public __extern_cpp int add`), 221 (`public __extern_cpp myFunc` — also missing int return), 278 (`__global public __extern_cpp int myGlobal`), + prose 208-214 conflating `public` w/ host-export. Change samples `public`→`export __extern_cpp`, fix prose. **FULL CIRCLE: lands exactly where ORIGINAL TRIAGE recommended — "Approach A docs-only" (bullets 15-17)** — after the entire compiler-change detour jkwak had steered into. **Surface-and-defer VINDICATED: because code was NEVER merged at any intermediate design, the revert is clean, nothing shipped to unwind.** Routed triager→fixer: revert compiler+test code (slang-ir.cpp/slang-ir-link.cpp/slang-lower-to-ir.cpp back to master, drop the added tests), apply docs-only fix (public→export in samples + prose), keep `Fixes #9401`, verify docs sample now describes correct behavior (`export __extern_cpp` emits), codex-gate, push. Post-back authorized. Approver can run once docs-only diff settles + jkwak approves. Chain OPEN — resolved to docs-only, reimplementation dispatched.
- **2026-07-30 21:20Z — #12242 REVERTED to DOCS-ONLY @ head `d76d8bd7034f4dc4576b6218177f4846e93a9c9c`, Main-verified on GitHub:** `gh pr diff --name-only` = EXACTLY ONE file `docs/cpu-target.md` (compiler+test reversion confirmed on GH, not just relayed); mergeable, REVIEW_REQUIRED. Reply to both maintainers posted (issuecomment-5136388022, 21:20Z). Triager independently diffed vs true merge-base f282bdf9c: only cpu-target.md (9ins/9del), slang-ir.cpp/link/h byte-identical to fork-point, 3 tests deleted. Commit "docs: use `export __extern_cpp` for host-callable symbols (#9401)". **Docs fix:** public→export in C++ comments (:151/:162), 3 samples (:218/:221/:278 + `int` return on myFunc), prose (:208/:210/:214/:275); global→`__global export __extern_cpp` (empirically: export emits -target hpp, public/bare don't). Precision baked in (codex): `export` alone=preserved into -target cpp MANGLED no-hpp-decl; `export __extern_cpp`=preserved+unmangled+hpp-decl; `public`=visibility only. All corrected samples verified emit on slangc. Formatting: file prettier-dirty repo-wide (pre-existing); fixer correctly did NOT prettier --write (would reflow=unrelated churn), zero new format delta. codex CODE+OUTPUT approved. **State: clean docs-only PR, correct outcome = original triage's Approach A (FULL CIRCLE after compiler detour). Surface-and-defer VINDICATED (nothing merged at intermediate design → clean revert). NOT merged — jkwak approves docs diff then human merge. CLOSE-OUT: #12242 merges → triager refreshes issue verdict "fixed (docs) merged #12242" + final [Triage Resolution].** Chain OPEN — docs-only PR awaiting jkwak approval+merge. Approver can run on the docs-only diff (no compiler risk).
- **2026-07-30 22:53Z — jkwak APPROVED #12242 (Main-verified: pullrequestreview-4823893310 "Looks good to me", reviewDecision=APPROVED @ head `d76d8bd703`, mergeable, OPEN).** Docs-only fix greenlit; design conflict fully behind us; maintainer signed off on the correct outcome (docs `public`→`export __extern_cpp`, Approach A). **TERMINAL APPROVAL — only the HUMAN MERGE remains** (never auto-merge/flip per standing rules; jkwak/maintainer merges). No GitHub post from us (fix trail on PR). Triager primed for CLOSE-OUT: on merge → refresh issue #9401 verdict "fixed (docs), merged #12242" + final [Triage Resolution]. GATE 2 = `-whole-program` workaround (pending reporter confirm). Chain essentially RESOLVED, awaiting merge only.
- **2026-07-28 — jkwak REVIEW on #12242 (COMMENTED, pullrequestreview-4792801890): "Looks good to me. But I like to discuss with @csyonghe before approving it because there might be gotchas when it comes to export and modules."** = positive lean, but APPROVAL HELD pending maintainer-to-maintainer consult w/ csyonghe on export/modules gotchas. NOT our action — maintainer hold. (This likely absorbs/supersedes the separate CUDA-scope ruling into a broader csyonghe design consult.) Approver stays HELD (open review + no approval yet).
- **2026-07-28 00:56Z — jkwak test-coverage Q (discussion_r3661994978 @ tests/headers/extern-cpp-gpu-no-root-9401.slang:8):** *"Shouldn't we also test `-target hpp` and cpp? Because that's how the initial report came in."* Main-verified the 3 #12242 test files each cover a distinct target: `extern-cpp-public-export-9401.slang`=**hpp** (positive: public+export emit in header); `extern-cpp-imported-9401.slang`=**cpp** (negative: imported/non-public excluded, CHECK-NOTs); `extern-cpp-gpu-no-root-9401.slang`=**spirv-asm/-emit-spirv-via-glsl** (the GLSL-regression guard, jkwak commented HERE). So hpp+cpp ARE covered (across sibling files) — like the CHECK-NOT case, jkwak's looking at one file. BUT legit nuance: the POSITIVE host-emission case (original report shape) is asserted on hpp but maybe not cpp — fixer should check if positive `public __extern_cpp`-emits is exercised on BOTH hpp+cpp; if cpp lacks a positive assert, ADD it for symmetry (report came in for host C++ = both). Routed triager→fixer: reply affirming hpp+cpp already covered (cite the sibling files/targets) + add cpp-positive assertion if missing. Post-back authorized. Non-gating (coverage robustness).
- **2026-07-30 ~21:05Z — jkwak GATE-2 disposition (issuecomment-5136252442), Main-routed id 49 / triager-confirmed id 50:** jkwak tells reporter **@NBickford-NV** (NOT the bot): for the 2nd problem (no-entrypoint — `export __extern_cpp` not emitted when no entry point exists) → use **`-whole-program`** (docs link); "let us know if that's not satisfying workaround." Aligns exactly with triage (GATE 2 decoration-independent; `-whole-program` already resolves, repro rows 3/4). = **GATE 2 effectively CLOSED as workaround, pending @NBickford-NV confirmation.** Maintainer↔reporter exchange → bot does NOT inject; NO bot GitHub post, NO fixer action. Independent of the #12242 docs-only vehicle. **RE-OPEN GATE 2 as a real code change ONLY if reporter says `-whole-program` insufficient.**
- **STALE-FRAMING CORRECTION (triager id 50):** any earlier note that "#12156 GATE-1 compiler PR is approved & awaiting merge" is STALE. #12156 was **CLOSED UNMERGED** (07-28, jkwak "in favor of #12242"); after jkwak↔csyonghe convergence the GATE-1 compiler change was **FULLY REVERTED** → the single live vehicle is **docs-only #12242** (Approach A), awaiting jkwak's approval of its docs diff. Fixer in flight on the revert.
