---
name: project_12099_profile_capability_conflict_diag
description: "slang#12099 reject conflicting -profile/-capability CLI args; PR #12122 BLOCK (RED_BUG) — E00046 false-positives on legit cmd lines, 29 test-slang fails; fixer narrowing check"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0b8447c6-ce65-4af5-9a41-633ac235ab25
---

slang#12099 — "Reject conflicting -profile and -capability command-line arguments." Enhancement, P2, frontend (options parsing + capability/target). Reporter **jkwak-work self-filed + self-assigned** ⇒ classified + verdict + label, **NO fixer dispatch** (per [[feedback_deadpromise_check_assignee_before_rewake]]).

**What:** `-profile spirv_1_4` + `-capability spvShaderInvocationReorderNV` silently emits SPIR-V **1.5** (bug) instead of erroring. Both constraints explicit on CLI ⇒ should be a hard, order-independent command-line error, WITHOUT `-restrictive-capability-check`. Capability satisfiable via extension while keeping selected core version must still be ACCEPTED (not a conflict).

**Repro VERIFIED on ToT @3eeda847c** (CLI/SPIR-V-text, no GPU): all three cases (both arg orders + `-profile p+cap` appended syntax) emit 1.5 silently, exit 0.

**Root-cause layer:** `TargetRequest::getTargetCaps()` slang-target.cpp:214-232 folds `-capability` atoms via `if (!isIncompatibleWith(toAdd)) join(toAdd)`. `CapabilitySet::isIncompatibleWith` (slang-capability.cpp:487-512) returns "compatible" whenever sets share ANY target abstract node — spirv_1_4 & spirv_1_5 are the SAME SPIR-V target ⇒ intersect ⇒ join proceeds and takes MAX version. A version *raise* is a *compatible* join, never flagged. Missing prerequisite: no flag records that the profile's concrete version was USER-EXPLICIT.

**Recommended:** Approach B (parse-time CLI diagnostic in slang-options.cpp, needs explicit-version flag); Approach A fallback (validate at getTargetCaps join, needs sink plumbing + once-guard). C (extend E41012/E41013) rejected by author.

**Ordering dep on #12097:** fix must key off "capability has NO valid realization at explicit version" (extension paths included), NOT "default realization exceeds version" — once #12097 lands, spvShaderInvocationReorderNV gains a valid SPIR-V 1.4 extension path and the exact example should SUCCEED at 1.4. That's the discriminator: true conflict vs satisfiable-via-extension.

Related: #12097 (self-assigned jkwak, complementary), #4165 (shader-code-implied capability diagnostics; this is the CLI-explicit analog). Triager owns GitHub verdict post (closest-to-the-state). Debounce re-webhooks; re-engage only on human comment or PR.

**07-15 re-open (jkwak asked bot "do we need to wait for #12097?"):** Answered NO on GitHub (comment 4980845026). Verified vs capdef @694022a11: `spvShaderInvocationReorderNV` has a SINGLE purely-conjunctive realization requiring `_spirv_1_5` (capdef:624/629/857, no `|` disjunct) ⇒ genuinely NO 1.4 realization today ⇒ example is a REAL conflict now ⇒ #12099 check fires correctly, needs no wait on #12097. Orthogonal: only coupling is the design constraint (phrase check as "no valid realization at explicit version", extension-inclusive) so it auto-flips to accept-at-1.4 once #12097 lands, zero follow-up edit. Still HELD no-fixer (self-assigned). Chain closed again; re-open on further substantive human comment.

**07-15 FIX AUTHORIZED — dispatched to slang-fixer:** jkwak (assignee) commented "Please make a PR for this issue" (comment 4981320957) ⇒ owner authorization OVERRIDES the self-assigned stand-down. Dispatched slang-fixer MODE=fix-issue on canonical thread with full inlined context (root cause, Approach B recommendation, the extension-inclusive design constraint, test matrix: both arg orders + `-profile p+cap`). DRAFT PR default (guardrail — draft unless maintainer flips).

**07-15 DRAFT PR #12122 OPENED** (branch `fix/issue-12099`, head 9fe3de9e1f, `Closes #12099`, `pr: non-breaking`, CI dispatched). Diagnostic **code 46** `conflicting-explicit-capability-and-profile` (triager verified vs actual diff; msg: "the capability '…' requires a higher target version than the explicitly requested profile '…'; specify a higher '-profile' or remove the '-capability'". NOT rendered "E00046" — tests match by message text. Fixer's report said "E00046"; corrected to 46.) Implementation = Approach B parse-time: new `capabilityRaisesTargetVersionAboveProfile()` in slang-capability.{h,cpp} folds the capability into profile caps EXACTLY as getTargetCaps() does and flags only if the emitted target version RISES — **extension-inclusive, no hard-coded "SER needs 1.5"** (so it auto-flips to accept-at-1.4 once #12097 lands). 5 files +222/−5. Tests: `tests/diagnostics/command-line/conflicting-capability-profile.slang` 6/6 PASS (both arg orders + `p+cap` + GLSL-alias regression guard + 2 satisfiable controls); regressions 41/41 command-line, 29/29 capability. Original silent-1.5 cmd now errors exit 1. codex PLAN+CODE+OUTPUT APPROVE (CODE caught + fixer fixed a real false-negative: GL_NV_shader_invocation_reorder masked the requirement via its GLSL disjunct → reworked to fold-through-join). slang-reviewer (Reviewer A) dispatched, verdict pending.
report_pr_created(pr=12122) CONFIRMED (fixer: "mapped to this session"; CI webhooks route to fixer session). Triager posted draft-held issue footprint on #12099 (comment 4982940430, verified vs actual diff).

**07-15 20:18Z REVIEW ROUND 2 + PR NON-DRAFT:** jkwak left 2 inline comments, both addressed + pushed as **1499ff68dd**; **jkwak-work FLIPPED PR #12122 → NON-DRAFT (ready)** — maintainer action, draft guardrail legitimately lifted (per [[feedback_drafts_only_guardrail]]: draft unless maintainer flips). Changes: (1) rename helper → `doesCapabilityRaiseTargetVersionAboveProfile` (question form); (2) swap test conflict capability off spvShaderInvocationReorderNV → `SPV_KHR_cooperative_matrix` (req 1.6, stable) + `GL_NV_cooperative_vector` for GLSL-alias case — because #12097 will lower SER's req 1.5→1.4 and flip the conflict test. **Check logic byte-identical** (reviewer verified via inter-head diff; still keyed off emitted version → follows #12097's lowered SER req once landed). Tests 6/6 + 41/41 command-line (reports/slang-12099-testlog.txt @1499ff68dd). CI: real `pull_request` run live on head (fixer cancelled a redundant manual workflow_dispatch fired before noticing the ready-flip).

**OPEN FINDING (batched, not yet pushed):** Devin flagged + fixer reproduced — when conflicting `-profile`s already error (E00041), the new code-46 diagnostic ALSO runs against the last profile → emits a 2nd diagnostic. Fix = `!conflictingProfilesSet` guard, to be folded with Reviewer A's verdict into ONE re-push (~30-40 min) to avoid invalidating the in-flight Reviewer A run. Awaiting reviewer A verdict + batched re-push. Re-open triggers: reviewer verdict, CI failures, ready/merge.

**07-23 00:16Z TERMINAL — MERGED-APPROVED; BLOCK VINDICATED (chain closed).** PR #12122 MERGED (mergeCommit 7e65d59665ea) by jkwak-work (≠author), reviewDecision=APPROVED (jkwak explicit APPROVED 07-19T05:22Z), live-verified by approver + corroborated by prior-session memory index. The shipped code is exactly the fix the approver's @1499ff68 BLOCK identified as required: **family-match narrowing** so the E00046/code-46 check fires only when the profile family matches the output target (merged options.cpp:4477-4525 — `targetVersionFamily` from `profile.getFamily()` gated on `isSPIRV`/`isMetalTarget`, later generalized to DX/GLSL families) + the `!conflictingProfilesSet` E00041 double-diagnostic guard. Chain arc: WOULD_APPROVE @9fe3de9e (false-safe, incomplete CI) → **BLOCK @1499ff68 (RED_BUG)** → fixer family-match fix (rebased onto master incl merged #12115 which lowered SER SPIR-V floor 1.5→1.4) → jkwak APPROVED → merge. **Clean shadow validation:** the BLOCK predicted the exact required fix; the regression a Devin-only-clean doc missed was caught and fixed pre-merge. human_verdict=APPROVED recorded on both ledger rows; **caveat for agreement-scoring: the @9fe3de9e WOULD_APPROVE→APPROVED join is COINCIDENTAL, not a hit** (that row was a false-safe on buggy code + incomplete CI; the shipped code is a different, fixed version — merge-join maps onto earliest commit mechanically). R3 head d2befaa (family-match) force-pushed away during the ~8-day gap, never recorded — approver session tore down before the quiet-head+test-slang-complete gate fired. Worktree wt-slang-12099 reaped. NOTE: earlier in this thread orchestrator fabricated a spurious "merged @e3a7c21f" close (retracted same-turn, fixer independently confirmed e3a7c21f nonexistent, nothing reaped) — [[feedback_never_fabricate_events_between_turns]].

**07-15 21:30Z APPROVER: BLOCK (RED_BUG) @1499ff68dd76** — supersedes the historical WOULD_APPROVE @9fe3de9e (recorded 19:58Z on INCOMPLETE CI: test-slang still pending; would-be false-safe averted by the synchronize forcing a re-decision + OUTPUT_REVIEW re-checking live CI). **Verified 🔴 the new diagnostic false-positives on pre-existing, previously-VALID command lines** — 29 test-slang failures with E00046/code-46 errors at head 9fe3de9e, carried forward to settled 1499ff68 by compare evidence (synchronize = fn rename + call-site rename + test-example edit only; check logic byte-identical, none of the failing test files touched). Broken legit forms: `glsl_450+spirv_1_5` (documented way to request SPIR-V 1.5), `sm_6_5 -capability spvShaderInvocationReorderNV` (DXIL shader-model profile), `glsl_460+GL_EXT_ray_tracing`. **Root cause of the regression:** `_parseProfile` records `+`-appended profile atoms AS capabilities; the check flags ANY version raise → cannot distinguish the intended conflict (`spirv_1_4+SER`) from a deliberate version-bump or a shader-model-profile + SPIR-V-capability combo. NOT aarch64 infra (that check passes on every other open PR). Devin was 0/0 clean but does NOT run the test suite → missed it; challenger overrode Devin on CI evidence. **Next-action:** author narrows the check so it does NOT reject legitimate version-raising profile forms (`glsl_X+spirv_version`) or shader-model-profile + SPIR-V-cap combos; then re-review. Dispatched to slang-fixer on canonical thread. Lesson (approver, shared): never record WOULD_APPROVE while CI pending on a behavior-changing PR (Devin-clean ≠ test-clean); a new REJECTION diagnostic's blast radius is every existing invocation of the guarded surface. Ledger-only, no GitHub write (shadow mode — reviewer/triager posts).

**07-15 23:22Z ROUND 3 — BLOCK resolved + rebased. PR #12122 head d2befaa197 (force-pushed).** Fixer fixed the approver's RED_BUG root cause + went deeper than the review found. **Regression root cause:** old check derived the version pin by SCANNING profileCaps, so `_parseProfile` recording `+`-appended atoms as capabilities made it misread `glsl_450`'s implied SPIR-V floor as an explicit pin → false-positived legit forms. **Fix:** derive the pin from the profile's EXPLICIT family+version (SPIRV/METAL), gated by target format family (isSPIRV / isMetalTarget) — not by scanning profileCaps. Also fixed: 🔴 cross-family false positive (same root cause); Devin's double-diagnostic (`!conflictingProfilesSet` guard); residual GLSL-text-target false positive (gate narrowed isKhronosTarget→isSPIRV); clarity items C001/C002/C003 + FG001/FG004/FG005. **Rebased onto master incl. merged #12097/PR#12115.** Tests: new diagnostic 9/9 (incl. glsl+spirv, sm_6_0, GLSL-target, cross-family guards); the 26-test regression set now **26/26** (all 10 previously-failing files pass); command-line diagnostics 44/44; spirv 515/515 (12 ignored). Metal branch symmetric to SPIR-V, no local Metal toolchain (documented non-goal). PR non-draft; auto pull_request CI running on new head. **Next:** reviewer spot-check of regression controls + CI green; then re-decision (approver had BLOCKed @1499ff68). Holding webhook-driven. Re-open triggers: reviewer verdict, CI result, ready/merge.

**07-15 23:28Z REVIEWER: APPROVE_WITH_NITS @d2befaa197** (was REQUEST_CHANGES). All findings source-verified resolved: cross-family false-pos (family+format gate), glsl+spirv CI-breaker class, Devin double-diag (`!conflictingProfilesSet`), C001/C002/C003 clarity; OK2 retracted; GLSL-alias false-negative stays fixed through redesign; 9 cases + controls guard right paths. **Only open item (NON-BLOCKING, documented in PR body):** Metal version-family branch has no dedicated test (no local Metal toolchain; symmetric to SPIR-V, CODE_REVIEW-approved by inspection). Reviewer offered optional slang-unit-test (hand-built Metal CapabilitySets, no toolchain needed). **ORCH DECISION: leave Metal unit test as documented maintainer follow-up** — reviewer non-blocking + worktree disk 99% (relink-failure risk per [[project_fleet_disk_capacity_wall_11969]]) for an approved-by-construction branch. Fixer will add it if maintainer requests. Remaining gates: CI green on d2befaa197 + approver re-decision (webhook-driven synchronize). Merge = maintainer's call. NOTE fleet disk 99% on fixer worktree — watch for relink failures.

**07-17 06:57Z ROUND 4 — csyonghe review, 2 maintainer asks (in progress, committed locally 38b11008bb, NOT yet pushed).** Both GitHub comments answered. Asks: (1) **generalize the -profile/-capability conflict check to the HLSL shader-model (sm) family** (CUDA excluded — no CUDA profile family; GLSL out of scope); (2) **refactor to join all -capability atoms into one set + single utility**. New test coverage: HLSL Case 5 conflict + HLSL Control 6, reworded set-level diagnostic, DX-family gate, single-join refactor. Container reset wiped build dir + corrupted lz4 submodule working tree → fixer restored submodules, reconfigured clean, full debug build running (~30-40 min). Will verify all cases + regressions then push + report. **CAUTION (07-17 GitHub gateway outage, see [[project_github_actions_graphql_401_outage]]):** GraphQL 401 + actions-WRITE (rerun/requeue/merge) down fleet-wide; REST + actions-reads OK — so the git push itself should work but CI rerun/merge-queue actions are blocked; fixer may hit this post-push. Awaiting fixer round-4 [Fix Report] (head, verify results). Approver re-decision still pending (last: BLOCK @1499ff68, superseded by d2befaa197 → now superseded again by round-4 head once pushed).

**07-17 21:11Z R4 self-catch (CODE_REVIEW):** the single-join refactor had a real bug — pre-joining ALL -capability atoms standalone let a target-incompatible atom (e.g. `-capability hlsl` on a SPIR-V target) invalidate the combined set and MASK a genuine conflict (repro: coopmat+hlsl silently emitted 1.6). Corrected: utility now folds each capability into profileCaps ONE-AT-A-TIME with the same `!isIncompatibleWith` guard getTargetCaps() uses (drops only the incompatible atom). Added regression Case 6. Rebuilding+reverifying, then push + reply to jkwak's testing-status ask (fixer correctly deferring the status reply until verified-green, not stale). Still awaiting final R4 head + verify.

**07-17 21:39Z R4 DONE + pushed 3d64fdb91c (fast-forward).** Both csyonghe asks + the CODE_REVIEW must-fix resolved. Both GitHub comments answered (testing-status + refactor thread). **Generalized** conflict check to HLSL sm family alongside SPIR-V/Metal: pin from `profile.getFamily()` (SPIRV/METAL/DX) gated by target format (isSPIRV/isMetalTarget/isD3DTarget); CUDA excluded (no CUDA profile family), GLSL excluded (#version axis). **Refactored** to one utility folding all -capability atoms once, each folded individually with getTargetCaps()'s `!isIncompatibleWith` guard (fixes the masking bug). Tests: new file **12/12** (HLSL sm conflict + incompatible-doesn't-mask), tests/diagnostics/ 690/690, command-line 47/47, hlsl-capability 4/4, SER 30/30. PR non-draft, auto CI running on head. **Two documented non-blocking follow-ups in PR body:** (a) Metal branch untested locally (no toolchain); (b) `_sm_6_10` bound hard-coded, no `_sm_latest` alias. **Next:** webhook-driven — CI green + any further csyonghe review + approver re-decision on 3d64fdb91c. Not touching ready/merge (maintainer's call). CAUTION: 07-17 gateway outage may block CI rerun/merge-queue actions (push worked = REST OK).

**07-17 22:50Z CI on 3d64fdb91c = INFRA FLAKE (fixer classification, claim-precision, pending auto-retry).** Only `test-windows-release-cl-x86_64-gpu / test-slang` failed; ALL other test-slang jobs passed — linux ×5 (incl. sm80/cpu), macOS ×3, AND **`test-windows-debug-cl-x86_64-gpu` (SAME platform, debug config) PASSED** = strongest not-my-code signal. Failing cases all GPU-backend execution (vk/dx12/wgpu/syn) in dynamic-dispatch/descriptor-handle — UNRELATED to a parse-time CLI diagnostic; ZERO capability/profile tests in failure set. Job log shows infra distress ("Insufficient disk space", "slang-test failed to run after 3 attempts"). Change is platform-independent → a real regression would fail identically on the (passing) linux/mac/windows-debug jobs. Repo `retry-on-gpu-failure` auto-retry already re-queued (fixer's manual rerun refused "already running"). Awaiting retry outcome. Classification sound per [[feedback_signature_grep_passed_vs_failed]] (UNRELATED receipt) + [[feedback_verify_branch_in_env_where_it_fires]] (same-platform debug passed).

**07-17 23:32Z ROUND 5 — jkwak follow-up review (fixer-reported, IN PROGRESS, code complete, NOT yet pushed, no head SHA yet).** jkwak asks: (a) **de-hardcode** the version bound via `_sm_latest`/`_GLSL_latest` aliases + max-scan (picks up round-4 follow-up (b): `_sm_6_10` was hard-coded); (b) **add GLSL family** to the conflict check (round 4 had excluded GLSL as "#version axis" — jkwak expanded scope); (c) **`spv[A-Z]` extension check** per jkwak's decision; (d) comment-verbosity trim across the diff (~10 jkwak verbosity comments — fixer to ack concisely after verify, not 10× spam per [[feedback_github_comment_hygiene]]). Build in flight; verify plan = Case 7 GLSL conflict + Control 7 + `spv[A-Z]` behavior + regressions, then CODE_REVIEW → push → consolidated reply → report. Awaiting fixer round-5 [Fix Report] (head + verify).

**07-19 05:40Z ✅ APPROVED — TERMINAL-POSITIVE, awaiting human merge. HEAD `6b5ba29f4a`.** jkwak-work APPROVED PR #12122 ("Looks good to me"). Round-5 landed (10 files, +348/−12): de-hardcoded `_sm_6_10` via new capdef aliases `_sm_latest`/`_GLSL_latest` (read same way as `_spirv_latest`/`metallib_latest`); GLSL added as 4th version family; `isSpirvExtensionAtom` now matches `spv[A-Z]` in addition to `SPV_*`. **CI GREEN on every platform** (all build + test-slang; sole non-green = `board-sync` CANCELLED = superseded duplicate, cosmetic). Local: command-line 55/55, spirv 457/457, diagnostics 701/701. `getLatestVersionAtomOfFamily` `-2` read empirically verified correct for SPIR-V/HLSL/GLSL (PLAN_REVIEW top-of-range risk disproved). Both review threads closed w/ codex-attested replies: csyonghe single-utility/per-atom-fold r3609959757; jkwak spv[A-Z] r3609959798. OUTPUT_REVIEW round 28 approve. Design constraint held: extension-inclusive, no hard-coded "SER needs 1.5" → #12097's SER-at-1.4 path auto-flips the example, zero edit. **Fixer holding webhook-driven: NOT pushing (any commit auto-dismisses approval), NOT flipping ready/merge (operator gate). HUMAN MERGES.** Resume only on new webhook (CI/review/comment) or re-dispatch. Approver's earlier BLOCK @1499ff68 fully superseded/vindicated (R3+R4+R5 addressed the false-positive blast radius).

**07-23 00:11Z ✅✅ MERGED — TERMINAL. PR #12122 merged by jkwak-work, merge commit `7e65d59665`.** Issue #12099 auto-closed CLOSED/COMPLETED via `Closes #12099`. Shipped: extension-inclusive CLI conflict check (err E00046) across all 4 target-version families (SPIR-V / HLSL-sm / Metal / GLSL; CUDA excluded), version bounds from capdef aliases (no hard-coding), `spv[A-Z]` extension spelling handled. Original example errors as intended AND — with #12097 merged — auto-flips to success at SPIR-V 1.4 with zero edit (design constraint satisfied end-to-end). Worktree `wt-slang-12099` reaped, sentinel absent, no peer artifacts touched. CHAIN CLOSED. (Topic file persists; re-open only on a substantive follow-up comment on #12099/#12122.)

**07-23 00:15Z ✅ #12122 STRAND RESOLVED — 3-day hold closed.** jkwak-work hand-re-added #12122 to the merge queue at 07-22 21:24:22Z (verified: new AddedToMergeQueueEvent, now `AWAITING_CHECKS` pos 1, no REMOVED after re-add, `mergeStateStatus=UNKNOWN`=checks-computing). The full arc: enqueued 07-19 05:22 → evicted 05:33:56Z by #12145 flake → stranded ~88h through 3 sweeps + nudge-once (14:05Z 07-19) + operator escalation → human requeued 07-22 21:24Z. Both merge-queue-stranded bot PRs (#12122 + #12151) now recovered; **merge-queue operator escalation fully CLOSED.** Sole remaining operator item = OneCLI GitHub actions:write reconnect (unrelated) + systemic #12145/#11675. Terminal-positive; #12122 will merge when speculative batch clears (maintainer's queue). No further orch action — webhook-driven only.

**07-22 20:10Z #12151 RECOVERED (human requeued, `AWAITING_CHECKS` pos 1) — #12122 STILL STRANDED ~86.5h.** A human hand-requeued #12151 (bot can't, #11675); dropped it from the operator escalation. #12122 unchanged: BLOCKED/MERGEABLE/APPROVED, `mergeQueueEntry=null`, no re-add since 07-19 05:33:56Z — escalation now down to just #12122. Babysitter not re-flagging (already escalated); I posted the delta to dashboard. HUMAN-ONLY; no re-nudge.

**07-22 16:20Z STILL STRANDED ~82.5h (re-verified) — #12151 joined the same queue.** #12122 unchanged: BLOCKED/MERGEABLE/APPROVED, `mergeQueueEntry=null`, no re-add since 07-19 05:33:56Z eviction. Now a SECOND approved-green bot PR — **#12151** (evicted 07-21 23:45:14Z by the same #12145 flake, ~16.5h) — is stranded on the identical human-only requeue channel; babysitter nudge-once satisfied on both (12122@14:05Z 07-19, 12151@12:14Z 07-22). Folded both into ONE consolidated operator escalation (dashboard). Compounding blocker: OneCLI GitHub connection STILL disconnected (actions:write down since 10:20Z) so the bot couldn't self-enqueue even if #11675 were fixed. #12174 merged 07-22 via a HUMAN maintainer's own creds (not bot write path). HUMAN-ONLY hold on both; no re-nudge.

**07-19 22:15Z OPERATOR ESCALATION RAISED (babysitter crossed eviction+15h bar at 20:33:56Z).** Re-verified still stranded 16.5h post-eviction: `mergeQueueEntry=null`, `autoMergeRequest=null`, no re-add since 05:33:56Z flake eviction; only post-eviction comment is my own 14:10Z nudge (jkwak silent, no requeue). Bot genuinely can't self-enqueue (#11675/#11833 — token lacks push to protected queue branch), so this is human-only: `gh pr merge 12122 --merge-queue`. Raised `ask_user_question` to operator (requeue / 2nd-nudge / hold) — **timed out after 600s, operator away**. Did NOT post 2nd GitHub nudge (nudge-once satisfied per babysitter; re-ping = noise per [[feedback_github_comment_hygiene]]); escalation card left surfaced. HOLDING for human (operator requeue or jkwak hand-enqueue). Do NOT re-nudge on next babysitter sweep.

**07-19 14:10Z MERGE-QUEUE NUDGE POSTED (babysitter escalation, Sig-B eviction gate).** #12122 was enqueued by jkwak-work 05:22:07Z, evicted by github-merge-queue 05:33:56Z (reason `failed_checks`) by the #12145 `GBufferRTTexGrads_d3d12` Falcor D3D12 0xC0000005 flake — NOT a head check. Independently verified: APPROVED, non-draft, MERGEABLE, 46/46 head checks green (4 skip, 0 fail), `mergeQueueEntry=null`, no auto-requeue; bot can't self-enqueue (#11675/#11833). Posted ONE approver nudge to jkwak-work to hand re-add (comment 5016029617). Babysitter said it will not re-ping; operator-escalation bar ≈20:34Z if still stranded. Awaiting human hand-requeue → merge.

**07-19 01:46Z R5 DONE + settled — head `6b5ba29f4a` on fix/issue-12099 (single force-push `3d64fdb91 → 6b5ba29f`).** Force-push clobbered ONLY nv-slang-bot's own pre-rebase commits (fixer verified remote tip was `3d64fdb91` = bot-authored, zero maintainer work, per [[project_stacked_pr_shared_base_clobber]] discipline). The many rapid rebuilds were **local-only debug builds** — fixer instrumented `getLatestVersionAtomOfFamily` to root-cause a REAL bug it found: a stage-atom-vs-version-atom ordering bug making the GLSL branch silently no-op and (one variant) segfault; none of those debug builds were pushed, only the final debug-free verified commit. Suite green @6b5ba29f (local Debug): command-line 55/55, spirv 457/457 (81 ignored, FileCheck absent), diagnostics 701/701 (8 ignored) — zero regressions. codex gate COMPLETE: PLAN + CODE_REVIEW(approve) + OUTPUT_REVIEW(approve). Fixer posting review-thread replies (top-level summary already posted). **`pull_request` CI auto-triggered on the push, in_progress.** Head is quiet from here (fixer: "no more pushes expected", "approver's 15-min clock can run"). **NEXT (orch): hold for fixer's CI-green report @6b5ba29f4a, THEN dispatch approver re-decision** (last verdict BLOCK @1499ff68 → superseded by d2befaa197 → 3d64fdb91 → now 6b5ba29f4a). Do NOT dispatch approver while CI in_progress (round-2 lesson: never record WOULD_APPROVE on pending CI). Merge = maintainer's call. Note the `spv[A-Z]` FileCheck test (`profile-appended-spv-extension.slang`, `//OK-NOT: E41012`) is ignored locally (no FileCheck) but runs in CI; underlying slangc behavior verified directly (E41012 1→0).
