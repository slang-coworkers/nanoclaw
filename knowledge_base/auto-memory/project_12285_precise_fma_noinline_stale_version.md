---
name: project_12285_precise_fma_noinline_stale_version
description: "slang#12285 precise FMA compensated-sum wrong unless noinline — stale-version dup of"
metadata: 
  node_type: memory
  type: project
  originSessionId: 04a03e1f-29f2-49e9-806a-649c4ec6a031
---

shader-slang/slang **#12285** (ccummingsNV): Neumaier compensated-summation shader under `SlangFloatingPointMode.precise` returns wrong result on Vulkan (GB300, driver 610.43.02) when correction helper is **inlined**; `[noinline]` fixes it (function-call boundary = driver optimization barrier). CUDA correct both ways. Symptom: emitted SPIR-V had `OpFAdd`→`GLSL.std.450 Fma`→`OpFAdd` but **0 `NoContraction`** decorations.

**RE-OPENED 2026-08-05** — human comment 5195147926 (jhelferty-nv, NV maintainer) confirms the fix but names a remaining gap in **a different repo**: slangpy pins Slang **2026.12**. Slang side genuinely done; residual is a slangpy dependency-version task, NOT a slang fix. → `slangpy-triager` → **slangpy#1092** filed + cross-link comment 5195351159; fixer dispatched to judge draft-vs-issue, NOT to land a bump. Steps 10–11 (fixer `[Fix Report]`) OPEN — chain not closed.

**I VERIFIED at primary source (2026-08-05), did not inherit:**
- Pin: `external/CMakeLists.txt:85` @slangpy main = `set(SGL_SLANG_VERSION "2026.12" ...)`. ✅ Mechanism confirmed = prebuilt **release tarball** via `sgl_download_package`/`FetchContent` with version interpolated into BOTH tag and filename, **no `URL_HASH`** — moves only on manual bump. Escape hatch exists: `SGL_LOCAL_SLANG=ON` + `SGL_LOCAL_SLANG_DIR` builds against a local slang checkout.
- Fix commit `33f9ed0cea` = "Emit NoContraction under -fp-mode precise on direct SPIR-V (#11935)", 2026-07-07. ✅
- **Per-tag containment (`compare/<tag>...33f9ed0c`; `ahead`=fix ABSENT, `behind`=PRESENT), with positive control (`fix...fix`=`identical`):** ABSENT from v2026.5.2, v2026.12, v2026.12.1, v2026.12.2 · PRESENT in v2026.13, v2026.13.1, v2026.14, v2026.14.1. ⭐**Confirms the trap: the 2026.12.x patch line does NOT carry the fix — a patch-level bump silently fixes nothing.**
- ⚠️**TAG THE TRIAGER'S TABLE MISSED: `v2026.12.0.1` (published 2026-07-16, i.e. AFTER v2026.13) returns `diverged`, not `ahead`** — a patch cut from an older branch. `diverged` ALSO means fix absent, so the verdict holds and is in fact strengthened. ⭐**Lesson: ENUMERATE tags from `gh release list`, never from the version numbers you expect.** 🔴**MY OWN ERROR, corrected 18:23: I wrote "a third status" — GitHub REST compare has FOUR (`ahead`/`behind`/`identical`/`diverged`), and it's the REST status, not `git`'s. I inherited the peer's framing while "verifying" their data; their codex-critique caught it, not me.** ⇒ ⭐⭐⭐**Verifying the DATA is not verifying the CLAIM — I confirmed every tag/status fact and copied the wrong generalization sitting around them.** Use `merge_base_commit.sha == fix sha` as the unambiguous test. → [[technique_fix_containment_use_merge_base_four_rest_statuses]]
- ✅**CORRECTION PROPAGATED + RE-VERIFIED BY ME (2026-08-05T18:09):** 🔴**`v2026.12.0.1` is a LIVE FOOTGUN, not a table gap** — `compare/v2026.12...v2026.12.0.1` = `ahead_by=1` (v2026.12 +1 commit); vs fix = `diverged, ahead_by=134, behind_by=1`; and **its assets are named `slang-2026.12.0.1-<platform>.tar.gz`, byte-matching the pattern `external/CMakeLists.txt:87` interpolates** (I confirmed `slang-2026.12.0.1-linux-x86_64.tar.gz` is present) ⇒ **a selectable `SGL_SLANG_VERSION` that downloads and builds GREEN while lacking the fix; "sort by date, take newest" selects it.** Fixer re-briefed with an explicit do-not-pick-12.0.1 warning. Both GitHub artifacts corrected **in place** (slangpy#1092 body PATCHed @18:08:58; slang comment `5195351159` edited @18:09:28, `edited=true`, **still only 3 comments on #12285 ⇒ edit not append, no double-post**). ⭐Process lesson (theirs, sharper than mine): **`v2026.12.0.1 diverged` WAS in their own first raw output — the check ran, the publication narrowed it.** → [[feedback_publish_a_claim_as_wide_as_your_evidence]]
- ✅**CORRECTION v2 (18:22, triager ran codex-critique on their OWN correction; 2 defects found, verdict/target UNCHANGED).** (a) **Crossed counters**: `ahead_by=1, behind_by=1` was two different comparisons spliced; vs the fix it's `diverged, ahead_by=134, behind_by=1`, and **`behind_by=1` is the tag's own commit `f17d619e` (#12106 Val/type-DAG memoization) ⇒ structurally incapable of being evidence about the fix.** I verified: `merge_base` = `a7fbf1ab0e9d` for v2026.12 **and** .12.0.1 vs `33f9ed0ceae1` for .13/.13.1/.14/.14.1; all four REST statuses observed. (b) **"three statuses" → FOUR, and REST's not git's** (my error too, above). (c) ⭐⭐⭐**THE REAL UPGRADE — ancestry proves containment of a SHA, NOT binary behavior (a release could carry a cherry-pick under another SHA). They MEASURED the official assets: `2026.12.0.1` → 0 `NoContraction`, `2026.14.1` → 8; controls: default fp-mode → 0 on BOTH (⇒ attributable to the FLAG), `OpFAdd`+`OpExtInst` = 6 on both (⇒ same graph decorated, not different code).** I independently closed the cherry-pick hypothesis from the other side: .12.0.1's only out-of-history commit is `f17d619e`, touching `slang-ast-val.cpp`/`slang-check-expr.cpp`/… — **nothing fp-mode related.** ⇒ **2 curls + 2 slangc runs = cheap acceptance test for the bump, no slangpy build, stronger than "CI is green"** — handed to the fixer. Artifacts: #1092 body PATCHed (empirical table @line 57), slang cmt edited in place (**still 3 comments**; 0 stale `ahead_by=1, behind_by=1`, 0 "three", `four` present ✅). ⭐⭐⭐**Their kept lesson: THE CORRECTION IS WHERE THE NEXT OVERCLAIM HIDES — errors introduced while fixing arrive wearing "already reviewed" credibility, which is exactly what stops re-checking. Run the critique on the FIX, not just the original.** They'd held "corrections need the same standard as what they replace" as an abstraction and not applied it to their own ⇒ **holding a rule ≠ executing it; the gate caught what recall didn't.**
- 🔴**SUPERSEDED — target is `2026.13.1`, NOT `14.1`.** *(This line read "Target **v2026.14.1** = genuinely latest release (published 2026-07-30)". The shipped PR #1093 pins **13.1**, the EARLIEST release carrying all four fixes; 14.1 is merely also-sufficient + best-measured. See the version-choice correction below.)* ✅Still true and worth keeping: Slang release bodies are **empty** (`bodyLen:0`) ⇒ "no breaking changes in the notes" is a **NULL signal**, not clearance.

🔴**FIXER ASSESSMENT 2026-08-05T18:31:58 (comment on #1092) — PARTLY OVERTURNS THE "NO PERF EVIDENCE" BLOCKER I HAD RELAYED AS SETTLED.** ⭐⭐⭐**The blocker was "no perf data exists for ≥2026.13"; the truth was "no perf data exists IN THE LANE WE LOOKED AT." Upstream publishes it: [`shader-slang/slang-compile-perf`](https://github.com/shader-slang/slang-compile-perf) + dashboard, per-release/daily JSON with per-phase timers, NO GPU and no local build needed** (repo existence + public, VERIFIED by me). ⇒ ⛔**"We have no evidence for X" is a claim about WHERE YOU LOOKED — enumerate the surfaces that could carry it before declaring an evidence gap. A structurally-blind instrument (the commented-out `ci-benchmark.yml` lane) does not mean the measurement does not exist elsewhere.**
- **Pin history is no longer a downgrade story:** #1012→2026.10.2 (06-04), #1016→2026.5.2 downgrade "severe perf regressions" (06-05), **#1037→2026.12 (06-30, current)**; `compare/v2026.10.2...v2026.12`=`ahead` ⇒ 2026.12 already descends from the version #1016 fled. (#1037 body empty ⇒ rationale unknowable; fixer correctly refused to infer it.)
- **`minimal` workload `linkAndOptimizeIR`:** v2026.5 1.19 · v2026.7 **23.39** (regression appears) · v2026.10 16.91 · **v2026.12 (current pin) 3.55** (~3× the 2026.5 floor) · v2026.13 1.03 · **v2026.14.1 0.65** (below pre-regression floor). ⇒ **current pin still carries regression residue; the bump IMPROVES compile perf.** Low-confound pairs (control `frontEndExecute`≈0%): 12→13 −9.8%, 13→13.1 −35.5%.
- ⭐**Caveats the fixer stated rather than buried, all load-bearing:** a naive 2026.12-vs-14.1 "−25%" is **WRONG** (different sweep runs, control phase moves −19%) — do not quote · some workloads **regress ~7%** (`module_link`, `codegen_spirv`) vs 1–3% repeatability ⇒ "aggregate improved" ≠ "nothing regressed" · no commit-level attribution (13→13.1 holds TWO quadratic fixes) · **this is SLANG COMPILE TIME, not SlangPy runtime/dispatch — and #1016 never said which perf it meant, so the durable gap behind #1016 REMAINS.**
- 🔴**VERSION-CHOICE CORRECTION: `2026.13.1`, not `2026.14.1`, is the earliest release containing all FOUR needed commits.** I VERIFIED by merge_base: `33f9ed0c` and `22d27646` are in **both** v2026.13 and 13.1; **`caa2ff45` and `85d79c676` are `ahead` of v2026.13 (ABSENT) and `behind` v2026.13.1 (PRESENT)**. So 13.1 = minimal correct target; 14.1 = measures best + latest ⇒ **either defensible, maintainer's call.** ⭐**My earlier "target 2026.14.1" was not wrong but was UNDER-SPECIFIED — I had verified containment of ONE fix (`33f9ed0c`) and let "latest release" carry the rest.**
- ⚠️Also flagged: `external/CMakeLists.txt:208` only `message(STATUS)`s a missing stdlib dir ⇒ **a wrong version fails SILENTLY, not at configure time.** Build risk otherwise low (one `CACHE STRING`, no `URL_HASH`, all interpolated paths resolve in real 14.1 archives; Slang-only bump can't trip `types.h:124` submodule static_assert).
- **Next:** fixer opening a **DRAFT** PR for `skallweitNV` — explicitly not merge-ready (SlangPy-side runtime perf unmeasured, pin selection maintainer-owned). ⚠️**Draft ⇒ per this store's own rule, its CI is `skipping` and the issue needs the 5-bullet footprint; #1092 already carries it.**

⚠️**STATE PRECISION (settled 22:30, after a peer correction I半-accepted):** **slang#12285 is `state: OPEN, closedAt: null`, sole label `RTR`** — but **NOT because a Slang defect is live.** It is open because **nobody closed a stale-version report awaiting the reporter's re-test on ≥2026.13.** Evidence (all from the issue itself): the triager's own comment `5130968682` concluded *"already fixed on top-of-tree"* (0 `NoContraction` @2026.12 vs 32 @HEAD on the reporter's exact repro); the **only non-bot comment** is jhelferty-nv **agreeing** (*"sounds like this is already fixed, but will need a slang release update in slangpy"*); **zero humans disputed it.** ⇒ ⭐⭐⭐**AN OPEN ISSUE IS NOT EVIDENCE OF AN UNFIXED DEFECT — issue STATE ≠ defect STATE.** A peer argued from `OPEN` that "the Slang side was done" is unsupportable; that inference is refuted by their own posted triage. ⭐**But my "done from the start" WAS loose: precise form = "the compiler defect is fixed and released (`33f9ed0c` in v2026.13+); the issue stays open pending reporter re-test."** ⭐⭐**Two failure modes, opposite directions, same substitution — I let "fixed" imply "closed", they let "open" imply "broken". Name the artifact you measured (tracker state) vs the thing you're claiming (defect state).**

🔴**6th INSTANCE OF THE PUBLICATION ERROR, and this one is MINE (accepted 22:30):** I wrote **"#12285's compiler fix shipped in v2026.13"** — **true of `33f9ed0c`, FALSE of "the compiler fix" as a noun**, because `caa2ff45` + `85d79c676` require **13.1**. Left standing it invites the next reader to ask why we didn't pin the cheaper earlier tag — and the answer is in the two rows I'd dropped. **The narrower true statement was already in the chain.** ✅**Peer's cross-check is independently valuable: they ran `compare/<sha>...<tag>` (REVERSED from my `<tag>...<sha>`), where `ahead`=PRESENT — and it agrees cell-for-cell with my `merge_base` sweep. Two instruments pointed opposite ways converging is worth more than either.** ⇒ ⭐⭐**When a peer's table looks inverted, check the ARGUMENT ORDER before calling it wrong; `compare` is direction-sensitive and both readings are valid.**

✅✅**CHAIN CLOSED 22:28 — awaiting maintainer only.** DRAFT **[slangpy#1093](https://github.com/shader-slang/slangpy/pull/1093)** = `external/CMakeLists.txt` +1/−1, pin → **`2026.13.1`**, `Fixes #1092`, **15/15 green**, assigned `kaizhangNV`, requested `kaizhangNV`+`skallweitNV`. Rolling artifact = issue comment **`5195731476`** (ONE comment on #1092 — always edited in place, never appended; **VERIFIED: 5 corrections each marked as a correction, and the only `14 build` string left is INSIDE the correction notice quoting the superseded figure — 0 live assertions**). ⭐**Marking corrections rather than silently rewriting is what let me audit it in one pass — and it means a `grep` for the old wrong figure HITS, so distinguish CITED-in-a-correction from ASSERTED before calling an artifact stale.**
**OPEN MAINTAINER QUESTIONS (the only live items):** (1) **SlangPy RUNTIME perf is unmeasured by any lane** — the durable residual; (2) **13.1 vs 14.1 pin choice.**
⭐**Error ledger, 5 total across 3 tiers, all caught before a human acted: 2 triager (14-vs-12 builds; sparse-matrix-as-cross-product), 2 fixer (14.1-vs-13.1 asset tag; over-claimed silent-failure), 1 mine (relaying "no perf evidence for ≥2026.13" from a blind lane).** ⇒ ⭐⭐⭐**In every case the NARROWER TRUE STATEMENT WAS ALREADY IN HAND — I had "no `pull_request` trigger", they had "12 jobs over 4 pairs" — and the wider version got written anyway BECAUSE IT READ BETTER. That is the failure mode: not a measurement error, a PUBLICATION error.**
- ✅**Their `:89-113` reframing beats my "4 of 6 pairs" and I verified it:** `external/CMakeLists.txt` has **SIX** `set(SLANG_URL …)` version-interpolated branches (windows×2, linux×2, macos×2 — confirmed by reading lines 89-113 on the PR head) and **CI exercises FOUR** ⇒ **two live download paths IN THE CHANGED FILE that no build touched** (`macos-x86_64`, `windows-aarch64`). ⭐⭐**Name the untested CODE PATHS, not the missing matrix cells — "4 of 6 platforms" understates that the gap is in the diff itself.** For those two, evidence remains only "the named assets exist in the release."
- ⭐**Free tell I also missed: the sentence asserted 17 parts and 15 total simultaneously. SUM A DECOMPOSITION AGAINST ITS OWN HEADLINE — zero API calls.**
- 🔴**Their standing retraction to me: the "durable gap behind #1016" as they first stated it was WRONG.** Correct residual is narrower: **the benchmark lane never proactively tests Slang NEWER than the pin, and no lane measures SlangPy runtime at all.** ⇒ **"commented-out step" ≠ "blind lane"** — I had propagated the stronger version upstream.

✅**RESOLVED 20:30:33 — DRAFT PR #1093 LANDED, 29 min after my nudge, from the exact stalled branch.** https://github.com/shader-slang/slangpy/pull/1093 — draft, `dev/fixer/slangpy-1092`→`main`, **+1/−1 `external/CMakeLists.txt`** (pin `2026.13.1`), 7.4KB body, **`Fixes shader-slang/slangpy#1092`**, cross-ref on #1092 timeline @20:30:34. ⭐⭐⭐**I nearly escalated to the operator on a 20:01 read while the PR landed at 20:30 during a container restart — RE-MEASURE BEFORE ESCALATING; the restart gap (~108 min) was longer than the stall I was escalating.** ⇒ **An escalation is a claim about NOW; a stale read makes it a false alarm that costs credibility.** ⚠️Note `Fixes` (not `Refs`) ⇒ **merging auto-closes #1092**, correct here only because the pin bump IS the whole ask.
- 🔴**TWO OF MY OWN CLAIMS FALSIFIED on this PR — both by controls I nearly skipped:**
  1. ⛔**"A draft's CI is `skipping`" is FALSE for slangpy: #1093 ran and passed 15/15.** CONTROL that makes the green trustworthy: the same tab-aware parser on draft #1088 returns `14 pass / 1 skipping` ⇒ **the parser CAN see non-pass, so 15/15 is a measurement not an artifact.** ⇒ ⭐⭐**"drafts don't run CI" is PER-REPO/PER-WORKFLOW-TRIGGER, never a general rule — check `pull_request` trigger types before asserting it.** (My first tally used `awk '{print $2}'` on space-split output and produced garbage — `8 (linux,` — **the status field needs `-F'\t'`**; a nonsense tally is the tell.)
  0. ✅**CI GREEN CONFIRMED, but the peer's 22:23 addendum MIS-COUNTS IT — I re-derived: `15` total, NOT `14 build jobs + 3 others (=17)`.** Exact decomposition (both surfaces, head `88fbfc8610`): **`check-runs` API `total_count=14`** = **12 `build` jobs** + `pre-commit` + `board-sync`; **plus 1 legacy status context** `license/cla=success` ⇒ **15, matching `gh pr checks`.** ⭐**Their "14 build jobs" is almost certainly the check-runs TOTAL (14) re-labelled as the BUILD count** — a real number attached to the wrong noun. ⛔**Their matrix description is also wrong:** they wrote "windows/linux/macos × x86_64/aarch64 × msvc/gcc/clang × Debug/Release", but **`macos-x86_64` and `windows-aarch64` have ZERO jobs** — actual is linux×{x86_64,aarch64}×{gcc,clang}×{Debug,Release}=8 + macos-aarch64×clang×2 + windows-x86_64×msvc×2 = **12**. ⇒ ⭐⭐⭐**A CROSS-PRODUCT PHRASE IS A CLAIM ABOUT COVERAGE — "A×B×C×D" asserts every cell exists. Enumerate the job names; a sparse matrix described as full silently over-claims which platforms verified the change.** The *conclusion* (asset paths for 2026.13.1 empirically download+unpack on every platform CI covers) SURVIVES — it just covers 3 platform/arch pairs, not 6.
  2. ⛔**"My 13.1 asset check is NOT in the PR body" was MY GREP FAILING, not a gap.** It IS there at body line 48, and **more thorough than mine**: all six platform paths (`external/CMakeLists.txt:89-113`) plus all six `-debug-info` variants, citing `SGL_SLANG_DEBUG_INFO` default-ON at line 79 / consumed 211-213, and explicitly stating it was verified against 13.1 *not* 14.1. ⇒ ⭐⭐⭐**A narrow grep pattern returning empty is evidence about the PATTERN. I printed "^^ empty = NOT in the PR body" as a conclusion — an inline label that asserts a verdict the command cannot support is how a false negative gets published.**

🔴**STALL RECORD (measured 19:37–20:01, after the peer's 2nd 429 in ~29 min) — resolved above, kept for the mechanism.** ⭐⭐⭐**Applied "watch the deliverable, not the worker" — the branch/PR state is a surface no probe of mine can perturb, unlike `last_active`.** MEASURED: branch **`dev/fixer/slangpy-1092`** pushed **18:33:40**, commit `88fbfc8610` "Update bundled Slang to 2026.13.1", **+1/−1 in `external/CMakeLists.txt` only** (line 85 → `"2026.13.1"`) — **but NO PR EXISTS** (`gh pr list` since 18:00Z = empty) and **#1092 has had no activity since 18:31:58**. ⇒ **The commit landed and the PR-open step did not; two 429s bracket the gap.** ⭐⭐**A pushed branch with no PR is INVISIBLE to every human surface — the issue says "next: opening a draft PR", so silence reads as in-progress, and the work is one API call from being lost.**
- ✅**I closed a real gap the fixer left: they verified asset-path resolution against the real **14.1** archives, then pinned **13.1**.** Different tag ⇒ different asset names ⇒ the verification did not cover the shipped choice. VERIFIED by me at `releases/tags/v2026.13.1`: `slang-2026.13.1-{linux-x86_64.tar.gz, linux-aarch64.tar.gz, windows-x86_64.zip, macos-aarch64.zip}` **all present**, plus `-debug-info` variants for linux/windows (**`SGL_SLANG_DEBUG_INFO` defaults ON**, so those are load-bearing). Branch is sound. ⭐⭐⭐**When a peer verifies X and then ships Y, the verification does not transfer — re-run it against what actually shipped.** (Same family as: a draft's CI cannot verify, a test whose directives exclude the config cannot verify.)

🔴**SUPERSEDED GATING — the "no perf evidence for ≥2026.13" framing below was RETRACTED (see the fixer-assessment block above: the data exists at `shader-slang/slang-compile-perf`).** Correct narrow residual: **the benchmark lane never proactively tests Slang NEWER than the pin, and NO lane measures SlangPy RUNTIME at all** — that second half is the genuine open question. Also retracted: "last pin move was a downgrade" (#1037 later moved to 2026.12, which descends from the 2026.10.2 that #1016 fled). Kept below verbatim for the audit trail only — **do not quote it as current.**

**[RETRACTED, historical] Gating (triager's finding, NOT independently verified by me — treat as their claim):** last pin move was a *downgrade*, slangpy#1016, for "severe perf regressions", one day after #1012 bumped it; `ci-benchmark.yml:75`/`:104` have the latest-Slang path commented out ⇒ benchmark lane measures the *pinned* Slang and structurally cannot see a 14.1 regression ⇒ **no perf evidence for ≥2026.13**. Plus slang-rhi lockstep judgment (`SLANG_RHI_FETCH_SLANG OFF` — verified in the CMake I read; `types.h:124` static_assert keyed to the **submodule**, so a Slang-only bump won't trip it). All 3 recent pin moves were `skallweitNV`'s ⇒ maintainer call.

**Verdict (triage, 2026-07-30): stale-version report — already fixed upstream. No fixer, no new track, chain CLOSED.**
- Root cause = manifestation of CLOSED **#11933**, fixed by **PR #11935 / commit `33f9ed0c`** (2026-07-07), first shipped **v2026.13**. Reporter on **2026.12** (cut 2026-06-25, before fix).
- Verified locally @HEAD `7c58a326b`: reporter's exact `repro.slang` → their 2026.12 capture = 0 NoContraction module-wide; top-of-tree = **32 NoContraction** on byte-identical instruction graph, decorating the exact add the driver was miscontracting.
- **NOT a fold into [[project_12198_precise_qualifier_spirv_nocontraction]].** #12285 uses **global `-fp-mode precise`** (owned by #11933/#11935). #12198 = per-**variable** `precise` **qualifier** in **default** fp-mode (still open). Keep the distinction: global fp-mode precise vs per-variable precise qualifier are different code paths.
- Parent's "inlining drops the decoration" hypothesis **refuted**: fp-mode is target-scoped; only per-function override producer is autodiff, which only sets `Fast`. In 2026.12 decoration was globally never emitted → inlining irrelevant.
- GitHub: 5-bullet verdict posted (comment 5130968682), Type=Bug, asked MEMBER reporter to re-test ≥v2026.13. No `reproduced`/`regression` labels (symptom does NOT repro at HEAD; precise-SPIR-V never worked pre-#11935 so the fix *added* it, not a regression). Honest hedge: verified compiler symptom not GB300 runtime; residual wrong-result on 2026.13+ = downstream NVIDIA driver question.
- Triager memo: `/workspace/agent/memory/triage-12285.md`. RE-OPEN only on substantive human reply.
