---
name: VK_KHR_shader_abort follow-ups — #11528 Gap C (operator-pending) + #11790 runtime test (parked on slang-rhi#781)
description: Two follow-ups to the shipped abort feature (#11542) — Gap C emit-token conformance bug awaiting operator go/no-go; #11790 runtime test parked on slang-rhi#781 (RHI-first per jkwak)
type: project
originSessionId: 43bedef2-9dcf-4531-bd2e-53b4b0a47e9b
---
Both stem from the VK_KHR_shader_abort feature shipped via merged PR #11542 (2026-06-16): `void abort<each T>(NativeString format, expand each T args)` overloading SM4 `abort()`, lowered to `kIROp_Abort`, emitted as `OpAbortKHR` (SPIR-V) / `abortEXT` (GLSL). Compiler side has emit + diagnostics coverage (`tests/spirv/abort*.slang`, `tests/diagnostics/abort-*.slang`).

## #11528 Gap C — emit-token conformance bug (PENDING OPERATOR go/no-go → default HOLD)
Issue #11528 (closed/shipped). Verified by Main at claim-precision (2026-06-23): merged `source/slang/slang-emit-spirv.cpp` emits `OpExtension "SPV_KHR_shader_abort"` (1×; that's the *Vulkan* name) but the SPIR-V registry token is `SPV_KHR_abort` (0× in file) → a module declaring it fails `spirv-val`. Fix = one-line token + a FileCheck/spirv-val test. Flagged on GitHub (issue comment 4784413276). slang-fixer PARKED; operator go/no-go via ask_user_question TIMED OUT 2026-06-23 → defaulted to HOLD (issue closed/shipped + active author jkwak may self-fix). **Do NOT re-dispatch the fixer for Gap C without explicit operator go.**

## #11790 — runtime slang-test for abort (RESOLVED → PARKED on slang-rhi#781, RHI-first)
jkwak-work filed-by-request (#11528 comment) → triager created #11790 (assigned jkwak, labels Testing/slang-test/spirv_vulkan), then jkwak asked the bot for "a PR" (#11790 comment 4813905366). Fixer feasibility verdict (re-verified vs slang master `3873af2dbf` / slang-rhi pin `687dc18`, HEAD): **NOT FEASIBLE today** — 3 structural blockers, all in `shader-slang/slang-rhi` (separate repo): (1) no `ShaderAbort` Feature / `VK_KHR_device_fault` retrieval in slang-rhi; (2) nothing to gate `-render-features` on + no `vkGetDeviceFaultDebugInfoKHR` message path; (3) device-loss from `abort` incompatible with dispatch→readback→compare model. Gap is purely runtime/RHI. Fixer posted feasibility + B/C choice on #11790 (comment 4813957758).

**jkwak chose RHI-first** (#11790 comment 4814300428, 2026-06-27): "create a new issue on slang-rhi about the required task; I'll come back to #11790 when the slang-rhi side completes." Fixer executed (Main-verified 2026-06-27): filed **shader-slang/slang-rhi#781** (https://github.com/shader-slang/slang-rhi/issues/781, OPEN, label enhancement) covering the 4 RHI items — (1) enable `VK_KHR_shader_abort` + `VkPhysicalDeviceShaderAbortFeaturesKHR::shaderAbort` in `vk-device.cpp`; (2) `VK_KHR_device_fault` retrieval (`vkGetDeviceFaultDebugInfoKHR` + chained `VkDeviceFaultShaderAbortMessageInfoKHR`); (3) a `Feature` enum entry (`SLANG_RHI_FEATURES` in `include/slang-rhi.h`) for runtime detection/gating; (4) host-side abort-message + device-loss handling. Parked note on #11790 (comment 4814335108); **#11790 kept OPEN** (verified), assigned jkwak. No PR (work parked); (B) scaffold path OFF; fixer worktree torn down.

**RELEASED for implementation (2026-06-27):** jkwak commented on slang-rhi#781 (comment 4814514761): *"@nv-slang-bot, please give me a PR for this issue."* — explicit maintainer invite to implement + design latitude delegated (issue body says feature naming "your call"). Main dispatched **slang-fixer** to implement slang-rhi#781 → draft PR (thread `gh-issue-shader-slang/slang-rhi-781`). Scope = the 4 RHI items: (1) VK_KHR_shader_abort + `VkPhysicalDeviceShaderAbortFeaturesKHR::shaderAbort` in `src/vulkan/vk-device.cpp`; (2) VK_KHR_device_fault retrieval (`vkGetDeviceFaultDebugInfoKHR` + chained `VkDeviceFaultShaderAbortMessageInfoKHR`); (3) `Feature` enum entry (`SLANG_RHI_FEATURES` in `include/slang-rhi.h`); (4) host-side abort-message + device-loss handling. Plumbing compiles without abort-capable hardware; full runtime validation (the #11790 test) still needs a GPU/driver with both extensions.

**DELIVERED (2026-06-27 ~02:02Z):** fixer opened draft **slang-rhi PR #782** (https://github.com/shader-slang/slang-rhi/pull/782, branch `fix/issue-781`, `Fixes #781`, drafts-only — not flipped ready). 6 files +128/−4: **Vulkan-Headers bump v1.4.318→v1.4.347** (CMakeLists.txt — required, v1.4.318 predates the ext; this is the headline maintainer-decision flagged in the PR body for jkwak); `Feature::ShaderAbort` (`"shader-abort"`, enum *appended* for ABI value-stability); 3 guarded feature structs (vk-api.h); all-or-nothing device-creation enablement of `shader_abort`+`device_fault`+`shader_constant_data` (feature reported only when all three ext+bits present); `reportShaderAbortMessage()` two-call `vkGetDeviceFaultDebugInfoKHR` retrieval via existing `handleMessage` debug seam on device-loss; no new public COM/ABI method. Compile-verified clean against v1.4.347, warnings-as-errors. codex PLAN/CODE/OUTPUT run — CODE+OUTPUT approve; PLAN 4 must-fix → applied 3, declined 1 with codex concurrence (flagged queue is internal upload-only; public `CommandQueueImpl` path already routes device-loss through `reportVulkanError`). Fixer posting status footprint on issue #781.

`report_pr_created({shader-slang/slang-rhi, 782})` CONFIRMED fired (mapping → slang-fixer session, context-preserving). Issue footprint posted on #781 (issuecomment-4814666829, 5-bullet + header-bump caveat).

**MAINTAINER-DRIVEN to ready+approved (state as of 2026-06-28 ~00:15Z, Main-verified via timeline):**
- **slang-rhi#782** — jkwak flipped it **ready_for_review** (00:15:09Z, NOT the bot → no drafts-only breach) and posted **"Looks good to me."** (issuecomment-4823263549). Fixer ack'd 👀 + reply (issuecomment-4823271064) reiterating the two merge caveats (Vulkan-Headers pin bump; compile-verified only) and that the bot won't self-flip/merge. NB: fixer's msg-16 still called #782 "a draft" — STALE (jkwak flipped 4 min earlier); trust GitHub state over fixer draft-claims (recurring pattern, cf. #11538). **UPDATE 2026-06-29 ~07:49Z:** a *second* reviewer, **skallweitNV, submitted CHANGES_REQUESTED on #782 with 4 inline findings**. Session A (#782 owner) is working them in its worktree autonomously (review fixes = code work, not gated; no re-dispatch needed) — full report to follow on the #782 thread. So #782 is NOT clear-to-merge: jkwak "looks good" but skallweitNV requested changes. **ADDRESSED + PUSHED 2026-06-29 ~08:12Z:** all 4 findings fixed, lib rebuilt clean (135/135 PASS), **HEAD dfd2e66→`bef295c`** on `fix/issue-781`; fixer replied on all 4 threads, left them unresolved for skallweitNV to close (human reviewer). Changes (−29/+8, 5 files): dropped all `#ifdef VK_KHR_*` guards (slang-rhi pins its own headers v1.4.347, no arbitrary-version support → header-bump caveat now moot/justified), regrouped `Feature::ShaderAbort` into the Vulkan section (dropped append-for-ABI comment, no binary-compat guarantee), `reportShaderAbortMessage` queries `hasFeature()` (dropped `m_supportsShaderAbort`), `checked_cast<DeviceImpl*>` over `static_cast`. One judgment call flagged on-thread: used `checked_cast<DeviceImpl*>` instead of skallweitNV's suggested `getDevice<DeviceImpl>()` (latter is a `DeviceChild` member; `reportVulkanError` only has base `Device*`) — offered to switch if he prefers. CI auto-triggered by the non-draft push (no manual dispatch — correctly avoided cosmetic-red). Two further review rounds: skallweitNV round-2 (relocate `reportShaderAbortMessage` next to debug-message fns, pure move) → `bef295c→2b9f58c`; then jkwak inline-suggested the idiomatic downcast (wrap `Device*` in `DeviceChild`, `getDevice<DeviceImpl>()`, `#include "device-child.h"`) which also closed the round-1 `checked_cast` judgment call → `2b9f58c→fe9e7e1`. **APPROVED BY BOTH MAINTAINERS 2026-06-29 ~16:06Z:** jkwak submitted an **approving** review (pullrequestreview-4593419640): *"Looks good to me. And Simon agreed to merge as well."* Maintainers intend to **merge it themselves** — bot takes NO merge action (operator-gated + maintainers handling). HEAD `fe9e7e1`. Fixer (Session A) will send a definitive merge flag on the `CLOSED`/`MERGED` webhook + clean up its worktree.

**✅ #782 MERGED 2026-06-29 16:33:40Z** by jkwak into slang-rhi `main`. Merged SHA = **`fb908a7c72ef1944b36a386ddc0fa2536fa4445f`** (squash commit "Vulkan: support VK_KHR_shader_abort + VK_KHR_device_fault (#782)") — Main-verified live as both #782's `merge_commit_sha` AND the current slang-rhi `main` tip. Session A's #782 chain is terminal/complete; it cleaned up its worktree.

**✅ RE-PIN LANDED + VERIFIED CLEAN 2026-06-29 ~16:40Z** (Main dispatched Session B via msg 55, pinned; Session B responsive, pushed it; Main confirmed independently via a bounded monitor + gh checks — NOT just on Session B's word):
- **#11792** — `external/slang-rhi` gitlink now `fb908a7c72ef1944b36a386ddc0fa2536fa4445f` (== #782 merged main). New HEAD `f92bbccf8dc7ced97cac8377365d32ab17b25252`, base master, open, non-draft, `mergeable:true` but `mergeable_state:"behind"` (needs master catch-up before merge — routine; Session B/maintainer handles). CI re-triggered (queued/in-progress).
- **#11799** (stacked, base `fix/issue-11790`) — **NOT clobbered**: head `fa1a35d976...` unchanged since 06-28, `mergeable_state:"clean"`. The stacked-base clobber risk did NOT materialize. ✓
- **Session B report received + reconciled (msg 56, 16:43Z):** matches Main's independent verify. Re-pin was a **clean fast-forward** push `10544f5321..f92bbccf8d` (no force), staged diff = exactly the single gitlink line, no collateral. **`report_pr_created` mappings CONFIRMED for BOTH #11792 and #11799** (registered earlier this session, originSessionId `2d9ecedb…`, stable across compaction; not re-fired). #11799 untouched (`fa1a35d976`). Session B deliberately did **NOT** rebase #11792 onto master (would force-push + clobber #11799's stacked base — the flagged failure mode); left the `BEHIND` state for the maintainer's "Update branch"/merge-queue, which is benign for a squash-merge. Also refreshed #11792 body (merged-pin note + corrected hardware section: test ran on **L40S which lacks VK_KHR_shader_abort** → the runtime abort test skips on that CI HW, consistent with the GPU-only/opt-in gating).

**STACK COLLAPSED TO ONE PR 2026-06-29 ~17:19Z — jkwak asked to COMBINE #11792 + #11799** (#11792 comment 17:09Z: "use the ToT hash [done] … can you also combine this PR with #11799? I don't see much of differences"). This is the foldable-on-maintainer-preference path pre-authorized in msg 34. Session B folded #11799's render-test message-capture (`render-test-main.cpp`) + `abort-message.slang` into **#11792** as cherry-pick commit `6b94ff539a` (fast-forward, no force; dropped #11799's redundant re-pin), updated the body to combined scope, replied to jkwak confirming both asks. **Main-verified:** #11792 now has 4 files (`external/slang-rhi`, `abort-runtime.slang`, `abort-message.slang`, `render-test-main.cpp`), HEAD `6b94ff539a`, mergeable/behind. #11799 now genuinely superseded (content lives in #11792).

**#11799 CLOSE — NOT bot-closed (no-auto-close rule):** jkwak said *combine*, NOT *close*. Per standing operator rule (never auto-CLOSE PRs/issues — surface to a human; szihs #11719), Main DENIED the bot-close green-light. Directed Session B (2026-06-29) to: post a "superseded → folded into #11792 (commit 6b94ff539a)" comment on #11799 + explicitly ask jkwak whether he'll close it or wants the bot to; close ONLY on jkwak's explicit instruction, else leave open (it resolves when #11792 merges & `fix/issue-11790` is deleted). Bot never unilaterally closes.

**Remaining (maintainer + webhook-driven; Main passive-tracking):** the stack is now ONE PR — **#11792** catches up to master + merges (maintainer, NOT bot) → closes #11790; #11799 close handled per above. The "#11799 up for maintainer merge" milestone is MOOT. Don't auto-close #11790/#11792; #11799 close = jkwak's call. Active coordination COMPLETE — monitor stood down; Session B flags Main when #11792 merges.
- **slang#11792** — integration PR the fixer created autonomously (Jun-27 12:44Z, branch `fix/issue-11790`, base master) bundling BOTH the slang-rhi submodule bump (`687dc18`→`dfd2e66` = #782 head) AND the #11790 abort runtime test. Opened as draft; **jkwak flipped it ready** (Jun-27 17:45:54Z) → maintainer has embraced the #11790 work via #11792 (so #11790 effectively un-parked by jkwak). Open, non-draft, mergeable. **Must NOT merge until #782 lands on slang-rhi `main` AND the pin moves to the merged SHA** (currently pinned to unmerged `dfd2e66`).

**TWO fixer SESSIONS + a 3-PR cross-repo STACK (mapped 2026-06-28 via Session A's clarification + Main gh/ncl verify):**
- **Session A** = slang-rhi#781/#782 (thread `gh-issue-shader-slang/slang-rhi-781`). The one Main has been messaging. Owns the #782 worktree.
- **Session B** = `sess-1782513891995-4cesfk` (slang-fixer `ag-1780667166439-vmjrwe`, thread `gh-issue-shader-slang/slang-11790`, active/running). Owns slang#11790 + integration #11792 + follow-up #11799 + their worktrees. **Built #11792/#11799 without reporting up to Main** — visibility gap; status requested 2026-06-28. A slang-reviewer session (`sess-1782564430939-lh70bt`, stopped) is also on the #11790 thread.
- **slang#11799** — STACKED PR (branch `fix/issue-11790-abort-capture`, **base `fix/issue-11790`** = #11792's head), title "render-test: capture shader-abort message to stdout + abort message tests". Open, non-draft, mergeable, jkwak-engaged. ⚠️ Stacked-base clobber risk (cf. memory `project_stacked_pr_shared_base_clobber`): a stale checkout of #11799's chain can force-push `fix/issue-11790` back, reverting #11792. Both owned by Session B (one owner lowers but doesn't eliminate the risk).

**`report_pr_created` ownership rule (learned 2026-06-28):** it remaps the PR→session mapping to the *calling* session. #11792's mapping is owned by **Session B** (it fired it). Do NOT ask Session A (or any non-owner) to "re-confirm" by re-firing — that STEALS the PR's review/CI webhooks into a session lacking its worktree. Verify a mapping only with its owning session.

**Merge ordering / next (3-PR stack, bottom→top):** (1) maintainer merges **slang-rhi#782** on slang-rhi `main`; (2) **Session B** re-pins #11792's submodule `dfd2e66`→merged-SHA (code push, not gated; webhook-driven when #782 closes); (3) #11792 merges → closes #11790; (4) #11799 (stacked on #11792) rebases onto master & merges. All maintainer-driven — no operator action unless we're asked to bot-merge (operator-gated; `gh pr ready` moot, jkwak flipped all). Don't auto-close any of #781/#782/#11790/#11792/#11799.

**#11790 RE-OPENED → integration-PR dispatch (2026-06-27, #11790 comment 4817125769):** jkwak reviewed draft slang-rhi PR #782, said "looks good to me," and asked the bot for a **slang integration PR**: cherry-pick/bump the slang-rhi submodule to #782's commit + add slang-test abort test cases, *"I want to see if the slang-rhi PR actually works with slang-test."* This satisfies the prior park gate (#782 up + jkwak re-engaged). Dispatched slang-fixer (thread `gh-issue-shader-slang/slang-11790`). Deliverable: draft slang PR `fix/issue-11790` bumping the slang-rhi submodule pin to #782's head commit (UNMERGED — so the slang PR stays draft, can't merge until #782 merges + pin moves to a merged SHA) + abort slang-test gated on the new `shader-abort` render-feature (skips where unsupported). VERIFICATION LIMIT: no local/CI GPU with VK_KHR_shader_abort+VK_KHR_device_fault — fixer verifies compile + clean-skip only; the abort-fires/message-round-trip validation is jkwak's to run on hardware (the whole point of the PR).

**DELIVERED 2026-06-27 ~12:45Z:** draft **slang PR #11792** (https://github.com/shader-slang/slang/pull/11792, Main-verified OPEN+draft, head `fix/issue-11790`→master, label `pr: non-breaking`). 2 files: `external/slang-rhi` pin 687dc18→**dfd2e66** (#782 head `fix/issue-781`) + new `tests/spirv/abort-runtime.slang`. Key build mechanic: bumping the slang-rhi pin ALONE pulls Vulkan-Headers v1.4.347 (slang-rhi self-fetches via FetchPackage; slang doesn't override `SLANG_RHI_VULKAN_HEADERS_URL`; slang's own external/vulkan v1.4.307 unused by slang-rhi VK backend). Test design: a SIMPLE emit line (verified everywhere — `OpAbortKHR`/`AbortKHR`/`SPV_KHR_abort`) + a `-vk COMPARE_COMPUTE` line gated on `-render-features shader-abort` (macro-driven from SLANG_RHI_FEATURES, no render-test edit). The `abort()` sits on an unreachable branch (`tid.x>0x1000`, 4-thread dispatch) so capable HW creates the OpAbortKHR pipeline + gets a clean PASS WITHOUT firing the abort (sidesteps device-loss-vs-compare-model) — honestly documented as NOT demonstrating the abort firing. codex PLAN/CODE/OUTPUT approve; **slang-reviewer APPROVED as a held draft, NO code changes** (source-verified the two-stage gate: unknown `-render-features` = loud SLANG_FAIL from the pin's X-macro, device-absent = SLANG_E_NOT_AVAILABLE clean IGNORE; `x(ShaderAbort,"shader-abort")` present at dfd2e66; CHECK 1-4 match the shader; cannot-merge framing is a HARD dep). Ack on #11790 (issuecomment-4817577868). **`report_pr_created({shader-slang/slang, 11792})` CONFIRMED fired** (mapping → this slang-fixer session; #11792 webhooks route here). **Cannot merge** until #782 lands + pin moves to a merged main SHA (stated in PR body). Sole blocker external (slang-rhi#782 merge); no slang-side blocker. Follow-up webhook-driven on #11792; `gh pr ready`/merge operator-gated; don't auto-close #11790/#782/#11792.

**jkwak APPROVED #11792 (2026-06-27 ~17:54Z) + steered test design** via 2 review comments: (1) more type/arg-count variations (%f, >1 arg) — emit-level mostly covered by `tests/spirv/abort.slang`; (2) "test the messages from abort() itself" = the runtime message ROUND-TRIP. The round-trip needs NEW render-test C++ — a device-loss-tolerant capture mode that fires an aborting shader, tolerates DEVICE_LOST, and captures #782's `reportShaderAbortMessage`→`IDebugCallback::handleMessage` text for assertion. This is **scope growth** beyond the original bump+gated-skip dispatch: compile-verifiable locally only, GPU round-trip is jkwak's to run, and it's the harness piece jkwak owns. Fixer did NOT speculatively build — replied on PR (issuecomment-4819855355) with a concrete proposal + one design question (auto-capture on `-render-features shader-abort` vs explicit `-expected-abort-message` flag) and is holding for jkwak's flag-shape answer (webhook-driven). Main steer: when building, offer jkwak the option of landing the capture-mode as its own PR to keep #11792 a clean bump+test; keep honest GPU-only caveat. Both still external-blocked on #782 (the capture path lives in the #782 pin).

**CAPTURE-MODE DELIVERED 2026-06-27 ~18:37Z → stacked draft PR #11799** (https://github.com/shader-slang/slang/pull/11799, base `fix/issue-11790` = STACKED ON #11792, not master — `Feature::ShaderAbort` only exists in the bumped submodule so it won't compile on master). Adds render-test auto-capture mode + 4 abort-message `-vk` test lines. `report_pr_created` CONFIRMED; `pr: non-breaking`; CI run 28298149702. codex CODE_REVIEW approve (no must-fix). Build green; SIMPLE emit still PASS; all 4 new `-vk` lines skip cleanly (no GPU); non-abort render tests byte-for-byte unchanged (strictly gated). Honesty bar held — GPU abort-fire→device-fault→message-round-trip→FileCheck is jkwak's hardware run (PR body + reply issuecomment-4820159448). Kept SEPARATE per Main's guardrail (so #11792 stays a minimal bump that un-drafts the moment #782 lands); offered jkwak the fold-into-#11792 option. **Draft chain: #11799 → #11792 → slang-rhi#782 must merge first.** All webhook-driven; un-draft/merge operator-gated; don't auto-close #781/#782/#11790/#11792/#11799.

**PARKED — FULLY APPROVED, HELD ON slang-rhi#782 MERGE (2026-06-28 ~00:18Z).** jkwak posted "Looks good to me — blocked until slang-rhi#782 merges" on BOTH #11792 and #11799 and resolved his abort-runtime.slang review thread. Review-2 fork closed: he LGTM'd the inline `filecheck=`+`filecheck-buffer=EMPTY` design (HEAD `fa1a35d976`); did NOT want the write-to-buffer alternative. No code changes pending on either PR. Fixer posted close-the-loop acks recording the resumption plan.
**Resumption plan (when slang-rhi#782 merges):** bump #11792's `external/slang-rhi` pin `dfd2e66` (PR-branch) → merged slang-rhi `main` SHA → rebase #11799 on top. NOTE: **jkwak (maintainer) already UN-DRAFTED both** for review — #11792 ready_for_review 2026-06-27T17:45Z, #11799 2026-06-28T00:07Z (verified via timeline; actor=jkwak-work, NOT a bot breach per the maintainer-flip exception). So the resumption no longer has an un-draft step; both are review-ready (#11792 reviewDecision=REVIEW_REQUIRED). The actual **MERGE stays operator/maintainer-gated** — and #11792 must NOT merge until its pin moves off the `dfd2e66` PR-branch commit to a merged slang-rhi `main` SHA (else slang master's submodule points at a dead branch). jkwak is aware (his "blocked until #782 merges" note on both + the PR bodies + REVIEW_REQUIRED are the guards now that draft status is gone).
**⚠ Resumption trigger is NOT auto-webhooked:** a #782 merge lands on the slang-rhi repo and won't fire a webhook on these slang PRs. Fixer (correctly) scheduled NO poller (no-pollers rule). Resumption depends on the **supervise-issues cron** catching it or a manual nudge when #782 lands. Future supervisor/Main: when slang-rhi#782 shows merged, nudge slang-fixer on thread `gh-issue-shader-slang/slang-11790` to execute the resumption plan.

**Resumption SAFETY NET scheduled (Main, 2026-06-28 ~00:30Z):** `list_tasks` showed NO active tasks (pending or paused) — the supervise-issues cron the fixer was counting on is NOT armed in this Main context. To prevent a stranded chain, Main scheduled guarded recurring task **`task-1782606083585-erbd4z`** (every 4h, cron `0 */4 * * *`): a script polls `gh pr view 782 --repo shader-slang/slang-rhi` and only wakes Main when #782 leaves OPEN (MERGED → dispatch fixer to bump pin dfd2e66→merge_sha + rebase #11799, un-drafts operator-gated; CLOSED-unmerged → surface stranded PRs to operator). The waking session self-cancels the task after firing. **If you (future Main/supervisor) handle the resumption manually, cancel `task-1782606083585-erbd4z` to avoid a double-dispatch.** Note: slang-rhi#782 is now **un-drafted (OPEN, mergedAt:null)** as of 2026-06-28 ~00:25Z — progressing toward merge; un-draft of #782 is NOT the trigger (the slang PRs are gated on #782 MERGING).

**SAFETY-NET TRIGGER FIRED → NO-OP + CANCELLED (2026-06-29 ~20:00Z fire, handled this session):** guarded task `task-1782606083585-erbd4z` woke on `state=MERGED` (#782 merged 16:33:40Z, merge_sha `fb908a7c72ef1944b36a386ddc0fa2536fa4445f`). But the resumption it was meant to trigger was **already executed manually earlier today** (re-pin landed ~16:40Z, stack-collapse ~17:19Z — see above). Main VERIFIED current state before acting: `#11792`'s `external/slang-rhi` gitlink is **already** `fb908a7c72ef…` (== merged main), HEAD `6b94ff539a` (combined 4-file PR), non-draft, MERGEABLE/BEHIND. **Did NOT re-dispatch the fixer** (would be a double-dispatch on completed work + stacked-base clobber risk per line 61's warning). **Cancelled `task-1782606083585-erbd4z`** so it stops firing. No further automation armed for this chain; remaining steps (#11792 master catch-up + maintainer merge → closes #11790; #11799 close = jkwak's call) are maintainer/webhook-driven, Main passive-tracking.

**GPU-premise CHALLENGED by szihs (human maintainer, 2026-06-28, #11790 comment 4824486245):** quoted the fixer's "not verified — no capable hardware" note and said *"@nv-slang-bot you should have GPU hardware for test. Confirm at your side and update."* This challenges the standing "GPU-less" premise the whole chain rested on. Fixer VERIFIED the environment (2026-06-28, evidence in #11790 comment 4824552890) and CORRECTED the premise — **the "no GPU" framing was wrong**: this box HAS a capable Vulkan GPU (**NVIDIA L40S**, driver **565.57.01**, Vulkan **1.3.239**, slang-test `vk: Supported`). The real gap is the **driver/extension**: `VK_KHR_shader_abort` is NOT exposed by driver 565.57.01 (vulkaninfo grep → 0; the ext first ships in Vulkan-Headers v1.4.347, which 565.57.01 predates), and only `VK_EXT_device_fault` is present, not `VK_KHR_device_fault`. So the abort round-trip is **driver-gated, not GPU-absent** — `-render-features shader-abort` → `Feature::ShaderAbort` unavailable → `SLANG_E_NOT_AVAILABLE` → the 4 message-capture cases skip by design (live `slang-test -api vk` run: vk Supported, 4 ignored, 0 failed, 0 ran round-trip; SPIR-V emit side passes). Fixer did NOT claim to have run the round-trip (honesty bar held). **Implication: even after #782 merges, the round-trip can't be validated on THIS box until the driver exposes VK_KHR_shader_abort.** Fixer asked szihs to either point to a runner whose driver exposes the ext (→ run the real round-trip + post the captured `Shader abort:` text), or confirm skip-on-unavailable is the acceptable landing bar. Awaiting szihs's reply (will arrive as a bot-mention webhook → Main forwards to fixer).

**jkwak CONFIRMED the driver-gating + PARKED #11790 on an NVIDIA driver fix (2026-06-28, #11790 comment 4846244378):** *"The 'abort' feature is currently not available with the latest NVIDIA graphics driver. The Beta graphics driver is supposed to work for the feature, but I encountered a known bug (https://developer.nvidia.com/vulkan-driver). I will have to revisit this issue once the bug is resolved."* This corroborates the fixer's L40S finding and resolves szihs's "you should have GPU" challenge — the round-trip is genuinely un-runnable anywhere accessible right now (latest driver lacks VK_KHR_shader_abort; the Beta driver has a known bug). **Chain now parked on TWO external deps:** (1) slang-rhi#782 merge → lets the PRs land (their gated tests skip-on-unavailable, so landing does NOT need the driver); (2) the NVIDIA Beta driver bug fix → lets jkwak run the actual abort round-trip validation, which he owns and will revisit. We do NOT push to merge — jkwak un-drafted/approved but is in "revisit later" mode and owns the merge. Posture: handed-off / stand-by; don't close #11790 (jkwak keeps it open for his revisit).

**CURRENT STATE (2026-06-30, Main-verified — supersedes the older "resumption plan / #782 pending / two-PR stack" framing above):** slang-rhi#782 **MERGED 06-29 16:33Z** (`fb908a7c72ef1944b36a386ddc0fa2536fa4445f`); my safety-net trigger fired on that merge, was a NO-OP (re-pin already done manually), and **was CANCELLED** (no tasks remain — confirmed via list_tasks). The stack **collapsed to ONE PR**: #11799 **CLOSED 06-29 17:32Z** (folded into #11792). **#11792 is the single live artifact** — re-pinned to the merged SHA, head now `c4fbe5add` (jkwak pushed his OWN fixes for 2 CodeRabbit findings — maintainer hands-on), state OPEN / non-draft / **MERGEABLE / mergeStateStatus BLOCKED** (branch-protection — awaiting jkwak's merge, which he owns). Fixer posted the parked-on-driver ack (#11790 comment 4846282465) reflecting this single-PR state. **Two remaining deps, both maintainer-owned + webhook-driven (NO poller, NO Main action):** (1) jkwak merges #11792 whenever he chooses → likely does NOT auto-close #11790 (he keeps it open for the round-trip revisit); (2) NVIDIA Beta driver bug fix → jkwak runs the actual abort round-trip on his #11790 revisit. A #11792 merge webhooks to the fixer's mapped session; a jkwak #11790 comment webhooks via bot-mention → Main → fixer. Terminal holding; fixer flags Main on a #11792 merge or substantive change.
