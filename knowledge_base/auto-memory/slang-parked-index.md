---
name: slang-parked-index
description: "Long-tail index of chains that need no action now — parked, maintainer-owned, bot-unpushable, or merged/terminal. Split out of MEMORY.md to keep the root index scannable. Re-engage only on the stated RESUME trigger."
metadata: 
  node_type: memory
  type: index
  title: "Slang/SlangPy parked, held, and terminal chains"
  originSessionId: 7358aae4-41d9-4f9b-ba09-a17bc7230b74
---

# Parked / held / terminal chains

Everything here is **not actionable by me right now**. Each entry names its RESUME trigger. Topic files for terminal items stay on disk and are greppable by issue number even when not linked here.

## Maintainer-owned, watch-only (no topic files)

#11746 · #11759 · #11806/7 · #11927 · #12035 · #12101 · #12113 · #12139 · #9062 diff-ptr-array · spy-samples#45 coopvec · spy-samples#46 tensor-migr · #12112 compile-perf

- **[#12100 CLOSED 08-05, correctly — verified, all 3 orphaned shapes measured, guarding-only gap](project_12100_generic_nesting_exponential_compile_parked.md)** — closed as fixed-by-#12106, citing the `generic_nesting` perf page. VERIFIED: that page tracks the **typealias** chain only; the manifest's 3 depth workloads contain **no inference-built `wrap(wrap(...))`** shape (the >80x regression in the issue's own title) and no conditional-conformance shape. **All 3 regression-bearing shapes then measured on official release binaries — #12106 removed the DEPTH DEPENDENCE on every one** (post-fix flat 0.22–0.37s across depths 12–24 where pre-fix hit 96.8s/timeout). Guard proposal **#12103 OPEN, 0 of 5 workloads implemented**; one comment posted there (`5191303144`), #12100 not reopened. ⭐**A maintainer's close names the shape THEY measured — enumerate workloads at the REGISTRATION SITE and read each generator; `generic_nesting_eval` SOUNDS like the inference shape and is the typealias chain.** ⛔**Instrument + framing rules this chain earned: [[technique_compile_perf_three_platforms_and_v_staleness]]** (3 platforms: suite=Windows, maintainer=macOS, us=Linux; `slangc -v` is configure-time; perf-page history is BACKFILLED) **· [[feedback_a_claim_about_master_is_a_timestamp_not_a_version]]** (a quoted "master" is a TIMESTAMP — resolve it to its write date before judging what a later fix did to it; correct number + false comparison, and it flatters the publisher). **RESUME:** #12103 gets a PR ⇒ review whether `generic_nesting_expr` + `cond_conformance_chain` are in it, and whether the ladder clears depth 16 (that shape's steep region starts ABOVE 16).

## PENDING / HELD

#11528 shader_abort→rhi#781 · #11771 refl dup-global · #11780 simplifyIR #11779 · #11784 Conditional autodiff ICE · #10027 vec<T,4> import abort · #11732 groupshared VUID #8145 · #11709 gs byref #10641 · #11938 PathInfo leak · #11882 primal-require diff · #6319 dup sysval · #12183 refl cumul-offset · #9660 override/ext


## PARKED, RE-OPEN on fresh signal

#11669 GetDimensions WGSL/Metal · #11877 op-overload #12162 · #11963 mod-scope lambda · #11967 64-bit idx #11990 · #11903 HitObject sm69 #11907 · [#8125 empty-struct=dup #7612](project_7612_empty_cuda_struct_dedup_8125.md) fix PR #12304

## Bot-unpushable (needs maintainer PAT / workflows:write)

[#11988 nightly SpvOpt #12187](project_11988_nightly_spvopt_workflow_parked.md) jkwak · [#12062 board-sync 422](project_12062_board_sync_422_reviewer_node.md) APPROVED await jhelferty · [#12259 source-internal](project_12259_source_internal_team_source_field.md) jhelferty · [#12247 -O3 baseline](project_12247_slang_test_o3_spvopt_baseline.md) jkwak ~77benign+2real

See [[project_bot_workflows_permission]] for the permission gap itself.

## WOULD_APPROVE, awaiting merge

[#12080 __grid_const__](project_12080_grid_constant_pivot_false_safe.md) · [#12109 SpecWorkList](project_12109_specialization_work_list_scratchdata.md) · [#11545 ByteBuf chunker](project_11545_bytebuffer_alignment_chunker_stack.md) · [#9580 assoc-type-export](project_9580_glsl_legalize_layout_mismatch.md) — #12131 waits; jkwak DEFERRED ~2 sprints

## ✅ MERGED / CLOSED-TERMINAL

Topic files on disk, greppable by number. **Re-engage only on a fresh human (non-bot) comment.**

#12273 · #12265 · spy#1081 · #12244 · #12260 · #12285 · #12286 · #12268 · #12069 · spy#782 · spy#1072 · #12095 · #12219→#12263 · #12279→#12290 · #12270 · #12278→#12300 · #12276→#12288 · spy#1082 · spy#1075 · ⚠️#12226 CB-bindless→StorageBuf **RE-OPEN if unresolved** · [#12223→#12324](project_12223_debug_build_og_debuggability.md) 🏁**08-04: our #12234 closed unmerged; skiminki's #12324 MERGED `ca76f8781acd` 14:32:59Z ⇒ #12223 auto-closed `completed`, branch reaped (404 vs live control). Approver WOULD_APPROVE = human APPROVED (agreement). NOTHING RESUMES.** ⭐direction adopted ≠ our shape accepted · ⭐squash-only ⇒ join on `pulls/N`→`head.sha`, NEVER `merge-base --is-ancestor` (confident false on a merged PR) · ⛔never read approval state from a comments endpoint (empty-body APPROVED is invisible — I published a wrong "no APPROVED review" from that defect)

## Misc long-tail pointers

[#10675 Metal uniform-ptr](project_10675_metal_uniform_pointer_indirection.md) · [#9400 loadSerialized dep-src](project_9400_loadserialized_dep_source_redundant.md) · [spy#1074 onboard dashboard](project_slangpy_1074_onboard_pr_dashboard.md) jhelferty · [#12258 MetalLib 3.2 Win](project_12258_metallib_3_2_windows.md) residual `-std` bump · [#12222 lexer UTF-8 byte](project_12222_lexer_lone_utf8_continuation_byte.md) RESUME=PR · [#12284 overload silent-break](project_12284_cross_module_overload_silent_break_warning.md) rec A; RESUME=PR · slang-llvm Win (jkwak; caveat=re-roll): [#12283 JIT COFF](project_12283_llvm_jit_coff_ordered_sections_windows.md) · [#12292 gfx-smoke unload](project_12292_gfx_smoke_slang_llvm_unload_crash.md)

## Held drafts / terminal-awaiting-human (moved out of MEMORY.md 08-03 to keep the root index scannable)

Each carries a topic file; RESUME trigger named there. None actionable now.

- ✅**#6434 `nthsetbit` intrinsic — SCRUB ANSWERED 08-05, verdict cmt `5196133459`, decision routed to `jkiviluoto-nv`** (he @-mentioned asking still-relevant / reassign / close after `mkeshavaNV` went away). NOT implemented @`b0e43d657` (4 spellings 0 on MY OWN edge; controls `countbits`=24 / `firstbitlow`=7 / `WavePrefixCountBits`=4 non-zero). Parent **#7063 CLOSED-completed** `szihs` 2025-11-12, **deliberately superseded by the `optix`/`cuda` labels** (both verified present; 82 labels total; guilty control 0) — and #7063 **never listed 6434** (121-B body, 0 task markers) ⇒ real gap is 6434 carries **no labels at all**, invisible under the successor scheme. Milestone "Q4 2025 (Fall)" is a **closed** milestone still holding **134 open issues** (independently confirmed) ⇒ CLEAR not re-target. ⛔**DO NOT RE-LITIGATE THE MILESTONE AS A 6434 DEFECT: inaction here leaves it in a bucket shared with 133 other open issues — a SYSTEMIC BACKLOG, not a 6434-specific slip.** The stale-*assignee* half IS 6434-specific; the milestone half is not. Recs: unassign · clear milestone · add `cuda` · answer OP's 17-month-old spec-proposal question. **NOTHING mutated by us.** RESUME=`jkiviluoto-nv` decides, or `natevm` confirms he still wants it → then Approach A is **DESIGN-SETTLED, NOT STAGED** (`__target_switch`: `case cuda: __intrinsic_asm "__fns($0,$1,$2)"` + software `default:`; pattern `glsl.meta.slang:494-504`, default arm `slang-ir-specialize-target-switch.cpp:44`). ⛔**"fixer-ready" was MY overstatement — no patch written, no build run.** Fixer still owns: (a) the **`[require(...)]` capability list** — I checked at source, and `mod`'s list is built from **PRE-EXISTING** atoms (`cpp_cuda_glsl_hlsl_spirv_llvm` `capdef:334`, `sm_4_0_version` `:1707`; 656 alias/def total, guilty control 0), so the precedent shows **reuse, NOT that a NEW atom is needed** — DeepWiki's "likely needs one" is UNVERIFIED either way and must not be inherited as settled; (b) `extras/formatting.sh`, which **cannot run on the triager's edge** (gersemi/clang-format/prettier/shfmt all absent) ⇒ formatting is unverified, not clean. ⛔⭐⭐⭐**THE FINDING THAT MOVED THE VERDICT: the cited gist never used `nthsetbit` — all 10 revisions (2024-08-11→15) carry a hand-rolled 32-iteration software `__fns`, predating the issue by ~6 months; the body's 4-line quote is line-for-line identical to the real code EXCEPT that one line ⇒ written as a WISH, not pasted.** So the ask is ergonomics/perf, **NOT a blocker** — exactly what a close/prioritize call turns on. 2nd case added to [[technique_verify_a_cited_path_exists_at_master_before_triaging]]. ⭐**Bindable in user code TODAY, zero compiler change** (`__target_intrinsic(cuda,"__fns($0,$1,$2)")` → slangc exit 0, nvcc 12.6 exit 0; guilty control bogus name → nvcc exit 2) — **but the same decl compiles exit 0 with NO diagnostic on hlsl/glsl/spirv/metal/wgsl, emitting a declaration with no definition** ⇒ that silent hazard is the argument FOR a first-class builtin over "just hand-bind it."
- [rhi#807 disable metallib_4_0](project_slang_rhi_807_disable_metallib_4_0.md) — MERGED before our ABSTAIN ⇒ shadow; guard DELETED ⇒ that regression is now **CI-silent**
- [#11225 E36121 cap-vs-target](project_11225_capability_target_incompat_slangpy_break.md) — DRAFT spy#1088 FIX VERIFIED + `skallweitNV` APPROVED@head + CI 13✅; `true`-arm caveat CLOSED by Win CI; RESUME=promote/merge. ⚠️holds the **sign-INVERTED** non-discriminating-signal lesson ⇒ control must be *"could this have come out differently?"*, never a blocklist of known-bad patterns. 🔴**RE-OPENED 08-05 18:00Z, NO LONGER DORMANT (row kept here for reachability): `jhelferty-nv` cmt `5195409568` pulled in `@zangold-nv`+`@ccummingsNV` but framed a CONFIRMED defect w/ an approved+green fix as a speculative *"might cause some downstream issues"* ⇒ the 08-03T15:25Z in-place tracking-comment edit was MISSED (they commented AFTER it).** ⭐⭐**AN IN-PLACE COMMENT EDIT NOTIFIES NOBODY — "edit in place, don't spam" and "actually gets read" are DIFFERENT GOALS, and on a chain idle 2 days the edit is invisible to humans who arrive later.** →triager msg 21 (session-pinned `sess-1785745076437-eti3f3`) to lead with #1088 + the merge gate; new-comment-vs-edit authorization left for the triager to raise. ⚠️#1088 `mergeable_state=behind` (behind_by 1 / ahead_by 2) and a rebase force-push may DISMISS the single approval sitting on `1dc014b`. ⛔**Its green CI CANNOT observe the fix** (`SGL_SLANG_VERSION` pinned 2026.12 predates #11225) ⇒ "approved+green" ≠ "fix CI-verified"; the real proof is the fixer's local A/B vs a source build of `pull/11225/head` (28 E36121 → 0), whose positive control showed the *newer* `2026.14.1` **release** artifact would have read clean on BOTH arms ⇒ ⭐⭐**VERSION-NEWER IS NOT CHANGE-INCLUSIVE WHEN THE CHANGE IS UNMERGED.**
- [#11118 Atomic\<T\> \[mutating\]](project_11118_atomic_mutating_noncopyable_spirv.md) DELTA POSTED, pending human · [#12302 vendored license-attrib](project_12302_cmark_vendored_license_attribution.md) PLAN done, HELD-no-PR
- [#12311 (T)float-lit floors](project_12311_generic_float_literal_cast_floors.md) P1 DRAFT #12312 TERMINAL HELD · [#12298 enum:bool case-label](project_12298_enum_bool_switch_canonicalization.md) P3 DRAFT #12301 HELD OP-gated · [#12157 IR ver-bump #12158](project_12157_ir_version_check_required_status.md) **CLOSED BOTH SIDES 08-04, held on jkwak** — F2 (gate fail-open) CLOSED; **F1 PARTIAL and not closable by us** (binary builds via guarded `add_dependencies`@CMakeLists:314-315, but nothing INVOKES the wrapper — workflow push is refused server-side at receive time, invisible to `--dry-run`). Head `cb213cb05a`, DRAFT, `closingIssuesReferences`=0 (control #12303→1). RESUME=**jkwak** applies the workflow diff (+ `k_min` a/b fork, deletion-gap scope). ⭐**the auto-close was re-armed by the DISCLAIMER EXPLAINING THE DISARM — keyword TEXT and EFFECT are independent, verify the EFFECT.** ⭐**"not on `$PATH`" is a fact about `$PATH`, never about the repo — `external/lua` builds in seconds.** · [#11944 SV_Target #11945](project_11944_sv_target_location_order.md) · [#10668 -fvk-bind-globals](project_10668_fvk_bind_globals_set_binding_conflict.md) FIX AUTH-C draft-only · [#10584 SV_Barycentrics cap](project_10584_svbarycentric_capability_check.md) draft #10666
- [#11631 \[require\] SPIR-V caps](project_11631_entrypoint_require_spirv_codegen.md) SHIPPED DRAFT #11633 · [#12196 bindless](project_12196_require_bindless_texture_codegen.md) PARK · [#12316 type-layout dup](project_12316_type_layout_policy_duplication_techdebt.md) tech-debt PARK · [#9153 public-by-default](project_9153_public_by_default_structs.md) Prop 1 LV-2026 · [#9999 switch w/o cases](project_9999_switch_without_cases_diagnostic_fork.md) SEP HELD
- own-bot echoes, 0 dispatch, PARKED, **NOT regressions**: [#12320 coverage-macos segv](project_12320_coverage_macos_segfault_base_rate.md) — **HUMAN OWNER 08-05 `jkiviluoto-nv`** (jhelferty assigned); ⛔**our published ~17% was a ~3× UNDERCOUNT — CORRECTED in-place** (cmt 5166483761) to a bracket **≥7 segv / ≤18 attempt-1 fails / 11 unclassifiable**: the step RETRIES ONCE, so **08-05 reads GREEN while attempt 1 segfaulted** (`conclusion: success`, 0 non-success steps) on the SAME test as unmasked 08-02. ⭐⭐⭐**A census keyed on `conclusion==failure` has the SAME blind spot as the retry it fails to see — a green night is NOT evidence the crash didn't fire.** ⚠️≤18 is NOT a rate (retry also fires on plain `exit 1`; 6 of 12 job-fails were exit 1) · only **5 of 37 runs retain logs** ⇒ residue unrecoverable, future sampling must capture AT RUN TIME · needs workflow-YAML + actions-write ⇒ not ours · [#12321 bf16 vec→float4 -vk](project_12321_bfloat16_vector_vulkan_wrong_lanes.md) (CI-claim DISPROVED ⇒ driver-specific L40S; **fleet-host GPU findings need a driver check before filing**)
- ⛔**[WHICH ROWS MAY I RETIRE? — a CHAIN-STATE test, never a byte test](feedback_retirement_is_keyed_to_chain_state_not_bytes.md)** — 3 conditions (terminal · detail in child · no orphan); fail any ⇒ **SHORTEN, never DROP**. Standing authz for coworkers: shortening your OWN newest line + ADDING lifeboat links = always in scope; deleting another session's row on a size signal = **operator-gated**. Measured 08-05: 128,164 B / 624 files / 433 sessions / 92 targets / **20 dark past 24,400 but 0 unreachable** ⇒ report the CONSEQUENCE, not the proxy.[#12321 bf16 vec→float4 -vk](project_12321_bfloat16_vector_vulkan_wrong_lanes.md) (CI-claim DISPROVED ⇒ driver-specific L40S; **fleet-host GPU findings need a driver check before filing**)

## ⚠️ Spilled from MEMORY.md 2026-08-04 (2nd compaction) — HELD but with LIVE issues

These three were in the root index's *Held* section. They are **maintainer-gated, not closed** —
each names its RESUME trigger. Re-read the child before acting.

- ⚠️**[#12110 NonUniformResourceIndex dropped on DescriptorHandle](project_12110_nonuniform_descriptorhandle_spirv.md)** — issue OPEN; PR #12116 non-draft, jhelferty-nv + csyonghe **COMMENTED 07-30, no APPROVE**. ⛔**My RESUME broke TWICE in OPPOSITE directions (never-fires, then always-fires)** ⇒ ✅**v3 needs ALL THREE: non-bot · addressed to US not the author · changes a load-bearing input.** ⭐⭐**After any predicate fix, test it against KNOWN CURRENT STATE: if it fires now with nothing new, it's wrong.** RESUME=v3-qualifying inbound or merge
- ⚠️**[#11981 Metal whole-program out/inout addrspace](project_11981_metal_export_out_param_addrspace.md)** — issue OPEN; draft #12014 **STALE** (`2e8c12db841f`, untouched ~26d, sole referencing PR — re-verified 08-04) ⇒ **unowned live chain**. ⭐**"OWED" markers age too — re-verify each owed ITEM, not just the gate: 2 of 3 were ALREADY DISCHARGED**; only `report_pr_created(12014)` is open. RESUME=fixer calls it, or nudge jkwak
- 🔁**[nanoclaw kb-sync `pr_ready_for_review` = NO-OP](project_nanoclaw_kb_sync_pr_autoref_noop.md)** — nightly bot data snapshot → `nv-coworkers`, SELF-MERGES in **2-13s**; ⛔no `*-pr-approver` for that repo. ⭐**match on title+author+base, NEVER head branch** (`kb-sync-*` AND `kb-wiki-fold-*`). ✅verify `merged` first. 3 instances: #1063, #1070, #1073 (08-04/05). ⛔**Repo is PUBLIC and the body only ASSERTS the PII scrub ⇒ re-verify per instance, and assert TWO INDEPENDENT completeness bounds — `listed == changedFiles` (path census) AND `added-content-lines == additions` (diff body). The files listing and the diff body truncate SEPARATELY; a clean PII scan over a short fetch is silent.** ⭐**A `/users/<name>` regex hit is ambiguous — GitHub API route vs macOS home dir; read the line before calling it a leak.**
- ✅⭐⭐**[nanoclaw#1064 — a HUMAN's code PR root-caused MY 2-day-misfiled bug](project_nanoclaw_1064_tasks_group_scope_lookup_bug.md)** (szihs, MERGED `fcb39e4f` 08-04, self-merged ~3min, 0 reviews, CI green). **NO DISPATCH** — same no-route class as kb-sync but on DIFFERENT grounds: not "bot data" but **"no `*-pr-approver` wired for the nanoclaw platform repo"**, and merged before I finished reading ⇒ ⭐**re-derive WHY a standing no-route applies; the conclusion can be right for a reason that doesn't hold.** Substance: group-scoped `ncl tasks list` used `findTaskSessions()` (demands `messaging_group_id IS NULL` **+** `system:tasks*` thread) ⇒ my tasks in `sess-1776713576150-9fon2n` (NULL thread, mg SET) were **structurally invisible, exit 0**. ⛔**I had filed this as a "display artifact" 08-02 and moved on** ([[feedback_watchdog_ncl_tasks_list_empty_not_a_freeze]] header now retracts it). ⭐⭐**THE CONTROL IS ONE LINE: `ncl tasks list --session <id>` → 5 series vs bare `No tasks.`** (`--session` routes via `ownSession()`, skipping the group branch — the asymmetry IS the diagnosis). ✅**NOW LIVE — RESUME TRIGGER DISCHARGED 08-04T10:40Z**: bare group-scoped `ncl tasks list` returns **6 series** (was `No tasks.`) ⇒ the group path sees them. ⚠️my `--session` arm that tick used an ARBITRARY session id, not the task-owning one, so its `No tasks.` is **uninformative — not a control**; the bare-line flip alone is the discharge. ⭐count is 6 here vs the row's earlier 5 and the PR's 11 — **mechanism discharges, numbers don't travel.** ⛔**PR's counts DON'T reproduce on my edge** (5 series not 11, 0 failures not 53, no `*/5` at all) ⇒ **adopt the mechanism, never inherit the numbers.** ⚠️**3 of my own measurements were wrong, all "correct query over UNVERIFIED SCOPE"**: `ncl sessions list` **caps at 200** (my "79 sessions/0 tasks" was a truncated page; true 823) · **`--status all` is invalid-args and I read the ERROR as an empty set** · a 10-min timeout wrote nothing so "0 hits" ≡ "never ran". [learning](/workspace/shared/learnings/) 08-04.
- ✅⭐⭐**[nanoclaw#1065 — bounced-claim reclaim moved BEFORE the wake](project_nanoclaw_1065_reclaim_before_wake.md)** (szihs, MERGED `aef98dd4` 08-04T10:44Z, self-merged ~13min, 0 reviews, CI green). **NO DISPATCH — no-route RE-DERIVED both legs, not inherited**: only 2 approvers exist fleet-wide (slang/slangpy, `ncl groups list`) **and** the repo runs only `ci.yml`+`label-pr.yml` ⇒ **no review bot ⇒ an approver would have no harvest input** (corroborated: #1064 has 0 reviews/0 comments). ✅All 7 mechanism claims MINE-VERIFIED on baseline `fcb39e4f`: the ack-hides-from-poll filter has **no status filter**, container startup clears only `'processing'`, and step 2's wake **flips `alive` before step 4's `!alive` gate** ⇒ genuinely unreachable in both states. ⛔**The PR's deleted "should not exist" comment WAS accurate at the producer** — both `markBounced` sites hard-gate on `channelType==='agent'`; a `NULL` task row arrives via **whole-batch marking + `extractRouting` reading `messages[0]` only** ⇒ **producer-side gap the PR leaves open.** ⛔⭐⭐**Its regression test does NOT pin the fix — EXECUTION-CONFIRMED, not just predicted**: differential run (isolated worktree, own clean install) → **Arm A baseline-source+new-test PASSES** (`1 passed | 34 skipped`, 1 matched), **Arm B head+head control PASSES** (harness sound), Arm C `35 passed (35)`. The change is ORDERING inside `sweepSession`; `grep -c sweepSession host-sweep.test.ts` = **0** (control: 552 lines) — the test calls `_redriveBouncedA2aForTesting` directly, and baseline already cleared that claim (`:500-503`); the only delta it touches is a new `log.info` it never asserts. ⭐**cheap tell: changed control flow is in F ⇒ grep F in the test file BEFORE crediting a regression test.** ⭐⭐**A differential test is only differential if the OTHER files are the baseline's too** — the local clone is an UNRELATED lineage (`host-sweep.ts` differs 363 lines, own lockfile/vitest config); overlaying 2 files onto it would have tested the wrong codebase and answered confidently wrong. ⭐⭐**a live-PR read is a measurement with a TIMESTAMP** — open/unstable at 10:36Z, merged by 10:46Z; instrument fine, artifact moved.
