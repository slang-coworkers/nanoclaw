---
name: project_9153_public_by_default_structs
description: "#9153 public-by-default struct members — maintainer-authorized impl, Proposal 1, LV-2026 gated"
metadata: 
  node_type: memory
  type: project
  originSessionId: f00cad78-6d98-4ab9-9c85-a792a90c2555
---

**🎉🎉 TERMINAL / ✅ MERGED 07-22 21:39:34Z — PR #12151 merged by jkwak-work, merge commit `4f9d626be234`; issue #9153 AUTO-CLOSED (Closes #9153 fired). Feature LIVE: public-by-default struct/class members in Slang 2026.** getDeclVisibility inherits enclosing aggregate's effective visibility (mirrors interface-member rule). ~13-LOC semantic change + 3 cross-module visibility tests + user-guide & lang-ref docs. Approved by jkwak @049dac19cd, positive (COMMENTED) from Yong/csyonghe. **E30604 OPEN_GAP disposition:** maintainers merged WITHOUT folding it in — design question parked on-PR (comment 5013385417) unless re-raised; fixer's pinning test held uncommitted; was diligence-only, never blocked merge (exactly as scoped). **Shadow-vs-human calibration — JOIN RECORDED 07-22 21:45Z:** approver R3 = ABSTAIN_POLICY (OPEN_GAP) @ `049dac19cd73`; human = APPROVED+MERGED at that exact head (single-commit PR, nothing landed after R3 → E30604 shipped as-is; jkwak formal APPROVED per GraphQL review.commit.oid; author=nv-slang-bot ⇒ genuine maintainer approval, not self-merge). `record_human_verdict(shader-slang/slang, 12151, 049dac19cd73, APPROVED)` stamped on R3 ledger row. **Calibration = clean withhold resolution / AGREEMENT, NOT false-safe** (approver never claimed a defect; ABSTAIN was correct-class = design call, maintainer resolved fork toward (a) intended-hygiene + shipped). Learning captured approver-side: ABSTAIN-on-maintainer-flagged-design-fork → merged-at-exact-head-with-formal-Approve = clean withhold resolution. Chain fully closed; fixer reaped worktree/branch. Re-engage only on fresh substantive human comment.

---

shader-slang/slang **#9153** — "Syntactic sugar for public-by-default structs". jkwak-work (COLLABORATOR) authorized impl 2026-07-15 (comment 4985648666): "aligned to go with proposed solution 1 ... make a PR." Design settled, `Dev Reviewed`, Q1-2026 milestone.

**Proposal 1:** `public struct` ⇒ unmodified members default to `public`; per-member `private`/`internal` still override. **Hard gate (bmillsNV):** LV ≥ 2026 only; pre-2026 unchanged (breaking visibility change).

**Triager finding (VERIFY before relaying as fact):** semantics-only, NOT parser — parser already attaches `PublicModifier` to `public struct` and per-member override already works. Recommended approach A: mirror interface-inherits-visibility branch at choke point `getDeclVisibility` (slang-check-decl.cpp:21251), gate on `getModuleDecl(decl)->languageVersion >= SLANG_LANGUAGE_VERSION_2026`. ~8 lines. Watch: ctor-synthesis cascade (tests/initializer-list). HEAD 8e3f9163d.

**Chain:** orch → slang-triager (owns fixer wire) → slang-fixer. Thread `gh-issue-shader-slang/slang-9153`.

**✅ DRAFT PR OPEN — #12151 (07-18 14:37Z).** Fix @ `source/slang/slang-check-decl.cpp:21289-21300` — ~8 LOC, 2026-gated "effectively-public parent → public member" branch mirroring interface rule + 4 cross-module test files. Fixer's local tests: visibility 2/2 PASS, revert-drill proved 2026 test fails without fix, initializer-list 24/24 + diagnostics 681/681 unchanged. Codex 3-stage gate (CODE/PLAN/OUTPUT_REVIEW) all APPROVE. Labeled `pr: non-breaking`, CI dispatched, webhook-mapped. Held DRAFT per drafts-only guardrail (jkwak asked for a PR = draft satisfies; flip-to-ready is separate maintainer/operator gate — do NOT auto-flip). CI red run = manual workflow_dispatch draft priority-yield (wait-for-human-priority + check-ci fail, builds skipped) — cosmetic, not a failure.
- **⚠️ slang-reviewer unreachable** when fixer tried peer-review dispatch — "not addressable via messaging tool" (possible dropped a2a edge, cf [[project_coworker_named_edge_dropped_silent_hang]]). Codex 3-stage stands as review-of-record; fixer to re-dispatch when reachable. NOT blocking (draft held pending review regardless). Triager to own re-dispatch / verify edge.
- **✅ Triager-verified [Triage Resolution] 07-18 14:40Z:** read actual PR diff (matches Approach A: 2026-gated branch after interface rule in getDeclVisibility, keys off parent *effective* visibility w/ compose-transitively comment, +13/-1 + 4 test files public-struct-2025/2025-lib/2026/2026-lib); confirmed OPEN/isDraft=true/`closingIssuesReferences=[9153]`/branch fix/issue-9153→master. Issue comment id 5011371964 refreshed IN PLACE → "draft PR #12151 up, held pending review."
- **Open loop 1 — `report_pr_created`:** confirmation still PENDING from fixer (triager chasing on fixer edge). Watch per [[feedback_verify_report_pr_created]].
- **Open loop 2 — slang-reviewer peer-review:** triager CANNOT re-dispatch (slang-reviewer NOT in triager destinations; triager holds only fixer+parent edges — `ncl destinations list` confirmed). fixer→reviewer is the FIXER's edge; triager asked fixer to retry. Non-blocking (codex 3-stage = review-of-record; draft held; webhook review path exists if flipped to ready). Main deferred wiring — if it recurs & blocks, verify fixer destinations + wire_agents(slang-fixer, slang-reviewer). cf [[project_coworker_named_edge_dropped_silent_hang]].
- **Chain OPEN** pending maintainer review of #12151 (webhook-driven from here). Next human action: jkwak/maintainer reviews draft.
- **07-18 ~14:56Z — reviewer findings C001/C002 in flight** (per triager echo): review produced 2 findings on #12151; fixer owns folding them in (its edge). Content NOT yet in hand — don't treat as verdict ([[feedback_never_relay_a_verdict_not_in_hand]]). Suggests earlier slang-reviewer unreachable loop resolved / a review path fired. Fixer iterates; triager watching.
- **✅ 07-18 ~23:11Z — jkwak REVIEWED #12151, HANDOFF to Yong.** [Fix Update] direct from fixer: jkwak-work (authorizing maintainer) reviewed — *"Looks good to me but I will discuss with Yong before merge."* No code change requested. **PR now NON-DRAFT** — jkwak flipped it ready (NOT the bot → drafts-guardrail satisfied, sanctioned maintainer flip). `reviewDecision: REVIEW_REQUIRED` (jkwak's review = COMMENTED, not approval). CODEOWNERS auto-requested **csyonghe (Yong)** — the discussion partner. CI now on `pull_request` path @ head 044c1e1b09 (Verify PR Labels ✅, build/test in progress; fixer did NOT manual-dispatch — correct for non-draft). Review posture: codex 3-stage all-approve; Reviewer C = 2 ADVISORY clarity findings (nested-enum comment accuracy; extension-member test gap — non-blocking); Reviewer A (correctness) still running; Devin (B) skipped on timeout. Fixer will batch A+C into ≤1 revision round if warranted (max 2), won't push post-approval without re-verify. **Merge = jkwak↔Yong's call; nothing blocked on us.** Fixer acknowledged jkwak on PR (no-ping). Forwarded to triager to refresh issue comment 5011371964 → handoff state.

- **✅ 07-18 ~23:33Z — slang-pr-approver SHADOW verdict on #12151: ABSTAIN_POLICY (OPEN_GAP) @ `044c1e1b09b0` (mode live_late).** Ledger-only, nothing posted to GitHub (approver never posts, cf [[feedback_approver_never_posts_route_reviewer]]). 6/6 clauses PASS; Devin-only tier, 0 bugs — change clean & well-tested at the visibility choke point. **BUT challenger found an untested behavioral cascade (NOT yet verified by us — [[feedback_never_relay_a_verdict_not_in_hand]] / [[feedback_verify_before_relaying]]):** elevating unmodified members of a 2026 `public struct` to Public makes `checkVisibility` (slang-check-modifier.cpp:2360-2371, per-field via checkVarDeclCommon @ slang-check-decl.cpp:2947) fire hard error **30604 UseOfLessVisibleType** when a field's type is less visible — e.g. `public struct Foo { Helper h; }` with internal `Helper` compiles at 2025 but errors at 2026. Reachable/common shape, real 2025→2026 migration blast radius, tension with PR's "fewer annotations" goal, NO test covers it. Genuinely uncertain: intended-by-design (can't expose public field of inaccessible type) vs defect — a maintainer design call, not a proven 🔴. Corroborated by jkwak COMMENTED-not-approved. **Action:** forwarded to triager→fixer to empirically verify the 30604 cascade; if real+untested, fixer surfaces as a design note/question on the PR for the jkwak↔Yong discussion (fixer posts, closest-to-state); if intended-by-design, note + drop. Approver will record human verdict on merge/close join.

- **✅✅ 07-18 ~23:46Z — 30604 OPEN_GAP EMPIRICALLY CONFIRMED + SURFACED ON PR. Diligence loop CLOSED; chain returns to normal #9153 merge-watch.** Fixer reproduced on built PR branch head 044c1e1b09: `#language slang 2026; struct Helper{int x;} public struct Foo{Helper h;}` → **E30604 UseOfLessVisibleType** on `Helper h;` AND on synthesized `Foo.$init`. Fixer's isolation matrix pins THIS PR as cause: `-std 2025` clean (no elevation), `public module` clean (escape hatch), explicit `public Helper` clean, nested-type-elevated clean → genuinely NEW hard error for a reasonable pattern, untested in PR. **Design note posted on PR #12151 → comment 5013385417** (nv-slang-bot[bot] 23:44:53Z; triager independently verified author/URL/disclaimer via gh). Fixer posted (closest-to-state), NO unilateral behavior change. **Human fork for jkwak↔Yong (on-PR):** (a) intended hygiene — can't expose public field of inaccessible type; author makes type public / uses `public module`; needs pin test + 2025→2026 migration note; vs (b) cap member visibility at field-type visibility (small localized change in new getDeclVisibility branch). Fixer holds a passing DIAGNOSTIC pinning test uncommitted, flips either way, queued into its one batched revision round once they decide + Reviewer A lands. **Nothing blocked on us; merge = jkwak↔Yong's call.**
- **07-18 ~23:41Z — triager SOURCE-CONFIRMED the 30604 mechanism (superseded by empirical confirm above).** Trace @ HEAD 203065d66 / PR branch 044c1e1b09b0: PR's getDeclVisibility elevates a 2026 effectively-public struct's unmodified member to Public → checkVisibility (slang-check-modifier.cpp:2360-2373) compares vs getTypeVisibility(field type); less-visible type → fires 30604 (slang-diagnostics.lua:2345), per-field via checkVarDeclCommon:2947. **Genuinely NEW error:** pre-PR the member ALSO defaulted to Internal (=internal Helper) → Internal<Internal false → no error; the PR is exactly what raises member to Public while field type stays Internal → compare newly true. ModuleDecl::defaultVisibility=Internal (slang-ast-decl.h:835) unless `public module`, so common shape `public struct Foo { Helper h; }` should trip it in a normal 2026 module (`public module` = escape hatch, not general defense). Fixer running minimal single-module repro on its build edge; if reproduces → fixer posts maintainer design NOTE/QUESTION on PR (intended vs defect + pinning test + migration note), no unilateral behavior change. Holding for fixer empirical confirm.

- **07-19 ~00:13Z — synchronize: fixer addressed jkwak's INLINE review (NOT the OPEN_GAP resolution).** Head 044c1e1b09 → **f0371d1dd7** (triager gh-verified: non-draft/OPEN/MERGEABLE). jkwak asked "test the access" + add a `private struct` nested case → fixer added same-module DIAGNOSTIC test `public-struct-nested-visibility.slang` (1575B) covering public/private/internal nested shapes, fixed Reviewer-C clarity nit ("struct/class" → "aggregate (struct, class, or nested enum)"), rebased onto master 203065d66. Fixer replied to jkwak in-thread (r3609458844); CI auto on pull_request path. Fixer-verified: visibility 3/3, rebuilt clean, codex CODE+OUTPUT re-approve. **Empirical boundary note (for merge discussion):** a `private struct`'s unmodified member trips E30601 "visibility higher than parent" — but that's PRE-EXISTING (fires at 2025 + with change reverted); the 2026 rule does NOT elevate through a private/internal parent. New test documents that boundary. **E30604 design fork STILL OPEN (jkwak↔Yong) — this push did NOT resolve it.** Still open for future rounds: E30604 fork, C002 extension-member test, Reviewer A correctness verdict. Approver re-decision (synchronize) in flight @ f0371d1dd7 — corrected to expect OPEN_GAP still open (likely ABSTAIN_POLICY holds).

- **✅ 07-19 ~00:32Z — approver R2 (synchronize) re-decision: ABSTAIN_POLICY (OPEN_GAP) UNCHANGED @ `f0371d1dd791`.** Supersedes R1 row @044c1e1b09b0; ledger-only. 6/6 clauses pass; Devin-only tier re-run head-current after ~15-min debounce (0 bugs; first T+4min Devin run was stale "unknown" → discarded + re-ran on settled head — good hygiene). Verdict WITHHELD unchanged: this push addressed jkwak's separate inline review (E30601 nested-visibility pin test = PRE-EXISTING, fires at 2025 + reverted, not approver's concern) — the **E30604 getDeclVisibility branch is byte-unchanged, no E30604 pin test, no migration note**, design fork (a intended+test+migration vs b cap-at-field-type) STILL undecided (jkwak↔Yong; fixer holds flippable pin test uncommitted). jkwak COMMENTED ×2 (not approved); reviewDecision=REVIEW_REQUIRED. CI: only `check-ci` shows failure = 3s meta-aggregator empty-output pre-main-CI; build/test legs `cancelled` (superseded-R1 runs), none failed; policy doesn't require green. Approver records human verdict on merge/close join. **Chain returns to #9153 merge-watch — webhook-driven; next real events: jkwak↔Yong design call, Reviewer A correctness verdict, C002 extension test, or merge.**

- **✅ 07-21 ~18:02Z — BOTH CODE OWNERS POSITIVE; Yong's 3 asks applied + pushed → head `049dac19cd73`** (triager gh-verified non-draft/OPEN/MERGEABLE). ⚠️ **reviewDecision STILL REVIEW_REQUIRED** — both owners' "looks good" landed as **COMMENT-state** (csyonghe 17:33–17:35Z), NOT formal Approve flags → NOT merge-authorized yet (same COMMENT-vs-formal-approve distinction as [[project_12051_descriptor_reuse_pinning]]). Fixer correctly does NOT flip ready/merge. **Yong's 3 asks landed:** (1) pinned `#language slang 2025` on both cross-module consumer tests — proves elevation keys off *provider* module version not importer's; (2) **adopted Yong's simplification — getDeclVisibility 2026 branch now unconditionally `return getDeclVisibility(parentAggTypeDecl)`** (mirrors interface rule); bonus: a member of a nested private/internal struct now cleanly *inherits* that visibility instead of tripping E30601 cap; nested test rewritten cross-module; regressions clean (init-list 38/38, diagnostics 687/687); (3) docs updated (user guide + lang ref). **⚠️ NOTE: this push materially changed the getDeclVisibility branch — the exact code the approver's R2 OPEN_GAP rationale cited as "byte-unchanged." Approver re-decision dispatched @ 049dac19cd73 to re-check whether E30604 still reproduces after Yong's simplification** (triager says fork "unaffected," but approver must verify on the changed branch). E30604 design fork (intended-hygiene vs cap) STILL OPEN per triager; repro+fork posted on-PR, pinning test held ready.

- **✅ 07-21 ~18:07Z — triager SOURCE re-verify @ changed head 049dac19cd73 (self-corrected its earlier "fork unaffected" relay, which was on fixer's word).** Read ACTUAL changed getDeclVisibility branch via gh contents: **E30604 gap STILL fires — outcome-identical for public-parent/less-visible-field case.** OLD (044c1e1b09): `if (getDeclVisibility(parentAgg)==Public) return Public;`. NEW (049dac19cd): `return getDeclVisibility(parentAgg);` (unconditional, moved before interface check). For `public struct Foo { Helper h; }` w/ internal Helper: member `h`=getDeclVisibility(Foo)=**Public in BOTH** → checkVisibility computes Internal(Helper)<Public(h) → **E30604**. Yong's change is ORTHOGONAL to the gap: (a) non-public parent now inherits private/internal visibility (kills E30601 cap = the "bonus", private-parent case NOT the gap); (b) interface-member ordering (irrelevant to struct field). **Confidence boundary:** triager's EMPIRICAL matrix was at OLD head; at NEW head it's a SOURCE read predicting still-fires — approver's R3 build @ 049dac19 is ground-truth. My push to re-verify (not rubber-stamp triager's "unaffected") was correct — cf [[feedback_never_relay_a_verdict_not_in_hand]]/[[feedback_verify_before_relaying]]. Design question on-PR stands unchanged. Awaiting approver R3 empirical verdict.

- **✅✅ 07-21 ~18:32Z — approver R3 (code-owner changes) EMPIRICALLY CONFIRMED: ABSTAIN_POLICY (OPEN_GAP) SUSTAINED @ `049dac19cd73`.** Supersedes R2 @f0371d1dd791; ledger-only. Because this push changed the exact getDeclVisibility branch R2 called "byte-unchanged," approver verified by BUILD not inspection (per my instruction; did NOT take triager's word). Built slangc with Yong's refactor (`return getDeclVisibility(parentAggTypeDecl)`), codex independently re-ran + confirmed: 2026 `public struct Foo { Helper h; }` w/ internal Helper **still hard-errors E30604 TWICE** (field `h` + synthesized `Foo.init`); 2025 control + `public struct Helper` workaround compile clean. Refactor did NOT resolve gap — public parent still yields Public member over less-visible type. Proven 2025→2026 migration break (behavior change, not miscompile) on reachable code; still untested; 2 new docs commits describe feature but OMIT E30604 migration note. 6/6 clauses pass; Devin 0 bugs/0 flags. **NOT BLOCK:** E30604 is intended/opt-in/fail-safe (stricter error, no miscompile) — "too aggressive vs correct hygiene" is the open design call maintainers own, not a defect. **NOT WOULD_APPROVE:** gap proven-and-unresolved, migration undocumented, review COMMENT-only. jkwak+csyonghe both COMMENT-state; reviewDecision=REVIEW_REQUIRED (not merge-authorized). Approver records human verdict on merge/close join. **Re-verify (not rubber-stamp) was the right call — [[feedback_verify_before_relaying]] vindicated.** Chain → #9153 merge-watch, webhook-driven; next real events: jkwak↔Yong E30604 fork decision + formal Approve flags, or merge.

- **🎉 07-21 ~21:57Z — jkwak-work FORMALLY APPROVED #12151 (triager gh-verified).** `reviewDecision` **REVIEW_REQUIRED → APPROVED**; jkwak's APPROVED review is on commit `049dac19cd` == **current head** (not stale, covers exactly what's pushed); non-draft/OPEN/MERGEABLE. Yong (csyonghe) remains "looks good" COMMENTED; jkwak's is the formal Approve. **Approval ≠ merge-auth** — merge is maintainers' action; fixer correctly will NOT push (would auto-dismiss approval), merge, or flip. **KEY: maintainer approved WITH E30604 fork STILL OPEN** — effectively judged the gap acceptable / their call to fold a follow-up; pinning test held ready. **⚠️ Human APPROVED diverges from approver shadow R3 ABSTAIN_POLICY (OPEN_GAP)** — legit calibration data point (maintainer judged the fail-safe/opt-in/intended-hygiene gap OK); approver records human verdict on merge/close join (NOT now — approval can still be dismissed by a push, so join = terminal merge/close). Triager owns close-out: verify MERGED → forward final [Triage Resolution]. **Chain webhook-driven; next real event = merge (or a follow-up push, which would re-open review + dismiss approval).**

**GitHub comment trail (jkwak re-pinged 3×):** triage-incoming id 4985783248 → WIP ack id 4988924263 (re-ping 4988906492) → honest holding id 5011371964 (stall re-ping 5011345569).

**⚠️ STALL RESOLVED — cause was SESSION REAP, not the 07-16→18 gateway outage.** Fixer's session was reaped ~07-15 23:02Z (container teardown) mid-build (killed ninja 156/1176, still in spirv-tools deps, hadn't reached changed file). Worktree `wt-slang-9153` + **uncommitted** test/code edits deleted → NEVER pushed → no 401/403 → my outage flag did NOT apply (verified NOT the cred; did NOT reinforce operator escalation). Branch `fix/issue-9153` in base clone = 0 commits beyond master (matches upstream-404). **Nothing lost that matters:** plan report `reports/slang-9153.md` survived, design intact vs HEAD, re-doable in one clean pass. Fixer self-recovering 07-18 ~13:09Z: recreate worktree → re-author 4 tests + ~8-line getDeclVisibility change → commit → build → draft PR. ETA ~30-40 min (build = long pole). No re-dispatch needed. Fixer's lesson: commit WIP immediately.

**Briefing memo:** triager's full memo (A/B/C solution space, file:line @ HEAD 8e3f9163d, cascade watch-items, 4 tests) at inbox path `/workspace/inbox/a2a-1784153638851-ngpuhg/triage-9153.md` (msg #6).
