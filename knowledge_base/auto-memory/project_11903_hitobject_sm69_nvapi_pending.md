---
name: project_11903_hitobject_sm69_nvapi_pending
description: #11903 HitObject sm_6_9 NVAPI — PR #11907 NON-DRAFT (maintainer-flipped), jkwak 3 review asks done @ 28f6a3d95d; MERGEABLE/REVIEW_REQUIRED; still blocked-on #10660 + jkwak re-review
metadata: 
  node_type: memory
  type: project
  originSessionId: 448717b5-feb3-42fa-ad15-3a52c1e6e16e
---

**#11903 "Invalid HLSL for SM 6.9 with NVAPI"** (author skallweitNV, RTR/Dev-Opened, filed 2026-07-02, one day after merged #11889). RE-OPENED 2026-07-02: maintainer @szihs replied (issuecomment-4865865705) "can you do (B) Single source of truth" → direct answer to the posed either/or → cleared parked-chain release bar → fixer released to build (B) as a DRAFT PR. Then @szihs answered the 2-arg-Invoke sub-decision too (issuecomment-4865963271): chose (ii) "emit a clean cross-ABI diagnostic when a native-only overload is selected on an NVAPI target ... to avoid confusion" — NOT (i) invent an NVAPI mapping. ALL design points now settled; fixer implementing B + diagnostic, no open questions.

**Corrected root cause (v1 was WRONG):** triager's original diagnosis ("#11889's `implies(hlsl_nvapi)` guard inert on sm_6_9, type stays `dx::HitObject`") was a STALE-BINARY artifact — reset source to HEAD but ran a prebuilt slangc (5230a81f2, built ~19h BEFORE #11889 merged). Fixer instrumented at HEAD + triager rebuilt → REFUTED. At HEAD #11889 works: HitObject type IS `NvHitObject`, all ops NVAPI-consistent. Approach A is a verified no-op (no PR opened, fixer stood down, worktree clean). The triager's shared learning about `implies` was refuted; meta-lesson logged (after `git reset --hard`, REBUILD before repro; `slangc -v` unreliable on incremental builds).

**Actual issue:** the sole invalid emission is native-only 2-arg `HitObject::Invoke(hit,payload)` → `dx::HitObject::Invoke(NvHitObject)` (hlsl.meta.slang:23786; NVAPI Invoke is 3-arg at :23708). A per-operation ABI-coverage gap between NVAPI SER and DXR-1.3 native SER — NOT a type-emitter bug. Separate `ser_dxr` E41011 observed but NOT root-caused (noted in memo, not a blocker).

**Options for maintainers:** (B) single-source-of-truth so type + ALL ops/overloads resolve one ABI; OR an ABI-coverage audit giving 2-arg Invoke an NVAPI form / cross-ABI diagnostic.

**SHIPPED as DRAFT (2026-07-02 ~13:50Z):** DRAFT PR #11907 (branch `fix/issue-11903` @ `bedaf481`, base master, author nv-slang-bot, `pr: non-breaking`, body "Closes #11903"). Implements (B) single-source SER-ABI + (ii) cross-ABI diagnostic. Files: hlsl.meta.slang +9, slang-emit-hlsl.cpp +45/−16, new DIAGNOSTIC_TEST +33. `report_pr_created` DONE (triager live-verified isDraft=true + SHA-on-origin, not relayed on trust). codex PLAN/CODE/OUTPUT all APPROVE. Tests: repro PASS (E41400 + "has no NVAPI form" at call site), native regression hit-object-dxr-native.slang PASS, SER suite 29/29. CI all "skipping" (benign draft priority-yield, nothing failing). Draft-PR state posted on issue: comment 4866459853.

**State: HELD DRAFT, BLOCKED ON #10660.** 2026-07-02 ~later: @jkwak-work posted a SEQUENCING instruction (issuecomment-4868816318): "revisit after we resolve #10660 ... CI is not covering NVAPI tests ... fix the test coverage first and address NVAPI in a way we can test and protect." This RE-OPENED the chain but is a HOLD-AND-SEQUENCE, NOT a design reversal (szihs's B + diagnostic stand) and NOT an edit request. So #11907 stays exactly as-is (verified-good held draft), re-classified blocked-on #10660. Do NOT dispatch fixer — nothing to implement, the ask is to WAIT. #10660 = "NVAPI tests silently ignored" (jkwak-assigned; render-test hard-codes removed `external/nvapi/nvHLSLExtns.h` path, headers now FetchContent'd to build/_deps/nvapi-src/; draft PR #10865 in flight; headers missing from CI test artifact → tests "ignored" not run). Next human action = land #10660 (via #10865) → then revisit + validate #11907 against restored NVAPI tests → ready-flip + merge (operator/maintainer-gated, bot must NOT flip). RE-OPENS when #10660 lands, or on a substantive human comment / PR webhook. Separate `ser_dxr` E41011 noted out-of-scope in the PR.

**Priority P0→P1 (2026-07-02):** @jkwak-work follow-up (issuecomment-4870680570): Slack discussion concluded #11903 is NOT blocking (workaround: cap highest SM at 6.8, skip 6.9 for now), so he lowered priority P0→P1 himself and reaffirmed the #10660 sequencing. CONFIRMS existing posture — no design change, no edit request, no resumption trigger. Stayed parked, no fixer dispatch; acked light (reaction, not a 3rd comment in quick succession). Priority label is jkwak's own action (first-person "I am lowering") — don't set it ourselves.

**Review round applied + PR NON-DRAFT (2026-07-08):** chain resumed (jkwak reviewed #11907). Fixer implemented jkwak's 3 review-thread asks, pushed commit `28f6a3d95d` (ff from bedaf481; origin fix/issue-11903 head verified match). The 3 asks: (1) new native-path test `hit-object-invoke-dxr-native.slang` (2-arg Invoke on plain sm_6_9 → native `dx::HitObject::Invoke`, SIMPLE-filecheck); (2) new reusable `CapabilitySet::impliesAtLeastOneSet` helper replacing ~8 hand-inlined sites; (3) removed `HitObjectSERAbi` enum, inlined the resolver — single-source-of-truth preserved via the shared `impliesAtLeastOneSet` primitive. Replied on all 3 threads; codex CODE+OUTPUT approve. PR #11907 verified **OPEN, isDraft=FALSE** — a MAINTAINER readied it (fixer confirmed it did NOT flip; bot never flips → NOT a drafts-only breach, see [[feedback_drafts_only_guardrail]]). MERGEABLE, reviewDecision=REVIEW_REQUIRED (jkwak's threads COMMENTED, re-review pending). Tests: local rebuild green (nvapi-error test fires E41400 at call site, new native test PASS, SER 30/0/31, unit 492/0/11); live CI on 28f6a3d = 9 pass / ~3 running / 2 skipping, nothing failed. PR is now the public artifact (Closes #11903) — no extra issue comment. **Next human action = jkwak re-review; still gated on #10660 (CI-infra dependency stands).** Merge maintainer-gated, bot won't flip/merge. Fixer owns #11907 webhooks via /slang-github-webhook.

**Round-2 review + DISK-FULL blocker (2026-07-08):** jkwak reopened with 2 review comments — (A) r3540499903 "don't grow slang-capability.h; a similar helper may exist in a util file — double-check"; (B) r3540501955 "why is the COMPARE_COMPUTE disabled? enable it?". Fixer investigated read-only + made edits (NOT yet build-verified/pushed): (A) no capability-imply util helper exists anywhere (only pre-existing bool `CapabilitySet::implies()` is whole-set predicate, wrong semantics) → removed the new `impliesAtLeastOneSet` method, reverted capability files to master, inlined the idiom as a local lambda at the one call site; (B) `HitObject::Invoke` is a raygeneration construct (COMPARE_COMPUTE is the wrong lane) + sibling `hit-object-dxr-native.slang` is filecheck-only → removed the misleading disabled line. codex OUTPUT_REVIEW (inspection-only) approved edits + escalation. BLOCKED: `/dev/vdb` (`/workspace/agent`) at 100% (251G, ~26 sibling wt-slang-* build trees ~7G each; wt-slang-11903 = 7.2G, holds uncommitted round-2 edits). Pure I/O, not code failure; shipped commit 28f6a3d safe on PR, CI running. Main AUTHORIZED (routed via triager) the standing merged-PR-worktree reap ([[feedback_always_reap_merged_worktrees]]), scoped: MERGED PRs only, verify each on GitHub, NEVER touch wt-slang-11903 or any open/parked/uncommitted worktree. On space freed: fixer rebuilds→verifies→commits→pushes→replies to jkwak's 2 threads (soften "can never run").

**Round-2 VERIFIED & PUSHED (2026-07-08 ~01:33):** commit `8e4ad4e566` pushed to origin fix/issue-11903 (ff 28f6a3d→8e4ad4). Disk cleared by SELF-RECOVERY (sibling build finished, 776K→5.2G free); merged-only reap freed nothing (all 42 siblings open/parked/closed-not-merged, none touched) — see [[disk_full_fixer_reap_frees_nothing]]. Incremental rebuild (1 TU) succeeded. Verify: BUILD_EXIT=0, Test A E41400 fires at user call site (refactor intact), Test B native `dx::HitObject::Invoke` no NvHitObject, SER 30/30/0, codex OUTPUT_REVIEW approve. Replied to jkwak's 2 threads (r3540652369/r3540652434): (A) confirmed no capability-imply util helper → removed method, reverted capability files to master, inlined local lambda; (B) COMPARE_COMPUTE wrong lane for raygen HitObject + sibling filecheck-only → removed disabled line, offered separate raygen-runtime-harness follow-up. PR #11907 head=8e4ad4e566, non-draft, mergeable; CI auto-runs on push. **Awaiting jkwak re-review + auto-CI on 8e4ad4e566; still gated on #10660; merge maintainer-gated (bot flips nothing).** Reopens on next PR webhook / substantive human comment.

**GitHub trail:** original verdict comment 4864499570 (later PATCHed/corrected after stale-binary refute); B-ack 4865899278; draft-PR state 4866459853. Design decided by @szihs — a reply re-opened discussion and (here) DID release because it named direction+mechanism (see [[feedback_reopen_not_release_parked_feature]]). Chain twice bitten by stale state early on: my fabricated PR #11905 table + triager's stale binary → see [[project_never_fabricate_pr_state]] / verify live before asserting.