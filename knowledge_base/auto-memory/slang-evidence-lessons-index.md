---
name: slang-evidence-lessons-index
description: "The evidence & verification standing lessons — full one-line-per-rule text, moved verbatim out of MEMORY.md. Open BEFORE any verification, correction, or claim of absence. Derivations in slang-evidence-lessons-derivations.md."
metadata: 
  node_type: memory
  type: index
  originSessionId: 5c386752-328d-4e3b-85ea-e19e41121b53
---

# Standing lessons — evidence & verification

⛔ **Open this file before verifying anything, correcting anyone, or claiming content is absent.**
Moved verbatim from `MEMORY.md` 2026-08-03 (index was 22.3KB; the section alone was 10.6KB). Rows are
unabridged — the compression pressure that kept truncating them is exactly what these rules warn
about. Proofs: [[slang-evidence-lessons-derivations]].

⛔⭐⭐⭐ **THE ROOT RULE, added 2026-08-04 — [run a control on the INSTRUMENT; re-reading the reasoning cannot find a measurement defect](feedback_control_the_instrument_not_the_reasoning.md).** ⛔**PUBLISH THE ENUMERATION, NEVER A BARE COUNT** — the child holds 17 enumerated + at least 6 more unregistered, and a peer's rival tally (10→13→15→17→18) turned out to be an INCREMENTED TALLY with no list behind it. The load-bearing claim needs no number: **every defect was in the MEASUREMENT; none was findable by re-reading the argument.** The inferences were valid every time; the *measurements* weren't. ⭐⭐⭐**"check your work" and "control your instrument" feel IDENTICAL from the inside, and only the second works.** Holds: **a zero without a non-zero control is not evidence** · **7a can the instrument return non-zero at all?** / **7b can its output carry the claim?** · resolved `committer.login` ≠ free-text `commit.committer.name` (and `web-flow` = server-side) · `git log`/`show` **suppress merge diffs — pass `-m`** · the 9-dimension **containment checklist** · ⭐⭐⭐**confirmation is when verification is cheapest and feels least necessary** · **a ladder of finer numbers is a symptom, not progress** · **an unearned RECOMMENDATION costs someone else's work.** ⇒ **executed as a checklist at the point of claiming, not held as an insight — insight does not survive one message boundary** (each agent committed the shape *while articulating it*).

## The two rules that catch the most, stated as commands

```bash
# EXISTENCE (0/1 only — see caveat): collapse wrap + squeeze indent + literal + case-insensitive
tr '\n' ' ' < FILE | tr -s ' ' | grep -ciF -e 'fragment'
grep -c '' FILE                      # CONTROL — must be non-zero before you believe any zero

# COUNTING is a DIFFERENT command — never reuse the line above for a count
# ⛔ RANGE-PIN IT — an unscoped count decays as the file grows more numbered lists (mine: 20 vs 8)
awk '/^## The .* mechanisms/,/^## Cure/' CHILD | grep -cE '^[0-9]+\. '   # items, one item = one line
grep -oiF -e 'frag' FILE | wc -l      # true occurrences
```
⚠️ **The collapse cure makes the file ONE line, so `-c` can only ever return 0 or 1** — it is a
**presence bit, not a count** (measured: `-ciF 'ladder'` → 1 while true occurrences → 10). Fine for
existence checks; **fatal if you back a count claim with it.** Also: `-c` counts *lines* even with
`-o`, and always pass `-F -e` so a leading-dash token can't error to stderr and read as absence.

Run the ladder — punctuation → `-i` → shorter fragment → collapse+squeeze → synonym/inflection —
**before** claiming absence. A zero without a control is not evidence.

## The rules
- ⛔⭐⭐⭐**MEASURING A BRANCH? `origin/<branch>` MAY NOT EXIST OR MAY BE MONTHS STALE — the provisioned clone has a MASTER-ONLY refspec** ([both defects](feedback_shallow_clone_makes_your_head_the_graft_root.md)). `git fetch origin <b>` downloads objects but never updates `refs/remotes/origin/<b>`. **Per-edge by FETCH HISTORY: never-fetched ⇒ absent, aborts (SAFE); fetched-once ⇒ stale, answers confidently (DANGEROUS)** — a peer got "54 behind" against a true 4. ⭐⭐⭐**ABSENCE OF THE SYMPTOM ON YOUR CLONE IS NOT EVIDENCE AGAINST THE HAZARD** — the safe half looks exactly like no bug, so it LICENSES INACTION (same shape as a false capability-negative). ✅`git ls-remote origin refs/heads/<b>` → measure with the **literal SHA**; or `gh api contents?ref=<sha>`. ⚠️writing `origin/<branch>` (the natural form) resolves stale and SUCCEEDS. ⭐⭐⭐**Cheapest check: a branch you merged master into hours ago cannot be 54 behind.** ⭐⭐⭐**ASK WHAT WOULD MAKE THEIR NUMBER CORRECT BEFORE FILING THEM AS WRONG** — codex's "wrong" head was this stale ref, read correctly.

- 📁 **[Evidence-lesson DERIVATIONS](slang-evidence-lessons-derivations.md) — proofs for the starred rules below. Open before restating any upstream; a maxim without evidence gets tidied away.**
- ⭐⭐**I CORRECTED A COWORKER WITH THE VERY TOOL THAT CAUSED MY ERROR** (`#elif`-blind grep ⇒ inverted dead/live) ⇒ **a challenge owes a DIFFERENT INSTRUMENT** (their broken tool repeats the error *more* confidently; a marked "error" resists the next reader's fix) · ⭐⭐**ENABLER = an UNDERSPECIFIED claim** (status without cause ⇒ contestable BOTH ways) ⇒ **state status AND mechanism**; **ask what makes it precise, don't assert its opposite** · ⭐contested live/dead ⇒ **read the adjacent rationale comment**. [§1](slang-evidence-lessons-derivations.md) · [#12331](project_12331_spirv_opt_size_preset_Os.md)
- ⭐⭐[`reviews[].commit_id` can POST-DATE `submitted_at` — it is NOT "the SHA approved"](feedback_reviews_commit_id_can_postdate_submitted_at.md) — spy#1068's 07-29 approval carries a commit dated **08-03**; #1084 same (+69min). **Fails toward APPROVE**: an "approved at head" clause reads TRUE on a 5-day-stale approval. ⛔a spot-check can't catch it (#1063/#1075 kept *historical* commit_ids) ⇒ **compare TIMESTAMPS only**: any commit date > that review's `submitted_at` ⇒ stale
- [Green job + skipped backend = 0 coverage](feedback_green_job_skipped_backend_zero_coverage.md) — verify execution, not job conclusion. **5 shapes**: backend skipped · test DIR absent from the PR run (nightly-only) · backend ran but log can't say **which path** · ⭐⭐**5th = DENOMINATOR error — dead tests are NOT the coverage** (slang#10480: verified 8 `replayRecord_*` dead at `unit-test-replay-record.cpp:170`, then wrongly called the subsystem uncovered; it holds **125** tests, only those **8** dead ⇒ **count the SET before claiming zero**; per-file `grep -c SLANG_UNIT_TEST`/`SLANG_IGNORE_TEST`, then classify each ignore unconditional-vs-`#if`-arm. ⭐⭐**EVERY INPUT VERIFIED, OUTPUT FALSE — a set-boundary mismatch between two clauses**, both numbers individually measured ⇒ survives "check your work" (✅corrected in place). ⭐⭐**too-narrow glob and too-wide substring are ONE defect in opposite signs — check BOTH polarities.** ✅**filter on header-include or test-name PREFIX, never a substring; publish the boundary beside the figure.** ⛔**a REST `PATCH` notifies nobody ⇒ "not worth a round trip" argues against a superseding CREATE, NEVER an in-place edit** — I wrongly advised leaving a false number live) · **could the ARBITER run?** — `slang-test` loads FileCheck **in-process from the `slang-llvm` LIBRARY** (`test-context.cpp:95-113`, gated `:5915 if(hasLlvm)`), NOT a PATH binary ⇒ `pip install filecheck`/apt-LLVM change nothing, and when it can't load, `filecheck=` tests go **`Ignored`, not failed**. ⛔**But "absent locally" was RETRACTED same-hour: LLVM FileCheck DOES run — the lib is `build/Debug/lib/libslang-llvm.so`, and `ls …/bin` was a ONE-DIRECTORY negative published as tree-wide** (`find build -iname '*slang-llvm*'`). ✅**Settle it with the FAILABLE CONTROL — inject a broken CHECK, require `FAILED`** ⇒ ⚠️⭐**`slang-test` exits 0 EVEN ON `FAILED` — parse `FAILED test:`/`% of tests passed`, NEVER `$?`, else the control is inert at its own job.** ⭐⭐**a correct 07-02 note already said local FileCheck works ⇒ contradicting an existing note is a signal to RE-VERIFY, not to publish**; a false CAPABILITY-NEGATIVE is the worst thing to leave in shared prose — readers obey it by **not trying**, so it never enters a transcript
- ⭐**Both failure directions are RELEVANCE errors** — ask *does this bear on my conclusion?* of **disqualifying** evidence as rigorously as supporting. **Destroying a conclusion is harder — over-correcting reads as honesty.** [Full rule](feedback_correction_must_sweep_whole_file.md)
- ⭐[All legs verified ≠ explains THIS instance — the mechanism must predict the OBSERVED coordinates](feedback_mechanism_must_predict_observed_coordinates.md) — ⭐over-stated **REFUTATION** worse than over-stated mechanism · ⭐⭐**ASYMMETRIC HEDGING: audit the artifact that DRIVES THE DECISION** · ⭐⭐**NAME THE DEFECT, THEN THE ASSERTION THAT FAILS WHEN ONLY IT IS REINTRODUCED — BOTH directions (the negative-only control LIES)**, domain = **ANY status artifact** incl. a CI `conclusion`; ⭐**firing on 4 disguises but not the 5th = a missing DOMAIN** · ⭐⭐**THE DISCRIMINATOR: name the field that would DIFFER if it hadn't happened, not the one that looks healthy** · ⭐**PRE-INSTRUMENTATION: would the outcome change if this mechanism were ABSENT?** · ⭐⭐**two agents can hold complementary halves of ONE blind spot**; the correction came **one leg short** ⇒ the trap recurs INSIDE its own fix
- ⭐⭐[NARROWING ≠ TESTING the premise; CHECK YOUR OWN STORE — then confirm the check WORKS](feedback_narrowing_is_not_testing_check_own_store.md) — narrowing inherits the untested premise, launders it as diligence · **recall, not evidence, failure** · ⭐⭐⭐my store was **UNEXECUTABLE** (0 hits) ⇒ run any "always check X" as a stranger; deliverable = the **INDEX ENTRY** · agreement on an untested premise ≠ corroboration. ⛔**evidence travels with it** → [§2](slang-evidence-lessons-derivations.md). ⭐⭐⭐**RUN THE CHAT-ONLY AUDIT BEFORE CLOSING ANY LONG EXCHANGE** — for each claim you asserted in replies, `tr '\n' ' ' < f | tr -s ' ' | grep -ciF -e '<frag>'` across the store; **a claim stated fluently 3× FEELS recorded and often isn't** (2026-08-03: two independent audits, **two unstored claims, both about verification discipline**). ⚠️Ladder any zero before believing it; the output that matters is `CHAT-ONLY` ⇒ **write to the child, then decide what the hook needs.** The procedure itself was chat-only for most of that session — it is now a command in the child
- ⭐[A published capability-NEGATIVE closes doors — re-derive before relaying it](feedback_published_negative_env_claims_need_rederivation.md) — "no NVIDIA Vulkan ICD" was FALSE (probed 1 dir) and **I relayed it as fact**; it was the ONLY thing blocking the decisive test ⇒ probe with the capability itself, not `ls` · ⭐⭐**ONLY error class with NO observable failure signature** (acts by **not attempting** ⇒ invisible in principle). ✅Write **"could not verify X by method M"**, M NAMED, never "X is unavailable" · ⛔⭐⭐⭐**THE CONVERGENT FORM (08-04, #11616): AGREEMENT BETWEEN TWO TIERS IS NOT CORROBORATION WHEN BOTH CLAIMS ARE CAPABILITY-NEGATIVES** — fixer's store said "FileCheck doesn't run locally", triager re-derived the same from `ls` on ONE directory; a **failable control** refuted both, and a correct 07-02 note already said so. **This class CONVERGES** because its whole content is *"don't try"* ⇒ neither party generates the refuting evidence, and two independent derivations FEEL like confirmation. ⇒ **a SHARED capability-negative is the moment to demand a control, not to relax** · ⭐**cost asymmetry: one command to re-test, a whole parallel path to obey**
- ⭐⭐**NEVER TAKE *STATE* FROM A NARRATIVE TOOL — EITHER DIRECTION.** Prose answers *what is this about*; only **FIELDS** answer *what is it NOW*. ⛔A summarizer says **"resolved through PR #N" about an OPEN DRAFT** — narrative-true, state-false, and **nothing in the answer flags the difference** (slang-fixer misled identically TWICE in one day — #12186, #12201; truth `state:open merged:false draft:true`, dependent issues still OPEN). ⭐**POSITIVE state claims are WORSE than the negatives**: a false negative BLOCKS action, a false positive **UNBLOCKS** it — *"unblocked invites someone to start"* — and retires a hold that still holds. ⭐⭐**A false BANNER out-harms the stale entry it replaced** (durable, top-of-file, reads as verified) ⇒ **before any banner premised on another PR's state, FETCH IT**: one `pulls/{n}` GET returns `state`+`merged`+`draft`+`head`, cheaper than the prose call that gets it wrong. ⭐**a blocker's DESIGN changing ≠ the blocker CLEARING** · ⭐**a blocker clearing ≠ your plan surviving** — slang#12093: the maintainer closed it by **DISABLING** vec4 ctors our triage recommended **ADDING**, so that file turned contrary to shipped code. [shared learning: never-assert-a-negative-from-a-summarizing-tool §EXTENSION] ⭐⭐**3rd instance same day, and it hit the NEW direction the extension covers:** asked for check-runs on a head, the summarizer reported *"No check_runs with failure conclusion were found"* — **false**; `gh api` returned **2 failures** immediately. Tell-tale: **its own counts didn't sum (13+1+12 of 110)**. ⇒ **a summarizer's arithmetic not reconciling with its stated total is a self-refuting signal — check it before trusting any tally** (cf. `rate_limit`'s `graphql:0`+`search:10`). ✅**DISCRIMINATOR for cumulative CI state: a commit's check-run list is CUMULATIVE ACROSS RUNS, so old failures persist forever — key on `check_suite.id` + `started_at`, never on the head's aggregate** (d2621e72cd45: 2 failures both in superseded draft-era suite `83637265602` @18:58/18:59Z, while the live `pull_request` suite passed `filter`+`wait-for-human-priority`).
- ⭐⭐⭐**AN INSTRUMENT INSIDE THE PHENOMENON CANNOT MEASURE IT — would this probe report the same whether or not the thing I'm testing is TRUE?** If yes it isn't a probe. ⭐**ONE PROBE PER COMMAND** · ⛔**never `rate_limit`** (use `gh api -i <PATH-YOU-WILL-USE> | grep -i x-ratelimit`, **PRESENCE not value**) · ⭐**ONLY PASSING CASES LOCATE A TRIGGER** · ⭐**LOUD>QUIET: a quiet self-consistent wrong answer is WORSE than a loud 401** · ⭐⭐**having the rule filed does NOT execute it.** [probe/rate-limit](project_apparatus_probe_failures_rate_limit.md) + [gate + only-passing-cases](project_critique_gate_pulls_pattern_builtin_floor.md) — ⚠️**COMPLEMENTARY halves; neither alone backs this line** (a link repointed to a new file silently orphaned 2 claims ⇒ **a link SWAP is a content move: re-verify EVERY claim against the NEW target**)
- [A correction appended ≠ applied — ask WHERE ELSE the claim lives](feedback_correction_must_sweep_whole_file.md) + [18-error study](feedback_sweep_rule_case_study_rhi800.md) — sweep classes (**not numbered in the child ⇒ no count claim**, per the marker-vs-item rule), **carry-through the largest** (needs the POSITIVE half); ⭐⭐**a RESUME TRIGGER GOES STALE SILENTLY** — when the resolution path changes, **REWRITE** the trigger (mark `⚠️SUPERSEDED` + `✅CURRENT`), don't append; the row still reads "HELD" while the named event can no longer occur · ⭐⭐**RECENCY OF EDIT beats any keyword probe as a defect predictor** — audit what you just changed, and ask *"is the trigger still reachable?"*, not *"does the row contain 'RESUME'?"*. ⭐**a SPLIT is a content move**; ⭐⭐its mirror is a **FORWARD REFERENCE** (pointer to a child predating the claim ⇒ links all-green, content never there) ⇒ **content-grep the child when you WRITE a pointer; measure the delta on THAT LINE**; ⭐⭐**MISMATCH ⇒ go to GROUND TRUTH, not your newer text**; ⭐⭐**a SELF-EXPIRING note does NOT expire itself**; ⭐**the DETECTOR needs its own verification**. **`/workspace/shared/` is Main-write-only ⇒ stale shared prose is MY repair** — ⚠️not via `learnings/INDEX.md`. [§3–3d = full classes](slang-evidence-lessons-derivations.md)
- ⭐⭐**MODE 7 — a rule REACHABLE TO THE TEAM but INVISIBLE TO THE AGENT (cross-store).** Fired **both directions in one session**: jkwak's `no ../ #include` directive sat in the fixer's store and was **0 hits** in mine; *never-take-state-from-a-summarizer* sat in `/workspace/shared/learnings/` and was absent from the fixer's 199 files ⇒ **that error fired TWICE, not once.** ⭐⭐**"The corpus knows" is not a property any agent can rely on** — and the SHARED store failing is worse, since its existence creates false assurance. ✅**When a peer cites a rule you don't hold, FILE AND INDEX IT IN YOUR OWN STORE — don't merely comply** (compliance ends with the message; the next fresh context has neither), and **don't assume the shared copy protects your peers.** · ⭐**A sweep's detector will eventually match the note DESCRIBING the detector** — my dead-link sweep flagged `](file.md)`, its own inline example. ⛔**CONFIRM, don't "fix"** (creating it = Mode-4-manufactured-by-repair). ✅Keep a **not-a-link class** before reporting any count (`[[nodiscard]]`, `[[noreturn]]`, `[[LOAD]]` — a store documenting bracket syntax has a false-positive FLOOR), and write sweep docs with placeholders that **cannot resolve** (`<file>.md`, `$f`). · ⭐**A COUNT in an index is a claim that ages the moment you append** ("6 ways" vs 7 sections, same pass) ⇒ prefer no count. [§3g-3h](slang-evidence-lessons-derivations.md)
- ⭐⭐⭐[`learnings/INDEX.md` is REGENERATED — a fix to a GENERATED file is not a fix](feedback_shared_index_is_generated_use_shared_root.md) — verified-working hand-edit **destroyed in minutes** by `append_learning`. ✅Durable home = **`/workspace/shared/CANONICAL-ENV-FACTS.md`**. ⚠️slugs **LOSSY 3 ways** ⇒ search lowercase punctuation-free fragments. Knowing a generator exists ≠ asking **"does it own the file I'm editing?"**
- ⭐⭐[An audit grep FALSE NEGATIVE is ASYMMETRIC — reads as "content is GONE", justifying the undo of a correct edit](feedback_audit_grep_false_negatives_asymmetric.md) — **8 numbered mechanisms** (recomputes ⛔**only RANGE-PINNED**: `awk '/^## The .* mechanisms/,/^## Cure/' CHILD | grep -cE '^[0-9]+\. '` → 8; the **unscoped** form now returns **20** because appended sections grew their own numbered lists — M9 sixth form) **+ **M9 = a stored check reports a pass it hasn't earned — 4 forms:** wrong **file set** (`dir/**.py` isn't recursive) · **non-discriminating** pattern (same answer either way) · **unarmed absence** (no proof it looked ⇒ `CANNOT VERIFY` must be a THIRD outcome) · **correct command, MOVED subject** (record `0 of C`; a **falling** C ⇒ re-read the claim; a **rising** C ⇒ **ask whether the added files are IN SCOPE** — benign for a zero, load-bearing for a HIT) · **5th: TOO MANY files** — `grep -r` scans untracked build output / fetched binaries, and `getsize` inside a `.so` reads as a source hit (real: `16→26` ⇒ false "BROKEN") ⇒ ✅**scope to tracked source AND pass `-I`**: `git ls-files … | xargs grep -lIE`; ⭐`grep` prints `binary file matches` — **the tell is in output you skim**). ⇒ ⭐⭐**a stored verification command is CODE: it needs a FAILING TEST before you trust it — "it ran clean today" is the weakest possible evidence** (6 stored checks, 6 independent defects, none caught by re-running)** + the **MIRROR class** (an over-broad audit passes for the wrong reason ⇒ carries no information). ✅**Needed without opening the child:** `tr '\n' ' ' < F | tr -s ' ' | grep -ciF frag` + `grep -c '' F` as a **NON-ZERO control**; **probe INSIDE one styled run**, never across `**`/`` ` ``/`[…]`; **run the ladder to completion — dies at step 1–4 = your PATTERN, survives step 5 = content genuinely MISSING**; **read WHAT a late hit matched**. ⭐⭐⭐**THE APERTURE LADDER — 4 scopes fail distinctly and ALL read as "swept clean": command-scoped (blind to prose claims) · word-scoped (unrelated senses) · phrase-scoped (**~150 hits, signal buried**) · ✅claim-scoped (the claim's GRAMMAR — what must be ADJACENT for it to be wrong) ⇒ **match the claim's SHAPE, not its vocabulary; 2 hits and 150 hits are the SAME failure** ⛔⭐⭐⭐**AND FIXTURE-TEST THE APERTURE — mine flagged the ALREADY-FIXED form** (pos✅ neg-unrelated✅ **neg-fixed❌**) ⇒ **a pattern that flags the fixed form reports the defect FOREVER, so the fix never registers and you learn to IGNORE THE SWEEP** — worse than the defect. ✅require the literal command AND absence of a range-pin; **every negative-control set needs the ALREADY-FIXED case** (it only exists after you start repairing, so it's absent from fixtures built before the fix). Store-wide: **0 of 875 files, planted positive still fires**. ⭐⭐**BUILD NEGATIVES FROM THE REAL REPAIRED TEXT, never an imitation — and WHICH CLAUSE IS LOAD-BEARING DIFFERS PER AUTHOR**: on the triager's repair the exclusion clause is INERT (raw already clean), on mine it FIRES (my wording puts `awk`+`grep` inside the window) ⇒ **same rule, opposite verdict; don't adopt a peer's "that clause is inert"** (3rd instance today of re-run-it-on-your-own-corpus). ⚠️mine is load-bearing **by accident of phrasing, not design**. ⛔⭐⭐⭐**A WINDOWED APERTURE MAKES EVERY ZERO NARROWER THAN YOU STATE IT** — my "0 of 875" meant *0 within 80 chars*; a fixture measured at **151** chars MISSES. ✅re-ran unwindowed/line-scoped, 876 files ⇒ **1 hit, narrative-only** (store result stands, claim was narrower than stated). ⇒ **report the aperture's bound WITH its zero**, and ⛔**when a fixture must sit outside a numeric threshold, MEASURE the offset — never eyeball it** (triager's "far" fixture was **81 vs a window of 80** and it nearly credited distance-robustness). **Any instrument parameter — window, depth, page size, timeout — is a SCOPE ON ITS RESULT.** (the 150 reads as thoroughness)** · ⭐⭐**A note about a check is the most dangerous place to run that check** (4 of 8 mechanisms + all 3 mirror instances surfaced there) ⇒ budget an extra ladder run in rules files. ⭐⭐⭐**AN AUDIT SCRIPT IS ITSELF A PROBE**: always `grep -ciF -e "$pat"` — a leading-dash token errors to stderr and the loop reads it as **absence** (a tool error becoming an evidence claim), `[bot]` is a character class ⇒ **print the raw output on any zero**. ⭐⭐**Scope each check to the LINKED child, not the store** — a whole-store grep MASKS the misfiled-claim class
- [Name what you held fixed](feedback_name_what_you_held_fixed.md) — a check reading ONE element of an arbitrarily-ordered set carries zero info when it passes; cure = move 1 constant, store as runnable cmd · [Shallow clone ⇒ your HEAD is the graft root](feedback_shallow_clone_makes_your_head_the_graft_root.md), fires at ANY depth ⚠️I hold NO slang-rhi clone ⇒ my git facts are REST
- [`gh --paginate` 401s on page 2+](feedback_gh_paginate_401s_on_page2_use_explicit_pages.md) — exit 0 + silent truncation = phantom-green ⇒ **reconcile on RAW page length, not your filtered count**. ⭐⭐**3rd repeat because my line kept the RULE not the NUMBERS ⇒ store the datum with the rule**: 📌**slang open PRs = 76 non-draft of 233**. ✅**one-call denominator**: `search/issues?…&per_page=1` → `total_count` (⚠️30-bucket ⇒ denominator only). ⭐**never "no failures" without `checked N of M`**. 401 ⇒ GraphQL empties are absences *made by the outage*. [derivation](slang-verify-lessons-pointers.md)
- ⚠️[20 of 427 memory notes EXCEED the 24.4KB Read limit](project_memory_files_over_read_limit_backlog.md) — **size is the trigger, append-only ORDERING is the vulnerability**: truncation eats the newest corrections (incl. LIVE-chain RESUME tripwires). Cure = **controlling block at the TOP**; check the limits of the tool you RETRIEVE with. ⭐⭐⭐**VERIFY THE CONSTRAINT BEFORE OPTIMIZING AGAINST IT** — I shaved bytes for many turns toward a hook's **17.1KB** while a concurrent writer added ~1KB/turn (rewrites measured **+476/+255/−6**), then read the file at 18.1KB **complete**: real limit ~**24985**, so 17.1KB was a **margin, not a cliff**, never once checked. ⇒ (a) confirm the threshold is real, (b) confirm your progress metric can detect your own edits (**per-line delta**, never the file total, when anything else writes), (c) prefer **one structural move to N trims**
- [Unattributed fact reads as your own](feedback_unattributed_fact_reads_as_your_own.md) · ⭐⭐**RELAYING A COWORKER'S NUMBER UPWARD LAUNDERS IT INTO FACT** ⇒ probe your own edge first; **a challenge is not a reason to adopt it**; **tense carries a claim** · ⭐⭐**THE SYMMETRIC PAIR: don't accept credit you didn't earn — and don't accept BLAME you didn't earn either, when a one-second grep settles it** (accepting credit is tempting; accepting blame **reads as humility** ⇒ neither invites a check; only which one *feels virtuous* differs) · ⭐⭐**5th form: DON'T CREDIT A PEER FOR WHAT THE HARNESS SHOWED YOU IN YOUR OWN FILE** — a `system-reminder` is harness output, **never quote it as someone's words**; grep their message text before writing "X said/quoted" · **[N registered, M executed always paired](project_slang_rhi_800_evidence_methods.md)** (207 registered / **0 executed** — ⚠️was pointing at the wrong child, a FORWARD REFERENCE caught by ladder-to-exhaustion 08-03) · ["Recorded" is unauditable](feedback_recorded_is_unfalsifiable_across_tiers.md). [§4](slang-evidence-lessons-derivations.md)
- [A descope can silently fail the acceptance bar](feedback_descope_recheck_original_acceptance_bar.md) — narrower abort ≠ fixed · ⭐[A fix for an abort can INVERT into over-rejection](feedback_fix_can_invert_into_overrejection.md) — both halves must hold; **a patch EDITING a passing test ⇒ predicate too wide, that test was the oracle**; never let a spec *summarizer* be load-bearing · ⭐⭐[An OPTIMIZED lane can be INERT for the fix](feedback_optimized_lane_can_be_inert_for_the_fix.md) — if the optimizer already folded the construct you changed, the lane checks the END STATE and **would stay green with the fix REVERTED** ⇒ require an unoptimized lane that observes the intermediate form; **enumerate the ops the patch adds and confirm EACH is exercised** (one rides along untested); **`CHECK-NOT` must be BOUNDED between positives** or the excluded thing reappears exactly where the bug would. ⭐⭐⭐**ROOT RULE (3 sibling defects, one mistake): VERIFY A TEST UNDER THE HARNESS'S OWN INVOCATION** — exact flags/lanes, and confirm it FAILS with the fix reverted; hand-running with your own flags proves nothing about a directive-driven test (the `-O0` ask caught a real CI failure *before CI*)
- [#11225](project_11225_capability_target_incompat_slangpy_break.md) — a WRONG premise supporting a RIGHT conclusion is the hardest error to catch; distrust "structurally cannot" in my own output, esp. when correcting someone · ⭐rhi#797: **a severity DOWNGRADE resting on API semantics ⇒ verify receiver+signature of the exact method**. [§5](slang-evidence-lessons-derivations.md)
- ⭐[SQUASH merge ⇒ `merge-base --is-ancestor` NON-ZERO though the fix landed](feedback_squash_merge_breaks_merge_base_ancestor_check.md) — arbiter = merged file **content** + parent count (1=squash), never the graph · closure is **eventually consistent** ⇒ re-read before acting on "`Closes #N` didn't fire" · ⭐⭐**a blocked verification call ⇒ UNKNOWN, not UNCHANGED** · ⭐⭐**attribute causes only to whoever could OBSERVE them** (a charitable story draws less scrutiny ⇒ needs MORE evidence) · ⭐file **at the granularity of what was VERIFIED**. [§6](slang-evidence-lessons-derivations.md)
- 📁 [Verification-lesson POINTERS](slang-verify-lessons-pointers.md) — reporting discipline (no verdict not in hand · no fabricated events · pushed-state by branch · missing artifact ≠ outage · suspicions = hypotheses · job logs PUBLIC) · the #802 set (3 readers wrong) · verify/CI misc incl. **a tool impeached ⇒ re-derive what leaned on it**

- ⛔⭐⭐**[A REPORTER'S FRAMING IS A HYPOTHESIS, NOT A FINDING](feedback_a_reporters_framing_is_a_hypothesis_not_a_finding.md)** — MINE-DEMONSTRATED 08-04 on slang#12338: every number in the report reproduced EXACTLY and the title still mislocated the cause (blamed `Optional<IFoo>`; the actual trigger was constructing `none` — one deleted store flips the lowering). ⭐⭐**A REPRODUCTION CONFIRMS THE SYMPTOM, NEVER THE REPORTER'S CAUSAL CLAIM** — I had matched their counts across 4 backends and was still one step from publishing their framing as my diagnosis; every check was correct, none VARIED THE CONSTRUCT THE TITLE BLAMED. **A reproduction is not a control.** ⭐**The tell: you are about to restate the issue title as your conclusion — that paraphrase feels like synthesis and is an echo; ask which experiment distinguished them.** ⭐**A narrower cause is worth more than a confirmed symptom** (2 named functions vs. a language feature, same evidence). ⭐⭐**"performance-only / no miscompilation / behavior unchanged" IS A CORRECTNESS CLAIM ⇒ execute it with a NON-ZERO CONTROL; disassembly is not behavior** (I drafted "all three produce correct code" from reading SPIR-V, then executed all 3 over 8 inputs → byte-identical, plus a perturbed-`eval` control proving the harness could see a difference). ⭐**`slangc -target exe` exits rc=0 and writes NO FILE ⇒ rc=0 is not evidence an artifact exists — stat it**; working path is `-target cpp` + a driver (`ComputeVaryingInput`, `startGroupID`/`endGroupID`, `main_0`), `g++ -I<slang>/prelude`, `#include <cstdio>`. ⭐**Don't let one narrowed cause absorb a second reproducer** — #12338 had two; I isolated only the closed-conformance one and said so publicly.

- ⛔⭐⭐**[TWO FINDINGS FROM ONE FILTERED SET SHARE ITS FILTER — not two independent measurements](feedback_two_findings_from_one_filtered_set_share_its_filter.md)** — a peer published *"7 of 186 are maintainer self-closes"* + *"25 of 186 have zero comments"* as separate limits; **the 7 were exactly `self-close ∩ comments=0`** (real: 33 self-closes / **18 MEMBER** / 22 incl. COLLAB+CONTRIB) — item 2's filter had leaked into item 1. ⭐⭐⭐**The error direction was BENIGN (it UNDERSTATED their own finding) so no outcome could object** ⇒ ⛔**the check must be STRUCTURAL: which set was each number counted over, and was that the set you meant? "Does my conclusion still hold?" returns YES and teaches nothing.** ✅**State scope PER FINDING, not per message.** ⭐**Caught only because I ENUMERATED the set to use it rather than accepting the count** — a bare figure is unfalsifiable at read time. ⭐⭐**It sat in a published retraction that MY OWN banner called "authoritative on the fix" ⇒ a false number on the path to the correct rule is worse than one in a superseded file** (2-actor remedy: they filed the correction, I bannered the original AND struck the figure at its mid-document position). ⚠️1 case for the mechanism, inside a 3-instance adjacent-question pattern.
- ⛔⭐⭐⭐**[A REPLACEMENT DISCRIMINATOR INHERITS THE BURDEN OF PROOF OF THE ONE IT REPLACES](feedback_state_reason_is_not_polarity_either.md)** — MINE 08-04: two agents an hour apart shipped two different metadata tests for *"did the maintainer refuse?"*, and **both answered a NEIGHBOURING question**. Mine (`closed_at==comment.created_at && closed_by==comment.user`, published to a lesson + an index + a dispatch) = **deliberateness**, refuted by a *merge* close matching it exactly; the peer's replacement (`state_reason`) = **done-vs-abandoned**, refuted by `not_planned` meaning ≥4 things incl. **a reporter self-closing a LIVE segfault promising to reopen**. ⭐⭐⭐**I shipped the broken fix INSIDE a lesson about verification, one exchange after being correctly refuted — the correction slot is where scrutiny dies, and this is that rule demonstrated ON MYSELF.** ⭐⭐**A single field appearing to settle a COMPOUND question is the tell; it presents as elegance.** ⭐⭐**When a peer states the limit of their own check ("~12 issues, I didn't sample the repo"), MEASURE THAT — the defect lived exactly there and one search-API query closed it.** ⭐⭐**Adopting a peer's fix is PUBLISHING it** — "they proposed it" is no defense. ⭐**A page-of-100 returning 33 rows is a PAGE DEFAULT, not a distribution** (67 slots eaten by PRs ⇒ a false "`not_planned` is rare"); population = 186 vs 3758 via search API. ✅**Polarity is in the COMMENT BODY — quote the sentence**; `author==closed_by` ⇒ reporter self-close.

---

📁 **Overflow: [[slang-evidence-lessons-instruments]]** — 4 unabridged rules moved out 2026-08-04 ~~when this file crossed its own 24.4KB read limit~~ ⛔**FALSE PREMISE, corrected in [[project_memory_files_over_read_limit_backlog]]: `>24.4KB` is NOT a read cutoff on this edge.** Discriminating test on record: a **25,264 B** file read **in full** (61/61 lines), and a 321 KB file reads line 298. **This file read COMPLETE at 29,376 B on 08-04, tail line starting at byte 29,030** ⇒ the split bought *navigability*, not readability, and rows past 24.4KB ARE reachable. ⭐**The 17.1KB hook nag, the 24.4KB figure, and an actual truncated read are THREE DIFFERENT QUANTITIES — re-measure before spilling on a byte figure.** Contents: `check-runs?filter=latest` two-suites/currency · **ANY PATH-ADDRESSED FACT is per-container and per-moment** · **TICK-87** unverified-scope · **containment claim needs a containment check**.
