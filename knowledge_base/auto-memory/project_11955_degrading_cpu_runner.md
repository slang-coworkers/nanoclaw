---
name: project_11955_degrading_cpu_runner
description: "#11955 CI flake: test-linux-release-gcc-x86_64-cpu/test-slang intermittent SIGSEGV/HANG/OOM, binary-independent, merge-queue evictor. CORRECTED 07-15: runner is GitHub-hosted EPHEMERAL ubuntu-24.04 (ci.yml:367-381) — no persistent host, 're-image' RETRACTED; re-anchored on resource-envelope (LLVM-JIT mem ceiling). jkwak-work engaged → maintainer owns; operator-escalation DISARMED"
metadata: 
  node_type: memory
  type: project
  originSessionId: ddab6188-0f28-4222-8b37-6ae1cbb4b414
---

**#11955** — CI-health Infra issue (labels `CI`/`Infra`, assigned **jkwak-work**): `test-linux-release-gcc-x86_64-cpu/test-slang` intermittently **SIGSEGV / HANG / OOM / cancel**, **binary-independent** (green on 8–10 sibling runners on the same binary in the same run → it's the job/runner envelope, not the code). Crash slot repeatedly at `tests/compute/static-const-matrix-array.slang.3 syn (llvm)` (exit 139 after ~1150 tests pass). Convergence lead with [[project_11951_testserver_jsonrpc_pathlevel_flake]] (same `.slang.3 syn (llvm)` boundary) but **distinct mechanism** (this = in-process linux-cpu SIGSEGV via LLVM JIT; #11951 = test-server JSON-RPC IPC drop on windows-debug-gpu) — kept separate pending a backtrace.

**Became a merge-queue evictor (07-09):** jvepsalainen-nv reported queue run 29012014464 (`pr-11976`) hit the SIGSEGV and evicted APPROVED-green #11976; babysitter CONFIRMED as 5th occurrence / first MQ eviction, verified #11976 was already re-queued (bot enqueue blocked, [[project_nv_slang_bot_readonly_incident]]).

**⚠️ CORRECTION (07-15, babysitter verified at HEAD `master @ 623227f`):** the earlier "degrading runner / re-image **this specific** runner" framing was **factually WRONG and is RETRACTED**.
- `ci.yml:367-381`: `test-linux-release-gcc-x86_64-cpu` runs on a **hardcoded GitHub-hosted `ubuntu-24.04`** — **ephemeral, fresh VM per job**. YAML comment literally says "GitHub-hosted; no GPU" + carries a `server-count: 1` note re "intermittent JSON RPC failures on GitHub-hosted runners." `ci-slang-test.yml:56` honors the label verbatim.
- **No persistent host exists to re-image.** jkwak-work (comment 4985975717) called this out; babysitter confirmed and posted the correction ([comment 4986021588](https://github.com/shader-slang/slang/issues/11955#issuecomment-4986021588)).
- GCP wiring is for the **build pool only**: `vars.LINUX_BUILD_RUNS_ON` gates build+sanitizer, `ANALYTICS_RUNS_ON` gates analytics/ci-health — **neither touches this test job**. jkiviluoto-nv's GCP runners are not in this path.

**Re-anchored diagnosis:** resource-envelope, not a bad host — LLVM JIT (`-api cpu+llvm`) memory peak occasionally exceeding the standard `ubuntu-24.04` (~16 GB) image. Concrete lever offered to maintainers: **backtrace on the `static-const-matrix-array.slang.3 syn (llvm)` crash slot** to rule the LLVM-synth path in/out; if clean, treat as a memory ceiling → candidate fix = move this test tier to a **larger self-hosted linux runner** (NOT a re-image). Ball is with maintainers (jkwak owns).

**Distinct from #11951 — now EMPIRICALLY CONFIRMED (07-15, babysitter, [comment 4986112848](https://github.com/shader-slang/slang/issues/11955#issuecomment-4986112848)):** jkwak asked whether #11951's fix resolves #11955 (and floated close-and-reopen-fresh due to stale content). Answer = **NO**, with receipts:
- #11951 was closed **manually** by jkwak for **PR #12056** "AVX-512 JIT workaround" (`SLANG_DISABLE_AVX512=1`, merged **07-10 22:10Z**).
- **Crux post-merge check:** the #11955 signature **recurred 4 days later** — #12080 run 29357993030 attempt-1 (07-14 18:55Z), `test-linux-release-gcc-x86_64-cpu / test-slang` SIGSEGV exit 139 at the `static-const-matrix-array.slang.3 syn (llvm)` slot, **with `SLANG_DISABLE_AVX512=1` verifiably active** (confirmed from raw attempt-1 log) + #12086 HANG same day. So #12056 did NOT clear #11955 → distinct-mechanism call proven (in-process linux-cpu LLVM-JIT SIGSEGV ≠ windows-gpu test-server RPC drop).
- [[project_11951_testserver_jsonrpc_pathlevel_flake]]'s #12114 (JIT use-after-free, merged 07-15 21:40) is **not** `Closes #11951`, is **debug-info-gated** (no `-g` on the `syn (llvm)` path), and too new for CI data — NOT expected to resolve #11955 either.
- **Close-and-reopen is jkwak's call** (maintainer-owned; bot never closes). Babysitter agreed a clean restart is reasonable **iff the live signature carries forward** (not closed-as-fixed) and offered to file the fresh issue with a verified summary. If a fresh issue opens, retarget tracking to it.

**OPERATOR-ESCALATION DISARMED (07-15):** the armed trigger required (a) re-image ask sitting with **no maintainer action** AND (b) continued MQ eviction. Condition (a) is now **false** — jkwak-work engaged directly on the issue and the whole re-image premise was retracted. Maintainer owns it; do **not** re-arm an operator escalation on the old framing. If evictions continue, the surface is "this test tier needs a larger runner," routed through the maintainer/CI-fleet owners — not a dashboard-operator re-image card.

**RUNNER-BACKING BY TIER (Main-verified 07-16 via ci.yml, reusable — the discriminator that made "re-image" wrong for THIS tier but VALID for others):**
- `test-linux-release-gcc-x86_64-cpu` (#11955) = **ephemeral GitHub-hosted `ubuntu-24.04`** → no host to re-image; fix = resource-envelope/larger-runner.
- **Windows GPU tier** (`build/test-windows-*-gpu`) = **`["Windows","self-hosted","build"]` — PERSISTENT self-hosted GPU hardware.** So for a Windows-GPU device-loss (`DXGI_ERROR_DEVICE_HUNG`/TDR/`VK_ERROR_DEVICE_LOST`), "recycle/quarantine a bad physical host" IS a legitimate lever (NOT the retracted re-image mistake) — a real machine that can genuinely go bad.
- Linux GPU + SM80Plus test tiers = also self-hosted (`["Linux","self-hosted",...]`). macOS + aarch64 = hosted.
- **Escalation for a bad self-hosted host still routes through the maintainer / CI-fleet owner, NOT a dashboard-operator card** (same as #11955's larger-runner ask).

**07-16 — Windows-GPU device-loss WATCH (babysitter; distinct from #11955; single-host inference REFUTED, Main-caveat resolved):** 2 GPU device-loss events ~1h apart — #12064 MQ eviction (22:57Z, agent `win-test-a25bec8d`) + #12128 head-check (23:50Z, agent `win-test-cc83a149`). I flagged "same physical host" as unverified; **babysitter verified and REFUTED it (00:22Z):**
- **Decisive proof:** on run 29454574765, two agents (`win-test-a25bec8d`/89983 + `win-test-152c8e56`/89984) ran **concurrently**, BOTH reporting `Machine name: SLANG-WINDOWS-2`. A self-hosted runner does one job at a time → two concurrent agents under one "Machine name" ⇒ **"SLANG-WINDOWS-2" is an IMAGE-BAKED POOL NAME, not a physical host.** Resolve runners by `runner_name`/`runner_id`, never "Machine name". (Shared learning posted.)
- **Tier corrected:** device-loss jobs run on `["Windows","self-hosted","GCP-T4"]` (ci.yml:467-504) — the **GCP-T4 test pool**, autoscaled ephemeral runners (monotonic runner_ids 89125→90179/~24h, unique `win-test-<hex>` labels). Genuinely self-hosted (recycle-lever valid) but the recyclable unit = **pool/image/driver config, NOT one box.**
- **Limit:** bot can't resolve physical GCP-node identity (runners API → 403 "Resource not accessible by integration"; no GCP instance IDs in logs). Only per-job runner_name/runner_id visible.
- **✅ REVISED ESCALATION (pre-armed for 3rd hit):** frame as **"recurring GPU device-loss/TDR across the GCP-T4 Windows GPU pool"** → route to **CI-fleet owner/maintainer** (the #11955-larger-runner channel, NOT a dashboard-operator card), citing device-loss `runner_name`s + run IDs, asking them to check T4 driver / TDR-timeout config / per-VM GPU health. NOT "quarantine SLANG-WINDOWS-2." If a *specific* runner_name recurs, flag that too. Babysitter owns the watch + the escalation-post; my caveat is resolved — no further action unless 3rd hit fires or a specific host recurs.

**CLOSED by jkwak-work 07-15** ([comment 4986136886](https://github.com/shader-slang/slang/issues/11955#issuecomment-4986136886)): "I am closing this. When there are more intermittent failures observed, please create a new issue to track them. This issue has too much outdated info." **#11955 is a retired tracking surface — do NOT reopen it.** Standing instruction now live for slang-ci-babysitter: on next recurrence of THIS signature (`test-linux-release-gcc-x86_64-cpu / test-slang` SIGSEGV exit 139 at `static-const-matrix-array.slang.3 syn (llvm)`, or the OOM/HANG sibling modes), **file a FRESH issue** with a clean verified summary (occurrences + runner-backing fact + resource-envelope diagnosis) — never append to or reopen #11955. Carry forward: the ephemeral-`ubuntu-24.04`/resource-envelope framing (NOT re-image) and the distinct-from-#11951 finding.
