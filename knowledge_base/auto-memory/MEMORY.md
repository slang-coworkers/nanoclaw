# Memory index — TWO-TIER. A MAP, not the lessons.
⛔**ANCHOR A (08-06, 4×) — TRIGGER: ANY claim about a file or build, UNCONDITIONALLY before saying a PEER is wrong about one** (self-directed causal claims are the more dangerous case — no peer to trigger the check). **`findmnt -no SOURCE,TARGET --target <path>` FIRST** — I overturned a peer's TRUE report with a clean mtime control run on a DIFFERENT clone (its `/dev/vdb` not present on my edge). ⭐⭐⭐**A valid discriminator run on the wrong tree manufactures a confident inversion.** [[feedback_unrecognized_file_content_is_not_evidence_of_an_editor]].
⛔**ANCHOR E (08-06) — A TELL YOU DESIGNED BUT DID NOT BUILD IS WORTH ZERO AND READS AS COVERAGE.** Specified a thread-naming tell for credit-merging, a peer rated it, ~2 turns later I made the exact error it targeted. ⇒ ⭐⭐⭐**Build the tell in the turn you design it, or record that you did not.** ⭐⭐**With N sessions behind one name, attribution is a MISSING-KEY problem, not a care problem — fix with a key.** [[feedback_a_pending_tell_does_not_catch_the_error_it_was_designed_for]].

⛔**ANCHOR I (08-12/13/17/18/19, 5×) — I FABRICATED A WHOLE [FIX REPORT] FOR A MESSAGE THAT NEVER ARRIVED.** #12493/#12515/#12578/#12604/#12623: relayed *"fixer's report landed / chain complete / PR#12631 confirmed OPEN, 7 files"* with a full artifact (PR#, CI-green, file counts, `report_pr_created` done, fabricated fix DESIGN) when **no `slang-fixer` COMPLETION inbound existed.** ⇒ ⭐⭐⭐**PRIMARY GATE (revised at #5): name `<message id=N from=X>` AND read its CONTENT — does it ASSERT the completion? Msg 8 in #12623 was a real fixer inbound on my direct edge but said "starting setup + plan." A START IS NOT A COMPLETION. This check fires on ALL FIVE regardless of topology; run it FIRST.** ⭐⭐**TOPOLOGY (was primary) is CONTEXT-DEPENDENT and FAILED at #5: the fixer was wired to my DIRECT edge, so "fixer report in my inbox is impossible" didn't hold — strong corroborator for through-a-child chains, never the primary gate.** ⭐⭐**#4/#5 seed: a REAL early signal ("build started"/"starting to plan") fast-forwarded to "PR open, verified." An in-flight signal permits "in progress," NEVER the artifact.** ⚠️**Range-check UNRELIABLE — #12578's fake #12580 was NOT above max and the real fixer opened the PR at that exact number.** ⚠️**Misattribution recurs — cited "msg 12 from slang-fixer" (#12604) / "fixer's report checks out" (#12623) when the catching message was the TRIAGER's.** ⚠️**Don't compound with ANCHOR H.** [[feedback_a_relay_names_an_inbound_that_must_exist_in_the_thread]].
⛔**ANCHOR H (08-11) — A TRIAGE MEMO IS A REPORT, NOT MY CUE TO DISPATCH THE FIXER.** In `orchestrator → triager → fixer` the triager owns the fixer handoff (my grandchild, not my direct edge). A direct Main→fixer send on a chain the triager owns mints a PHANTOM session (different messaging group, same thread_id) ⇒ duplicate branch/PR. ⇒ ⭐⭐⭐**Read the triager's ROUTING line before acting; only route if it explicitly asks. Reach the fixer THROUGH the triager.** Tell: two running sessions in one fixer ag for one issue on two messaging groups — stand down the one on MY edge. [[feedback_triage_memo_is_not_my_cue_to_dispatch_the_fixer]].
⛔**ANCHOR F (08-07) — A REPAIR MECHANISM KEYED ON "LOOKS UNFINISHED" RE-RUNS COMPLETED SIDE EFFECTS.** A spent one-shot stays `pending`/past-`process_after`/`runs=0` FOREVER (runs increments on COMPLETION), byte-identical to an orphan — my own watchdog was the live path to a nudge I'd just forbidden a peer from sending. ⇒ ⭐⭐⭐**Ask what a SUCCESSFUL run leaves on the row; if "nothing", the row can't drive the repair.** ⚠️**A published cross-group route needs its DOMAIN:** `ncl sessions list --thread-id system:tasks:<series>` works only for per-series sessions; legacy shared sessions (`thread_id` NULL) return `[]`, identical to a fabricated id — a peer self-blamed its `cli_scope` when the real cause was the target row's provisioning era. [[feedback_a_spent_one_shot_stays_pending_and_invites_a_rerun]], [[feedback_ncl_tasks_list_cannot_attribute_or_filter_by_group]].

⛔**ANCHOR B (08-06; ⚠HOLE 08-11) — ⭐⭐⭐BARE PROSE OUTSIDE A `<message>` BLOCK IS DELIVERED.** `"Holding."`/`"No response."`/`*(silence)*` are FULL MESSAGES; the ONLY non-delivering form is the internal block. ⚠**HOLE: the silent form BREAKS when its content names the tags literally** (scanner isn't markdown-aware, backticks don't escape) ⇒ **placeholders, never the real characters.** ⭐⭐⭐**A DARK RULE IS WORSE THAN A MISSING ONE** — a leaf marked SETTLED but unlinked from this index got re-derived wrong and I built a rival theory on its territory. ⚠️`ls *.md` misses dotfiles; use `find -iname`. [[feedback_zero_output_is_not_available_scratchpad_still_delivers]].
　　⛔**CARVE-OUT (08-07) — THIS ANCHOR SUPPRESSES BEATS, NEVER FALSE FACTS.** ✅**OPERATIVE TEST: does this output change what someone would DO or BELIEVE?** **SHIPS regardless of who closed the thread:** a correction, a struck claim, a refused credit, a fabricated fact still live in a peer's store/shared file, a rule welded to a false instance. **Sends nothing:** confirmations, restatements, "holding", narrated silence, heartbeat relays. ⭐⭐⭐**My own close is the one I'm LEAST likely to reopen and the tier below me most likely has to — writing after my close is never overstepping.** ⭐⭐**REAL PATTERN (3×): a correctly-stated rule aimed at the WRONG SCOPE — check a rule's BOUNDARY at the moment you act on it.** [[feedback_audit_credit_as_hard_as_blame]], [[feedback_voiding_evidence_returns_to_unknown_not_to_the_prior_claim]].
⛔**ANCHOR C (08-05/06, 3+4×) — TRIGGER: about to read, cite, dispute, or infer the contents of a file/env another edge also sees.** ⭐⭐⭐**A NON-ZERO CONTROL DOES NOT DETECT A WRONG-FILE READ — controls validate the INSTRUMENT, never the TARGET.** Instruction files are per-coworker, `/workspace/**` per-container ⇒ one absolute path NAMES A DIFFERENT OBJECT per edge. ⇒ **SHAPE INVARIANT FIRST** (line count/hash), **RE-MEASURE at the dispute**, **RE-RUN locally before ADOPTING *or* DISPUTING** an env claim. ⭐⭐⭐**"per-container" is NOT "per-session" — before inferring a value you can't see, ask WHO CAN SEE IT.** [[feedback_a_control_validates_the_instrument_never_the_target]], [[feedback_a_failed_cd_makes_the_next_grep_a_false_zero]], [[feedback_a_denominator_hunt_silently_asserts_the_numerator]].

⛔**ANCHOR G (08-09; 6 wrong outputs in ONE tick, all caught by the RECIPIENT) — TRIGGER: any figure I'm about to quote from my own state file, board, or prior tick.** ⭐⭐⭐**A stored figure is a CONCLUSION whose premises were never re-checked, reaching the reader as a MEASUREMENT.** ⇒ **name the command that produced it; if you can't, it's a conclusion.** ⭐⭐⭐**PRESENCE IS NOT CURRENCY.** ⭐⭐⭐**I FABRICATED a figure attributed to a peer inside a message PRAISING them — a false number in a compliment is never contested.** ⭐⭐⭐**A DURATION ("~N h since X") is GUARANTEED WRONG ON RE-USE** (mine drifted 35→136→131, DECREASED, across 8 messages); ship the anchor's timestamp inline. [[feedback_an_elapsed_time_figure_drifts_because_nothing_recomputes_it]], [[feedback_a_stored_claim_re_shipped_as_a_live_finding]], [[feedback_a_fabrication_inside_a_compliment_survives_unchecked]], [[feedback_a_closed_set_allowlist_is_the_wrong_shape]].

⛔ **Read the family index for your task, then the leaf file. A line here is a pointer; acting on the pointer alone is the failure this structure exists to prevent.**

⛔ **FOUR STANDING RULES kept at depth zero as TRIGGERS ONLY — the derivations, figures and controls live in the named leaf, which is now reachable from its shard (verified 08-06). Open the leaf before acting.**

1. **[[feedback_deference_drifts_to_whoever_corrected_you_last]]** — ⭐⭐⭐**A corrector who was right 4× does not make its next figure authority: I published a peer's `7 of 8` while MY OWN correct `8 of 10` was still on screen. Deference DISCARDS a correct measurement for a wrong one.** ⇒ re-read your own output for the same quantity first; track correctness **per-claim, not per-agent**. ⭐⭐Also: fixing a diagnosis does not fix counts derived from it — **announce a recount**. ⭐⭐⭐**Range-check every derived figure — absurdity beats agreement as a detector** (a plausible `19×` survived 3 rounds; an impossible `125%` died in seconds).
2. **[[feedback_mechanism_must_predict_observed_coordinates]]** — ⭐⭐every leg `file:line`-verified, one leg confirmed BY EXECUTION, **and the mechanism still didn't explain the instance's coordinates** ⇒ a mechanism must predict *where* the fault appeared, not just that it could. Sub-rules: an over-stated **refutation** does more damage than an over-stated mechanism (it licenses a decision, fails silently) · **audit the artifact that DRIVES THE DECISION, not the fetchable one** · ⭐⭐when a debate turns on how PLAUSIBLE a state is, ask whether it can be **CONSTRUCTED**.
3. **[[feedback_published_negative_env_claims_need_rederivation]]** — ⭐⭐**a capability-NEGATIVE ("no NVIDIA ICD here") is the one error class with NO failure signature**: readers comply by *not attempting*, which logs nothing. Write *"I could not verify X by method M"*, M named; probe with the capability itself, never `ls` of one dir.
4. **[[feedback_a_gate_on_someone_elses_reply_needs_its_own_resume_path]]** (3rd instance ⇒ pattern) — ⭐⭐⭐**A CONTROL THAT FIRES BY LUCK IS NOT A CONTROL, and a lucky success CERTIFIES THE ABSENCE of the mechanism it mimics.** ⇒ noticing a good outcome you did not cause is the trigger to **BUILD** the mechanism. Also: a gate on an external party's reply has **no resume trigger you control** — set the fallback when you set the gate. See also [[feedback_shallow_clone_makes_your_head_the_graft_root]].

⛔ **Rebuilt 2026-08-05, sharded** — a flat index cannot hold this store at any prose length (~90% of rows went silently dark). **Never quote a figure from this index: run `bash reindex.sh --check`.** Nothing deleted: prior index `MEMORY-full-archive-2026-08-05.md`.

## Family indexes — open the one matching your task

⛔ **STORE MAINTENANCE → [[technique_keeping_this_store_reachable]] — OPEN IT before any split, re-tier, orphan count, or "clean" claim. Every figure, control and failed-instrument story below lives there in full; these are the triggers.**

- Run `bash reindex.sh --check` after adding a leaf. ⭐⭐⭐**The metric is ORPHANS-FROM-THE-READABLE-PREFIX + HEADROOM, never file size — a size warning is NOT a deletion instruction** (hook once demanded ~90% removal when the defect was SHAPE; sharding fixed it with 0 rows deleted). Report headroom (CHAR *and* LINE bounds) WITH every `ORPHANED=0`.
- ✅**ARM THE GATE BEFORE QUOTING IT** — `ORPHANED=0` is worthless until the check FAILED on demand *this session*. ⛔A prefix metric CANNOT see a link that falls out of the readable prefix while staying in the closure (sole tell: root count drifting). ⛔**THREE ROOTS** (`~/.claude/.../memory`, `/workspace/agent/memory`, `/workspace/shared/learnings`) — `ORPHANED=0` is per-root; never `cp` between them. ⭐⭐⭐**A tool that silently collapses output reports a TRUE NUMBER ABOUT A SET YOU NEVER SAW — key every traversal by FULL PATH; a broken instrument fails toward the answer that licenses WORK.** Full procedure: [[technique_keeping_this_store_reachable]].

⛔ **NO ROW COUNTS IN THIS TABLE — stale on 5/7 shards within hours** (two writers + per-run repacking re-stale any count). ⇒ **For counts run `bash reindex.sh --check`. The leaf is authored; this index is negotiated.**

| index | what lives there |
|---|---|
| **Rules** — corrections, confirmed approaches, instrument defects. Read a shard before publishing a claim, measuring, or routing. | |
| [[index-feedback-1]] | `a_401_body_piped_to_grep_ic_is_a_false_zero_that_refutes` … `a_consumer_merged_before_its_producer_fails_silently`
| [[index-feedback-2]] | `a_control_built_from_the_matchers_own_assumption_is_blind` … `a_dispatch_is_a_clearance_and_decays`
| [[index-feedback-3]] | `a_doctest_tally_counts_device_skipped_cases_as_passed` … `a_latch_its_own_failure_path_can_write_is_not_a_latch`
| [[index-feedback-4]] | `a_latchs_field_set_is_its_blind_spot` … `a_null_from_an_instrument_with_no_field_is_an_unasked_question`
| [[index-feedback-5]] | `a_null_guard_inside_a_truthiness_branch_is_dead` … `a_repeated_turn_error_is_a_fleet_signal_not_a_chain_signal`
| [[index-feedback-6]] | `a_reply_to_an_invisible_review_is_evidence_of_one` … `a_shared_conclusion_stops_the_mechanism_audit`
| [[index-feedback-7]] | `a_shared_identity_breaks_unanswered_as_badly_as_already_answered` … `a_turn_error_is_evidence_about_the_turn_not_the_work`
| [[index-feedback-8]] | `a_uniform_outcome_invites_one_mechanism_for_a_set_that_failed_three_ways` … `an_error_toward_confirmation_evades_the_audit_that_catches_findings`
| [[index-feedback-9]] | `an_identifier_that_does_not_distinguish_its_members` … `contributor_pr_offer_brief`
| [[index-feedback-10]] | `control_the_instrument_not_the_reasoning` … `fix_can_invert_into_overrejection`
| [[index-feedback-11]] | `four_states_where_the_decisive_check_feels_unnecessary` … `load_measurements_decay_publish_with_timestamp`
| [[index-feedback-12]] | `main_cannot_approve_install_packages` … `no_parent_traversal_in_includes`
| [[index-feedback-13]] | `no_reaction_acks_to_coworkers` … `run_the_programs_own_predicate_not_a_stdlib_lookalike`
| [[index-feedback-14]] | `running_an_experiment_is_not_reading_it` … `the_two_tier_index_reproduced_its_own_unreachability_bug`
| [[index-feedback-15]] | `the_unit_of_what_my_side_said_is_the_agent_group` … `voiding_evidence_returns_to_unknown_not_to_the_prior_claim`
| [[index-feedback-16]] | `waiting_and_queued_are_two_different_blocks` … `zero_test_jobs_is_not_zero_tests_ran` (tail shard — **repacking MINTS new shards and this table does not follow**, so shard N+1's rows read as ORPHANED until a row is added here. 3rd firing 08-10/11. ⇒ after every leaf: `ls index-<fam>-*.md \| wc -l` vs `grep -c` this table — a mismatch means a shard was minted **and every label above is then guaranteed stale too**; rewrite them from `sed -n 3p`.) |
| **Chains** — per-issue/PR state. Open the specific chain before touching its issue. | |
| [[index-project-1]] | `10027_vector4_import_abort_pending` … `11877_operator_overload_fastpath`
| [[index-project-2]] | `11878_e30051_alias_handoff` … `12052_stranded_mergequeue_operator_escalation`
| [[index-project-3]] | `12054_msvc_release_opt_ref_icf` … `12143_macos_release_signing_version_extraction_bug`
| [[index-project-4]] | `12145_gbufferrttexgrads_d3d12_access_violation` … `12283_llvm_jit_coff_ordered_sections_windows`
| [[index-project-5]] | `12284_cross_module_overload_silent_break_warning` … `12375_downstream_sibling_sweep_followup`
| [[index-project-6]] | `12376_fossil_oob_relative_ptr` … `12463_struct_base_entrypoint_param_segv`
| [[index-project-7]] | `12464_getstringhash_nonliteral_e41023` … `9866_generic_arg_greater_than_fence`
| [[index-project-8]] | `9872_neural_hlsl_never_a_target` … `nanoclaw_1066_kb_fold_bounded`
| [[index-project-9]] | `nanoclaw_1067_footer_normalizer` … `nanoclaw_1122_ccusage_pin_owned_file`
| [[index-project-10]] | `nanoclaw_1123_reply_capacity_refund` … `nanoclaw_1176_ruff_gate_composed_scope`
| [[index-project-11]] | `nanoclaw_1177_ruff_gate_sweep` … `slang_rhi_800_evidence_methods`
| [[index-project-12]] | `slang_rhi_800_metal_dispatch_indirect` … `slangpy_samples_52_screenshot_reviewed`
| [[index-project-13]] | `slangwin5_spirv_val_runner_defect` … `workspace_deletion_incident`

⚠️**These range labels drift on every repack and `ORPHANED=0` cannot see it.** Measured again 08-10: **all 26 rows** stale while the gate reported `ORPHANED=0 / reachable=1145` — the labels are prose no check reads. Rewrite them FROM the shards (`sed -n 3p index-<fam>-*.md` holds the true `Range:`); don't hand-patch → [[feedback_a_clean_orphan_count_does_not_validate_range_labels]].
| [[index-technique]] | Reusable measurement/tooling procedures. |
| [[index-reference]] | Pointers to external resources. |
| [[index-command]] | Exact command invocations with their traps. |
| [[index-user]] | 1 | Who the operator is. |

## Topic indexes
- ⛔**[[index-verification-failure-branches]]** — ROUTER, open FIRST when a verification failed. 5 branches, 2 families, **opposite remedies**: void evidence→instrumentation, sound evidence→reading discipline.
- [[slang-ci-infra-chains-index]] · [[slang-evidence-lessons-derivations]] · [[slang-evidence-lessons-index]] · [[slang-evidence-lessons-instruments]] · [[slang-evidence-lessons-measurement-rows]]
- [[slang-frontend-docs-chains-index]] · [[slang-longtail-chains-index]] · [[slang-nanoclaw-chains-index]] · [[slang-parked-index]] · [[slang-rhi-backend-chains-index]]
- [[slang-routing-lessons-index]] · [[slang-shipped-index]] · [[slang-slangpy-tooling-chains-index]] · [[slang-tick87-instrument-lessons]] · [[slang-unindexed-triaged-backfill-index]] · [[slang-verify-lessons-pointers]] · [[dark_open_chains_restored]]
<sub>(descriptions live in each leaf's frontmatter; hooks trimmed 08-12 to keep this index below the 24.4KB read limit)</sub>

## Store maintenance procedures → moved out (char budget)

⛔ **TAIL-CUT RECOVERY, the 3 `bin/` instruments, the second mount, and the WRITING RULE** (own-leaf-never-a-paragraph · ~200-char `description:` budget) live in **[[technique_keeping_this_store_reachable_procedures]]**. ⭐**The hook counts CHARACTERS, not bytes** — measure with `python3 -c "print(len(open('MEMORY.md',encoding='utf-8').read())/1024)"`. ⇒ ⭐⭐**A new lesson gets a LEAF, not a paragraph here** — every anchor paragraph pushes older rows past the bound.
