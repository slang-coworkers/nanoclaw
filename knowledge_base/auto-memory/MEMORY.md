# Memory index — TWO-TIER. This file is a MAP, not the lessons.
⛔**ANCHORED TOP (08-05) — [[feedback_a_failed_cd_makes_the_next_grep_a_false_zero]] 2nd instance + ✅`<internal>` SETTLED**: ⭐⭐⭐**A NON-ZERO CONTROL DOES NOT DETECT A WRONG-FILE READ — controls validate the INSTRUMENT, never the TARGET.** A peer disproved my `CLAUDE.md:64` scratchpad claim with clean controls (`scratchpad=0 internal=0`, 549 lines, non-zero `Slang=15`); my mount said (08-05) `scratchpad=1 internal=2`, **464 lines, `Slang=0`** ⚠️**RE-MEASURED 08-06 after an instruction update + restart: now `478 lines / 45,597 B`, `scratchpad=2 internal=4`, `Slang=0`, line 64 still the `<internal>` row — the 464/1/2 values are a HISTORICAL snapshot, do NOT quote them as current.** ⭐⭐**A shape invariant ends an argument only while FRESH: these files are recomposed on every container wake, so RE-MEASURE at the moment of the dispute instead of citing a stored count. The method survives; the values expired.**, line 64 verbatim `| Internal scratchpad | `<internal>…</internal>` | not delivered |`. **DIFFERENT FILES — instruction files are COMPOSED PER COWORKER.** ⇒ ⭐⭐⭐**When two parties disagree about a file, compare a SHAPE INVARIANT FIRST (line count / hash / a control's count) — it ends the argument in one exchange where prose comparison runs for rounds.** ⭐⭐**Absolute paths in every cross-party file claim; `CLAUDE.md` is not a referent between coworkers.** ⛔**My own near-miss: `cd <dir> && grep -c … CLAUDE.md` (RELATIVE name, cwd resets between calls) errored instead of printing 0 — had that dir held a `CLAUDE.md` I'd have counted the wrong file and CONCEDED A TRUE CLAIM from a false zero.** ✅**`<internal>…</internal>` IS suppressed (peer settled it from EXISTING logs, no test message: 2 composed turns where the `<internal>` half is absent and the bare half delivered ⇒ "whole turn dropped" excluded BY CONSTRUCTION; 5 phrases ×0 over 114 rows).** ⇒ ⭐⭐**Before accepting "this needs a live test", ask what you have ALREADY RUN that discriminates it — zero cost, zero intrusion, cannot perturb the measured thing.**

⛔ **Read the family index for your task, then the leaf file. A line here is a pointer; acting on the pointer alone is the failure this structure exists to prevent.**

⛔ **PROMOTED 08-05 (its `index-feedback` row sits at offset 39,021, PAST the ~24,985 readable prefix — a pointer nobody can load is not stored): [[feedback_deference_drifts_to_whoever_corrected_you_last]]** — ⭐⭐⭐**After 4 rounds of a peer correctly catching my errors, its figures became authority and mine draft: I published its `7 of 8` into a maintainer-facing comment while MY OWN audit output, still on screen, read `8 of 10` (correct). Deference DISCARDS a correct measurement for a wrong one — every other failure that day replaced nothing with something wrong.** ⇒ **Before adopting a corrector's figure, re-read your own output for the same quantity; track correctness PER-CLAIM, not per-agent.** ⭐⭐**Companion: fixing a diagnosis does not fix counts derived from it — nor your COUNTERPARTY's** (peer diagnosed its regex's generic-blindness, then quoted `8`/`11` from that same probe 2 messages later; I propagated the 8 — one broken instrument, two tiers, four rounds) ⇒ **announce a recount, not just a diagnosis.** ⭐⭐⭐**Plausibility is what let the wrong number live: a plausible `19×` survived 2 tiers × 3 rounds of mutual verification; an impossible `125%` died in seconds ⇒ range-check every derived figure — absurdity beats agreement as a detector.**

⛔ **PROMOTED 08-05 (spy#1089 — both rows sit at `index-feedback` offsets 51,273 / 60,697, past the ~24,985 readable prefix):** [[feedback_mechanism_must_predict_observed_coordinates]] — ⭐⭐**every leg `file:line`-verified, the key leg later confirmed BY EXECUTION, and the mechanism still didn't explain the instance** (null-proc theory vs a fault at the *second* of two calls; a null fn ptr faults at the **FIRST** ⇒ grep every call site). Sub-rules: an over-stated **refutation** beats an over-stated mechanism for damage (it licenses a *decision* and fails silently) · **asymmetric hedging — audit the artifact that DRIVES THE DECISION, not the fetchable one** (I gated the public comment twice, never asked for the memo, and the unhedged copy was in the handoff) · ⭐⭐**when a debate turns on how PLAUSIBLE a state is, stop arguing and ask whether it can be CONSTRUCTED.** · [[feedback_published_negative_env_claims_need_rederivation]] — ⭐⭐**a capability-NEGATIVE ("no NVIDIA ICD here") is the one error class with NO failure signature** — readers comply by *not attempting*, which logs nothing; it was the sole blocker on the test that settled the chain, and **I relayed it to the operator as fact.** Write *"I could not verify X by method M"*, M named; probe with the capability itself (`vkEnumeratePhysicalDevices`, not `ls` of one dir).

⛔ **PROMOTED 08-05 — 3rd INSTANCE, so a pattern not an anecdote: [[feedback_a_gate_on_someone_elses_reply_needs_its_own_resume_path]]** — ⭐⭐⭐**A CONTROL THAT FIRES BY LUCK IS NOT A CONTROL, and a lucky success CERTIFIES THE ABSENCE of the mechanism it mimics.** spy#1089: the reporter-silence gap was resolved by an unrelated maintainer assignment · I ran the check that distinguished two retrieval-failure modes only as a **side effect** of measuring index byte offsets · and weeks earlier my #802 verifications were depth-independent by "luck of habit, not design" ([[feedback_shallow_clone_makes_your_head_the_graft_root]]). ⇒ **noticing a good outcome you did not cause is the trigger to BUILD the mechanism**, while you can still see what it should have been. Also: **a gate on an external party's reply has no resume trigger you control** — low friction ≠ a reply arrives; set the fallback when you set the gate. ✅**Counter-example — what a real control looks like:** the `no-Fixes`-on-a-latent-defect-PR guardrail held **deliberately** across 4 rounds, 2 public PR bodies, and a tier that never re-asserted it — stated once, survived every hop, left a checkable artifact.

⛔ **Rebuilt 2026-08-05.** The prior flat index was **221KB against a 24.4KB read limit** — ~90% of rows were silently dropped on load, so most "stored" lessons were unreachable in practice. Arithmetic that forced the change: **667 entries ÷ 17.1KB = 25.6 bytes/entry**, less than one filename. A flat index cannot hold this store at any prose length. Nothing was deleted: full prior index archived at `MEMORY-full-archive-2026-08-05.md` (221KB, 182 lines), and every leaf `.md` is untouched on disk.

## Family indexes — open the one matching your task

⛔ **STORE MAINTENANCE → [[technique_keeping_this_store_reachable]]. Run `bash reindex.sh` after adding a leaf (`--check` = read-only audit, nonzero exit on any orphan).** ⭐⭐⭐**The metric is ORPHANS-FROM-THE-READABLE-PREFIX, never file size** — and ⛔**a size warning is NOT a deletion instruction**: the hook demanded ~90% removal when the defect was SHAPE; sharding fixed it with **0 rows deleted** (515 of 729 leaves had been dark). ⭐⭐**Orphans regrew TWICE in 40 min from concurrent sibling writes ⇒ a fix that must be REMEMBERED by the next writer is not a fix**, hence a script. ⭐⭐⭐**"0 orphans" is a SNAPSHOT — the durable question is WHERE THE NEXT WRITE LANDS** (equal-row-count shards left one with ~8 rows of headroom; now size-packed, min ~31). ⛔**TWO STORES: this one, and `/workspace/agent/memory/` (hook-loaded, 52 `legoop-*` + the ONLY copy of a 🔴 `addAttrs` bug memo) — a link-walk there reached 3 of 77 because its index was named in BACKTICKS, never linked ⇒ ⭐⭐⭐a filename in prose is invisible to every reachability check. Never `cp` between them.** Now 732/732 and 60+16-scratch.

⛔ **NO ROW COUNTS IN THIS TABLE — they were stale on 5 of 7 shards within hours (table said 37/35/48/43/47/41/4; disk was 36/35/43/46/45/41/13).** Two writers touch this store, `reindex.sh` re-packs shard boundaries on every run, and **a count here is re-staled by the next leaf anybody adds.** A stale number that reads as fact is worse than no number: it invites arithmetic. ⇒ **For counts, run `bash reindex.sh --check` — it reports live totals and orphans. The leaf is authored; this index is negotiated.**

| index | what lives there |
|---|---|
| **Rules** — corrections, confirmed approaches, instrument defects. Read a shard before publishing a claim, measuring, or routing. | |
| [[index-feedback-1]] | `a_batch_census_needs_the_o…` … `a_multi_probe_turn_has_a_w…` |
| [[index-feedback-2]] | `a_negative_control_must_va…` … `a_tools_output_set_is_scop…` |
| [[index-feedback-3]] | `a_transmitted_artifacts_si…` … `dont_route_operator_gates_…` |
| [[index-feedback-4]] | `drafts_only_guardrail…` … `near_miss_number_is_a_boun…` |
| [[index-feedback-5]] | `never_cite_a_peers_artifac…` … `six_errors_one_mechanism_a…` |
| [[index-feedback-6]] | `squash_merge_breaks_merge_…` … `void_the_execution_claims_…` |
| [[index-feedback-7]] | `watchdog_ncl_tasks_list_em…` … `zero_test_jobs_is_not_zero…` |
| **Chains** — per-issue/PR state. Open the specific chain before touching its issue. | | |
| [[index-project-1]] | 52 | `10027_vector4_import_abort…` … `11878_e30051_alias_handoff…` |
| [[index-project-2]] | 67 | `11881_numthreads_override_…` … `12069_endian_ptrsize_white…` |
| [[index-project-3]] | 48 | `12071_bwddiff_loop_vector_…` … `12154_relocate_slang_test_…` |
| [[index-project-4]] | 73 | `12157_ir_version_check_req…` … `12320_coverage_macos_segfa…` |
| [[index-project-5]] | 40 | `12321_bfloat16_vector_vulk…` … `8183_wgsl_metal_displaceme…` |
| [[index-project-6]] | 39 | `8306_8785_triager_session_…` … `dup_pr_inadequate_existenc…` |
| [[index-project-7]] | 53 | `fixer_restart_tripwire…` … `release_ci_babysitter_stal…` |
| [[index-project-8]] | 53 | `review_pipeline_shared_tmp…` … `slangpy_samples_52_screens…` |
| [[index-project-9]] | 8 | `slangwin5_spirv_val_runner…` … `workspace_deletion_inciden…` |
| [[index-technique]] | 16 | Reusable measurement/tooling procedures. |
| [[index-reference]] | 4 | Pointers to external resources. |
| [[index-command]] | 3 | Exact command invocations with their traps. |
| [[index-user]] | 1 | Who the operator is. |

## Topic indexes
- [[slang-ci-infra-chains-index]] — "LIVE CI-infra chains spilled from MEMORY.md 2026-08-04 (4th spill): SPIR-V validation outage, #12145 queue-evicting flake, supervisor bounce-limb fal
- [[slang-evidence-lessons-derivations]] — Long-form derivations for the evidence & verification standing lessons — split out of MEMORY.md to keep the index under the Read limit. Conclusions 
- [[slang-evidence-lessons-index]] — "The evidence & verification standing lessons — full one-line-per-rule text, moved verbatim out of MEMORY.md. Open BEFORE any verification, correcti
- [[slang-evidence-lessons-instruments]] — "Four unabridged evidence rules split out 2026-08-04 because the evidence index crossed its 24.4KB read limit: check-runs currency, per-container path
- [[slang-evidence-lessons-measurement-rows]] — "Five evidence/verification lesson rows: subagent-restore-claim (added 08-04, root index was over the Read bound), plus four spilled verbatim from MEM
- [[slang-frontend-docs-chains-index]] — Live slang frontend/codegen/docs chains spilled from MEMORY.md at the 08-04 compaction floor. Every row keeps its RESUME trigger verbatim; all are mai
- [[slang-longtail-chains-index]] — "Chains with no live RESUME trigger of their own, folded out of MEMORY.md on 2026-08-04 to stay under the index read limit. Each row is a pointer plus
- [[slang-nanoclaw-chains-index]] — "slang-coworkers/nanoclaw (platform-infra repo) live chain rows, spilled from MEMORY.md at the index floor 2026-08-04. Open before any nanoclaw PR/rev
- [[slang-parked-index]] — "Long-tail index of chains that need no action now — parked, maintainer-owned, bot-unpushable, or merged/terminal. Split out of MEMORY.md to keep th
- [[slang-rhi-backend-chains-index]] — LIVE chains — slang-rhi + backend-codegen (Metal/CUDA/HLSL-SPIRV). Most rows were spilled from MEMORY.md 2026-08-04 (3rd spill) and are gated on a N
- [[slang-routing-lessons-index]] — "Standing lessons on routing, posting authority, GitHub write gates, a2a edges, infra/perms, and tooling. Split out of MEMORY.md to keep the root inde
- [[slang-shipped-index]] — "Long-tail index of shipped / merged / reference chains kept for lookup only — no action pending. Split out of MEMORY.md to keep the root index scan
- [[slang-slangpy-tooling-chains-index]] — Live slangpy and approver/gate tooling chains moved out of the index at the 08-04 compaction floor. Each row keeps its RESUME trigger verbatim.
- [[slang-tick87-instrument-lessons]] — "Five verification failures from one supervisor tick, each with the check that catches it. Common root: a correct measurement over an unverified scope
- [[slang-unindexed-triaged-backfill-index]] — "Backfill index for the 18 still-OPEN slang issues that slang-triager triaged but that had ZERO footprint in my store. One row per issue: verdict, com
- [[slang-verify-lessons-pointers]] — Pointer-only rows split out of MEMORY.md — one-line verification/CI lessons whose full text lives in their own feedback files. No content here beyon
- [[dark_open_chains_restored]] — "RESTORED-FOR-REACHABILITY index: every memo found reachable from NO index whose issue is STILL OPEN upstream (24 at restore time — treat the count 

## ⛔ TAIL-CUT RECOVERY — rebuild from disk, no surviving link required

⛔**GLOB-FREE BY DESIGN.** The prior version of this block enumerated by prefix
(`ls slang-*.md dark_*.md`, `ls feedback_*.md technique_*.md …`) — ⛔**a glob index is blind to exactly
the size of your naming inconsistency, and it fails SILENTLY: a file matching no prefix simply does not
appear, with no error.** Measured 08-05: the six globs covered 727 of 729 files and missed
**`MEMORY-full-archive-*.md`** — i.e. the archive that is this block's own fallback. **List the
directory; grep for the prefixes only to sort what you find.**

```
# 1. THE WHOLE STORE, no prefix assumptions
cd /home/node/.claude/projects/-workspace-agent/memory && ls *.md | wc -l && ls *.md

# 2. The archive of the pre-rebuild flat index (the globs missed this)
ls MEMORY-full-archive-*.md

# 3. Instruments — NOT .md, so every glob above is blind to them
ls bin/*.py     # THREE tools, three questions a single one cannot answer:
#   fragcheck.py  content: is X present?  (given X)
#   nbrcheck.py   loss:    did this edit destroy anything? (harvests the expected set)
#   rootcheck.py  universe: which ROOT does this reference resolve in? (sibling-authored;
#                 tier-2 errors — wrong universe entirely — are invisible to widening a
#                 search within one root, so neither tool above can see them)

# 4. SECOND MOUNT — 71 files that exist ONLY there; no glob in this store can reach them
ls /workspace/agent/memory/*.md /workspace/agent/memory/*.json 2>/dev/null | wc -l

# 5. Live chains: read the description: field, do not trust the filename
grep -l 'RESUME' *.md
```

Regenerate a family index after adding a memory (idempotent, reads `description:` from each file):
```
fam=feedback; { printf -- '---\ntype: index\n---\n\n# %s_*\n\n' "$fam"; for f in ${fam}_*.md; do n="${f%.md}"; d=$(awk '/^description:/{sub(/^description: */,""); print; exit}' "$f"); printf -- '- [[%s]] — %s\n' "$n" "${d:-(no description)}"; done; } > index-$fam.md
```

## Writing rule for this store

⭐⭐⭐ **A new lesson goes in its own leaf file with a tight `description:`, and its family index is regenerated. It does NOT get a paragraph here.** Every anchored paragraph appended to the top of the old index pushed ~2KB of older rows past the read bound — the store grew while becoming less reachable, and each rescue row displaced more ([[feedback_a_remedy_that_can_reproduce_its_own_bug]]). Keep the `description:` field under ~200 chars: it is the retrieval surface, and at 667 entries the median is 167 but the max was 1905.

✅ **Frontmatter repaired 2026-08-05** — [[feedback_ncl_sessions_list_agent_group_flag_not_filtering]] had `name: ""` and no `description:`/`type:`, leaving the store's richest instrument file unreachable by scan. All three keys present; `index-feedback` = **235 entries, 0 missing descriptions, 0 split-frontmatter**. ⚠️Its FILENAME still asserts what its own correction retracts (`--agent-group` doesn't exist; `--agent-group-id` works) — kept to preserve inbound links; the description leads with the retraction. ⭐**"Repair when next editing it" was a deferral with no trigger — it sat for a day. Fix frontmatter on sight** ([[feedback_a_deferral_whose_trigger_cannot_fire_is_a_deletion]]).

⚠️ **Measured 08-05 21:57 — "0 missing descriptions" is a PRESENCE check, and the store's live defect is now the opposite: OVERSIZE.** Of **696** files with a `description:`, **83 exceed 400 chars** (max **2,568** — `project_slangwin5_spirv_val_runner_defect`; then 1,873 / 1,550 / 1,386 / 1,312). Against the ~200-char budget on line 56 that is ~12% of the store spending the retrieval surface it was meant to save. ⭐⭐ **A present-but-1,000-char description is as unscannable as an absent one, and it passes every completeness check** — presence and fitness are orthogonal, so `0 missing` says nothing about usability. Cut on sight when already editing a file (move the full text to a body section — nothing need be lost); **not worth a sweep of 83 files unasked**. Three cut this way already: `feedback_ncl_sessions_list_agent_group_flag_not_filtering` (991→256, full text kept as a body section), plus the two #8373 leaves (365→259, 520→280).
