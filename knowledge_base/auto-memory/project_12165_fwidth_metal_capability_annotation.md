---
name: project_12165_fwidth_metal_capability_annotation
description: IN-FLIGHT
metadata: 
  node_type: memory
  type: project
  originSessionId: 6742f01a-01b6-4371-b3e9-a9afc2aabf9b
---

IN-FLIGHT shader-slang/slang#12165 — `fwidth(float2)` rejected in `fragment` stage for `metal` target with E36107 "unavailable features in entry point". Reporter hapcode, Slang 2026.13.1, M1 Pro.

**Triaged 07-21** (triager, reproduced @HEAD 6a244fee2, verdict posted [comment 5032598566](https://github.com/shader-slang/slang/issues/12165#issuecomment-5032598566), `reproduced`+Type=Bug labels, forwarded to slang-fixer):

**Root cause — stdlib capability-annotation error, NOT a real Metal gap.** Vector (`hlsl.meta.slang:11537`) and matrix (`:11559`) `fwidth` overloads omit the `metal` atom in `[require]`; scalar overload + all ddx/ddy overloads already carry it. Repro's `fwidth(float2)` hits vector overload → E36107. Proven: scalar `fwidth`+metal COMPILES, vector+metal FAILS, vector+hlsl/wgsl COMPILE (controls).

**Fix (fixer's task):** add `metal` to both require clauses + a `case metal:` to the vector body (body has no metal case and no default — require-only fix incomplete); matrix body needs no change (`default: MATRIX_MAP_UNARY`). Draft PR + regression test (emit-level MSL check). `fwidth_coarse`/`fine` deliberately excluded (no Metal coarse/fine control).

Classification: bug / medium / P2 / frontend+Metal-emit.

**Fix landed 07-21** (slang-fixer): draft PR **#12172** open, verified @HEAD 8cb0af1b65. 2 files +24/−2 — `hlsl.meta.slang` (vector+matrix `fwidth` gain `metal` in `[require]`, +`wgsl` on matrix, +`case metal:` in vector body) + new `tests/metal/fwidth.slang`. Approach A; coarse/fine untouched. Tests: baseline reproduces E36107 @:11537; post-fix vector+matrix emit MSL `fwidth(`, SPIR-V validates, hlsl/wgsl controls compile; `slang-test tests/metal/` 40/40 pass. Review: cross-backend-reviewer NO DEFECTS; codex PLAN/CODE/OUTPUT approved. Peer review dispatched to slang-reviewer by fixer. Labeled `pr: non-breaking`; initial `ci_failed` webhook = priority-yield cosmetic red (all builds skipped, no-op — matches bot-PR workflow_dispatch pattern).

Chain OPEN — awaiting slang-reviewer verdict + real CI. Drafts-only guardrail applies (held draft until maintainer flips).

**Reviewer running (07-21 10:56):** slang-reviewer running 3-reviewer pass (A correctness + C clarity in PATCH mode against fetched `refs/pull/12172/head` = 8cb0af1b65 matching HEAD; B Devin anonymous-scrape live URL). No `<github-post-authorized />` marker in my tasking → reviewer will NOT auto-post; delivers combined review via file + relays verdict to me on canonical thread (exactly what I want — issue surface is triager's, PR is held draft). ~20-30 min. Reviewer also flagged `gh` token invalid for pr-mode diff-fetch — but NOT escalated: triager successfully edited comment 5032598566 in place at 10:47 (confirmed write 9 min prior), and per [gh auth probes are misleading] + [reads-OK 401 flaps], reviewer routed around via git-through-proxy. Watch for a CONFIRMED push/write 401 before treating as outage.

**Interim finding (11:04):** Reviewer C (clarity) flagged **FG002** — real test-discrimination gap: `tests/metal/fwidth.slang` has two identical `fwidth(` checks that can BOTH be satisfied by the matrix call alone, so a vector-call regression (the exact bug fixed) wouldn't be caught. Likely fixer follow-up: make the vector-emit check independently distinguishable. Reviewer A (correctness) + B (Devin) STILL RUNNING — hold for consolidated verdict, relay all findings to fixer in ONE pass (no piecemeal action on one reviewer's partial).

**07-23 13:58 — MAINTAINER ENGAGED (jkwak-work):** reviewed PR #12172, flipped it **ready-for-review** (non-draft — MAINTAINER flip, not the bot → drafts-only guardrail intact; human took the wheel, merge still operator/human-gated). jkwak's review ask: add a `-target metallib` test ("make sure the metal compiler is happy with what Slang emits, not just what Slang emits" — metal only checks MSL text, metallib runs real Metal compiler → AIR). **Fixer addressed:** added `//TEST:SIMPLE(filecheck=METALLIB): -target metallib` + `// METALLIB: define {{.*}} @fragMain` to `tests/metal/fwidth.slang`, replied in-thread, pushed **commit `ad29b27f92`** (HEAD moved from 8cb0af1b65 — CODE fix in hlsl.meta.slang UNCHANGED, only test file gained the metallib directive). Local: metallib gets through front-end+MSL emission cleanly, fails only at E00100 no-Metal-SDK-locally (NO E36107 → fix holds; metallib macOS-CI-gated). Real `pull_request` CI run **30013449344** running.

**Open reviewer reconciliation:** my dispatched slang-reviewer (07-21) reviewed OLD head 8cb0af1b65; consolidated verdict (Reviewer A correctness + B Devin) NEVER relayed to me — 2-day gap, likely in-session monitor died on teardown. FG002 (test-discrimination gap) STILL STANDS against new head — fixer only ADDED a directive, didn't fix the two-identical-`fwidth(`-checks issue. Re-pinged reviewer for consolidated verdict vs ad29b27f92 + surfaced FG002 to fixer to bundle with jkwak's test-hardening.

**CONSOLIDATED REVIEWER VERDICT (07-23 14:26, file combined-review-12172-v2.md):** `APPROVE_WITH_NITS — 0 bugs, 2 gaps` @ ad29b27f92. All 3 reviewers converge; **core Metal fix verified CORRECT** (matrix relies on `default:`→`MATRIX_MAP_UNARY`→per-row vector fwidth, no explicit matrix `case metal:` needed — confirmed idiomatic, 30+ matrix intrinsics use same pattern). Reviewer B (Devin) v2 CLEAN 0/0. Two gaps (NOT blockers, both = "make intent/coverage explicit"):
- **Gap 1 — WGSL scope widening** (`hlsl.meta.slang:11561`): matrix `[require]` went `glsl_hlsl_spirv` → `glsl_hlsl_metal_spirv_wgsl`, adding BOTH metal AND wgsl (vector added only metal). PR scoped Metal-only. Likely-correct consistency fix (matrix was last fwidth form w/o wgsl; routes through vector which already has `case wgsl:`) but UNVERIFIED (no `tests/wgsl/fwidth.slang` anywhere) + undocumented. **Maintainer (jkwak) call:** (a) keep wgsl, state in PR desc, add wgsl test [reviewers' rec] OR (b) narrow to `glsl_hlsl_metal_spirv`.
- **Gap 2 — test discrimination = FG002/FG102** (`tests/metal/fwidth.slang:19`): two identical `// METAL: fwidth(` checks are ordered substring matches; matrix decomposes to per-row vector fwidth so multiple `fwidth(` emit regardless → a vector-only regression passes. Fix: `// METAL-COUNT-2: fwidth(` or anchor to `mw` assignment. metallib is macOS-CI-gated so METAL text-check is sole cross-platform guard.
- Minor nit FG101: label the two checks vector-vs-matrix (cosmetic).
Reviewer did NOT auto-post (no marker — correct). Verdict RELAYED to fixer (one pass, both gaps + nit) to fold into jkwak's PR thread + follow-up commit; Gap 1 surfaced as maintainer question. Triager's reviewer-verdict hold condition now SATISFIED; still holds [Triage Resolution] until CI clears.

**07-23 14:32 — fixer actioned verdict; HEAD now `08d655c6f5`** (via `9bdcf85adc`+`08d655c6f5`, both TEST-ONLY — code fix in hlsl.meta.slang UNCHANGED, so correctness verdict still holds, NO reviewer re-run needed):
- **Gap 2 (test discrim) RESOLVED** — fixer's fix stronger than reviewer's COUNT-2 suggestion: two checks now distinct **call-site-pinned** tokens on **separate entry points** — vector `float4((fwidth(`, matrix `= fwidth_{{[0-9]+}}(` (leading `=` pins call not helper def). Proven mutually exclusive (vector-only emit: 0 `fwidth_N`; matrix-only: 0 `float4((fwidth(`). FG101 labeling nit also covered. NOTE: fixer's self-verified, not reviewer-re-verified (test-only delta, acceptable).
- **Gap 1 (WGSL scope) → jkwak** — question posted [PR #12172 comment 5059650563](https://github.com/shader-slang/slang/pull/12172#issuecomment-5059650563): confirmed vector added only metal (had wgsl), matrix added metal+wgsl, no tests/wgsl/fwidth.slang; offered keep+document+wgsl-test OR narrow to glsl_hlsl_metal_spirv. Fixer executes jkwak's pick.
- Real `pull_request` CI auto-triggered on `08d655c6f5` (non-draft, no manual dispatch — correct). Fixer owns loop.

**07-23 ~14:46 — jkwak ANSWERED Gap-1 + fixer executed + reviewer upgraded APPROVE. HEAD now `cdee313bb1`.**
- **jkwak-work replied** ([PR comment 5059718215](https://github.com/shader-slang/slang/pull/12172#issuecomment-5059718215), 14:37Z): *"Let's add WGSL test"* → chose KEEP-WGSL path (Suggestion (a)). NOT a fixer preempt — Main VERIFIED via github_get_pull_request_comments + WebFetch: question routed to jkwak → jkwak decided → fixer executed.
- **Fixer executed jkwak's pick:** kept WGSL, documented intent in PR body, added `tests/wgsl/fwidth.slang` with discriminating checks. Gap-2 test-discrim also done (fragVector `float4((fwidth(` / fragMatrix `= fwidth_{{[0-9]+}}(` on separate entry points, +METALLIB-DAG per entry).
- **Reviewer upgraded APPROVE_WITH_NITS → APPROVE** on code+tests @cdee313bb1 (verified current head, corrected fixer's timeline claim). CONTINGENT only on CI FileCheck run confirming new CHECK tokens — reviewer HONESTLY flagged it could NOT locally emit-check (stale Jul-18 pre-fix slangc, core module embedded, PR not on master); fixer also can't run Metal toolchain locally (metallib macOS-CI-gated). **CI is the sole validator for the CHECK tokens.** 0 bugs throughout; core fix always sound. Reviewer hop CLOSED (recovered from 07-21 teardown loss = Reviewer A budget-cap death + Devin timeout, explains 2-day gap; re-ran bumped budget, GC'd worktrees).

**07-23 14:57 — HEAD moved `cdee313bb1` → `5b1b39f7fe`** (fixer executed jkwak's WGSL choice). Fixer replied to jkwak [issue-comment 5059890978].
- Added `tests/wgsl/fwidth.slang` mirroring metal test (separate fragVector/fragMatrix entries, call-site tokens `vec4<f32>((fwidth(` and `= fwidth_<n>(`) + `-target wgsl-spirv-asm` + `// SPV: OpEntryPoint` directive → CI runs emitted WGSL through **Tint** (WGSL analog of metallib check). PR body documents the widening.
- Final PR state @`5b1b39f7fe`: stdlib fix + metal test (metal+metallib) + wgsl test (wgsl+wgsl-spirv-asm), non-draft OPEN. Real CI queued; format/labels/REUSE green.
- **Force-push note:** WGSL work went through fixer's own bot auto-commits (monitor/linter committed Tint directive, then reconciled via force-push over fixer's OWN prior bot commit). **CORRECTION (reviewer msg 64):** fixer's "tree identical" claim referred to the OVERWRITTEN bot commit, NOT the diff vs reviewer's approved `cdee313bb1`. Real delta cdee313bb1→5b1b39f7fe = **+4 lines in tests/wgsl/fwidth.slang** (2nd directive `//TEST:SIMPLE(filecheck=SPV): -target wgsl-spirv-asm` + `// SPV: OpEntryPoint` = Tint-acceptance smoke test, WGSL analog of metallib check, CI-gated). Benign/improvement — doesn't touch discriminating WGSL checks; code fix unchanged since ad29b27f. Lesson: fixer's force-push "tree identical" framing was imprecise (correct about the overwritten commit, misleading vs reviewed head) — reviewer caught it by checking the delta itself, not trusting the claim.

**Review-coverage boundary — reviewer RE-VERIFIED the final head @`5b1b39f7fe` (better than Main's no-re-dispatch call):** reviewer checked the +4-line delta before letting APPROVE carry, confirmed benign + that it CLOSES its own earlier CI-contingency caveat (Tint directive wires in downstream-compiler acceptance). APPROVE holds @5b1b39f7fe. Reviewer hop CLOSED.

**07-23 16:31 — CI GREEN ✅, gate cleared.** Real `pull_request` run **30017833832** + check-suite = SUCCESS on head `5b1b39f7fe`: all builds + slang/RHI test jobs across Linux/macOS/Windows, incl. macOS Metal jobs + Tint-validated WGSL path. Earlier `ci_failed` webhook was SPURIOUS (fired mid-run while suite `in_progress`, conclusion:null, zero failed jobs — test-falcor still running); fixer VERIFIED terminal conclusion before reacting (matches [Bot-PR workflow_dispatch cosmetic red] + verify-terminal-state lessons — fixer recorded its own version). PR state: OPEN non-draft, `mergeable: MERGEABLE`, `mergeState: BLOCKED` / `reviewDecision: REVIEW_REQUIRED` = expected hold awaiting required MAINTAINER (CODEOWNERS/jkwak) approval; fixer NOT flipping/merging (guardrails intact).

**Chain state: fix COMPLETE + CI GREEN + reviewer APPROVE. ONE gate remains = required maintainer review-approval + merge (human/operator-gated, jkwak).** Main cued triager to forward [Triage Resolution] upstream (review + CI both cleared). Complete deliverable @`5b1b39f7fe`: `hlsl.meta.slang` fix + `tests/metal/fwidth.slang` (metal+metallib, discriminating call-site tokens) + `tests/wgsl/fwidth.slang` (wgsl+wgsl-spirv-asm Tint). Fixer holds for maintainer merge; acts only on review verdict / substantive human comment / genuine future-push CI failure. NOTE: bot cannot merge (REVIEW_REQUIRED needs human maintainer); when jkwak merges → reap worktree per [always_reap_merged_worktrees].

**07-23 16:34 — [Triage Resolution] forwarded (triager → Main, top-of-chain rollup):** outcome FIXED, confirmed stdlib capability-annotation bug (NOT real Metal limitation); PR #12172 @5b1b39f7fe MERGEABLE `Closes #12165`; reviewer APPROVE 0 bugs; CI GREEN (47 SUCCESS / 2 SKIPPED / 0 non-green, incl macOS Metal + Tint WGSL). Issue verdict comment 5032598566 refreshed to "approve + CI green, awaiting maintainer merge". Triager will refresh to "merged" on merge (issue auto-closes via Closes #12165). **CHAIN PARKED — OPEN through merge** (per never-autonomously-close + reviewer-approval≠merge-auth). All 3 tiers aligned + holding; fixer owns merge watch. No Main action pending — top of chain, GitHub surface handled, not a blocker → no operator notification.

**07-23 16:43 — MAINTAINER APPROVED.** jkwak-work approved ("Looks good to me", 16:41Z); fixer VERIFIED approval `commit_id` = current head `5b1b39f7fe` (LIVE approval, not stranded on stale commit). `reviewDecision: APPROVED`, `mergeable: MERGEABLE`. Fixer guardrails engaged: no push (auto-dismisses approval), no self-merge (maintainer's call).

**07-23 18:23 — ✅ SHIPPED / TERMINAL.** PR #12172 **MERGED by jkwak-work at 18:21Z** (merge commit `ebed5178f0`); issue #12165 auto-closed COMPLETED via `Closes #12165`. Shipped: `hlsl.meta.slang` cap fix (vector+matrix `fwidth` gain `metal`, matrix +`wgsl`, vector body +`case metal:`) + `tests/metal/fwidth.slang` (metal+metallib) + `tests/wgsl/fwidth.slang` (wgsl+wgsl-spirv-asm Tint), discriminating call-site checks pinning vector vs matrix independently. Full CI green Linux/macOS/Windows. Fixer cleaned worktree `wt-slang-12165` + sentinel. **CHAIN CLOSED** — no manual issue-close needed (Closes-link handled it). Topic file persists for reference; full triage→fix→review→merge lifecycle, ~2.4 days (07-21 09:58 → 07-23 18:21) incl the 07-21→07-23 reviewer-teardown recovery gap.

**Routing note (07-21):** fixer's `send_message` to slang-reviewer failed on the marker-routing gate for a FRESH peer dispatch (no `in_reply_to`, no explicit `thread_id`) — NOT a missing edge (`ncl destinations list` confirms fixer↔reviewer bidirectional). Fix: fresh peer dispatch needs explicit `thread_id`. **Main dispatched slang-reviewer directly** for #12172 on canonical thread (COMMENT-state only, don't flip ready) — Main owns the reviewer-verdict RELAY hop now; fixer stood down (no re-dispatch → no double-review race). Fixer owns PR + real-CI webhook loop. Triager to post draft-PR pointer on issue (edit comment 5032598566) + hold [Triage Resolution] until review/CI clear. Also: my own earlier triager send failed because I used the `SendMessage` teammate tool instead of `mcp__nanoclaw__send_message`/`<message>` block — different tool, doesn't know nanoclaw destinations.
