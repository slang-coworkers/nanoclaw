---
name: project_12204_select_spvopt_passes
description: "#12204 select individual SpvOpt passes — enh/P3 PARKED jkwak Office-Yong design gate"
metadata: 
  node_type: memory
  type: project
  originSessionId: 300c1234-7825-4e49-b006-b14e7b6ec619
---

**shader-slang/slang#12204 — "Provide means to select SpvOpt"** — enhancement/P3, feature request. jkwak-work SELF-FILED + SELF-ASSIGNED; labels `Dev Opened` + `Office-Yong` (maintainer office-hours DESIGN gate). Milestone Q3 2026.

**Ask:** today `-OX` (0..3) is the only SPIR-V opt knob — each level is a hard-coded pass *preset* (`switch(optimizationLevel)` at `slang-glslang.cpp:316`, ~40 hand-unrolled `RegisterPass(CreateXxxPass())`). jkwak wants a new CLI arg to select *individual* SpvOpt passes. Related #11045 (slow `-g2 -O2` motivates granular control).

**Triaged @HEAD e438c5aef** (triager memo `triage-12204.md`). Clean primitive already exists: SPIRV-Tools `Optimizer::RegisterPassesFromFlags(vector<string>)` (`external/spirv-tools/include/spirv-tools/optimizer.hpp:140`) maps spirv-opt CLI flags → passes (SPIRV-Tools owns flag vocab, no hand-maintained table). Any pass-string needs a new versioned ABI struct `glslang_CompileRequest_1_3` (current `1_2` carries only `optimizationLevel`, `slang-glslang.h:105`, set at `slang-glslang-compiler.cpp:275`, parsed `slang-options.cpp:3589`).
- **Approach A (recommended):** new first-class flag (e.g. `-spirv-opt-passes <a,b,…>`, reuse `-capability a+b` split idiom) → `RegisterPassesFromFlags`.
- **Approach B:** wire existing `-Xspirv-opt <arg>` passthrough (already a registered target `SLANG_PASS_THROUGH_SPIRV_OPT`, but args don't reach `glslang_optimizeSPIRV`) — same ABI cost, UX call only.
- **Approach C REJECTED:** bulk presets (`RegisterSizePasses`/`RegisterPerformancePasses`) = more presets, not per-pass control.

**Open design Qs (owner/office-hours):** (1) A vs B surface/spelling; (2) is exposing spirv-opt flag vocab an acceptable semi-public commitment (tracks bundled SPIRV-Tools version); (3) interaction w/ `-OX` presets (override/additive/exclusive); (4) diagnostics for unknown flags.

**DESIGN RESOLVED by owner 07-23 (jkwak-work cmt 5061882883):** (1) surface = **Approach B** (`-Xspirv-opt` passthrough) — "sounds better"; (2) **NO diagnostic check** for the passthrough arg (consistent w/ `-Xdxc` etc.); (3) option is **ADDITIVE to `-OX`**. Answers all 4 open Qs. Design-map verdict was POSTED to GitHub (nv-slang-bot cmt 5061681364, 17:59Z).

**State: DRAFT PR #12206 OPEN 07-23** (https://github.com/shader-slang/slang/pull/12206, branch fix/issue-12204 @ 66b4d4e691). jkwak-work gave explicit "@nv-slang-bot, make a PR" (cmt 5061940269) after design gate cleared (triager ack cmt 5061921273).

**PR #12206 (Approach B exactly):** 5 files +212/−19. New versioned `glslang_CompileRequest_1_3` ABI struct carries `-Xspirv-opt` flags across slang-glslang C boundary → `spvtools::Optimizer::RegisterPassesFromFlags` ADDITIVE on top of `-OX` preset; forwarding gated on spirv-opt instance (so `-Xglslang` not misrouted); fail-on-partial-registration (no Slang validator). +test tests/spirv/spirv-opt-passthrough.slang. Repro 3/3 (BASE/STRIP/O0, mutation-proven), regr 530/530 spirv + 337/337 glsl. Manual: `--strip-debug` OpName 4→0, `-O0` inert, invalid flag→exit255. codex PLAN/CODE/OUTPUT APPROVE. Labels `pr: non-breaking` + `pr: new feature`. ci_failed webhook = benign priority-yield (0 real failures, retry-reruns).

**R1 review addressed 07-23:** 3-reviewer pass = APPROVE_WITH_NITS (0 bugs, 3 gaps none merge-blocking). Fixer re-pushed head **2aa7eb79de**: +4th test (invalid `-Xspirv-opt` flag fails compile), docs in docs/user-guide/08-compiling.md (pass-selection scope, `-O0`-inert), `-emit-spirv-via-glsl` limitation documented (Gap 2 = scoped follow-up), ABI prefix-compat invariant in comments, `_invoke` now diagnoses `-Xspirv-opt` vs pre-`_1_3` lib (was silent drop) + filled `getVersionString` `_1_2` branch. test 4/4, spirv 531/531, glsl 337/337, codex triple-approve. Reviewer re-run dispatched.

**R2 review addressed 07-23:** round-2 3-reviewer pass = APPROVE_WITH_NITS (0 bugs, 2 gaps, none blocking; down from 3 — doc+invalid-flag gaps resolved). Substantive item = `_invoke` pre-`_1_3` downgrade guard coupled fail-decision to `request.diagnosticFunc` non-null (latent-not-reachable today since compile() always sets it, but flagged unanimously by all 3 reviewers). Fixer re-pushed head **7dc9dec332**: guard now fails UNCONDITIONALLY w/ best-effort diagnostic — codex caught that a `SLANG_ASSERT` would compile to `__builtin_assume` in release and ELIDE the null-check, so removed it. +ABI invariants/size-clamp asymmetry in comments. Gap 2 (`-emit-spirv-via-glsl`) stays deferred+documented (reviewer+maintainer agreed). test 4/4, spirv 531/531, codex triple-approve. Round-3 reviewer re-run dispatched.

**REVIEW-COMPLETE / MERGE-READY 07-23, head 1e8d639942.** Arc R1(3 gaps)→R2(2 gaps)→R3(0 gaps, 1 optional nit taken) — **0 bugs throughout**, all rounds. R3 = APPROVE_WITH_NITS, Gap 1 confirmed resolved + UB-free, reviewer says merge-ready. Fixed across arc: invalid-flag test, `-Xspirv-opt` docs, ABI-invariant comments, triple-converged `_invoke` guard hardening (unconditional fail; dropped `SLANG_ASSERT`→`__builtin_assume`-in-release UB catch by codex, now a shared learning). Gap 2 (`-emit-spirv-via-glsl` inert) deferred+documented per reviewer+maintainer. Verify: test 4/4, spirv 531/531, glsl 337/337, codex PLAN/CODE/OUTPUT approve.

**MAINTAINER REVIEW 07-24, head 71aaf84802.** jkwak-work left 2 inline comments, both addressed+replied on-thread: (1) inheritance Q → refactored `struct glslang_CompileRequest_1_3 : public glslang_CompileRequest_1_2` (layout byte-identical, verified offsetof probe; trivially-copyable; `set()` base-slice assigns; `_invoke` downgrades slice to typed base; +`is_trivially_copyable`/`is_base_of` static_asserts); (2) `../compiler-core/` include → `compiler-core/` in slang-emit.cpp. Verify: test 4/4, spirv 531/531, glsl 337/337, codex PLAN/CODE/OUTPUT approve (OUTPUT_REVIEW caught wrong "aggregate init is gone" claim in reply text pre-post). Reviewer re-dispatched on new head.

**R5 addressed + NON-DRAFT 07-24, head 5d7aad1a08.** Round-5 3-reviewer pass = APPROVE_WITH_NITS, 0 bugs (5 rounds, 0 bugs throughout). ABI inheritance refactor empirically verified byte-identical (reviewer's probe + fixer's). R5 fixes: 2 factually-wrong comments corrected, `-O0`/old-lib guard scoped so `-O0 -Xspirv-opt` INERT (matches doc, preserves #11662), size-regression `static_assert` + field-lifetime doc, +2 tests (multi-flag accum, `-Xglslang` non-routing gate). Declined 3 cosmetic nits. **Rebased onto master** (was 5 behind). Gates: 6/6 test, spirv 533/533, glsl 337/337, codex triple-approve.

**GH state VERIFIED (get_pull_request 07-24):** PR #12206 `draft:false`, `Closes #12204`, base master, head fix/issue-12204, assignee jkwak-work, requested reviewers jkwak-work + pdeayton-nv, labels `pr: new feature`+`pr: non-breaking`, open/not-merged. Fixer attributes the non-draft flip to a MAINTAINER (fixer did NOT self-flip) — consistent w/ jkwak active review + pdeayton-nv added as reviewer; flip actor not directly readable from tools.

**test-falcor RED = INFRA** (fixer): fails in git checkout/submodule phase (no compile/test error), 2/5 recent MASTER runs also red, change only activates on `-Xspirv-opt` path Falcor doesn't exercise. NOT a real regression — don't mistake for one.

**R6 addressed 07-24, head d887a75714 (COMMENT-ONLY, no code/behavior change).** Round-6 = APPROVE_WITH_NITS, 0 code bugs, all R5 fixes verified sound. 2 comment-accuracy nits fixed: (1) XGLSLANG test comment relabeled to claim only the real property it proves (reviewer caught it doesn't exercise the instance gate; via-glsl alt also vacuous due to Gap 2); (2) dropped inaccurate "copies before returning" ABI-field lifetime comment. codex triple-approve, 6/6 tests. test-falcor NOT red on current head; reviewer concurred earlier red was infra.

**MAINTAINER ACTIVELY MERGING-PATH 07-24.** jkwak-work RESOLVED both his earlier review threads (`../` include on slang-emit.cpp + inheritance-refactor on slang-glslang.h — fixes satisfied). Then 1 new comment "Too verbose." → fixer head **05479d71ea** trimmed 7-line forwarding comment to 2 lines (kept load-bearing gate rationale: class backs both spirv-opt + glslang passthroughs so `-Xglslang` must not reach optimizer; dropped redundant via-glsl aside + lifetime note already on ABI field). Comment-only; 6/6 tests; codex OUTPUT approve. test-falcor infra-flake re-run cleared earlier.

**ALL 3 jkwak THREADS RESOLVED 07-26** (include, inheritance, verbose-comment — head 05479d71ea trim satisfied the last). GH state VERIFIED (get_pull_request 07-26): open, **NOT merged** (`merged_at:null`), non-draft, `Closes #12204`, assignee jkwak-work, **requested_reviewers now ONLY pdeayton-nv** (jkwak DROPPED off requested-reviewer list = his pass complete). labels new-feature+non-breaking.

**REBASED per jkwak request 07-27, head 68fac80cd7.** jkwak asked fixer to resolve rebase conflict. Conflict in `slang-emit.cpp` include block: master's #12216 ("Remove parent traversal from internal includes") de-`../`'d includes, colliding w/ this PR's added `slang-slice-allocator.h`. Resolved = took master's de-`../` form, kept include as `compiler-core/slang-slice-allocator.h`. Final diff vs master = exactly the 6 files (+299/−33), no `../` churn. Force-pushed, replied on PR. Clean build, `-Xspirv-opt` tests 6/6.

**SEPARATE finding RESOLVED — DISMISS (triager VERIFIED 07-27):** `tests/spirv/internal-spirv-asm-opname-prefix.slang` failure (emitted `OpName %dotResult`, expected `__dotResult`) = **STALE PREBUILT BINARY artifact, NOT a master regression. No issue filed.** Root cause: on-disk slangc built at g3649fb982 (07-23 03:41Z) predates the `__`-prefix fix ([[project_12099]] #12108→PR #12190, commit 72985f871, landed 07-24 04:31Z which added BOTH the `%dotResult`→`%__dotResult` rename in hlsl.meta.slang AND this test). Core module embedded into slangc at compile time → stale binary emits old un-prefixed name. Proven both directions: stale binary→fail; fresh rebuild @HEAD a4168d47c (w/ core-module header regen)→`%__dotResult`→PASS 1/1. Git: zero post-fix commits touch meta.slang/test; only spirv-emit touch = #12216 mechanical `../` refactor. Fixer's "not #12206" instinct correct; cause = binary provenance not broken master. **LESSON:** rebuild (`cmake -E touch hlsl.meta.slang` + `generate_core_module_headers`) before trusting tests/spirv/ results against a stale prebuilt. Memo: triage-opname-prefix-stale-binary.md.

**DESIGN REVERSAL by owner jkwak 07-27:** `-Xspirv-opt` should now RUN spirv-opt EVEN AT `-O0` (reversing the "inert at -O0" behavior 6 review rounds settled on). Owner's call. Fixer implementing: run optimizer when passthrough flags present regardless of `-O0` — updating `glslang_optimizeSPIRV` early-return, emit-path gate, `_invoke` guard, docs, O0 test. ⚠ GUARDRAIL flagged to fixer: PR's own notes said forcing-optimizer-on-at-`-O0` reverses deliberate fix #11662 (skips loading slang-glslang for single-module `-O0 -target spirv`); change MUST stay scoped to flags-present so plain `-O0` (no `-Xspirv-opt`) still skips slang-glslang, preserving #11662.

**-O0 REVERSAL IMPLEMENTED 07-27, head 9af1bd1b6a** (jkwak PR review cmt 3659075623). 3 coordinated gates; `-Xspirv-opt` now runs spirv-opt even under `-O0`, **#11662 PRESERVED exactly** (guardrail I flagged held): plain `-O0` still skips slang-glslang / keeps OpName + un-inlined call; `-O0 -Xspirv-opt --strip-debug` strips names but doesn't inline; `-O1 -Xspirv-opt` does both. Empirically verified fresh binary, slang-test 6/6. codex CODE+OUTPUT approve (must-fix on reply-accuracy caught: `CompactIdsPass` can still append, slang-glslang still loads for link/validate/debug-info). Fixer replied to jkwak on-thread (r3659353937) w/ interpretation I asked it to confirm: **`-O0 -Xspirv-opt` runs ONLY the passthrough passes** (no preset). PR body refreshed (test 4→6, `-O0` section rewritten). Reviewer dispatch routed THROUGH orch per fixer standing rule → orch relayed `[Fix Review Request]` to slang-reviewer on canonical thread.

**jkwak comment-trim round 07-27, head 6c1a980cd3 (COMMENT/DOCS-ONLY, no behavior change).** jkwak left 6 review comments: trim/remove verbose/self-evident code comments (×5) + add SPIRV-Tools `tools/opt/opt.cpp` link to docs (×1). Fixer applied all 6, replied on all 6 threads w/ SHA. codex CODE_REVIEW=approve (no executable code altered). Orch ruling: NO reviewer re-dispatch for comment-only (would burn ~$90 to re-verify text; debounce-on-churn) — in-flight 3-pass verdict on 9af1bd1b6a transfers to 6c1a980cd3 since no executable change.

**Reviewer 3-pass (on prior head 9af1bd1b6a, transfers to 6c1a980cd3):** independent traces CONFIRM #11662 preserved (the guardrail orch flagged): plain `-O0` → `needsDownstreamCompiler=false` → slang-glslang NOT loaded; only flags-present forces optimizer; no unconditional-load path. 3-gate consistency ✓ (emit-path + library early-return w/ new `case NONE: break;` preventing default-preset fall-through + `_invoke` guard, all key on flags-present). O0 test non-vacuous ✓ (+ pre-existing gh-11662.slang guards plain-`-O0`). Reviewer A R8: 0 bugs, 1 gap, 2 clarity notes, drift clean (A hit $30 cap after producing review → false "0 bytes"; recovered from stream, drift false-pos = 0 review-POST calls). Reviewer B (Devin) clean 0 bugs. Reviewer C (clarity) still running → combined verdict to parent on in_reply_to=24 edge.

**ROUND-8 COMBINED VERDICT 07-27 = APPROVE_WITH_NITS, 0 bugs, 1 gap, 0 questions** (reviewed 9af1bd1b6a, transfers to 6c1a980cd3 = comment-only delta; diff_hash ad0b4d896c7f). Memo combined-review-12206-round8.md. **#11662 CONFIRMED PRESERVED by all 3 reviewers + codex + reviewer's own trace** (emit gate `needsOptimization = optLevel!=None || spirvOptArgs.count!=0`; plain `-O0`→needsDownstreamCompiler=false→slang-glslang NOT loaded; no unconditional-load path). 3-gate consistency ✓ (emit-path + library early-return w/ new `case NONE: break;` + `_invoke` guard `flagCount!=0 && !m_compile_1_3` — `!=NONE` conjunct correctly removed since flags run at -O0 now). O0 test non-vacuous ✓.
- **1 GAP (non-blocking, = already-deferred Gap 2):** `-emit-spirv-via-glsl` non-forwarding contract has no test (Reviewer A). Previously deferred by reviewer+maintainer agreement; Rev A suggests adding a documented directive/comment even if it can't run headless in CI. NOT a new blocker.
- **Clarity nits (non-blocking):** C001 (`_1_3::set` "old caller" comment imprecise), C002/FG002/FG001 (bare `"spirv-opt"` literal vs `getPassThroughName` one-source-of-truth; `hasPassthroughFlags` re-derived inline; `_invoke` guard count-only vs ptr+count), FG003 (`-O0` comment "from loading at all" omits link/validate/debug load reasons), C003/C004/FG004/FG005 (comment precision). Devin (Rev B) 0 bugs; its "pre-1.3 lib fails even at -O0" info = INTENDED post-reversal behavior.

**NIT-PASS 07-27, head 6b128d9b46 (comment-only, codex approve).** Fixer folded in ONE nit — FG003 (narrowed `slang-emit.cpp` #11662 comment from "…from loading slang-glslang **at all**" → "…with no such flags, and no link/validation/separate-debug-info…", since `needsDownstreamCompiler` also true for link/validate/debug). Deliberately DEFERRED all rest (Gap 2 deferral stands; C001-C004/FG001/FG002/FG004/FG005 + strengthened `_invoke` memcpy note) — reason: jkwak just did a comment-REMOVAL round, adding comments would fight his edits; per over-iteration caution, one high-value precision fix to the exact line a maintainer reads to audit #11662.

**✅ APPROVED by maintainer jkwak-work 07-27** (verified live gh: `reviewDecision=APPROVED`, "Looks good to me", `commit_id=6b128d9b46`=`headRefOid` so approval covers current head; 0 unresolved threads). State: OPEN, non-draft, **`mergeStateStatus=BEHIND`** (branch behind base). Fixer correctly took NO action — a branch update/new commit would DISMISS the approval, so it will NOT update the branch; merge + BEHIND-resolution are OPERATOR-gated (merge queue handles BEHIND on merge, or maintainer updates+re-approves).

**CI red on approved head 6b128d9b46 = KNOWN FLAKE, not regression** (07-27). Failed `test-falcor / Test (Falcor)` (full 17m35s run, not priority-yield) on `renderpasses/test_GBufferRTTexGrads_d3d12` = **catalogued anchor [[project_12145_gbufferrttexgrads_d3d12_access_violation]]** (Mogwai 0xC0000005 crash, PR-code-independent across 16+ PRs, maintainer/jkwak fix-or-quarantine, babysitter-tracked). Corroborates fixer: change produces byte-identical Falcor codegen (Falcor passes no `-Xspirv-opt`); test-falcor NOT required check; SUCCESS on master. NOT stranding this PR (approved+mergeable, non-required check). No new babysitter routing (single occurrence ≠ material cost shift per anchor's standing threshold). Fixer: NO branch-update (dismisses approval), NO GH flake-note (noise on approved PR maintainer watches) — orch concurred.

**REBASED + AUTO-MERGE ENABLED 07-28, head 8c98e5bb2c.** Fixer rebased+pushed → BEHIND CLEARED. Reviewer verified (live gh): `mergeStateStatus=BLOCKED` (= required CI running, NOT review block), `mergeable=MERGEABLE`, `reviewDecision=APPROVED` — **jkwak ENABLED SQUASH AUTO-MERGE** → lands automatically on green CI. Reviewer re-verified R8 verdict transfers to 8c98e5bb2c: PR-own executable code BYTE-IDENTICAL to approved R8 (isolated via merge-base both heads, stripped comments); only diffs = (a) fixer comment-trims, (b) master churn rebase pulled in (#11476 autodiff, #12202 debug-info — verified autodiff hunk in slang-emit.cpp is from #11476/origin-master NOT this PR). #11662 gate byte-identical+intact; fixer 545/545 spirv-suite + empirical -O0/flags match static trace. **APPROVE_WITH_NITS/0 bugs STANDS.** Orch verified via get_pull_request: open, NOT merged, non-draft (reviewDecision/auto-merge fields not exposed by MCP tool → reviewer's live-gh account, corroborated by visible state).

**State: APPROVED + AUTO-MERGE ARMED (squash), BEHIND cleared, BLOCKED=CI-running — LANDS ON GREEN CI. NOT YET MERGED.** Feature cleared full review (6 rounds + design-reversal round 8, 0 bugs throughout, #11662 preserved). Only known CI risk = flake [[project_12145_gbufferrttexgrads_d3d12_access_violation]] (non-required, can't gate). Reviewer: closed once merges unless required check fails (will triage infra-vs-code if pinged). Internal review stays FILE-ONLY (shadow mode; jkwak driving directly = posting would be noise). NO operator push (auto-merge lands it; nothing operator-actionable). Bot must NOT branch-touch (dismisses approval); merge is automatic.

**Impl shape (captured for whoever implements):** forward parsed `-Xspirv-opt` DownstreamArgs into OPTIMIZE-SPIRV request (today only `optimizationLevel` reaches `glslang_optimizeSPIRV`) via new `glslang_CompileRequest_1_3` field → `Optimizer::RegisterPassesFromFlags(...)` ON TOP of preset passes (additive).

**✅ 2026-07-28 01:11 — BROUGHT CURRENT + AUTO-MERGE ARMED (fixer msg 70712).** Fixer merged `origin/master` (to `15863db482`) into `fix/issue-12204` — **clean, no conflicts; `BEHIND` CLEARED → `mergeable=MERGEABLE`**; rebuilt, re-verified, pushed head **`8c98e5bb2c`**. Master brought in #11476 (autodiff), #12202 (`-debug-info-include-source`), #12217 (cap-gen), #12228 (reflection-json) — all this PR's changes intact. **#11662 guardrail RE-VERIFIED intact post-merge** — static (`needsDownstreamCompiler` gate byte-identical; `shouldEmitSeparateDebugInfo` untouched; #12202 didn't touch `slang-emit.cpp`) + empirical (plain `-O0`→slang-glslang not loaded; `-O0 -Xspirv-opt --strip-debug` strips-not-inlines; `-O1` both; invalid flag fails). Tests: passthrough 6/6, full `tests/spirv/` 545/545. `reviewDecision=APPROVED` on current head; **jkwak ENABLED SQUASH AUTO-MERGE** → PR self-lands once required CI passes (`mergeStateStatus=BLOCKED` now reflects only in-progress required checks, not a real block). **Nothing further from fixer to land it** — a branch-update/push would dismiss the approval + disarm auto-merge; fixer correctly holds, watches for the MERGED webhook → reaps worktree. **Terminal-pending = auto-merge fires on CI-green.** (Note: maintainer-armed auto-merge is the maintainer's own merge action — NOT a bot merge; operator/merge-gate honored.)

**✅ 2026-07-28 03:56 — MERGED, CHAIN CLOSED (fixer msg 70738).** PR #12206 **squash-merged by jkwak-work** (auto-merge landed on green CI), merge commit `335d24689409` at 03:56Z; issue #12204 auto-closed COMPLETED via the `Fixes` link (maintainer's action). Delivered Approach B exactly — versioned `glslang_CompileRequest_1_3` ABI carrying `-Xspirv-opt` pass flags → `RegisterPassesFromFlags`, additive to `-OX`, runs spirv-opt at `-O0` when flags present (jkwak's reversal) while flag-less `-O0` still skips slang-glslang (#11662 preserved, re-verified statically + empirically at merged head). passthrough 6/6, `tests/spirv/` 545/545; reviewer independently re-verified `8c98e5bb2c` (R8 APPROVE_WITH_NITS, 0 bugs). Worktree `wt-slang-12204` reaped, sentinel cleared. **TERMINAL — no follow-up owed.** Arc: 6 review rounds + design-reversal round 8, 0 bugs throughout, #11662 preserved end-to-end.

Orthogonal: PR #12187/#11988 = slang-test `-OX` (test-harness level), see [[project_11988_nightly_spvopt_workflow_parked]].
