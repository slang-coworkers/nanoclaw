---
name: project_nanoclaw_pr874_webhook_route_approver
description: "slang-coworkers/nanoclaw#874 bot fix — webhook task string routes reviewable PRs to *-pr-approver not reviewer; reviewed inline clean+green, maintainer owns merge"
metadata:
  node_type: memory
  type: project
  originSessionId: pr874-webhook
---

**slang-coworkers/nanoclaw#874** — `fix(webhook): route reviewable PRs to *-pr-approver not reviewer`, branch `fix/haaggarwal/webhook-route-pr-approver` → `nv-main`. **Bot-authored** (nv-slang-bot), maintainer-namespaced branch (haaggarwal = Harsh Aggarwal = szihs, per [[project_nanoclaw_pr873_sync_nvmain]]). `pr_ready_for_review` webhook fired 2026-07-10.

**Handled inline by Main — NOT routed.** Same repo-class as [[project_nanoclaw_pr873_sync_nvmain]] / #864 / #868 / #871: platform-infra fork, not in the product-coworker routing map, no `nanoclaw-reviewer` wired. Do NOT route a NanoClaw-platform PR to compiler/SlangPy reviewers/approvers.

**Meta-irony:** this PR's own trigger webhook arrived with the STALE string it fixes (`"Route it to the reviewer coworker"`). The fix doesn't change how #874 itself is handled (fork PRs are inline, not routed) — it corrects routing for future PRODUCT `pr_ready_for_review` webhooks.

**Inline review verdict: clean, correct.** One-liner in `src/webhook-github.ts` `deliverGitHubPrReviewable` task string → `Route it to the project's *-pr-approver coworker (never a reviewer/fixer).` Aligns host with SKILL #852; matches real destinations (`slang-pr-approver`/`slangpy-pr-approver` exist; pr-approve workflow delegates review internally). This stale string was the causal reason shader-slang/slang#12033 mis-dispatched to slang-reviewer instead of slang-pr-approver.

**CI:** `check`✓ `ci`✓ `label`✓ all green; `mergeStateStatus: CLEAN`, `mergeable: MERGEABLE`, not draft, 0 reviews.

**Merge is the maintainer's** (haaggarwal-owned branch → `nv-main`). NOT auto-merged: my standing auto-merge grant ([[feedback_nv_coworkers_automerge]]) is `nv-coworkers`-scoped; `nv-main` tracks upstream and this is a maintainer-namespaced branch. No GitHub comment posted — nothing substantive to add to a clean, green, self-evident one-line maintainer fix (comment hygiene). On redelivery: state unchanged; do NOT route to product reviewers, do NOT re-review, do NOT auto-merge.

**#880 (2026-07-10)** — `docs(slang-pr-approve): offline batch launches each PR as its own self-thread session`, branch `fix/haaggarwal/slang-approve-offline-launch` → `nv-slang`, bot-authored, docs-only (+10/-2 in `container/workflows/slang-pr-approve/WORKFLOW.md`). `pr_ready_for_review` webhook. Same repo-class handling — reviewed inline (clean: clarifies underspecified offline-batch step to launch each manifest PR as an isolated per-thread `send_message(to:"self", thread_id:"gh-pr-<repo>-<pr>")` session + `ncl sessions list` verify; consistent with session model, flags companion config dep adding `self` dest row). NOT routed to product approvers, NOT commented (clean self-evident maintainer docs fix), merge is maintainer's. CI `label`✓ / `ci` pending at review time.

**#884 (2026-07-10)** — `docs(slang-pr-review): wire Devin exit-4 (transient launch failure) + refresh semantics`, branch `fix/nv-slang/pr-review-devin-exit4` → `nv-slang`, bot-authored, **docs-only** (+4/-3, single file `container/workflows/slang-pr-review/WORKFLOW.md`). More-substantial redo/superset of #882 (different branch namespace: `nv-slang` not `haaggarwal`); companion to merged **shader-slang/slang-skills#53** (fixed `devin-fetch.sh`: exit-4 Chrome-launch retry, Informational-findings capture, done-detection). `pr_ready_for_review` webhook — arrived with the correct post-#874 task string (`Route it to the project's *-pr-approver coworker (never a reviewer/fixer)`), but standing rule overrides: NanoClaw-platform fork PRs are reviewed inline, NOT routed to product approvers. Diff extends Reviewer-B exit-code legend (exit 4 = transient browser-launch-failure w/ explicit "NOT a deterministic environment failure" wording), updates skip-fallback line, adds two bullets (transient-retry semantics: script clears stale `/tmp/agent-browser-*` profile + retries once; anonymous-scrape reality: no login-gated manual re-run, Devin auto-re-analyzes PR head per commit, done-check polls it out). Internally self-consistent, matches companion skill fix. Same repo-class handling — reviewed inline CLEAN, NOT routed, NOT commented (self-evident maintainer docs fix), merge is maintainer's (`nv-slang` not `nv-coworkers` → outside auto-merge grant). CI `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery: no-op. **`synchronize` redelivery same day** = byte-identical re-push (same blob `190b97503e`, +4/-3 unchanged; rebase onto `nv-slang`, head `cee1085`), CI re-triggered — no content change, verdict stands, no-op.

**#882 (2026-07-10)** — `fix(slang-pr-review): treat Devin Chrome-launch failure (exit 4) as transient, self-heal + retry`, branch `fix/haaggarwal/slang-pr-review-devin-exit4` → `nv-slang`, bot-authored, **doc-only** (+4/-3, single file `container/workflows/slang-pr-review/WORKFLOW.md`). `pr_ready_for_review` webhook. Adds exit-4=browser-launch-failure to the Reviewer-B exit-code legend + skip message + two explanatory bullets (transient infra hiccup, script clears stale `/tmp/agent-browser-*` profile & retries once; Devin auto-re-analyzes PR head on new commits, no anon manual re-run). The actual profile-clear/retry logic lives in the `slang-pr-review-runner` LOCAL skill ("no upstream sync") — NOT committed to the fork, so code-search finds nothing; this PR is the doc catching up to that behavior. Diff internally self-consistent. Same repo-class handling — reviewed inline, NOT routed to product `*-pr-approver`, NOT commented (self-evident maintainer doc fix), merge is maintainer's. CI `label`✓ SUCCESS / `ci` IN_PROGRESS at review time; `MERGEABLE`, UNSTABLE (CI running), not draft, 0 reviews.

**#885 (2026-07-10)** — `fix(supervise-issues): nudge fixer-owned chains that go dark (bot-last ≠ handoff)`, branch `fix/nv-main/supervise-nudge-fixer-owned-chains` → `nv-main`, bot-authored, **code** (+126/-8 across 5 files: `scan.py` +39/-1, `pull-universe.sh` +21/-5, `test_scan.py` +49, `SKILL.md` +4/-1, `reference.md` +13/-1). Touches **my own `/supervise-issues` skill**. Root-cause fix for slang#12002 (fixer edited code, said "waiting on the build monitor", idle-exited under `--rm` so the in-container Monitor died, never woken because supervisor read bot-last as `awaiting_human` forever; supervisor ran 4 ticks, nudged 0). **Fix:** new `we_owe_next_step()` gate flips bot-last → `awaiting_us` + nudge fixer when ALL hold: no human-owned disposition token, no PR (no owed artifact), a fixer-role session owns the thread (`"fixer" in group_folder`), silent ≥ 60 min BY US. `pull-universe.sh` now joins `ncl groups list` → stamps each gh session's `group_folder` (best-effort, absent → `""` → non-fixer = current behavior, try/except-guarded). **Inline review verdict: clean, correct.** Verified at head `fb32ccc`: (a) `silent_age` is the by-us clock (`compute_last_activity_by_us`, NOT reset by human pokes — `test_human_pokes_do_not_reset_our_clock` still green → no #11594/#11613 regression); (b) `compute_ball` untouched, new branch fires only INSIDE `ball=="human"`; (c) `group_folder` plumbing coherent end-to-end (`sessions_by_id` preserves the stamped field, `we_owe_next_step` reads it); (d) all 3 guards proven by tests — PR-exists→parked, human-owned-disp→parked, triage-only(no-fixer)→parked, dark-fixer-no-PR→awaiting_us. Ran `test_scan.py` locally at head: **19/19 green** (15 existing + 4 new `NextStepOwnership`). Minor non-blocking: disposition match is substring (`tok in disp`) so `advisory`/`stood-down` are broad — fine, tokens are internally controlled; reference.md token list matches code tuple exactly. Same repo-class handling — NOT routed to product `*-pr-approver` (no nanoclaw approver wired; webhook carried generic post-#874 task string, standing rule overrides), NOT commented (clean self-evident maintainer fix, comment hygiene), merge is maintainer's (`nv-main` tracks upstream; nv-coworkers auto-merge grant doesn't cover it). CI `check`✓ `ci`✓ `label`✓ all pass; `MERGEABLE`, not draft, 0 reviews. Follow-up noted in PR body (separate, not this PR): fixer-spine rule — never park a turn on an in-container bg process/Monitor that can't survive `--rm` idle-exit. On redelivery: no-op.

**#886 (2026-07-10)** — `fix(supervise-issues): partial-tolerant PR enrichment (batched issueOrPullRequest + salvage)`, branch `fix/nv-main/pull-universe-partial-salvage` → `nv-main`, bot-authored, **code** (+216/-37, 4 files: `scripts/pull-universe.sh` +102/-37, new `scripts/test_pull_universe.py` +89, `scripts/test_scan.py` +14, `reference.md` +11). Touches **my own `/supervise-issues` skill** — direct root-cause fix for the tick-80 enrichment collapse ([[project_supervisor_pull_universe_enrichment_fail]], "1/147 PRs"). Two compounding causes fixed: (1) `gh_graphql()` now **salvages partial success** — `gh api graphql` returns HTTP 200 + valid `data` + `errors[]` but exits non-zero when a few aliased numbers miss; old code treated rc≠0 as total failure and threw the whole 50-batch away. Now returns parsed data whenever `data is not None`, only bails to REST when nothing usable. (2) Resolver switches `issue(number:)` → **`issueOrPullRequest(number:)`** (union, `__typename`-discriminated) — a chain keyed on a PR number 404'd the strict `issue()` type and *triggered* the (1) partial errors; PR arm now captures the chain's own PR as `self_pr → chain["pr"]`. Steps 4a/4b + final loop consume `self_pr` first (else `fix/issue-*` cross-ref), `fixes_issue` defaults to chain number when PR body omits `Fixes #`. Also closes the latent #885 false-positive (PR-keyed chain wrongly flipped `awaiting_us`). Null-node misses recover via single per-miss REST `gh issue view` (non-fatal, defaults OPEN). **Inline review verdict: clean, correct.** Ran both suites locally at head `66a3ad9`: **test_pull_universe.py 5/5** (clean-success / partial-salvage / total-failure→None / unparseable→None / data-null→None) + **test_scan.py 20/20** (incl. new `test_pr_keyed_chain_is_not_false_flipped` + prior `test_human_pokes_do_not_reset_our_clock` still green → no #885/#11594 regression) = 25/25 green, matches PR claim. `compute_ball` + by-us clock + #885 carve-out untouched (confirmed in diff). Same repo-class handling — NOT routed to product `*-pr-approver` (no nanoclaw approver wired; webhook carried generic post-#874 task string, standing rule overrides), NOT commented (clean self-evident maintainer fix, hygiene), merge is maintainer's (`nv-main` tracks upstream; nv-coworkers auto-merge grant doesn't cover it). CI `check`✓ `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery: no-op.

**#892 (2026-07-10)** — `Sync nv-dashboard with upstream/main`, branch `sync/upstream-nv-dashboard` → `nv-dashboard`, bot-authored (`sync-upstream.sh` automated periodic sync), **large merge** (+3195/-639, 92 files — identical sync payload to #889/#890/#891). `pr_ready_for_review` webhook (reason `opened`). **Fifth member** of the same-day upstream-sync batch (#888 nv-coworkers CLEAN / #889 nv-slang / #890 nv-slangpy / #891 nv-nanoclaw). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook carried generic post-#874 task string, standing rule overrides). **CI `ci` RED — same composed-state cause as #889/#891, verified in run 29125578545:** the `ci.yml` "Merge nv-* branches (test composed state)" step hits ONE modify/delete conflict OUTSIDE `is_owned()` — `##[error]nv-main merge produced conflicts outside nv-main's owned set: container/skills/slack-formatting/SKILL.md` (deleted in HEAD/upstream since formatting skills moved to the `channels` branch; still modified in origin/nv-main). All other merge-log conflicts (ci.yml/CLAUDE.md/claude.ts/agents.ts/pnpm-lock/init-first-agent/register/migrations/agent-groups/db-v2.test + add-dashboard/add-deltachat modify/delete) auto-resolve as owned; only slack-formatting trips the loud fail-by-design. Sync correctly imports the upstream deletion — divergence is nv-main-side. Resolution maintainer's: (a) delete from nv-main to match upstream, or (b) extend `is_owned()` for `container/skills/slack-formatting/*`. NOT commented — #891's diagnostic comment (issue-comment 4939734590) already documents the shared root cause for the whole batch; a duplicate per-PR comment on the identical failure is noise (comment hygiene), same as #889. `label`✓ pass; MERGEABLE, UNSTABLE, not draft, 0 reviews. NOT merged (maintainer's; `nv-dashboard` outside nv-coworkers auto-merge grant + red CI). On redelivery: no-op unless CI flips green or a substantive human comment lands.

**#891 (2026-07-10)** — `Sync nv-nanoclaw with upstream/main`, branch `sync/upstream-nv-nanoclaw` → `nv-nanoclaw`, bot-authored (`sync-upstream.sh` automated periodic sync), **large merge** (+3195/-639, 92 files). `pr_ready_for_review` webhook (reason `opened`). nv-nanoclaw-overlay sibling of #890 (nv-slangpy) / #873 (nv-main). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired). **CI `ci` RED — REAL merge conflict, not a flake, and NARROWER than #890:** the `ci.yml` "Merge nv-* branches" composed-state step hits a SINGLE modify/delete conflict OUTSIDE the owned set — `container/skills/slack-formatting/SKILL.md` (deleted in HEAD/upstream-sync since formatting skills moved to `channels` branch, still modified in origin/nv-main). `is_owned()` covers `container/skills/{spine-base,base}/*` but NOT `slack-formatting/*` → guard fails loudly by design. (Other merge-log conflicts — ci.yml/CLAUDE.md/claude.ts/router.ts/etc. — are auto-resolved as owned; only slack-formatting lands the `::error::…outside owned set`.) Not a defect in THIS PR's content — sync correctly imports the upstream deletion; divergence is on the `nv-main` side. **Resolution is maintainer's:** (a) delete the file from `nv-main` to match upstream, or (b) extend `is_owned()` to auto-resolve `container/skills/slack-formatting/*`. **DID post diagnostic comment** (unlike #890 broad-drift which I left silent) — single crisp file + two concrete fixes = signal not noise; via REST (`gh pr comment` GraphQL blocked "Resource not accessible by integration", REST `issues/891/comments` works — [[project_nv_slang_bot_readonly_incident]]) → issue-comment 4939734590. `label`✓ pass; MERGEABLE, UNSTABLE, not draft, 0 reviews. NOT merged (maintainer's; `nv-nanoclaw` outside nv-coworkers auto-merge grant + red CI). On redelivery: no-op unless CI flips green or a substantive human comment lands.

**#890 (2026-07-10)** — `Sync nv-slangpy with upstream/main`, branch `sync/upstream-nv-slangpy`-class → base `nv-slangpy`, bot-authored (nv-slang-bot), **automated periodic upstream sync** (+3195/-639, 92 files; generated by `sync-upstream.sh`, accumulates upstream/main merge commits into `nv-slangpy`). SlangPy-overlay sibling of the #873 `nv-main` sync. `pr_ready_for_review` webhook. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork, no nanoclaw approver wired; webhook carried generic post-#874 task string, standing rule overrides), NOT auto-merged (`nv-slangpy` outside nv-coworkers auto-merge grant), merge is maintainer's. **CI `ci`✗ FAILED** / `label`✓ SUCCESS; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. **Failure is NOT the sync content** — it's the "Merge nv-* branches (test the composed state, not standalone)" CI step: merging `nv-main` into this head produced conflicts OUTSIDE nv-main's auto-resolvable owned set (owned = package.json/lockfiles/.github/CLAUDE.md). Real content conflicts: `src/router.ts`, `src/types.ts`, `src/db/session-db.ts`, `src/db/migrations/index.ts`, `src/modules/permissions/{index,user-dm}.ts`, `container/agent-runner/src/providers/claude.ts`, `scripts/init-first-agent.ts`, `setup/register.ts` + modify/delete skill conflicts (add-dashboard/add-deltachat deleted in nv-main but modified in HEAD; slack-formatting deleted in HEAD, modified in nv-main). = overlay-branch drift between `nv-slangpy` and `nv-main`; maintainer integration/reconciliation task, related to [[project_upstream_sync_incident]]. NOT commented (automated sync PR, red CI is self-evident on the merge button, maintainer owns resolution — comment hygiene). On redelivery: no-op unless a substantive human comment lands.

**#889 (2026-07-10)** — `Sync nv-slang with upstream/main`, branch `sync/upstream-nv-slang` → `nv-slang`, bot-authored (nv-slang-bot, `sync-upstream.sh`), **automated periodic upstream sync** (+3195/-639, 92 files). `pr_ready_for_review` (reason `opened`). nv-slang-overlay member of the same 4-PR batch as #888 (nv-coworkers, CLEAN) / #890 (nv-slangpy) / #891 (nv-nanoclaw). **Batch significance for [[project_upstream_sync_incident]]:** cross-fork PR-creation RECOVERED — all four sync PRs opened normally (old `gh pr create` GraphQL block gone). Same repo-class handling — NOT routed to product `*-pr-approver` (no nanoclaw approver wired; webhook post-#874 task string overridden by standing rule), NOT auto-merged (`nv-slang` outside nv-coworkers grant + RED), merge + conflict-resolution are the maintainer's. **CI `ci` RED — same composed-state cause as #891, NOT this PR's content:** the `ci.yml` "Merge nv-* branches (test composed state)" step hits the single modify/delete conflict OUTSIDE `is_owned()` — `container/skills/slack-formatting/SKILL.md` (deleted in HEAD/upstream since formatting skills moved to the `channels` branch; still modified in origin/nv-main). Other merge-log conflicts (ci.yml/CLAUDE.md/claude.ts/router.ts/types.ts/session-db/migrations/permissions/init-first-agent/register) are auto-resolved as owned; only slack-formatting trips `::error::…outside owned set`. Sync correctly imports the upstream deletion — divergence is nv-main-side. Resolution maintainer's: (a) delete from nv-main to match upstream, or (b) extend `is_owned()` for `container/skills/slack-formatting/*`. NOT commented on #889 — the #891 diagnostic comment (issue-comment 4939734590) already documents the shared root cause for the whole batch; a duplicate per-PR comment on the identical failure would be noise (comment hygiene). `label`✓; MERGEABLE (into nv-slang), UNSTABLE, not draft, 0 reviews. On redelivery / remaining-sibling `pr_ready_for_review`: no-op unless CI flips green or a substantive human comment lands.

**#893 (2026-07-11)** — `Sync nv-main with upstream/main (a30547fb)`, branch `sync/upstream-nv-main` → `nv-main`, author **szihs** (Harsh Aggarwal, human/maintainer — not the bot this time), **large sync merge** (+3227/-580, 89 files). `pr_ready_for_review` webhook (reason `opened`). Next-day sibling of #873 (also nv-main sync) and the same-day #888–892 batch. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook carried the advisory post-#874 task string naming the approver, standing rule overrides). **CI ALL GREEN** — `check`✓ `ci`✓ `label`✓; crucially the `ci` composed-state ("Merge nv-* branches") check PASSES here, unlike the #889/#891/#892 siblings that tripped the `container/skills/slack-formatting/SKILL.md` modify/delete-outside-owned-set failure. That divergence was nv-main-side (upstream deleted the file when formatting skills moved to the `channels` branch; nv-main still had it modified) — THIS sync imports the upstream deletion, so nv-main now matches upstream and the conflict is resolved. `MERGEABLE`, UNSTABLE→now-green, not draft, 0 reviews. NOT commented (clean green self-evident maintainer sync — comment hygiene). NOT merged — maintainer's (szihs-owned; `nv-main` tracks upstream, outside nv-coworkers auto-merge grant). On redelivery: no-op unless a substantive human comment lands.

**#895 (2026-07-11) — KB-WIKI-FOLD SELF-MERGE CLASS** — `knowledge_base sync 2026-07-11: wiki-synth fold`, branch `kb-wiki-fold-20260711` → `nv-coworkers`, bot-authored (nv-slang-bot), daily `/learnings-wiki` synth+fold+link-fixup (coverage 1040/1040, 43 uncovered learnings folded into 13 concept pages, PII scrub). **Already MERGED+closed at webhook arrival** — `created_at 06:21:57` → `merged_at 06:22:05` (self-merged in 8s via REST, within [[feedback_nv_coworkers_automerge]] nv-coworkers grant). `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides: NanoClaw-platform fork, no nanoclaw approver wired, and PR is already terminal. **Pure no-op** — not routed, not reviewed, not commented. **CLASS RULE: daily `kb-wiki-fold-*` / `kb-sync-*` → nv-coworkers self-merge PRs are recognized no-ops on webhook** (auto-merged within grant before webhook lands); do NOT route/review/comment on redelivery or future daily instances.

**#906 (2026-07-12) — KB SELF-MERGE CLASS (see #895)** — `knowledge_base sync 2026-07-12`, branch `kb-sync-20260712` → `nv-coworkers`, bot-authored (nv-slang-bot), nightly knowledge_base snapshot of coworker memory + learnings (PII scrub: emails redacted, @handles + internal URLs retained per admin). **Already MERGED+closed at webhook arrival** — `created_at 03:01:31Z` → `merged_at 03:01:38Z` (self-merged in 7s via REST, within nv-coworkers auto-merge grant). `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired, PR already terminal). **Pure no-op** — not routed, not reviewed, not commented. On redelivery / future nightly `kb-sync-*` instances: no-op.

**#907 (2026-07-12) — KB-WIKI-FOLD SELF-MERGE CLASS (see #895)** — `knowledge_base sync 2026-07-12: wiki-synth fold`, branch `kb-wiki-fold-20260712` → `nv-coworkers`, bot-authored (nv-slang-bot), daily `/learnings-wiki` synth+fold+link-fixup. **Already MERGED+closed at webhook arrival** — `createdAt 06:20:08Z` → `mergedAt 06:20:14Z` (self-merged in 6s via REST, within [[feedback_nv_coworkers_automerge]] nv-coworkers grant). `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired, PR already terminal). **Pure no-op** — not routed, not reviewed, not commented. On redelivery / future daily `kb-wiki-fold-*` instances: no-op.

**#909 (2026-07-13) — KB SELF-MERGE CLASS (see #895/#906)** — `knowledge_base sync 2026-07-13`, branch `kb-sync-20260713` → `nv-coworkers`, bot-authored (nv-slang-bot), nightly knowledge_base snapshot (+1048/-188, 53 files). **Already MERGED at webhook arrival** — `createdAt 03:01:38Z` → `mergedAt 03:01:48Z` (self-merged in 10s within nv-coworkers auto-merge grant); `state MERGED`, terminal. `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired, PR already terminal). **Pure no-op** — not routed, not reviewed, not commented. On redelivery / future nightly `kb-sync-*` instances: no-op.

**#910 (2026-07-13) — KB-WIKI-FOLD SELF-MERGE CLASS (see #895/#907)** — `knowledge_base sync 2026-07-13: wiki-synth fold`, branch `kb-wiki-fold-20260713` → `nv-coworkers`, bot-authored (nv-slang-bot), daily `/learnings-wiki` synth+fold+link-fixup (coverage 1101/1101, +1899/-19, 109 files). **Already MERGED at webhook arrival** — `state MERGED`, terminal. `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired, PR already terminal). **Pure no-op** — not routed, not reviewed, not commented. On redelivery / future daily `kb-wiki-fold-*` instances: no-op.

**#931 (2026-07-14) — KB SELF-MERGE CLASS (see #895/#906/#909)** — `knowledge_base sync 2026-07-14`, branch `kb-sync-20260714` → `nv-coworkers`, bot-authored (nv-slang-bot), nightly knowledge_base snapshot (+6283/-4514, 136 files). **Already MERGED at webhook arrival** — `createdAt 03:02:05Z` → `mergedAt 03:02:18Z` (self-merged in 13s within nv-coworkers auto-merge grant); `state MERGED`, terminal. `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired, PR already terminal). **Pure no-op** — not routed, not reviewed, not commented. On redelivery / future nightly `kb-sync-*` instances: no-op.

**#932 (2026-07-14) — KB-WIKI-FOLD SELF-MERGE CLASS (see #895/#907/#910)** — `knowledge_base sync 2026-07-14: wiki-synth fold`, branch `kb-wiki-fold-20260714` → `nv-coworkers`, bot-authored (nv-slang-bot), daily `/learnings-wiki` synth+fold+link-fixup (coverage 1173/1173, +3217/-41, 220 files). Wiki-fold sibling of same-day #931 kb-sync. **Already MERGED at webhook arrival** — `state MERGED`, terminal. `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired, PR already terminal). **Pure no-op** — not routed, not reviewed, not commented. On redelivery / future daily `kb-wiki-fold-*` instances: no-op.

**#925 (2026-07-13) — CHANGELOG-NV DAILY-REFRESH SELF-MERGE CLASS (new; see #895 KB class)** — `docs(changelog-nv): 2026-07-13 daily refresh (9 PRs)`, branch `docs/changelog-nv-2026-07-13` → `nv-coworkers`, bot-authored (nv-slang-bot), **docs-only** (+20/-3, one file `CHANGELOG-NV.md`). Daily automated changelog refresh: bumps the per-branch merged-count table (nv-main 330→337, nv-slang 117→118, nv-slangpy 55→56) + adds a `## 📅 2026-07-13` section listing the 9 PRs merged today (nv-main: #911/#912/#915/#916/#917/#918/#924; nv-slang: #913; nv-slangpy: #914 — matches the set I reviewed inline today; 7+1+1=9 = title count, internally consistent). **Already MERGED at webhook arrival** — `createdAt 18:27:13Z` → `mergedAt 18:27:33Z` (self-merged in 20s via nv-coworkers auto-merge grant, same terminal-on-arrival pattern as the KB class); `state MERGED`, terminal. `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired, PR already terminal). **Pure no-op** — not routed, not reviewed, not commented. **CLASS RULE: daily `docs/changelog-nv-*` → nv-coworkers self-merge PRs are recognized no-ops on webhook** (auto-merged within grant before webhook lands, alongside the `kb-sync-*`/`kb-wiki-fold-*` classes); do NOT route/review/comment on redelivery or future daily instances.

**#896 (2026-07-11)** — `feat(slang-pr-approver): self-harvest posted bot reviews + run Devin, drop reviewer delegation`, branch `fix/nv-slang/approver-self-harvest` → `nv-slang`, author **szihs** (human/maintainer), **code** (+506/-661, 9 files). **Slang sibling of #897** (slangpy); `harvest-reviews.py` byte-identical between them, #897 recorded first. Same architectural pivot: slang-pr-approver (Verity) STOPS delegating review to `slang-reviewer` and builds its own input — (a) new `harvest-reviews.py` (+172) reuses the bot review production CI already posted (primary `github-actions[bot]` = claude-pr-review.yml/claude-code-action, byte-equivalent to old Reviewer A so its 🔴/🟡/🔵 + `Findings (N total)` table + `**Verdict**:` line + `diff sha256` footer feed the existing parser unchanged; secondary `coderabbitai[bot]`; skips own `nv-slang-bot[bot]` echoes), matched to pinned head by `.commit_id`; (b) runs Devin itself via `agent-browser` + `slang-pr-review-runner`'s `devin-fetch.sh`. Deletes `prepare-offline-rounds.py`/`score-decisions.py` (live-only now); drops `slang-reviewer` destination edge + `vars.reviewer`/`vars.approver` compose subs + reviewer's `approver:` var; removes historical/R0-pinning throughout. **Inline review verdict: clean, correct, internally consistent.** Verified same as #897: exit-code contract (0 fresh / 10 stale→Devin-only / 20 none→Devin-only / 21 fetch-error→ABSTAIN_INFRA — fetch-failure≠no-review, a hidden REQUEST_CHANGES behind a rate-limit stays safe) maps 1:1 through WORKFLOW.md Step 1b branches; `eval-clauses.py` `_result_block()` parses the workflow-stamped `_approver_result:true` block (untrusted harvested body may carry its own ```json fences → marker-preferred, last-block fallback, nested-brace-tolerant full-object parse); `commit_match` pivots diff_hash→`commit_id == pinned sha` (present-but-different=synthesis-error FAIL, absent=unevaluable), diff_hash kept secondary w/ `commit:<sha>` sentinel; `login_rank` min() returns newest-from-primary; `mode` enum reduced to `{live,live_late}`; no dangling reviewer/historical/R0 refs in added lines. "Absent bot reviews alone are not an abstain: decide from Devin" invariant coherent; fallback-tier uncertainty→ABSTAIN never rounds up. Deploy note in body: mount-refresh + operator `ncl destinations remove` for dead approver↔reviewer edges (both projects; operator task, not this PR). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook carried generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang-pr-approver at a nanoclaw-repo PR *about* the slang approver would be nonsensical), NOT commented (clean self-evident maintainer feature PR, hygiene), merge is maintainer's (`nv-slang` outside nv-coworkers auto-merge grant). CI `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#897 (2026-07-11)** — `feat(slangpy-pr-approver): self-harvest posted bot reviews + run Devin, drop reviewer delegation`, branch `fix/nv-slangpy/approver-self-harvest` → `nv-slangpy`, author **szihs** (human/maintainer), **code** (+500/-654, 9 files). SlangPy sibling of **#896** (slang, still OPEN at review time; `harvest-reviews.py` byte-identical between them). Architectural pivot: the slangpy-pr-approver STOPS delegating review to `slangpy-reviewer` and builds its own review input — (a) new `harvest-reviews.py` (+172) reuses the bot review production CI already posted (primary `github-actions[bot]` = claude-code-action; secondary `coderabbitai[bot]`, the only signal on slangpy; skips own `nv-slang-bot[bot]` echoes), matched to pinned head by `.commit_id`; (b) runs Devin itself via `agent-browser` + `slang-pr-review-runner`'s repo-agnostic `devin-fetch.sh`. Deletes `prepare-offline-rounds.py`/`score-decisions.py` (live-only now); drops the `slangpy-reviewer` destination edge + `vars.reviewer`/`vars.approver` compose-time subs from `coworker-types.yaml`; removes historical/R0-pinning throughout (live-only). **Inline review verdict: clean, correct, internally consistent.** Verified: exit-code contract (0 fresh-harvest / 10 stale→Devin-only / 20 none→Devin-only / 21 fetch-error→ABSTAIN_INFRA — a fetch failure is NOT treated as "no review", correctly, since a real REQUEST_CHANGES may hide behind it) maps 1:1 through WORKFLOW.md branches; `eval-clauses.py` `_result_block()` now parses the workflow-stamped `_approver_result:true` block (untrusted harvested body may carry its own ```json fences — marker-preferred, last-block fallback), nested-brace-tolerant full-object parse; `commit_match` clause pivots diff_hash→`commit_id == pinned sha` (present-but-different = synthesis-error FAIL, absent = unevaluable), diff_hash kept as secondary w/ `commit:<sha>` sentinel when footer lacked sha256; `commit_id` field flows end-to-end; login_rank min() correctly returns newest-from-primary-source; no dangling `slangpy-reviewer`/historical/R0 refs in added lines. "Absent bot reviews alone are not an abstain: decide from Devin" invariant coherent. Deploy note in body: mount-refresh + operator `ncl destinations remove` for dead approver↔reviewer edges (operator task, not this PR). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook carried generic post-#874 task string, standing rule overrides — a slangpy-pr-approver reviewing a nanoclaw-repo PR *about* the slangpy approver would be nonsensical), NOT commented (clean self-evident maintainer feature PR, hygiene), merge is maintainer's (`nv-slangpy` outside nv-coworkers auto-merge grant). CI `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#898 (2026-07-11)** — `docs(approval-ledger): drop stale offline-scoring references after approver went live-only`, branch `fix/nv-main/approver-ledger-stale-comments` → `nv-main`, author **szihs** (human/maintainer), **docs/comments + 1 test fixture** (+16/-17, 3 files). Host-side doc catch-up to the #896/#897 approver self-harvest pivot (which deleted `score-decisions.py`/`prepare-offline-rounds.py`, reduced `mode` enum to `{live,live_late}`, renamed the no-review reason code). No behavior change. Files: (a) `src/db/migrations/929-approval-decisions.ts` — comment-only: `mode` enum comment `historical | live | live_late` → `live | live_late`, `reason_code` example `REVIEW_DOC_MISSING` → `NO_REVIEW_SIGNAL`, `human_verdict` prose drops offline-manifest/`score-decisions.py` join path (keeps live-`pr_review`/terminal-event path); column DDL untouched (`mode TEXT NOT NULL`, comment-only enum, no CHECK). (b) `src/modules/approval-ledger/store.ts` — doc-comment reword: removes `score-decisions.py`/"offline manifest", `R0`/`R0..Rn` → "decision row / revision decisions". (c) `src/modules/approval-ledger/store.test.ts` — fixture `mode:'historical'`→`'live'` AND matching assertion `'historical'`→`'live'` in lockstep (free-form TEXT column, round-trip test still valid). **Inline review verdict: clean, correct, internally consistent** — no dangling `historical`/`score-decisions.py`/`R0` refs in added lines; PR body reports 9 ledger tests green. (Aside: host-side `mcp__nanoclaw__record_decision` tool schema still carries the old `mode`/`REVIEW_DOC_MISSING` strings — a separate host-tool description, NOT in this PR's scope.) Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook carried generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer docs fix, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant). CI `check`✓ `label`✓ pass / `ci` IN_PROGRESS at review time (nv-main composed-state should stay green post-#893 slack-formatting resolution); `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#899 (2026-07-11)** — `fix(slangpy-pr-approver): wait for a pending review bot instead of racing to Devin-only`, branch `fix/nv-slangpy/harvest-race-wait` → `nv-slangpy`, author **szihs** (human/maintainer), **code** (+80/-4, 2 files: `container/skills/slangpy-pr-approver/scripts/harvest-reviews.py`, `container/workflows/slangpy-pr-approve/WORKFLOW.md`). Follow-up to the #896/#897 self-harvest pivot. **Root-cause fix for slang#12064 `harvest_used=0`:** `harvest-reviews.py` returned exit 20 ("no bot review → Devin-only") when the primary review simply hadn't POSTED yet on a fresh PR, discarding the imminent primary signal. **Fix:** new `pending_review_bot(repo, commit)` checks BOTH review surfaces at the pinned head — commit-status contexts (CodeRabbit on slangpy, `state=pending`) and check-runs (Claude/review on slang, `queued`/`in_progress`); read-only, both lookups try/except-wrapped → returns `None` on failure so a lookup miss falls through to genuine-skip (exit 20) not hang. New **exit 22** = "review pending, timing race, NOT a skip" carrying `pending_bot` in `harvest.json`; exit 20 narrowed to genuine production-skips. WORKFLOW.md exit-22 branch: wait ~6 min / poll ~30s / re-harvest / take terminal code, Devin-only only if never settles. **Inline review verdict: clean, correct, internally consistent** — exit-22 fires only inside `if not cand:` so exit 21 (fetch-fail → ABSTAIN_INFRA) still precedes; `main()` exit-22 print added. Minor non-blocking: `PENDING_STATUS_RE=/coderabbit|claude|review/i` deliberately loose (rename-proof); a false-positive only costs a bounded ~6-min wait then correct Devin-only fallback, so exit-22 bias is right. Body verification (mock all paths pending→22 / skip→20 / match→0; live slangpy#1050 + bogus SHA → None) consistent. **Slang companion copy (nv-slang-owned) lands separately** (sibling pattern like #896/#897). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook carried generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing slangpy-pr-approver at a nanoclaw-repo PR *about* the slangpy approver would be nonsensical), NOT commented (clean self-evident maintainer fix, hygiene), merge is maintainer's (`nv-slangpy` outside nv-coworkers auto-merge grant). CI `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#900 (2026-07-11)** — `fix(slang-pr-approver): wait for a pending review bot instead of racing to Devin-only`, branch `fix/nv-slang/harvest-race-wait` → `nv-slang`, author **szihs** (human/maintainer), **code** (+80/-4, 2 files: `container/skills/slang-pr-approver/scripts/harvest-reviews.py`, `container/workflows/slang-pr-approve/WORKFLOW.md`). **This is the slang companion copy #899 foreshadowed** ("Slang companion copy (nv-slang-owned) lands separately") — `harvest-reviews.py` byte-identical to #899, workflow prose slang-specific. Root-cause fix for slang#12064 `harvest_used=0`: old code returned **exit 20** ("no bot review → Devin-only") whenever the primary review simply hadn't POSTED yet, racing a fresh-PR review still `queued`/`pending`, discarding the imminent primary signal. **Fix:** new `pending_review_bot(repo, commit)` checks BOTH surfaces at the pinned head — commit-status contexts (CodeRabbit on slangpy, `state=pending`) + check-runs (Claude/review on slang, `queued`/`in_progress`) — matched loosely by `PENDING_STATUS_RE = coderabbit|claude|review` (IGNORECASE, survives context rename). New **exit 22** = "review pending, timing race, NOT a skip" (`harvest.json.pending_bot` names it); **exit 20** now reserved for genuine production-skips (no review AND nothing running). WORKFLOW.md exit-22 branch: WAIT for `pending_bot` to settle (~6 min, poll ~30s), re-harvest, take whatever terminal code the re-run yields (0 primary / 10/20 Devin-only); Devin-only only if it never settles. **Inline review verdict: clean, correct, internally consistent** (verified at head `fix/nv-slang/harvest-race-wait` via `contents?ref=`): (a) `pending_review_bot()` called ONLY inside `if not cand:` → zero extra API cost on the fresh-match happy path, and exit 21 (fetch-fail→ABSTAIN_INFRA) still precedes it; (b) each surface wrapped in `try/except: pass`, read-only, never raises — lookup failure falls through to exit-20 genuine-skip rather than hanging; (c) exit 22 flows through `main()` print branch → `sys.exit(code)` correctly; (d) loose regex fails in the SAFE direction — a false positive costs a bounded ~6-min wait, never a false skip; (e) WORKFLOW.md branch matches code 1:1. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook carried generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang-pr-approver at a nanoclaw-repo PR *about* the slang approver would be nonsensical), NOT commented (clean self-evident maintainer fix, hygiene), merge is maintainer's (`nv-slang` outside nv-coworkers auto-merge grant). CI `label`✓ SUCCESS / `ci` IN_PROGRESS at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#901 (2026-07-11)** — `fix(supervise-issues): archive closed chains + never suppress a needs_nudge row`, branch `fix/nv-main/supervise-open-and-nudge` → `nv-main`, author **szihs** (human/maintainer), **code** (3 files: `SKILL.md` +11, `scan.py` +~33, `test_scan.py` +70/+4 cases). Touches **my own `/supervise-issues` skill** — root-cause fix for the shader-slang/slang#12059 miss (fixer killed at 3h ceiling mid-task, chain sat `awaiting_us` ~11h, tick nudged 0, no PR; manual wake → draft PR #12065). **Two gaps fixed:** (1) `scan.py run()` never read `issue_open` — a CLOSED-issue stub (`issue_open:false`, emitted by `pull-universe.sh`) still held a board row, counted `in_flight`, and (empty comments → `ball=none` → silence clock) could draw a nudge on a chain no human waits on. New guard at TOP of per-thread loop (before `classify()`): `if chain.get("issue_open") is False` → move to `state._archived` once (preserving `githubArtifactUrl` from prior snapshot), `next_state.pop(thread)`, `counts["closed"]+=1`, `continue`. Idempotent (`if thread not in archived_map` guards `archivedAt` re-stamp). New `summary.closed` count. (2) SKILL.md §3 new `[MUST]`: `needs_nudge=true` from `scan.py` is authoritative — send exactly one nudge, do NOT suppress by narrating "fixer dispatched"; `classify()` already sets `needs_nudge=false` for a `running` in-window container, so a chain still flagged at Step 3 has a DEAD/stalled container (the #12059 stall mistaken for progress). **Inline review verdict: clean, correct, internally consistent** — verified at head: guard placement before classify is right; `_archived` is `_`-prefixed so never caught by `startswith("gh-issue-")` key-math (`journaled_keys`/`archived_keys`/`new_keys`); idempotency confirmed by `test_archival_is_idempotent` (archivedAt preserved); 4 new `ClosedIssueArchival` tests cover archived-not-classified / never-nudged / open-alongside-closed-still-boards / idempotent; body claims full suite 24/24 (prior 20/20 #886 + 4). Minor non-blocking: shallow-copy `next_state["_archived"]` shares object with `prior_state["_archived"]` (harmless one-shot); `_archived` grows unbounded across ticks (small stubs, intended artifact-preservation). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook carried generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer fix, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant). CI `check`✓ `label`✓ pass / `ci` pending at review time (nv-main composed-state green post-#893 slack-formatting resolution); `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#904 (2026-07-12)** — `fix(supervise-issues): name-agnostic all-tier worktree GC discovery`, branch `fix/nv-main/gc-uniform-worktrees` → `nv-main`, author **nv-slang-bot[bot]**, **docs/prose only** (+46/-19, 2 files: `container/skills/supervise-issues/SKILL.md` +5/-3, `reference.md` +41/-16). Touches **my own `/supervise-issues` skill** — §8 Worktree-GC discovery-breadth fix. **Problem:** the reap-candidate scan used `du … prod-groups/*/wt-* | sort -rh`; only *fixer* worktrees follow `wt-slang-<n>`, but *reviewer* worktrees are freehand (`slang-11544-verify`, `slang-pr11511-r2`, `slang-clarity-*`) so the glob silently missed ~half of slang-reviewer's (6 of 13 on prod) → GB of reclaimable review-isolation dirs invisible/never reaped. Discovery-side complement to #903 (operator-only docker reclaim). **Fix:** (1) name-agnostic discovery `find prod-groups -mindepth 2 -maxdepth 3 -name .git -type f -not -path '*/.*/*'` → strip `/.git`, `du -sh`, sort; (2) issue-number = first ≥4-digit run in basename (`grep -oE '[0-9]{4,}' | head -1`), no-number → NO-PR wake-to-confirm; (3) owner routing = the tier folder the path lives under (`slang-reviewer/…`→reviewer, not always a fixer); (4) generalized dispatch template `wt-<slug>`/`/workspace/agent/slang`→`<dir>`/`<base-clone>`; added R8 `prunable`-is-not-a-reap-signal caveat. **Inline review verdict: clean, correct, internally consistent.** Verified the load-bearing git fact: a linked worktree's `.git` is a FILE (`gitdir:` pointer), a base clone's `.git` is a DIR → `-name .git -type f` finds exactly worktrees, never base checkouts; `-not -path '*/.*/*'` excludes the `.pr-worktrees` scratch dir; all four cited naming styles resolve via the ≥4-digit extractor; `-mindepth 2 -maxdepth 3` matches `prod-groups/<folder>/<wt>/.git` (depth 3) coherently; `prunable`-over-read-only-mount caveat is an accurate git nuance. No behavior code, no tests (prose-only). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; webhook `pr_ready_for_review` carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer docs fix, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant). CI `check`✓ `label`✓ pass / `ci` pending at review time (nv-main composed-state green post-#893 slack-formatting resolution); `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#905 (2026-07-12)** — `fix(slang-reviewer): uniform wt- worktree naming + trap on stop`, branch `fix/nv-slang/wt-uniform-naming` → `nv-slang`, author **nv-slang-bot[bot]**, **code+docs** (+8/-2, 2 files: `container/skills/slang-clarity-review-runner/scripts/run-clarity.sh`, `container/workflows/slang-pr-review/WORKFLOW.md`). **Direct creation-side sibling of #904** (which fixed the GC *discovery* breadth on `nv-main`) — this makes worktree *creation* follow the `wt-` convention so discovery stays uniform. Two real changes: (1) Reviewer C's isolation worktree `slang-clarity-${RUN_KEY}` → `wt-clarity-${RUN_KEY}` (matches the `wt-` convention #904's name-agnostic backstop still catches, but keeps discovery unambiguous); (2) `trap cleanup_worktree EXIT` → `trap cleanup_worktree EXIT INT TERM` — covers graceful container SIGTERM/SIGINT stop that plain `EXIT` misses, so a stopped review doesn't orphan its worktree (SIGKILL still untrappable → supervisor GC backstop, correctly noted). Plus one advisory WORKFLOW.md bullet telling reviewers to name ad-hoc worktrees `wt-<num>-<tag>`. **Inline review verdict: clean, correct, internally consistent.** `cleanup_worktree` is idempotent (`worktree remove --force … || rm -rf` + `prune`) so a double-fire (TERM handler resumes → EXIT also fires, since a bash TERM trap returning without `exit` doesn't auto-terminate) is harmless; the fix achieves its stated no-orphan goal regardless. Diff matches PR body (`bash -n` clean per body). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer fix, hygiene), merge is maintainer's (`nv-slang` outside nv-coworkers auto-merge grant). CI `label`✓ pass / `ci` pending at review time (nv-slang composed-state); `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#912 (2026-07-13)** — `feat(base): universal TodoWrite step-tracking + compaction survival`, branch `nv-base-todowrite-v2` → `nv-main`, author **szihs** (human/maintainer), **code+docs** (+8/-3, 3 files). Root-cause fix for workflow steps near the end of a chain (peer review, cleanup, report) silently dropping under context-thrash — they live only as on-demand workflow prose (a compaction summarizes them away) while the always-in-context spine survives; diagnosed from slang-fixer reaching `[Fix Report]` without dispatching `[Fix Review Request]` on thrashing sessions (matches my [[project_11917_fixer_compaction_loop]] class). **Three mutually-reinforcing edits:** (1) `container/workflows/base/WORKFLOW.md` — new **Step 0 "Track"**: seed a TodoWrite from the *concrete* workflow's steps (not the stub's), mark complete as you go; every workflow `extends: base` so all inherit it. (2) `container/spines/base/invariants/principles.md` — strengthens the plan-first invariant from a "3+ step tasks" advisory to a firm workflow-start requirement + adds the compaction-survival rationale (the list outlives a compaction where prose does not). (3) `container/agent-runner/src/compact-instructions.ts` (PreCompact hook) — new preserve-step 3 "if a TodoWrite checklist is active, preserve items + status (pending/in_progress/completed) verbatim"; old step 3→4. **Inline review verdict: clean, correct, internally consistent.** Verified full `compact-instructions.ts` at head: the numbered list is a plain `instructions.join('\n')` output STRING — the "3."/"4." are cosmetic prose, nothing parses them, so the renumber cannot regress; new step 3 sits coherently between "preserve chronological sequence" (2) and the verbatim destinations reminder (now 4). No behavior code in the host — soft/mandatory tier only (PR body: "No hard gate — codex CODE_REVIEW stays the hard-gated independent check; peer review is complementary and now always-visible rather than gate-blocked"). Pairs with a slang-side invariant PR (nv-slang). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a compiler/slangpy approver at a base-spine infra PR is nonsensical), NOT commented (clean self-evident maintainer feature PR, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant; author szihs is the maintainer). CI `check`✓ `label`✓ pass / `ci` IN_PROGRESS at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#913 (2026-07-13)** — `feat(slang-fixer): peer-review-before-report as a spine invariant`, branch `nv-slang-peer-review-clean` → `nv-slang`, author **szihs** (human/maintainer), **code** (+11/-0, 2 files: `container/spines/slang/coworker-types.yaml` +4, new `container/spines/slang/invariants/peer-review.md` +7). **This is the slang-side sibling #912's body foreshadowed** ("Pairs with a slang-side invariant PR (nv-slang)"). Root-cause fix for **slang-fixer reaching `[Fix Report]` without dispatching `[Fix Review Request]` to slang-reviewer** — peer review lived as conditional workflow-step prose (slang-fix-issue Step 8, near end of chain) that eroded under context-thrash and got silently skipped (slangpy-fixer, not thrashing, completed it fine → context-pressure erosion, not config bug; matches [[project_11917_fixer_compaction_loop]]). **Fix:** promote the rule from a skippable workflow step to an always-in-context spine invariant so it survives compaction. New `peer-review.md`: dispatch `[Fix Review Request]`→slang-reviewer (when in destinations) BEFORE `[Fix Report]`; **complements, NOT substitutes** the hard-gated `CODE_REVIEW` codex critique (codex reads the diff; reviewer builds it + Devin + clarity); only legit skips = A/B-test mode or reviewer-not-in-destinations (patch mode is NOT a skip); after dispatch end-turn, handle reply per REQUEST_CHANGES/max-2-rounds. Added to slang-fixer's `invariants:` list (extends slang-writer). **Explicitly separate from #896** (which intentionally dropped the *approver→reviewer* edge — this keeps the *fixer→reviewer* edge firing). **Inline review verdict: clean, correct, internally consistent.** Verified both load-bearing claims at head: (1) `invariants:` is an established field in this exact YAML (slang-common/slang-writer/slang-maintainer/slang-reviewer/slang-pr-approver all declare it) and union-across-`extends` is already load-bearing (slang-reviewer extends slang-common, declares only code-changes+review-output, inherits abi-stability from slang-common) → slang-fixer's new block is additive to the inherited slang-writer/slang-common chain exactly as claimed; (2) peer-review.md is well-scoped (PR/patch-only), complement-not-substitute wording coherent, skip-list correct, end-turn/reply-handling matches workflow. Mandatory-but-soft (no hard gate; codex CODE_REVIEW stays the hard gate). Body notes symmetric slangpy-fixer→nv-slangpy change as natural follow-up. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang-pr-approver at a nanoclaw-repo PR *about* the slang fixer's review invariant would be nonsensical), NOT commented (clean self-evident maintainer feature PR, hygiene), merge is maintainer's (`nv-slang` outside nv-coworkers auto-merge grant; szihs-owned). CI `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#914 (2026-07-13)** — `feat(slangpy-fixer): peer-review-before-report as a spine invariant`, branch `nv-slangpy-peer-review` → `nv-slangpy`, author **szihs** (human/maintainer), **code** (+11/-0, 2 files: `container/spines/slangpy/coworker-types.yaml` +4, new `container/spines/slangpy/invariants/peer-review.md` +7). **This is the symmetric slangpy sibling #913's body foreshadowed** ("symmetric slangpy-fixer→nv-slangpy change as natural follow-up") — third of the trio (#912 nv-main base TodoWrite + #913 nv-slang + this #914 nv-slangpy). Byte-mirror of #913's slang change, retargeted to slangpy: promotes peer-review-before-report from an on-demand workflow step (`slangpy-implement` "Peer review", near chain end, erodes under compaction) to an **always-in-context spine invariant** that survives compaction. Per PR body, slangpy-fixer is NOT currently failing this — keeping it identical to slang-fixer is the point (divergence is what let the slang side erode silently while slangpy didn't; matches [[project_11917_fixer_compaction_loop]]). New `peer-review.md`: dispatch `[Fix Review Request]`→slangpy-reviewer (when in destinations) BEFORE `[Fix Report]`; **complements NOT substitutes** the hard-gated `CODE_REVIEW` codex critique (codex reads the diff; reviewer builds it + Devin + clarity); only legit skips = A/B-test mode or reviewer-not-in-destinations (patch mode is NOT a skip, state reason when skipping); after dispatch end-turn, handle reply per REQUEST_CHANGES/max-2-rounds. Added to slangpy-fixer's `invariants:` list (extends slangpy-writer). **Inline review verdict: clean, correct, internally consistent.** Verified both load-bearing claims at head `nv-slangpy-peer-review`: (1) `invariants:` is an established field in this exact YAML (slangpy-common declares `[]`, slangpy-writer declares code-changes.md, slangpy-reviewer declares code-changes.md+review-output.md) and union-across-`extends` is load-bearing → slangpy-fixer's new block is additive to the inherited slangpy-writer/slangpy-common chain exactly as claimed; (2) peer-review.md well-scoped (PR/patch-only, slangpy-fixer only), complement-not-substitute wording coherent, skip-list correct, end-turn/reply-handling matches workflow. Mandatory-but-soft (no hard gate; codex CODE_REVIEW stays the hard gate). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slangpy-pr-approver at a nanoclaw-repo PR *about* the slangpy fixer's review invariant would be nonsensical), NOT commented (clean self-evident maintainer feature PR, hygiene), merge is maintainer's (`nv-slangpy` outside nv-coworkers auto-merge grant; szihs-owned). CI `label`✓ pass / `ci` pending at review time (nv-slangpy composed-state); `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#915 (2026-07-13)** — `feat(supervise-issues): critical-pressure tier — reclaim idle KEEP builds at ENOSPC risk`, branch `nv-gc-critical-tier` → `nv-main`, author **szihs** (human/maintainer), **code+docs** (+83/-5, 3 files: `scripts/worktree-gc.py`, `scripts/test_worktree_gc.py`, `reference.md`). Touches **my own `/supervise-issues` skill** — live-emergency fix for the 2026-07-13 12:10 tick where `/ephemeral` hit 754M free but `worktree-gc.py` reclaimed 0 GB: every 7 GB build was `KEEP` (open PRs touched 1–3d ago, correctly classified), so the STALE-OPEN tier (idle >14d) had no lever. **Fix:** new **CRITICAL tier** — below `CRITICAL_GATE_GB=5` (`summary.critical:true`, ENOSPC-imminent) the build-reclaim pool widens to also include **idle KEEP builds** (open PR touched within 14d but idle > `CRITICAL_IDLE_DAYS=2` AND no running session). Still **build-only** (`rm -rf <dir>/build`) — worktree/branch/PR/uncommitted work survive, cmake rebuilds on resume. Routine 5–25 GB pressure unchanged (STALE-OPEN only). **Inline review verdict: clean, correct, internally consistent.** Verified merged tree at `12b0606b`: (a) `running = set(payload["running_dirs"])` defined; `critical` branch nests inside `under_pressure` (5<25 so always reachable when critical); (b) idle-KEEP filter triple-guards `has_build ∧ dir not in running ∧ (pr_idle_days or 0) > 2`; (c) None-safe sort `pr_idle_days or 0` (matches #886/#901 fixes) → most-idle-first regardless of tier; (d) `classify()` untouched, KEEP still = running OR (OPEN/OPEN ∧ idle ≤14) — the critical tier reclaims a *subset* of KEEP without reclassifying. Ran suite locally at merged tree: **20/20 green** (incl. 4 new `TestCriticalTier`: routine-spares-KEEP / critical-reclaims-idle-KEEP / running-KEEP-never-reclaimed / idle-floor-respected / stale-then-idle-oldest-first). Minor cosmetic: PR body says "+5 new tests", actual = 4 new methods + 2 threshold asserts folded into existing test — non-blocking. **ALREADY MERGED at review time** — `mergedAt 12:52:02Z` by szihs, `state MERGED`, terminal; branch deleted post-merge (read via merge-commit SHA). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer fix, already terminal — hygiene), merge was maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant; szihs-owned). On redelivery: pure no-op.

**#916 (2026-07-13)** — `fix(agent-runner): skip git worktrees in additionalDirectories (autocompact thrash)`, branch `nv-fix-additionaldirs-worktree-dup` → `nv-main`, author **szihs** (human/maintainer), **code** (+61/-22, 2 files: `container/agent-runner/src/index.ts`, new `container/agent-runner/src/additional-dirs.test.ts`). **THE ROOT-CAUSE FIX for the fixer-autocompact-thrash class** ([[project_11917_fixer_compaction_loop]]) — #913's peer-review-invariant only mitigated the symptom (Step-8 skips); THIS removes the cause. **Root cause:** `index.ts` added EVERY `/workspace/agent/*` subdir with a `.claude/` to the SDK's `additionalDirectories`; the SDK loads each dir's `.claude/agents/` + `CLAUDE.md`. A writer tier accumulates many git *worktrees* of the same repo (prod slang-fixer: 54 `wt-slang-*`), each carrying upstream Slang's identical `.claude/` (5 reviewer subagents + 33.7KB CLAUDE.md) → repo agents registered ~54× + CLAUDE.md injected ~54×, re-sent every turn as ~69KB `agent_listing_delta`+`nested_memory` that survives compaction → window refills in ~3 turns → thrash. slangpy-fixer (fewer worktrees, smaller CLAUDE.md) didn't thrash on identical config → confirms it's worktree COUNT not config. **Fix:** include a repo's PRIMARY checkout, skip its linked worktrees — a worktree's `.git` is a FILE (gitdir pointer), a primary clone's `.git` is a DIR (same load-bearing git fact as #904/#905). Extracted pure exported `discoverAdditionalDirectories(bases, cwd)`; guarded `main()` with `import.meta.main` so the helper imports without booting the poll loop. Prod effect: slang-fixer additional-dirs 57→3, agent registrations ~270→~5. **Inline review verdict: clean, correct, internally consistent.** Verified the diff: (a) worktree-skip applies ONLY to CWD subdirs (`base === cwd`) that already passed the `.claude/` gate — non-CWD `/workspace/extra` subdirs still included unconditionally (behavior-preserving); (b) `.git` stat wrapped try/except → can't-stat falls through and INCLUDES the dir (safe: a real clone with an odd `.git` isn't wrongly dropped); (c) `import.meta.main` is the correct Bun idiom — container runs `exec bun …src/index.ts` as entrypoint per CLAUDE.md → true there, importable-without-boot in tests; (d) 5 tests cover the real 1-clone+54-worktree→collapses-to-clone, plain `.claude/`-no-`.git` included, no-`.claude/` excluded, non-CWD base includes all. PR body: `bun test` 275 pass / 0 fail. **Deploy note in body (operator task, NOT this PR):** agent-runner src reaches containers via the copy-once per-group overlay (`data/v2-sessions/<gid>/agent-runner-src/`, seeded only when absent — `group-init.ts`), NOT auto-refreshed like the skill mirror → landing needs overlay re-sync into existing groups + container respawn. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer fix, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant; szihs-owned). CI `check`✓ `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#917 (2026-07-13)** — `refactor(agent-runner): precise linked-worktree predicate + side-effect-free helper`, branch `nv-additionaldirs-worktree-predicate` → `nv-main`, author **szihs** (human/maintainer), **code** (+ new file; `bun test` 279 pass / 9 new). **Direct follow-up to #916 addressing review feedback** — refines the autocompact-thrash root-cause fix ([[project_11917_fixer_compaction_loop]]). **Two changes:** (1) **Correctness — #916's predicate was too broad.** #916 skipped any CWD subdir whose `.git` is a FILE, but `.git`-is-a-file ALSO matches **submodules** (`gitdir: ../.git/modules/<name>`) and **`--separate-git-dir`** clones — both legitimate distinct checkouts that *should* contribute their `.claude/` skills; #916 wrongly excluded them. New `isLinkedGitWorktree(dir)` (in new `container/agent-runner/src/additional-directories.ts`) reads the `.git` pointer and skips **only** when `gitdir:` contains the `.git/worktrees/` admin segment — the exact `git worktree add` signature (regex `/(^|\/)\.git\/worktrees\//` on separator-normalized gitdir). Submodules + separate-git-dir fall through → kept. (2) **Testability** — discovery extracted from the runtime entrypoint into side-effect-free `additional-directories.ts`; `index.ts` imports `discoverAdditionalDirectories(['/workspace/extra', CWD], CWD)` and drops its now-unused `fs` import; `import.meta.main` guard retained (defense in depth). **Inline review verdict: clean, correct, internally consistent.** Verified full helper + tests + index.ts at head `nv-additionaldirs-worktree-predicate`: (a) predicate is precise — worktree→skip, submodule (`.git/modules/`)→keep, separate-git-dir (arbitrary path)→keep, primary clone (`.git` dir)→keep, no `.git`→keep; (b) **every uncertainty/failure path errs toward INCLUDING** — `statSync` throw→false, `!isFile()`→false, unreadable `readFileSync`→false, no `gitdir:` match→false (same safe-direction property as #916); (c) worktree-skip + `.claude/` gate apply ONLY inside `if (base === cwd)` — non-CWD `/workspace/extra` subdirs still included unconditionally (behavior-preserving from #916); (d) `index.ts` confirmed to use only `path`/`url`, no `fs`; (e) 9 tests match behavior 1:1 incl. real 1-clone+54-worktrees→collapses-to-clone and submodule-with-`.claude/`→kept. Deploy note in body (operator task, NOT this PR): same copy-once overlay path as #916 but adds a NEW file → overlay sync must include `additional-directories.ts` (not just overwrite `index.ts`) into the 13 group overlays + container cycle; ref review-note #5 (stale copy-once overlay is its own bug) filed as a mental follow-up (versioned/mtime `agent-runner-src` refresh). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer refactor, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant; szihs-owned). CI `check`✓ `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#918 (2026-07-13)** — `feat(agent-runner): refresh primary clones at boot (ff-only + submodules)`, branch `nv-boot-fetch-primary-clones` → `nv-main`, author **szihs** (human/maintainer), **code** (+170/-0, 3 files: `container/agent-runner/src/index.ts`, new `refresh-clones.ts` +111, new `refresh-clones.test.ts` +51). **Direct follow-on to #916/#917** (autocompact-thrash worktree-dedup) — now that the SDK loads `.claude/agents/`+`CLAUDE.md` from the PRIMARY clone only (worktrees skipped), a STALE primary clone means outdated agent defs/project memory + worktrees branched off a base behind `origin` (observed: prod slang-fixer's slang-rhi clone 34 days stale). **Fix:** `refreshPrimaryClones()` at container boot iterates the discovered primary clones: `git pull --ff-only --quiet` (NOT bare fetch — a plain fetch only advances remote-tracking refs, leaving the working-tree files the SDK reads stale; NOT `reset --hard` — would clobber base state; `--ff-only` fetches AND fast-forwards the tree, no-op on diverged/dirty base) → `git submodule update --init --recursive` (slang/slang-rhi carry submodules). Guards: `shouldRefresh` 30-min TTL via FETCH_HEAD mtime (a group-restart boots 15+ containers sharing ONE clone — first refreshes, rest no-op, no remote-storm); best-effort try/catch (offline/token/non-FF/detached-HEAD logged not fatal); bounded timeouts (120s pull / 300s submodule). Pure helpers `shouldRefresh`/`isPrimaryClone`/`FETCH_TTL_MS` exported for tests. **Inline review verdict: clean, correct, internally consistent.** Verified: (a) `isPrimaryClone` — `.git` dir→true, worktree `.git`-file→false, absent→false (same git fact as #904/#905/#916/#917); (b) `shouldRefresh` null→true / within-TTL→false / older→true / `>=` boundary→true, tests 1:1; (c) `--ff-only` genuinely no-ops on diverged/dirty (non-zero exit→caught), never merges/clobbers; (d) worktree-safe (FF of base branch can't touch independent worktree checkouts; git forbids same branch in two checkouts); (e) best-effort try/catch + bounded timeouts → boot never blocks; body: `bun test` 286 pass / 7 new. **ONE non-blocking cosmetic NIT:** the call-site comment in `index.ts` reads `FETCH-ONLY (never pull/checkout …)` — STALE wording contradicting the actual `git pull --ff-only` and the PR body's own (correct) pull-over-fetch argument (per body, the fetch→pull change came from review feedback; the call-site comment wasn't updated). Code behavior is right; only the leftover comment misleads. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer feature PR, green, hygiene — same as #912–#917; the stale-comment nit is cosmetic, maintainer-owned, not worth a nag on their own green PR), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant; szihs-owned). Deploy note in body (operator task, NOT this PR): same copy-once overlay path as #916/#917 — sync `refresh-clones.ts`/`refresh-clones.test.ts`/`index.ts` into the 13 group overlays + container respawn. CI `check`✓ `ci`✓ `label`✓ ALL GREEN; `MERGEABLE`, `mergeStateStatus: CLEAN`, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#919 (2026-07-13)** — `fix(channels): restore dropped dashboard barrel self-registration import`, branch `fix/restore-dashboard-barrel-import` → `nv-main`, author **nv-slang-bot[bot]**, **code** (+3/-0, one file: `src/channels/index.ts`). Restores `// dashboard` + `import './dashboard.js';` to the channel self-registration barrel. **Root cause:** the #893 nv-main sync merge `63c14a86` ("Sync nv-main with upstream/main (a30547fb)", 2026-07-11) dropped the two lines during conflict resolution — kept upstream's barrel (never had the line); parent `952f1e2c` carried it. Symptom: prod delivery logged `No adapter for channel type channelType="dashboard"`, dashboard-bound messages never marked delivered. **Inline review verdict: clean, correct, internally consistent.** Verified all load-bearing claims at PR head: (a) `dashboard.ts` self-registers — `registerChannelAdapter('dashboard', …)` at line 13 on `nv-dashboard`; only the barrel import was lost, the adapter module is intact; (b) `dashboard.ts` is ABSENT from `nv-main` (404) → the restored import is **dangling-by-design** on nv-main and resolves only in the COMPOSED state; (c) `ci.yml` "Merge nv-* branches" step gated `if: github.base_ref != 'main'` merges `nv-dashboard` (in its `for branch in nv-main nv-dashboard nv-slang nv-slangpy nv-nanoclaw` loop) BEFORE build/test → the import resolves in CI, no standalone `tsc` failure introduced; nv-main is never built standalone in the normal nv-*→nv-* path. Diff is exactly the two lines + surrounding blank, additive, one file. PR body's own follow-up note (correct, NOT in scope): restoring the line doesn't stop the next `sync-upstream.sh` from re-dropping it (integrity-check restores from nv-main, the branch that lost it) — a durable guard (composed-CI assertion that `dashboard` registers, or hardening sync conflict-resolution to preserve coworker-owned barrel lines) is a separate change. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides), NOT commented (clean self-evident maintainer/bot fix, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant). CI `check`✓ `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#920 (2026-07-13)** — `fix(slang-mcp): forward Discord follow-ups from the always-on daemon`, branch `fix/nv-slang-bot/discord-followup-daemon-forward` → `nv-slang`, author **nv-slang-bot[bot]**, **code** (+282/-16, 3 files: `slang-mcp/src/discord/discord.py` +29/-3, `feedback_collector.py` +120/-13, new `test/test_discord_followup_forward.py` +133). **Root-cause fix for intermittent Discord OP-follow-up drops.** The follow-up forwarder (`on_message`→POST dashboard-ingress) lived ONLY in `slang-mcp/discord.py`, whose Discord Gateway is per-MCP-session and reaped after ~10 min idle by `supergateway --stateful` (child killed) → forwarding worked only while a container's MCP session was warm; cold windows dropped follow-ups (verified via `thread_state.jsonl` gaps, e.g. 07-09→07-13). **Fix:** make the always-on `feedback_collector.py` daemon (permanent Gateway, own systemd service) the canonical forwarder. **Inline review verdict: clean, correct, internally consistent.** Verified all load-bearing claims at head: (1) **ownership axis is a correct mirror-image** — both gates key on `DISCORD_POST_SUMMON`: `discord.py._forward_followups_disabled()` = `env!="1"` (default→disabled=daemon owns; `=1`→enabled=slang-mcp owns), `feedback_collector._forward_followups_here()` = `env!="1"` after a `DISCORD_READ_ONLY=1` hard-off → exactly one forwards (prod→daemon / lego→slang-mcp), no duplicate public reply when both Gateways momentarily warm; (2) **cap pre-count claim true** — slang-mcp `SummonView.get_help` ALREADY does `summoned`+`bot_reply` (discord.py 304-305); PR adds the matching pair to daemon `SummonView` (203, 207) so shared `thread_state.jsonl` bot_reply accounting agrees across both processes; (3) **thread_id passthrough** — `_post_to_dashboard(content, thread_id)` adds `body["thread_id"]` only when truthy (both files), passed on summon + follow-up paths, omitted when None→group catch-all; routes each thread to its own per-thread session (wiring `session_mode=per-thread`); (4) daemon `on_message` **audits FIRST** (`_save_thread_reply` unconditional) then gates OP-only/summoned/resolved/cap (mirrors slang-mcp), pre-records bot_reply before POST (race guard); the `state.bot_reply_count - 1` log correctly recovers pre-increment (daemon `state` is the LIVE defaultdict mutated by `_record_thread_event`, unlike discord.py's dict-snapshot which logs pre-value directly — both log pre-value, consistent, not a bug). 8 new tests cover both gates (exactly-one-forwarder prod+lego), thread_id passthrough (present+None), summon pre-count; body: slang-mcp suite 61 pass/5 skip, 4 fails pre-existing on baseline nv-slang (test_config env + test_discord send-allowlist, verified by stash). **Deploy note in body (operator task, NOT this PR):** inert until `systemctl --user restart nanoclaw-prod-discord-feedback.service` (brief Gateway drop — do when forum quiet). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang/slangpy approver at a nanoclaw-repo PR about the slang-mcp Discord forwarder is nonsensical), NOT commented (clean self-evident maintainer/bot fix, green, hygiene), merge is maintainer's (`nv-slang` outside nv-coworkers auto-merge grant). CI `ci`✓ `label`✓ ALL GREEN (run 29262849193/86860386725); `MERGEABLE`, UNSTABLE→now-green, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#922 (2026-07-13)** — `feat(dashboard): dedicated PR-approver (Verity) shadow-decision panel`, branch `nv-approver-panel-dashboard` → `nv-dashboard`, author **szihs** (human/maintainer), **code** (+80/-1, one file: `dashboard/public/app.js`). Adds a "PR Approver — Verity (shadow)" panel under the issue funnel rendering `snap.approverDecisions` (columns PR · Decision→Human · PR State · Reason · Mode · Decided, per-decision-state count header, bot/human author tag). **Why:** the existing funnel row table is gated by `pr_session_mappings` so it lists only bot-authored PRs — hiding Verity's shadow reviews of human-authored PRs (18 of 22 decided PRs never appeared); this panel reads the standalone `approverDecisions[]` (from the `approval_decisions` ledger) so every decision is visible with live PR state. **Companion #921** (nv-main) supplies `snap.approverDecisions` via `scripts/funnel.ts`. **Inline review verdict: clean, correct, internally consistent.** Verified at head `nv-approver-panel-dashboard`: (a) new `funnelApproverPanel(decisions)` guarded by `if (!Array.isArray(decisions) || decisions.length===0) return ''` → until #921 merges + snapshot regenerates, `snap.approverDecisions` is absent → renders nothing (safe to merge in either order, as body states); (b) **XSS-safe** — every interpolated data field wrapped in `esc()` (repo/decision/human/prState/reason/mode) + `esc(prUrl)` on the href (numeric `d.pr` interpolated raw into `#${d.pr}` is a ledger PR number, low-risk, matches surrounding idiom); (c) `formatTime(d.decidedAt)` guarded by `d.decidedAt ?`; (d) both `esc` (line 3145) + `formatTime` (line 3115) helpers exist in the file at head — the one thing that would break the render; (e) purely additive — appended to `partHtml` in `loadFunnel()`, funnel board/win-rate/existing tables untouched; (f) color maps literal hex + CSS vars, consistent with the in-code comment (palette object scoped to `funnelFlowHtml`). Body: `dashboard/server.test.ts` 78 pass; rendered against 22-decision snapshot (4 WOULD_APPROVE / 1 BLOCK / 17 ABSTAIN_POLICY). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang/slangpy approver at a nanoclaw dashboard PR is nonsensical), NOT commented (clean self-evident maintainer feature PR, green, hygiene), merge is maintainer's (`nv-dashboard` outside nv-coworkers auto-merge grant; szihs-owned). CI `ci`✓ `label`✓ ALL GREEN (run 29263606136/86862982797); `MERGEABLE`, `mergeStateStatus: CLEAN`, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#923 (2026-07-13)** — `docs(codex-critique): add DECISION_REVIEW to the stage menu + table`, branch `nv-codex-critique-decision-review` → `nv-main`, author **szihs** (human/maintainer), **docs-only** (+4/-1, one file: `container/skills/codex-critique/SKILL.md`). Closes a vocabulary-drift gap: the slang/slangpy `*-pr-approver` coworker types **require** `[DECISION_REVIEW, OUTPUT_REVIEW]` and the on-deliver critique gate blocks their decision until both have a recorded round, but the `codex-critique` SKILL.md (the skill the approver opens to *construct* a critique call) listed only `DIAGNOSIS_REVIEW|PLAN_REVIEW|CODE_REVIEW|OUTPUT_REVIEW` — `DECISION_REVIEW` appeared nowhere, so an approver satisfying its gate had to invent how to run the stage it's required to record. Observed impact (PR body): slang-pr-approver session `sess-1783955216591-gx4xw5` (slang PR #12082) tried to deliver with **zero** critique rounds, hit the gate 3× (`missing critique stages: DECISION_REVIEW, OUTPUT_REVIEW`), and escalated a `critique_gate_bypass` request instead of running the critique (admin correctly rejected). **Fix (3 edits):** (1) adds `DECISION_REVIEW` to the prompt `STAGE:` menu; (2) new table row — trigger "Verdict derived, before recording it", context "the derivation: clauses from data, verdict parse vs. the review doc, source tier stated"; (3) a "Decision-style work" note distinguishing `DECISION_REVIEW` (vets the *derivation*) from `OUTPUT_REVIEW` (vets the ledger line + message), mirroring the existing `ANSWER_REVIEW → OUTPUT_REVIEW` note. **Inline review verdict: clean, correct, internally consistent.** Diff matches body byte-for-byte at head `80a9133e`. Verified the "not a mechanism change" claim on nv-main: `container/hooks/track-critique.sh:58` parses a GENERIC `grep -m1 -oE 'STAGE:[[:space:]]*[A-Z_]+'` → an emitted `STAGE: DECISION_REVIEW` records fine with no code change; `gate-critique-on-deliver.sh` reads required stages from `.critique-required-stages` and checks `count>=1` (behavior unchanged). Diff touches only the skill body — no `coworker-types.yaml`, so no skill-reference errors (body: `npm run validate:templates` exit 0). The approver `coworker-types.yaml` files that declare the `[DECISION_REVIEW, OUTPUT_REVIEW]` requirement live on the `nv-slang`/`nv-slangpy` overlay branches (composed at CI-merge time), consistent with the fork's overlay model — body's reference accurate. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang/slangpy approver at a nanoclaw-repo PR *about* the codex-critique stage menu is nonsensical), NOT commented (clean self-evident maintainer docs fix, green, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant; szihs-owned). CI `check`✓ `ci`✓ `label`✓ ALL GREEN; `MERGEABLE`, `mergeStateStatus: CLEAN`, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#924 (2026-07-13) — SUPERSEDES #923** — `docs(codex-critique): add DECISION_REVIEW to the stage menu + table`, branch `fix/codex-critique-decision-review` → `nv-main`, author **szihs** (human/maintainer), **docs-only** (+2/-1, one file: `container/skills/codex-critique/SKILL.md`). **Trimmed re-do of #923** — #923's `nv-*` head branch (`nv-codex-critique-decision-review`) was frozen by the "Protect nv-* branches" ruleset so its over-scaffolded prose note couldn't be removed in place; #924 carries the trimmed version on an unprotected `fix/*` branch. Diff is a **strict subset of #923** (which I already reviewed CLEAN): (1) adds `DECISION_REVIEW` to the `STAGE:` prompt menu; (2) one new table row (trigger "Verdict derived, before recording it"; context "the derivation: clauses from data, verdict parse vs. the review doc, source tier stated"). **Drops** #923's third edit (the "Decision-style work" DECISION_REVIEW-vs-OUTPUT_REVIEW prose note) — that's the "over-scaffolded prose" the supersede message names. Same rationale/impact as #923: closes vocabulary-drift where slang/slangpy `*-pr-approver` require `[DECISION_REVIEW, OUTPUT_REVIEW]` + on-deliver gate blocks until both recorded, but codex-critique SKILL.md listed neither DECISION_REVIEW; slang-pr-approver session `sess-1783955216591-gx4xw5` (slang PR #12082) tried delivery with 0 critique rounds, hit gate 3×, escalated `critique_gate_bypass` (admin rejected). **Not a mechanism change** — `track-critique.sh` parses generic `STAGE:\s*[A-Z_]+`; `gate-critique-on-deliver.sh` reads `.critique-required-stages` + checks `count>=1`; guidance gap only. **Inline review verdict: clean, correct, internally consistent** — diff matches #923's verified subset byte-for-byte; touches no `coworker-types.yaml` (body: `npm run validate:templates` exit 0). Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang/slangpy approver at a nanoclaw-repo PR *about* the codex-critique stage menu is nonsensical), NOT commented (clean self-evident maintainer docs fix, hygiene), merge is maintainer's (`nv-main` tracks upstream, outside nv-coworkers auto-merge grant; szihs-owned). CI `check`✓ `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#926 (2026-07-13) — UPSTREAM-SYNC CLASS, AUTO-MERGED** — `Sync nv-coworkers with upstream/main`, branch `sync/upstream-nv-coworkers` → `nv-coworkers`, bot-authored (nv-slang-bot, daily `sync-upstream.sh`), **large merge** (+15517/-7948, 220 files / 100 in file API). `pr_ready_for_review` webhook (reason `opened`) carried the generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired). **DISTINGUISH from the #888-class sync PRs into OTHER nv-* branches** (nv-main/nv-slang/nv-slangpy/nv-dashboard/nv-nanoclaw): those are maintainer's to merge (outside the nv-coworkers grant), but THIS one targets **`nv-coworkers`** → squarely within my standing auto-merge authority ([[feedback_nv_coworkers_automerge]]). **Verified before merge (per grant's checklist):** (a) commit authors all genuine upstream nanocoai/nanoclaw contributors — claude/gavrielc/Koshkoshinsk/glifocat/omri-maya/github-actions/moshe-nanoco/gabi-simons, no bot-injected content; (b) file list clean — no `knowledge_base/`/`.env`/secrets; the `repo-tokens/`+`update-tokens.yml` hits are a benign upstream feature (a GH Action counting **LLM** tokens for a README size-badge, verified README content — nothing credential-related); (c) `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, `label`✓, no CI gate on nv-coworkers (`ci.yml` fires only on PRs into `main`). **MERGED via REST** `merge_method=merge` (PR body asks "Create a merge commit" to preserve upstream parent links, same as #888) → merge-commit `b7cac68d`, `mergedAt 21:33:40Z`, `state MERGED`, terminal. Not routed, not commented (self-evident automated sync, clean merge — hygiene). On redelivery / future daily `sync/upstream-nv-coworkers` instances: verify authors+file-list clean, then auto-merge; no route/review/comment.

**#928 (2026-07-14) — UPSTREAM-SYNC CLASS, composed-BUILD red (new failure mode)** — `Sync nv-slang with upstream/main da9a74fc (2026-07-14)`, branch `sync/upstream-nv-slang` → `nv-slang`, bot-authored (nv-slang-bot, daily `sync-upstream.sh`), **large merge** (+15615/-8046, 220 files). `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired). Next-day nv-slang sibling of #889. Same repo-class handling — NOT routed to product `*-pr-approver`, NOT auto-merged (`nv-slang` outside nv-coworkers auto-merge grant + red CI), merge + drift-resolution are the maintainer's. **CI `ci` RED — NEW manifestation of composed-state overlay drift, NOT this PR's content:** unlike #889/#891 (which tripped the `container/skills/slack-formatting/SKILL.md` modify/delete "outside owned set" merge failure), here the `ci.yml` "Merge nv-* branches (test composed state)" step **MERGED CLEANLY** (BASE=nv-slang; no `::error::…outside owned set` emitted — the only occurrences of that string in the log are the script's own echo definition) — then the downstream **"Build (needed for fetch-skills + tests)" step failed** with TS2xxx compile errors (exit 2). Errors are all host-side `src/` in the COMPOSED tree: `ApprovalHandlerContext` has no `approval` property (`dispatch.ts:228`, `message-gate.ts:7`, `response-handler.ts:142`), `config.js` has no `DEFAULT_AGENT_PROVIDER` (`container-configs.ts:1`, `group-init.ts:4`), `self-mod/request.js` has no `handleAddMcpServer`/`handleInstallPackages` (`self-mod/index.ts:24`), `delivery-action.ts:63`/`delivery-actions.test.ts` arg-count (2 vs 3), `templates/parse.ts:3` cannot find module `yaml`. **Diagnosis: overlay drift** — the freshly-synced upstream refactored these host APIs (renamed/moved exports, changed signatures, added the `yaml` dep) while sibling `nv-*` overlay branches still carry stale copies that import the old names; the composed merge mixes new-upstream + stale-overlay → doesn't typecheck. The sync correctly imports upstream; divergence is overlay-side. **Resolution maintainer's:** reconcile the stale overlay host-side source (approvals/self-mod/container-configs/templates/delivery-action) against the synced upstream + ensure `yaml` is in the composed lockfile. **NOT commented** — broad multi-module drift (5+ files across approvals/self-mod/config/templates), self-evident red on the merge button, maintainer (szihs) actively reconciling these overlays daily; follows #890's broad-drift silence, NOT #891's single-crisp-file comment. `label`✓ pass; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips green or a substantive human comment lands.

**#930 (2026-07-14) — UPSTREAM-SYNC CLASS, composed-BUILD red (= #928 sibling)** — `Sync nv-dashboard with upstream/main da9a74fc (2026-07-14)`, branch `sync/upstream-nv-dashboard` → `nv-dashboard`, bot-authored (nv-slang-bot, daily `sync-upstream.sh`), **large merge** (+15550/-7987, 220 files). `pr_ready_for_review` webhook (reason `opened`) carried generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired). **nv-dashboard sibling of the same 2026-07-14 `da9a74fc` sync as #928 (nv-slang)** and next-day sibling of #892 (prior nv-dashboard sync). Same repo-class handling — NOT routed to product `*-pr-approver`, NOT auto-merged (`nv-dashboard` outside nv-coworkers auto-merge grant + red CI), merge + drift-resolution are the maintainer's. **CI `ci` RED — IDENTICAL composed-BUILD failure mode to #928 (run 29301478153), NOT this PR's content:** the `ci.yml` "Merge nv-* branches (test composed state)" step MERGED CLEANLY (BASE=nv-dashboard; no `::error::…outside owned set` — distinguishing this from the older #889/#891/#892 `slack-formatting` modify/delete class), then the downstream **"Build (needed for fetch-skills + tests)" step failed** (exit 2) with the same overlay-drift TS2xxx set as #928: `ApprovalHandlerContext` has no `approval` (`dispatch.ts:228`, `message-gate.ts:7/29`, `response-handler.ts:142`), `config.js` no `DEFAULT_AGENT_PROVIDER` (`container-configs.ts:1`, `group-init.ts:4`), `self-mod/request.js` no `handleAddMcpServer`/`handleInstallPackages` (`self-mod/index.ts:24/31/32`), `delivery-action.ts:63`/`delivery-actions.test.ts` arg-count (2 vs 3), `templates/parse.ts:3` cannot find module `yaml`. **Diagnosis: overlay drift** — synced upstream refactored these host APIs (renamed/moved exports, changed signatures, added the `yaml` dep) while sibling `nv-*` overlay branches still carry stale copies importing the old names; composed merge mixes new-upstream + stale-overlay → doesn't typecheck. Sync correctly imports upstream; divergence is overlay-side. **Resolution maintainer's:** reconcile stale overlay host-side source (approvals/self-mod/container-configs/templates/delivery-action) against synced upstream + ensure `yaml` in the composed lockfile. **NOT commented** — same broad multi-module drift as #928 (5+ files across approvals/self-mod/config/templates), self-evident red on the merge button, maintainer (szihs) reconciling these overlays daily; follows #890/#892/#928 broad-drift silence, NOT #891's single-crisp-file comment. Duplicate per-PR comment across the identical daily-sync-batch failure = noise (hygiene). `label`✓ pass; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips green or a substantive human comment lands.

**#934 (2026-07-14)** — `feat(slang): add code-comment discipline as a spine invariant`, branch `fix/issue-933-comment-invariant` → `nv-slang`, author **nv-slang-bot[bot]**, **code** (+11/-0, 2 files: `container/spines/slang/coworker-types.yaml` +4, new `container/spines/slang/invariants/comments.md` +7). **Fixes #933** (szihs "Fix the verbosity": (1) comment only if it adds value + be concise, add invariant to nv-slang+nv-slangpy; (2) commit message and comment treated separately). Same invariant-promotion pattern as #913/#914 (peer-review) — promotes a rule from skippable workflow prose to an always-in-context spine invariant that survives compaction. New `comments.md` (scope: "writers, fixer, reviewer; readers can ignore"): (a) comment only when it adds value — explain *why*, don't restate code, prefer clearer code; (b) be concise, no banners/changelog-narration/"added by" markers; (c) commit-message ≠ code-comment, keep separate. Wired into `slang-writer` (inherited by slang-fixer) + `slang-reviewer`. **Inline review verdict: clean, correct, internally consistent.** Verified in the YAML at head `fix/issue-933-comment-invariant`: `slang-fixer` (L96) `extends: slang-writer` → inherits the new invariant via the established union-across-`extends` mechanism (same load-bearing pattern as #913/#914); `slang-reviewer` (L108) gets it directly alongside `code-changes.md`+`review-output.md`; scope line matches exactly where it's wired; diff matches body. Maps issue #933 1:1 (bullet 1+2 = add-value+concise, bullet 3 = commit≠comment), correctly targets nv-slang with companion-slangpy parity note. **PR body carries a "please confirm" interpretation question** (code-comments vs GitHub-comment verbosity) directed at szihs — maintainer's to resolve (szihs authored the issue), NOT a bot-comment matter. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang/slangpy approver at a nanoclaw-repo PR *about* the slang spine's comment invariant is nonsensical), NOT commented (clean self-evident maintainer/bot fix, green, hygiene), merge is maintainer's (`nv-slang` outside nv-coworkers auto-merge grant). CI `ci`✓ `label`✓ ALL GREEN (run 29318971163); `MERGEABLE`, `mergeStateStatus: CLEAN`, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#935 (2026-07-14) — SLANGPY SIBLING of #934** — `feat(slangpy): add code-comment discipline as a spine invariant`, branch `fix/issue-933-comment-invariant-slangpy` → `nv-slangpy`, author **nv-slang-bot[bot]**, **code** (+11/-0, 2 files: `container/spines/slangpy/coworker-types.yaml` +4, new `container/spines/slangpy/invariants/comments.md` +7). **Fixes #933** (szihs "Fix the verbosity"; same issue as #934). Byte-mirror of #934's slang change retargeted to slangpy — promotes code-comment discipline from skippable workflow prose to an always-in-context spine invariant that survives compaction (same pattern as #913/#914 peer-review). New `comments.md` (scope: "writers, fixer, reviewer; readers can ignore"): (a) comment only when it adds value — explain *why*, don't restate code, prefer clearer code; (b) be concise, no banners/changelog-narration/"added by" markers; (c) commit-message ≠ code-comment, keep separate. Wired into `slangpy-writer` (inherited by slangpy-fixer via `extends`) + `slangpy-reviewer` alongside `code-changes.md`(+`review-output.md` for reviewer). **Inline review verdict: clean, correct, internally consistent.** Verified in the YAML at head: `slangpy-writer` declares `invariants: [code-changes.md, comments.md]`; `slangpy-fixer extends: slangpy-writer` → inherits via the established union-across-`extends` mechanism (same load-bearing pattern as #913/#914); `slangpy-reviewer` gets it directly (invariants list = code-changes+comments+review-output); scope line matches where it's wired; diff matches body (exactly 2 files, additive). Maps issue #933 1:1. **PR body carries the same "please confirm" interpretation question** (code-comments authored during a change vs GitHub PR/issue-comment verbosity — retarget to `review-output.md` if the latter) directed at szihs — maintainer's to resolve (szihs authored the issue), NOT a bot-comment matter; identical hedge to #934. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slangpy-pr-approver at a nanoclaw-repo PR *about* the slangpy spine's comment invariant is nonsensical), NOT commented (clean self-evident maintainer/bot fix, green, hygiene), merge is maintainer's (`nv-slangpy` outside nv-coworkers auto-merge grant). CI `ci`✓ (run 29318984350) `label`✓ ALL GREEN; `MERGEABLE`, `mergeStateStatus: CLEAN`, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#936 (2026-07-14) — DASHBOARD CLASS (sibling of #922)** — `fix(dashboard): make Context histogram respect the 1d/7d/30d/all period selector`, branch `nv-dashboard-context-period` → `nv-dashboard`, author **nv-slang-bot[bot]**, **code, server-only** (+152/-57, one file `dashboard/server.ts`). **Root cause:** the Overview "By Coworker" table's Context column (per-session peak-context histogram + mean peak % + compaction `⟳`) was served from a single period-BLIND all-time cache (`contextStatsCache.get(groupId)` → one `ContextStats`; `refreshContextStatsCache` folded every session's all-time peak into one histogram ignoring line timestamps), so it showed all-time numbers regardless of the 1d/7d/30d/all selector while the cost/token columns beside it filtered by period — silent disagreement. **Fix (server-only, no frontend change):** (1) `scanFileContext` now buckets each transcript's signal (`assistant` usage + `compact_boundary`) by the line's own ISO timestamp day via new `isoDayKey(ts)` (`ts.slice(0,10).replace(/-/g,'')` → `YYYYMMDD`, no `Date` parse) into a `PerFileContext.days: Map<YYYYMMDD, DayContextAgg>` (replaces the old flat `peak/compactions/auto/manual/preTokensSum` fields); per-day granularity reconstructs any day-window without re-reading the append-only mtime-cached file. (2) `refreshContextStatsCache` computes the four cutoffs once via `ccusageSinceDate(0/7/30)` + `''` for all, then folds each file's per-day map into all four periods in one pass; a session with no in-window day drops out of that period. (3) `contextStatsCache` value type widened flat `ContextStats` → `Record<ContextPeriod,ContextStats|null>`; new `buildContextStats(acc, maxContext, capped)` (math mirrors the original single-period build; returns null on 0 sessions). (4) endpoint picks `rec[ctxPeriod] ?? rec.all ?? null`, `ctxPeriod` narrowed from the `period` query param (which is typed over ccusageCache keys incl. `lastRefresh`) via `CONTEXT_PERIODS.includes(...)` default-all. **Inline review verdict: clean, correct, internally consistent.** Verified the ONE load-bearing correctness claim at head `nv-dashboard-context-period` — that the Context windows agree with the cost columns *by construction*: `ccusageSinceDate` returns `YYYYMMDD` (server.ts:1921 `toISOString().slice(0,10).replace(/-/g,'')`); the cost path's `within` (server.ts:2416) does `entry.date.replace(/-/g,'') >= since` LEXICALLY; the new Context path (server.ts:1747) does `key >= since` where `key = isoDayKey = ts.slice(0,10).replace(/-/g,'')` — identical big-endian `YYYYMMDD` lexical `>=` against the identical cutoffs → same boundaries. Also verified: (a) `MISSING_TS_KEY='00000000'` (timestamp-less/invalid signal) never satisfies a real cutoff so lands ONLY in `all` (dated periods gated by `period !== 'all' && !(key >= since)`); (b) `maxContext` (`groupMaxWindow || modelMaxContext('unknown')`) folded once at file level → fixed % scale/histogram bucketing across periods, only session membership changes when toggling; (c) all FIVE `contextStatsCache` refs (decl 1593, two `.delete` 1705/1777, `.set` 1780, endpoint read 7159) match the new `Record` shape — no stale flat-shape caller; (d) `!rec.all` delete guard = the old `sessions===0` condition (all = superset); (e) no frontend change needed — period buttons already re-fetch (`app.js:loadMetricsTokens`), `contextCellHtml` already renders `—` for null. Body verification (tsc clean, `dashboard/server.test.ts` 78/78, prod-transcript replay: periods differ e.g. Orchestrator 1d=40 vs all=400 sessions, maxContext fixed across periods, counts monotonic 1d≤7d≤30d≤all, old-only groups → null → `—`) consistent with the diff. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook reason=opened carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang/slangpy approver at a nanoclaw dashboard PR is nonsensical, same as #922), NOT commented (clean self-evident maintainer/bot fix, hygiene), merge is maintainer's (`nv-dashboard` outside nv-coworkers auto-merge grant). CI `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#937 (2026-07-14) — DASHBOARD CLASS (sibling of #922/#936)** — `fix(dashboard): spawn ccusage directly (node cli.js), not via npx — kill the fan-out pileup`, branch `nv-dashboard-ccusage-direct-spawn` → `nv-dashboard`, author **nv-slang-bot[bot]**, **code, server-only** (+107/-25, one file `dashboard/server.ts`). **Root-cause fix for a REAL prod incident today (~13:30 UTC):** dashboard cost panel wedged the whole server — systemd `active (running)`, node held :3737, but every HTTP request hung (listen-socket `Recv-Q` piling, one worker thread pinned ~64% CPU >1h); cgroup full of stacked `npx ccusage codex daily` subprocesses. **Cause:** `ccusage` is NOT a declared dep → every call is `npx ccusage …`, re-running npm exec resolution (`sh -c npx`→`npm exec ccusage`→node) PER CALL (measured npx 2.48s vs direct node 0.73s, ~1.7s tax). One refresh fans out ~30–56; under host load the fan-out outran the 60s refresh interval, batches stacked, process count spiralled, event loop wedged. Prior fixes #484 (subprocess cleanup)/#488 (bound fan-out)/#617 (re-entrancy+concurrency cap) all capped the *number* of calls, never removed the *per-call npm-exec tax* → kept recurring. **Fix:** new memoized `resolveCcusageCli()` — scans `~/.npm/_npx/*/node_modules/ccusage/src/cli.js`, picks most-recent mtime (version bumps win), warms once via `npx --yes ccusage --version` (execSync, 120s timeout) on cold cache then re-scans; tri-state cache sentinel (`null`=unresolved / `''`=resolved-failed→npx fallback / non-empty=path). Both `runCcusage` (Claude) + `runCodexCcusage` (Codex) pivot `proc = cli ? execFile(process.execPath, [cli, ...ccusageArgs], opts, cb) : exec('npx ccusage …', opts, cb)` — direct `node <cli.js>` (one process, no shell, no npm exec) when resolved, npx fallback otherwise; callback/timeout/env logic unchanged. **Inline review verdict: clean, correct, internally consistent.** Verified at head `nv-dashboard-ccusage-direct-spawn`: (a) all new-code imports present in the edited block — `execFile`+`execSync` (child_process), `homedir` (os), `existsSync`/`readdirSync`/`statSync` (fs), `join` (path) all imported (`execFile`+`homedir` newly added by the diff) → no build break; (b) `findCli()` guards `readdirSync`/`statSync` in try/catch → returns `''` on any FS failure, never throws; (c) **no warm-up storm** — `resolveCcusageCli()` is called SYNCHRONOUSLY inside each `new Promise()` executor (executors run sync at construction), and the fan-out builds the promise array left-to-right, so the FIRST `runCcusage()` blocks on the one-time execSync warm-up and every subsequent construction sees the populated cache → exactly one warm-up per process, not per call; (d) fallback-to-npx on unresolved CLI = cost reporting degrades not breaks (fix's stated safety property); (e) `--offline` + `CCUSAGE_MODEL_ALIASES` (Claude) + `CODEX_HOME` (codex) env preserved; the actual fix = memoizing the resolve so the ~1.7s tax is paid once, not 30–56× per refresh. Body verification (esbuild transform passes = same tsx runtime transform, no new type errors in edited region, live-npx-cache run resolves cli.js + valid daily JSON ~0.2s + memoized 2nd resolve 0ms) consistent with the diff. Same repo-class handling — NOT routed to product `*-pr-approver` (NanoClaw-platform fork; no nanoclaw approver wired; `pr_ready_for_review` webhook reason=opened carried the generic post-#874 "route to *-pr-approver" task string, standing rule overrides — routing a slang/slangpy approver at a nanoclaw dashboard PR is nonsensical, same as #922/#936), NOT commented (clean self-evident maintainer/bot fix, hygiene), merge is maintainer's (`nv-dashboard` outside nv-coworkers auto-merge grant). CI `label`✓ pass / `ci` pending at review time; `MERGEABLE`, UNSTABLE, not draft, 0 reviews. On redelivery / `synchronize`: no-op unless CI flips red or a substantive human comment lands.

**#939 (2026-07-14) — UPSTREAM-SYNC CLASS into nv-coworkers, AUTO-MERGED (= #926 sibling)** — `Sync nv-coworkers with upstream/main b7e2412 (2026-07-15)`, branch `sync/upstream-nv-coworkers` → `nv-coworkers`, bot-authored (nv-slang-bot, daily `sync-upstream.sh`), **trivially small** (+1/-1, ONE file `.claude/skills/add-telegram/SKILL.md` — unlike #926's 220-file payload; this sync window carried only one upstream commit). `pr_ready_for_review` webhook (reason `opened`) carried the generic post-#874 "route to *-pr-approver" task string — standing rule overrides (NanoClaw-platform fork, no nanoclaw approver wired). **Targets `nv-coworkers` → within my standing auto-merge authority** ([[feedback_nv_coworkers_automerge]]), same as #926 (distinguish from #928/#930 sync PRs into other nv-* branches, which are maintainer's). **Verified before merge (grant checklist):** (a) authors genuine upstream contributor — `amit-shafnir` (nanocoai/nanoclaw PR #3043: `fix(skills): switch Telegram deep-link from t.me to telegram.me`) + bot sync-merge commit, no bot-injected content; (b) file list clean — single skill-doc file, no `knowledge_base/`/`.env`/secrets; (c) `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, `label`✓ pass, no `ci` gate on nv-coworkers (ci.yml fires only on PRs into `main`), not draft, 0 reviews. Diff = one line in the add-telegram pairing prose: `https://t.me/{{bot_username}}` → `https://telegram.me/{{bot_username}}`. **GraphQL merge blocked** ("Resource not accessible by integration" — [[project_nv_slang_bot_readonly_incident]]); MERGED via REST `PUT /pulls/939/merge` `merge_method=merge` (preserve upstream parent links, same as #926/#888) → merge-commit `75efb659`, `mergedAt 21:34:15Z`, `state MERGED`, terminal. Not routed, not commented (self-evident automated sync, clean merge — hygiene). On redelivery / future daily `sync/upstream-nv-coworkers` instances: verify authors+file-list clean, then auto-merge via REST; no route/review/comment.

**#883 (2026-07-10)** — `fix(nanoclaw-pr-review-runner): transient Chrome-launch retry + stable-done for Devin scraper`, branch `fix/nv-nanoclaw/devin-fetch-launch-retry` → `nv-nanoclaw`, bot-authored, **code** (+63/-17: `container/skills/nanoclaw-pr-review-runner/scripts/devin-fetch.sh` +61/-16, `container/workflows/nanoclaw-pr-review/WORKFLOW.md` +2/-1). Ports the copy-agnostic Devin-scraper fixes from merged `shader-slang/slang-skills#53` into the nanoclaw-repo-owned reviewer — unlike #882/#884 (slang copies whose profile-retry logic lived in a local-only skill), THIS is committed to the fork so the code is inspectable. Two real changes: (1) new `open_page()` wraps `agent-browser open` with one-shot retry — on launch-fail regex or nonzero rc, closes daemon, clears `/tmp/agent-browser-chrome-*`/`-profile-*`, retries once, `exit 4` (transient best-effort skip) if still failing; (2) stable-done detection — done-signal must hold across TWO consecutive polls, and a lingering "PR analysis in progress" substring no longer vetoes an otherwise-complete page (`done = heading && summary`; in-progress only vetoes when `!done`), `Generating…` guard preserved. **Inline review verdict: clean, correct.** `set -e` idioms verified safe (`if err="$(… 2>&1 >/dev/null)"` capture keeps nonzero `open` from aborting; `[ "$stable" -ge 2 ] && break` is non-final in AND-OR list); redirect order right (stderr→err, stdout discarded); two confirming polls are POLL-sec apart (follow a sleep). Minor non-blocking: profile-clear globs narrower than the `/tmp/agent-browser-*` comment, but a surviving failure still exit-4 (non-fatal). Same repo-class handling — NOT routed to product `*-pr-approver`, NOT commented (clean self-evident maintainer fix), merge is maintainer's (nv-coworkers-scoped auto-merge grant doesn't cover `nv-nanoclaw`). CI `label`✓ SUCCESS / `ci` IN_PROGRESS at review time; `MERGEABLE`, not draft, 0 reviews. On redelivery: state unchanged; do NOT route, re-review, comment, or auto-merge.
</content>
</invoke>
