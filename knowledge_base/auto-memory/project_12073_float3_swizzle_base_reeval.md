---
name: project_12073_float3_swizzle_base_reeval
description: #12073 CUDA/CPP float3 swizzle re-evals base per component (~3x) — MERGED via #12078 (szihs 07-14 10:45Z, merge d6c7720112ad; approver WOULD_APPROVE=agreement). slangpy#1059 downstream now resolved-pending-release.
metadata: 
  node_type: memory
  type: project
  originSessionId: 9e45fd10-83bf-4fc5-add9-e325929c4585
---

**shader-slang/slang#12073** — CUDA + CPU/C++ targets re-evaluate a multi-component swizzle's base once per component. When the base is a folded value (texture fetch / buffer load), the fetch is textually duplicated N times → ~3x memory traffic for `.rgb`. SPIR-V (`OpVectorShuffle`) and HLSL (native `.xyz`) evaluate base once → no penalty. Output is correct; this is a **perf/codegen** bug, not correctness or layout.

**Escalated from** shader-slang/slangpy#1059 (`float3` loop math ~2.9x slower than `float4` on CUDA). SlangPy is type-transparent and can't fix — lowering lives in the compiler. Issue #12073 was auto-filed by nv-slang-bot.

**Root cause CONFIRMED (triager-validated, not just relayed):** `CPPSourceEmitter::kIROp_Swizzle` override (`source/slang/slang-emit-cpp.cpp:1692-1716`) re-emits the base via `emitOperand` inside the per-component brace-init loop, and `shouldFoldInstIntoUseSites` folds the load/fetch into each use site. `CUDASourceEmitter : CPPSourceEmitter` (`slang-emit-cuda.h:44`) inherits it. GPU-free repro: CUDA `tex2Dfetch` hot-loop count 1/3/1 for `f4_all`/`f3_loop`/`f3_epi` at HEAD `8f0c3515d`; CPP duplicates `src[q]` subscript 3×.

**Classification:** bug(perf) / medium / target-emit (CUDA·CPP) / P2 · no dup.

**Recommended fix (triager):** extend the existing `CPPSourceEmitter::shouldFoldInstIntoUseSites` vector guard (`emit-cpp.cpp:1943-1951`, already refuses folding for reshape/cast "multiple references") to also refuse folding `kIROp_Swizzle` with `elementCount>1` → base materializes as a temp → CUDA/CPP reach SPIR-V/HLSL parity. GPU-free FileCheck test feasible.

**Chain state (07-13):** triaged & REPRODUCED, verdict posted on issue (comment 4953942759), forwarded to slang-fixer (peer-wired via triager — do NOT double-dispatch). Fixer expected to open a DRAFT PR per drafts-only guardrail [[feedback_drafts_only_guardrail]].

**BLOCKER (07-13 03:2x):** slang-fixer #12073 session (`sess-1783910553647-6y3q0i`, group `ag-1780667166439-vmjrwe`) is autocompact-thrashing (7 "Context compacted" notices, refill-to-limit within 3 turns 3× — same failure mode as #12071/#11917). Fix WAS applied before the loop (Approach B: `CPPSourceEmitter::shouldFoldInstIntoUseSites` refuses to fold base into multi-element `kIROp_Swizzle`; build in worktree wt-slang-12073). Triager escalated (msg 12), correctly NOT messaging the thrashing session. Recovery memo is fully captured in triage-12073.md (re-apply = guard at slang-emit-cpp.cpp:1943-1951 for kIROp_Swizzle elementCount>1).

**Restart path — HOST-ONLY (see [[agent-ncl-restart-cant-target-another-group]] (shared: `1783913779722-agent-ncl-restart-can-t-target-another-group`)):** I cannot restart another group's session from my container (agent-caller restart kills MY session, ignores --id). Operator escalation sent 07-13 03:36.

**RESOLVED (07-13 09:2x):** thrash halted — #12073 fixer container (`sess-1783910553647-6y3q0i`) now `stopped`; stray restart approval cards rejected/cleared by operator (notif #36). No collateral (only #12048 container running in group, untouched). Recovery re-handed via triager on a **FRESH** sub-thread `gh-issue-shader-slang/slang-12073/resume` (NOT the poisoned canonical thread — restart≠/clear, session history persists in DB → poking canonical would re-thrash). Fresh session picks up on-disk worktree `wt-slang-12073`: check diff/build.log → re-apply emit-cpp.cpp:1943-1951 guard if lost → build → GPU-free FileCheck (tex2Dfetch 3→1) → DRAFT PR (Closes #12073) + report_pr_created. AWAITING fixer's single [Fix Report] (triager forwards [Triage Resolution] up on canonical thread). Verify report_pr_created was called when PR lands [[feedback_verify_report_pr_created]].

**Fresh session VERIFIED (09:25):** resume sub-thread minted new session `sess-1783934741132-3oqycw` (distinct from poisoned `sess-...6y3q0i`, which stays `stopped`). Routing correct. NO fix PR yet — triager self-corrected: msg 42 was a "Context compacted" notice, and PR #12074 is UNRELATED (compile-perf nightly UTF-8, closes:[], non-draft) — do NOT conflate.
**WATCH-ITEM REALIZED (09:31):** fresh resume session ALSO thrashed — 3 compactions in ~4 min, 873k→876k→882k climbing (ids 42/44/48). A fresh thread did NOT fix it → cause is NOT poisoned history; it's the fixer's I/O (whole-file build.log/git diff reads) or bloated base context (873k-on-first-notice is abnormal for a truly empty session).

**DECISION (07-13 09:31): MOVE FIX OFF THE CYCLING FIXER → slang-triager implements it.** Two thrash loops on a ~5-line guard = third cycle low-EV. Triager is the right actor: reproduced from source during triage (warm slangc build + repro, tex2Dfetch 1/3/1 confirmed), knows the seam, never thrashed. Mandate: implement guard at emit-cpp.cpp:1943-1951 (refuse fold kIROp_Swizzle elementCount>1) + GPU-free FileCheck test (tex2Dfetch 3→1) + build → DRAFT PR (Closes #12073) + report_pr_created; drafts-only, ready/merge operator-gated; reviewer still reviews before ready (merge gate preserved). Triager reports [Fix Report] directly to me on canonical thread (it IS the implementer now — no separate [Triage Resolution] forward).
**Operator asked (09:31):** host-shell `ncl groups restart --id ag-1780667166439-vmjrwe` to STOP the thrashing fixer (token burn). Plain restart (no --message) so nothing re-wakes with bloated context; won't send #12073 inbound to fixer again. Also bounces idle #12048 container — acceptable (its work durable in draft PR #12050, FIXED+APPROVED, operator-gated). fix/issue-12073 fixer branch/worktree wt-slang-12073 stays on disk; triager can cherry-pick or re-implement.

**FIXED — draft PR #12078 (07-13 10:44, Main-verified via github_get_pull_request):** triager implemented it (fixer thrashed 2×, was stopped). PR #12078 real, `draft:true` (gate respected), `Closes #12073`, head `fix/issue-12073`, base master, author nv-slang-bot, label `pr: non-breaking`, assignee/reviewer jvepsalainen-nv → report_pr_created confirmed (webhooks route to triager's session). **Fix:** +21/−1 in slang-emit-cpp.cpp — extend the existing vector/matrix "multiple references" guard in `shouldFoldInstIntoUseSites` to also block folding a value used as base of a multi-element `kIROp_Swizzle` → base materializes as temp → CUDA/CPP parity w/ SPIR-V/HLSL. +51-line GPU-free FileCheck test `tests/cuda/swizzle-base-fold.slang`; +2 golden-line update in `dispatch-thread-id-extraction.slang` (asserted old duplicated form). **Tests:** repro 3→1 on fresh binary; cuda 60/60, types+compute 614/614, swizzles+bugs 644/644. Caveat: clang-format unavailable locally → hand-formatted, flagged for `/format` if CI drifts.
**NEXT (all operator-gated):** reviewer verifies → maintainer flips ready → CI → merge. Triager owns PR #12078 webhooks (review comments / CI) on canonical thread. Drafts-only holds [[feedback_drafts_only_guardrail]].

**COLLISION RISK (07-13 12:34):** TWO slang-fixer containers alive+running on #12073 threads — canonical `sess-1783910553647-6y3q0i` (woke 12:34) AND resume `sess-1783934741132-3oqycw` — both re-attempting the already-shipped fix (build `btuw1wmvv` ~15-25min then plans to push fix/issue-12073 + open PR). Risk: DUPLICATE PR + force-push CLOBBER of triager's pushed fix/issue-12073 behind draft #12078 [[project_stacked_pr_shared_base_clobber]] [[project_dup_pr_cross_instance]]. PR #12078 VERIFIED still intact @12:34 (draft, updated_at 10:44, no clobber yet). Triager sent 1 stand-down to fixer + escalated. Root: sessions re-wake on ANY inbound → earlier plain restarts didn't stick. Operator asked (12:34) to stop BOTH #12073 fixer sessions; NOBODY should send those sessions a new inbound (re-wakes them). Fix is DONE — fixer work here is pure waste+risk.
**COLLISION seemed RESOLVED (12:40):** fixer acked triager's stand-down (its msg 76) — not pushing, no dup PR, build dead (disk-full). Retracted operator ask. NB: disk-full → possible [[project_fleet_disk_capacity_wall_11969]] recurrence (54wt/124G @07-10).
**REVERSED (12:55): fixer BACK in hard thrash (its 2nd distinct episode today, "3 in a row").** A thrashing session can lose stand-down context → re-attempt #12073 → clobber fix/issue-12073 / dup PR. Main-verified 12:56: 2 fixer containers still running (`sess-...6y3q0i`, `sess-...3oqycw`); PR #12078 branch STILL INTACT (updated_at 10:44, draft). Re-escalated operator stop (12:56) — NOT whipsawing (state genuinely changed). Plain restart alone hasn't held (sessions re-wake on ANY inbound) → after stop, nobody sends #12073 inbound. If it cycles a 3rd time, may need harder measure (close+recreate session, or investigate why slang-fixer respawns onto completed threads). Fix DONE + safe while branch unclobbered.

**✅ TERMINAL (07-14 10:45:53Z): PR #12078 MERGED** into master (Main-verified: state=closed, merged_at=2026-07-14T10:45:53Z, draft=false, mergeCommit d6c7720112add..., merged by szihs; approvals szihs + jvepsalainen-nv; 3 files +77/−3; branch survived the fixer-thrash ordeal UNCLOBBERED). Issue #12073 auto-closed (Closes #12073). Downstream slangpy#1059 resolved-pending-release. Chain closed.

**FOLLOW-UP — docs PR #12079 now OBSOLETE:** interim docs note "CUDA/C++ swizzle base re-evaluation limitation" (head `docs/cuda-swizzle-limitation-12073`, OPEN+DRAFT, assignee+reviewer skiminki-nv, adds new page `docs/user-guide/a2-05-cuda-target-specific.md`). Explicitly framed "retire when #12078 lands" — #12078 MERGED 07-14, so the documented limitation no longer exists. NOT mine to close (maintainer-assigned draft; never auto-close [[feedback_github_writes_operator_authorized]]). Flagged to operator: skiminki-nv should close it, OR repurpose the new a2-05 page minus the now-fixed limitation note. Maintainer's call. OUTSTANDING (separate concern, NOT this chain): slang-fixer container health — the zombie #12073 fixer sessions thrash/API-errored intermittently through the day; escalated to operator (msgs re: `ncl groups restart --id ag-1780667166439-vmjrwe` + optional session-close if it recurs). Track under fixer-lifecycle, not #12073.

**MERGED (07-14 10:45Z) — CHAIN CLOSED.** szihs merged #12078 unchanged (single commit `d62f938a93c9`, merge commit `d6c7720112ad`), two maintainer APPROVEDs (szihs, jvepsalainen-nv). slang-pr-approver decided WOULD_APPROVE (CLEAN) @ `d62f938a93c9` in shadow mode (Devin-only fallback tier — production claude-pr-review skips bot-authored PRs, harvest exit 20; Devin head-current 0/0/0; challenger confirmed the `shouldFoldInstIntoUseSites` fold guard scoped/precise/safe). Human verdict APPROVED stamped via record_human_verdict → **calibration = agreement**. Approver flagged one tag-only clause gap: ledger `mode=live` should have been `live_late` (szihs review landed 07:12 during the ~3-min Devin run) — agreement unaffected, re-sample reviews at record time on fast PRs.

Sibling/cross-ref: slangpy#1059 (downstream, resolved-pending-release now that the compiler fix has merged — closes on the SlangPy release picking up the new slang).

**Ordeal summary (historical):** fixer autocompact-thrashed 2× on this ~5-line fix (canonical session then a fresh resume sub-thread, both 873k+ climbing) — proved cause was fixer I/O (whole-file build.log/git diff reads), not poisoned history. Lesson: [[agent-ncl-restart-cant-target-another-group]] (only host can restart another group's session; agent-caller restart kills own session + ignores --id/--message; approval replay preserves caller:agent → misfires). When a small fix thrashes a coworker 2×, re-route to a warm, disciplined actor rather than cycle a 3rd time.
