---
name: project_12225_json_reflection_size_alignment
description: "#12225 add size/alignment to JSON reflection for every type layout — draft PR; real-CI-failure self-correction (grep skipped category-suffixed reflection tests)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5bec4191-e017-44da-b211-e48a8839d909
---

shader-slang/slang **PR #12225** (draft, nv-slang-bot, base master) "Add size/alignment info to JSON reflection for every type layout" — slang-fixer chain (webhook-originated; Main not looped at dispatch). Adds a `sizes` array to JSON reflection output for every type layout.

**✅ 2026-07-26 12:48 — REAL CI FAILURE self-corrected + re-pushed (fixer msg 65288; supervisor CI flag was RIGHT).** Head now **`6b78029f64`** (CI run 30202747959 in progress).
- **The miss:** fixer's earlier "99/99 local pass" was an INCOMPLETE set. Its local test-discovery grep required `//TEST:REFLECTION` **immediately**, which silently **skipped all 8 category-suffixed reflection tests** (`//TEST(64-bit):REFLECTION`, `//TEST(smoke):REFLECTION`) → their `.expected` never got the new `sizes` array → stale → **CI caught it** (run 30126603300 built+ran, `test-slang` failed cross-platform on 7 reflection tests: `actual-global`, `cross-compile`, `resource-in-cbuffer`, `ptr/ptr-{generic,global,self-reference,struct}`). Supervisor correctly flagged it as REAL, not a priority-yield — fixer re-checked live job conclusions and agreed.
- **Fix:** rebased onto fresh master (`926f93ed17`; #12216's include-path change → full rebuild), regenerated **ALL** reflection expected with the corrected discovery pattern (`TEST(\([^)]*\))?:(CPU_)?REFLECTION` → **68 tests vs the broken 60**), verified every diff purely additive, re-verified **107/107 pass**, force-pushed `6b78029f64`. **C++ UNCHANGED** since the 3-stage-critique approve — this round was test-expected-data + rebase only.
- **Deliberate scope hold:** left the `.32` CPU_REFLECTION variant untouched — no CI job runs 32-bit (wasm build-only), master's own `.32` was already stale independent of this change, and hand-editing an unbuildable golden risks a wrong file. Sound (don't guess a golden you can't build/verify).
- **Lesson (fixer captured as shared learning):** local test-discovery grep that anchors on the exact `//TEST:REFLECTION` form silently skips **category-suffixed** directives (`//TEST(cat):REFLECTION`) → false "N/N local pass". Use the category-tolerant pattern. Same class as [[feedback_signature_grep_passed_vs_failed]] (a grep that matches the happy form under-counts) + [[feedback_verify_regression_claims_at_precision]].
- **✅ 2026-07-28 01:14 — REBASED, #12228-integrated, jkwak APPROVED, MERGEABLE (fixer msg 70716).** Rebased onto master `15863db482` → head **`52ed18417c`**; the prior category-suffixed-test failure fixed + the Falcor image-test red was a "6 commits behind" artifact cleared by rebase; all 107 local reflection tests pass. **#12228 ("reflection-json: emit resultType for typed buffers") landed on the SAME file mid-flight** — rebase auto-merged cleanly, both `resultType` (#12228) + this PR's `sizes` coexist; regenerated all reflection expecteds to carry both. **jkwak-work APPROVED on the rebased head `52ed18417c`**; `reviewDecision=APPROVED`, `mergeable=MERGEABLE`. tangent-vector's pre-rebase approval was dismissed by the required rebase (PR was CONFLICTING → push required; fixer posted a transparent mechanical-rebase note). Non-draft (maintainer-flipped). **Fixer holds — no push (would dismiss jkwak's approval), merge maintainer-gated.** Terminal-pending: maintainer merges once CI completes. (This is a 2nd clean approval milestone; earlier CI-green @6b78029f64 below is superseded by the rebase.)

- **✅ CI CONFIRMED GREEN 2026-07-26 14:17 (fixer msg 65290, run 30202747959 @6b78029f64) [superseded by 07-28 rebase above].** All **9 `test-slang` jobs PASS** — the exact previously-failing jobs (linux/macos/windows × gcc/clang/cl × debug/release) now green → the category-suffixed-test regeneration fix worked. All builds, test-slang-rhi, sanitizer, materialx, compile-regression pass. **Only red = `test-falcor / Test (Falcor)` `test_GBufferRTTexGrads_d3d12`** — the [[project_12145_gbufferrttexgrads_d3d12_access_violation]] flake ANCHOR (G-buffer RT image-comparison; + `Unknown VCS root` env noise), categorically unreachable by a JSON-reflection text-emission diff (`slang-reflection-json.cpp` only), passes on recent master → intermittent flake, not this PR's regression. `check-ci` red only aggregates that one Falcor job. Fixer reran the Falcor job (`gh run rerun --failed`). **Next:** Falcor rerun outcome + slang-reviewer 3-reviewer pass (running against current head), webhook-driven; fixer flags only if something real appears. Draft-held; ready/merge OPERATOR-gated. Chain OPEN, fixer owns.