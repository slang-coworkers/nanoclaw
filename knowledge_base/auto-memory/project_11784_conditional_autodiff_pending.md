---
name: PENDING maintainer scope call — #11784 Conditional autodiff ICE
description: bwd_diff of Conditional<T,b>.get() ICEs (E99999); root-caused, held pending saipraveenb25's scope ruling — do not auto-release the fixer
type: project
originSessionId: 49818e1f-af98-4ea4-8b42-783f5e475896
---
shader-slang/slang **#11784** — `bwd_diff` of a `[Differentiable]` fn calling `Conditional<T,b>.get()` aborts with E99999, even with a concrete `true` flag and no generics. Bot-filed (nv-slang-bot) while investigating #11782.

**Root cause (triager source-traced + HEAD-verified `1a0c2a6d1`, NOT the reporter's guess):** Slang's autodiff transcriber wires the **`Optional`** intrinsic family but omits the parallel **`Conditional`** family. Inlined `Conditional.get()` → `GetConditionalValue` hits no case in `ForwardDiffTranscriber::translateInstImpl` (slang-ir-autodiff-fwd.cpp ~2600-2830), so `translateInst` raises InternalCompilerError at slang-ir-autodiff-fwd.cpp:3504 during the forward pass (before transpose). Triager corrected an earlier recall-subagent misattribution to transpose.cpp:1588 (that path is unreached). Classified Bug(ICE)/medium/autodiff/P2; GitHub labeled `reproduced`, Type=Bug; verified 5-bullet posted (issuecomment-4811289189).

**Distinct from #11782:** that's a *symbolic*-flag spirv-emit ICE (slang-emit-spirv.cpp:4832); this is a *concrete*-flag autodiff forward-pass ICE. Share only the `Conditional` type surface.

**Why HELD:** Maintainer szihs commented 2026-06-29 (issuecomment-4832315038, "Starting with @saipraveenb25 to take a look") — looped in @saipraveenb25, Slang's autodiff owner. The fix SCOPE is a maintainer call: full `Conditional` differentiability (mirror Optional-family handling across fwd-transcribe / transpose / primal-hoist + regression test) vs. clean diagnostic fallback in place of E99999.

**Current state (06-29, build-verified + publish-ready, PR HELD):** Fixer staged **A1 minimal-ICE-fix only** on `origin/fix/issue-11784` @ `9e6574e0c0` (single clean commit, **NO public PR**) — 2 forward-transcriber switch cases reusing the Optional op-generic handlers (`GetConditionalValue`→`translateGetOptionalValue`, `MakeConditionalValue`→`translateConstruct`) + `tests/autodiff/conditional.slang`. Has NOT touched the full-diff path A2 (`diff.meta.slang`/transpose/primal-hoist), per the hold. codex gate PLAN/CODE/OUTPUT all approve → **one `gh pr create --draft` from publishing.** Proven: ICE eliminated + gradients match on 2 scenarios (CPU); RED proven (revert→ICE, test load-bearing); regression optional.slang clean; full `tests/autodiff/` suite **837/837 passed, 0 failures, 286 GPU-ignored, no regressions**; clang-format clean. Caveat (triager-confirmed via the 286 GPU-ignored count): **CPU is the only target actually executed** — vk/cuda variants were IGNORED (no GPU in env), so the earlier "3/3 cpu/vk/cuda" framing overstates GPU coverage; proven core = CPU path. GPU exercise deferred to saipraveenb25/CI. Staged 5-part PR description exists (owner-deferred caveat + diagnostic-fallback documented), travels with the PR when/if published. **HOLD on public PR creation confirmed** — push within minutes only on saipraveenb25's signal he wants code OR operator go.

**Correctness — narrow claim VALIDATED, broad claim owner-deferred:** A1 reuses **Optional's** derivative handlers for **Conditional**. Triager source spot-check at HEAD: `Optional<T> : IDifferentiable` (diff.meta.slang:1392) but **Conditional has NO IDifferentiable conformance** → a differentiated Conditional is unrepresentable, so the `{primal,null}` diff-pair is *structurally forced*, NOT a silently-dropped gradient. That validates the narrow "no silent wrong gradient" concern I raised. The BROAD call (is reusing Optional's full fwd-transcribe handling correct for Conditional in *every* diff case?) remains genuinely owner-deferred to @saipraveenb25.

**How to apply:** Do NOT auto-release/dispatch the fixer to complete a fix, and do NOT publish the draft PR proactively. Release only on saipraveenb25's explicit scope convergence (A1 minimal vs. A2 full differentiability vs. diagnostic fallback) OR operator go. Chain topology orch → slang-triager → slang-fixer (triager owns the peer-wired fixer handoff — route through triager, never double-dispatch to fixer). Canonical thread `gh-issue-shader-slang/slang-11784`.
