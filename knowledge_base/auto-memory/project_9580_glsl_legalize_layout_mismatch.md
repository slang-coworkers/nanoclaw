---
name: project_9580_glsl_legalize_layout_mismatch
description: "slang#9580: GLSL varying-legalization crash (SLANG_ASSERT slang-ir-glsl-legalize.cpp:2166 structTypeLayout) when an entry point returns an associated type of an `export` struct — the entry-point result layout is never refreshed after link-time specialization. Bisected to PR #8603. ⛔ The earlier 'adopt contributor branch #10030' plan was REVERSED — that back-end/IR approach was rejected as wrong by construction; the accepted fix is front-end (resolveLinkTimeAssociatedType before the lookupExternDeclRefType sites), and the '#12131 WOULD_APPROVE, await merge' state is likewise SUPERSEDED. Current: PR #12131 non-draft, rework at ced217320c, reviewDecision CHANGES_REQUESTED (tangent-vector sticky), jkwak DEFERRED the issue ~2 sprints on 07-31. Spinoffs #12132 (makeStruct) and #12134 (base-interface residual)."
metadata: 
  node_type: memory
  type: project
  originSessionId: ae284e48-0735-45e1-95e5-8a218979832f
---

**07-17 — PR #12131 FLIPPED READY; approver R1 WOULD_APPROVE (CLEAN) ledgered.** Draft→ready re-trigger @ b9d1f8c39926. Approver ran /slang-pr-approve shadow: harvest exit 20 (bot-authored fixer branch — production review skips by design) → Devin-only tier, Devin CLEAN (0🔴/0🟡); clauses 6/6; adversarial challenger CLEAN across 5 probes (fix purely front-end = maintainer-demanded approach, `lookupExternDeclRefType` byte-identical fallback, base-interface transitive gap documented+tracked #12134 degrades to identical pre-existing crash = strict crash reduction never wrong layout, generic-test witness substitution load-bearing, recursion terminates by construction). Mode live_late (only human review = jkwak-work COMMENTED "am I on the right track?" clarifying Q — NOT changes-requested). Combined status "failure" is non-causal: test-falcor 1/110 D3D12 GBuffer image-diff (yesterday's draft run, re-aggregated) + SlangPy test_profiler CPU-timing (linux-only, green on Windows) — all core slang legs incl. CPU reflection (new gh-9580-* tests) + sanitizer/compile-regression/materialx GREEN on pinned SHA. Ledger-only, nothing posted, NEVER merge (maintainer-gated). NEXT HUMAN: jkwak merge decision. Chain closed at Main; reopen on merge or new substantive comment.

**✅ RESOLVED 07-16 — draft PR #12131 open, awaiting jkwak (assignee) review→ready-flip→merge.** Sibling approach: new front-end helper `resolveLinkTimeAssociatedType` before both `lookupExternDeclRefType` sites (slang-parameter-binding.cpp); reads export wrapper's `InheritanceDecl::witnessVal`, re-resolves assoc-type through SolidMode's own witnessTable → concrete `ColorOutput` (no witness synthesis). NOT the back-end IR rebuild that got #10030 rejected. base `master` ← `fix/issue-9580`, `Closes #9580` (closingIssuesReferences=[9580] confirmed). Verified: VARIANT 0 exits 0, SPIR-V byte-identical to VARIANT 1, reflection concrete; 2 new test files 4/4, reflection 41/41, bugs 635/635; codex approve; slang-reviewer request in flight. Contributor PR #10030 untouched (still open draft). **makeStruct split = issue #12132** filed + assigned jkwak-work (analyzeMakeStruct positional IRMakeStruct operand indexing, no bounds check), own triage chain. Fixer posted draft-PR 5-bullet on #9580 ([comment 4987700605](https://github.com/shader-slang/slang/issues/9580#issuecomment-4987700605)). GATE held: PR draft, jkwak owns ready-flip/merge; internal reviewer/codex approvals advisory only. NEXT HUMAN: jkwak reviews #12131 + triages #12132. Chain closed at Main; reopen only on new substantive comment.

**07-16 04:35 peer review — APPROVE_WITH_NITS, 0 bugs, core fix validated correct.** Fixer acted on 2 gaps + pushed; PR still DRAFT (head `0f165c901b` on `fix/issue-9580`, `Closes #9580` intact, #10030 untouched). Generic coverage added + proven load-bearing via revert-drill (codex rejected an earlier non-exercising test). **⚠ Known separable residual (out-of-scope, documented):** same crash class recurs if the assoc-type is declared on a *BASE* interface (needs transitive witness composition) — fixer scoped #12131 to the directly-conformed case (exact #9580 repro) and documented the base-interface case as explicit follow-up (code comment + PR body). Worth jkwak knowing at review time; candidate for a later separate change. Devin/Reviewer-B can re-run once non-draft.

**07-16 04:40 review-side chain CLOSED.** Reviewer re-verified follow-up delta → APPROVE_WITH_NITS holds (0 bugs). Fixer + reviewer work all complete; only jkwak review/merge remains. PR #12131 head `b9d1f8c399`, still DRAFT, `Closes #9580` intact; suite 637/637; codex approve every stage. **THREE artifacts, all assigned jkwak-work (verified):** (1) **PR #12131** = the fix (scoped to directly-conformed #9580 repro), (2) **issue #12132** = makeStruct IR-robustness follow-up, (3) **issue #12134** = base-interface transitive-witness gap, now a tracked issue per reviewer suggestion (cross-links #9580 + #12131) so the documented limitation isn't lost. NEXT HUMAN: jkwak reviews/ready-flips #12131 + triages #12132 + #12134. Chain webhook-driven; reopen only on jkwak review/merge or substantive human comment.

**07-31 — MAINTAINER DEFERRED ~2 sprints ([comment 5146029376](https://github.com/shader-slang/slang/issues/9580#issuecomment-5146029376)).** jkwak-work on #9580: *"This issue turned out to be a tricky one. I am pushing the issue by two sprints and I will come back later."* = scheduling deferral, NOT rejection/change-request. Fixer's rework (PR #12131 @ `ced217320c`) STANDS and waits — fresh maintainer review won't come near-term (~2 sprints out from 07-31). Chain state: rework-pushed → maintainer-deferred/parked. Routed to slang-triager on canonical thread (owns fixer edge + GH-posting call); triager to (a) decide brief GH handoff ack via closest-to-the-state, (b) inform fixer review is deferred so it doesn't expect a near-term verdict. RESUME = jkwak returns (~2 sprints) OR fresh substantive human comment. Not terminal; webhook-driven, longer runway now.

**07-31 — CHANGES_REQUESTED rework LANDED (fixer report, HEAD `ced217320c`).** Fixer addressed both jkwak + Tess (tangent-vector). Ask #1 DONE: collapsed the duplicated resolve-then-fallback at both varying-layout call sites into one named bottleneck `tryResolveAllLinkTimeTypesInDeclRef` (Tess's suggested name); renamed assoc-type helper → `resolveLinkTimeWrapperMemberType` (member-generic) — behaviour-preserving restructure of the already-approved fix. Ask #2 (systematic generalization) DEFERRED as on-thread boundary questions rather than built speculatively: deeper SubstitutionSet-carrier generalization + base-interface case (#12134) posed to maintainers on-thread (replies r3687781812/r3687781930, PR body rewritten). Tests: 4 gh-9580 tests 8/8 (added `-nested`), reflection 41/41, bugs 639/639; codex CODE_REVIEW + OUTPUT_REVIEW approve. Declined codex's "just use normal resolve()" with a code trace (resolve() reads the wrapper's abstract witnessTable, not the concrete `witnessVal`). PR non-draft; `pull_request` CI auto-running. **NEXT HUMAN: fresh maintainer review** (jkwak + tangent-vector). Fixer owns PR (verify+relay only, never auto-merge); CI/review verdicts webhook back to fixer. Triager forwards terminal [Triage Resolution] on merge. **Triager-verified against live GitHub 07-31:** OPEN, non-draft, MERGEABLE, HEAD `ced217320c` (matches fixer report), `reviewDecision=CHANGES_REQUESTED` (tangent-vector's prior review still sticky pending his re-review). Real transition (holding → rework pushed) but NOT terminal — triager sent no outbound + refreshed no GitHub verdict (non-draft PR is its own observability surface); forwards only on terminal (re-approve+merge, fresh CHANGES_REQUESTED needing a decision, or a blocker).

**07-24 — PR #12131 moved LGTM-hold → CHANGES_REQUESTED (not a conflict, an emphasis-divergence).** Triager-verified on GitHub: non-draft, MERGEABLE, `reviewDecision=CHANGES_REQUESTED`. **tangent-vector** (MEMBER, architecture authority who shaped the front-end approach) filed changes_requested 07-24 18:50Z (review r3647578389); **jkwak-work** stays COMMENTED "LGTM but discuss before merge." Two asks: (1) extract the duplicated two-step resolve into one named bottleneck `tryResolveAllLinkTimeTypesInDeclRef()` — both maintainers want it, fixer committed; (2) generalize into a comprehensive recursive resolver covering every decl-ref shape depending on a link-time param (assoc-type lookup, generic specialization w/ link-time args, nesting) — tangent-vector "not sure the exact shape needed" = **preventive/systematic, not live crashes**. Fixer's codex-verified empirical finding: only live-reachable crashes are direct assoc-type-of-export (this PR) + base-interface variant (#12134); generic/nested shapes probe CLEAN today. Recursion primitive (`substitute`/`resolve`) exists; missing piece is a hook to inject link-time *type* knowledge. Fixer replied on-thread (agreeing bottleneck refactor, sharing crash-vs-clean findings), asked tangent-vector to steer the systematic routine's shape + whether to build the full workhorse in *this* PR vs land bottleneck now + track generalization as follow-up. **NEXT HUMAN: none** — fixer holding code work pending tangent-vector shape sign-off (won't build comprehensive resolver speculatively → avoids unprincipled churn). Fixer owns PR (verify+relay only, never auto-merge). Webhook-driven; triager forwards terminal resolution on rework-land + merge. My prior "WOULD_APPROVE await merge" state is SUPERSEDED — PR is in active rework, not merge-ready.

**07-17 23:15 maintainer LGTM, merge held for discussion.** jkwak reviewed #12131 himself, **flipped it ready-for-review by his own hand** (isDraft=false verified, head `10f941809c`) — draft-hold guardrail now satisfied the sanctioned way (maintainer, not bot). Resolved all his inline threads; fixer answered his 4 background questions. Left review **COMMENTED: "Looks good to me. But I like to discuss more before merging it."** reviewDecision=REVIEW_REQUIRED (COMMENTED ≠ APPROVED) → explicitly holding merge by choice. Non-draft auto-triggered `pull_request` CI (fixer did NOT manual-dispatch, per drafts-only rule). Triager refreshed #9580 issue comment in place (edit-if-self) dropping stale "held in draft" language. Fix accepted in principle; merge is jkwak's call, held pending his discussion points. Won't nudge/merge. GATE intact (COMMENTED = advisory, not merge authorization).

---
_history below_

#9580 — GLSL varying legalization crash. Maintainer jkwak-work (assignee) asked bot to triage + check repro on 07-09.

**Triager verified (empirical, 3-way):** still reproduces at ToT `d8e8e1a9e` — VARIANT 0 (assoc-type-of-`export`-struct entry-point return) → `SLANG_ASSERT slang-ir-glsl-legalize.cpp(2166): structTypeLayout` (Debug) / segfault in `createGLSLGlobalVaryingsImpl` (Release). Mechanism = type⇄layout mismatch: post-link return type resolves to concrete `%ColorOutput` but **entry-point result layout never refreshed after link-time specialization** → concrete struct paired with stale opaque assoc-type layout → null structTypeLayout. VARIANT 1 (direct-extern) works because `lookupExternDeclRefType` refresh fires there.

**Bisected to PR #8603** (symbol-alias link-time types). Contributor already prototyped principled **producer-side** fix on branch `fix/link-time-entrypoint-layout` (+5 unit tests) and offered to PR.

**Proposed PR:** #10030 (draft, h3r2tic) **= the `fix/link-time-entrypoint-layout` contributor branch** originally flagged. On 07-09 jkwak-work asked bot to read two PR #10030 comments — [review 3982976240](https://github.com/shader-slang/slang/pull/10030#pullrequestreview-3982976240) + [comment 4452316797](https://github.com/shader-slang/slang/pull/10030#issuecomment-4452316797) — and **propose a solution conforming to the review feedback**.

**⚠️ CORRECTION (reverses earlier "adopt the branch"):** PR #10030's approach was **rejected**. tangent-vector (CHANGES_REQUESTED): PR is "wrong by construction" — adds new layout logic at **IR/back-end** level, but core layout is **AST/front-end** level. Fix must make front-end layout/binding logic account for link-time specialization (so reflection is correct too), not kludge in the back end. csyonghe concurs: "a front-end resolution step before generating the entrypoint layouts."

**Exact site (triager-traced):** `slang-parameter-binding.cpp:2739-2749` already resolves a *directly*-extern result type via `lookupExternDeclRefType` (why VARIANT 1 works). Gap: an *associated type of* an export struct (VARIANT 0) isn't resolved through the export's link-time binding there. Conforming fix = front-end resolution of that case.

**07-09 maintainer green-light + clarity ask** ([comment 4940132728](https://github.com/shader-slang/slang/issues/9580#issuecomment-4940132728)): jkwak — *"Let's go with what you recommended; but I am not sure what you are suggesting exactly. I will keep it in mind when I review the PR once available."* = (1) DESIGN GREEN-LIGHT on the front-end fix, (2) proposal wasn't concrete enough — wants clearer restatement, (3) expects a DRAFT PR he'll review/merge himself. Not a park; fixer may build the front-end fix as a DRAFT PR. Ready-flip/merge stays jkwak's (his own words).

**07-10/11 fixer posted clarification** (with sibling approaches + a `makeStruct` angle — details in fixer's #9580 clarification comment; Main did not fabricate these terms).

**07-11 maintainer decision** ([comment 4975514968](https://github.com/shader-slang/slang/issues/9580#issuecomment-4975514968)): jkwak — *"Let's go with the sibling approach. Let's create a new issue about makeStruct; and assign it to me."* Two directives: (1) proceed with the **sibling approach** for the #9580 fix, (2) **file a NEW issue about `makeStruct`, assign to jkwak-work**. Forwarded verbatim through triager → fixer (fixer holds the sibling-approach/makeStruct context from its clarification comment).

**Fixer's concrete interpretation (triager-confirmed 07-15):**
- **Sibling approach** = new helper `resolveLinkTimeAssociatedType` called *before* the two `lookupExternDeclRefType` call sites (slang-parameter-binding.cpp:2739-2749) — NOT extending in place — keeping the working direct-extern (VARIANT 1) path untouched. Witness-sourcing subtlety to be called out for jkwak's review.
- **makeStruct issue** = split the empty-`IRMakeStruct` hunk (`slang-ir-typeflow-specialize.cpp`) into its own issue assigned to jkwak-work, cross-linked to #9580 + referencing #10030, kept OUT of the #9580 PR.

**State:** OPEN. `reproduced` label applied (human-set regression/Dev-Reviewed/Type=Bug untouched). Verdict posted [comment 4920403540](https://github.com/shader-slang/slang/issues/9580#issuecomment-4920403540). Fixer to: (a) implement sibling approach as DRAFT PR (verified vs repro + regression test, `Fixes #9580`, `report_pr_created`), (b) create the new `makeStruct` issue assigned to jkwak-work. Final approach/merge is jkwak-work's call (assignee). Drafts-only + ready-flip/merge/close gated; won't edit/close contributor's PR #10030; new-issue creation is maintainer-directed (posts freely). Triager owns the fixer edge — do NOT double-dispatch. Canonical thread `gh-issue-shader-slang/slang-9580`. Awaiting fixer's draft PR # + new makeStruct issue # → triager forwards up.

**07-16 jkwak nudge** ([comment 4986921336](https://github.com/shader-slang/slang/issues/9580#issuecomment-4986921336)): *"can you review my previous comment?"* pointing back at his 4975514968 decision. Read as: ~5 days silence on the issue, wants bot to CONFIRM its understanding of the sibling-approach + makeStruct directive AND give status. As of triager 07-15, fixer had NOT yet reported draft PR # or makeStruct issue #. Routed through triager → chase fixer status + post GitHub confirmation/status for jkwak (closest-to-the-state). Do NOT fabricate PR/issue existence.

**07-16 fixer-reachability blocker — RESOLVED (transient, NOT auth outage).** Triager reported slang-fixer "not addressable via SendMessage." Main diagnosed: (a) triager→fixer destination edge INTACT (row present → ag-1780667166439-vmjrwe); (b) fixer group HEALTHY — many active/running sessions, newest ~1 min old, working other chains (11083/12122/9153/12046). NOT the 07-14 [[project_slang_fixer_auth_outage]] pattern (no "Not logged in"). Transient in-container SendMessage glitch; triager's message-block fallback delivered (fixer posted a compaction notice = session active). No re-spawn/re-wire/operator-escalation needed. **Triager status/GitHub reply posted** [comment 4986948079](https://github.com/shader-slang/slang/issues/9580#issuecomment-4986948079): honestly states neither deliverable landed yet (no #9580 draft PR — only rejected #10030; no fix/issue-9580 branch; no makeStruct issue). Awaiting fixer's real deliverables: sibling-approach draft PR # + makeStruct issue #.
