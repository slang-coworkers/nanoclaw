---
name: slang-slangpy-tooling-chains-index
description: Live slangpy and approver/gate tooling chains moved out of the index at the 08-04 compaction floor. Each row keeps its RESUME trigger verbatim.
metadata: 
  node_type: memory
  type: index
  title: SlangPy + tooling live chains (spilled from MEMORY.md)
  originSessionId: fa2a5394-3823-4780-87fa-cc306bdc4156
---

# SlangPy + tooling live chains

Spilled from `MEMORY.md` 2026-08-04 because the index hit its read limit. **These are LIVE, not parked** —
each row carries its own RESUME trigger. Open this file whenever you would have scanned the index's
Live-chains section for slangpy or tooling work.

- ⛔**[[project_slangpy_820_tagged_kernel_dispatch_segv]]** — LIVE: functional API + a `[shader("compute")]`-tagged kernel → **rc=139 SIGSEGV in TARGET CODEGEN AT PIPELINE CREATION** (`calldata.py:524` → `:318`; ⛔**"at dispatch" RETRACTED** — `defer_target_compilation` defaults `True` at `:513-515`, so the log prints `Dispatching…` before the deferred compile faults ⇒ **a log line is not a program counter**), CUDA **and** Vulkan, 6/6 @ `507b4cf1`; `.dispatch()` with the same kernel is rc=0. ⛔**`E38040`-explains-it also RETRACTED** — incidental; with a semantic-carrying param there are **zero diagnostics** (positive-controlled vs `E30015`) ⇒ silence points UPSTREAM. Published as amendment 4 on slangpy#768 cmt `5196679064`. Live owner **@ccummingsNV** ⇒ fixable regardless of the retire-vs-keep ruling. **RESUME = the native backtrace** (decides slangpy-side guard vs. upstream Slang companion issue) + the untested `[CUDAKernel]` Arm-A variant. ⚠️**Evidence is ONE L40S — replication (6/6) is NOT a second case, and that limit is NOT stated in the public comment; neither I nor the triager has a GPU.** ⭐Cite `calldata.py` **0** `.entry_points` refs vs. `dispatchdata.py:84-87` (the only one) — NOT "generator.py:768 is unconditional" (it's gated at :767).
- **[[project_slangpy_1089_shader_cache_path_vulkan_segv]]** — `shader_cache_path` SIGSEGV on 1st pipeline creation (Vulkan), P1 regr. **08-05 MAINTAINER-OWNED**: `kaizhangNV` assigned by jkwak-work (no bot mention ⇒ awareness only); issue OPEN, **root cause STILL UNLOCATED**. ✅**Boundary MINE-VERIFIED — NOT an rhi regression**: `vk-pipeline.cpp` blob-SHA **identical** across the 0.36→0.37 pin move; slangpy `persistent_cache.h` 404@`v0.36.0`/200@`v0.37.0` ⇒ 0.37.0 *turned on* a latent path. ⭐Branch 1 (null optional proc) **REFUTED BY EXECUTED TEST** 3/3 — proc `(nil)`, faults at the **FIRST** call `:170`, `RIP=0x0`, no frame — vs reporter's **named** frame at `:178` (the 2nd call) ⇒ different signature. Branch 2 (driver-side handling of the `pNext`-chained create-info on the 2nd key query) survives, unconfirmed. rhi **[#808 MERGED](https://github.com/shader-slang/slang-rhi/pull/808)** (cache-blob length/offset validation) + **[#809 draft](https://github.com/shader-slang/slang-rhi/pull/809)** (Approach A gate fix) — ✅both carry a #1089 note, **NEITHER carries `Fixes`** (the guardrail survived 4 rounds into 2 public artifacts unprompted); residual **serializer size arithmetic** left out of scope. ⚠️**Reporter never ran the 1-line discriminator — 2 days silent** ⇒ a gate on a reporter's reply needs its own resume path. ⚠️I learned of both PRs by LOOKING, not from a `[Fix Report]`. ⚠️`v`-prefix trap: bare `0.36.0` 404s because the TAG is absent, not the file. ✅**CLOSED at both tiers 08-05 22:16** — delta comment [`5198010118`](https://github.com/shader-slang/slangpy/issues/1089#issuecomment-5198010118) posted for the new assignee (**fresh, not a PATCH — a human had commented after us; edit-if-self applies only while yours is the last word**), telling him the comment above it is stale. ⭐**Deliberately unlinked work is UNREACHABLE work**: withholding `Fixes` was right, but it left the issue pointing at a hypothesis we'd already killed ⇒ if you withhold the link you owe the issue a prose pointer. ✅Triager's extra check I'd MISSED: #808's clamp is on **`main`**, not merely merged (`:355-358`, before the `memcpy`) — **"merged" ≠ "present on main"**. **RESUME** = reporter answers `has_feature` · kaizhangNV engages · `Fixes #1089` appears (esp. on a **successor** PR after everyone stopped watching) · #809 merges (verify #1089 stays OPEN). Lessons: [[feedback_mechanism_must_predict_observed_coordinates]], [[feedback_published_negative_env_claims_need_rederivation]], [[feedback_a_gate_on_someone_elses_reply_needs_its_own_resume_path]].
- ⛔**slangpy#768 / #832 / #844 scrub batch (2026-08-05) — all three ANSWERED on GitHub**, verdicts `5196679064` / `5196131630` / `5196607327`; no state/label/assignee touched. **7 sub-issues, not 4**: #819 closed/completed, **#820+#821 → @ccummingsNV (LIVE)**, #822+parent → departed mkeshavaNV, plus #832/#844/#806. ⛔**#807 MUST NOT BE CLOSED** — sub-task of **#806**, awaiting @szihs's A/B/C pick (our own triage `4660823599`, 2026-06-09), and its 0-D guard is **"incidental — not required"**, NOT a blocker (I asserted "live blocker" twice — wrong). **Open item: maintainer ruling retire-vs-keep on `.dispatch()`** — now gates only `dispatchdata.py` deletion + the "port items 2-3" wording. Method lessons: [[technique_scrub_a_checklist_issue_per_item_per_path]], [[feedback_a_ci_job_name_scan_is_blind_to_test_steps]].

- 🔴⭐⭐**[nanoclaw#1066 kb-fold-bounded — MERGED `c2a7639`; MY REVIEW LANDED 26s LATE; defect LIVE on `nv-main`, FOLLOW-UP PR OWED](project_nanoclaw_1066_kb_fold_bounded.md)** (cmts `5179404594` + post-merge `5179614225`) — ⛔⭐⭐**a "ready for review" webhook is a claim about a MOMENT, not a lock — RE-CHECK `merged`/`state` BEFORE writing a verdict; if it merged, re-verify against the MERGED TREE and say so** (a finding under a merged banner is easy to miss). ✅Re-verified on merged code: `2/2 live (1 superseded)` → daily `build` → **marks surviving 0** → `2/3 live (0 superseded)`. ⚠️**`/tmp` is WIPED BY A CONTAINER RESTART** — my re-run silently lost its fixture and printed `0/2`; ⭐**a rebuilt harness returning a DIFFERENT number means the HARNESS broke, not the finding.** RESUME=**szihs accepts the offer ⇒ open follow-up PR** (preferred: persist retirement OUTSIDE the regenerated tree, keeping L1-immutability intact). — szihs bounds the learnings-wiki fold (touches **my own spine** + the Main-write-only KB ⇒ I am closest-to-the-state). ⛔**nanoclaw has NO `*-pr-approver`; slang/slangpy's are REPO-SCOPED ⇒ handle nanoclaw PRs MYSELF, never dispatch there.** ⛔**MINE-MEASURED 08-04: `gh pr review --request-changes` → `Resource not accessible by integration (addPullRequestReview)` while perms `{admin,maintain,push,triage}:true` AND `x-ratelimit-limit:5000` both read GREEN ⇒ `permissions` is NOT evidence about which VERBS an integration may use** (same family as endpoint-split); ✅**post an ISSUE COMMENT instead — don't burn a round-trip on `--request-changes`.** ⭐⭐Blocking finding: `superseded_by` is wiped by every `build()` (deletes all of `WIKI` except `concepts/`) and the daily order is `build`→synthesize→`finalize` ⇒ retirement works **ONCE**, between two always-paired commands, then reverts — **GREEN-LOOKING**, since `0 superseded, excluded` cannot distinguish "none retired yet" from "not persisting". L1-placement DOES persist but contradicts the same diff's own `immutable; never edit` (:22 **and** :137) and matches in the page **BODY** via `fm()`'s `re.M` **by accident, not design**. ⭐**the fenced SKILL.md block ≠ the deployed `.learnings_wiki.py`** (14,108 vs 16,590 B, different sha256) — wipe present in BOTH so the finding holds, but **never assume fence==live**. ⭐Also posted: cap+spine land before any page satisfies them (**live: 47 concept pages, 0 with a `## TL;DR`**; the spine's `limit=60` takes effect next container wake ⇒ asked for "TL;DR if present, else widen"). ⚠️Outlives the PR: `slangpy-pr-approver` SHA-256-attests two 178/180 KB pages it has seen 37%/50% of. RESUME=**szihs answers the persistence question or pushes a fix** → re-review.
- [spy#1072/#1073 profiler flush barrier](project_slangpy_1072_profiler_drain_snapshot_race.md) — **COMPLETE** (2 suggestions + 3 `end_zone` defects, revert-verified). ⭐**`GH_TOKEN` 401 RETRACTED — asserted live 6×** ⇒ re-probe capability-negatives every round. RESUME=`ci` green **on the head under review** → approver + reviewer
- ✅[spy#1074 onboard repo to PR dashboard](project_slangpy_1074_onboard_pr_dashboard.md) — **RESOLVED/closed 08-04 by jhelferty's OWN PR #1084** (board-sync callers → org project #10, merged 15:11:25Z). Was option **(c)** — a board **NEITHER triager NOR I had enumerated**, so our "(a)/(b) already covered" was true **and irrelevant** ⇒ ⭐⭐**enumerating the dashboards you know does not license "those are the options"** (same family as the ENUMERATE-over-consumers defect). ✅Verified: old `add-pr-to-project.yml` retired, 6 caller runs all `success`, **org secret resolves** — decisive: guard step `Note skipped (no token)` was itself **SKIPPED** while `Reconcile board` **RAN** ⇒ ⭐⭐**caller `success` ≠ effect happened; read STEP outcomes, and prefer a guard whose skip/run polarity is OPPOSITE what you're confirming.** ⛔**Board contents UNREADABLE from my seat: `projectsV2 totalCount`=0 is INSTRUMENT FAILURE — project #10 provably exists (named in this repo's own live workflow) yet the same token 404s it; `GH_TOKEN` lacks project scope** ⇒ never report slangpy tile state from here. RESUME=**none (closed)**; optional backstop = nightly sweep's first fire ~08-05 07:00Z (0 runs yet, cron not reached)
- [spy#1090 Metal `create_buffer_from_native_handle`](project_slangpy_1090_metal_buffer_from_native_handle.md) — R1 **ABSTAIN_POLICY `OPEN_GAP`** @`5c384a20b11b` (G1 zero executing coverage · G2 Vk/D3D12 no size-check), nothing posted. 🔁**SYNCHRONIZE 08-05 → RE-DISPATCHED, debounce correctly DECLINED**: head→`bb870c1750cc` (ahead 1) and the delta IS the two premises (+74 NEW `test_buffer_from_native_handle.py` · `resource.cpp` +31 not-impl + handle-type `SGL_CHECK`s). 🔴**HUMAN `CHANGES_REQUESTED` ccummingsNV 08-05T11:42Z in the window** (asks: throw not-implemented + add a test) → author pushed 12:37Z in response ⇒ ⭐**the debounce rule's load-bearing job paid off exactly as documented — a diff-only check would have buried a human blocker**. ⚠️**G1's premise MOVED but is NOT closed**: MINE-VERIFIED `ci.yml:219` Unit-Tests-(Python) is **NOT** macOS-excluded (unlike `:205`/`:164`), both macOS rows carry `unit-test`, and `helpers.py:43-44` darwin ⇒ `[DeviceType.metal]` ⇒ test parametrizes to Metal. ✅**R2 FINAL = BLOCK `VERIFIED_BUG:vulkan_import_undefined_state`** @`bb870c1750cc`, policy `v0-shadow-wide` (⚠️mounted policy changed mid-chain, approver VERIFIED not assumed), nothing posted. CI SETTLED 18/18, 4 failure. 🔴**G1 CLOSED ON TARGET: MINE-VERIFIED job 92307324662 `PASSED …[DeviceType.metal]` + `_invalid[metal]`, `1771 passed` — the test EXECUTES on Metal, rhi#802 pattern does NOT apply** (answer went AGAINST my emphasized skip-risk). ⭐⭐⭐**MY DISCRIMINATOR WAS RIGHT — approver WITHDREW its `vkMapMemory` root cause** (codex: `to_numpy()`→`readBuffer()` maps a SEPARATE staging buffer, never the imported `m_memory` ⇒ could not have fired); real defect, wrong causal role, recorded as **real-but-unfired**. ⇒ ⭐⭐⭐**A MECHANISM THAT CANNOT EXPLAIN WHY THE PASSING CASES PASS IS NOT A ROOT CAUSE YET** — the per-device cut forced the real boundary: MINE-VERIFIED `fixupBufferDesc` present in metal `metal-buffer.cpp:141` + wgpu `:115`, **ABSENT** in vk `vk-buffer.cpp:441-456` and d3d12 `d3d12-device.cpp:1588-1603`, d3d12 survives via `d3d12-utils.cpp:312` `Undefined→COMMON`. ⚠️approver **refused to round up — story does NOT explain cuda** (passes via `pytest.raises("not implemented")` pre-import). FIX=call `fixupBufferDesc()` in vk import or set non-`Undefined` `default_state`; `m_memory` init = follow-up NOT required to clear. Block is **narrow, not a rejection** — both human asks met. ⭐**my 4th null-result miss: grepped `d3d12-buffer.cpp`, empty — import path is in `d3d12-device.cpp`; a zero in the WRONG FILE == absent code.** ⚠️`codex-reply` can't carry developer-instructions ⇒ a stage re-review sent as a REPLY is **silently not recorded** (gate-relevant). 🟢**R3 SYNC 08-07 — BLOCK'S FIX LANDED, re-dispatched**: head→`eca1dc49e1eb`, delta = **LONE GITLINK** `11eefdc6`→`5f00bdc5` (+1/-1) ⇒ ⭐**the D3 shape carrying the REMEDY — a gitlink-only delta must not be debounced as trivial in EITHER direction**. MINE-VERIFIED `vk-buffer.cpp`@`5f00bdc5` blob `eda0548ead59` (was `3318cadb8cd8`), `fixupBufferDesc` **1→2**: `:340` createBuffer + **`:443` createBufferFromNativeHandle** = exactly option 1 of the fix guidance, closing the intra-file asymmetry. Provenance: **slang-rhi#813 MERGED 08-07T14:30:33Z, merge_commit `5f00bdc50f1f`** == the new pin ⇒ fixed at the RIGHT LAYER. 3 non-bot inbounds read: author answers ccummings (d3d/vk/webgpu done; **cuda+cpu have NO native-handle impl**), author names #813 as the failure cause (same root cause as the BLOCK), **NEW 3rd party `guoxx` 08-06 confirms Vulkan works, wants NPU interop — corroborating**. ⚠️ccummingsNV's CHANGES_REQUESTED still OPEN, pinned 2 heads stale @`5c384a20b11b` — only he can dismiss. ⛔UNSETTLED: does `[DeviceType.vulkan]` now PASS (read pytest summary BY NAME, not job conclusion) · Metal still green · `m_memory` follow-up still open? CI at dispatch 14/14: 2 success, **8 in_progress + 4 queued**. ✅**R3 FINAL = BLOCK CLEARED → ABSTAIN_POLICY `OPEN_GAP`** @`eca1dc49e1eb`, nothing posted, head re-pinned after recording. 🟢**MINE-VERIFIED the deciding question: `[DeviceType.vulkan]` PASSED BY NAME on ALL 4 legs that crashed at R2** (92898524558/634/522/563), `crashed`=0 on each vs controls 4148/4187/5689/5749; Metal still PASSED+`_invalid` (92898524698). **Pre-registered bar met 4/4, nothing renegotiated** — ⭐⭐**pre-registering the pass rows BEFORE the legs land converts a post-hoc reading into a test**, the direct remedy for state-5. ⭐`4139→4148` = **+9 passes / +8 COLLECTED** (R2's total included a failure — approver caught its own draft error); **a pass-count INCREASE beats a green conclusion, it shows tests were GAINED**. 🔴**Abstain is on a CARRIED-OVER gap, not the fix**: `device.h:404` promises *"size must not exceed the native allocation"* and **MINE-VERIFIED only metal enforces it** (`desc.size > nativeBuffer->length()`); **vulkan + d3d12 + wgpu = 3 of 4 never check size**, all Python-reachable — approver had 2 of 4 and **corrected itself UPWARD, it had missed wgpu**. ⭐⭐⭐**PROCESS FINDING TO PROPAGATE: ASK FOR BOTH DIRECTIONS BY NAME.** OUTPUT_REVIEW took **6 must-fix rounds**, 3 of them the SAME overclaim leaking one abstraction level at a time (stale fact→unverified inference→categorical claim) because after each fix it **grepped its own NEW WORDING instead of the CONCEPT**; the wgpu gap surfaced only when it explicitly asked *"did I UNDER-claim anywhere?"* ⇒ **narrowing rounds bias toward under-claiming while reviewers optimise for overclaims, so under-claiming has NO natural detector.** ⚠⚠Approver withdrew a relayed subagent *"byte-identical finding set"* (hashes differ, uncheckable) ⇒ **relaying a subagent's verification as your own is the same laundering as a peer's**; and harvest exit 10 **+ Devin's 1st run BOTH STALE on a revision whose entire delta IS the gitlink** — re-ran, head-current, but **establishes head-currency + unchanged counts ONLY** (submodule covered solely by source reading + CI). ✅**CORRECTION TO ME, re-verified: ccummingsNV's review is 2 heads stale, NOT 3** — `compare/5c384a2...eca1dc4` → `ahead_by: 2`; the PR has had 3 HEADS but 2 MOVES since the reviewed one. I said 3 twice ⇒ ⭐**"N stale" is a DISTANCE, not a population count — run `rev-list --count`.** 🔁**R4 08-08 REBASE → ABSTAIN_POLICY `OPEN_GAP` @`f906a11983f8`, R2 BLOCK stays cleared.** ⛔⭐⭐⭐**ON A REBASE THE OLD-HEAD COMPARE IS THE WRONG INSTRUMENT**: `compare/eca1dc49...f906a119` = **`diverged`, ahead 6/behind 3, 22 files** incl. 7 `.github/workflows/*` + `external/slang-rhi` — **all upstream `main` traffic**; the PR's OWN diff (`pulls/1090/files`, base…head) is **6 files/+189−29**. ⭐**Use `pulls/N/files` or the merge-base, NEVER `compare/<old-head>...<new-head>`** — and ⭐**the vulnerable STEP is a human hand-feeding the diverged list; `eval-clauses.py` is ALREADY correct** (diffs `base_ref...sha`, reported 6 paths/218 lines) ⇒ name the step, not the tool. 🔴**GITLINK BUMP LEFT THE PR AND THAT IS *NOT* A REVERT** — MINE-VERIFIED 3 ways: head pin `8ffe21c501b2` **== main's pin** (landed upstream independently), `compare/5f00bdc5...8ffe21c5` **ahead_by 1 ⇒ #813 is an ANCESTOR**, and `vk-buffer.cpp` blob **`eda0548ead59` identical to R3** with `fixupBufferDesc` at `:340`+`:443`. ⇒ ⭐⭐⭐**A file LEAVING a PR's diff reads as *reverted* OR *merged upstream* — only the BASE's value distinguishes them** (mirror of D3: the gitlink hid an arrival, its absence hides that nothing changed). 🟢Pass set HOLDS across the rebase, MINE-VERIFIED by test name, `crashed`=0, counts identical to R3: 93075061205/236 `4148`, 93075061194/171 `5689`, macOS 93075061188 `1772` (`PASSED …[metal]`); bar **pre-registered again before reading**. Gap UNCHANGED (re-grepped all 4 import paths at the NEW pin; **#814 inert** — CUDA capability files, `createBufferFromNativeHandle`=0); `m_memory` still uninitialized. ⚠**CORRECTION TO ME: 22 files, not 24 — I printed the list then stated an uncounted number**, the same ⭐**"count the hits"** half I'd corrected the approver on one round earlier. ⭐⭐**Devin's staleness discriminator INVERTED**: with no gitlink in the PR, *rendering one proves staleness* (its group 4 = `slang-rhi +1/-1`, 7 files vs true 6) ⇒ **a discriminator's polarity is a function of the REVISION, not a fixed property**; approver instead measured **per-file blob SHAs across the rebase** (4 files byte-identical ⇒ findings apply verbatim) ⇒ ⭐**blob-SHA equality is the right instrument for "does a prior finding survive a rebase"**. 🔴**CodeRabbit's new 🟠Major belongs to #1094, NOT this PR** — MINE-VERIFIED: this PR's only `device.cpp` hunk is `@@ -578`; `31a351726ec2` (#1094) owns `@@ -116,8 +116,16` = the flagged `create_directories`-outside-try at `:119-128` ⇒ ⭐**a rebase makes a bot attribute UPSTREAM code to the PR that merely rebased onto it**. ⭐⭐Approver's own catches: wrote **"Blocker: None" on an ABSTAIN** (an UNDER-claim — the direction with no natural detector) and found its concept-grep leak one level deeper (**kept `grep`, so a newline-wrapped claim slipped**) ⇒ **fixed the TOOL CLASS with a multiline whitespace-insensitive matcher, not the instance — the artifact-over-noticing rule paying out**. RESUME=**human looks at the 3-of-4 size gap** (fix the 3 rhi paths, or validate once in SGL, or downgrade the doc promise) · ccummingsNV dismisses his own CHANGES_REQUESTED (only he can) · `m_memory` follow-up (#813 doesn't touch `vk-buffer.h`) · ⚠**#808 `fcbacea7433b` arrives via this bump ⇒ evaluate on the [[project_slangpy_1089_shader_cache_path_vulkan_segv]] thread, NOT here**
- 🔧⭐⭐⭐[Approver-pipeline defects — D1pt2 **AND** "D2 fired" both RETRACTED; D2 stays LATENT](project_approver_pipeline_defects_devin_fetch_ci_green.md) — spy#1090, fail TOWARD clean/green. ⭐⭐⭐**agreement isn't corroboration when the peer's source is ME** · **the counterfactual IS the test** · **a null result doesn't name its own cause**. 🔴**3 ROUNDS ON ONE FACT, resolved as DIFFERENT FILES both read correctly**: my disk's BUNDLED policies = `require_ci_green:true`; its RUN loaded a MOUNTED `v0-shadow-wide` = **false** ⇒ clause took the `:184` skip, **never queried the status API**, certified nothing ⇒ ⛔⭐⭐⭐**"every policy on my disk says X" NEVER settles what a RUN did — only the run's loaded policy does** (I let a true claim about *bundles* speak for the *run*; my "inert" framing was RIGHT and my self-retraction was the error). ⚠️**I CANNOT verify its `clauses.json`/loaded policy — different filesystem** (`work/1090-*` absent here). ✅**SURVIVING defect (mine, independent): identical `status:"pass"` at `:184` AND `:190`, only `evidence` differs** ⇒ a consumer keying on `status` can't tell "checked, green" from "never checked" — **fix = distinct `skipped` vs `pass`**. D2 **untested, not disproven**; `:183` defaults True ⇒ **a MISSING policy file activates it**. ⭐⭐⭐**3rd no-counterfactual instance in 2 days and the worst — the artifact stated its own cause in a field already open**; it felt safe because it **confirmed a just-filed prediction** (⇒ **a prediction confirmed by a REINTERPRETED observation is NOT confirmed**) and was **packaged as correcting a peer for being too lenient** ⇒ ⭐⭐⭐**self- and peer-criticism both FEEL like skepticism while functioning as confirmation bias**; ⭐⭐**an unnecessary retraction is a real error, not humility**
- 🔧⛔**[`[GATE AUDIT]` "codex never invoked" — BOTH my claims WRONG](project_gate_audit_shared_jsonl_mtime_race.md)** — emitter is an **MCP tool, NOT a hook**; cause = mtime pick over a shared jsonl dir. ⭐⭐**find the EMITTER before naming the defect** · ⭐⭐**"same symptom class" ≠ same mechanism** · ⭐⭐**a relayed symptom arrives WITHOUT its mechanism.**
- [spy#1089 shader_cache_path SEGV (vk)](project_slangpy_1089_shader_cache_path_vulkan_segv.md) — P1 regr. **MINE-VERIFIED: rhi is NOT the regression** (blob SHA identical across the pin move; 0.37.0 turned ON a latent rhi path). ⚠️tag-prefix trap: bare `0.36.0` 404s. RESUME=reporter `p api.vkGetPipelineKeyKHR`
- ⭐**[spy#823 TensorView interop non-CUDA](project_slangpy_823_tensorview_interop_buffer_noncuda.md)** — delivered as *"new"*, actually **filed 02-26, ~5mo stalled** ⇒ ⭐**an inbound's FRAMING is not evidence about the artifact**. P3. ⭐**a PR file table is the author's PROSE — open the diff.** ⛔**#934 carries ONLY the ADDRESS half, NOT copy-back** (I relayed it as "the fix" — overstated, retracted 08-04 incl. the public comment) ⇒ ⭐⭐**"contains a fix for these lines" ≠ "fixes the issue" — enumerate what the ISSUE NEEDS, not what the DIFF TOUCHES.** RESUME=mkeshavaNV's A/B/C **or** #934 leaves `dirty`.
- [spy#1079 array-of-tensors](project_slangpy_1079_array_of_tensors_metal_d3d12.md) — D3D12 root @tensor.cpp:442, Metal→slang#12291. 🔴**#1078's squash BROKE stacked #1080 (CONFLICTING); REBASED 08-05 → MAIN-VERIFIED `MERGEABLE`, 8 commits, head `3d50ffc2`, 2 files, 44 passed/2 skipped** ⇒ ⭐⭐**2 causes: redundant commits + STALE BASE (50 files/4068 dels) — `--json commits` is blind to the 2nd, measure the net `--stat`.** ⚠️#1080 intentionally REMOVES #1078's 2 D3D12 guards — do NOT restore. 🔴⛔**CI now GREEN (12/12 jobs, 15/15 checks) BUT the force-push DISMISSED @ccummingsNV's approval ⇒ MAIN-VERIFIED `reviewDecision: REVIEW_REQUIRED`** — so `BLOCKED` CHANGED MEANING (draft+CI → draft+MISSING REVIEW) ⇒ ⭐⭐⭐**a composite status token is not a fact, decompose it every time; and the re-approval cost belongs in the DECISION to rebase.** RESUME=**human: re-review → approve → promote → merge** (never self-ready/self-approve) · [spy#996→carrier #1078](project_slangpy_996_carrier_pr_1078.md) ✅**TERMINAL — MERGED 08-05 squash `507b4cf1`** by @ccummingsNV (he blocked orig #996); #996 closed superseded; @fangjunzhou credited via `Co-authored-by` ⇒ ⛔**a squashed bot commit is NOT evidence credit was lost — anchor `^co-authored-by:`**; RESUME=none (**#1079 stays OPEN** for the guarded Metal/D3D12 defects) · [spy#827 torch WTensor<T,0>](project_slangpy_827_torch_scalar_return_wtensor0_crash.md) P1, A/B maintainer-gated, HELD
- 🔴⭐**[spy#1040 CI GPU OOM = peak concurrent VRAM, not a leak](project_slangpy_1040_ci_gpu_oom_peak_vram.md)** — ⚠️**was DARK ~5wk: OUR bot filed it, NO index row, no owner** (this row is the tripwire). **szihs DECLINED 08-04 ⇒ UNOWNED.** ⭐MINE-VERIFIED premise holds @main (`tools/ci.py:156,164` still `-n auto --maxprocesses=4`; GPU legs = self-hosted `nvrgfx-kernelvm-bridge`); ⛔**all VRAM numbers are INHERITED bot claims, NOT mine.** ⛔**never re-propose the `maxprocesses` cut — that IS rejected PR #1024.** ⭐**a runner LABEL is not a VRAM spec** ⇒ the issue's own open question is unanswered. ✅**MINE-VERIFIED + POSTED cmt `5176441320`: NOT RECURRED in 5wk — 154/154 GPU jobs, 0 hits, 4 CONTROLS.** ⭐⭐**the positive control validated the SEARCH but its labels PRE-DATE `nvrgfx-*` ⇒ silent about the fleet; the `cuMemcpy`=14-vs-`cuMemAlloc`=0 same-family control is the one that speaks.** ⭐**`-2147467259`=`E_FAIL`, NOT `E_OUTOFMEMORY` — decode the HRESULT before classifying.** ⚠️07-29 d3d12 cascade = morphological match, SEPARATE mechanism, don't merge. RESUME=**jhelferty-nv names an assignee, or a maintainer authorizes fix-2 / a runner measurement**
- ✅**[spy#1067 / PR #1068 MERGED](project_slangpy_1067_macos_wheels_pyframe_getlasti.md)** 08-10T11:25:31Z by skallweitNV, merge `35e262ab73`; #1067 closed completed, **#1066 still OPEN**. `ABSTAIN_INFRA`/`NO_REVIEW_SIGNAL` (Devin exit 0 on a **HOLLOW artifact**) — **not contradicted**: MINE-VERIFIED zero code delta decision→merge (blob `039be94b54` identical at `266b2072e6`/`4639cff88a`/`main`), zero human signal added after the decision. ⭐⭐**THE GAP WAS ACCEPTED, NOT CLOSED** → [[feedback_merged_does_not_mean_the_flagged_gap_was_closed]]: post-merge `main` STILL `workflow_dispatch:`-only, branch wheels `total_count: 0`, `ci.yml:43` still py3.10, **last wheels run on `main` = 2026-04-16** ⇒ the symbol has never been compiled at 3.11+ anywhere; risk moved PR→**release**. ⭐**`commit_id` false-safe FIRED FOR REAL.** ⛔my "13+CLA"=14 was WRONG ⇒ [two sets, same cardinality](feedback_two_sets_same_count_different_members.md). 🔴**NO LEDGER ROW EVER EXISTED** (`APPROVAL_LEDGER_WRITERS` unset ⇒ fleet-wide) → [[feedback_record_decision_ok_proves_emission_not_persistence]] — **MY operator action, OUTSTANDING**
- 🔴⭐⭐⭐**[spy#925 manylinux_2_28 — ABSTAIN_POLICY (100% protected-path) but AUTO-MERGE IS ARMED with a confirmed 🟠 regression](project_slangpy_925_manylinux_2_28_version_override.md)** — Linux wheels lose `SLANGPY_VERSION_OVERRIDE` (`CIBW_ENVIRONMENT_LINUX` at `wheels.yml:25` REPLACES step-level `:133`; nightly ⇒ Linux dev wheels mis-versioned vs win/mac in the same run, one-line fix). ✅**CONFIRMED by EXECUTING the pinned `cibuildwheel==3.4.1`'s own `OptionsReader`, not by reasoning** — linux drops it, win/mac keep it; controls: merged-both-markers `False` (REPLACE), glibc-flag-present `True` (probe live); `options.py:741` passes no `env_rule` ⇒ `InheritRule.NONE` ⇒ `_apply_inherit_rule` returns `after`. ⛔**`next-action: human review` was WRONG about the operational state — `autoMergeRequest` SQUASH armed 12:55:44Z by ccummingsNV, `reviewDecision: APPROVED`, held only by `BEHIND`** ⇒ ⭐⭐**read `autoMergeRequest`+`reviewDecision` BEFORE naming a human as the blocker; "awaiting review" and "armed to self-merge" are opposite states the review surface cannot distinguish.** ⛔**PROVENANCE CORRECTED: approver said the 07-29 approval "predates" the finding — measured backwards.** Per-ref with positive controls: `CIBW_ENVIRONMENT_LINUX` present at day-one `6286baba0908`, step-level override arrived **from main** at `e5f2299b2b63` (06-23) ⇒ defect born 06-23, approval **POSTDATES it ~5wk** (a human already saw this tree) ⇒ ⭐⭐⭐**a merge-born defect has TWO birthdays; grepping the PR's first commit dates it wrong.** ⭐⭐**that review's `commit_id` `4743d90ff367` was COMMITTED 08-05, 7 days AFTER `submitted_at` — its main-side parent is 08-04 ⇒ `commit_id` is NOT evidence of the reviewed tree, yet `commit_match` read `pass`** → [[feedback_a_reviews_commit_id_can_postdate_the_review]]. ⛔**Green CI exercises NONE of it: `wheels.yml:3` is `workflow_dispatch:`-only (no dispatcher; `ci.yml:12/21` merely `paths-ignore`s it), and `ci.yml` has 0 hits for `SGL_SLANG_GLIBC_COMPAT`/`CMAKE_ARGS` (control `cmake`=3) ⇒ every leg takes the default OFF** — the PR's own 3-item test plan is unobserved; CodeRabbit's `!external/**` exclusion leaves the URL-building file unreviewed by any bot. ✅slang#10777 DISCHARGED (pinned `2026.12` ships both x86_64+aarch64 glibc-2.28 assets; the stale 🔴 reasoned about v2026.5.2). ⚠️Approver-pipeline race worth a debounce: harvest exit 10 (stale-only) at 13:01Z vs CodeRabbit posting 13:06Z is **indistinguishable from exit 22** at observation time. RESUME=**maintainer pushes the one-line `:25` fix, disarms auto-merge, or it lands as-is ⇒ follow-up fix PR against main**
- 🔴⛔⭐⭐⭐**FLEET-WIDE, outlives spy#925: `ci_green_on_sha` IS THE WRONG INSTRUMENT, and its shipped replacement `APPROVER_CI_GATE` DEFAULTS OFF** (`project_slangpy_925_manylinux_2_28_version_override.md` §§gate/build-blind) — clause read `success` @13:10Z from a surface with **ZERO build legs**; last build finished **13:44:09Z**. `commits/$SHA/status` cannot see Actions check-runs by construction. ⛔**`github-webhook-server.ts:315-316` DOCUMENTS that the gate exists to replace this exact blindness ⇒ a KNOWN defect with a SHIPPED fix left on the live path** — ⭐⭐⭐**a distinct class from a reasoning error: no care inside the clause finds it, only reading the code that SUPERSEDES it.** ✅Gate state established BEHAVIOURALLY (`config.ts:318-320` default OFF · park guard `webhook-github.ts:657` reduces to the flag alone since `github-webhook-server.ts:347` always passes `head.sha` · I was delivered not parked). ⚠️**`.env` grep was a FALSE ZERO — no `.env` in that tree at all; and a synced snapshot NEVER settles what the host PROCESS loaded.** ⚠️**Task string is NOT a discriminator** — `releaseParkedReviewable:721` re-enters the same function ⇒ byte-identical; only TIMING discriminates. ⇒ **ARMING PROCEDURE: `APPROVER_CI_GATE` + `CI_GATE_REQUIRED_SUITE` in the SAME change** (`config.ts:322-329`: unset ⇒ ANY suite releases ⇒ spy#925's own coderabbitai+2-trivial green releases straight back into the race). ⭐⭐⭐**Hazard is `0 < n_status << n_check_runs`, NOT `n_status == 0`** → [[feedback_the_platform_guards_empty_the_bug_lives_just_past_empty]]: **`slang` is the fleet's worst case (2 contexts, one a CLA bot, for 278 check-runs)**; slangpy/rhi return 0 ⇒ `pending` ⇒ **fail-SAFE**. ⭐⭐**Every leg eventually passed ⇒ the unfounded pass was ACCIDENTALLY RIGHT and generated no evidence of its own existence.** ⭐⭐⭐Approver's transferable audit: **grep a clause's source for how many states map to `pass` — more than one means it cannot express its own doubt** (`eval-clauses.py:173-176` `commit_id`; `:184`/`:190` CI). RESUME=**operator decides whether to arm the gate (+required-suite); until then EVERY approver run fleet-wide can be woken mid-CI with a build-blind green**
- ⛔⭐⭐⭐**"THE API RETURNS IT" IS NOT "A HUMAN SEES IT" — my own falsified claim, twice relayed to the operator** (spy#925): I grepped CodeRabbit's review body for `SLANGPY_VERSION_OVERRIDE`, got 4 hits, reported the defect **"already public, no post owed."** Re-measured: **0 inline comments** (control: 3 exist), **0 issue comments**, and the text sits **two `<details>` deep** inside `⚠️ Outside diff range comments` — collapsed by default, and *outside the diff range* is precisely **why** it could not be inlined. ⇒ ⭐⭐⭐**a claim about RENDERING answered with a claim about CONTENT; the collapsed-details case is where they diverge, and it is the NORMAL shape for any finding whose target line is outside the diff.** ✅Approver's other half holds: `AutoSquashEnabledEvent` **is** a public typed timeline node (GraphQL: `12:55:44Z` ccummingsNV) ⇒ **nobody needs to post auto-merge** — the two halves SWAPPED. ⚠️**Do NOT inherit its sequencing gloss** ("armed 11 min BEFORE the finding existed ⇒ not armed-while-known"): true of the BOT REPORT (10m42s), but the **defect dates to 06-23** and the arming is **still active after** it ⇒ ⭐⭐**the two-birthdays error in a new costume — dating a risk from when a bot REPORTED it, not when the CONDITION arose.** ⭐⭐⭐**THIRD CI-trust axis, approver-found, passes BOTH our filed falsifiers (17 runs, 0 incomplete, 0 failed) and is still false-safe: all 17 legs are `ci.yml`, which `paths-ignore`s `wheels.yml` on push AND pull_request, and `wheels.yml` is `workflow_dispatch:`-only ⇒ WHEEL LEGS = 0. Green ABOUT OTHER FILES.** The new conditional is dead under all PR CI: `external/CMakeLists.txt:87` defaults OFF, `:100-104` consumes, **only setter repo-wide is `wheels.yml:25`** ⇒ condition-false 17×, true **0×** ⇒ ⭐⭐⭐**a both-directions probe generalizes one indirection out: not "a flag with no setter" but "a flag whose only setter is OUTSIDE CI's reach"** — coverage is a **third axis**, independent of timing (gate) and instrument (surface); a required-suite gate still releases onto a green that proves nothing. ✅**I closed its 404 risk: MINE-MEASURED both `slang-2026.12-linux-{x86_64,aarch64}-glibc-2.28.tar.gz` → HTTP 206, negative control `-glibc-9.99` → 404** (correct today; CI would not have caught it otherwise). ⛔**LIVE GATE STATE UNRESOLVED FROM EITHER SEAT — approver's `/app/src` grep was a false zero (container runner has no webhook files; control fired) and MY read is ALSO indirect (snapshot tree `4c41224f`, no `.env`, logs mention the name only in prose)** ⇒ ⭐⭐**I have a BEHAVIOURAL determination (delivered-not-parked ⇒ off at delivery time), NOT a config read — never report it as "I read the flag."**
- 🔴🔴🔴**spy#925 MERGED 08-10T10:15:13Z (ccummingsNV) WITH ALL 12 LINUX WHEEL LEGS RED — and the approver's BLOCK could not be recorded** → [[feedback_a_loud_defect_recruits_a_fixer_a_quiet_one_does_not]]. R2 @`3627a9a032f3`: **BLOCK · `VERIFIED_BUG:linux_wheel_version_override_shadowed` · `bugs:1`**, critique-approved, **LEDGER APPEND DENIED** (`record_decision` returned `"Decision recorded"`, host then denied: *no `APPROVAL_LEDGER_WRITERS` configured*) ⇒ ⛔⭐⭐⭐**THE ACKNOWLEDGEMENT WAS NOT THE RECEIPT — variant #5 firing on the agent that filed variant #5**; absent an out-of-band host notice it would have reported a row that does not exist and I'd have relayed it. ⇒ **possibly EVERY prior decision is absent ⇒ no agreement statistic from that ledger until rows are counted** (and it retro-explains the 1-of-57 `record-payload.json` I let it blame on cleanup — I correctly said a workspace census can't speak to ledger contents, then neither of us asked whether the ledger had ANY rows). ⚠️**`record_human_verdict` NO LONGER EXISTS** (`No such tool available`) ⇒ the Aug-5 `#918`/`#1002` stamps are **UNCONFIRMED, not merely unverified**, and this merge cannot be joined. ⛔**MERGE-SAFETY FINDING (its, verified by direct read `branches/main` → `.protection.required_status_checks.contexts` = 13, ZERO matching `/wheel/i`): `wheels` was never a required context, so red wheels cannot block a merge** — `BLOCKED` was 6 in-progress `ci` legs. ✅**MY CORRECTION: "both defects are on main" is FALSE — the LOUD one was fixed 2h16m later.** MINE-MEASURED: merge `45270bbd44aa` has `epel-release=1`; main `c6f97171a353` (12:31Z) has **0** — skallweitNV's **#1096** strips the `epel-release` + modular-perl line outright. ⇒ its `perl-FindBin`/EL8-modular-filtering diagnosis was RIGHT and is now historical; **the QUIET `:25`-shadows-`:133` version defect is STILL LIVE ON MAIN.** ⭐⭐⭐**LOUDNESS ⊥ SEVERITY, and loudness predicts TIME-TO-FIX: the loud defect recruited the very maintainer who dispatched the build at 10:00:54Z and watched it fail; the quiet one recruited nobody** ⇒ **escalate the QUIET defect; weight DERIVED above REPRODUCED.** ⭐⭐Its Codex-forced `bugs:2→1` scope cut felt like a cost and was **validated by outcome** — the excluded defect got fixed, the retained one didn't ⇒ ✅**free post-hoc test: when scope discipline drops a finding, come back and ask WHICH DEFECT SURVIVED.** ⚠️Gap 2 splits: `SGL_SLANG_GLIBC_COMPAT=ON` was **SELECTED but never REACHED** (died in `before_all` pre-CMake) ⇒ URL construction still untested; override path unexercised (non-nightly dispatch, `Compute version override` skipped). ⚠️4 macOS cp311+ failures = **pre-existing #1067** (`_PyFrame_GetLasti` gated `PY_VERSION_HEX>=0x030b0000` ⇒ cp39/cp310 pass — mechanism PREDICTS the split), **not #925**. ⚠️Fleet: `harvest-reviews.py` died on `gh --slurp` (needs 2.47, container 2.46) ⇒ **spurious ABSTAIN_INFRA on every PR**; `collect-reviews.sh` prints `FETCH FAILED` while **exiting 0**. ⛔`gh api --allow-escape-sequences` **is not a flag** (it's gh's own message) — plain `gh api .../logs` works, 322KB retrieved. ⚠️The Aug-5 `policy_path` fix **was reverted by a 10:04Z skill re-extraction** and re-applied — predicted rebuild-transience, recovery text at `tools/PENDING-SKILL-FIXES.md`. RESUME=**operator: set `APPROVAL_LEDGER_WRITERS`, count dropped rows, restore the verdict-join tool; decide whether `wheels` should be required**
- 🔴🔴⭐⭐⭐**spy#925 R1 — ABSTAIN WAS RECORDED FOR THE WRONG REASON — per-PR staged policy SHADOWS the human-signed mount** (`project_slangpy_925_manylinux_2_28_version_override.md` §resolved). `clauses.json` says `policy_version: v0-shadow`, **not** the signed `v0-shadow-wide`; `eval-clauses.py:267-283` gives **precedence 2 (per-PR `<ws>/policy/`) OVER precedence 3 (group mount)**, so `no_protected_paths` fired on `.github/**` — the exact class the 08-04 widening (haaggarwal, 232 decisions, 53% abstain / 91% of decisive abstains approved) was signed to stop abstaining on. **ROOT CAUSE NARROWED: all 4 stale pins are byte-identical (`cmp`) to the skill-BUNDLED default while the 17 era-correct ones differ (negative control) ⇒ staging FELL BACK to precedence 4 and wrote it in as an authoritative pin. Pinning stays; the fallback is the bug** (+ record the loaded policy's absolute path in `clauses.json`). ⛔**BLAST RADIUS CORRECTED 21→4 — I had already sent 21/57 upstream: 17 of 21 loaded the policy IN FORCE AT THEIR OWN STAGING DATE (correct auditability).** ⇒ ⭐⭐⭐**A VERSION-DRIFT SWEEP MUST BE ERA-RELATIVE; the absolute form gave a 5× false-positive rate** — two-birthdays error a 3rd time, now on an inventory. ⛔⛔**AND "#925 WAS WOULD_APPROVE-ELIGIBLE" IS FALSE — I relayed it twice: CLAUSE-ELIGIBLE ≠ APPROVABLE, passing Step 1 only means STEP 2 RUNS.** Step-2 input reads `verdict: REQUEST_CHANGES`, 2 gaps (both MINE-VERIFIED: the `:25`/`:133` shadowing + the missing trigger-present control) ⇒ real outcome **ABSTAIN_POLICY:OPEN_GAP or BLOCK. Same verdict, sound derivation.** ⇒ ⭐⭐⭐**WHEN FIXING A DEFECT AT STAGE N, REPORT THE CORRECTED DERIVATION, NEVER THE CORRECTED OUTCOME, UNTIL STAGE N+1 HAS RUN — say "would have reached Step 2", NEVER "was approvable"** (the phrase CONTAINS the verdict token). ⚠️`1078`×2 + `918` generalization **RETRACTED** — same inference ×3, no Step-2 read. ⭐⭐⭐**THE TELL WAS IN ITS ROUND-1 REPORT: `policy v0-shadow` verbatim, while #1090's said `v0-shadow-wide` — two PRs minutes apart, two versions, ONE string comparison available from message one.** ⇒ **a `policy_version`/`schema_version` field is a CLAIM, not a label: diff it against the version in force AND against sibling reports.** ✅Withdrawn: the hold on the 53%/91% founding numbers (pre-widening by construction ⇒ **unverified, NOT tainted**; calling them contaminated hands someone a lever to revert a signed widening). → [[feedback_the_more_sayable_version_wins_before_verification_runs]]. RESUME=**approver re-derives `925`/`1078`×2/`918` individually + fixes the staging fallback; nobody quotes a re-derived OUTCOME until each Step 2 is read**
- 🔴⭐⭐⭐**2ND FALSE-NEGATIVE #1002 — A CLASS NO POLICY AUDIT COULD FIND, plus the correction-cost ratchet** → [[feedback_a_correction_inherits_the_ceremony_of_its_destination]]. Approver built the ABSTAIN-vs-merged join **after we both agreed the set was closed** and it fired first run: 28 abstains → 10 HIGH → **2 strict** (#918 staging-fallback; **#1002 era-correct policy, NO bug — `no_protected_paths` fired LEGITIMATELY**). ✅MINE-VERIFIED all 4 facts: merged 07-15T23:37Z, `jkwak-work` APPROVED **at the decided head** `34e5df38dddf`, diff **does** touch `.github/workflows/wheels.yml` (+12/−4), 07-15 ∈ [07-10 relaxed, 08-04 wide]. ⇒ ⭐⭐⭐**TWO CAUSES, SAME RECORDED ABSTAIN, DISJOINT INSTRUMENTS: "wrong policy loaded" (version audit) vs "correct policy, over-strict clause" (ONLY an outcome join). A version audit answers *did we use the intended rules* — it CANNOT answer *were the intended rules wrong*** ⇒ every instrument in 14 rounds was the first kind, so #1002 was **structurally invisible**; **the instances you find are the ones some OTHER signal surfaced** ⇒ build the detector even when you believe the set is closed. ✅**POLARITY: #1002 is evidence FOR `v0-shadow-wide`** (the `_comment` justifies widening by `no_protected_paths` firing exclusively on `.github/**`, 32 cases — #1002 IS that cost, human-approved+merged) ⇒ **cite it if anyone proposes re-tightening.** ⛔**CORRECTION COST: abstain→WOULD_APPROVE is gate-exempt→FULLY GATED (`SKILL.md:142-153`, "cannot author or edit verdict state")** ⇒ **the error was free, each fix is a fresh gated decision** — caught by reading the procedure for the DESTINATION state. ⭐⭐⭐**RATCHET: the cheapest output (abstain) is ALSO the one whose errors are invisible AND the costliest to reverse — the class with no detector has the highest correction cost**, which is why 2 sat unnoticed. ⚠️**OPEN, operator's call: record the 2 corrected rows or document the gap?** Backfilling writes decisions no live gate produced ⇒ **a ledger with a KNOWN 2-row gap may beat one silently containing reconstructions.** ⚠️8 `APPROVE_WITH_NITS` rows (2-4 gaps) deliberately UNCOUNTED pending individual Step-3 reads. ⚠️**Only 1 of 57 workspaces retains `tmp/record-payload.json`** (all 57 keep `clauses.json`+`tmp/context.json`) ⇒ payload-based retrospectives are mostly impossible.
- ⛔⭐⭐⭐**"THE MERGE WAS TEXTUALLY CLEAN / GIT MERGES TEXT NOT MEANING" — FALSIFIED BY REPLAYING THE MERGE** (spy#925 onset). Approver's BIRTHDAY is right: parents verified w/ per-fetch control (`name: wheels`=2 each) — `6cfb1df2149f` `_LINUX`=1/override=0, `2c253730768a` `_LINUX`=0/override=1, merge `e5f2299b2b63` = **1/1 ⇒ defect**; neither parent carries it, `_LINUX` REPLACES not extends ⇒ merge is the onset (04-09=one conjunct, 08-05=observation). ⛔**But `git merge-tree` (git's OWN machinery, not my `merge-file` approximation) reports `CONFLICT (content): .github/workflows/wheels.yml` — THE DEFECT FILE. The marker DID fire.** Author hand-resolved and the RESOLUTION built the shadowing pair (kept A's `_LINUX` `:25` + took B's quoted global AND its step-level `:133` + A's `epel-release`). ⇒ ⭐⭐⭐**the failure was the HUMAN RESOLUTION of a conflict git CORRECTLY RAISED, not git's silence — filing it as "invisible to every conflict marker" builds a detector for SILENT merges and misses this case entirely.** RIGHT PROBE = **"was this file hand-resolved during a merge, and did the resolution co-locate two individually-correct edits?"** (conflicted-file review, NOT marker-absence). ✅**Surviving half is the valuable one: review-vs-base still cannot see it** — vs base, `:25` reads as the intended one-liner and nothing shows `:133` now loses ⇒ ⭐⭐⭐**a check that only ever sees ONE SIDE OF A JOIN cannot see a join defect** (same shape as 17-green-legs-covering-none-of-the-diff). ⭐Its trap-avoidance is sound: `cibuildwheel 3.0.0rc1→3.4.1` lands at HEAD and CodeRabbit's explanation leans on 3.4.1 semantics ⇒ tempts dating to 08-05; **the bump changes SEVERITY, not the birthday — don't let a report's chosen mechanism reset the clock.** ⭐⭐⭐**REFINED (approver, verified): the merge was LOUD ON ONE CONJUNCT, SILENT ON THE OTHER — conflict hunk = lines 23-31 (global + `_LINUX` only); the shadowed step-level override AUTO-MERGED at line 139, 108 lines below, purely B's ⇒ both halves NEVER ON SCREEN TOGETHER.** ⇒ probe needs TWO clauses: hand-resolved? **AND** did the hunk contain ALL conjuncts? ⭐⭐**conflict-region review is ITSELF a one-sided view of the join** ⇒ read the whole post-resolution file. ⛔**TOOLING: `git merge-tree base A B` (3-arg) is UNINFORMATIVE IN EVERY FIELD — exit 0 both ways, `changed in both`=1 both ways, `CONFLICT`=0 both ways; only `--write-tree --name-only` discriminates (exit 1 + CONFLICT vs 0)** → [[feedback_a_negative_control_must_vary_exactly_one_thing]] ⭐⭐⭐**a different-files clean control faked a usable 1-vs-0; the same-file-non-overlapping control reads 1-vs-1 — and MY table was right only BY ACCIDENT (my clean case happened to touch the same file).** ⚠️`exit=128` from my `--filter=blob:none` clone was a claim about THE CLONE (`could not fetch … from promisor remote`), not the command. ⭐**JOIN GENERALIZATION HELD AT 4 ALTITUDES: 17 green legs covering none of the diff · a merge of 2 clean parents · a hunk showing 1 of 2 conjuncts · a harness varying one side while the real case varied another.**

---

## ⚠️ Rows here WITHOUT an explicit `RESUME=` — trigger lives in the child, verify before assuming closed

Three rows carry no `RESUME=` token (the spill audit caught this; **`grep -c 'RESUME'` counts OCCURRENCES, not rows** — my first count said 7/8 and was measuring the wrong unit):

- **Approver-pipeline defects** — fix-state row; triggers are per-defect inside `project_approver_pipeline_defects_devin_fetch_ci_green.md`. Re-read before claiming any of D1/D2 fixed.
- **`[GATE AUDIT]`** — advisory-only noise; **no action pending by design**. Closing state, not a stalled chain.
- **spy#1079 / spy#996 / spy#827 bundle** — three sub-chains on one row: #1079 awaits a fixer draft, **#1078 is TERMINAL (MERGED 08-05 `507b4cf1`) — no RESUME, but #1079 remains the live successor**, #827 is maintainer-gated A/B and HELD.

⭐ **A row without a named trigger is indistinguishable from a closed one at a glance** — that is exactly how a chain goes dark. When you touch one of these, add its trigger here in the form `RESUME=<the human step>`, not `RESUME=<a milestone>`.
