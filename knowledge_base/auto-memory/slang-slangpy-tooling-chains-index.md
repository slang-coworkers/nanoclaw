---
name: slang-slangpy-tooling-chains-index
description: Live slangpy and approver/gate tooling chains moved out of the index at the 08-04 compaction floor. Each row keeps its RESUME trigger verbatim.
metadata: 
  node_type: memory
  type: index
  title: SlangPy + tooling live chains (spilled from MEMORY.md)
  originSessionId: fa2a5394-3823-4780-87fa-cc306bdc4156
---

# SlangPy + tooling live chains

Spilled from `MEMORY.md` 2026-08-04 because the index hit its read limit. **These are LIVE, not parked** —
each row carries its own RESUME trigger. Open this file whenever you would have scanned the index's
Live-chains section for slangpy or tooling work.

- 🔴⭐⭐**[nanoclaw#1066 kb-fold-bounded — MERGED `c2a7639`; MY REVIEW LANDED 26s LATE; defect LIVE on `nv-main`, FOLLOW-UP PR OWED](project_nanoclaw_1066_kb_fold_bounded.md)** (cmts `5179404594` + post-merge `5179614225`) — ⛔⭐⭐**a "ready for review" webhook is a claim about a MOMENT, not a lock — RE-CHECK `merged`/`state` BEFORE writing a verdict; if it merged, re-verify against the MERGED TREE and say so** (a finding under a merged banner is easy to miss). ✅Re-verified on merged code: `2/2 live (1 superseded)` → daily `build` → **marks surviving 0** → `2/3 live (0 superseded)`. ⚠️**`/tmp` is WIPED BY A CONTAINER RESTART** — my re-run silently lost its fixture and printed `0/2`; ⭐**a rebuilt harness returning a DIFFERENT number means the HARNESS broke, not the finding.** RESUME=**szihs accepts the offer ⇒ open follow-up PR** (preferred: persist retirement OUTSIDE the regenerated tree, keeping L1-immutability intact). — szihs bounds the learnings-wiki fold (touches **my own spine** + the Main-write-only KB ⇒ I am closest-to-the-state). ⛔**nanoclaw has NO `*-pr-approver`; slang/slangpy's are REPO-SCOPED ⇒ handle nanoclaw PRs MYSELF, never dispatch there.** ⛔**MINE-MEASURED 08-04: `gh pr review --request-changes` → `Resource not accessible by integration (addPullRequestReview)` while perms `{admin,maintain,push,triage}:true` AND `x-ratelimit-limit:5000` both read GREEN ⇒ `permissions` is NOT evidence about which VERBS an integration may use** (same family as endpoint-split); ✅**post an ISSUE COMMENT instead — don't burn a round-trip on `--request-changes`.** ⭐⭐Blocking finding: `superseded_by` is wiped by every `build()` (deletes all of `WIKI` except `concepts/`) and the daily order is `build`→synthesize→`finalize` ⇒ retirement works **ONCE**, between two always-paired commands, then reverts — **GREEN-LOOKING**, since `0 superseded, excluded` cannot distinguish "none retired yet" from "not persisting". L1-placement DOES persist but contradicts the same diff's own `immutable; never edit` (:22 **and** :137) and matches in the page **BODY** via `fm()`'s `re.M` **by accident, not design**. ⭐**the fenced SKILL.md block ≠ the deployed `.learnings_wiki.py`** (14,108 vs 16,590 B, different sha256) — wipe present in BOTH so the finding holds, but **never assume fence==live**. ⭐Also posted: cap+spine land before any page satisfies them (**live: 47 concept pages, 0 with a `## TL;DR`**; the spine's `limit=60` takes effect next container wake ⇒ asked for "TL;DR if present, else widen"). ⚠️Outlives the PR: `slangpy-pr-approver` SHA-256-attests two 178/180 KB pages it has seen 37%/50% of. RESUME=**szihs answers the persistence question or pushes a fix** → re-review.
- [spy#1072/#1073 profiler flush barrier](project_slangpy_1072_profiler_drain_snapshot_race.md) — **COMPLETE** (2 suggestions + 3 `end_zone` defects, revert-verified). ⭐**`GH_TOKEN` 401 RETRACTED — asserted live 6×** ⇒ re-probe capability-negatives every round. RESUME=`ci` green **on the head under review** → approver + reviewer
- ✅[spy#1074 onboard repo to PR dashboard](project_slangpy_1074_onboard_pr_dashboard.md) — **RESOLVED/closed 08-04 by jhelferty's OWN PR #1084** (board-sync callers → org project #10, merged 15:11:25Z). Was option **(c)** — a board **NEITHER triager NOR I had enumerated**, so our "(a)/(b) already covered" was true **and irrelevant** ⇒ ⭐⭐**enumerating the dashboards you know does not license "those are the options"** (same family as the ENUMERATE-over-consumers defect). ✅Verified: old `add-pr-to-project.yml` retired, 6 caller runs all `success`, **org secret resolves** — decisive: guard step `Note skipped (no token)` was itself **SKIPPED** while `Reconcile board` **RAN** ⇒ ⭐⭐**caller `success` ≠ effect happened; read STEP outcomes, and prefer a guard whose skip/run polarity is OPPOSITE what you're confirming.** ⛔**Board contents UNREADABLE from my seat: `projectsV2 totalCount`=0 is INSTRUMENT FAILURE — project #10 provably exists (named in this repo's own live workflow) yet the same token 404s it; `GH_TOKEN` lacks project scope** ⇒ never report slangpy tile state from here. RESUME=**none (closed)**; optional backstop = nightly sweep's first fire ~08-05 07:00Z (0 runs yet, cron not reached)
- [spy#1090 Metal `create_buffer_from_native_handle`](project_slangpy_1090_metal_buffer_from_native_handle.md) — **ABSTAIN_POLICY `OPEN_GAP`**, 6/6 clauses, nothing posted. G1=**zero executing coverage at any slangpy layer** · G2=Vk/D3D12 type-check-no-size-check. ⭐**MINE-VERIFIED the rhi mask fix is REGISTRATION not EXECUTION** (`macos-latest` paravirtual ⇒ skipped). RESUME=maintainer reviews G1/G2
- 🔧⭐⭐⭐[Approver-pipeline defects — D1pt2's CAUSATION RETRACTED](project_approver_pipeline_defects_devin_fetch_ci_green.md) — spy#1090, fails TOWARD clean/green. ⭐⭐⭐**agreement isn't corroboration when the peer's source is ME** · **the counterfactual IS the test** · **a null result doesn't name its own cause**
- 🔧⛔**[`[GATE AUDIT]` "codex never invoked" — BOTH my claims WRONG](project_gate_audit_shared_jsonl_mtime_race.md)** — emitter is an **MCP tool, NOT a hook**; cause = mtime pick over a shared jsonl dir. ⭐⭐**find the EMITTER before naming the defect** · ⭐⭐**"same symptom class" ≠ same mechanism** · ⭐⭐**a relayed symptom arrives WITHOUT its mechanism.**
- [spy#1089 shader_cache_path SEGV (vk)](project_slangpy_1089_shader_cache_path_vulkan_segv.md) — P1 regr. **MINE-VERIFIED: rhi is NOT the regression** (blob SHA identical across the pin move; 0.37.0 turned ON a latent rhi path). ⚠️tag-prefix trap: bare `0.36.0` 404s. RESUME=reporter `p api.vkGetPipelineKeyKHR`
- ⭐**[spy#823 TensorView interop non-CUDA](project_slangpy_823_tensorview_interop_buffer_noncuda.md)** — delivered as *"new"*, actually **filed 02-26, ~5mo stalled** ⇒ ⭐**an inbound's FRAMING is not evidence about the artifact**. P3. ⭐**a PR file table is the author's PROSE — open the diff.** ⛔**#934 carries ONLY the ADDRESS half, NOT copy-back** (I relayed it as "the fix" — overstated, retracted 08-04 incl. the public comment) ⇒ ⭐⭐**"contains a fix for these lines" ≠ "fixes the issue" — enumerate what the ISSUE NEEDS, not what the DIFF TOUCHES.** RESUME=mkeshavaNV's A/B/C **or** #934 leaves `dirty`.
- [spy#1079 array-of-tensors](project_slangpy_1079_array_of_tensors_metal_d3d12.md) — D3D12 root @tensor.cpp:442, Metal UNCONF → fixer draft · [spy#996→carrier #1078](project_slangpy_996_carrier_pr_1078.md) DRAFT GREEN, OP-gate · [spy#827 torch WTensor<T,0>](project_slangpy_827_torch_scalar_return_wtensor0_crash.md) P1, A/B maintainer-gated, HELD
- 🔴⭐**[spy#1040 CI GPU OOM = peak concurrent VRAM, not a leak](project_slangpy_1040_ci_gpu_oom_peak_vram.md)** — ⚠️**was DARK ~5wk: OUR bot filed it, NO index row, no owner** (this row is the tripwire). **szihs DECLINED 08-04 ⇒ UNOWNED.** ⭐MINE-VERIFIED premise holds @main (`tools/ci.py:156,164` still `-n auto --maxprocesses=4`; GPU legs = self-hosted `nvrgfx-kernelvm-bridge`); ⛔**all VRAM numbers are INHERITED bot claims, NOT mine.** ⛔**never re-propose the `maxprocesses` cut — that IS rejected PR #1024.** ⭐**a runner LABEL is not a VRAM spec** ⇒ the issue's own open question is unanswered. ✅**MINE-VERIFIED + POSTED cmt `5176441320`: NOT RECURRED in 5wk — 154/154 GPU jobs, 0 hits, 4 CONTROLS.** ⭐⭐**the positive control validated the SEARCH but its labels PRE-DATE `nvrgfx-*` ⇒ silent about the fleet; the `cuMemcpy`=14-vs-`cuMemAlloc`=0 same-family control is the one that speaks.** ⭐**`-2147467259`=`E_FAIL`, NOT `E_OUTOFMEMORY` — decode the HRESULT before classifying.** ⚠️07-29 d3d12 cascade = morphological match, SEPARATE mechanism, don't merge. RESUME=**jhelferty-nv names an assignee, or a maintainer authorizes fix-2 / a runner measurement**
- [spy#1067 macOS cp311+ wheel link / PR #1068](project_slangpy_1067_macos_wheels_pyframe_getlasti.md) — `ABSTAIN_INFRA`/`NO_REVIEW_SIGNAL` (Devin exit 0 on a **HOLLOW artifact**). ⭐**`commit_id` false-safe FIRED FOR REAL.** ⛔my "13+CLA"=14 was WRONG ⇒ ⭐**[two sets, same cardinality, different members](feedback_two_sets_same_count_different_members.md)**; `blocked` = a review gate, **UNVERIFIED which** ([#1076](project_slangpy_1076_branch_protection_review_gate.md)). RESUME=**human**: manual `wheels build-only` cp311–cp314, or fix Devin auth

---

## ⚠️ Rows here WITHOUT an explicit `RESUME=` — trigger lives in the child, verify before assuming closed

Three rows carry no `RESUME=` token (the spill audit caught this; **`grep -c 'RESUME'` counts OCCURRENCES, not rows** — my first count said 7/8 and was measuring the wrong unit):

- **Approver-pipeline defects** — fix-state row; triggers are per-defect inside `project_approver_pipeline_defects_devin_fetch_ci_green.md`. Re-read before claiming any of D1/D2 fixed.
- **`[GATE AUDIT]`** — advisory-only noise; **no action pending by design**. Closing state, not a stalled chain.
- **spy#1079 / spy#996 / spy#827 bundle** — three sub-chains on one row: #1079 awaits a fixer draft, #1078 is DRAFT GREEN on an operator gate, #827 is maintainer-gated A/B and HELD.

⭐ **A row without a named trigger is indistinguishable from a closed one at a glance** — that is exactly how a chain goes dark. When you touch one of these, add its trigger here in the form `RESUME=<the human step>`, not `RESUME=<a milestone>`.
