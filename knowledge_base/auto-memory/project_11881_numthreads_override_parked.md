---
name: RELEASED — #11881 [numthreads] override / warn / duplicate-error (fixer working)
description: Follow-up issue spun off PR #11715; three requirements (a/b/c). Was parked (jkwak self-assigned); jkwak invited a PR 2026-07-01 → park RELEASED, fixer dispatched for (c)+conditional(b)
type: project
originSessionId: 36b4547f-4327-4214-89f5-a6de5054eb1a
---
**STATE 2026-07-02 01:10Z: (c) SHIPPED — PR #11883 MERGED to master.** jkwak-work merged #11883 at 2026-07-02T01:04:56Z (merge commit `16a1114dd8`); fix confirmed on origin/master at `slang-check-modifier.cpp:1628` (`case ASTNodeType::NumThreadsAttribute`) — duplicate `[numthreads]` now errors E31202. `pr: non-breaking` (maintainer-accepted). Approvals: peer reviewer APPROVE_WITH_NITS (0 blockers, both nits applied) + jkwak APPROVE + codex 3-stage. **Guardrails intact end-to-end: bot NEVER flipped ready OR merged — jkwak self-flipped ready, "Update branch", and merged.** CI-hold MOOT (jkwak merged an APPROVED+MERGEABLE PR before the priority-yielded CI ran — real CI never needed to run; local-test gap moot). Fixer reaped its own #11881 worktree on merge (disk healthy). **Issue #11881 stays OPEN (correct — `Addresses` not `Closes`):** (a) shipping via open PR #11715 (@NBickford-NV); (b) deferred follow-up (layout-vs-[numthreads] warning, held to avoid #11715 conflict). Issue footprint issuecomment-4854158547 → (c)-merged. Chain CLOSED (triager's end + Main). ↓ prior state below is superseded.

**STATE 2026-07-01 22:40Z (superseded): (c) PR #11883 MAINTAINER-APPROVED + READY — awaiting jkwak merge + real CI.** All 3 reviewers + codex → APPROVE_WITH_NITS, 0 blockers (both optional clarity nits applied). **@jkwak-work APPROVED** (review 22:35:32Z, reviewDecision=APPROVED, mergeable). **jkwak drove the ready-flip HIMSELF** (`ready_for_review` by jkwak 22:35:24Z) + "Update branch" merge commit (head `a538af6146`, parents b5f4e43112+master) — **the bot did NOT flip ready; drafts-only guardrail INTACT** (verify-who-flipped confirmed maintainer, per standing rule). Operator ready-flip authorization is **MOOT**. Remaining gate = **MERGE = jkwak/CODEOWNERS call** (bot won't merge; jkwak positioned to). Label RESOLVED: `pr: non-breaking` (jkwak approved with it in place; no unilateral change). CI: real post-yield conclusion on a538af6146 still pending (priority-yield skipping, not draft-gated); local-test gap closes on green; triager rolls up + `gh pr checks 11883`. Issue footprint issuecomment-4854158547 → approved+ready. NOTE the fixer's earlier "did NOT flip / operator ready-flip decision" msg was STALE — triager corrected by verifying PR state directly.

Prior state (superseded): **DRAFT PR #11883 OPEN — in review.** jkwak-work (maintainer, self-assignee) commented *"@nv-slang-bot, please make a PR for the issue"* (issuecomment-4854123634) → park released; triager scope-acked (issuecomment-4854158547) + dispatched slang-fixer via peer-wire (Main routed THROUGH triager, not directly, avoiding double-dispatch). **Result: draft PR #11883** https://github.com/shader-slang/slang/pull/11883 — `Addresses #11881` (not Fixes), `pr: non-breaking`, 2 files +23. Diff = exactly Approach A (`case ASTNodeType::NumThreadsAttribute: return modifierType;` in getModifierConflictGroupKind() → existing dup-modifier loop emits E31202). Test `tests/diagnostics/execution-model/duplicate-numthreads.slang`. codex 3-stage APPROVE; slang-reviewer INTERIM verdict **LGTM correctness, 0 blockers** (independently verified: NumThreads own conflict-group, `mapExclusiveGroupToModifier` per-call-local → no cross-EP false positive; synthesis guarded by `!findModifier<NumThreadsAttribute>()` + dup-loop pre-validateEntryPoint; 4× E31202 sound) — full combined-review.md (A/B/C) still pending. **OPEN LABEL Q for ready-flip:** reviewer independently flags `pr: breaking` is arguable under repo's literal "errors on existing code" def; triager kept `pr: non-breaking` (defensible — duplicate `[numthreads]` was never valid, silently discarded; #6216 precedent) + documented both in PR body → maintainer relabel call, surfaces at the operator-gated ready-flip, NOT flipped. Deferred nit: caret-span test assertion (bare errorCode match is robust; no code change, trivial follow-up once disk returns). **Gap:** repro NOT run locally — fixer worktree disk 98%/ENOSPC on fresh Debug build; verified by source-inspection + codex; local-test gap closes on REAL CI green — NOTE run 28517028789 is the COSMETIC priority-yield run (build/test skipped, cosmetic red ≠ failure); the real signal is the `retry-yielded-bot-ci` rerun (not yet run as of 07-01 12:33). (a) left to #11715; (b) DEFERRED as verified-sound (confirmed #11715 edits same check-shader.cpp synthesis block → bundling = real conflict risk). Issue 5-bullet refreshed to draft-held (issuecomment-4854158547, edit-in-place). Next: reviewer verdict + CI result; ready-flip/merge stay operator-gated (NOT flipped). Guardrails passed: draft-only, Addresses-not-Fixes. report_pr_created: CONFIRMED CALLED (fixer re-ran, returned "PR shader-slang/slang#11883 mapped to this session") → mapping written, follow-up review comments + CI on #11883 route to the fixer session, no cold-route risk. Worktree-hygiene sequenced strictly AFTER #11883 CI concludes (active fix/issue-11881 worktree untouched).

shader-slang/slang#11881 — bot-authored follow-up to PR #11715 ("makes [numthreads] take precedence over global GLSL `layout(local_size_*)`"). Splits three requirements. Triaged + HEAD-verified at master @ `7f79b923f`. Classification: **Feature · low · P3 · frontend**. Type=Feature, **@jkwak-work self-assigned the whole issue** (in PR #11715 thread r3505374325 — maintainer-wanted, not a mere tracking artifact). Verdict posted: issuecomment-4854033212.

Per-requirement:
- **(a)** override for multi-entry-point — **already delivered by open maintainer PR #11715** (NBickford-NV). `slang-check-shader.cpp:1852` already gives per-EP [numthreads] precedence in reflection; #11715 deletes the duplicate SPIR-V-emit apply (`slang-lower-to-ir.cpp:10521-10547`) so SPIR-V agrees. **Bot must NOT re-implement.**
- **(b)** warn on inconsistency — not implemented; needs a new warning diag at the merge site (`slang-check-shader.cpp:1851-1907`). Follow-up to (a).
- **(c)** error on duplicate [numthreads] — **CONFIRMED undiagnosed at HEAD** (source + empirical rc=0). Clean STANDALONE fix, **Approach A**: add `NumThreadsAttribute` to `getModifierConflictGroupKind()` → reuse existing error **E31202** (~1 line + test). CORRECTION vs issue body: compiler keeps the **last-written** duplicate, not "first" (modifiers stored reverse-declaration order).

**Why parked:** jkwak self-assigned the whole issue + owns #11715 for (a) → dispatching even a (c)-scoped fixer races a maintainer on his own assigned Feature. Matches #6970/#11717/#11806/#11813 park precedent. Low/P3 = zero urgency. Fixer NOT dispatched. GitHub verdict is the durable artifact — jkwak has the Approach-A analysis to do (c) himself or invite us.

**How to apply / re-engage (chain CLOSED — (c) shipped):** reopen only on (1) a substantive human comment on #11881, or (2) #11715 [(a)] or (b) movement. If reopened, route to slang-triager on thread `gh-issue-shader-slang/slang-11881`. Bot merges stay operator-gated (moot here — maintainer merged).
