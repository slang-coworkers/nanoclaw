---
name: project_12096_metal4_oscap_macos26_rhi795
description: "#12096 root-cause tracker for macos-26 Metal-4 CI fail — PARKED, fix in slang-rhi, self-assigned"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6de733b6-79a8-4b16-b7b4-39c66593979f
---

**#12096** (shader-slang/slang) — Metal 4 capability inferred from OS version fails MSL compile on `macos-26` CI images (toolchain lacks metal4.0). Author **jvepsalainen-nv** (NVIDIA, CONTRIBUTOR, **SELF-ASSIGNED**). Category bug/regression/CI, P2, medium.

**What:** On `macos-26` runners, 4 Metal gfx unit tests fail at `createComputePipeline` — Slang emits `[[required_threads_per_threadgroup(...)]]` (needs metal4.0) but the image's `metal` CLI (32023.883, Xcode-16 era stub) rejects it. Failing: neuralSlangNetworkConverterMetal, pointerInBufferDoublePtrRoundtripMetal, pointerInBufferRoundtripMetal, pointerInParamBlockRoundtripMetal (all `.internal`). Passes on `macos-15`.

**Root cause = slang-rhi, NOT slang core.** `external/slang-rhi/src/metal/metal-device.cpp:266` adds `metallib_4_0` from `osVersion.majorVersion >= 26` — OS version, not the offline MSL compiler/language version. Introduced by **slang-rhi#795** (Simon Kallweit, MERGED 07-06, merge commit `29dc332e55` == slang's current submodule pin; bumped into slang via ef06ca406 07-07 ~1h before first failing nightly). slang-emit-metal.cpp:213-229 emitter is REACTIVE (emits iff getTargetCaps implies metallib_4_0) — correct given inputs; **no core change warranted**.

**Recommended fix: Approach A** — probe actual Metal language/compiler version at device init (MTLCompileOptions.languageVersion / 4.0 construct), replace OS inference at metal-device.cpp:266. Lands in slang-rhi → submodule-bump. Reject C (GPU-family ≠ toolchain) and D (env-var stopgap).

**DECISION 07-14: PARKED at triaged — NO slang-fixer dispatch.** Reasons: (1) self-filed + self-assigned by the NVIDIA reporter who wrote the full diagnosis (no-auto-fixer pattern, cf [[project_12095_autodiff_promotion_placement_loops]], [[project_12035_overload_diag_reasons]]); (2) fix is in a different repo (slang-rhi) needing real macos-26 hardware the fixer can't test; (3) workaround already merged. Triager posts verified 5-bullet on the issue (closest-to-the-state). Re-engage only if reporter/maintainer asks.

**UPDATE 07-14 (comment 4973807829):** maintainer **jkwak-work SELF-ASSIGNED**, taking #12096 + #11999 together, asserting they share the **same root problem**. This runs *against* our triager's finding that #11999 is a DISTINCT macОS intermittency. Unreconciled — but jkwak now OWNS both; his classification call stands. Confirms/strengthens stand-down (maintainer-owned, cf [[feedback_deadpromise_check_assignee_before_rewake]]). Routed to triager to (a) reconcile the #11999-distinct vs same-root tension and (b) decide GitHub reply vs defer (default: no bot comment on a maintainer's own issue). No fixer.

**Relations:** formal root-cause tracker for the macos-26 Metal-4 slice of [[project_11985_macos_metal_capability_regression]] (#11985 MITIGATED; gates close on reverting #12075). #12075 = the macos-15 pin this issue exists to remove.

**RECONCILED 07-14 (triager msg 14/16): #11999 is the SAME root, NOT distinct.** Triager conceded its earlier "#11999 distinct" was too strong (weighted surface signature over mechanism). Both hit the same `metallib_4_0`-from-OS → `createComputePipeline` path: gpu-printing (#11999) is a *compute* example → emits `[[required_threads_per_threadgroup]]`; #11999's graphics siblings passed same runner (device WAS created → fails PAST createDevice); intermittent-vs-deterministic was just the `macos-latest` image lottery, made deterministic by the #12075 pin. Corroboration comment 4973854927 posted on #12096. See [[project_11999_gpu_printing_reenable_parked]].

**⚠️ FIX-DIRECTION CONTRADICTION — surfaced to triager 07-14, UNRESOLVED in my records.** The #12096 triage verdict (comment 4972220997) recommends **Approach A in slang-rhi**: stop advertising `metallib_4_0` unless the compiler accepts it (a *retreat* to metal3.1), premised on "the macos-26 toolchain LACKS metal4.0" and "slang core is correct, no core change warranted." But the fix actually converging — **PR #12009** (the #11999 fix, jkwak-authorized, Main-verified) — is the **OPPOSITE philosophy**: RHI's per-OS 4.0 advertisement is *correct* (macOS 26 genuinely supports Metal 4.0), and the defect is Slang's **downstream `-std=metal3.1` hardcode** (slang-gcc-compiler-util.cpp:971) rejecting the correctly-emitted 4.0 attribute. #12009 derives `-std=metal4.0` from the requested cap — a **Slang-CORE fix** — and went **GREEN on all 6 macOS-aarch64 jobs** (head aba3cd7dd6 / 2a6410d9). That green run **falsifies the "toolchain lacks metal4.0" premise**: the compiler accepts metal4.0 once invoked with `-std=metal4.0`. So #12096's "fix in slang-rhi / core correct" record is **superseded** by a Slang-core fix that advances-to-4.0 rather than retreats. jkwak self-assigned BOTH and linked them → #12009 very likely RESOLVES #12096 too. If #12096 re-opens, the correct fix direction is **#12009's advance-to-4.0, NOT the triage memo's Approach-A retreat.**

**COVERAGE GAP — VERIFIED by triager 07-14 (msg 18), the bar before #12096 = fixed:** #12009's green does **NOT** cover #12096's four `gfx-unit-test-tool/*Metal.internal` tests. Those run only in the **Nightly Slang Coverage Test** workflow, still **macos-15-pinned (#12075)**; #12009 only reverted the gpu-printing quarantine in `expected-example-failure-github.txt` + re-enabled the gpu-printing *example* on macos-latest. #12009 exercises the SAME path (compute→createComputePipeline→metal4.0) via gpu-printing on macos-26 → strongly corroborates the mechanism fix, but the 4 named tests are not confirmed on macos-26. **Final Test-Plan step (jvepsalainen-nv's own): remove the macos-15 pin from `nightly-slang-coverage-test.yml` (#12075) + confirm nightly green on macos-26.**

## 🔴 2026-08-06 — REASSIGNED to jhelferty-nv, and the issue's OWN TEST PLAN is now EMPIRICALLY SATISFIED (Main-verified)

**Webhook:** jkwak-work comment **5199597341** — *"Assigning to @jhelferty-nv because he is working on a related PR"*, citing slang-rhi#802 comment **5165765104** (= **our own bot's** comment). Issue `assignees` now **`["jhelferty-nv"]`**, still OPEN, milestone Q3 2026. A maintainer ownership move — **nothing contested, no work requested of us.**

**BOTH Test-Plan bullets are DONE — verified from logs + YAML, not inferred:**
1. **The four named tests PASS on `macos-26`.** Nightly Slang Coverage Test run **30970902426** (08-05, job **92194767660**) and run **30873402266** (08-04, job **91879803328**) — both `Image: macos-26-arm64`, all four `passed test: 'gfx-unit-test-tool/{neuralSlangNetworkConverter,pointerInBufferDoublePtrRoundtrip,pointerInBufferRoundtrip,pointerInParamBlockRoundtrip}Metal.internal'`, **zero** `required_threads_per_threadgroup` occurrences.
2. **The `macos-15` pin is GONE.** #12075 was reverted by **#12129** (jkwak-work, merged 2026-07-15T22:25:25Z); `nightly-slang-coverage-test.yml` `coverage-macos` reads `runs-on: '["macos-latest"]'` at master — no pin.

⚠️ **Do NOT cite the 08-03 red nightly as a #12096 regression** — run **30780633558** (job **91584414600**). ⛔**CORRECTED 08-06 (triager caught it; my original text here was wrong twice).** It was **NOT** `docs/generated`/IR-reference tests — measured on that job: `ir-reference` **0**, `docs/generated` **2** (both shell comments in the workflow script), `FAILED test:` **0**. I had grafted the failure list from a *different* log open in the same shell (the 08-05 **green** run: 13 `docs/generated` entries, 671 `ir-reference`). **Actual cause: `Segmentation fault: 11` in `slang-test` at `run-coverage.sh:306`, on BOTH the initial run and the retry ⇒ `Process completed with exit code 139` = #12320.**

⛔⭐**And my exonerating evidence was VACUOUS, not just mis-labelled: `gfx-unit-test-tool` appears exactly ONCE in that whole log — the harness segfaulted before ever reaching the four Metal tests.** So `required_threads_per_threadgroup = 0` there proves nothing; the conclusion ("unrelated") was right by luck. ⚠️The obvious control **passes** (`passed test:` = **8,737** — thousands ran, then it crashed), so "harness alive" ≠ "target covered". The check that fires is a **per-subject** presence count. Full lesson: [[feedback_a_zero_on_a_crashed_run_is_vacuous]]. Corollary from the triager: **a crashed harness emits zero `FAILED test:` entries, so absence of failure entries reads as "nothing failed" — establish the verdict from the exit code first, per-test lines last.**

⭐**Sharpened 08-06 (triager) — LIVENESS vs COVERAGE control.** `passed test:`=8,737 is a *liveness* control ("did my instrument read anything?"); the claim needed a *coverage* control ("did the run reach the subject?"). **A crashed/truncated/early-exiting run passes liveness and fails coverage, and liveness is the one everybody runs.** Discriminator, reproduced on my edge: `gfx-unit-test-tool` **1** (08-03 red) vs **220** (both greens); `Metal.internal` **0** vs **14** — and the lone 08-03 hit is a *link* line (`Linking … libgfx-unit-test-tool.dylib`), so even nonzero needs a look at what matched. **Available before either of us published.** ⚠️Also: I over-corrected calling my verdict "right by luck" — it was right for a verifiable reason (#12320's three signatures); **the luck was in the support, not the conclusion. Audit the two separately.**

**✅ COMMENT PATCHED 08-06 02:42:17Z** — triager patched 5199730407 in place (4,910→**5,256** chars, `created` 02:33:27Z / `updated` 02:42:17Z, comments still **5** ⇒ edited not stacked; Main-verified: stale "zero failing *Metal.internal* tests" string **0**, new framing present). Right call to **patch, not annotate**: the original wording invited an incoming assignee to read it as *"the tests ran and passed"* — actively misleading, not merely weak. Now states the crash preceded the tests, all four names occur zero times, the sole mention is the build step, and *"that run is silent on this issue either way, rather than evidence of health."* PATCH was safe: no drift (`cmp` identical), still last commenter, 9 min old, **and the correction sits with the claim it fixes.**

**⭐ THE ISSUE'S CENTRAL PREMISE IS FALSIFIED, and the mechanism is the opposite of its triage verdict.** slang's submodule pin is **still `29dc332e55`**, and at that exact ref `metal-device.cpp` **still has `if (osVersion.majorVersion >= 26) addCapability(Capability::metallib_4_0);` LIVE** (verified at the ref, not at `main`). So slang CI **does** advertise metal4.0 from OS version — the tests pass anyway, because **#12009** made slang send `-std=metal4.0`: producer `source/slang/slang-code-gen.cpp:788-803` (`metalLanguageVersion = SemanticVersion(4,0)` from the same predicate as the emit gate), consumer `source/compiler-core/slang-gcc-compiler-util.cpp:975-987` (else historical `-std=metal3.1`). ⇒ **`metal 32023.883` ACCEPTS metal4.0 when invoked with `-std=metal4.0`.** The triage memo's "toolchain lacks metal4.0 / no slang-core change warranted / retreat via Approach A" is superseded on all three counts. Consistent with [[project_12325_metal4_std_flag_vs_capability]].

**Residual justification for keeping #12096 open is now narrow and hypothetical:** a real macOS-26 user whose toolchain genuinely predates 4.0 (not the CI images). **Not ours to close** ([[feedback_dont_close_open_proposals]]) — jhelferty-nv owns it.

⚠️ **Divergence worth stating plainly: slang and slang-rhi are now in OPPOSITE states on this capability.** slang's pinned rhi advertises `metallib_4_0` (and works); slang-rhi **`main` has those two lines COMMENTED OUT** (#807 stopgap, skallweitNV) with a TODO naming **#12325**. So the live tracker for *restoring* Metal 4 in slang-rhi is **#12325's Slang-version bump (7 hash sites)** — **not** this issue's OS-inference fix. Anyone acting on #12096 to unblock #802 is aimed at the wrong tracker: #807 already removed the Metal *skip*, and #802's live blocker is the bindless-ABI/Apple6-HW finding, independent of #12096.

**Routed to slang-triager** on `gh-issue-shader-slang/slang-12096` (closest-to-the-state — it owns comments 4972220997 / 4973854927) to decide the public reply. **No fixer** (pin already reverted; the rhi bump is tracked by #12325).

⛔**FACT CORRECTION 08-12: #12325 is NOT "skallweitNV's."** He *authored* it; it is **assigned to `jkwak-work`** (verified on my own edge). I conflated author with owner and wrote "skallweitNV's" three places in-store. #12325 is the right *tracker* for the rhi bump regardless of holder; name no owner rather than misattribute. [[feedback_deference_drifts_to_whoever_corrected_you_last]] inverse — a self-directed miscredit, caught by the triager.

## ✅ 08-06 — CORRECTION POSTED, chain terminal coworker-side
Triager posted comment **5199730407** (fresh delta, not an edit — last commenter was human jkwak-work, so a fresh comment notifies him; both prior bot comments left `created==updated`). 4,910 chars. **Nothing mutated**: still open / `jhelferty-nv` / `regression`+`Testing` / Q3 2026. **No close proposed** ([[feedback_dont_close_open_proposals]]). All five of my load-bearing claims re-derived independently on the triager's edge and **they hold** — decisively, the OS-inference still live at pin `29dc332e55` (read via `git show <ref>:`, not `main`).

⚠️ **One overclaim of mine the triager caught via codex BEFORE posting — keep this, it is a live slang-side hazard.** I wrote that the emit gate and the `-std` producer "test the same predicate, so they cannot disagree." **Too strong:** the producer fires only under `PassThroughMode::MetalC` (**one** site, `slang-code-gen.cpp:786`), so a **metal-source-only** output still carries the metal4.0 attribute with **no `-std` attached** for whoever compiles it downstream. Published with that caveat explicit; "it is what fixed this" softened to "fixed the observed CI failure", preserving the older-toolchain hazard as a live reason to keep the issue open.

**Provenance sharpened:** #12009's *title* is about instrumenting `gpu-printing`, but merge `a2596654f` genuinely carries the `-std` chain (5 files, both sites), clean ABSENT→PRESENT boundary; before it `-std=metal3.1` was **unconditional** at `:973` — exactly the deterministic-failure condition. First release **v2026.14**. ⚠️**The rhi submodule clone is SHALLOW (22 commits) while the superproject is not (6,747)** ⇒ `git log -S` in `external/slang-rhi` gives a false origin; use the API for any rhi history ([[feedback_shallow_clone_makes_your_head_the_graft_root]] — 2nd confirmed instance).

**RESUME:** jhelferty-nv's call on close-vs-keep-open. **Nothing owed by us.** The live tracker for restoring Metal 4 in slang-rhi remains **#12325's version bump (7 hash sites)** — **assigned to jkwak-work** (skallweitNV authored it; see the 08-12 fact correction above — do not write "skallweitNV's").

**⚠️ The pin-removal note below is now HISTORICAL — it was done by #12129 on 07-15.** Kept for the reasoning record. That it was **a WORKFLOW-YAML edit the bot CANNOT push** ([[project_bot_workflows_permission]] — no `workflows` perm) → it is a **maintainer/human action, NOT a fixer deliverable.** jkwak owns the close decision on BOTH and can edit his own pin. **DECISION 07-14: declined triager's offer to flag the fixer** — fixer is mid-iteration on #12009 with jkwak live (profile-vs-capability design round); pin-removal can't be a fixer push anyway; jkwak sequences it. Re-surface the gap closest-to-the-state ONLY when jkwak moves to close #12096 or @-mentions the bot to remove the pin (then: offer the one-line diff, note it needs a human push).
