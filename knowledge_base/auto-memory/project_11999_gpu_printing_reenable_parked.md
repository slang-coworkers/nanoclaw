---
name: project_11999_gpu_printing_reenable_parked
description: "#11999 re-enable gpu-printing macOS test — bot-filed tracking issue, blocked on infra #11973, PARKED (no dispatch)"
metadata: 
  node_type: memory
  type: project
  originSessionId: a257d1cf-f1e8-4611-904f-073f92b7d3cf
---

**#11999** ("Re-enable gpu-printing example test disabled in #11995 (new hosted macOS runners)") — opened by `nv-slang-bot[bot]` (our own coworker) on 2026-07-08 as a follow-up tracking issue.

- **Not a Slang bug.** Bot's root-cause claim (unverified by me, consistent with priors): GitHub-hosted macOS aarch64 runners intermittently fail Metal device creation → `gpu-printing` exits 255 with no output on some instances; identical code passes on others. Infra/runner-health, tracked in [[project_11985_macos_metal_capability_regression]] neighborhood (macOS hosted-runner Metal flakiness — same class as #11903 "macOS hosted-runner infra flake").
- **What was disabled:** #11995 added `macos:aarch64:(debug|release):gpu-printing` to `tests/expected-example-failure-github.txt`, consumed by `.github/workflows/ci-examples.sh --skip-file`. Quarantined on macOS aarch64 debug+release. Still covered on Windows; already skipped on Linux x86_64 under #5520.
- **Blocked on #11973** (runner-health tracking). Follow-up = remove the one quarantine line + confirm `gpu-printing` passes reliably on macOS aarch64.

**Initial routing (07-08): PARKED** — bot-authored, self-triaged, not actionable until #11973. Superseded ↓.

**RE-OPENED 07-08 by maintainer `jkwak-work`** (comment 4919492752, `@nv-slang-bot` mention). jkwak **disputes the bot's "runner-health, not a Slang bug" root cause**: "The example is very simple and it shouldn't fail… I believe this is a Slang bug not a runner problem, because whatever the OS or spec it is, the example is expected to run properly." Also: test **cannot stay disabled** (temporary workaround only), and he **needs more info to decide how to proceed**.

- Dispatched to **slang-triager** on `gh-issue-shader-slang/slang-11999`, `<github-post-authorized />` (real bot mention). Ask: investigate the *actual* `gpu-printing` failure on macOS aarch64 — is it a genuine Slang/Metal codegen bug (jkwak's hypothesis) vs. the runner Metal-device-creation flake the bot originally claimed? Reproduce if possible; distinguish "exit 255, no output, device-creation failure" (env) from a deterministic compile/runtime bug (Slang). Report findings to jkwak on GitHub.
- ⚠️ My original park cited "runner-health flake" as the root cause — that is the bot's **unverified** claim, now explicitly challenged by the maintainer. Do NOT relay it as fact. Triager must verify from scratch.

**TRIAGE COMPLETE 07-08 (slang-triager, verified vs master d8e8e1a9e).** Verdict posted to jkwak on GitHub: comment **4919689391** (https://github.com/shader-slang/slang/issues/11999#issuecomment-4919689391). Memo: `inbox/6/triage-11999.md`.

Evidence-backed findings — **both prior positions partly wrong:**
- Red = "Run Slang examples" step; `gpu-printing` exits **255 / zero output** = `execute()` returned SLANG_FAIL. "Test Slang" (slang-test) step is GREEN same jobs.
- **NO Slang diagnostic on failure** → compile/codegen ruled out. Dies at an RHI null-return: `createDevice`/`createShaderProgram`/`createComputePipeline` (`examples/gpu-printing/main.cpp:85-104`).
- **INTERMITTENT on unchanged master** (~4 pass / ~15 fail same hours) → jkwak's "deterministic Slang bug" NOT supported.
- Bot's cited `metal4.0/E40003/createComputePipeline` signature is a DIFFERENT non-fatal **gfx-unit-test** signature in the green Test Slang step — #11973 conflated the two. Bot's specific root cause WRONG.
- 3 sibling Metal examples (platform-test/shader-toy/triangle) PASSED on the SAME runner in a failing job → "device dead on this instance" also refuted. Runner = hosted `macos-latest` **"Apple Paravirtual device"** (virtualized Metal).
- Env-vs-Slang **not decidable from current logs** — the example swallows the error silently.

**Recommended path (Approach A):** instrument `gpu-printing/main.cpp:85-104` to report WHICH stage failed + enable RHI debug layers → next red run is definitive; then bisect vs slang-rhi ToT bump **#11960 (07-06)** if it dies past device creation.

**DECISION 07-08: HELD/parked for jkwak — no fixer dispatch.** The instrumentation touches `examples/` + interacts with jkwak's own quarantine (#11995) = maintainer/CI-policy call. The GitHub comment already offers the draft PR conditionally ("if you'd like"); dispatching before he answers would make that ask rhetorical. jkwak self-fixes often → may take it himself. Comment hygiene: bot is last commenter, no new comment until he replies.

**Re-engage when jkwak replies (webhook → this canonical thread):** if "yes, instrument it" → dispatch **slang-fixer DRAFTS-ONLY** via slang-triager (Approach A instrumentation, then bisect vs #11960 if it dies past device creation); if he takes it himself → stand down; if he wants a different path → adjust. Quarantine stays temporary per his instruction.

**JKWAK APPROVED 07-08** (comment **4919730075**): "please do as you said: I can hand a fixer this instrumentation change as a draft PR if you'd like." → Forwarded to **slang-triager** on canonical thread to dispatch **slang-fixer DRAFTS-ONLY** for Approach A:
- Instrument `examples/gpu-printing/main.cpp:85-104`: report WHICH RHI stage failed (device/module/program/pipeline) + enable RHI debug layers so the next CI red run is diagnosable (not silent exit 255).
- `Addresses #11999` (NOT Closes — instrumentation is diagnostic, doesn't fix; re-enable only after root cause + fix). Then bisect vs slang-rhi ToT bump #11960 (07-06) if failure is past `createDevice`.
- Fixer: open DRAFT PR, `report_pr_created`, and (closest-to-state, draft-held) post the 5-bullet on #11999 (verdict = "instrumentation in draft PR #N, held pending review"). Quarantine stays temporary.
Gated: ready-flip + merge = maintainer only.

**RESOLVED (this step) 07-08 — DRAFT PR #12009 (Main-verified live via GitHub API):** OPEN/draft, base `master` ← head `fix/issue-11999`, `examples/gpu-printing/main.cpp` only (+63/−0), title "Instrument gpu-printing example to diagnose macOS Metal failure (#11999)", body **`Addresses #11999`** (NOT `Closes` ✓), `pr: non-breaking`. Quarantine line in `expected-example-failure-github.txt` untouched; no test re-enabled. `report_pr_created` done (webhooks route to fixer). 5-bullet posted on #11999 (verdict "held pending review").
- **What shipped:** local `IDebugCallback` + `enableValidation=true` on `DeviceDesc` + stage-labeled `reportError` before each of the 4 previously-silent `return SLANG_FAIL` sites (device/module/program/pipeline). Safe to enable validation unconditionally — `ci-examples.sh run_sample` is exit-code-only, no golden stdout for gpu-printing. Compile-verified (BUILD_EXIT=0, clang-format clean, fixer codex critique passed); NOT runnable on Metal locally (no GPU in Linux container) — correct bar, diagnostic value materializes only on the macOS CI runner.
- **Gated correctly:** fixer did NOT flip ready/merge (maintainer-only).

**07-09 00:02 — jkwak APPROVED + self-flipped ready (Main-verified via timeline API):** `ready_for_review` by **jkwak-work** @00:02:35Z, then review APPROVED ("Looks good to me", 4658743807) @00:02:45Z. Non-draft state is **maintainer-driven → NOT a fixer breach** (fixer correctly never flipped; [[feedback_drafts_only_guardrail]] exception applies — verified actor). PR still `Addresses #11999` (not Closes), main.cpp only (+63/−0). `mergeable_state: blocked` (CI/merge-queue gate), state open, **not yet merged**. Approval ≠ merge — remaining gate is jkwak's own merge, his call.

**Stale public comment (07-09, Main-verified):** fixer comment **4920280530** (nv-slang-bot @00:07:12Z) asserts "intentionally a **draft**" + "bot can't mark its own PRs ready-for-review" — but jkwak self-flipped ready @00:02:35Z (~4.5 min earlier) and PR is `draft:false`. → Directed triager→fixer to **edit-in-place** (bot was last commenter; hygiene rule [[feedback_github_comment_hygiene]]). **Edit 403'd** → fixer posted referencing correction **4920297346** (@00:10:22Z): "disregard the 'intentionally a draft' line; PR is non-draft, approved, mergeable, awaiting only your merge." Stale 4920280530 still present but neutralized by the correction. Main-verified — public surface now accurate. (Edit-403 → referencing-correction is the acceptable fallback when in-place edit is blocked.)

**07-09 ~01:25 — approval DISMISSED by a real build fix (Main-verified at claim-precision):** jkwak's ready-flip auto-triggered full pull_request CI → `build-macos-{release,debug}-clang-aarch64` failed `-Werror -Wformat-security`: 3 of the 4 new stage-labeled `reportError` calls passed a **non-literal format string with zero varargs** (clang errors; gcc/Linux doesn't → fixer's Linux-container verify structurally COULDN'T catch it). Fix = commit **0df0c244** (main.cpp only, +5/−3): routes the 3 strings through `reportError("%s", "...")`, matching the file's existing diagnoseIfNeeded idiom. Scope unchanged (diagnosability-only, quarantine untouched, still `Addresses #11999`). Verified: head now `0df0c244a1`, `reviewDecision: REVIEW_REQUIRED` (force-push dismissed jkwak's approval — expected), non-draft, `mergeable_state: blocked` (CI re-running on new head, auto-triggered not manual). Explanation posted cmt 4920644189. **Net-positive:** ready-flip's macOS CI caught a bug pre-merge no Linux verify could — better here than post-merge. Lesson reinforced: [[feedback_verify_branch_in_env_where_it_fires]] — Linux-container compile ≠ macOS-clang compile.

**NEXT:** CI must go green on `0df0c244` → jkwak re-reviews + merges (maintainer-gated). After merge + an instrumented macOS CI run names the failing stage → if it dies **past `createDevice`**, follow-up = **bisect vs slang-rhi ToT bump #11960 (07-06)** (flagged in PR body, separate PR). slang-triager holds this triage thread; re-engages on any substantive jkwak comment on #11999. slang-fixer owns #12009 review/CI webhooks + will act on next ci_* result.

**⚠️ RE-APPROVAL DISMISSED AGAIN 2026-07-14 (Main-verified at HEAD — fixer's "RE-APPROVED, awaiting merge only" is STALE/WRONG).** Fixer folded jkwak's requested #11995-revert, jkwak APPROVED, but current HEAD `67429ebdff` shows **reviewDecision=REVIEW_REQUIRED**, jkwak review DISMISSED (dismissal 16:47:47, was `approved`, on commit `67429ebdff`). So #12009 is NOT approved-awaiting-merge; it needs **RE-approval** (a review-request is now on zangold-nv/others per reviewRequests). A post-approval push dismissed the approval at HEAD (dismissal 16:47:47, was `approved`, on `67429ebdff`). Do NOT record #12009 as approved; it awaits re-approval; mergeState BLOCKED = REVIEW_REQUIRED + CI. **OWNERSHIP AMBIGUOUS (unresolved):** the slang-fixer session I corrected (working #11982/#12034) **disclaims touching #12009 this session** — yet a slang-fixer msg (35648) ~6 min earlier reported driving #12009's revert-fold + re-approval. So either two slang-fixer sessions (shared identity) or memory drift; I did NOT force the #12034 session to act on #12009 (correct — don't push into another owner's head, [[project_stacked_pr_shared_base_clobber]]). The dismissed-approval is a real HEAD state that the OWNING #12009 session must handle (its webhooks / the supervise tracker), NOT this session. Do not adjudicate the session-identity puzzle from Main; let the owning session's webhook flow carry it. Shared learning (post-approval-push-dismisses) saved regardless. Reopens on re-approval or substantive jkwak comment.

**🎯 DECISIVE ROOT CAUSE + SCOPE EXPANSION 2026-07-14 ~17:07 (fixer msg 35658 — ownership now UNAMBIGUOUS: this slang-fixer session OWNS #12009, maintainer-authorized).** The instrumentation shipped in #12009 did its job — the next macOS red run named the failing stage decisively:
- **Evidence (CI run 29350920318, job 87148047069, macOS 26.4, head 67429ebd):** gpu-printing fails at `createComputePipeline` (device+module+program all succeed). The PR's added instrumentation logged verbatim: `[Slang]: ERROR: metal 32023.883: kernels.slang(19): error : 'required_threads_per_threadgroup' attribute requires Metal language standard metal4.0 or higher` — for emitted `[[required_threads_per_threadgroup(32, 1, 1)]]` (lowered from `[numthreads(32,1,1)]`). All sibling Metal examples PASSED the same job → **gpu-printing-specific, deterministic codegen — NOT the flaky driver race.**
- **This REVIVES the #11985 Metal-4.0-attribute diagnosis I overturned 2026-07-10** — I overturned it *conditionally* ("only revives if #12009's instrumentation implicates the attribute"); the condition is now MET. See [[project_11985_macos_metal_capability_regression]].
- **Scope expanded — AUTHORIZED:** jkwak-work @-mentioned the bot (cmt 4971839190): "review the log and iterate the fix until the issue is resolved on the CI workflow … make sure the test is passing with the MacOS 26." Moving from instrument-only → Metal-codegen fix. Fixer acked on PR (cmt 4971844602). **This supersedes my earlier "#12009 not your chain, stand down" — that stand-down was scoped to the #12034 session; THIS session owns #12009 with explicit maintainer authorization to iterate the fix.** Code pushes to fix/issue-11999 are not gated ([[feedback_pushes_not_gated]]); jkwak flipped #12009 ready himself (07-09) and now asked for iteration, so no drafts-only concern.
- **DECISION FORK routing to me (fixer's own contract, endorsed):** the likely fix touches #10592's capability-default policy (bare `-target metal` implying `metallib_4_0`) = **jhelferty-nv's sign-off area**. Fixer will pick the NARROWEST correct fix. If it's a **local codegen gate** (don't emit the attribute below metal4.0) → fixer proceeds solo. If it turns on a **capability-DEFAULT policy call** → fixer surfaces A1/A2/A4 to me BEFORE committing the policy-level one. **I endorsed this split; awaiting either the fix-report (local gate) or the A1/A2/A4 options (policy-level).** Code-reader is confirming the current emit gate + default still match the stale-hold note's claims.

**🔧 FORK RESOLVED → SOLO PATH, NOT POLICY-LEVEL 2026-07-14 ~17:22 (fixer msg 35670; 2 code-readers + fixer read the key line directly).** Full mechanism nailed — reconciles the triager's retraction AND supersedes jkwak's driver-race theory with a DETERMINISTIC cause:
- **slang-rhi #795 (`29dc332e`, 2026-07-06 = the #11960 ToT bump #11985 predicted)** made the Metal backend advertise caps by OS version: `external/slang-rhi/src/metal/metal-device.cpp:266` → **macOS ≥26 registers `metallib_4_0`** (macOS 15 only reached `metallib_3_2`), passed to the Slang session.
- Slang emit gate `slang-emit-metal.cpp:215` (`implies(metallib_4_0)`) then **correctly** emits `[[required_threads_per_threadgroup(32,1,1)]]`.
- BUT Slang compiles emitted MSL→metallib via its downstream `metal` invocation with **`-std=metal3.1` hardcoded** (`slang-gcc-compiler-util.cpp:973`, unconditional for MetalAIR — fixer read directly). 4.0 syntax + 3.1 std → metal compiler rejects. The `[Slang]:`-tagged error = Slang capturing its own downstream compiler's stderr (RHI just `newLibrary`s the finished blob).
- **Why macos-15 passed = mechanism not race:** macos-15 never advertised 4_0 → attribute never emitted → no collision. macos-26 flips emission on → hits the 3.1-std hardcode. Deterministic.
- **Fork classification = LOCAL codegen inconsistency, NOT #10592 capability-default policy:** bare `-target metal` still correctly does NOT imply 4_0 (confirmed); RHI's per-OS 4_0 advertisement is *correct* (macOS 26 genuinely supports Metal 4.0). Defect is Slang-side internal inconsistency — emitter honors requested metallib cap, downstream `-std` ignores it. → **"local gate, proceed solo" bucket. No jhelferty-nv sign-off, no A1/A2/A4. I STOPPED holding for the policy options.**
- **Planned fix (Slang-side, one source of truth):** derive `-std=metalX.Y` from the requested metallib capability instead of hardcoding 3.1 (metallib_4_0→metal4.0, 3_2→3.2, floor 3.1 when no cap). Makes emit+compile consistent by construction; makes the 4.0 attribute work on macOS 26. One reader confirming whether the metallib version reaches the command-builder or must be threaded through, before writing.
- **Verify limit:** Linux has no metal compiler → emit verifiable locally (`-capability metallib_4_0` present / `3_1` absent) but the `-std` change is macOS-CI-only. "Iterate until CI green" is the oracle, as jkwak framed it.
- **This fix RESOLVES the "latent emit-vs-std inconsistency" #11985's reconciliation flagged** ([[project_11985_macos_metal_capability_regression]]) — strengthens the reconcile narrative. But #11985 is TRIAGER-owned and separately gated on reverting #12075; fixer must NOT touch #11985.
- **⚠️ BLAST-RADIUS caution I gave the fixer:** the `-std` derivation change affects ALL Metal downstream compilation, not just gpu-printing — the existing slang-test Metal suite must stay green; only the CI oracle proves the std-flag change (Linux verifies emit only). Reconcile-post must be honest that CI-green is the proof.
- **PRE-GITHUB CHECKPOINT:** fixer committed to report the DIFF + the reconcile-post text to me BEFORE it lands on GitHub. **I'm holding for that** — it's my verification gate (not re-reading the source now; the diff review is the gate). Proceeding solo to implement+test+push.

**✅ CHECKPOINT REVIEWED + GO 2026-07-14 ~17:53 (fixer msg 35672 = diff +43/−1, 4 files + reconcile-post-draft.md).** Diff design (fixer's report): new `SemanticVersion metalLanguageVersion` field on `DownstreamCompileOptions` (trivially-copyable, default {}=unset); `slang-gcc-compiler-util.cpp:971` MetalAIR branch → `if isSet() emit -std=metalMAJ.MIN else the exact old -std=metal3.1` (byte-identical else-branch); `slang-code-gen.cpp:774` MetalC path → `if implies(metallib_4_0) set SemanticVersion(4,0)` (SAME predicate as emit gate slang-emit-metal.cpp:215); test adds a `-capability metallib_4_0` metallib case to `tests/metal/threadgroup-size.slang` asserting `define void @computeMain`.
- **Main independently VERIFIED the load-bearing external claim** — `slang-rhi@29dc332e src/metal/metal-device.cpp`: caps advertised by OS version, `>=15 → metallib_3_2`, `>=26 → metallib_4_0` (no 4.0 before 26), passed via `m_slangContext.initialize(..., getCapabilities(), ...)`. RHI mechanism CONFIRMED, not relayed.
- **Blast radius is well-bounded (I reasoned through it):** the ONLY behavior change fires when `implies(metallib_4_0)` — i.e. macOS-26 runs. macOS-15 (no 4.0 cap) hits the byte-identical else → `-std=metal3.1` unchanged. And even IF emit/code-gen caps sources diverged, the failure mode is SAFE (either stays-red-and-caught, or `-std=metal4.0` on 3.1-valid code compiles since 4.0 std is a superset) — NOT a silent-correctness hazard. Guardrail #1 holds robustly.
- **Reconcile-post reviewed:** accurate mechanism narrative, honest about the CI-is-oracle verify limit, posts #12009-only, leaves #11985 to triager, has the automated-coworker disclaimer footer. GAVE 2 precision refinements before it posts: (a) don't overclaim the original triage was simply "right" — the attribute+std-3.1-mismatch INSIGHT was right, but the cap-SOURCE (RHI-per-OS, NOT #10592's `-target metal` default) and the DIRECTION (fails on NEW macos-26 where 4.0 IS advertised, not on OLD <4.0 runners as originally predicted) are the corrections the instrumentation supplied; (b) explicitly CREDIT jkwak's macos-26 correlation as vindicated (his data was right; only "race" interpretation was wrong) — accurate + good maintainer relations.
- **GO given:** push once the ~20min build passes its local Metal-EMIT checks (`-capability metallib_4_0` present / `metallib_3_1` absent); CI is the oracle for the `-std` change. Awaiting the fixer's post-push fix-report (PR #12009 head + CI status).

**✅ FIX PUSHED + VERIFIED AT HEAD 2026-07-14 18:55 (fixer msg 35764; Main-verified via PR files+comments API — not relayed).** #12009 head `aba3cd7dd63ee8` (non-draft), `reviewDecision=REVIEW_REQUIRED` (fix push dismissed prior; expected), MERGEABLE, `merge=BEHIND` (master advanced — rebase-at-merge, not a CI blocker). **PR now bundles the full scope (verified via `pulls/12009/files`):** instrument `examples/gpu-printing/main.cpp` (+65) + **QUARANTINE REMOVAL** `tests/expected-example-failure-github.txt` (−1 — the actual macOS re-enable, correct now that it's a fix-not-instrument PR: removing the quarantine is HOW CI proves macOS-26 passes) + fix (`slang-downstream-compiler.h` +6, `slang-gcc-compiler-util.cpp` +16/−1, `slang-code-gen.cpp` +12, `tests/metal/threadgroup-size.slang` +10). Body `Addresses #11999` (conservative — maintainer decides auto-close; re-enable+fix does resolve it, but `Addresses` is safe). Main `ci.yml` id=29359816710 auto-running on the head (no manual dispatch). Local (Linux) verify: emit gate unperturbed (4_0→attr present / 3_1→absent, both PASS), metallib case ignored (no metal compiler); codex CODE_REVIEW + OUTPUT_REVIEW approve, 0 must-fix. **The `-std` change itself is macOS-CI-only — green macOS-26 ci-examples is the proof.**
- **Reconcile comment 4972846497 posted on #12009 (Main read full body) — incorporates BOTH my refinements accurately:** (a) credits jkwak's macOS-26 correlation as correct ("only the driver-race reading was off; the cause is a deterministic version mismatch"); (b) corrects the retracted diagnosis's SOURCE (slang-rhi per-OS advertisement, not #10592's bare-`-target metal` default) + DIRECTION (fails on NEW macos-26 where 4.0 IS advertised, not older sub-4.0 runners) without overclaiming "right all along" — "surviving insight is the attribute-vs-`-std=metal3.1` mismatch." Honest CI-is-oracle note + automated-coworker footer. #12009 only; #11985 left to triager. ✓
- **slang-reviewer dispatched in parallel** (fixer owns that peer-wire). Chain webhook-parked: next `ci_*` (the macOS-26 oracle) / review verdict. **Terminal path:** green CI → jkwak re-review → maintainer merge (operator/maintainer-gated) → issue re-enabled/closed → reap. If CI RED on the `-std` change → fixer iterates ("until CI green" per jkwak). Do NOT relay green/merge until webhook-confirmed.

**🎯 FIX GREEN ON THE ORACLE 2026-07-14 20:16 (fixer msg 35778; Main-verified independently via `commits/aba3cd7dd6/check-runs`).** **All 6 macOS-aarch64 jobs SUCCESS** — `build-macos-{release,debug}`, `test-macos-{release,debug}-clang-aarch64 / test-slang`, both `test-slang-rhi`. **The `-std=metal4.0` fix works on macOS-26 — gpu-printing no longer fails there** (the exact thing jkwak asked "make sure the test is passing with the MacOS 26"). This validates the whole mechanism end-to-end (RHI advertises 4.0 by OS → emit → derived -std → compiles).
- **BUT 4 jobs RED (Main-verified, exactly the fixer's list):** `check-ci` (aggregator) + `test-windows-{release,debug}-cl-x86_64-gpu / test-slang` + `test-linux-release-gcc-x86_64-cpu / test-slang`. All formatting/labels/40-other-checks green.
- **These are NON-Metal platforms — the `-std=metal4.0` change is structurally inert there** (only fires for `MetalC && implies(metallib_4_0)`; Linux-CPU/Windows-GPU never take that path). So a real cross-platform regression FROM the fix is near-impossible. Fixer's live hypotheses (correct ranking): (a) the NEW `tests/metal/threadgroup-size.slang` METALLIB4 case misbehaving cross-platform (most likely — if not gated to mac: `-target metallib` on a platform w/o the metal toolchain, in the broad test-slang corpus, would fail on Linux/Windows exactly as observed); (b) pre-existing `compute/*` SIGSEGV / JSON-RPC infra flakes known on these exact jobs. **Fixer classifying now** (subagent extracting exact failing test names + whether its test is among them). Committed action tree: its test → fix/gate it; flake → `gh run rerun --failed`; real regression → investigate (unlikely). **Fixer owns this — do NOT double-dispatch/duplicate the classification** ([[feedback_let_fixer_own_single_session]]). Awaiting classification + action report.
- **Do NOT relay "CI green / merge-ready" upstream yet** — macOS-green is confirmed but `check-ci` is RED until the 3 non-Metal jobs resolve; jkwak's "iterate until CI green" oracle isn't satisfied at the aggregate level. Reviewer A (correctness) still running; B/C done 0 bugs.

**🔬 RED CLASSIFIED + jkwak DESIGN-REVIEW ESCALATION 2026-07-14 20:42 (fixer msg 35780).** Head now `2a6410d9`.
- **RED classification (fixer):** (a) Linux-CPU = **pre-existing slang-test SIGSEGV flake, not the fixer's** (matches known `compute/*` infra flakes on that job); (b) Windows-GPU = **the fixer's new metallib test case mis-gated** — Windows-GPU + macos-15-nightly ship **pre-4.0 metal compilers** and slang-test has NO downstream-compiler-VERSION gate → the `-target metallib -capability metallib_4_0` case fails on older-toolchain lanes. Fixer **removed the non-portable test case** (threadgroup-size.slang == master again; emit-only cases retained). Fresh CI id 29366018580 on `2a6410d9`; codex CODE+OUTPUT approve.
- **jkwak escalated 2 asks on #12009 (both acked GitHub same-turn):**
  1. **Design question (inline slang-code-gen.cpp:787):** "is `options` the right place to store this? investigate how SPIRV decides its version; check not only the capability-atom but ALSO the profile setting (users may set profile to metal 4.0)." → **genuine correctness point:** the fixer's `implies(metallib_4_0)` gate may MISS a profile-set-4.0 case → derived `-std` would stay 3.1 → SAME bug class. Fixer investigating: how SPIR-V version-selection models this (jkwak's reference); whether metal profiles can express ≥metal-3.0/4.0; whether profile feeds the capability set (if so, `implies()` already covers it; if profile is a separate channel that bypasses the atom, the check is incomplete).
  2. **Re-add a metallib test** (ref #10592) — the exact case just removed. jkwak's evidence: on HIS metal compiler (32023.155) the attribute is `warning: unknown attribute ignored`; on macos-26 CI (32023.883) it's honored → **genuinely toolchain-version-sensitive.** Fixer flagged the CI-portability wrinkle, will find a form giving real coverage without redding older-toolchain lanes (gate to macos-latest, or key off actual 4.0 acceptance).
- **Main GUARDRAIL given (msg below):** resolve the profile-vs-capability path DEFINITIVELY, be honest if inconclusive rather than overclaiming "already covered" on jkwak's own correctness point (a wrong "covered" claim = public miss needing retraction). Findings-comment answering jkwak's design question may post directly (free-posting dialogue turn); **the CODE change is gated — fixer committed to report findings + planned change to me before pushing.** Chain is a LIVE design-review round, NOT terminal/blocked. Webhook-driven.

**✅ DESIGN ROUND RESOLVED — "NO CODE CHANGE NEEDED" VERDICT, Main-INDEPENDENTLY-VERIFIED at source `2a6410d9` 2026-07-14 20:49 (fixer msg 35782 + my own read of slang-target.cpp + slang-profile-defs.h).** The fixer met my trace-to-ground bar; I re-verified the 2 load-bearing claims myself (not relayed):
- **CLAIM 1 — profile is NOT a bypass; it feeds the same cap set the gate reads. CONFIRMED.** `slang-target.cpp getTargetCaps()`: `CapabilitySet profileCaps = optionSet.getProfile().getCapabilityName();` then, after building the target atom set (`metal` for Metal targets), `if (profileCaps.atLeastOneSetImpliedInOther(targetCap)==Implied) targetCap.join(profileCaps);` and the explicit `-capability` atoms are joined too → `cookedCapabilities = targetCap`. So the fixer's gate `getTargetCaps().implies(metallib_4_0)` (slang-code-gen.cpp:782) reads a set that already incorporates any profile-carried version. Not a separate channel.
- **CLAIM 2 — Metal profiles cap at metallib_2_4; no metal 3.x/4.0 profile exists. CONFIRMED.** `slang-profile-defs.h`: only `PROFILE_VERSION(METAL_2_3/2_4)` + `PROFILE(metallib_2_3/2_4)` — NO metallib_3/4 profile. So jkwak's hypothetical "user sets profile to metal 4.0" is **unreachable today** (no such profile). The gate can't be under-covered by a profile channel that doesn't exist at ≥3.0.
- **SPIRV model (jkwak's reference) confirms the LAYER:** `determineSpirvVersion` scans `getTargetCaps()` atoms (profile already folded in) and stores per-emit (`SPIRVEmitSharedContext::m_spvVersion`), not on TargetRequest — the fixer's change mirrors this (capability-driven, stored per-emit in `DownstreamCompileOptions`). Same shape/layer.
- **⚠️ ONE UNVERIFIED-BY-MAIN detail (non-load-bearing):** the fixer's *future-proofing* line ("if a metal_4_0 profile is ever added, getCapabilityName() maps it via the existing `METAL_4_0=metallib_4_0` alias → gate fires automatically") — I could NOT find that alias in slang-profile-defs.h; it'd be in the capdef, which I didn't pull. This is forward-looking and NOT part of the current-correctness verdict (which rests only on the 2 confirmed claims). Flagged to fixer: if jkwak probes the future case, verify the alias in the capdef before defending it; don't over-defend proactively.
- **jkwak WITHDREW the metallib-test request** (cmt 4973830865): "we cannot really test if metallib will be happy … when the runner may run with an older version of metal compiler. Please ignore my request." Fixer's portability evidence landed. Test stays removed; threadgroup-size.slang == master. Ask #2 CLOSED.
- **Gate released:** no code change to review (verdict = gate is correct at the right layer). Fixer posted the trace on jkwak's inline thread (r3582522948); offered an optional in-place invariant comment/assert (trivial gated follow-up if jkwak wants). **Net: both jkwak asks resolved on-thread, zero code change. Head `2a6410d9` = macOS-26 fix VERIFIED green + instrument + revert + `-std` fix. Awaiting jkwak response + maintainer merge.** Fresh CI 29366018580 on `2a6410d9` (metallib test removed → Windows-GPU should clear; Linux-CPU was the pre-existing SIGSEGV flake). Do NOT relay merge-ready until check-ci green + jkwak re-approve, webhook-confirmed.

**✅ DESIGN ROUND CLOSED BY MAINTAINER 2026-07-14 20:55 (fixer msg 35788).** jkwak replied "Sounds good then" on the design thread (r3582560588), **accepting the "no code change needed" trace.** Both asks now CLOSED on-thread: (1) profile question resolved (no change, gate correct at right layer — Main-verified at source); (2) metallib test withdrawn by jkwak. The design thread is SETTLED, not contested/open. **No further code action on #12009.** Only remaining gates: (a) fresh CI on `2a6410d9` (whether Windows-GPU cleared with the non-portable test removed; Linux-CPU was the pre-existing SIGSEGV flake), (b) jkwak's merge. Fixer correctly not replying to the ack, not polling; will report the CI result or merge webhook. **Still do NOT relay merge-ready until check-ci green webhook-confirmed** — design-acceptance ≠ CI-green ≠ merged.

**✅ jkwak APPROVED at HEAD 2026-07-14 20:56:03Z (Main-verified independently — genuine, not stale).** jkwak-work re-APPROVED #12009 (review 4698468311, "Looks good to me") on the CURRENT head `2a6410d9b8` — after the design round + test resolution. `gh pr view`: isDraft=false, reviewDecision=**APPROVED**, MERGEABLE, `mergeStateStatus=BEHIND`. So the fix is now **maintainer-approved end-to-end**: instrument → revert → `-std=metal4.0` fix, macOS-26 CI verified green earlier, design round closed, approved. **REMAINING GATES (not yet merge-ready):** (a) fresh `ci.yml` 29366018580 on `2a6410d9` still **queued/in-progress** — Windows-GPU/Linux jobs pending; can't yet confirm Windows-GPU cleared with the non-portable test removed (that's the next real event); (b) `BEHIND` (master advanced — rebase-at-merge, jkwak's to action). Fixer will NOT rebase/push (a push dismisses the fresh approval; rebase/merge operator/maintainer-gated). **Terminal = CI green → jkwak merges.** Fixer reporting only on the next real event (CI result esp. Windows-GPU, or merge). Approved ≠ merged; do NOT relay merged until the merge webhook.

**✅ CI FULLY GREEN + APPROVED END-TO-END — MERGE-READY 2026-07-14 23:42 (Main-verified independently via `commits/2a6410d9/check-runs`).** Run `ci.yml` 29366018580 = **conclusion SUCCESS**: 51 jobs success, 16 skipped, **ZERO failures** at head `2a6410d9`. All 3 previously-red lanes cleared: `test-macos-{debug,release}-clang-aarch64` ✅ (the `-std=metal4.0` fix works — original macOS-26 gpu-printing failure GONE), `test-windows-release-cl-x86_64-gpu` ✅ (cleared by removing the non-portable metallib test), `test-linux-release-gcc-x86_64-cpu` ✅ (the earlier SIGSEGV was the pre-existing flake — cleared on the fresh run, confirming the fixer's classification). **jkwak's "iterate until CI green" oracle is now SATISFIED.** reviewDecision=APPROVED (jkwak review 4698468311 + observation cmt 4975113135). **Full PR scope shipped:** instrument (diagnose) + revert #11995 (re-enable) + `-std` fix (derive metal language std from metallib capability).
- **⚠️ merge-state label:** fixer reported `BEHIND`; I see `mergeStateStatus=BLOCKED` (MERGEABLE=true, APPROVED, all CI green). BLOCKED here = branch-protection "must be up-to-date with base" / merge-queue gate, NOT a code/CI problem — resolved by the maintainer's rebase/update-branch at merge. Either label → the action is jkwak's; fixer correctly won't rebase (dismisses approval) or merge (gated). Operationally identical, no concern.
- **Terminal = jkwak merges** (only remaining gate; operator/maintainer-gated). **Still do NOT relay "merged" until the merge webhook** — merge-ready ≠ merged. Fixer holding, webhook-driven, reaps on MERGED/CLOSED. This chain (the #11985 macOS-26 reconciliation + re-enable) is one merge away from terminal-success; the merge feeds #11985's closure (triager-owned, gated on #12075 revert — unaffected here).

**✅✅ MERGED — TERMINAL SUCCESS 2026-07-15 02:46:38Z (Main-verified via `github_get_pull_request`: state=closed, merged_at=2026-07-15T02:46:38Z).** jkwak-work merged #12009 (merge commit `a2596654` per fixer). Full three-part PR shipped upstream: (1) instrument gpu-printing's 4 silent SLANG_FAIL sites + slang-rhi validation callback (surfaced the root cause); (2) revert #11995 (re-enable the example on hosted macOS); (3) the fix — derive downstream `-std=metalX.Y` from the target's metallib capability instead of hard-coded `-std=metal3.1`, so the metal4.0-only `[[required_threads_per_threadgroup]]` compiles on macOS-26. Both jkwak review asks resolved on-thread with no code change (profile-vs-capability design Q traced to ground + Main-verified at source; metallib-test withdrawn by jkwak after CI-portability evidence). Full CI was green at merge; approval stood. Fixer reaped worktree `wt-slang-11999` + cleared sentinel.
- **Issue #11999 stays OPEN** — PR used `Addresses` (not `Closes`) → no auto-close; per standing order [[feedback_github_writes_operator_authorized]] the bot does NOT nudge it closed. Whether to close #11999 (now that the re-enable+fix landed) is a maintainer call; the tracking issue can stay open until a maintainer confirms macOS-26 stability over subsequent runs, or close it — their decision, not the bot's.
- **This is the #11985 macOS-26 reconciliation arc's payoff:** the instrumentation the triager/fixer shipped in #12009 supplied the missing link that revived + CORRECTED the retracted attribute diagnosis (source = slang-rhi per-OS advertisement, direction = fails on NEW macos-26), superseding jkwak's driver-race theory with a deterministic mechanism — and the `-std` fix resolves the latent emit-vs-std inconsistency #11985 flagged. **#11985 itself (triager-owned) remains separately gated on reverting #12075** (jkwak's closing precondition) — #12009's merge does NOT auto-close #11985; the triager owns that. **Chain TERMINAL.** Note historical.

**⚖️ APPROVER DECISION 2026-07-14 ~17:25 — ABSTAIN_POLICY / OPEN_GAP @ head `291f75b1` (slang-pr-approver msg 6, shadow-mode ledger-only, NOTHING posted to GitHub).** Ready-for-review/synchronize webhook for #12009 routed to approver; it decided:**
- **Head advanced past memory's last-recorded `67429ebdff` → now `291f75b103c47555d6485d31863d9dd3a0e92cb5`.** So the fixer has pushed further (std-flag fix work + the un-quarantine line removal — the PR now REMOVES the `expected-example-failure-github.txt` quarantine, re-enabling the example on macOS).
- **Verdict = ABSTAIN_POLICY (OPEN_GAP), NOT a block.** All 6 eligibility clauses pass (policy `v0-shadow-relaxed`, no CI-green requirement). Tier = Devin-only fallback (production Claude PR Review SKIPPED — bot fix-branch touching `examples/**`+`tests/**`; harvest exit 20; Devin attempt-2 clean: 0 bugs / 0 flags / 1 informational). Instrumentation code judged correct (RHI API verified). Challenger held the block: the re-enabled example **still fails on `291f75b1`** — `test-macos-release-clang-aarch64` FAILURE, gpu-printing exit 255, `kernels.slang(19)` emits `[[required_threads_per_threadgroup(32,1,1)]]`. "Human must look."
- **⚠️ FRAMING TENSION — UNADJUDICATED (do NOT relay approver's read as fact):** approver's decision text frames the failing runner as **sub-Metal-4.0** ("`GPUFamilyApple6 not supported`", "needs Metal 4.0", runner sub-4.0). This CONTRADICTS the fixer's deep root-cause (msg 35670, 2 code-readers read source directly): macOS 26 runner genuinely SUPPORTS Metal 4.0 (RHI #795 correctly advertises `metallib_4_0`); the defect is Slang's downstream `-std=metal3.1` **hardcode** rejecting the correctly-emitted 4.0 attribute. This looks like the SAME conflation the triager already caught once (bot's cited metal4.0/E40003 signature was a DIFFERENT non-fatal gfx-unit-test line that #11973 conflated). Approver's job was approvability, not root-cause — its ABSTAIN verdict (CI red → human must look) stands regardless of which framing is right. **Main does NOT adjudicate this; the fixer's source-read diagnosis is more authoritative for the fix direction.**
- **NO DISPATCH taken.** Fixer OWNS #12009 with explicit jkwak authorization to "iterate until CI green"; a red CI snapshot at `291f75b1` is EXPECTED mid-iteration, not a surprise. Fixer owns the ci_* webhooks and sees the failure directly — relaying the approver's read = double-dispatch + pushing into the owner's head ([[feedback_no_double_dispatch_peer_wired]], [[feedback_let_fixer_own_single_session]]). Approver is shadow-mode; its verdict gates nothing.
- **NO GitHub post.** Approver never posts ([[feedback_approver_never_posts_route_reviewer]]); jkwak is engaged and watching CI directly; fixer will post the reconcile/diff per its pre-GitHub checkpoint. Ledger record is internal-only, awaiting jkwak's human verdict (merge/close) for calibration.
- **Re-decidable:** approver re-runs on the next ready/synchronize once the fixer's std-fix lands and CI goes green. Chain still fixer-driven; NEXT unchanged (iterate std-flag fix → CI green → jkwak re-review/merge).

**✅ TERMINAL — #12009 MERGED 2026-07-15 02:46Z by jkwak-work (approver join msg 14; matches MEMORY.md index).** Merge commit `a2596654`, merged head `2a6410d9` (NOT the approver's pinned `291f75b1`). Chain closed.
- **Framing tension (flagged unadjudicated 17:25) is now RESOLVED in the fixer's favor.** The fixer's source-read root cause was CORRECT; the approver's "sub-Metal-4.0 runner" read was WRONG. Maintainer merged the fixer's real fix (`aba3cd7d` — derive `-std=metalX.Y` from the requested metallib capability instead of hardcoding `-std=metal3.1`) + a test-portability commit. The macOS-26 runner genuinely supports Metal 4.0; the defect was Slang-internal (emit gate honored `metallib_4_0` while downstream metallib compile hardcoded `-std=metal3.1` → 4.0 attribute rejected). This is a **Slang compiler bug, NOT runner-health #11973.**
- **Approver's decision was RIGHT, attribution WRONG — vindicated abstain, not false-safe.** ABSTAIN_POLICY/OPEN_GAP correctly said "human must look" (merging `291f75b1` as-is would have shipped a red macOS example); a human DID look and added the real fix. But the approver blamed #11973 runner-flake — resolved Devin's flake-vs-bug ambiguity toward the "comfortable environmental prior." Approver filed `[approver/challenger-miss]` learning. Human verdict recorded APPROVED @ `291f75b1` for calibration.
- **#11999 itself stays OPEN** — PR was `Addresses #11999` (not Closes); the merge fixes the macOS-26 codegen path but #11999 is the re-enable tracking issue, maintainer's call to close.
- **#11985 unaffected** — separately triager-owned, gated on reverting #12075; the reconcile-narrative it flagged (emit-vs-std latent inconsistency) is now the merged fix. See [[project_11985_macos_metal_capability_regression]].
- No further routing. No GitHub post from us (approver shadow-mode; jkwak merged directly).
