---
name: project_8183_wgsl_metal_displacement_segfault
description: "#8183 vk_slang_editor displacement WGSL/Metal segfault — triaged, fixer drafting"
metadata: 
  node_type: memory
  type: project
  originSessionId: acbf4e19-181c-4682-a515-46a4a04909c8
---

shader-slang/slang#8183 — compiling vk_slang_editor displacement example for WGSL/Metal segfaults. jkwak-work asked bot to triage (07-18); jkwak-work is the human assignee (Type=Bug, Dev Reviewed/Metal/WebGPU labels).

**Triage (slang-triager, REPRODUCED @aaa07fe29):** confirmed compiler crash (not user error). bug/crash · high · P1. Subsystem: target-emit Metal+WGSL / IR varying-param legalization. Root cause = type/layout desync: `wrapReturnValueInStruct` flattens the vertex output struct but reuses the un-rebuilt original `resultLayout`; `ensureStructHasUserSemantic` walks flattened fields by positional index into the stale layout → null deref at `IRVarLayout::findOffsetAttr` on first unsemanticed flattened field. Matches jkwak's on-issue diagnosis. Verdict posted issuecomment-5011412057; `reproduced` label added.

**Fix (07-18):** slang-fixer shipped **draft PR #12155** (https://github.com/shader-slang/slang/pull/12155), `Fixes #8183`, label `pr: non-breaking`. Approach A = rebuild flattened output var-layout at producer (`wrapReturnValueInStruct`) so `ensureStructHasUserSemantic` positional walk is valid; dead-dup removed + bounds guard; regression test added. 2 files +195/−1. Repro EXIT 139→0 (WGSL+Metal); originalBitangent→@location(3)/[[user(_SLANG_ATTR_3)]]. Tests under active asserts: metal 163/163, wgsl 53/53, +977/977 broader; formatting clean; codex PLAN+CODE+OUTPUT approved. Root cause proven via IR dump + pass-order (input path immune — packStageInParameters pre-stamps _slang_attr).

**Review:** slang-fixer dispatched slang-reviewer (pipeline running). On the PR: jkwak-work self-assigned + jhelferty-nv review-requested jkwak-work (both human actions, fixer left untouched — requested no reviewer/assignee).

**Watch:** HELD AS DRAFT per guardrail — human flips ready. Don't auto-close; don't nudge merge. Reviewer posts verdict; fixer forwards. CI dispatched.

**Update 2026-08-06:** PR #12155 still **draft**, unmerged, reviewer-requested + assigned to jkwak-work. Bot posted issuecomment-5207672921 (17:04) documenting **6 additional crash shapes narrower than the filed one** — an unsemanticed `out` param, no nesting required; root cause is `lowerOutParameters` (`slang-ir-lower-out-parameters.cpp`) appending a field to the return struct while constructing **no layout** at all. Metal-only lowering; the WGSL half of the asymmetry is filed as **#12400**. ⇒ #12155's producer-rebuild fix covers the flatten path only; the append path is separate scope.

**Verdict PATCHED 2026-08-06 18:02:53Z (slang-triager, verified by me):** issuecomment-5011412057 1724→3825 chars, comment count unchanged at 5, no third comment from the triager. Now states the real state — `reviewDecision=REVIEW_REQUIRED`, **zero** submitted reviews, zero review comments since jhelferty-nv requested jkwak-work at 2026-07-18T16:27:47Z ⇒ **review never started**, not "stale". `next-action:` = a human flips #12155 out of draft; self-referential "updates to merged when it lands" removed.

⚠️**CI RED IS NOT A CODE FAILURE HERE.** All 44 check runs on head `a859c217` are skipped/null (`ci.yml` `draft != true` guard); `mergeStateStatus=BLOCKED`. Two `workflow_dispatch` runs on the branch concluded **failure** but failed only at `wait-for-human-priority`/`check-ci` with **zero** build or test jobs executed — the throttled bot draft-testing path. ⇒ flipping out of draft is also what makes CI run for the first time. Don't read the red as broken code; don't "fix" it.

**Base drift measured by me 2026-08-06 (adds to the triager's report):** the 3 commits of 2026-08-06 are comment-wording only — 6 changed lines net across `slang-ir-legalize-varying-params.cpp`, **all inside `//` comment blocks**, 0 non-comment lines. BUT `92c6b259a` is a **merge of master `d7d59f374`**, advancing the base **97 commits** past the `aaa07fe296` the test numbers were measured at. Upstream touched **none** of `slang-ir-legalize-varying-params.cpp` / `slang-ir-lower-out-parameters.cpp` / `slang-ir-metal-legalize.cpp` in that range, so the fix's mechanism is intact — but the suites grew (`tests/metal` 119→122, `tests/wgsl` 51→55 files) ⇒ ⭐**the "163/163 · 53/53" figures are counts against the OLD suite and are no longer the denominators.** Re-run before quoting them as current; they are not falsified, they are unentitled.

**⛔ MY REMEDY FOR THAT WAS WRONG — corrected by slang-triager 18:38Z, verdict patched a 3rd time (3825→4516 chars, count still 10).** I recommended dating the figures *"measured at aaa07fe29, pre-merge"*. The triager went to re-run in order to word it and found the figures **not reproducible at all**: `tests/wgsl` 56/56 (reconciles with my +4 files) but `tests/metal` **197/197** vs published **163** — a **+34** gap where the suite grew by 3 files. Then, same commit/suite, one flag apart: `-use-test-server`/single-process/`-skip-api-detection` all **56/56**, but **`-api '-all'` → 49/49**. ⇒ ⭐⭐⭐**a slang-test ratio is a function of {suite, commit, API detection, flags}** — 163-vs-197 probably just means a different machine's detected backends. **Dating an unreproducible figure makes it LOOK audited**; the right move (published) is to demote the claim to *"suites green — no failures"* and keep `EXIT 139 → 0`, which is a repro not a ratio. Full rule: [[feedback_a_pass_ratio_is_a_function_of_four_things_not_one]].
⚠️**Scope limit on that probe:** it ran **pristine master with the fix ABSENT** (the PR's test file isn't on master) ⇒ denominator probe, **not** a re-verification of #12155. A like-for-like re-run needs a build of the PR branch (+2 directives from its own test) — nobody has run that.

**Assignee changed 2026-08-06 17:36 (issuecomment-5207873343):** jkwak-work removed himself and **assigned zangold-nv for triaging** — the issue's sole assignee is now zangold-nv (PR #12155's reviewer/assignee is still jkwak-work). Routed to slang-triager on the canonical thread; no new bot comment forced (the draft-PR state comment 5011412057 + the crash-shapes comment 5207672921 are both fresh, and a third comment 30 min later is noise). ⚠️ A human maintainer reassignment is not a chain input we act on beyond making sure the public trail is current — do NOT re-triage.

**⛔⛔ 4th PATCH 18:48:13Z — MY "chain closed / trail coherent" WAS FALSIFIED, and I had already certified it.** A sibling posted **PR** comment `issuecomment-5208184633` at **18:25:31Z** (4457 chars) — inside the window between the triager's 18:38Z measurement and both our certifications. Neither of us saw it: **we both verified the ISSUE only, and it landed on the PR.** Verdict re-patched (4516→5277 chars, `upd` 18:48:13Z, issue comment total still **10**). Three of the triager's published claims removed as false: *"may well stop the crash for them too"*, *"that is **untested**"*, *"expected to cover both targets"*.

**The finding — MEASURED against a build of `a859c21797`, and it is bad for the PR:** nested struct 139→**0** ✅; struct-return + `out` 139→**0** ✅; but **both non-struct-return `out`-param shapes 139→139** ❌. ⭐**The crash MOVED ONTO THE LINE #12155 ADDS.** `buildFlattenedResultVarLayout` opens `if (!resultTypeLayout) return resultLayout;` ⇒ returns the layout unchanged on a non-struct original return ⇒ the new `typeLayout->getFieldCount()` bounds check **derefs null**, and `SLANG_ASSERT(typeLayout)` one line above is **`SLANG_ASSUME` in Release** (`slang-common.h:371`) — an optimizer promise on a false premise, not absent protection. Trigger isolated: *non-struct return **AND** ≥1 `out` param*. I re-verified in a fresh clone at both revisions: guard `:3302`, assert `:3298`, `typeLayout` from `:3270`; **`typeLayout->` derefs before the assert = 0 on both revs** ⇒ the PR's line is genuinely the first deref on that path. Rule: [[feedback_a_release_compiled_out_assert_does_not_protect_a_new_deref]].

**⇒ #12155's status changed: it now has a substantive pre-merge finding against it.** Still draft / `REVIEW_REQUIRED` / 0 reviews — but "flip out of draft" is no longer the only thing a maintainer needs to know. Framed on the PR as **not an objection** (the PR's own test scopes out-params out); flagged because the failing line is one it introduces.
⚠️**My closing "zero reviews, zero review comments" went stale:** `reviews: []` holds, but the new item is an **issue-level PR comment** — neither a review nor a review comment, so the verdict's phrasing survives *technically*. Left as-is rather than forcing a 5th patch; noted as a boundary a maintainer shouldn't have to parse.

**✅ SETTLED 19:01Z — NO 5TH PATCH, both candidates declined with evidence.** Rule extracted: **edit to remove falsehood, never to improve phrasing** — patch 4 was mandatory (3 false claims); a 5th is discretionary polish and *edit count is itself a cost the next edit must beat* at 4 revisions before a cold reader. The producer-vs-consumer framing was checked before declining: `lowerOutParameters`=1, `constructing no layout at all`=1, `does **not** touch`=1 in the verdict ⇒ **facts present, only the summarizing sentence absent, nothing false**. And the decline was evidenced both ways — `wrong to`/`should not have`/`not an objection` = **0 each**, confirming no harmful reading of "#12155 shouldn't have been opened" is standing. Full rule: [[feedback_edit_to_remove_falsehood_not_to_improve_phrasing]].

**Triager re-derived both of my sharpenings on its own edge (not inherited):** all **five** `SLANG_ASSUME` arms are optimizer promises, none a check — `[[assume(X)]]` `:336`, GCC `if(!(X)) __builtin_unreachable()` `:338`, `__builtin_assume` `:345`, `__assume` `:347`, `invokeUndefinedBehaviour()` `:350`. And the first-deref control reproduces: 0 `typeLayout->` before the assert on both revs (nonzero control 2 in-function); **master's first deref `:3299`, PR's `:3302`** ⇒ ownership claim, not narrative.

**⇒ Actionable framing for `zangold-nv` (agreed, recorded):** #12155's guard **declines to read**; the root is `lowerOutParameters` **recording no layout for what it appends**, which is out of that PR's scope. A human scoping call, NOT a defect in the triage.

**⛔RESUME TRIGGER (owned by slang-triager, accepted):** if `head.sha` moves off `a859c2179`, re-run the 3-cell matrix on the new revision — struct return + `out` ⇒ 0 · non-struct, no `out` ⇒ 0 · non-struct + `out` ⇒ **139 today**. Recorded there with the mechanism, the `SLANG_ASSUME` form and the first-deref control so a cold session re-derives rather than re-discovers.

**Scope now spans 4 issues — a maintainer dedup decision is pending, ours is not to make:**
| issue | covers | state |
|---|---|---|
| **#8183** (this) | nested-struct output + unsemanticed field → segfault, Metal **and** WGSL (both `EXIT=139`) | open, `zangold-nv`, fix = draft #12155 |
| **#12400** | WGSL unsemanticed `out` param → `ptr<function,T>`, `EXIT=0` silent | open, bot-filed 2026-08-06, unassigned |
| **#7176** | ⭐**same WGSL defect, filed 2025-05-20 by a HUMAN, already `Dev Reviewed`+`WebGPU`** | open, 15 months, unassigned, milestone Q4-2025 |
| — | 2nd independent WGSL invalidity: `SV_POSITION` on an out-struct field → two `@location(0)` fields, no `@builtin(position)` | **NOT FILED**, flagged only |

⇒ **#12400 is a bot-filed duplicate of a human-filed issue.** The older/human/reviewed one is #7176. I did not close or merge either — dedup is the maintainer's call and #12400's own triage says so. ⚠️Also corrected on-issue: "same root cause as #8183" is **overstated** — Metal *executes* `lowerOutParameters` and leaves layout stale (crash); WGSL *never calls it* (silent invalid emit). An **absent** call is not the same root as a **faulty** one ⇒ two sibling defects joined by one asymmetry. And #12400's description claim that "the semanticed form is handled correctly" is **false**: `out float4 extra : SV_Target1` → `EXIT=255 error E55202`; a user semantic (`TEXCOORD0`) still emits `ptr<function,…>`. The `out`-param path is broken regardless of semantic.

**#12155's coverage, read against its diff:** bounds guard is unconditional ⇒ expected to stop the Metal *and* WGSL crash walk; the layout rebuild is gated on `returnStructType != flattenedStruct` ⇒ **nesting only**, so no-nesting out-param shapes never reach it; PR touches **0 lines** of `slang-ir-lower-out-parameters.cpp`; its own test covers only the filed nested shape. ⇒ **do not read "fix in draft #12155" as closing this issue.** (Coverage derived from reading the diff — no revision-matched build was run.)
