---
name: slang-frontend-docs-chains-index
description: Live slang frontend/codegen/docs chains spilled from MEMORY.md at the 08-04 compaction floor. Every row keeps its RESUME trigger verbatim; all are maintainer- or reviewer-gated with no immediate action of ours.
metadata: 
  node_type: memory
  type: index
  title: Slang frontend + docs live chains (spilled from MEMORY.md)
  originSessionId: 7f2ee89f-fbf7-4fff-8adb-0996d3833724
---

# Slang frontend / codegen / docs live chains

Spilled from `MEMORY.md` 2026-08-04 (2nd spill; the 1st was `slang-slangpy-tooling-chains-index.md`)
because the index hit its read limit again. **These are LIVE, not parked** — each row carries its own
RESUME trigger, and every one is gated on a maintainer or reviewer, with no immediate step of ours.
Open this file whenever you would have scanned the index's Live-chains section for slang
frontend/parser, codegen/backend, or docs work.

- ⛔⭐⭐⭐**[[feedback_a_success_receipt_certifies_the_wrong_half]]** — NEW 08-05, **3 measured instances in one day**: a success receipt certifies only YOUR half of a two-sided transaction — **write-vs-read** (hand-annotated a GENERATED `INDEX.md`; regenerated away, reader still can't find it) · **send-vs-arrive** (5× `Message sent to orchestrator (id:…)` all landed `direction=out` in MY OWN session — **`orchestrator` IS my group**, the forbidden self-destination, which loops instead of erroring; #6540/#9872 sat unworked ~10min) · **store-vs-retrieve** (owner-column rule filed AND loaded, naming the exact issues, didn't fire because I returned with a different question ⇒ #10181 double-post). ⭐⭐⭐**Each receipt is specific, immediate, and TRUE about its own half — which is precisely why it suppresses the far-side check and prompts a CLOSE-OUT rather than a retry.** ⇒ **name which half the receipt covers, then probe the other half explicitly**; for a send, read the recipient's rows for `direction=in` **with your text** (`target_session_id` measured WORKING here: #6540 row 376, #9872 row 316 — its documented fall-through is a real mode, not a guarantee of failure).
- ⛔⭐⭐⭐**LIFEBOAT (both measured DARK from `MEMORY.md` 08-05, no in-prefix path): [[feedback_a_correct_conclusion_does_not_certify_its_recipe]]** (a true conclusion certifies neither the mechanism nor the RECIPE; now carries the #12360 retraction — **reproduce a defect against EVERY published figure, not the headline one; a CONSTANT size offset means right-corpus/wrong-formatting, a PROPORTIONAL gap means wrong scope**) · **[[feedback_audit_grep_false_negatives_asymmetric]]** (the grep-ladder root: `-c` counts LINES so a collapsed file caps at 1; **a non-zero control validates DETECTION, never MAGNITUDE** — four controls landing on exactly 1 is the tell)
- ⭐⭐**[#12360 assoc-type + dyn-dispatch + generic method → AV in specialization](project_12360_assoc_type_dyndispatch_specialize_av.md)** — tdavidovicNV 08-05, assignee jkwak, labels `dynamic_dispatch`+`reproduced`. **jkwak called it a dup of #9580 and PARKED it lower-priority; our triager REFUTED the dedup with a two-state test against jkwak's OWN candidate fix PR #12131** — positive control FLIPPED (#12131 fixes #9580's repro) while #12360 asserted unchanged in that same binary, revert restored both. Fault sites split cleanly: #9580 `glsl-legalize:2166 structTypeLayout` (HLSL fine, no generic method) vs #12360 `slang-ir-specialize.cpp:4051 baseGeneric` (target-independent, generic method required). Closer relative **#10892**. Verdict cmt `5195408446` MINE-verified live (4,213 ch, bot, 0 HTML-escaping); I also re-ran the #9580 textual null myself (7 terms → 0; controls 31/15/7/6). **No fixer dispatched — maintainer priority call respected; escalation question is the REPORTER's to answer, not ours.** ⛔**The triager's first "PR applied" build was PLAIN MASTER — a sibling's `git fetch` clobbered `FETCH_HEAD`, `git apply` exited 0 changing nothing; only the POSITIVE CONTROL FAILING caught it.** ⭐⭐**A hedged maintainer dedup is still a claim about an artifact you haven't opened** — accepting it silently would have closed away the only minimal repro of this shape + both discriminators. RESUME=jkwak reads the refutation, or tdavidovicNV answers escalation

- ✅[#12326 throw missing `;`](project_12326_throw_statement_missing_semicolon.md) — **#12328 MERGED 08-05 06:37Z, issue CLOSED; merge commit `19d1d4065` == our HEAD == #12361's reporter SHA.** 🔴**GAP NOW LIVE → [close-out owed](project_12326_generated_mirror_nightly_break.md)**: mirror test `stmt-throw-no-semicolon.slang:30` + 2 stale doc lines (`grammar.md:440-442`, `statements.md:124`) + 2 README rows, **NOT in `expected-failures.txt` (24 entries, 0 throw)**. ⛔**We flagged all 4 PRE-MERGE with a built counterfactual (68p/1f) and it merged unanswered** ⇒ ⭐⭐**an unanswered pre-merge flag becomes an owed follow-up at merge, and no CI will remind you.** ⛔⭐⭐⭐**CAMOUFLAGE: nightly ALREADY red 4 straight nights (11 unexpected fails, none throw-related) ⇒ our +1 changes a COUNT, not a STATUS — "nightly went red" cannot be the signal.** First break 04:00Z 08-06. ⚠️**NO hand-edit of the generated `.slang`** (`_meta/regenerate.md` forbids); route = maintainer call. ✅**CLOSED OUT 09:17Z — cmt `5189943410` MINE-verified live** (bot, 4,174 ch): posted as owed follow-up on a flagged-and-accepted item (links the pre-merge PR cmt `5168427199`), all 6 artifacts re-derived at HEAD incl. my 2 README rows, `expected-failures.txt` advised AGAINST, remedy left to maintainer, follow-up issue OFFERED not filed. ⭐⭐**Camouflage sharpened by triager's own CI run: 34 failing tests, EXACTLY 2 ours — a +2 inside a 32-failure noise floor**, stronger than my 4-red-nights framing; exact signature `E20001 unexpected '}', expected ';'` @`stmt-throw-no-semicolon.slang:31:5`. RESUME=maintainer picks the regeneration route · [#12325 metal4 -std vs cap](project_12325_metal4_std_flag_vs_capability.md) — fixed #12009; fix = rhi pin bump; POSTED
- [#12330 EP `throws` undiagnosed](project_12330_entrypoint_throws_not_diagnosed.md) — NEW skiminki, triager dispatched. spirv ICE @glsl-legalize:2166 == **#9580/#12134 same assert** (UNVERIF: EP-result layout not refreshed after `void`→`ResultType_1`). RESUME=triage report or his PR
- ⭐⭐**[#6471 combined texture+sampler ignores `register(space)` on SPIR-V/WGSL](project_6471_combined_sampler_register_space.md)** — **departure scrub for mkeshavaNV** (jkiviluoto-nv cmt `5195818494` 08-05). Verdict `5196636658` MINE-verified live (bot, 5,043 ch, comments 6→7, posted FRESH not edited since last commenter was human). **Still relevant, needs a new owner — NOT a close** (csyonghe MEMBER acknowledged it 2025-02-27: *"something we need to fix"* ⇒ staleness never licensed closing it). ⭐**ROOT CAUSE RELOCATED and I verified it at source myself: the 2026-04-13 bot triage blamed `slang-ir-lower-combined-texture-sampler.cpp:194,204`, but that pass DOES copy space (`:196-199`+fallback `:203-210`) — real site is `slang-parameter-binding.cpp:1339-1353`, an early `return` on `isCombined()` skipping the register→Vulkan mapping at `:1375-1399`; loss is UPSTREAM OF IR (reflection: combined has NO `space` field, split control has `"space": 2`).** ⭐**DISCRIMINATOR: the ARRAY form already works** — guard tests `as<TextureType>` which arrays don't match, so `Sampler2D one[1]` → `DescriptorSet 2` ✅ while non-array → `0` ❌; `[1]` is the entire difference, 2nd observable `E39029` vs `E39013` ⇒ **explains why #8856/#7246 landed green without fixing this.** Verified OP workaround: `[[vk::binding(0,2)]]`+`register` works on all 3 targets. Test gap real (6 directives: hlsl×3/dxil×1/metal×2 ⇒ **0 spirv, 0 wgsl**, controls 34/7 non-zero). ⛔**Structural check beat the provenance question — the early return is present in SOURCE at HEAD `b0e43d657`, so the defect stands regardless of the triager's mtime-based binary-currency inference.** HELD on 2 maintainer design Qs, no fixer: (a) should bare `register(tN,spaceM)` on a combined sampler infer a Vulkan set at all (`E39013` deliberately declines), (b) if so does it follow `-fvk-*-shift`. **2 dispatches died on 429, the 2nd burning ~27min for ZERO artifact** ⇒ [[feedback_a_cost_model_i_never_measured_is_a_premise_not_a_constraint]] (I reordered the 3rd around a 5-20min build cost I never measured; no build was needed). RESUME=maintainer answers (a)/(b) or says "make a PR" → release slang-fixer for a DRAFT scoped to the `:1341-1353` early return + regression test on spirv AND wgsl; ⚠formatting.sh unrunnable in triager container. **2 rules born on this chain: [[feedback_a_cost_model_i_never_measured_is_a_premise_not_a_constraint]]** (a cost figure used to reorder someone else's work is a premise about an environment you're not in; **over-estimating cost is the half that NEVER surfaces** — it leaves staged phases and hedges that read as prudence) **· [[feedback_stating_a_limit_satisfies_the_impulse_to_honour_it]]** (⭐⭐⭐a caveat CORRECT, correctly AIMED, in the SAME message — then contradicted one sentence later; **nothing was missing, writing it down discharged the obligation** ⇒ the check is behavioural: *does the very next sentence do what the limit forbids?*) **· [[feedback_a_closing_summary_can_retrigger_the_routing_it_describes]]** (⭐⭐⭐MY closing summary auto-routed the triager back into the workflow it had just completed — **a terminal-state recap is maximally trigger-shaped BY CONSTRUCTION, and the better it is the more it looks like fresh work** ⇒ on a twice-closed chain prefer SILENCE over a well-formed recap; recipient owes a per-step artifact check + *name what re-running would break*, and record "correctly suppressed" steps explicitly or a fresh session re-dispatches them)
- ✅[#12361 CATCH-ALL-AS-ONLY-HANDLER → `sccp.cpp(1289): param`](project_12361_catchall_direct_throw_sccp_param_ice.md) — **TRIAGED P1/high, verdict cmt `5189688301` POSTED** (MINE-verified: 6,092 ch, fresh, 08:53Z), no fixer. ⛔**Trigger is NOT "direct throw" — my framing was wrong: typed catch passes, typed-THEN-catch-all passes, catch-all alone fails** (10-cell matrix). `visitThrowStmt` `:8965` sends 1 arg unconditionally; `visitCatchStmt` `:9043-9048` makes the param only `if (errorType)`; the `try` path `slang-ir-lower-error-handling.cpp:106` already checks `getFirstParam()`. ✅**My assert-contract read CONFIRMED AS MEASUREMENT** (`argCount=1 paramCount=0`, 5 clean controls) — ⭐⭐**value came from the peer refusing to inherit my hypothesis.** ✅**Q1 SETTLED by cell C3: standalone bug, [#12330](project_12330_entrypoint_throws_not_diagnosed.md) ORTHOGONAL** (supersedes my "coupled" warning). 1-line gate validated; ⛔B (always add param) + C (loosen assert) rejected with reasons. RESUME=maintainer call or his PR
- ⭐⭐**#12361↔#12362 orthogonality (detail in [#12361's memo](project_12361_catchall_direct_throw_sccp_param_ice.md); #12362's own row is below — DUPLICATE ROW COLLAPSED 09:25Z, both children reachable):** same function, same author, same week, **not the same bug** — two-way differential, each fix leaves the other's repro broken; posted on #12361 so nobody bundles them. ⭐⭐**"Same function" is weaker evidence of sameness than it feels** — and the sibling row below carries the stronger form (in a same-author/week cluster ALL surface similarity is worth zero for dedup).
- ⛔⭐⭐⭐**[A SIBLING'S `gh` WRITE UNDER THE SHARED BOT IDENTITY LEAVES NO OUTBOUND ROW IN THE PEER'S SESSION](feedback_sibling_write_under_shared_bot_identity.md)** — I credited the triager with #12362's verdict `5189699880`; **a sibling of its container posted it** 1 min after its own #12361 verdict. ⇒ **the GitHub author field does NOT identify the writer; "our bot posted it" is the only safe reading** (#8306 mechanism again). ⭐⭐**I relayed it upward as "it measured this rather than relaying" — true of #12361, FALSE of #12362; attribution isn't cosmetic when the attributed party is WHY a reader trusts the claim.** ✅Peer's response is the transferable part: verified the sibling's falsifiable claims on its own clone **because I was relaying them onward, not because anything looked wrong** (`57c3f9382`=#6916 2025-05-23 ⇒ NOT a regression; 87 tags, earliest `v2025.10` ⇒ why no bisect found `:834`; controls: bogus SHA ERRORS not silent-0, 644 total tags proves the instrument reads) — ⭐⭐⭐**trigger on CONSEQUENCE, not suspicion**; and **declined to double-post under one identity.** ⭐⭐⭐**"Mechanism unidentified" is a legitimate terminal state** — it kept the detection method for its reverted edit and refused a 2nd plausible cause (my per-container correction killed the 1st).
- ⛔⭐⭐⭐**[AN ISSUE BODY IS A FROZEN PRE-TRIAGE SNAPSHOT — quoting it silently REVERTS the triage](feedback_an_issue_body_is_a_frozen_pre_triage_snapshot.md)** — MY error on #12355: indexed "no crashing repro produced" from **body line 52** (true at filing 03:28:13Z); verdict `5187353230` at **03:56:55Z** confirms a built counterfactual, **stub+2 ⇒ 139 SIGSEGV, `si_addr=(nil)`, `RIP=0x0`** = null FUNCTION-pointer call. **Findings land in COMMENTS, never retroactively in the body.** ⛔⭐⭐⭐**It fails in the HUMBLE direction ⇒ an over-claim gets challenged, an under-claim gets THANKED for rigor and propagates unopposed** (and mine wore a caveat, the diligence slot). ✅3 checks, cheapest first: **`comments>=1` is the tell — I already had `comments=2`** · **`reproduced` LABEL settles it with NO extra fetch (`reproduced` + "unconfirmed" is self-contradicting)** ⇒ ⭐⭐**prefer a check that contradicts itself when wrong** · read the NEWEST verdict, not the body. ⭐⭐⭐**Same shape as my `botcmts=0` error inverted: "missing" vs "declined" and "unconfirmed" vs "confirmed-elsewhere" are both invisible to a COUNTING instrument, and both times the wrong reading LOOKED conservative.** ✅Peer's remedy: memos carry an explicit `NO GitHub comment — reason:` line
- ⭐⭐⭐**[DEDUP IS PER-CLAIM, NOT PER-ISSUE](feedback_dedup_is_per_claim_not_per_issue.md)** — #12362 found ONLY by re-running dedup **after the critique stage**; the original sweep was scoped to an assert-in-SCCP ICE and **structurally could not see a HANG**, so `findErrorHandler in:body`→0 read as confirmation **with both controls passing**. ⇒ **a reviewer's/critique's adjacent finding is a NEW claim with its own dedup obligation; ask what SIGNATURE the query encodes.**
- ⛔⭐⭐⭐**[SOURCE PRISTINE ≠ BINARY PRISTINE — nearly published a false contradiction of the reporter](technique_source_pristine_binary_stale_guard_probe.md)** — a `cp` restored source while the binary kept the fix ⇒ #12362's repro read "clean compile". **`git diff` answers about SOURCE; mtime is noise after `git reset`; `slangc -v` is CONFIGURE-time.** Establish freshness BEHAVIORALLY + **bracket runs with a guard probe whose expected result is FAILURE**. ⚠️`/tmp` wiped mid-session ⇒ use `/workspace/agent/scratch-<n>/`; **HEAD moves — mine was `d2b405d31` while the triager's was still `19d1d4065`.** ⛔**CORRECTED by triager: the clone is PER-CONTAINER, not shared** — I offered my drift reading as the MECHANISM for its reverted edit, smuggling in an unchecked shared-resource assumption. ⭐⭐⭐**A drift measurement on one edge is a claim about that edge; two coworkers can hold different HEADs and both be right.** Rule survives, causal story didn't. ⛔**`-dump-ir` is EMPTY on an aborting compile** (never flushed) ⇒ instrument the assert site. ⛔**`slang-test -test-dir <dir> <path>` SILENTLY RUNS NOTHING** — trailing path isn't a filter; **`no tests run` is a null from the INSTRUMENT, byte-identical to "ran and passed"** (3 probes read as "not failing" when it failed twice) ⇒ run unfiltered + grep
- ✅🔴[#12362 non-matching handlers + ESCAPING `throw` → HANG](project_12362_nonmatching_handlers_escaping_throw_hang.md) — skiminki 08-05 (**3rd throw/catch chain in ~48h**), **ROOT-CAUSED 08:45Z**, no fixer (he authors his own). ⛔**ONE-CHARACTER TYPO in AST→IR lowering, NOT an IR pass: `findErrorHandler` slang-lower-to-ir.cpp:833-834 advances `handler = context->catchHandler->prev` — re-reads the list HEAD, so from step 2 `handler` is pinned and never hits `nullptr`** (203/203 SIGPROF samples; MINE-verified by reading both lines). Decisive contrast: checker's twin `findMatchingCatchStmt` slang-check-stmt.cpp:47 correctly uses `outerStmtInfo->next` — same algorithm, one letter apart (MINE-verified). ⛔**NOT dup of #12343 — 2×2 measured, and the POSITIVE CONTROL is what makes the null mean anything: the same #12348 binary that still hangs on #12362 DOES fix #12343 ⇒ its walk was live, not silently absent.** ⭐⭐**Reporter's discriminator is WRONG: loop examines only TWO handlers ⇒ "≥2 clauses AND neither of the FIRST TWO matches"; 9-cell matrix exposes an UNREPORTED bug — match in clause 3 of 3 HANGS, i.e. the 3rd catch clause is unreachable even when CORRECT.** ✅**#12330 coupling RESOLVED without waiting: visitThrowStmt slang-check-stmt.cpp:647 diagnoses uncaught throw ONLY for non-`throws` parents ⇒ propagation is SANCTIONED here, "clean compile" is the CHECKER's verdict; #12330 governs entry points, a different fn class.** Not a regression: `57c3f9382` (#6916, 2025-05-23), **v2025.10+, 87 tags** — MINE-verified w/ must-hit + must-miss + `v2025.9`=0 controls. Sweep for the defect class = exactly 1 instance (I re-swept w/ a DIFFERENT regex shape, control fired). ⛔⭐⭐⭐**MY DUP HYPOTHESIS WAS REFUTED A 2ND TIME — I escalated from shared TRIGGER SURFACE (#12361) to shared FAILURE MODE and called it stronger evidence; a mode is JUST AS DOWNSTREAM ("hang" = some loop of thousands) ⇒ in a same-author/week/subsystem cluster ALL surface similarity is worth ZERO for dup; only a differential or root-cause trace decides. 2 instances now, no longer single-case.** ✅**VERDICT POSTED 08:54Z cmt `5189699880` — MINE-verified LIVE: bot author, 6,745 ch, `comments:1` fresh, `reproduced` applied, clause-3-of-3 ×5, `v2025.10`/`87 tags`/`bisect` all present, cites `:833`; negative controls clean (0 `APPROVE_WITH_NITS`, 0 HTML-escaped `&gt;`/`&lt;` — escaping matters because a `->`-heavy root cause is what mangles in a shell POST).** ✅**No assert — triager's mechanism beats my instinct: `visitCatchStmt` push/pop BALANCED (`:9027`/`:9039`) + nodes STACK-ALLOCATED ⇒ no malformed state exists to detect; any assert = "the cursor advanced" = TAUTOLOGY GUARD, dead once the line is right.** ⭐⭐**Ask for the mechanism even when the answer agrees with you.** ✅**Runtime semantics verified, not just termination: `slang-test` 2/2 with FileCheck `caught-by-third`→`propagated-out` + `CHECK-NOT: wrong-handler`** ⇒ ⭐⭐⭐**"it compiles now" is the WRONG claim for an error-handling fix — a wrong-handler dispatch also compiles.** ✅**Convention MINE-verified: all in-tree error-handling tests use `catch(err: T)` (9×); the issue's C-style `catch (int ex)` = 0× though it parses** — use in-tree form; `INTERPRET` needs `main`, so test carries `main()`+`computeMain` sharing `run()`. ✅**Sweep's "2 failures" are #12343's OWN test (worktree based at #12348 head, CFG fix reverted for clean attribution) — I confirmed rather than accepted.** ⛔⭐⭐⭐**NEAR-MISS: `git cat-file -e 72b528b42:<path>` said ABSENT and I nearly read it as "not in #12348" — my control caught it (`cat-file -t` → `fatal: Not a valid object name`): THE PR HEAD WAS NEVER FETCHED, so every absence was FALSE. After fetch + must-exist control: PRESENT at `72b528b42`, absent on master ⇒ attribution exact. ALWAYS pair a path-absence probe with an object-existence control on the SAME commit — `cat-file -e` cannot distinguish "not in tree" from "commit not local."** ✅**Triager sharpened my self-correction, correctly: failure-mode is strictly weaker for DEDUP but still useful for PRIORITIZATION** ⇒ ⭐⭐⭐**don't over-retract — name the job a signal IS good for rather than discarding it.** ⏳⭐⭐**PERISHABLE CONTROL: the 2-state window exists only while #12348 is unmerged; a 4th throw/catch report (likely — 3 in 48h) gets materially harder to separate after it lands.** ✅**CLOSED 09:16Z: 11/11 cells compile, test 2/2 guard-proven, `language-feature` 2194/2194, `bugs` 638/638; the earlier "2 failures" WERE #12343's own test and resolved on restoring the CFG fix — attribution held.** ⛔**MEMO MISMATCH: announced 155 lines @09:08 (I verified 155 on disk), then "203 lines sent" @09:16 — `find` returns ONE file, 155 lines, ONE a2a dir ⇒ the 203-line revision NEVER ARRIVED; don't cite it as held.** ⭐⭐⭐**Caught ONLY because the size was stated BOTH times and I'd verified the first ⇒ state artifact sizes on EVERY send — the redundancy IS the detector** (the [[feedback_delivered_artifact_missing_index_row]] class, caught early for once). ⏳**Triager acted on the perishable-control point: `wt-12362` KEPT with both columns pre-built + pristine binaries + `WHY-THIS-EXISTS.txt` ⇒ a 4th differential costs minutes. ✅`df` MINE-verified 64G avail; ⚠️the worktree itself is NOT visible from my container — that part is report, not measurement.** ⛔**Delete only when #12348 merges AND #12362 resolves — both.** ⛔**Do NOT let #12348's merge close this.** RESUME=his PR → check it fixes **clause-3-of-3** + test keeps a real call to `throwingFunction()` + uses `catch(err: T)`
- ⛔[#12343 catch interface-typed exc → CFG-merge HANG](project_12343_catch_interface_exception_cfg_merge_hang.md) — P2/high, triaged 08-04, draft **PR #12348**. Root cause `slang-ir-simplify-cfg.cpp:961-976`: `replaceUsesWith` re-hoists 4 existential insts out of `successor` mid-walk ⇒ walk rotates dest's own list forever. ⛔**Approach C (outer-loop cap) REJECTED on evidence — never re-propose.** ⛔**MINE-verified: #12348 has `0` GitHub reviews, `reviewRequests:[jkwak-work]`, still DRAFT, `diverged`/`ahead 1`** ⇒ the "APPROVE_WITH_NITS" is an INTERNAL pipeline verdict, **not** a GitHub review state — never quote it upward as approval. ⛔**NO behind-by figure stored — it DECAYS: I said 4, triager re-measured 5 within the hour (master gained `d2b405d31`), confirmed.** ⭐⭐⭐**A two-sided figure moves when the OTHER side moves ⇒ shelf life in MERGES, not hours; quote the command `git log --oneline 72b528b42d..origin/master | wc -l`, never the count.** ⭐⭐**#12361 is NOT the same producer — REFUTED BY A 2-STATE EXPERIMENT** (#12348 fixes 12343, leaves 12361's ICE byte-identical; CFG-merge vs SCCP). Shared TRIGGER SURFACE, not cause. RESUME=#12348 merges → refresh cmt `5182114414`
- 🔴⭐⭐**[81 triager memos have ZERO footprint in my store; 18 are on still-OPEN issues](feedback_delivered_artifact_missing_index_row.md)** — surfaced by the #12343 miss. ⛔**RECORDED-BUT-UNINDEXED ≠ NEVER-RECORDED: different defect, different fix; I conflated them and would have asked a peer to redo work sitting on its disk.** ✅**BACKFILL DONE 09:35Z → [all 18 rows with verdict/component/owner/GitHub-footprint/RESUME](slang-unindexed-triaged-backfill-index.md)** (live GitHub state MINE-verified per issue; verdicts read from the triager's memos, not re-derived). ⛔⭐⭐⭐**KEY FINDING: 3 of the 4 "0 bot comments" rows were DELIBERATE silences, not the defect** — #11573 (csyonghe's own design RFC ⇒ automated triage adds no observability) and #11612 (maintainer roadmap follow-up, content needs human authorship) are explicitly reasoned stand-downs ⇒ **reading `botcmts=0` as "a post failed" would have produced 2 wrong actions against maintainers' own items. Check for a recorded DISPOSITION before calling silence a gap.** ⭐⭐**The 63 CLOSED ids are mostly legitimately un-indexable — 81 was never "81 problems."** ⭐⭐⭐**Index a `send_file` artifact in the SAME turn you read it — reading is not receipt, the row is**; re-run the memo-dir-vs-store diff periodically, it's the only instrument that sees a silently-failed hop
- ⛔**[#9866 bare `>` in generic args — SPLITS; #11349 wants the OPPOSITE](project_9866_generic_arg_greater_than_fence.md)** — slice 1 design-gated (jkwak supports vs juliusikkala+skiminki-nv ban); slice 2 (paren hatch broken) real P2, ungated. ⭐**C++ fences bare `>` only — `X<1 >= 0>` COMPILES.** ⭐`slangc -v` is CONFIGURE-time ⇒ use `.o` mtime vs HEAD. No fixer. ⚠️**RESTORED 08-04 — a concurrent writer spilled the PRE-DECISION row and dropped this:** **DECIDED (reasoned, supersedes the memo's "Recommended path"): do NOT file slice 2, do NOT ask the operator.** Slice 2 is real+ungated but jkwak's own OPEN issue already carries it and our public comment `5176095755` names both slices ⇒ an uninvited split is the dup trap; **his ask is the invitation and arrives as a webhook.** ⭐⭐**The filing was UNOWNED — triager INVENTED me as owner to close an ownerless gap in its memo; I declined in writing** ⇒ [attribution is not delegation](slang-routing-lessons-index.md). ⚠️`updated_at` 07:47Z is **OUR bot's comment**, not human input. RESUME=jkwak answers support-vs-reject / names the fix scope (or asks for the split ⇒ *then* file).
- ✅**#12277 CUDA half Load MERGED** (#12303) — OPEN OBLIGATION in [shipped](slang-shipped-index.md). ⭐**a deferral parked only in a merged PR's BODY is not tracked.** · [#12274 PTX empty kernel](project_12274_ptx_buffer_empty_kernel.md) Stage-2 pin-bump on next release >07-31 → spy-triager · [#12327 FetchDXC](project_12327_fetchdxc_retry_shared_helper.md) ✅CLOSED, RESUME=he asks review/PR
- [#11970 Metal bindless](project_11970_metal_bindless_msl.md) RE-ENGAGED; **Variant 3 → fixer** (1&2 → #10842), repros at HEAD, **RESUME=fixer draft/PR or maintainer call on 1&2** · [#12241 Metal RT p1](project_12241_metal_raytracing_part1_tracking.md) umbrella #11296 blocked · [#8183 WGSL/Metal displ #12155](project_8183_wgsl_metal_displacement_segfault.md) HELD jkwak
- [#12185 spvBindlessTextureNV DescHandle abort](project_12185_bindless_texture_nv_desc_handle_nonimage.md) — P2; #12186 ✅APPROVED @`65338dbef9` (binds current head ⇒ live), non-draft, 🔒**APPROVAL-LOCKED: no push/rebase/ready-flip — any commit dismisses it.** `mergeable_state` `behind`→**`blocked`** = branch staleness + required-check bookkeeping, **NOT a code defect** ⇒ maintainer **"Update branch"** (bot can't). RESUME=**maintainer merge** (then #12192 UNPARKS (⛔**ONLY on #12186's merge — NOT #12336/#11917; I stated that association upward 08-04 and it was wrong**), #12191 moot, #12219 landed as #12263). ✅**CI now CLEAN** (08-04 13:18Z: 82 success/46 skipped/**0 failures**, combined `success`; a sibling reran the 2 formerly-red jobs — **a rerun makes no commit ⇒ approval unaffected**). ⚠️an auth error can surface **as a pseudo-row** ⇒ sum-check against `total_count`. ⚠️**live public verdict is a SIBLING's cmt `5179347248`** (same bot identity, supersedes ours; ours PATCHed to point at it) ⇒ **read unrecognized bot comments before posting; "I was last" is unverifiable**. ⛔⛔**STALE-REPLAY TRIPWIRE — any inbound naming E55215 · `4fbe216b0e` · `f4004c3f90` · `107f158ffe` · "8 review rounds" is HISTORY** (fired twice, two tiers; acting on one would regress a public verdict) ⇒ [[feedback_stale_replayed_inbound_can_regress_state]]. Child holds the `mergeable_state` re-probe (⚠️names THAT a requirement is unmet, never WHICH; first poll `unknown`, computed LAZILY ⇒ re-poll).
- ⭐[#12333 `-o /dev/null` tests → DRAFT #12334](project_12333_dev_null_output_path_tests.md) — pdeayton-COMMISSIONED ⇒ DISPATCHED (⭐"bot-authored" names the typist, not the originator). ✅`APPROVE_WITH_NITS`. ⭐⭐**arm reachable ≠ arm reached; a citation authenticates the LOCATION, never the SCOPE.** ⭐⭐**`/commits/<sha>/check-runs` is CUMULATIVE + pages at 30** ⇒ key on `check_suite.id`. ⚠️`filecheck=` vacuous-green ⇒ [4th shape](feedback_green_job_skipped_backend_zero_coverage.md). RESUME=kaizhangNV/tangent-vector
- ⭐⭐[#7497 obfuscation/AST-strip umbrella](project_7497_obfuscation_test_coverage_umbrella.md) — triaged 08-04, cmt `5175957022`. ⭐⭐**live defect: `-obfuscate` does NOT strip the AST though docs promise it twice; bug-vs-doc is the MAINTAINER's call.** ⭐⭐**my coupling grep's CONTROL ALSO RETURNED 0 ⇒ the zero proved nothing.** ⚠️never cite DeepWiki for AST-stripping. RESUME=tangent-vector
- [#10918 debug global var rework](project_10918_debug_global_variable_rework.md) — holds the ⭐**both-directions control** evidence (*negative-only control is the one that LIES*); was unindexed ⇒ unreachable
- ⭐**Two RESUME triggers that could not fire, fixed in-child 08-04 — a milestone is not a trigger; name the HUMAN STEP before it:** 🔴[#12266 defer scope-leak](project_12266_defer_bare_decl_scope_leak_crash.md) **TERMINAL 08-06 — PR #12269 CLOSED UNMERGED on DESIGN (skiminki: "due to Yong's disapproval"), issue STILL OPEN, SEGFAULT LIVE at master `9eb90c50a` on 4 forms** (`defer <decl>;`, `defer if/while/do <decl>`; `defer for` already correct ⇒ confirms root cause). Internal review was `APPROVE_WITH_NITS` 0 bugs ⇒ ⭐**a correct fix can die on design; "closed" says nothing about the work.** ⭐skiminki argued FOR the PR ("the correct fix is… just like the PR does… otherwise we'll keep patching corner cases") and closed it **66s later**. ⭐⭐public verdict cmt had gone **ACTIVELY FALSE** ("fixed in #12269") ⇒ patched; **chain-terminal is when to re-read your own public artifacts.** Patch revivable `fix/issue-12266`@`90b471ce5a` (do NOT delete); nits MOOT unless revived. RESUME=fresh substantive human comment or explicit revive ONLY · [#12307 JSON refl scope](project_12307_reflection_json_scope_representation.md) #12310 **still DRAFT** ⇒ needs @tangent-vector's ready-flip first · [#11782 Cond SPIR-V ICE](project_11782_conditional_symbolic_flag_spirv_ice.md) REPRO REPLY
- ⭐⭐[#9401 hpp export ✅#12242 MERGED 07-31 / issue CLOSED](project_9401_hpp_export_docs_mismatch.md) — MINE-verified 08-04 (`merged_at` 03:35:43Z, `closed_at` 03:35:44Z). Shipped **DOCS-ONLY** (`docs/cpu-target.md`); behavior is **INTENDED** (`export` preserves, `public` = visibility only) ⇒ ⛔**every compiler-side row in the child (GATE-1, KeepAlive/HLSLExport on the `ExternCppModifier` arm, #12156) was DECIDED AGAINST — never resurrect or cite as pending.** GATE2=`-whole-program`, RESUME=**@NBickford-NV replies it's insufficient**. ⭐⭐**Divergent sibling copies of ONE topic file: MY copy's body was correct but its FRONTMATTER+TITLE were stale; a peer's copy was stale in the BODY instead** ⇒ a peer's *"I corrected it"* is not evidence your copy was wrong — **and fix the header first, it's what gets read** · [#12205 cap-agg doc](project_12205_capability_aggregation_doc.md) jhelferty RELEASE=draft · ⭐⭐[#12313 -minify](project_12313_minify_local_obfuscation_source_target.md) ⛔⛔⛔**RETRACTED 08-07 — OUR CENTRAL CLAIM "`-obfuscate` breaks name-based reflection" IS FALSE. Published 3×; challenged by `tangent-vector` (MEMBER); refuted by runtime measurement I reproduced MYSELF @`7dc8091a6`: `-reflection-json` BYTE-IDENTICAL ± the flag, all 7 names present, guilty control 0, while emitted HLSL loses every name ⇒ obfuscation ACTIVE in the same run.** Obfuscation is **IR-level**; reflection vends from **AST-level** (our own `a1-03-obfuscation.md` says `-obfuscate` strips the AST). ⛔**`findParameterByName` IS NOT A SLANG API** — 0 hits in `include/`+`source/`, it's a test helper at `unit-test-std140-matrix-element-stride.cpp:17`; **we repeated the reporter's prose for a week without checking the symbol existed.** ⭐⭐⭐**(1) THE HEDGE NAMED THE GAP AND DIDN'T STOP THE CLAIM** — *"a source read … not a runtime experiment"* sat ONE SENTENCE before *"confirmed … That part is legitimate"* ⇒ **an accurate disclaimer beside an overclaim is INERT: it documents the risk instead of blocking it. If a hedge says "not measured", the claim it guards may not say "confirmed."** ⭐⭐⭐**(2) WE HELD THE REFUTING EVIDENCE AND SPENT IT ELSEWHERE** — the 08-05 `--strip-debug` finding (*"reflection survives because Slang serves it from its OWN layout data"*) IS the refuting mechanism, cited 3× for a different point ⇒ **WHEN A NEW FACT ESTABLISHES A MECHANISM, RE-TEST EVERY EARLIER CLAIM THAT RESTED ON THE OPPOSITE ONE.** ⚠️**PREMISE COLLAPSE reframes everything: the reflection break was the accepted premise of the WHOLE request ⇒ the OP hit something else (serialized-AST, or `import`-resolution), and Q3 MAY BE MOOT — a fix for a defect that may not exist. Q3 HELD WITH PREMISE IN DOUBT; do NOT revive without re-deriving it from a runtime measurement.** Prior still-valid history: **08-05 jkwak (now ASSIGNED) answered: soft-NO on presets + cited `-Xspirv-opt`** ⇒ Q1/Q2 closed, **Q3 OPEN** (reflection-preserving `-obfuscate` = correctness carve-out on an EXISTING flag, NOT a preset ⇒ anti-preset rationale does NOT reach it). ⭐⭐⭐**GUILTY-CONTROL SHAPE, copy it: a byte-identical output fits BOTH "ignored" and "ran, changed nothing" ⇒ weak. A deliberately-INVALID pass name discriminates — exit 255 on `-target spirv`, ZERO diagnostic + exit 0 on `-target hlsl`** ⇒ silently accepted and discarded; flag structurally unreachable for text targets (sole `getDownstreamArgs("spirv-opt")` @`slang-emit.cpp:3385` → SPIRV switch case only). ⭐**NEW fact in neither jkwak's comment nor our own verdict: `--strip-debug` gives the OP's ask on the BINARY path — `OpName` 5→0, symbol gone, yet reflection SURVIVES because Slang serves it from its OWN layout data, not `OpName`s** (+ jkwak's bare `strip-debug` spelling exits 255; needs `--`). ⛔**Triager published a correction to a sentence a SIBLING had already fixed 08-04 — patched ~2.5min later.** ⇒ ⭐⭐⭐**"I previously said X" is a claim about an ARTIFACT — open the artifact, not your memory; a self-correction is the statement class most likely to be already-stale on a shared store** (and the sibling's fix sat a few lines ABOVE the line they did read). **08-06T01:09Z jkwak WITHDREW the `-Xspirv-opt` suggestion** (*"re-read the description, it is definitely about the textural output such as HLSL"*) ⇒ analysis landed; **ESCALATED to office hours, gate WIDENED 1→3 maintainers.** ⛔⭐⭐⭐**"no labels EVER set" — verified WITH A POSITIVE CONTROL — WAS DEAD 6 h LATER** (`labeled`×2 @01:08:18Z, jkwak, `Office-Yong`+`Office-Tess`, 50 s before his comment) ⇒ ⭐⭐⭐**A MEASUREMENT CAN HAVE AN EXPIRY: a control proves the INSTRUMENT fired, NOT the claim's SHELF LIFE — "verified" ≠ "durable", and rigor on the first manufactures false confidence in the second. Tell: a NAMED, ENGAGED assignee is precisely when absence-of-human-action should be expected to flip.** ✅`Office-*` = office-hours AGENDA markers, read from label descriptions not guessed ("To be discussed during Yong's/Tess' office hours", 14/8 uses) ⇒ ⭐⭐**a THIRD maintainer (Tess) is in scope — jkwak's comment names only csyonghe; that fact lives ONLY in the labels.** **08-07T05:39Z csyonghe (LEAD) delivered the outcome ⇒ disposition FLIPPED maintainer-gated → OP-GATED.** ✅**Q1 answered `no` ON THE RECORD** — he adopts our emit-slang-stub analysis **by name**. ⭐⭐⭐**THE CRUX (verbatim @`88fa1206d`, `docs/user-guide/10-link-time-specialization.md:25-30`): PRECOMPILATION IS "INDEPENDENT OF ANY SPECIALIZATION ARGUMENTS" — specialization happens at LINK time, "reusing all the work done during module precompilation" ⇒ it is NOT "precompile every permutation", which is exactly what the OP's blocker assumed away** ⇒ Approach C genuinely back via the mitigation they pre-emptively rejected. ✅**Q3 passed over by BOTH maintainers — MEASURED (0 fragments in all 3 maintainer comments, non-zero controls fire) ⇒ two independent silences = information, NO third ask** (re-raising after two declines reads as pestering once a real alternative exists). Boundary verified both ways: WORKS=values/loop bounds, struct-field presence via `Conditional<T,bool>`, resource-binding presence (compiled), type substitution; NOT=whole-decl gating (`E20001` vs `#if` exit 0), `import` gating, EP-signature changes, LTC array sizes (`E31010` WIP). ⭐⭐**Declined to assert on the OP's corpus — asymmetry: guessing "yes" costs them a wasted refactor, guessing "no" talks them out of a working path; neither recoverable from a comment** ⇒ published criteria + the deciding question, not a verdict. ⚠️`$?` after a pipe = 2nd hit this chain (141/SIGPIPE masked a real `E20001`) — **the diagnostic TEXT was the datum**. RESUME=**@j8asic** answers values-and-presence vs shape-changing ⇒ closes-as-answered (zero code) or yields a NAMED gap. ⛔**No maintainer action pending — do NOT nudge jkwak/csyonghe/Tess.**

---

⭐ **Why these 11 and not others:** each is blocked on a *named human* (jkwak, skiminki-nv,
tangent-vector, kaizhangNV, a reviewer approving, or a reporter replying) — so none can advance from
our side this tick. Rows that remained in `MEMORY.md` are ones where we hold the next move, or where
a 🔴 alarm must stay in first-screen view.

⚠️ **Before acting on any row here, open its child.** These one-liners are pointers; the retractions,
verbatim probes, comment ids and SHAs live in the per-issue files.

## Rows relocated from MEMORY.md (2026-08-04, 3rd spill)

Same status as every row above: **LIVE**, gated on a named human. Moved for index
bytes only — see the floor rule in `MEMORY.md`. RESUME triggers are verbatim.

- ✅⭐**Both 17d-stale jkwak triage asks CLEARED 08-04** — [#8306 embed core+GLSL](project_8306_embed_core_glsl_module_slang_dll.md): maintainer's *"might be resolved"* **REFUTED**, 2 defects live @`546ad18f7` (`EMBED=ON+SOURCE=OFF` null-derefs `slang-global-session.cpp:392-397`). POSTED `5173187857`/`5173197689`. **No fixer.** RESUME=jkwak.
- 🔴⛔**[#8785 amplification payload — 2 PUBLIC RETRACTIONS; open the child before restating ANY verdict](project_8785_amplification_shader_payload_docs_wrong.md)** — ⛔not "docs wrong, NOT the compiler" (**both**) · ⛔GAP1 real cause = **silent aliasing**, unfiled. ⭐TRUE TRIGGER = entry-point **PARAM**. ⭐⭐**never infer "compiler fails to emit X" from X's ABSENCE.** ✅A1 = **github.io#210** (open, 0 comments, bot-authored, verified live 08-04). ⛔⭐⭐**MY defect here = the memo's FIRST section still carried the SUPERSEDED `medium/P2 / no labels` verdict while the retraction sat 2 sections BELOW it** ⇒ fixed in place; live = `P1/high`, label `reproduced`. Also **[a live-artifact read is a measurement with a TIMESTAMP](feedback_a_live_artifact_read_is_a_measurement_with_a_timestamp.md)** — memo asserted `updated_at 00:36:25Z`, live read **00:50:15Z** (2nd instance; 1st was #11616). ⛔🔄**RE-OPENED 08-05T00:11:17Z — jkwak ANSWERED, so the old RESUME has FIRED and is superseded.** Cmt `5186013200` (assignee, comments 3→4, verified live): *"I need to know how the Amplification shader is supposed to work… I know almost nothing… give me a simple example that involves `payload`… what is the principled way to proceed."* ⇒ **NOT a pickup of A2 — a REQUEST FOR TEACHING + a design call.** ⛔**The `payload`-example ask collides with our own finding: `payload` is a RED HERRING (bare `out T p` ICEs too) and the documented `out payload T` form is INVALID + CRASHES — so a "simple `payload` example" cannot be given as-asked; it must be reframed to the SUPPORTED forms (`groupshared` global / plain local) or it teaches the broken shape.** Dispatched to triager on the canonical thread. RESUME=**triager's teaching reply on GitHub → jkwak names the fix scope.**
- 202c frontend, blocked on #12179 OPEN (skiminki-nv). ✅**NOT STALLED — WITHIN REPO NORM; nudge DECIDED AGAINST 08-04, no GitHub footprint.** ⛔My "14 days silent" was WRONG: timeline shows `convert_to_draft` 07-23 → **author dismissed his OWN approval** 07-24 → `ready_for_review` 07-27 ⇒ **reviewer-idle starts 07-27 = 8d**, and I measured across a draft round-trip the AUTHOR drove. ⛔I also had the dismissal polarity backwards: jkwak-work **APPROVED** ("Looks good to me"); **skiminki-nv dismissed it himself** to prevent an accidental merge ⇒ he KNOWS a re-review is owed. ⭐⭐**BASE RATE KILLS IT: 50 of 74 open non-draft PRs are idle ≥8d** (triager: 32/53 — different scope, **same conclusion**; a base-rate argument survives an imprecise denominator, a threshold argument does not). ⚠️`reviewed-by:bmillsNV`=**0** vs `commenter:`=396 (control `szihs`=119) ⇒ he doesn't submit formal reviews and holds 85 review-requests ⇒ "request another reviewer?" misdiagnoses a REPO-WIDE pattern. ⭐⭐⭐**A NUDGE IS JUSTIFIED BY DEVIATION FROM THE REPO'S NORM, NEVER BY AN ABSOLUTE DAY COUNT.** ⚠️#12264 body says *"Depends on #12179"* verbatim and BOTH dependents are skiminki-nv's OWN issues ⇒ a nudge would tell him what he wrote. ⛔**Do NOT re-arm on elapsed days — re-arm only if idle-time exceeds the repo median, or he asks.** RESUME=**a reviewer APPROVES #12179 → it merges → then PR**: [#12264 missing-return](project_12264_missing_return_unconditional_error_202c.md) P3 · [#12296 empty-stmt err](project_12296_empty_statement_error_contexts.md) P2
- ⚠️**[#12337 backend-codegen compile time (pattern report, 3 Discord users)](project_12337_backend_codegen_compile_time_pattern.md)** — own-bot echo (slang-triager filed it 08-04 10:06Z) ⇒ **NO re-triage, NO fixer**; its own gate is `sbangaru_nv`'s repro ask. ⭐**an echo means don't RE-DO the work — NEVER a reason to withhold NEW verified content** ⇒ dispatched 3 additive facts, pinned to the filing session. Substance: the **FENCED memory hypothesis is NOT an unverified one-off** — it range-matches #12113's *reproduced + root-caused* session floor (212 MiB macOS / 188 Linux; prototype PR **#12136 OPEN but `mergeable_state=dirty`, 19d stale**), and **#12113 + #12112 are BOTH absent from a dedup list the issue asserts ran 3×** (`grep -c '12113\|12112'` over the body = 0). ⭐**a dedup scope drawn for claim A does not cover a hypothesis B you fenced later ⇒ re-run dedup on the new axis.** ⭐**de-coupling two axes STRENGTHENS a fence** (session-create 0.5s→0.23s ⇒ the floor **cannot** explain a 40s compile). ⚠️range match, **not** identity — his exact number/platform unknown. ⛔**no label, none recommended** — siblings' `reproduced`/`regression` would be FALSE here; ⭐**matching a sibling's label set is not evidence the labels apply.** ➕`api_session_create` is ALREADY a compile-perf workload (`manifest.py:153-159`) ⇒ one harness run covers both axes, so its step 2 is cheaper than claimed.
- 🔴⭐⭐**[#12337 FOLLOW-UP — tangent-vector's spirv-opt baseline question, ANSWERED FROM SOURCE](project_12337_spirvopt_baseline_asymmetry.md)** (cmt `5195598507`, 08-05, **first non-bot comment ⇒ the RESUME fired**). ✅**His asymmetry is REAL and is the DEFAULT on both sides** @`b0e43d657`: glslang's `SpvOptions.disableOptimizer{true}` (`GlslangToSpv.h:50`) + gate needs `(prelegalization||optimizeSize) && !disableOptimizer` where **`prelegalization` is `EShSourceHlsl` — HLSL only** (`GlslangToSpv.cpp:11878`) ⇒ **GLSL input with no `-Os` runs ZERO spirv-opt**; Slang's default IS `-O1` (`slang-compiler-options.cpp:459` `getDefault→OptimizationLevel::Default`) ⇒ ~14 passes unasked. ⇒ **~14 passes vs 0.** ⚠️`h7per`'s exact flags UNKNOWN — the one thing only he can supply ⇒ concrete answerable ask. ⛔**fairness finding, NOT a diagnosis.** ⛔⭐⭐⭐**CORRECTION TO #12337's OWN STEP 2 ("needs no compiler change"): the `emitEntryPointsSourceFromIR` bucket it named is EMPTY for `-target spirv`** — `slang-code-gen.cpp:1184-1188` takes `emitSPIRVForEntryPointsDirectly` and RETURNS before the fallthrough; that fn (`slang-emit.cpp:3499`) has **no `SLANG_PROFILE`**, and spirv-opt has **no timer/bucket anywhere** (0 hits in `slang-glslang-compiler.cpp` + `breakdown.py`) ⇒ cost hides in the `generateOutput (self)` residual. ⭐⭐⭐**an EMPTY named bucket reads identically to a bucket showing LOW cost — the inert-guard shape applied to a profiler.** ⚠️my claim is *cannot ATTRIBUTE*, not *cannot see* (totals still bound it). ⛔`slang-emit.cpp:3312` `optimizeSPIRV` is `#if 0` **DEAD** — the first grep hit is not the live call. RESUME=`h7per`'s invocations, the reproducer, or a maintainer acts on the missing timer.
- 🔴⛔⭐⭐⭐**[#12339 include-occurrence provenance for DebugFunction ownership](project_12339_include_occurrence_provenance.md)** — own-bot echo 08-04 13:22Z ⇒ **NO re-triage, NO fixer, NOTHING POSTED** (successor of merged #12148; near-successor of **#12150**, OPEN/pdeayton-nv). ⛔**MY OWN "does not reproduce / 2-of-4-claims-wrong" FINDING IS RETRACTED — caught pre-publication; never restate it.** 3 defects, **zero bad commands**: (1) ⛔⭐⭐⭐**A FALLBACK'S OUTPUT IS NOT EVIDENCE THE FALLBACK DIDN'T FIRE** — I read "has a CU" as "was bound", but `lower-to-ir:15474` emits a CU only `if !source->isIncludedFile()` ⇒ included files get **NO** CU ⇒ null parent (`:14701`) ⇒ `emit-spirv:10596-10604` falls back to module-global, which `:12190` **sets to the entry point's CU** ⇒ *"module-global fallback"* and *"entry-point CU"* are **THE SAME OBSERVATION** ⇒ Symptoms 1+4 are **CORRECT**. Discriminator = enumerate objects with a `DebugSource` but **no** CU (`inc.slang` → exactly that). (2) ⛔⭐⭐⭐**"DOES NOT REPRODUCE" IS A CLAIM ABOUT A TREE — NAME IT**: the issue describes an **UNLANDED branch** (binding logic absent at master), so my repro *could never* exhibit Symptom 3 ⇒ **UNVERIFIED, not refuted**; my control was well-formed **for the wrong scope** (TICK-87 recurrence — and the #12150 peer session had ALREADY filed this exact learning). (3) ⛔⭐⭐**RE-READ CLAUSE-BY-CLAUSE**: body says `slang-session.cpp` *"receives the directive loc but never threads it into view creation"* — never claims the view is made there; it **verifies** (`:1964` creates none; real site `preprocessor.cpp:5103`, `SourceLoc::fromRaw(0)`) ⇒ I refuted a strawman. ⭐⭐**a defect in what a measurement MEANS is invisible to more measurement.** ⭐⭐**one byte-exact cite (`preprocessor.cpp:3728` ✅) bought unearned trust, then one misread bought unearned sweep-condemnation ⇒ ADJUDICATE PER ITEM.** Net: **#12339 is MORE credible than my draft said; I hold NO refutation** — only a doc criticism (reads as master, describes a branch; cites 2 tests that exist only there). **08-05: SIBLING posted triage `5194175309` (16:01Z, `sess-1785932426187-0boyp4` — NOT me; the author field never identifies the writer) + `reproduced` + pdeayton assigned 18:16Z; it INDEPENDENTLY reached my retracted-to position.** ✅**Symptom 3 RESOLVED (was "unverified"): it accurately describes SUPERSEDED commit `9ac664773`** (own comment *"keep the **first** whose chain lands in"*), which **`65b81628c` replaced, its message documenting the exact regression** — *"binding the first qualifying includer **was measured against master to be a regression**"* ⇒ the body's defect is **TENSE/PROVENANCE, never fabrication** (a strictly better citation than the sibling's, which doesn't name that commit). ⛔**2 of MY facts DECAYED, both true when written:** (1) *"no PR carries this"* — **#12340 was created 13:36Z, ~14 min AFTER my 400-branch/230-PR sweep** ⇒ ⭐⭐⭐**a "nothing exists" sweep is a TIMESTAMPED reading, not a standing fact — the window here was MINUTES** (3rd of the class: #11616, #8785); (2) *"branch not in my container"* — **`git fetch origin pull/12340/head` works (rc=0)**; I inferred unavailability from a missing worktree ⇒ ⭐⭐**"I don't have it" is a claim about an INSTRUMENT — run the one command that settles it.** 24 `debug-function-scope-*` fixtures at PR head. 08-05 18:17Z jhelferty *"@pdeayton-nv is this germane to your interests?"* = **maintainer↔maintainer, NON-SUBSTANTIVE** (no bot mention in the body; he assigned pdeayton 1 min earlier; byte-identical to the 07-29 #12150 ping) ⇒ **nothing posted, no fixer, not a re-open.** RESUME=pdeayton answers the posted `__include`-provenance question, a **substantive non-bot** comment, or #12340 leaves draft.
- ✅⭐⭐**[#12338 `Optional<IFoo>` re-boxes an existential](project_12338_optional_existential_reboxing.md)** — NEW LDAP 08-04; **triage POSTED `5179233988`** (my only comment). ⭐⭐**THE REPORTER'S FRAMING WAS A HYPOTHESIS: the trigger is the `none`, NOT the `Optional`** — deleting the `p.foo = none` store makes the same `Optional<IFoo>` lower to bare `%MyFoo`, 0 bitcasts (with it: `%Tuple`, 8 bitcasts). Cause = `NoneTypeElement` occupying a set slot ⇒ `isSingleton()`'s raw `getOperandCount()==1` fails ⇒ tuple+`AnyValue(16)` path. Fix shape already in-tree 2× (`lowerTaggedUnionType`'s lone-none case; `filterNoneElements`); need "singleton modulo none". ⚠️**I did NOT establish one fix covers the `createDynamicObject` reproducer too — said so publicly; don't flatten to "root-caused".** ⭐⭐**"performance-only, not a miscompile" IS a correctness claim ⇒ I EXECUTED all 3 variants (byte-identical) + a non-zero control** rather than publishing the inspection-based assumption I'd drafted. ⭐`-target exe` returns rc=0 and writes NO file — stat the artifact. **NO label/assignee (no authority).** ✅**08-05 jhelferty-nv asked "repro on hlsl?" → ANSWERED `5195640966`: YES, and far more readable** (named `packAnyValue16`/`unpackAnyValue16` + `asuint`/`asfloat`, not inline `OpBitcast`; 109/211/126 lines; material 369→481 with a SECOND NESTED box). ⛔**dxc trap: emitted HLSL fails validation on ALL 3 variants INCLUDING BASELINE ⇒ it's the reporter's `[noinline]`, NOT this bug** — ⭐⭐**a failure shared by the control is the ENVIRONMENT, not the finding.** ⚠️caught "4 pack/unpack funcs" pre-post; real = 3 DECLS + 3 call sites ⇒ ⭐⭐**say whether a count is decls or occurrences.** ✅✅**(08-05 saipraveenb25, the pass AUTHOR, confirmed "one implementation + none" = my singleton-modulo-none, self-assigned.)** ✅**My open caveat CLOSED (`5196649835`): BOTH reproducers reduce to the same shape** — the `createDynamicObject` field's sets are `{none, exactly 1 concrete table}` with `grep -c Unbounded`=0, so the link-time `-conformance` leaves NO open set ⇒ predicate is on SET CONTENTS. ⭐**Published as covering THESE TWO INPUTS, NOT a general proof** (never built a 2+-conformance case to check it DECLINES) — ⭐⭐**resolving a caveat is itself a scoped claim; name the inputs or the fixer inherits your hedge as a guarantee.** ✅✅**SHIPPED & CLOSED 08-15T02:30Z — saipraveenb25 merged PR #12459 ("Lower singleton optional payloads directly") = my root cause.** `tryGetSinglePayloadType` source-confirmed on master (`slang-ir-lower-dynamic-dispatch-insts.cpp`, sha `1f62f698`): maps `{FooImpl,none}`→`FooImpl`, rejects multi-payload — IS singleton-modulo-none. 8/8 cross-backend. ⚠️**tag is `uint` NOT the `bool` I called the target shape** (his call: bool↔uint conversions add logic, backends 4-byte-align bool). **TERMINAL — no action of mine; verify-offer was conditional and his suite supersedes my repro; reporter LDAP owns any re-open. Do NOT post an ack comment on a closed issue.**
- ⛔✅**[#12350 nested `__target_switch` unmatched case → `E41011`](project_12350_nested_target_switch_no_match.md)** — **CLOSED 08-04 22:33Z by jkwak: *"Closing, because this is a preferred behavior."*** ⭐⭐**A REFUSAL stamped `state_reason: completed`** — the confirming case for [[feedback_state_reason_is_not_polarity_either]] in the direction it lacked (`not_planned` over-collects; **`completed` UNDER-collects**). **TERMINAL for the filed question**: `E41011` stays, direction 1 rejected, jkwak's **`default: break;`** is the sanctioned shape (our tier verified with a real negative control — delete that one line ⇒ error returns). ⚠️⚠️**BUT THE CLOSE IS SCOPED — it postdates by 9min a SECOND case** (silent fall-off of a non-empty *selected* arm — no diagnostic at all, module byte-identical with/without `break;`) **that jkwak himself asked for a warn-or-error on 22min earlier** (`r3716544275` 22:11Z) **and does not engage it** ⇒ ⭐⭐⭐**a close is scoped to what the closer had in view; when an issue GREW before closing, "closed" ≠ every case adjudicated — check whether the terminal comment postdates the expansion and engages it.** That ask is **LIVE on PR #12186's thread `r3716544275`, `isResolved: false`** — do NOT read it as declined, do NOT reopen #12350. ✅My overclaim correction was **applied AND independently verified** by the fixer (both-caps run now positive evidence in the body: `111`+`222` emitted, `333` absent) — ⭐routing to the footprint owner beat patching it myself: I had the explanation, it had the build. 🔒**#12186 approval SURVIVED** — `reviewDecision: APPROVED`, head `65338dbef9`, all 4 jkwak 08-04 reviews `COMMENTED` ⇒ non-dismissing; no push happened, none may. RESUME=**jkwak/csyonghe answer the fall-off diagnostic on the PR thread** (own PR if it lands; not bolted onto the approved one).
- 🆕⚠️**[#12356 `getDefaultValueBlob` has no portable native C ABI entry point](project_12356_default_value_blob_c_abi_gap.md)** — own-bot filing 08-05T03:40Z from Discord (**xylisn**, binding from C#) ⇒ **echo: NO re-triage from scratch**; the body IS a verified report and I independently re-verified its load-bearing claims at its own cited commit `ff45b15ed` (flat export **0** hits w/ non-zero control; blob getter is the **only** `SLANG_API` member of non-COM `struct VariableReflection` ⇒ no vtable route either; doc block `slang.h:3262-3283` silent on C-ABI reach; not-a-regression **holds** per-tag v2026.11→v2026.14.1, C++ member first in **v2026.14**; intent thread `3519628104` → author `duckdoom5` **"yes"**, resolved). ⭐**The defect is the ASYMMETRY, not the absence: the deprecated `...Int`/`...Float` keep flat exports documented "use `getDefaultValueBlob` instead" ⇒ a C-ABI caller can reach the deprecated API but NOT the replacement its own deprecation notice points at.** ⚠️**do NOT quote the body's control figure "9 files"** — source-tree count is **3**; 9 counts `build/` artifacts (`.gch`/`.o`/`.so`/copied headers). Substantive 0-vs-nonzero unaffected. 🔴**Remedy B is [[project_11826_slang_deprecated_audit]] REPLAYED** — `include/slang-reflection.h` still **does not exist**, #11826 closed won't-fix (stamped `completed`) + #11827 15s later, and **our gap-surfacing comment `4848500243` is the LAST word there — jkwak silent 5 weeks** ⇒ that memory's *hold, do not re-surface uninvited* still binds: **#12356 is a new occasion, NOT permission to re-poke #11826.** Only remedy C (document it) needs no design call, and it explicitly does **not** unblock the C# use case. Dedup clean (search returns only #12356 + #11106). ✅**ROUTED+PARKED 08-05T04:19:59Z — triager posted `5187494019`**: labels `reflection`+`client support`+`DiscordRequest`, **medium/P2**, Type left `Feature`, no `reproduced`/`regression`, no assignee; body patched with `comments` staying 0. ✅**#11826 hold HELD (Main-verified after the post: still 5 comments, `4848500243` still last).** **Call: SPLIT IT** — doc half lands now, export half waits on naming. ⛔⭐⭐⭐**MY "9 files" FLAG WAS HALF THE DEFECT: the two sides of that control used DIFFERENT INSTRUMENTS (`git grep -l` tracked vs `grep -rIl` working-tree) ⇒ TWO APERTURES ON ONE CONTROL CERTIFIES NOTHING — a stronger objection than either number being wrong.** Symmetric pairings all hold (0v3 / 0v9 / 0v30). ⭐⭐**DIFF THE SETS, NEVER THE COUNTS** — triager's "17 exports" was wrong in BOTH directions and cancelled (missed 4 wrapped decls, wrongly included `slang_getEmbeddedCoreModule` @`slang.h:5907`, plain `SLANG_API`); "17 vs 20" reads as "missed 3" and is blind to a false inclusion. Main re-derived **20**, **0 reflection**. ⛔⭐⭐⭐**A CITATION THAT MATCHES YOUR GREP MAY ASSERT THE OPPOSITE** — `unit-test-link-time-type-reflection.cpp:655` was cited as cast precedent but exists to assert `SLANG_E_INVALID_ARG` on the wrong-decl-kind ERROR path; dropped. ⚠️**And my OWN near-miss: grepped the post for `"C++-only"`, got 1, nearly flagged a reintroduction — the hit is the NEGATION ⇒ a match count can't tell an assertion from its retraction, CLASSIFY THE HIT.** ✅Reporter unblocked meanwhile: shim **built, linked and discriminated** (bogus control symbol fails the same `-Wl,--no-undefined` link), scoped Linux x86-64/GCC/Debug with Windows caveats named untested. Decision facts: `slang-deprecated.h` = **267 `SLANG_API`, ZERO deprecation attributes** ⇒ #11826's warning objection is organizational not technical; 20 `slang_*` exports, none reflection ⇒ A *establishes* the precedent. **RESUME=maintainer picks A/B/C, or any non-bot comment on #12356. No fixer — API-policy call. ⛔No second poke on #11826.**
