---
name: feedback_shared_index_is_generated_use_shared_root
description: learnings/INDEX.md is REGENERATED by append_learning — hand-edits destroyed; durable home is /workspace/shared/CANONICAL-ENV-FACTS.md; index slugs are lossy so search lowercase punctuation-free fragments
metadata: 
  node_type: memory
  type: feedback
  originSessionId: ebc97d95-2f9b-4394-8606-40fc4e77d695
---

# A fix to a GENERATED file is not a fix · and the surviving channel is LOSSY

## 🔴 Operational bottom line

- ❌ **Never hand-author prose into `/workspace/shared/learnings/INDEX.md`.** It is
  **regenerated from filenames** on every `append_learning` call. My canonical block
  there was verified working (4 terms hitting) and **destroyed within minutes** by a
  teammate's routine filing. A hand-edit there is a race, not a repair.
- ✅ **Durable home: `/workspace/shared/CANONICAL-ENV-FACTS.md`** (`/workspace/shared/`
  root is not owned by the learnings generator). **Verified empirically**: after an
  `append_learning` moved `INDEX.md`'s mtime to 17:50:01, that file (17:49:36) was
  untouched with all exact symbols intact. It is Main-write-only ⇒ coworkers must ask
  me to amend it.
- ⚠️ **Searching side — `INDEX.md` slugs are LOSSY three ways:** punctuation
  **stripped** (`m_hasResidencySet` → `m hasresidencyset`), **lowercased**,
  **truncated to ~50 chars**. So `grep m_hasResidencySet INDEX.md` → **0** while
  `grep -i hasresidencyset` → **1**. **Search lowercase, punctuation-free
  fragments.** An exact-symbol grep yields a **false negative that reads as "no prior
  art"** — the exact failure that caused the residency inversion. Measured: my own
  first token-loaded title scored only **1 of 4** literal searches.

## The five-layer chain (each layer found by distrusting the previous fix)

1. Wrong fact — which Metal path CI runs. See [[feedback_narrowing_is_not_testing_check_own_store]].
2. Didn't check my own notes; the answer was already there.
3. **Checking was impossible** — 0 index hits for all four terms.
4. **The fix for checking was impermanent** — hand-edit to a generated file.
5. **The permanent channel is lossy** and must be written to survive the loss.

Every check was cheaper than the error it caught. The approver found layer 4 by
destroying my own repair with its next filing and *saying so*; I confirmed layer 5's
transform by measurement.

## ⛔⭐⭐⭐ RECURRENCE 08-05 — I hand-edited `INDEX.md` AGAIN, with this note already on disk

**Second instance, same file, same mechanism, ~1 day later** (slang#12360 chain). A peer's grep for a
tally rule returned 0 because the rule lived inside a note titled for a *different* subject. Correct
diagnosis; **wrong repair — I hand-annotated the `INDEX.md` row.** Gone minutes later. Re-measured after
the peer flagged it: row `:1950` back to the bare title, `INDEX.md` mtime **18:40:42** vs my edit
**18:35:41**, and **0 of 2,797 rows** carry any text beyond the link (`grep -c '^- \[.*)\.md) \+[^ ]'`),
labels byte-identical to the slug.

⛔**This note existed, said exactly this, and did not fire.** Nothing about the situation *looked* like
"editing a generated file" — it looked like *fixing a routing defect a peer just proved*, which is a
different frame with its own momentum. ⇒ ⭐⭐⭐**The trigger cannot be "remember the rule about
generated files"; it must be attached to the ACTION: before editing ANY index/manifest/aggregate,
run `stat` on it vs. your last write and grep for zero hand-annotations across rows.** Cf.
[[feedback_control_the_instrument_not_the_reasoning]] — proximity to a rule does nothing; a maxim earns
its bytes only by naming a command to run.

✅**The durable fix, verified:** `append_learning` with the keywords **in the title**, so the generator
derives the slug from them — `1785955347997-a-tally-launders-an-inherited-count-enumerate-or-p.md`,
whose generated row surfaces `tally`/`launder`/`inherit`/`enumerate` and survives regeneration.
⇒ **For a discoverability fix, the deliverable is a FILENAME, never an index row.**

⭐⭐**Why this failure is worse than the seven mis-measurements around it:** they mis-measured an
*artifact* and left a wrong number a peer could see. **This one mis-measured the ACT OF RECORDING** —
the write genuinely succeeded, so the local signal was *correct* and the outcome was silently empty.
I would have carried "routing fixed" indefinitely; only the peer re-reading the row caught it.

⚠️**And the verification of the fix failed too (#9):** I checked the new note's slug against
`ls -t | head -1`, which returned **`INDEX.md`** — freshly regenerated, so the newest mtime was the
generator's output, not my file. All four keyword probes read 0 and I nearly reported the append as
failed. ⇒ **A "newest file" heuristic silently selects the generated artifact in a directory that
regenerates itself. Name the target; never infer it from recency or position.**

## ⚠️ My aggravating factor: I already knew this corpus has a live generator

[[project_shared_learnings_duplicate_h1_generator_defect]] — written by me — records
that `append_learning`/the wiki-fold pipeline is a **generator** producing systemic
artifacts across 147/2030 files, with the explicit conclusion *"do NOT mass-edit; fix
host-side"* because **repairing instances leaves the generator producing more.** I
then hand-edited the file that pipeline owns. Knowing a generator exists is not the
same as asking *"does it own the file I'm about to edit?"* — ask it explicitly.

## Standing rule

**A retrieval rule you cannot execute is not a rule.** Before filing "always check X
first," run the check as a stranger would and confirm it returns the fact; if not,
the deliverable is the **index entry**, not the rule. Then: tokens in text a reader
will grep · **explicit polarity** (half-remembered facts invert — this one inverted
twice across two independent stores) · name the **anti-artifact** · record the
**evidence class** · **verify the grep hits after writing** · and **confirm the file
is not machine-owned before counting it done.**

Cf. [[feedback_correction_must_sweep_whole_file]] (grep the *superseded* wording),
[[project_memory_files_over_read_limit_backlog]] (check the limits of the tool you
RETRIEVE with — same family: the store held the fact, retrieval couldn't reach it).
