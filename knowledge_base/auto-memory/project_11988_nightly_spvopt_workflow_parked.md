---
name: project_11988_nightly_spvopt_workflow_parked
metadata: 
  node_type: memory
  type: project
  originSessionId: 13ccda3c-664d-425d-97a8-b6642c9cd6f5
---

**✅ TERMINAL (07-22) — maintainer supersession, clean takeover (approach VALIDATED not rejected).** Bot draft #12159 CLOSED unmerged (`closedAt 2026-07-22T21:16:29Z`, mergedAt=null); jkwak close comment "Closing in a favor of: #12187". jkwak authored his own **[PR #12187](https://github.com/shader-slang/slang/pull/12187)** ("Add slang-test optimization option", **non-draft**, author jkwak-work, requested-reviewer bmillsNV, `Fixes #11988`) — same suite-wide `-O<level>` CLI-flag design (accepts `-O0..-O3` on the slang-test command line, threads `Options::defaultOptimizationLevel` through all command builders, `-Xslang -OX` for render-test, Metal `-O1` exception preserved) + new `tools/slang-test/README.md`. **PIECE #1 = maintainer's hands now, bot stood down.** **PIECE #2 (nightly workflow YAML) still LIVE** — #12187 touches no `.github/workflows/`, so git-apply diff [comment 5014780707](https://github.com/shader-slang/slang/issues/11988#issuecomment-5014780707) remains the standing artifact for jkwak to hand-apply once the flag lands (bot can't push workflows). Issue verdict [comment 5014611501](https://github.com/shader-slang/slang/issues/11988#issuecomment-5014611501) refreshed in-place → "PIECE #1 adopted by maintainer PR #12187; PIECE #2 available as a git-apply diff". slang-reviewer stood down (nothing posted; #12159 no longer a live diff); worktree removed, sentinel cleared. **Next human: @jkwak-work merges #12187 + applies workflow diff. Chain re-opens ONLY on fresh substantive human comment.** Verified independently via gh/graphql (triager + orch).

---

shader-slang/slang **#11988** — "Add a nightly slang-test workflow that runs the suite with SpvOpt enabled." Follow-up to #11805 (defaults slang-test to `-O0`, skipping spirv-opt) and counterpart to [[project_11919_remove_ox_optins_parked]] (Phase 2 HELD) — nightly SpvOpt run is the centralized coverage safety-net once per-test `-OX` opt-ins are gone.

**PARKED — no fixer dispatch, no GitHub post.** Two decisive constraints:
1. **Maintainer-owned.** Authored by nv-slang-bot at @jkwak-work's request AND explicitly assigned to @jkwak-work ("create the follow up task ... and assign it to me"). jkwak claimed ownership — same pattern as [[project_11746_witnesstable_refactor_pending]] / [[project_11806_cmake_options_maintainer_selffix]]. Do NOT auto-dispatch.
2. **Workflow-YAML deliverable the bot can't push** — see [[project_bot_workflows_permission]] (App lacks `workflows` perm). Any bot output would have to be a `.github/workflows/*.yml` diff posted as a comment for a maintainer to apply.

No unsolicited GitHub verdict: the issue body is already a self-documenting spec (4-step "Suggested approach") assigned to its owner; a bot "parking" comment = meta-noise + nudge risk.

**Re-engage only if:** jkwak (or a human) comments asking the bot to draft/implement it → then draft the YAML modeled on `.github/workflows/nightly-slang-test.yml` and post as a comment (never a pushed PR); OR a PR appears linking #11988 → route to slang-reviewer. Canonical thread `gh-issue-shader-slang/slang-11988` (fresh; distinct from #11805/#11919 chains).

**Update 07-19c — FIXED, DRAFT PR #12159 held pending review.** slang-triager [Triage Resolution]: both pieces delivered, held draft (no auto-close, drafts-only guardrail intact).
- **Draft PR [#12159](https://github.com/shader-slang/slang/pull/12159)** — piece #1: new `slang-test -default-optimization-level <level>` flag (`0/none|1/default|2/high|3/maximal`, or `-O<level>`; validated via existing `isSlangOptimizationArg`). Override threaded through both injection helpers (empty=byte-identical to today; Metal keeps `-O1`; override wins on non-Metal), all 16 call sites; new `slangTestDefaultOptimizationOverride` unit test; empty `tests/expected-failure-spvopt.txt`. 6 files +176/−22. `closingIssuesReferences=[11988]`, label `pr: non-breaking`.
- **Piece #2 (workflow YAML, bot can't push)** — posted as git-apply-able fenced-diff [issue comment 5014780707](https://github.com/shader-slang/slang/issues/11988#issuecomment-5014780707) (Linux-only, `-default-optimization-level 3`, cron `0 1 * * *`).
- **Tests:** local Debug 1180/1180; unit 2/2; flag accepts `3`, rejects `bogus` w/ diagnostic. codex CODE/PLAN/OUTPUT approved. Draft CI only-red = cosmetic priority-yield (builds skipped), not a regression.
- **Review:** peer review dispatched to slang-reviewer — VERDICT PENDING. Triager holds chain open, will refresh issue verdict + forward when reviewer lands.
- **Next human (jkwak):** review/merge draft #12159 + apply the workflow YAML from the fenced comment + resolve two open decisions (failure-policy: hard-fail+expected-failure-spvopt.txt vs report-only; flag-name/cron). Bot does NOT flip ready/merge. Issue verdict [comment 5014611501](https://github.com/shader-slang/slang/issues/11988#issuecomment-5014611501) refreshed in-place → "fix in draft PR #12159, held pending review."
- **✅ Webhook-routing verified (07-19c):** head branch = **`fix/issue-11988`** (gh-confirmed by triager; PR OPEN, isDraft=true) → matches CLAUDE.md path 2b branch-convention fallback, so #12159 webhooks (jkwak review, CI) route to slang-fixer regardless of whether `report_pr_created` fired. No orphaning risk. `report_pr_created({repo:"shader-slang/slang",pr_number:12159})` **receipt CONFIRMED** (host: "#12159 mapped to this session, webhook events will route here") — already exercised: a real `github.ci_failed` webhook for #12159 routed to fixer session (= cosmetic draft priority-yield, builds skipped, no action). Both primary session-map AND branch fallback cover routing. **No open items for this state.** Chain holds for: slang-reviewer verdict / #12159 merge / jkwak reply on the 2 open decisions.

**Update 07-19b — TRIAGED, verdict posted, fixer engaged.** slang-triager [Report]: enhancement/low/P3, CI+test-tooling(slang-test). Verdict posted on issue ([comment 5014611501](https://github.com/shader-slang/slang/issues/11988#issuecomment-5014611501)); triager forwarded to slang-fixer on its own downstream edge (chain owns that hop — do NOT double-dispatch fixer, do NOT reach past triager). **Recommended Approach A:** new slang-test CLI arg `-default-optimization-level <level>` overriding the hard-coded `-O0` suite-wide default from #11805 (verified `slang-test-optimization-options.h:14/66/126`; threadable via `context->options` at ~16 injection sites in slang-test-main.cpp; `-O0` is sole optimizer early-out at `slang-glslang.cpp:275`) + Linux-only nightly workflow. Files: `tools/slang-test/{options.h,options.cpp,slang-test-optimization-options.h,slang-test-main.cpp}`, `tools/slang-unit-test/unit-test-slang-test-optimization-options.cpp`; workflow → `.github/workflows/nightly-slang-test-spvopt.yml` (fenced comment, bot can't push). **Two open jkwak decisions flagged publicly (await his GitHub reply → webhook):** (a) failure-policy = hard-fail+expected-failure-list vs report-only; (b) flag-name/cron slot. Triager proposes free nightly cron slot **01:00 UTC**; force-max-SpvOpt = pass `-O3`/maximal (any level ≥1 runs optimizer). HEAD at triage = `a916653b7`. Triager forwarded fixer both the memo AND a ready-to-post YAML draft. Fixer proceeds to draft PR on recommended defaults meanwhile. Triager holds the loop open, will refresh issue verdict + forward resolution when fixer's [Fix Report]+PR# land. Await that via triager (my direct child) — NOT directly from fixer.

**Update 07-19 — PARK RELEASED, dispatched to slang-triager.** jkwak posted a real bot mention ([comment 5014572261](https://github.com/shader-slang/slang/issues/11988#issuecomment-5014572261)): "@nv-slang-bot , can you triage and make a PR?" — explicit request from the assigned maintainer. Routed issue→slang-triager on `gh-issue-shader-slang/slang-11988` with `<github-post-authorized />` (real mention). **Deliverable-split constraint propagated:** (1) slang-test CLI arg = pushable source → in PR; (2) nightly workflow YAML = NOT pushable (no `workflows` App perm) → post as diff-comment for jkwak. **Drafts-only guardrail stands** — "make a PR" authorizes creating a DRAFT PR, not flipping ready/merge (still operator/maintainer-gated). Await triager [Report] → fixer handoff.

**Update 07-08 — jkwak posted his own design plan** ([comment 4910355852](https://github.com/shader-slang/slang/issues/11988#issuecomment-4910355852)): two pieces — (1) a NEW slang-test CLI arg to change the default optimization level (the *inverse* of #11805's `-O0` default), (2) a new nightly CI workflow using the maximal SpvOpt option. NO @nv-slang-bot mention, no bot request — the `pr_mention` event was a false-positive on a plain issue comment. Treated as HOLD (maintainer self-driving his own assigned issue); no fixer dispatch, no GitHub reply. NOTE: the CLI-arg half is *source code the bot CAN push* (unlike the workflow YAML) — so if jkwak later asks the bot to draft it, dispatch slang-fixer for the slang-test arg only and leave the YAML as a comment for him.
