---
name: project_12048_fixer_autocompact_loop_0713
description: 07-13 slang-fixer autocompact thrash loop (self-confirmed); group-restart REQUESTED but operator-approval-gated; restart≠/clear caveat
metadata: 
  node_type: memory
  type: project
  originSessionId: cfcf4a4e-5244-42d9-83b6-81819372a1ae
---

**Incident 2026-07-13 ~07:46-07:52:** slang-fixer (ag-1780667166439-vmjrwe) entered an autocompact thrash loop. Triager flagged it (not a #12048 chain action — #12048 is closed/peer-approved). CONFIRMED by the fixer's OWN tooling: "Autocompact is thrashing: context refilled to the limit within 3 turns of the previous compact, 3 times in a row… try /clear to start fresh." 3 consecutive "Context compacted" notes ~5 min (07:46 868k → 07:49 906k → 07:51 893k), zero work product. Cannot self-recover.

**#12048 fix is SAFE regardless:** PR #12050 pushed (head 4c507cb94c, verified on remote), peer-APPROVED, `report_pr_created` confirmed → PR webhooks route to whatever session the fixer resumes as. Nothing lost by restarting.

**Action taken (Main, 07-13):** requested scoped `ncl groups restart --id ag-1780667166439-vmjrwe` (no `--message` — bare bounce, avoid injecting an inbound that re-triggers churn). **Result: `error (approval-pending)` — restart is HUMAN-OPERATOR-APPROVAL-GATED, Main cannot execute unilaterally.** Safe blast radius verified first: at request time ALL slang-fixer containers showed `container_status=stopped` (none running) → a group bounce kills no legitimately-running build; sibling sessions (#12069-12075, slangpy chains) persist across restart.

**CRITICAL CAVEAT [[project_11917_fixer_compaction_loop]]:** a `groups restart` is a CONTAINER BOUNCE, not a `/clear`. Session history lives in the two session DBs independent of container lifecycle — if the poison is accumulated history, the session reloads it on first wake and RE-THRASHES (empirically confirmed on #11917: probe re-thrashed on first turn post-restart). `ncl sessions` is READ-ONLY (no clear/kill verb) → Main has NO self-serve history-clear lever. If the approved bounce doesn't clear it, escalate operator for the real lever: (a) true `/clear` on the specific thrashing session (keeps id → webhooks still route), or (b) retire the session so next canonical-thread inbound mints a fresh clean one.

**UPDATE 07-13 09:23 — RESTART REJECTED by admin** (system-notification: "cli_command request was rejected by admin"). Operator declined the bare `groups restart`. Consistent with the caveat above: a container bounce likely wouldn't clear history-poison anyway, and the operator may prefer to leave the session inert or apply the real lever themselves. **NO retry** — respect the decision. Main has no self-serve alternative (`ncl sessions` read-only; no `/clear`/kill verb). The stuck session is INERT (only wakes on inbound) → no runaway token burn while parked; the blocked thing is fixer *work*, not a live fire.

**Posture (post-rejection):** PARKED, operator-owned. NOT messaging the fixer. Triager holding. Act ONLY on a state change — operator applies a clear/retire lever, OR a fixer session emits real work again (recovered), OR new fix work needs the fixer and the loop still blocks it (then re-surface ONCE with the sharpened ask: true `/clear` or session-retire, not another bounce). Do NOT re-request the restart. #12048 itself needs nothing (PR #12050 pushed+peer-approved, webhooks route on resume).

**UPDATE 07-13 09:48 — BRIEF SELF-RECOVERY.** Fixer emitted a coherent Fix Update (real work, NO compaction signal) after the rejection — loop transiently cleared without any restart. But recovery was PARTIAL/temporary (see next).

**UPDATE 07-13 11:05 — RELAPSED, re-open condition CONFIRMED MET.** Three compaction-only signals in a row, zero work product between: 10:53Z / 11:00Z / 11:05Z, plus fixer tooling re-confirmed the thrash. The 09:48 recovery regressed. This is the confirmed "compaction-only signals resume with zero work product" trigger. **Sharpened ask surfaced to operator ONCE (11:0x): true `/clear` or session-retire — NOT another `groups restart`** (bounce won't clear history-poison; operator already rejected that lever 09:23, correctly). Framed non-urgent: loop is INERT (no token burn), #12048 SAFE on GitHub. **What the loop blocks = the fixer's broader work queue** (other chains #12058/#12069/#12071 etc.), NOT the #12048 critical path right now — #12050's immediate gate is a red CI leg (test-windows-debug-cl-x86_64-gpu, likely FLAKE, all-else-green), which is a CI-health/maintainer requeue call, not the fixer's. The fixer's #12050 update-branch only matters AFTER CI greens. Operator owns the clear/retire decision; do NOT re-fire (escalate once). Act next on: operator applies lever, OR CI greens and update-branch is needed but fixer still stuck, OR a fixer chain hits a hard deadline.

**UPDATE 07-13 15:42 — LOOP CLEARED (fresh sessions healthy).** Post-11:05 relapse resolved on its own again. Verified `ncl sessions list ag-1780667166439-vmjrwe`: fixer is now minting FRESH per-thread sessions running healthily — sess-1783957374385-vywlqq (#12050, last_active 15:42, container running), plus #12083/#12084 chains active/running. The poisoned long-lived session that thrashed is no longer the active worker; new webhook-driven work spawns clean sessions. #12048 MERGED (see [[project_12048_enum_to_int_cast_infllight]]) — its fixer work is done regardless. **Loop side-thread CLOSED — no operator lever needed after all.** Pattern confirmed twice this incident: the autocompact loop is transient/self-clearing on this fixer; a container bounce (operator rejected) was correctly unnecessary, and the sharpened /clear ask became moot. Durable takeaway: on a fixer autocompact loop, DON'T rush a restart — flag once, hold (session is inert, no burn), verify current session state before escalating a lever; it often self-clears as new work mints fresh sessions. Re-open only if a NEW long-run session thrashes AND blocks a deadline-bound chain.
