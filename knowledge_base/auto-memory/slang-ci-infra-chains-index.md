---
name: slang-ci-infra-chains-index
description: "LIVE CI-infra chains spilled from MEMORY.md 2026-08-04 (4th spill): SPIR-V validation outage, #12145 queue-evicting flake, supervisor bounce-limb false positives. All operator/maintainer-gated. Each row keeps its RESUME trigger verbatim."
metadata: 
  node_type: memory
  type: index
  title: Slang CI-infrastructure live chains
  tags: 
    - slang
    - ci
    - infra
    - flake
    - live-chain
    - spillover
  originSessionId: a12dd085-6311-46eb-9247-cde76f71db62
---

# Slang CI-infrastructure live chains

Spilled from `MEMORY.md` on 2026-08-04 under the standing rule that **at the index floor the only
real lever is spillover, not deletion**. These are **LIVE** rows — each gated on an operator or
maintainer action, which is why they were movable rather than droppable.

⚠️**A row's presence here says nothing about its status.** Re-read the linked memo before acting;
a stale alarm outlives the thing it alarms about. The 🔴 rows below carry **retracted sub-claims** —
open the child before restating any of them.

## Rows relocated from MEMORY.md (2026-08-04, 4th spill)

- 🔴⭐⭐[**SPIR-V validation BROKEN wholesale — infra outage dressed as 866 shader bugs**](project_slangwin5_spirv_val_runner_defect.md) — ⛔**GREP KEY = `spirv-val` ALONE; the compact `spirv-val [0/866]` matches ZERO — emitted bytes have inner spaces (`[ 0 / 866 ]`), and believing the zero misclassifies infra as a code regression.** ESCALATED 08-04. ✅**08-05: FIRST SOLE-CAUSE eviction (#12252, merge-group `28b73d85`: 45 check-runs, only compile-regression + the check-ci aggregator red, both statuses green) ⇒ verified #12341 evictions = 3 (#12246/#12324/#12252), a VERIFIED FLOOR not a total.** ⭐⭐⭐**TALLY BY SIGNATURE, NEVER BY HOST** — a `SLANGWIN5` log grep gave 52 rows/11 PRs but 4 were `test-falcor` (#12145); two independent defects share that box. ⛔**"reruns futile" REFUTED (Main-re-derived 10:20Z, sibling-surfaced): `runs-on: [Windows,self-hosted,regression-test]` is a POOL** — same head `ba156ebf`, **att2 SLANGWIN5 `0/866` ❌ → att3 SLANGWIN4 `866/866` ✅** ⇒ a rerun is a ~2-in-3 draw and **a legitimate stopgap**; ⚠️**the cap 0/3 was justified BY "futile" ⇒ that justification is GONE — re-decide on merits, don't inherit.** ⛔⛔**MY "att1 ran 0 compile-regression jobs (0/1/1)" IS RETRACTED — I was wrong and the sibling was RIGHT.** Cause = **PAGINATION TRUNCATION**: `attempts/{n}/jobs` returns the **30-item default page** while `total_count` = **37**; compile-regression fell outside it. With `?per_page=100` ⇒ **n=1 on every attempt**. ✅**Evidence is a TRIPLE on ONE commit: att1 `91920971585` WIN5 ❌ · att2 `91937057380` WIN5 ❌ · att3 `91940624213` WIN4 ✅ = 2❌WIN5/1✅WIN4.** ⭐⭐⭐**`(.jobs|length)` is a PAGE SIZE, not a count — diff it against `.total_count` BEFORE any bound test; a returned 30/100 is the tell.** (Mirror of [[feedback_search_code_total_count_is_not_a_file_count]] — same two fields, opposite direction; I held one direction only.) ⭐⭐⭐**I overrode a peer's CORRECT claim with a defective instrument while invoking "verify, don't inherit" ⇒ when a fresh measurement CONTRADICTS a peer's, check your instrument BEFORE publishing — a contradiction says one of you is broken and does not say which.** ⛔**Also retracted: blaming the bad note on a sibling — I ran that test myself.** ⭐⭐⭐**I inferred a DISPATCH property from a DEFECT property — scope-of-fault ≠ scope-of-routing, and `runs-on` is the instrument for routing.** ✅eviction prediction DISCHARGED. ⛔⛔**open the child before restating ANY of it:** ❌"zero diagnostic text" **REFUTED** (1732 PASS paired 1:1 with 1732 FAIL; absent thing = **VALIDATOR ERROR BODY** — ⛔**which is ALSO non-discriminating on its own: the HEALTHY att3 log has those markers at 0 too. Measured signature = the SPLIT: non-validator modes `866/866` while validator modes `0/866`; PASS lines 1732 vs 3464**) · ✅**"runner-scoped" RE-ESTABLISHED — upgraded *unproven*→*supported by a same-head PAIRED CONTROL*** (att2 job `91937057380` WIN5 `0/866` ❌ → att3 job `91940624213` WIN4 `866/866` ✅, run `30885595493`, head `ba156ebf5c90`; **not** by the retention-limited survey, still worthless) ⇒ **reprovision/offline SLANGWIN5 is BACK ON THE TABLE**, alongside the re-grounded ask (restore validation + add a POSITIVE CONTROL so a dead validator ≠ mass shader failure). ✅tool-vs-host separation survives on an independent control. ⚠️⭐⭐**the CORRECTED discriminator is CONDITIONAL, not free-standing** — error-body counts are **0 in the HEALTHY att3 log too** (my control was mis-paired) ⇒ cite it only as *"GIVEN `spirv-val 0/N` with per-shader FAIL lines, absent error body ⇒ broken validator"*; ⛔**the "a real regression would NAME what was invalid" half is an UNVALIDATED COUNTERFACTUAL — we hold no genuine-mass-regression log.** ✅**healthy shape, measured + citable: `866/866` in ALL FOUR modes, `- PASS`=3464 (866×4), `- FAIL`=0** — the broken 1732/1732 = 866×2 ⇒ **both non-validator modes pass, both validator modes fail; that ratio beats any text-absence test.** Must fire WITHOUT opening: ⭐⭐**for any external-tool dependency, tool-ABSENT must be distinguishable from tool-REPORTS-FAILURE** (cf. DXC #12323) · ⛔**there is NO `spirv-val` binary** (in-process `glslang_validateSPIRV`) ⇒ **verify the gated artifact EXISTS before designing its gate** · ⭐⭐**check whether your remedy is the ONLY blocker before selling it as unblocking a victim** (#12246 had 2 banked causes) · ⭐⭐**re-verify a load-bearing ABSENCE with a DIFFERENT instrument.** 🔴**REAL in-tree defect, own PR:** `slang-glslang-compiler.cpp:359-371` + `slang-emit.cpp:3430-3438` conflate validator-absent with genuine-rejection ⇒ **a uniform `0/N` is indistinguishable from a mass regression BY CONSTRUCTION** (mechanism unestablished; attribution independent of it). ✅**08-04 own-PR case STRENGTHENED one-bad-path → REPO-WIDE DESIGN GAP: 2nd independent instance (E00028, 3 sites, message text carries NO discriminator) + the in-tree 2-STAGE REMEDY PRECEDENT to cite (`render-test -render-features`: bad NAME→loud `SLANG_FAIL`; absent DEVICE→`SLANG_E_NOT_AVAILABLE`→`Ignored`) ⇒ my PR needs NO novel design. ✅**SHA-transfer DISCHARGED** — reads were at my graft root `0864e60`, not #12322's head `ba156ebf5c90` (head object unfetched ⇒ local read at head was IMPOSSIBLE, not skipped); all **6** cited files byte-identical at both SHAs + `compare` bound test = exactly ONE differing path (the PR's own file) + non-identical control run ⇒ safe to cite at the head. Child §🔬. ⛔did NOT trace whether the sites can share an impl — don't claim a common fix is feasible.** ⭐⭐**A verification at the WRONG SHA transfers iff the cited PATHS are byte-identical — one `contents?ref=` per file converts "wrong commit" into usable corroboration; ⛔ancestry is UNANSWERABLE locally in a graft (`%P`=0 parents reports "not an ancestor" CONFIDENTLY — API said 2 parents, `0864e60` DIRECT).** [[feedback_shallow_clone_makes_your_head_the_graft_root]] §SALVAGE ⭐**OCC-5 (#12324) SELF-RESOLVED 13:05Z — queue re-dispatched unaided, landed SLANGWIN4, GREEN ⇒ requeue MOOT, not skipped** (consistent with the retracted futility claim: the pool self-heals ~2-in-3). **3 merge-queue evictions today: #12148 · #12246 · #12324.** ⭐**2nd SIGNATURE: nightly VKGLCTS `SLANG: failed to load slang.dll`, 11545/13792 fail vs 0/13792 the day before on a 10/10-day box** ⇒ unifying description = *a freshly built Slang binary can't resolve a DLL/symbol*, which fits the `validate()` symbol-lookup path. ✅⭐⭐**08-05 PARTIALLY EXPLAINED via #12364, and the WHOLE-SIGNATURE-IS-ONE-DEFECT reading is now WRONG: the VKGLCTS limb has its own SEPARATE, KNOWN latent cause — slang renamed `slang.dll`→`slang-compiler.dll` in `dcb47b716` (2025-10-31) and `nightly-slang-vkglcts-test.yml:96-99` copies ONLY `slang-compiler.dll`, never the forwarding shim (`source/slang/CMakeLists.txt:425`, `SLANG_ENABLE_SLANG_PROXY` default ON, added `e7e482ba4`/#11689). CTS 0.0.9 (VK-GL-CTS PR #17) loads the new name ⇒ 0 failures, so PR #12365 FIXES this limb.** ⛔**BUT THE ONSET IS STILL UNEXPLAINED AND THE RENAME IS NECESSARY-NOT-SUFFICIENT — Main-measured both nights: 08-03 `30796282680` PASS vs 08-04 `30889597224` FAIL have the SAME 0.0.7 artifact, the SAME 8 built DLLs (incl. `slang.dll`), the SAME 4-item copy list, and 0 vs 11,545 load failures.** The visible discriminator is the **test-server**: 08-03 shows 3 anchored `Z SLANG: ` lines, all `spawinAndWaitTestServerThread … succeded to launch server` (out-of-process, no in-process load attempt); 08-04 shows **23,090** (= 11,545×2) and **zero** test-server lines. ⛔**BUT THE `2×` IS *NOT* "2 `LoadLibraryA` attempts per test" — the triager asked me to carry that reading into this row and I refused it after source-checking: the 23,090 decomposes into TWO DIFFERENT messages at 11,545 each (`failed to load slang.dll` + `Failed to load SLANG DLL  InternalError (…vkPrograms.cpp:754)`), `uniq -c` gives 23,090 runs of length 1 (strict alternation, never a repeat), and 0.0.7-era source @`4899f2387afc` shows `SetupSlangDLL()` (:511) opens `if (!handle)` with a SINGLE `LoadLibraryA` (:529, logs :532) whose CALLER (:1033) logs again on the returned `SLANG_FAIL` (:1036) ⇒ ONE attempt, TWO log levels.** ⭐⭐⭐**An exact integer multiple feels like a mechanism because it is too clean for chance, but `2×` fits "twice the attempts" AND "one event logged at two levels" equally, and those are different defects — the arithmetic CANNOT discriminate; only decomposing counts BY MESSAGE and reading the call path can. Their phrasing upgraded it from "suggests" to "confirms", the wrong direction: the multiple is the WEAKEST part of the evidence.** ✅**What it DOES establish (still useful): the in-process path was taken and failed for all 11,545 GLSL-compiling tests — no partial success, no retry.** ✅**RECONCILED: their 220/17/5 vs my 69/7 post-rename was TWO QUESTIONS, not a miscount — MY filter carried an unjustified `< 2026-01-15` upper bound; theirs had none. Both re-reproduce exactly (85+99+36=220 / 10+1+6=17 vs 33+36=69 / 1+6=7).** ⭐⭐⭐**The near-miss rule fired correctly for once: when your recomputation lands NEAR a peer's, test that YOUR FILTER DIFFERS before calling their number wrong — one `select()` diff settled it in a single call, and "69 vs 220" was close enough in SHAPE (same direction, same conclusion) to feel like corroboration-with-a-correction.** ⚠️**A window you didn't justify is a claim you didn't make on purpose** — the question ("standing or latent?") wanted ALL post-rename nightlies; my cutoff silently answered a narrower one in the same units. ✅⛔⭐⭐⭐**GAP CLOSED — their "exhausted and negative" search (3 populations × 412 date boundaries ⇒ 0 matches) was STRUCTURALLY UNABLE to find my figure: measured on the cached 375-row population, ONE-SIDED cuts (space 750) yield 69/7 in **0** cases, TWO-SIDED pairs (space 70,125) yield it in **21**, INCLUDING my exact `("2025-10-31","2026-01-15")`. My query was two-sided; they enumerated single boundaries ⇒ the target was not missed, it was UNREPRESENTABLE — a DIMENSIONALITY gap, not a coverage gap (`2026-01-15` IS among their 375 dates).** ⇒ ⭐⭐⭐**"Exhausted and negative" is a claim about a SEARCH SPACE and is only as strong as that space's ability to REPRESENT the target — an exhaustive sweep of the wrong dimensionality returns a confident, well-quantified zero, and "412 boundaries × 3 populations" SOUNDS like overwhelming coverage because it is, of a space the answer doesn't live in.** Same family as my `GLSLSource` probe (null guaranteed by the schema) and their `.get()` near-miss: **a negative whose impossibility is invisible from the result.** ✅**Cheap check, needs no run: name the target's SHAPE and confirm ONE member of your space could produce it** ("could any single cut-point yield a bounded interval's count?" — no). ⭐⭐**They handled the negative RIGHT (reported unidentified, refused to invent an exculpatory mechanism for another tier's number) — the defect is that a well-conducted exhaustive search licensed "unidentified" when the correct verdict was "my instrument cannot express this."** ⚠️**MY share: I published "69/7 post-2025-10-31" and OMITTED the upper bound ⇒ a count without its predicate is not reproducible, and the reader reconstructs the predicate you DESCRIBED, not the one you RAN.** ⚠️**±1 REFINEMENT (theirs, numbers RIGHT / stated cause WRONG): my window gives 70/7 inclusive, 68/7 strict-both, 69/7 half-open. They credited "your notation was strict" — but strict-both is 68/7. Real mechanism: I compared a FULL ISO TIMESTAMP to a BARE DATE LITERAL as strings, and `"2025-10-31T07:06:05Z" > "2025-10-31"` is True while `"2026-01-15T07:09:15Z" < "2026-01-15"` is False ⇒ a predicate WRITTEN symmetric-and-strict EXECUTES as `[lo, hi)`.** ⭐⭐⭐**Mixing granularities makes the OPERATOR YOU WROTE differ from the INTERVAL YOU GOT, ±1 per endpoint — the size that reads as a rounding disagreement, not a bug ⇒ compare like-for-like; don't trust the operator to mean what it looks like.** ⭐⭐**They reproduced all four endpoint variants correctly and STILL mis-attributed the cause ⇒ agreement on OUTPUTS is not agreement on SEMANTICS** (deeper form of #12351's "reproducing a figure doesn't reveal which query produced it"). ✅Their practical point adopted: an inclusive re-derivation giving 70/7 is the endpoint convention, NOT a reopened gap. Command-keyed: [[command_iso_timestamp_vs_bare_date_compare]]. ⛔⛔⭐⭐⭐**MY OWN WORST ERROR OF THIS CHAIN, peer-measured and RETRACTED: I "corrected" the triager by publishing a 3-row table of ITS paths/sizes (`index.md` 1,808 B · `MEMORY.md` 10,964 B · 52 `legoop-*.md`) — EVERY FIGURE WAS MY OWN CONTAINER RELABELLED (theirs: 373 B · 2,027 B · 0 files), plus a byte count (39,570) NOBODY EVER MEASURED. Their nag's 37.8KB×1024=38,707 matches ONLY their `~/.claude/.../MEMORY.md` (38,929 B pre-edit) ⇒ the nag and the rule they cited ARE the same artifact; they were RIGHT to cite it and my "different file, different constraint" escape did not exist.** ⛔**AND THE CONCLUSION DIED WITH THE REAL NUMBERS: "unit is not bytes on two files" is FALSE — theirs sits 0.57% below BYTES, mine 3.60% below CODEPOINTS; my 1.0223-vs-1.0720 "corroboration" existed ONLY because the fabricated byte count produced it (real pair 1.0057 vs 1.0720 = 12× spread, no shared mechanism).** ⇒ ⭐⭐⭐**A second case assembled from data you did not measure is your FIRST case with someone else's name on it — and the wrong number was THE ONE THAT MADE THEM AGREE; fabrication converges on the answer you already hold, so the agreement is the tell.** ⇒ ⛔⭐⭐⭐**THE MOST DANGEROUS SHAPE A WRONG CORRECTION TAKES IS ONE THAT RELIEVES THE RECIPIENT OF A CHECK THEY WERE RIGHT TO RUN** — I am the admin tier, it arrived with a figure table in the "resolving your open caveat" slot, and had they deferred a real bound would have been dismissed. ⇒ **`/workspace/agent/` is PRIVATE PER COWORKER (my own CLAUDE.md says so verbatim) ⇒ a path-keyed claim about a peer is unverifiable from here BY CONSTRUCTION: quote a figure THEY reported or write "unmeasured from here".** ✅Surviving: their refusal correct on AUTHORIZATION-not-size (130 writers); `cp`-destroys-one stands. ⛔⛔**AND THE "MY FILE ONLY, ONE CASE" UNITS REMNANT IS RETRACTED TOO (2nd retraction, later same day) — the METHOD is void, not just the scope: a nag figure is computed at SESSION START, I measured bytes at the END, and `MEMORY.md` grew 95,814→97,670→101,210→102,819 B across four samples IN ONE SESSION from sibling writers ⇒ the residual swings −8,406 to −1,401 purely on WHICH STATE you pair with, so my "3.60% below codepoints" measured SIBLING WRITE VOLUME, not encoding. A reported size and a byte count are comparable ONLY if sampled at the SAME FILE STATE — unobtainable on a file I don't exclusively write. Quote no ratio, no percentage, no unit verdict.**  ⇒ ⭐⭐⭐**PARTIAL RETRACTION IS THE DANGEROUS KIND: I retracted "two files/two agents" and KEPT "one file" — trimming scope FELT like conservatism and preserved the defective instrument, which then failed for a SECOND independent reason. When a conclusion falls, RE-DERIVE the remainder from scratch; never subtract the refuted part and ship the rest.** (Counterpart to the root index's over-retraction rule — that warns against cutting too much; this is the commoner error: cutting exactly enough to LOOK responsive while the mechanism survives.) ✅**Their multibyte retraction is CORRECT on their own numbers — rounding envelope checked: a 1-decimal KB figure carries ±51 units, so their "222 B gap" is really [171,273], which cannot reach the ~821 codepoint delta.** ⚠️**But their replacement inherits my defect: "222 B — real, not rounding" is stated EXACTLY while carrying ±51 AND pairing a nag with a byte count from a different edit state (they compacted −1,311 B mid-window) ⇒ a small confident residual is what a false same-instant assumption PRODUCES.** ✅⚠️**3rd PASS — the edit-state fix INVERTED their retraction and "not bytes" is BACK for their file: the hook fires on `PostToolUse` of THEIR OWN Edit ⇒ same-state BY CONSTRUCTION, the pairing neither of us had. Pairing 40.1 KB=41,062 u @ 41,874 B ⇒ gap 812 vs ±51 tolerance = 16×, matching their multibyte delta (867/858) within 46 ⇒ REAL, and independent of anything I supplied.** ⛔⛔⭐⭐⭐**MY "FABRICATED 39,570 / LAUNDERED FIGURE" CHARGE IS RETRACTED — IT WAS FALSE AND IT WAS MINE. `39,570 B` was THEIR measurement (file immediately AFTER their lifeboat Edit; 38,929 B was the same file BEFORE — the 641 B delta is the #12364 row they'd just added), and MY OWN CONTEMPORANEOUS NOTE quotes 39,570 verbatim from their message. I had the receipt in my own store and never opened it.** ⛔**Also retracted: my premise "a nag is computed once, at session start" — they got 37.8/40.1/41.9/42.6 KB in ONE session ⇒ RECOMPUTED PER FIRING, which is precisely what makes two byte counts for "one nag" possible; the same-instant CAUTION was right, the SAMPLING MODEL I attached to it was invented, and the model is what generated the false conclusion.** ⇒ ⭐⭐⭐**A SELF-ACCUSATION IS A CLAIM AND GETS LESS SCRUTINY THAN AN ACCUSATION OF SOMEONE ELSE, BECAUSE CONFESSING READS AS DILIGENCE** — and mine metastasized into a charge against an agent who had measured correctly, costing them a round defending a good number. ⇒ ⭐⭐**38,929-vs-39,570 "for the same nag" LOOKED like a contradiction, so I reached for the explanation that indicted the NUMBER instead of the one that indicted MY MODEL OF WHEN IT WAS SAMPLED — the very edit-state insight I had just handed them, applied to their figures but not to my reading of them.** ✅**Their verdict now rests on THREE exact same-state pairs, none mine: 40.1@41,874 (+812) · 41.9@43,778 (+872) · 42.6@44,553 (+931); bytes/1024 misses every time (42.8/42.8/43.5 vs 41.9/41.9/42.6).** ✅**The generalized rule SURVIVES on its own merits (a retraction doesn't recall a number from downstream reasoning — say "do not use this number") but was DERIVED FROM A DEFECT THAT DIDN'T EXIST.** ✅**Real defects remain real: the 3 `/workspace/agent/` figures WERE my container's presented as theirs.** ⭐⭐**Their estimator sharpened 46→42→3 units toward codepoints and they STILL refused to name the unit (cp-vs-utf16 gap of 9 is 5.7× under the ±51 step ⇒ no further pairing can separate them) — KNOWING THAT MORE DATA CANNOT RESOLVE A QUANTITY IS WORTH MORE THAN THE DATA.** ⛔**Unit NAME still undetermined and may stay so (their reasoning, the sharpest of the exchange): codepoints 41,007 vs utf16 41,016 differ by 9 units = 5.7× SMALLER than the 1-decimal reporting step, and their pair sits ON the 40.05 rounding boundary where those 9 flip the digit ⇒ ⭐⭐PRECISION ON BOTH SIDES SAYS NOTHING ABOUT RESOLVING POWER when the quantity is smaller than the reporting step.** ⛔⭐⭐**AND I COMMITTED THEIR ORPHANED-BLOCK DEFECT IN MY OWN FILE: my anchored replace rewrote a stale section's OPENING and left the retracted "one case, one file" claim asserted OUTSIDE the `<details>` wrapper (line 564 vs wrapper @569) ⇒ AN ANCHOR MATCHING THE START OF A STALE BLOCK DOES NOT REMOVE THE BLOCK — read the whole edited region and grep retracted strings to 0; struck it, and verified the 2 remaining hits are QUOTED-AS-REFUTED, not asserted.** Rule: [[feedback_never_state_a_peers_filesystem_figure_as_measured]] ⭐**NEW CONTROL neither of us ran: pre-rename (`<2025-10-31`) = 132 success / 1 failure / 0 cancelled vs post = 220/17/5 ⇒ failure rate ~0.8%→~7%, real but MODEST, still 93% passing ⇒ independently supports "latent defect, occasionally triggered" over "the rename broke the nightly"** (the reading their retracted "11,545 per nightly" implied). ⚠️**Do NOT read all 17 post-rename failures as this signature — 2025-11 logs are HTTP 410 EXPIRED, returning `loadfail=0 spawn=0`, a control that fails silently in the CONVENIENT direction; onset stays UNEXPLAINED.** Predecessor id **88963700** verified `state:deleted`, `path:vk-gl-cts-nightly.yml`, created 2024-03-08. ⭐⭐**Frequency-lesson twin, one message apart and axes swapped: they attached an unearned RATE ("11,545 per nightly" — measured 35/37 nightlies SUCCEED, so the mismatch is LATENT not standing) to a verified mechanism; here an unearned MECHANISM to a verified count ⇒ a verified number lends credibility to whatever story is attached to it.** No build-config change exists in the window (`compare 53b76e6d30...0864e60e63` = 7 commits/97 files; only `cmake-options-build*.yml`) ⇒ **runner state (persistent workspace / leftover pre-rename `slang.dll` / test-server spawn failing) is the live candidate.** ⇒ ⭐⭐**"0.0.9 fixes it" is safe; "0.0.7 + this workflow always fails" would predict an 08-03 failure that DIDN'T HAPPEN — a necessary-condition story told in sufficient-condition grammar reads as root-caused and STOPS the search for the trigger.** ⛔⭐⭐⭐**GRAFT TRAP en route: local `git log -S SLANG_ENABLE_SLANG_PROXY` fingered `0864e60e6` — the exact head of the first failing nightly, i.e. the commit I already suspected. FALSE: the clone is shallow (`.git/shallow`, `rev-list --count HEAD`=8) and `0864e60` is the PARENTLESS GRAFT ROOT, so every file reads as all-`+` and `-S` "finds" any token in it; API says `0864e60` touches 8 files, NONE of them `CMakeLists.txt`.** ⇒ **In a shallow clone EVERY pickaxe/blame/diff result at the graft root is an artifact, and it arrives looking like causation — cross-check `commits/<sha> --jq .files` before believing local history.** [[project_12364_cts_storage_image_minnonuniform]] ⚠️**VS 17.14.19→18.8.2 upgrade is a HYPOTHESIS ONLY** — compile-regression sets up **no VS at all** (0 `vcvarsall`/`VSCMD` hits in green AND red logs) ⇒ correlation is host-and-time only; needs on-box confirmation that VS18 replaced the MSVC runtime `slang-glslang.dll` links against. ⚠️**my own cross-tab found only 4 compile-regression jobs in 100 runs (0 SLANGWIN5) — a SAMPLING artifact, NOT a refutation**: a smaller sample disagreeing with a larger one is uninformative until you show the windows overlap. ✅✅**FILED 15:06Z — durable artifacts at last: [#12341](https://github.com/shader-slang/slang/issues/12341) infra (depool) + [#12342](https://github.com/shader-slang/slang/issues/12342) in-tree (`validate()` conflation)**, both open, cross-linked. ⛔**COST CORRECTED 3→2 evictions** (#12246, #12324): #12148's only real failure was `test-falcor`, which fails on **SLANGWIN4 too** ⇒ tracked #12145, host-independent ⇒ ⭐⭐**"failed on a run that used the box" ≠ "failed because of the box"** — I accepted co-occurrence as causation because the box was already the suspect; publishing a 50%-overstated figure would have given a maintainer grounds to distrust the solid parts. ✅**occurrence 6 LIVE 14:50:36Z** (job `92023450909`) ⇒ inference caveat discharged. ⭐**killer control: #12322 head `ba156ebf`, 3 attempts, job only DOWNLOADS a prebuilt artifact ⇒ same binaries all 3 times** — SLANGWIN5 0/866 ×2, SLANGWIN4 866/866 ⇒ refutes "the artifact lacks slang-glslang". Cross-tab **SLANGWIN5 0/6 · WIN4 6/0 · WIN10X64-1 3/0**. ⭐**`gh search` returns FALSE ZEROES (indexing lag) — enumerate+grep before filing.** ⚠️path is `source/compiler-core/slang-glslang-compiler.cpp` (NOT `source/slang/`, 404). 🔴🔴**15:1xZ — THE PUBLISHED #12341 PROSE REPUBLISHES "zero validator diagnostic text", REFUTED ~7h EARLIER and sitting in this very child's FRONTMATTER.** Main-measured all 3 logs: att1 **1732 `- FAIL`** · att2 **1732** · att3(healthy) **0**; error-body markers **0 in BOTH poles** ⇒ no silence, and the fallback discriminates nothing. Next sentence ("866 invalid shaders *would* emit 866 messages") is the UNVALIDATED COUNTERFACTUAL. ⇒ ⭐⭐⭐**A CORRECTION THAT LIVES IN THE CHAIN DOES NOT REACH THE ARTIFACT THE CHAIN PRODUCES — not a timing race (7h gap, author held the retraction): narrative supplied the phrasing, the retraction was a later paragraph nobody re-read while drafting.** ⇒ **EXTEND the position sweep: headings → frontmatter → tables → index rows → prose → ⭐PUBLISHED ARTIFACTS DERIVED FROM THE NOTE.** ⭐⭐**A refuted detail inside a correct report damages the correct part** — a maintainer greps the linked log, finds 1732 FAIL lines, discounts a now-well-controlled attribution; same asymmetry the sibling named for 3-vs-2, recurring in the SAME document via a different clause ⇒ **catching one instance did not generalize the sweep.** ✅**Fix needs NO new work — the SPLIT is already in #12341's own code block; only the gloss is wrong.** ✅**NEW independent control (mine): 3 runners / 1 job / 22min — `92021839632` WIN4 ✅14:18 · `92022571008` WIN10X64-1 ✅14:29 · `92023450909` WIN5 ❌14:40**, `per_page=100`, **0 bound violations across 100 runs** ⇒ runner-scoping now on TWO independent controls. ⚠️**INSTRUMENT: `actions/workflows/304423275/runs` = `total_count=0` ALL-TIME because `ci-slang-regression-test.yml` is `on: workflow_call` ONLY — jobs live under the CALLER's run** (non-zero control: `88428719` → 13089) ⇒ **the morning's "0 rows from 400 runs" WAS THIS**; a zero from a `workflow_call` workflow's runs endpoint is a CATEGORY ERROR, not evidence — runs-axis sibling of [[technique_grep_in_repo_a_misses_reusable_workflow_in_repo_b]]. ⛔⭐⭐⭐**MY CONTROL `88428719` WAS STALE — babysitter-caught, Main-verified 15:5xZ: its newest run is `2026-06-17T04:26Z`, SEVEN WEEKS OLD (`total=13088`, name "Compile Regression-Test"). Today's `test-compile-regression` runs under `ci.yml` = `76941487` (verified: occ6 run `30914831831` → `workflow_id=76941487`, `path=.github/workflows/ci.yml`).** ⇒ **NEITHER id I paired was where the job lives**: `304423275` is STRUCTURALLY incapable of runs (`on: workflow_call`), `88428719` is RETIRED. ⭐⭐⭐**A NON-ZERO CONTROL PROVES THE ENDPOINT RESPONDS; IT DOES NOT PROVE THE OBJECT IS CURRENT — check `created_at` on the newest run before treating any workflow id as live.** A new axis on the control rule: I had been treating "returns data" as "is the right object", so my control was *valid and irrelevant*. ✅**Correct live enumeration (Main-run, bound-tested): `workflows/76941487/runs?created=>…` — 2 runs since occ6, exactly ONE compile-regression job: `15:25:48Z SLANGWIN10X64-1 success`; non-zero control = same probe over occ6's run returns `14:40:15Z SLANGWIN5 failure`.** ⇒ ⭐⭐**NO SLANGWIN5 DISPATCH SINCE OCC6 (14:50Z) ⇒ the defect is NEITHER persisting NOR cleared — it is an ABSENCE OF DISPATCH, not an absence of defect.** ⛔**I framed the depool ask as "your call" to the OPERATOR via the babysitter — WRONG TIER: depooling needs runner-admin rights on the self-hosted registration, which NO agent in this chain holds; #12341 IS the terminal action available to us.** ⭐**Their throughput argument to add for whoever acts: `runs-on` is a POOL, so depooling costs NO throughput (≥2 boxes absorb it) and converts a ~1-in-3 poisoned dispatch to 0 — the cheap case for acting BEFORE the VS-18 answer.** ⭐⭐**Also theirs: a grep-phrase FALSE NEGATIVE in the durability check — a hard-wrapped phrase straddling a newline returns 0 from line-oriented grep ⇒ VERIFY WITH A SINGLE DISTINCTIVE TOKEN, never a multi-word phrase; a 0 means absent OR unmatchable.** ⭐⭐⭐**"A false-negative instrument used to check for tampering MANUFACTURES phantom tampering — most convincing right after a real clobber has primed you to expect one."** ⚠️**And they correctly SCOPED my INDEX finding: it holds for the SHARED, multi-writer, machine-normalized index — NOT for single-writer hand-maintained `MEMORY.md`, where index hooks stay correct and recall depends on them.** Correction dispatched to `slang-ci-babysitter` (closest-to-the-state) on `gh-issue-shader-slang/slang-12341`. ⛔⛔**15:2xZ — THAT CORRECTION WAS ITSELF PARTLY WRONG; MY "REPUBLISHES A REFUTED CLAIM" VERDICT IS WITHDRAWN.** ✅**"zero validator DIAGNOSTIC text" is TRUE** — I refuted a **STRAWMAN** (read it as "any per-shader text"; a `- FAIL` token is a **HARNESS verdict**, not a validator diagnostic). Only **"plus SILENCE"** + the **"866 invalid shaders WOULD emit 866 messages"** counterfactual were defective. ⭐⭐⭐**2 defects, both in a CORRECTION I sent while the recipient was ALREADY EDITING A PUBLIC ISSUE: (1) refuted the WEAKEST reading of a phrase — enumerate readings, test the STRONGEST; (2) generalized FOUR 0/0 markers into a dead marker CLASS — the full ladder found `error` 1-vs-0 and `SPIR-V` 2-vs-0 asymmetric ⇒ LADDER EVERY HIT, NOT JUST EVERY ZERO (my own rule, unrun on my own correction).** ⇒ ⭐⭐⭐**a correction to a PUBLISHED artifact needs the FULL ladder BEFORE sending — the recipient acts concurrently with your reasoning; and errors cluster in corrections (authority high, scrutiny low) fired on me WHILE I was invoking that very rule.** ✅**Strongest measured form (Main-verified att1): two `following shaders failed … Val:` blocks at lines 5535/6402, each 866 lines / 866 bare-filename shape / 866 DISTINCT, 866+866=1732 = exact FAIL count, ZERO blocks healthy** — 866 shaders named, not one diagnostic attached. ✅**Babysitter's reframe beats mine: TWO result lines per shader per mode ⇒ BOTH poles have the SAME 3464 total** (broken PASS+FAIL 1732/1732; healthy PASS+PASS 3464/0) — same lines, half flipped. ⛔**"`- FAIL` comes from `compile_all_slang.sh`" is UNVERIFIABLE — script NOT in tree** (copied from `C:\slang_compile_test_suite_a`, `ci-slang-regression-test.yml:39-42`); they correctly refused to publish it ⇒ **a mechanism claim smuggled into a measurement correction, caught by the recipient.** ✅**Live body Main-verified** (not from their summary): `plus silence`/`zero-of-all`/`would emit 866`/`silence` **0** raw+collapse-squeeze, non-zero control 122 lines; `not one validator message`/`866 + 866`/`does not assert what such a log` **1** each; disclaimer last line; **all 6 job-log links resolve to the claimed runner+conclusion.** ✅**They characterized MY control better than I did: all 3 are `attempt=1`, `event=pull_request`, THREE DIFFERENT branches/heads** (`9702b6dc85`/`c514c1160f`/`f07e487123`) ⇒ **complementary controls — #12322 triple holds CODE constant & varies runner; mine holds a 22-MIN WINDOW constant & varies runner+code.** ⭐⭐**REPRODUCING SOMEONE'S NUMBERS DOES NOT VALIDATE THEIR INFERENCE FROM THOSE NUMBERS** (their line; my counts reproduced exactly, my reading did not). ⭐⭐**RERUN AFFINITY (occ 6/7/8 on #12125): rerunning the SAME RUN tends back to the box that just failed it — **4 of 7 draws ESCAPED (3 redrew), on the population *prior attempt drew SLANGWIN5*; every-attempt≥2 = 8-of-12; peer's reruns-it-fired = 2-of-2** ⇒ ⭐⭐**a ratio needs its SELECTION RULE, not just `n=` — irreproducibility from a peer's ledger is the detector** (Main-verified att-level runners; #12322 att3 DID move to WIN4 and pass ⇒ **a TENDENCY, not a rule — both facts travel together**). ⛔**Mechanism REFUTED, not unproven** (author-retracted 17:0xZ, Main-reproduced at SECOND resolution): fail-fast head start **≈16s** (WIN5 fail mean **10.36**min n=5 vs WIN4 pass **10.63**) vs idle gap between attempts **1.5**min (#12125 16:27:09→16:28:39) / **5.2**min (#12322 08:42:02→08:47:16) ⇒ **6–20× larger, so a 16s earlier finish cannot bias assignment. Affinity REAL, CAUSE UNKNOWN — do not substitute a new just-so mechanism** (this one was plausible, quantitative and wrong). ⛔⭐⭐⭐**MY defect was the PARKING verdict: I wrote "too coarse to confirm — `floor`-minute timestamps," but `started_at`/`completed_at` carry SECONDS (8× the needed resolution) ⇒ a 30-second subtraction would have refuted it an hour earlier.** ⭐⭐⭐**"too coarse to measure" is a CLAIM ABOUT AN INSTRUMENT and needs the same verification as a claim about the world — print the two raw values before writing "cannot be resolved."** ⭐⭐**An unfalsifiability verdict is the ONLY verdict that ENDS inquiry while reading as rigour** — it looks like epistemic caution and functions as a stop. ✅**Shared learning `1785861722411-rerunning-…` AMENDED by me 17:0xZ** (author cannot write `/workspace/shared/`): top banner carries the denominator table (4 populations) + this retraction; *Hypothesis* heading now reads ❌RETRACTED. ⭐⭐**The practical rule ("cap at 2") SURVIVED both corrections untouched because it was derived from the DISTRIBUTION, not the mechanism — a conclusion resting on a measurement outlives the story attached to it.** ✅**Rule: cap reruns at 2/run, then prefer a FRESH DISPATCH** — fresh dispatches were near-uniform across the 3-box pool, same-run reruns were not ⇒ ⭐**scope-of-routing differs between "rerun this run" and "dispatch a new run"**, the 3rd time a dispatch property proved independent of the fault property. ⛔**babysitter metric correction: 87/150 (58%) rows logged `action:"rerun"` were DECLINES** ⇒ its rerun counts (which I RELAYED) were inflated ~2.4× and **refused buckets outranked acted-on ones**; corrected 7d = **40 true reruns / 28 PRs**; caps never breached (tracker was right at 0 — only the log's reporting field was wrong) ⇒ ⭐⭐**when one counter feeds BOTH enforcement and reporting, check them against each other; here they disagreed and nothing noticed.** Falcor ~25% of real reruns = top target; 12/40 still untagged. ✅✅✅**RESOLVED 2026-08-05 BY MAINTAINER `jkwak-work` — ROOT CAUSE: he upgraded the VulkanSDK on SLANGWIN5 and DELETED the old version, but never restarted the GitHub runner SERVICE ⇒ its CACHED `PATH` still pointed at a deleted directory. FIX = RESTART THE RUNNER SERVICE (free).** Verified green by me on **SLANGWIN5** job `92450719106` / run `31020032307` (08-05 21:28→21:39Z): `866/866` in **ALL FOUR** modes, `- PASS` **3464**, `- FAIL` **0**, `following shaders failed` blocks **0** ⇒ matches the healthy baseline exactly, and is **the positive control on the bad box we never had**. ⛔⛔⭐⭐⭐**MY "this is explicitly NOT a reboot request" (a whole section of #12341) IS REFUTED — THE FIX WAS A RESTART.** I inferred a **REMEDY** property from a **DEFECT** property: `test-benchmark` green on WIN5 74s before the failure proves *the machine is up*, and says **NOTHING** about a long-lived service holding a **stale cached environment**. A restart doesn't repair the box — it **re-reads `PATH`**. ⚠️**Every job on that box inherited the same stale `PATH`, so a passing sibling job is FULLY CONSISTENT with the fault ⇒ NEITHER of my two controls could see this axis** (same-job/other-runner rules out code; other-job/same-host rules out the machine; *stale inherited env* is invisible to both because it is uniform across the host and only bites where a dependency was removed). ⭐⭐⭐**SECOND remedy-from-defect error on THIS chain, ~8h apart, and I had already written the lesson for the first** ("reruns futile": scope-of-fault→scope-of-routing; now box-health→process-environment) ⇒ **A REMEDY CLAIM NEEDS ITS OWN INSTRUMENT; measuring the fault tells you nothing about what fixes it.** ⭐⭐⭐**THE HEDGING CORRELATION — the sharpest thing in the whole chain: VS-17→18 was WRONG about the change (real: VulkanSDK) but LABELLED a hypothesis with a stated reason it wasn't proven ⇒ COST ZERO, and it routed the work to the one human who could answer. "Not a reboot" was stated as FACT, was wrong, and SHAPED THE ASK. Cost tracks the LABEL, not the confidence you felt.** ⭐⭐**Observability bias: TWO toolchain changes landed in that window and we latched onto the one our logs happened to PRINT (`VSCMD_VER`); nothing readable mentioned Vulkan ⇒ ask what changes would be INVISIBLE to your instruments before ranking the visible one.** ⛔**ASK OVERSCOPED — "depool or reprovision" when a FREE service restart sufficed; I also relayed the act-before-root-cause argument, internally sound (a pool absorbs the loss) but it advocated the HEAVY remedy while the cheap one was unknown ⇒ when the cause is unknown, ask for INVESTIGATION or the cheapest reversible action, never a named heavy intervention. #12341's real value was the EVIDENCE PACKAGE that got a human onto the box in a day — not its recommendation.** ⛔⛔⭐⭐⭐**AND I RETRACTED MY OWN "TWO SIGNATURES, ONE DESCRIPTION" SUCCESS BULLET MINUTES AFTER PUBLISHING IT AS THE THING THAT WORKED — THE TWO LIMBS HAVE TWO DIFFERENT CAUSES.** The VKGLCTS limb is the `slang.dll`→`slang-compiler.dll` rename `dcb47b716`, **dated 2025-10-31 — NINE MONTHS before the 08-04 onset** (Main-verified via the commits API) ⇒ **it was NEVER evidence for the SLANGWIN5 PATH fault**; two faults coincided on one box on one day. ⭐⭐⭐**I treated a description's COVERAGE as evidence of common cause, but coverage is what any sufficiently ABSTRACT description always has — "a freshly built binary can't resolve a DLL/symbol" fits ANY dynamic-linking failure on ANY host, so it was GUARANTEED to cover both limbs whether or not they shared a cause. The test for common cause is COUPLING IN TIME AND MECHANISM, and one date lookup refutes it.** ⚠️⭐⭐**I also praised the WRONG PROPERTY: I wrote it "survived even though every mechanism guess under it was wrong" — a claim that survives the refutation of every mechanism beneath it is not ROBUST, it is UNFALSIFIABLE at the level stated.** ⛔⭐⭐**And note WHERE that defect sat: in a SUCCESS BULLET of my own lessons-learned write-up, published while I was cataloguing my own errors ⇒ writing an accurate self-critique is NOT evidence every sentence in it is accurate; success bullets need the same instrument as failure bullets and get LESS scrutiny because they flatter nobody.** 🛑**Stop-work sent to the babysitter before they could post it to GitHub.** ✅**#12342 (`validate()` conflation) STANDS INDEPENDENTLY — it is why `0/N` was indistinguishable from a mass regression, i.e. why this cost a day rather than an hour.**
- ⛔⭐⭐⭐[**#12351 "agentic-tests NEVER passed (≥36 nights)" — FALSE, and the FLOOR INVERTS**](project_12351_agentic_tests_streak_bounded_regression.md) — Main-measured before routing 08-04. Bot's bound test was **CORRECT and IRRELEVANT**: `workflows/304423282/runs` `total_count 36 == 36 returned`, `status=success` → **0** … but a **RENAME MINTS A NEW WORKFLOW ID and retires the old one to `state:deleted`, ABSENT from the `actions/workflows` listing** (82 listed, `select(.path|test("agentic"))` → `[]`). Query by **FILENAME**: `workflows/ci-agentic-tests-nightly.yml/runs` → id `287019999`, **16 success / 17 failure**, newest = **SUCCESS 2026-06-29**, the night before rename `cf5d225f8c` (#11828). ⇒ **16 passes exist; the streak is EXACTLY 36 with a KNOWN BOUNDARY** (last pass run `28350804872` head `3a84a12b8e` → first fail `28422435803` head `80bf926b57`, **15 commits / 98 files**). ⭐⭐⭐**A per-id population being COMPLETE does not make it the RIGHT population — pair every `total_count`-equals-returned check with "does this id span the window I'm claiming?"** (⚠️**the tell: workflow `created_at` within SECONDS of the file's only commit, which is a rename** ⇒ `previous_filename`, transitively). ⭐⭐⭐**The `≥N` FLOOR HEDGED THE WRONG WAY — "possibly older, cannot tell" READ as caution but DISCARDED the bisect boundary; a floor is conservative only if the uncertainty axis is the one you think.** Direct sibling of the `88428719`-stale-control lesson in the #12341 row above (there: valid-but-irrelevant CONTROL; here: valid-but-irrelevant BOUND TEST) ⇒ **check `state` and `created_at`, not just that the endpoint answers.** ✅Rename measured **NON-CAUSAL** (patch = `name:` + 1 comment line; no `uses:`/`-test-dir`/path). ⚠️**Don't overcorrect to "green before the rename" — predecessor FLAPPED 16/17 with an 8-night streak (06-16→06-23); honest form = *always flapped, then stopped flapping into green at all*, 36 ≈ 4.5× worst prior.** ⚠️**07-09 reconciliation #12017 did NOT restore green — `+24/−0` ⇒ it CREATED the whole current 24-entry suppression set, and still 25 fail/1 cancelled/0 success after** ⇒ a prior fix already failed to hold; say so or a maintainer asks why this one works. ⛔**my "26 fail" was OFF BY ONE: I filtered `created_at>"2026-07-09"` but the merge was `09:03:26Z`, so I counted that night's `05:22:16Z` run `28996071406` from BEFORE the fix** ⇒ ⭐⭐**a DATE-ONLY boundary silently swallows pre-event rows when the event has a TIME; a same-day run sits on the wrong side of a mid-day merge.** ⛔**ATTRIBUTION RETRACTED — I filed this as a defect I CAUGHT IN THEIR ARTIFACT; it was only ever in MY intermediate.** They filtered on a full timestamp from the start, so the body always read 25+1 (Main-reconciled BOTH boundaries: date-only n=27/26 fail = my figure; `>09:20:23Z` n=26/25 fail+1 cancelled = theirs). ⭐⭐⭐**Reproducing a published figure does not reveal which QUERY produced it — when your recomputation lands ONE OFF a peer's, test that YOUR filter differs BEFORE concluding their number is wrong; a near-miss feels like corroboration-with-a-correction and is usually two different questions.** ⚠️**Symmetric, per their own admission: their instinct on seeing my 26 was also "one of us miscounted", not "we asked different questions" ⇒ this is a DEFAULT of the reading, not a lapse of one agent.** ✅**CLOSED 22:39Z — 4-round correction chain, every round net-positive, artifact never wrong in public.** Final catch was MINE and lands on BOTH: the `16 pass/17 fail` headline is **ALL-EVENTS** (33 = 28 `schedule` + 5 `workflow_dispatch`, dispatches clustered **06-02×2 / 06-04×3** — ⛔**I first wrote ×3/×4, which were the DAY TOTALS (3 and 4 runs incl. 1 scheduled each), not the dispatch counts; babysitter-caught, Main-verified `{date,total,schedule,dispatch}` per day.** ⭐⭐⭐**I mislabeled a breakdown inside the very row warning that a count means nothing without its denominator — the composite figure was RIGHT (5 total) so the error survived a sum check; only a PER-CLASS breakdown catches it** ⇒ when you split a total by class, print the classes AND the total, never one inferred from the other) while the 36 it's ratio'd against is **SCHEDULE-ONLY** ⇒ different denominators; schedule-only predecessor = **14/14**. Their zero-cancelled precondition was **necessary-not-sufficient** (it aligns the STREAK metrics, says nothing about event type). ✅Streak survives: 06-16→06-23 = **8 DISTINCT SCHEDULED nights**, and **0 days carry >1 scheduled run** in either population (Main-verified — that check is what licenses "28 scheduled nights" as nights rather than runs) ⇒ ratio holds under either counting. Line-8 prose now states both denominators inline. ⭐⭐**Across a rename, check `event` on BOTH sides before quoting counts — "runs" and "nights" diverge exactly where manual dispatches cluster, and a per-night claim built from an all-events count is a different metric in the same units.** ⭐⭐⭐**Both of my structural defects this session were in CHECKS, not claims** (`grep -oic` after `tr` = a probe that cannot fail; the date-only filter) ⇒ **a probe that cannot fail is indistinguishable from a probe that passed** — and I ran mine while auditing a peer, the diligence slot where scrutiny drops. ⛔⭐⭐⭐**BUT I THEN OVER-GENERALIZED IT TO "every defect either of us produced across 5 rounds was in an INSTRUMENT; not one was in a substantive claim" — FALSIFIED BY ROUND 1, babysitter-caught.** Round 1's defects were **claims**: "has never completed successfully in retained run history" (false, and it was the TITLE), "the Actions API cannot distinguish created-then from purged" (false belief ABOUT the tool), "195 lines" (correct measurement, wrong LABEL). ✅Verified from the ORIGINAL body in my own webhook payload — I did not need their saved copy, and **checking my own inbound rather than accepting a peer's quote is the move that made the correction independent.** ⭐⭐⭐**The tally was a COMPOSITE THAT SUMS ONLY IF YOU DROP THE ROUND IT WAS DERIVED FROM — the identical shape as my ×3/×4 split and as the `≥36` floor, three times in one session: a figure that reads as the careful version because it is CLEAN, and is clean because a class was silently excluded.** ⇒ ⭐⭐⭐**Before publishing any claim about a defect POPULATION, ENUMERATE THE ROUNDS/CASES AND ASK WHICH ONE YOU LEFT OUT — the excluded case is usually the one that generated the frame.** ✅**Their reframe is the keeper, and it INVERTS the pessimistic reading: defects were in CLAIMS before mutual audit and migrated to INSTRUMENTS after it ⇒ that is EVIDENCE THE ARRANGEMENT WORKS on the claim layer, not a property of how we err.** Corrected tally = **1 claim-round + 4 instrument-rounds**. Surviving narrow form: **mutual auditing catches the other party's CLAIMS and reliably fails to catch your own INSTRUMENTS.** ⛔⛔**08-05 — I MADE THE SAME EXCLUSION A THIRD TIME AND THE PEER FALSIFIED IT AGAIN.** I closed with *"3 instrument defects, none in a substantive claim"* — which **excluded a claim-layer defect I HAD REPORTED IN MY OWN PREVIOUS MESSAGE**: my published shared learning asserted **as an absolute** that a renamed workflow's old id *is* `state: deleted` + unlisted. The query was fine; the **claim about how the tool behaves** was false, it was PUBLISHED, and it would have branded a sound non-active-workflow control **inert** ⇒ **the most consequential defect of the whole chain, and it sat outside my count.** ⇒ ⭐⭐⭐**FOUR instances in two days of ONE genre — DEFECT-POPULATION CLAIMS ("none of them were X", "5 of 5", "≥36", "3 instrument defects") — and the tell is ALWAYS THE SAME: the frame was DERIVED FROM the member it excludes.** ⛔⭐⭐⭐**STANDING RULE, their proposal, adopted: STOP MAKING "none of them were X" CLAIMS ABOUT MY OWN ERROR SET.** Auditing such a tally after the fact has never once worked — 3 for 3 it was falsified by a peer, twice using evidence I had myself supplied one message earlier. ⭐⭐**A self-exculpating tally is the one claim I cannot audit from inside, because the frame and the excluded member are chosen by the same pass.** ✅Their 2 checks Main-verified independently: the 3 tests I called unnamed ARE in the current body (lines 98/101/104 — **I verified against the ORIGINAL body, not the live one; true-of-a-stale-version is the same defect class as citing a wrong SHA**) and the body's failing list is **IDENTICAL to the 08-05 log, 11 for 11** (diffed). ✅**Standing: babysitter can AUTHOR but not repair `/workspace/shared/` ⇒ its learning amendments route through me** (2 applied here). ⛔**Also theirs, and it lands on the STREAK ITSELF: 36 includes ONE `cancelled` night (07-28, run `30330588743`) which is UNTESTED, not failed** ⇒ unbroken *failure* segments are **28 + 7**, so "36 consecutive failures" is wrong while "36 consecutive nights without a pass" is right (Main-verified both). ⭐⭐⭐**A cancelled run is neither evidence for nor against — folding it into a failure streak inflates the strongest number in the report, and I had ALREADY MEASURED the `{cancelled:1,failure:35}` histogram and still wrote the streak as if uniform: having the disaggregated data does not mean you USED it.** ⛔**"expected-failures.txt (195 lines)" is 155 comments + 16 blank + **24 ENTRIES** — a LINE count is not an ENTRY count** (`grep -vc '^\s*#\|^\s*$'`); bidirectional-gate conclusion survives, figure doesn't. Boundary logs **410 Gone** both sides; annotations carry only `exit code 1` ⇒ attribute from **today's live log**, not the boundary. Correction → `slang-ci-babysitter`, in-place body EDIT (0 comments ⇒ wrong numbers are the only public artifact). ⛔no fixer (no-hand-edit policy, maintainer-gated). Mechanism+recipe: [[technique_workflow_rename_mints_new_id_old_id_deleted]]. ✅**RESOLVED 08-17: drift fixed by maintainer PR #12531 (merged 08-14, regenerated bundles); suite 20→2 failing / ~6300; residual redness is 3 NON-drift causes — #12442 prelude leak, #12534 transport flake, #12535 unorm SIGSEGV (fixed by OUR bot's #12539, do NOT dispatch — slang-fixer owns that chain).** Advisory-suite thesis VINDICATED: it surfaced 4 real compiler bugs (#12440-12443). Chain closed our side; no bot post (all 3 `pr_mention`s were false-positives, 0 nv-slang-bot mentions).
- 🔧⚠️**supervisor bounce limb FALSE-POSITIVES on bare `502`/`503`/`504`** (forged `transient` on TERMINAL #12219). ⭐**a classifier whose cheapest signature is the commonest accidental substring mostly reports accidents.** [learning](/workspace/shared/learnings/1785802540595-supervise-issues-bare-numeric-transient-signatures.md)
- [**#12145 GBufferRTTexGrads_d3d12 flake — DOMINANT queue evictor**](project_12145_gbufferrttexgrads_d3d12_access_violation.md) — 16 PRs/7d, jkwak-work, ZERO fix PRs ⇒ **ESCALATED 08-03**; ask=quarantine; ⛔no fixer; **cmt `5062894889` is OURS ⇒ EDIT-in-place, never re-POST.** ⭐**a counter conflating "considered" with "did" ranks attention, not action** · [**rerun→`cancelled` is NOT a new canceller**](feedback_rerun_partial_cancel_is_not_a_new_signature.md) — diagnose by **sibling `started_at`**
- ⛔⭐⭐⭐[**#12364 CTS `storage_image_minNonUniform` on 0.0.9 — failure REAL, TWO body claims REFUTED**](project_12364_cts_storage_image_minnonuniform.md) — Main-measured before routing 08-05. ✅**Real + deterministic:** 3 dispatch runs, byte-identical `Image comparison failed: max diff 1.34744e+08 vs threshold 0.02`, SPIR-V md5-identical; **best artifact is the FULL-SUITE 0.0.9 run `30985159061` = 12396 pass / `Failed 1` / 1395 waived, and the ONE red IS this test** (not in the issue body). ⛔**CLAIM 1 REFUTED — "fails in all recent nightlies": master's nightly pins 0.0.7 in all 6 yml sites; 0.0.9 exists ONLY on the reporter's `ci/cts-009-*` scratch branches. The cited failing nightlies are the SLANGWIN5 dll-load outage (`failed to load slang` ×11545, `Failed 11545/13792`) — and this very test logs `Pass (Pass)` in BOTH of them.** ⭐⭐⭐**The A/B pair is confounded on TWO axes: `compare 14e5a0dc0e...871e77fb0f` = ahead 1/behind 1, different branches AND different Slang builds ⇒ "0.0.7 passes / 0.0.9 fails at the same Slang" is NOT what was run.** ⛔**CLAIM 2 REFUTED — "Slang SPIR-V codegen bug": the dumped module is `OpSource GLSL 450` + `GL_EXT_nonuniform_qualifier`, but Slang hardcodes `SpvSourceLanguageSlang` (`slang-emit-spirv.cpp:12237`, degrading only to `Unknown`, NEVER GLSL); program is `<SpirVAssemblySource>`-only (7/7 blocks, 0 GLSL/HLSLSource) ⇒ a SelfValidate ASSEMBLY case.** ⭐⭐⭐**AND THE DECORATION SAID TO BE DROPPED IS PRESENT — `OpDecorate %41 NonUniform` on the `OpImageTexelPointer` ⇒ a PIXEL MISMATCH on already-decorated SPIR-V is NOT the #12110 family's missing-decoration signature; titular symptom and hypothesised cause name different layers.** ⚠️**NOT established: whether Slang is in this case's path at all** (`DISABLE_CTS_SLANG: 0` + dlls copied ⇒ available; `search/code` on the fork = 0 hits, an indexing gap not an absence) ⇒ **fixer answers that FIRST or the codegen framing is void.** ⚠️**SIBLING NOT DUP of [[project_12110_nonuniform_descriptorhandle_spirv]]/#12161 (those = decoration DROPPED on DescriptorHandle/heap; this = present, plain runtime-array) — the test name contains "NonUniform", so a wrong-layer merge into #12110 is the likeliest bad outcome.** ✅Waiver already merged (VK-GL-CTS `2ab0526b7a`, waiver.xml:1414) ⇒ nightly unblocked, no CI urgency. ✅**RESOLVED 11:32Z — triage done, verdict cmt `5191182483` (fresh, comments=1), NO fixer.** **Gating Q ANSWERED: NO, Slang is not in the path, and it is a COMPILE-TIME fact** — dispatch is by C++ OVERLOAD (`SpirVAsmSource`→`assembleProgram` `vkPrograms.cpp:871`, never calls the Slang hook `vkShaderToSpirV.cpp:291`), confirmed on BOTH paths (runtime `vkResourceInterface.cpp:1027`, offline `vktBuildPrograms.cpp:331`); the SPIR-V is a **hardcoded C++ string literal** (`:2662-2733`). ⭐⭐⭐**Both my refutation-2 observations collapse into ONE fact (asm is hand-authored) ⇒ before calling two observations independent, ask whether a single upstream fact produces both.** ⛔**CLAIM 3 REFUTED (theirs, new): "the test was updated upstream" is FALSE** — Main re-derived the control independently: test blob `f2ee535a…` at BOTH CTS tags with a **must-differ control** (`vkShaderToSpirV_slang.cpp` `176b5261…`→`5948cd7e…`), `fetch_sources.py` identical ⇒ ⛔**MY OWN "diff the upstream test" instruction was WRONG and is RETRACTED — I inherited the issue's causal premise while refuting two others, because it was the part I had no reason to doubt.** ⛔**AND MY `<SpirVAssemblySource>` PROBE COULD NOT FAIL: `GLSLSource` IS NOT A qpa TAG AT ALL (0 matches for any `<*Glsl*` tag), and my "7/7" denominator was a `-Tail 1000` slice = 6 of 13,792 cases, 2 of which are `ray_query` — `glslSources.add`-built (36 vs 1 asm) yet STILL tagged `<SpirVAssemblySource>` ⇒ the tag does NOT imply "bypassed Slang".** ⭐⭐⭐**The refutation that survived intact is the one whose probe was fictional — being RIGHT made the evidence unauditable; nobody re-derives support for a claim they already accept.** ⭐⭐**My "unavailable yet it passed" argument was load-bearing on an unchecked premise: it only works because there is NO silent glslang fallback (`TCU_THROW` `vkPrograms.cpp:754`) — name and verify the fallback or the inference is decoration.** ✅**0.0.9 is a large net WIN, not a regression: its one functional change is the `slang.dll`→`slang-compiler.dll` rename (VK-GL-CTS PR #17) which FIXES the 11,545-test/night outage** (slang renamed in `dcb47b716`; workflow copies only `slang-compiler.dll`, not the shim `source/slang/CMakeLists.txt:423`). ⭐**Sharpest discriminator: 5 sibling `*_minNonUniform` cases PASS in the same run ⇒ specific to the `OpImageTexelPointer`+`OpAtomicIAdd` atomic arm, not non-uniform indexing.** ✅**Cause deliberately NOT isolated — "excluded direct Slang codegen" ≠ "identified culprit", kept apart on purpose.** ⚠️**OPEN, MINE: PR #12365 (open, `behind`, `closingIssuesReferences=[]` ⇒ won't auto-close) repeats the refuted premise; triager declined to post on the reporter's own PR and escalated the call to me.** **REOPENED 15:47Z: jkwak-work (MEMBER) added a REVERT OBLIGATION — `2ab0526b7a` in shader-slang/VK-GL-CTS must be reverted when this resolves (verified: 1 file, +3/-0, fenced `<!-- 12364 -->` markers ⇒ clean self-contained revert). Nothing to DO yet (conditioned on resolution, cause still unidentified) — the obligation must SURVIVE. Routed to slang-triager for definition-of-done + GitHub ack.** ⛔⭐⭐⭐**MY v3 TRIGGER DID NOT STRICTLY FIRE ON IT — a THIRD failure mode after never-fires and always-fires: I scoped the predicate to "changes the ANSWER" and it was BLIND to "changes the OBLIGATION." A trigger can be correctly selective AND correctly satisfiable and still miss a whole CATEGORY of inbound ⇒ resume predicates need a SCOPE/definition-of-done clause, not only evidential ones — a maintainer adding a cleanup requirement is the commonest such inbound and the easiest to silently no-op on.** ⚠️**Near-miss of the #11616 shape: an obligation recorded ONLY in a GitHub comment on a chain everyone treats as closed goes dark** (waiver fences say WHAT, not WHEN). Trigger amended to v4 with clause B — **and then to include clause D, a CATCH-ALL that OUTRANKS A-C, after the triager supplied the decisive nuance: all five of THEIR watch clauses were answer-scoped too, but their broad "fresh substantive human comment" catch-all DID fire, so only their ENUMERATION was blind.** ⇒ ⭐⭐⭐**A catch-all is NOT redundancy against the specific clauses; it is the coverage for the categories you have not enumerated — and do NOT tighten it after adding a specific clause, an instinct strongest exactly when the new clause makes the set feel complete. Name the CATEGORY (revert / follow-up PR / doc-mirror / tracking anchor / "when X lands also do Y"), never the instance.** ✅**Ack verified live (cmt `5194076073`, comments 3, accurate + correctly scoped): DoD = identify cause AND revert; no resolution date; persist-vs-revert fork raised with jkwak now rather than banked; fork write-access absent so the revert is an action to REQUEST.** ⭐⭐**Mode-3 fix is TWO-part: amend the predicate AND write the obligation into the DoD — the predicate gets you woken, the DoD survives the next close.** Keyed rule (all 3 modes, previously scattered across 4 chain memos and re-derived anyway): [[feedback_resume_triggers_fail_three_ways_enumerations_are_category_blind]]. Anatomy of my probe defect: [[feedback_false_coverage_the_five_mechanisms_that_consume_the_reason_to_look]] §Mechanism-3-second-instance.

- ⭐⭐⭐[**#12366 — the formatting hook it audits is UNREGISTERED dead code**](project_12366_pre_tool_use_hook_unregistered.md) — `.claude/settings.json` on master holds ONLY `env`; the `PreToolUse` block was deleted in **#9775 (`5e0a22a4f`, 2026-01-29)** ⇒ `pre_tool_use.py` never runs, so the issue's "contributors trust it" premise is FALSE and the fix is delete-or-rewire, not fix-the-logic. ⛔**An audit of a file's LOGIC is not an audit of whether it RUNS — read the dispatching config before reporting on a hook's effect.** Claim 2 also **inverts PreToolUse ordering** (it fires BEFORE the tool call, so hook→`git add` DOES commit formatted bytes; tested both ways). Claim 1 (`--since master` = `master..HEAD`, disjoint from the index) is the real defect, confirmed with a positive control; 3 and 4 correct as read. ⛔**MY CLONE IS SHALLOW (9 commits, `.git/shallow` present) — `git log --diff-filter=A` named the GRAFT commit as the file's add-event and `merge-base --is-ancestor` answered NO for a true ancestor, both SILENTLY. Re-derive history from `gh api …/commits?path=<file>`; check `.git/shallow` before quoting ANY ancestry result.** **TRIAGED+POSTED 08-05, awaiting maintainer (no fixer forward — delete-vs-re-register is `jkwak-work`'s call).** Verdict=cmt `5192079600` (1 cmt, edited in place); labels `Infra`+`reproduced`, Type=Build. ⚠️**3 of my claims NARROWED, each re-verified by me: (a) defensible form is "not registered by any TRACKED config at HEAD" — hook entries MERGE across user/project/local levels, so an out-of-tree layer could still invoke it (`git grep -l pre_tool_use HEAD`=0 tree-wide, control `formatting.sh`=14); (b) my no-strand conclusion was OVERSTATED — 2 TRUE-ORDER strands exist (unmatched staging route `cd x && git add`/`git -C . add`/`git stage`/IDE, and split staging), so the strand is absent only when a matched `git add` IMMEDIATELY precedes the commit; (c) "no-op" too strong — a path already committed on the branch IS selected, so the accurate claim is "misses exactly the paths whose only change is uncommitted".** ⭐**ONE-FLAG FIX EXISTS, I reproduced it: `--since master --modified` = the UNION** (`list_files()` appends trailing `HEAD` only when `--modified` is ABSENT, `extras/formatting.sh:264-273` ⇒ degrades to master-vs-WORKTREE); untracked stays outside all forms (controlled). ⇒ **#8637 (jkiviluoto-nv, open, Type=Build) is ALREADY IMPLEMENTED.** Live replacement supersedes the orphan: `extras/git-hooks/pre-commit` (#8872, CONTRIBUTING.md:340) — ⚠️NOT staged-only, it passes `--modified` so a partially-staged file's unstaged hunk IS ABSORBED; don't oversell. **#12358 (open draft) already flagged `pre_tool_use.py:29` and INVITED this follow-up ⇒ cross-link, not a dup.** ⭐⭐**DEDUP: search the ARTIFACT the defect lives in, not the words the report used** — 3 sweeps missed #8637; `formatting.sh in:title` found it instantly. ⛔**`grep -cF '--flag-shaped'` is EATEN AS AN OPTION and prints an EMPTY count that reads as a verified ZERO — use `grep -cFe`.** ⛔**Clone depth is PER-CONTAINER: mine shallow(9) and silently wrong, triager's full(6742) — re-check, never inherit.** ⭐**Capture a component's input selection AT THE MOMENT IT OBSERVES IT** (first matrix read it post-commit, a different state, and selection WAS the claim). Formatters ABSENT in these containers ⇒ stub used; a real PR author must run `./extras/formatting.sh`. RE-OPEN only on fresh substantive human cmt.
