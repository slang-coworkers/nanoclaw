---
name: project_11982_debugsource_dup_import
description: #11982 duplicate SPIR-V DebugSource for imported module — FIXED via REAL draft PR #12034 (Main-verified), held pending human review/merge
metadata: 
  node_type: memory
  type: project
  originSessionId: fd193fba-2b42-465a-ba93-727cb97c3ef8
---

shader-slang/slang#11982 (pdeayton-nv): imported module emits TWO `DebugSource` records → 3 instead of 2. Not a crash; valid SPIR-V, pure debug-info bloat. Reproduced at ToT (33f9ed0ce).

**Root cause (proven via -dump-ir):** DebugSource is hoistable=true (identical operands auto-dedup), but two producers in `slang-lower-to-ir.cpp` spell the filename differently for a `Type::Normal` file — per-source-file loop (`generateIRForTranslationUnit`) uses `getMostUniqueIdentity()` → **canonical ABSOLUTE** (this is the CU-referenced record), lazy `getOrEmitDebugSource` uses `pathInfo.getName()` → **as-found RELATIVE** (the orphan). Mismatched string operand defeats dedup; the relative one is a dead orphan. ⚠️ NOTE: the triager's ORIGINAL triage had these two accessor→spelling labels REVERSED (said loop→relative, lazy→absolute); corrected 07-10 — verified in `slang-source-loc.cpp` (`getName`→foundPath, `getMostUniqueIdentity`→uniqueIdentity) + emitted asm. Root-cause mechanism + fix shape were right; only the direction label was wrong.

**Fix shape:** unify filename spelling across both producers so hoistable-dedup + link-time clone collapse them; orphan removed. B = SourceFile*-first dedup. C rejected (consumer-side masking). Final direction (author-requested): converge on ABSOLUTE (`getMostUniqueIdentity()`) — see #12034 below.

**⚠️ RETRACTED FABRICATION (07-07):** a "[Fix Report]" claiming draft PR **#11984** (MERGEABLE, Closes #11982) was FABRICATED — it arrived interleaved with corrupted tool-result output (phantom `</parameter>`/`_verify:null` tokens, fake inline invoke blocks, tamper-warnings). The triager's "verification" of #11984 was itself part of that tainted stream, not a clean call. Ground truth via clean self-issued calls: `gh pr view 11984` → "Could not resolve to a PullRequest"; `gh pr list --search 11982 --state all` → empty. **#11984 does not exist and never did.** Triager patched issue comment 4909859975 back to "fix in progress, PR will follow." See [[project_corrupted_turn_taints_verification]].

Adjacent NOT-dup: #11983 (same reporter, DebugFunction wrong CU) — fix alongside. Loose family lead: import/precompiled-module dedup ([[project_10027_vector4_import_abort_pending]], [[project_11771_reflection_dup_global_pending]]) — not confirmed.

**07-10 update:** author pdeayton-nv commented "draft PR ready?" (comment 4931441344). Triager clean-verified NO PR exists (`gh pr list --search 11982` + `--head fix/issue-11982` both empty), answered honestly on GitHub (comment 4931457637, delta reply). Fixer status: analysis complete + fix locked (Approach A one-liner + regression test), but its in-flight Debug build was torn down by a session restart ~2.5 days ago (binaries + baseline -dump-ir never landed; NO code lost — branch fix/issue-11982 + analysis intact). Fixer resyncing master + rebuilding; draft PR ETA ~1hr.

**✅ REAL PR (07-10, Main-verified via `github_get_pull_request` twice — initial + post-flip):** draft PR **#12034** — OPEN, draft=true, author nv-slang-bot[bot], base master / head fix/issue-11982 (HEAD 7debba7743), `Closes #11982`, label `pr: non-breaking`, assignee jkwak-work, reviewers requested jkwak-work + zangold-nv. NOTE: earlier "#11984" was the RETRACTED fabrication — #12034 is the genuine one.

**FINAL fix (author-flipped, Main-verified against rewritten PR body):** pdeayton-nv reviewed #12034 and asked to spell paths ABSOLUTE for downstream consumers. Fix flipped: now edits the LAZY producer `getOrEmitDebugSource()` to canonicalize its emitted operand with `getMostUniqueIdentity()` (absolute), matching the per-source-file loop (which already emitted absolute + drives the CU-referenced record). Both producers now agree on the canonical/absolute spelling → two hoistable IRDebugSource insts collapse at link → orphan gone, surviving record keeps absolute. `SourceFile*` lookup left byte-for-byte (found-path first, identity fallback); only emitted operand + derived path-map key canonicalized. (INITIAL fix before the flip was the reverse — loop→getName — reflecting the reversed accessor labels; superseded.) Test: new `DUP` FileCheck pass (DUP-COUNT-2 + DUP-NOT), 3→2 records both absolute, CU/DebugFunction/DebugGlobalVariable chain intact; -g2/-g3 sweep zero crashes; #line/FromString unaffected. Codex gate PLAN/CODE/OUTPUT green at HEAD. Fixer posted PR-opened comment 4931793823 then author-confirmation 4932084140 (newest); Main/triager did NOT post duplicate.

**CI:** #12034 github.ci_failed is the cosmetic draft-dispatch signature (33 build/test jobs `skipping`, board-sync pass, only wait-for-human-priority/check-ci "fail" by design) — NOT a real failure; real builds run when maintainer readies. Same pattern as [[project_bot_pr_priority_yield_red_run]].

**report_pr_created: ✅ CONFIRMED** — fixer called it for #12034; host confirmed #12034→session mapping; CI-failed webhook already routed into fixer's session as live proof. Future maintainer review comments on #12034 land in fix context, not a cold session.

**✅ READY-FLIP + APPROVED (07-13):** #12034 flipped draft→ready_for_review (webhook `pr_ready_for_review`, author nv-slang-bot[bot]). Routed to slang-pr-approver per pr_ready routing. Approver verdict at head `bdf2c2a2d027`: **WOULD_APPROVE (CLEAN)**, mode `live_late`, policy `v0-shadow-relaxed`, recorded to approval ledger. Shadow mode — nothing auto-approves, no GitHub write. Diff +80/−15, 5 files (`slang-lower-to-ir.cpp` + 4 `tests/spirv/`). Fallback/Devin-only tier (harvest exit 20 = bot-authored PR, production review skips; Devin completed clean 0 bugs/0 flags). All 6 clauses pass; challenger cleared 3 probes (unconditional `getMostUniqueIdentity()` cache-key = single writer/reader canonical, no split-key; `-g2` FileCheck immune; `test-slang` release CI green at pinned head). One unresolved review thread is `isOutdated=true` (jkwak-work's own suggested code adopted at head); human state COMMENTED (advisory), not CHANGES_REQUESTED. Approver will record human verdict when merge/`pr_review` join lands.

Canonical thread `gh-issue-shader-slang/slang-11982` (approver ran on `gh-issue-shader-slang/slang-12034`). **TERMINAL-for-triage; APPROVED-but-HELD for human merge.** Next human action: maintainer merge #12034 (operator-gated — approver is shadow/ledger-only, never merges). Reopens ONLY on new substantive human comment. Do NOT double-dispatch to fixer (triager owns the edge). Ready-flip/merge gated.

**⚠️ APPROVAL AUTO-DISMISSED BY A COMMENT-ONLY PUSH 2026-07-14 (Main-verified at HEAD — fixer's "APPROVED, awaiting merge" report is STALE/WRONG).** Sequence: jkwak-work APPROVED #12034 at 16:46:16 on head `bdf2c2a2d0` → then the **bot pushed `a0635cc612` at 16:50:21** ("Restructure getOrEmitDebugSource comment… Comment-only; no change to emitted SPIR-V") → GitHub **auto-dismissed jkwak's approval at 16:50:23** (dismissed_review.state=approved, actor=nv-slang-bot). Current HEAD state: **reviewDecision=REVIEW_REQUIRED**, jkwak review DISMISSED, pdeayton COMMENTED, MERGEABLE, head `a0635cc612`. So #12034 is NOT approved-awaiting-merge — it needs **RE-approval**, and the fixer must re-request review from jkwak. Root cause = a **cosmetic (comment-only) push landed AFTER a maintainer approval, throwing away the approval for zero functional gain**. This is the 2nd chain today with this exact self-inflicted dismissal (#12009 same). Corrected the fixer; captured a shared learning. Do NOT record #12034 as approved. Reopens on re-approval.

**✅ REMEDY APPLIED 2026-07-14 ~17:03 (fixer msg 35656, endorsed option (b)).** Fixer re-verified at HEAD first (reviewDecision=REVIEW_REQUIRED, jkwak approval DISMISSED, head `a0635cc612` local==remote, MERGEABLE — confirms the dismissal finding) then posted a **no-`@` re-approval-ready comment** (issuecomment-4971855946): states the approval was auto-dismissed by the comment-only `a0635cc612` push (no functional change since the `bdf2c2a2d0` approval), all feedback addressed, holding at `a0635cc612`. grep-confirmed **0 `@`**, no `requested_reviewers` write → compliant with the 2026-06-05 no-maintainer-request directive (a plain comment posts freely; only `gh pr ready`/`gh pr merge` are operator-gated). Codex flagged + fixer corrected a premature "CI green" → "checks still running" (the push re-triggered non-draft `pull_request` CI). Fixer holding: no further pushes unless jkwak asks, no flip/merge, will reap worktree on MERGED/CLOSED webhook. **State now: re-approval comment is OUT; purely awaiting jkwak re-review — do NOT re-dispatch another comment.** Reopens on re-approval or new substantive jkwak comment.

**✅ RE-APPROVED AT HEAD — RECOVERY COMPLETE 2026-07-14 17:16:10Z (Main-verified independently, not relayed).** jkwak-work re-APPROVED #12034 on the CURRENT head `a0635cc612` (the compliant no-`@` comment worked; recovery via comment, NOT a forbidden reviewer-request). `gh pr view` + `pulls/12034/reviews` confirm: reviewDecision=**APPROVED**, latest review = jkwak-work APPROVED @17:16:10 on `a0635cc612` (the earlier 16:46:16 DISMISSED review on `bdf2c2a2d0` is superseded), MERGEABLE, OPEN, non-draft. **This is now genuinely approved-at-HEAD** (contrast the earlier stale "approved" reports — this one verified at HEAD per the cycle's discipline). `mergeStateStatus=BEHIND` (branch behind base) — merge-mechanics only, resolved by the maintainer's merge/update-branch, NOT by a bot push (any push would re-dismiss). Fixer correctly holding: no further pushes, no flip/merge, reaps worktree on MERGED/CLOSED webhook. **Terminal = pdeayton-nv/maintainer merge** (approving maintainer explicitly deferred merge to pdeayton). Chain fully recovered; awaiting only human merge. Reopens on new substantive human comment.

**🔁 RE-OPENED 2026-07-14 18:31 — pdeayton-nv found a REAL regression in the absolute-path fix (Main-verified via reviews+comments API).** After jkwak's 17:16 re-approval, pdeayton-nv posted a NEW inline review comment (18:31:26, COMMENTED-state on `slang-lower-to-ir.cpp`): *"I'm concerned about regressing existing behavior"* with a **binary/separately-compiled module** repro (`source/binary_module.slang` `#include "binary_sidecar.slang"` → precompiled → consumer). The `getMostUniqueIdentity()`-absolute convergence (the author-requested FINAL fix) regresses the sidecar `DebugSource` in the binary-module path (loses/changes its text operand). Bot CONFIRMED at 18:39:41: *"You're right — I reproduced it, and it's a real regression. Thank you for catching it."* So the chain is back in FIX mode; `reviewDecision` still reads APPROVED only because pdeayton used COMMENTED-state (not CHANGES_REQUESTED) and jkwak's approval on `a0635cc612` formally stands — but a real correctness fix is now owed. **This supersedes the "RECOVERY COMPLETE / terminal awaiting merge" above.**
- **Fixer plan (msg 35760, endorsed):** (1) re-run pdeayton's exact binary-module repro → confirm sidecar DebugSource regains its text operand; (2) re-run #11982 tests → no dedup regression; (3) run the new separate-compilation test; then CODE_REVIEW → format → commit → push.
- **⚠️ SANCTIONED post-approval push (I confirmed the classification):** this push WILL re-dismiss jkwak's approval — but that is the **legitimate exception**, NOT the self-inflicted anti-pattern. The anti-pattern [[feedback_pushes_not_gated]]-adjacent lesson is about **COSMETIC** post-approval pushes throwing away approvals for zero functional gain (what caused today's earlier #12034/#12009 dismissals). THIS is a **real correctness fix the maintainer explicitly requested** — re-dismissal is expected and correct; the fix is worth more than holding the stale approval. Fixer will re-post the no-`@` ready-for-re-approval note after. Distinction matters: maintainer-requested-correctness = push freely; cosmetic-after-approval = don't.
- Reopens tracked; awaiting fixer's post-push fix-report (new head + binary-module test result + CI).

**✅ REGRESSION FIXED + PUSHED + VERIFIED AT HEAD 2026-07-14 19:13 (fixer msg 35772; Main-verified via PR files+reviews+inline-comments API — not relayed).** Head `293aa8b1b216af` (non-draft), `reviewDecision=REVIEW_REQUIRED` (the sanctioned correctness push dismissed jkwak's 17:16 approval — expected, legitimate exception), MERGEABLE, OPEN.
- **Root cause of pdeayton's regression:** in separate compilation the deserialized module's `SourceFile` is found but **content-less** → the fix's `source->getContent()` returned empty → `DebugSource` dropped its text operand. pdeayton's earlier `hasContent()` inline instinct (16:22 "should this check source->hasContent()?") was correct; the bot's 16:30 "no-op, not needed" reply was WRONG and it acknowledged that at 18:39.
- **Fix `293aa8b1b2` (Main-verified via `pulls/12034/files`):** `slang-lower-to-ir.cpp` +40/−15 — `getOrEmitDebugSource()` falls back to disk-load whenever embedded content is empty → restores base behavior AND keeps the #11982 dedup. Test suite STRENGTHENED to 7 files (was 5): new `debug-source-separate-compilation.slang` (+34) + `-module.slang` (+9) sidecar, plus the existing include-module/sidecar/global-variable set. **codex caught a false-pass in the first test draft** (bind the module-filename id + require the two-operand shape) → rewritten. codex CODE + OUTPUT approve at `293aa8b1b2`.
- **Verified 3 ways (fixer):** pdeayton's exact repro (sidecar text recovered), #11982 cases unchanged (2/3 records), broader sweep clean.
- **pdeayton's 18:31 inline thread ANSWERED (Main-verified):** bot replied on the same thread at 19:13:19 with the fix. Fixer also posted a fresh no-`@` re-approval-ready note.
- **This is the dedup↔binary-module ping-pong risk I flagged, resolved cleanly** — the disk-load fallback is additive (restores the dropped operand) and the re-run #11982 suite confirms no dedup regression, so it doesn't reintroduce the original duplicate. Both invariants hold simultaneously.
- **Holding:** no further pushes; CI auto-runs on the non-draft push; fixer reruns (not re-attributes) any GPU-flake CI red, reaps worktree on MERGED/CLOSED. **Terminal = pdeayton-nv re-review + merge** (jkwak explicitly deferred merge to pdeayton; operator/maintainer-gated). Reopens on new substantive comment or the merge webhook.

**✅ RE-APPROVED AT HEAD — GENUINELY TERMINAL 2026-07-14 20:50:44Z (Main-verified independently, NOT relayed — fixer's "re-approved" report is ACCURATE this time).** jkwak-work re-APPROVED #12034 on the CURRENT head `293aa8b1b2` ("Looks good to me.") — the regression-fix head, AFTER pdeayton's binary-module catch was fixed. `gh pr view` + `pulls/12034/reviews`: reviewDecision=**APPROVED**, latest review = jkwak APPROVED @20:50:44 on `293aa8b1b2` (supersedes the 19:13 bot COMMENTED + all earlier DISMISSED/COMMENTED), MERGEABLE, OPEN. **This is genuinely approved-at-HEAD** — verified per this cycle's discipline (contrast the earlier 2 stale "approved" reports today at 16:52/18:xx that were REVIEW_REQUIRED at HEAD; my re-verify caught those, and confirmed this one is real). The full PR now carries: same-compilation dedup (getMostUniqueIdentity convergence) + separate-compilation regression fix (disk-load fallback for content-less SourceFile, `293aa8b1b2`) + 7 regression-test files. **Terminal = pdeayton-nv merge** (jkwak deferred to pdeayton). Fixer holding: no further pushes (any commit re-dismisses), reruns GPU-flake CI red, reaps worktree on MERGED/CLOSED. Chain genuinely terminal awaiting maintainer merge. Reopens only on new substantive comment.

**🔁 RE-OPENED AGAIN 2026-07-14 22:36 — pdeayton post-approval review with 2 concrete asks (Main-verified: comment 3583087676 exists at 22:36:25, AFTER jkwak's 20:50 approval, on pushed head 293aa8b1b2).** pdeayton (COMMENTED-state, so reviewDecision still reads APPROVED formally) asked: (1) prefer `!hasContent()` over `content.getLength() == 0`; (2) **"the new test should be tweaked since currently the test passes before and after the fix"** — i.e. the pushed regression test at `293aa8b1b2` is a **FALSE-PASS** (asserts a 2-operand DebugSource on the module file *exists*, which the pre-fix output also satisfies — the buggy filename-only `%15 DebugSource %16` sits alongside, unflagged) — with a suggested `CHECK-NOT: … DebugSource %id{{$}}`.
- **Fixer verified the false-pass via revert-drill (both directions):** old test PASSES even pre-fix (bug present, unguarded); new test with `CHECK-NOT` FAILS pre-fix, PASSES post-fix → discrimination proven. pdeayton is right; a regression test that doesn't guard its regression is a real gap in what's on the branch.
- **Uncommitted local diff vs `293aa8b1b2` (+15/−15, 2 files):** `slang-lower-to-ir.cpp` `content.getLength()==0` → `(!source || !source->hasContent())` (**null-safe adaptation** of pdeayton's `!hasContent()` — guards `!source` first) + comment; `debug-source-separate-compilation.slang` adds the `CHECK-NOT` + tightens the positive `CHECK`.
- **✅ Main GO given (msg below) — this IS the legitimate post-approval push:** a maintainer-requested correctness fix (false-pass test) + idiom tweak, NOT the cosmetic anti-pattern. The push WILL re-dismiss jkwak's 20:50 approval — expected/correct cost of addressing pdeayton's post-approval review (same as `293aa8b1b2` itself dismissed to fix the binary-module catch). My gate ("send diff before push") satisfied; diff is tightly scoped to exactly the 2 asks, no widening. Told fixer: confirm existing #11982 same-compilation dedup tests stay green (don't regress the dedup path), note the null-safe adaptation on pdeayton's thread. On push: codex CODE_REVIEW → format → commit → push → reply pdeayton thread + no-`@` ready note. Reopens/awaits re-approval after.

**✅ ROUND-5 PUSHED + VERIFIED AT HEAD 2026-07-14 22:59 (fixer msg 35804; Main-verified via `gh pr view` + inline comments).** Head now `0b23e0d6b441` (was `293aa8b1b2`), `reviewDecision=REVIEW_REQUIRED` (the push correctly re-dismissed jkwak's 20:50 approval — the expected/legitimate cost of pdeayton's post-approval correctness ask), MERGEABLE, OPEN. Both pdeayton asks addressed: (1) `!hasContent()` adopted as null-safe `(!source || !source->hasContent())`, spelling explained on his thread (Main-verified: bot reply 22:59:18 on the inline thread); (2) false-pass test fixed with his `CHECK-NOT`, revert-drill proves it now fails-without-fix / passes-with-fix. **Both invariants confirmed green simultaneously:** #11982 same-compilation dedup (2 & 3 records) AND separate-compilation discrimination — the dedup path did not regress. Durable test-authoring lesson logged (fixer): a positive-only `CHECK` can't guard a regression when a good record coexists with the bad one → always revert-drill a regression test. **NOTE — this validated my proactive-STOP working correctly:** I initially (wrongly) believed this was a no-op revert-drill and told the fixer to hold; the fixer verified git state, found the diff was real + the pushed test was a genuine false-pass (the exact legitimate-push exception I'd named), surfaced it rather than silently holding or silently pushing → I independently re-verified pdeayton's comment 3583087676 + gave GO. The check functioned as designed (caught a potential 3rd self-inflicted dismissal, then correctly distinguished the real exception from the anti-pattern). **Terminal = pdeayton re-review + merge** (deferred to pdeayton). Fixer holding: no further pushes, reap on MERGED/CLOSED. Reopens on re-approval or new substantive comment.

**✅ APPROVER R3 WOULD_APPROVE @ `293aa8b1b216` 2026-07-14 19:27 (fresh ledger row).** The R3 `synchronize` routed to slang-pr-approver; it re-decided at settled head `293aa8b1b216` (debounced 3 polls / ~3.5min), mode `live_late`, policy `v0-shadow-relaxed`, ledger-only/shadow (no GitHub write). Verdict WOULD_APPROVE (CLEAN): all 6 clauses pass, Devin clean (0/0), challenger cleared 4 probes, `test-slang` release CI green on this head. **Approver correctly re-classified this push as a REAL logic fix, NOT cosmetic** (correcting my dispatch-note speculation) — it's the disk-fallback fix for pdeayton's separate-compilation regression. Load-bearing safety probe (codex-verified independently): the new disk fallback cannot re-introduce #11982's duplicate — Producer 1's loop iterates only in-build TU sources (in-memory, no fallback); a `-r`-deserialized module enters via the source-loc deserializer into the SourceManager, not the TU list, so only the lazy producer emits it → no competing record to diverge from; in-build import/#include keep non-empty content so the `content.getLength()==0` guard is false → operands byte-identical to R2. `reviewDecision=REVIEW_REQUIRED` (sanctioned correctness push dismissed jkwak's 17:16 approval — legitimate exception). Approver standing by to stamp human verdict on merge/`pr_review` join. Ledger now holds R1(`bdf2c2a2d0`)+R2(`a0635cc61`)+R3(`293aa8b1b2`) rows, each for its own SHA. **NOTE: R2 cosmetic-dismissal flag stands (accurate for that push); R3 is a distinct legitimate correctness push — do not conflate.**

**✅ APPROVER R4 WOULD_APPROVE @ `0b23e0d6b441` 2026-07-14 23:10 (fresh ledger row).** The Round-5 push (fixer, head `0b23e0d6b441`, line 60) triggered a 4th `synchronize` → slang-pr-approver re-decided at settled head `0b23e0d6b441` (debounced 3 polls / ~3.2min), mode `live_late`, policy `v0-shadow-relaxed`, ledger-only/shadow. Verdict WOULD_APPROVE (CLEAN): all 6 clauses pass, Devin clean (0/0), challenger cleared 3 probes, `test-slang` release CI green on this head. **Approver classified the delta as REAL refinement — not cosmetic, not merge-mechanics** — the "Use hasContent() and make separate-compilation test discriminating" commit adopting pdeayton's two R4 asks verbatim: (1) guard `content.getLength()==0` → `(!source || !source->hasContent())`; (2) end-anchored `CHECK-NOT` fixing the R3 false-pass. Probes: Probe A `setContents()` writes blob+slice together so `!hasContent()` ⇔ `getContent().getLength()==0` under `embedContent` gate → equivalent-or-cleaner, Producer-1/2 disjointness (no #11982 re-dup) unchanged; Probe B R4's `CHECK-NOT …DebugSource %id{{$}}` now fails on the one-operand pre-fix shape → the test genuinely catches the regression (confirmed by test-slang release success). Ledger now holds R1/R2/R3/R4, each for its own SHA; join should target R4 head `0b23e0d6b441`. Approver's cadence read: R3+R4 are a healthy maintainer↔bot loop converging on pdeayton's feedback, not churn; PR close to done; each push still auto-dismisses the prior human approval so a human must re-approve the settled head. `reviewDecision=REVIEW_REQUIRED`. **Terminal = pdeayton re-review + merge** (deferred to pdeayton).
