---
name: slangpy-1072-profiler-drain-snapshot-race
description: "slangpy#1072 profiler drain() snapshot-order race finalizes empty frames; P1 CI flake fix in flight"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1f98e769-cee1-48cb-a375-95117b083d43
---

## ⚡ CONTROLLING STATE — read this first

*This row is append-only: its newest and most authoritative content sits furthest from the top, and the file is
~47KB — past the ~24.4KB `Read` limit. The conclusions below are restated **in full** rather than pointed at,
because a forward pointer would resolve to bytes a truncated read already dropped. **Do not tidy this block away
as redundant — the duplication IS the point.***

- **✅ STATUS 08-03 17:27Z — CHAIN COMPLETE, PEER-APPROVED, AWAITING HUMAN MERGE ONLY.** Head **`34226ac79e`**, 9 commits, **+214/−13 / 2 files**, non-draft, **`mergeable: true`** (Main-verified via API). Reviewer round-2 **APPROVE_WITH_NITS** (0 bugs, 4 non-blocking gaps, 0 questions) — supersedes a round-1 review the reviewer **correctly VOIDED** after discovering the guards it relied on were `doctest::skip()`-disabled. All 4 fixer asks answered yes; every actionable nit shipped (`consumed`-at-swap invariant, correlation-id sentinel rationale, bounded-stall + wrap/ABA analysis in the body). Reviewer independently confirmed: barrier **complete** (enumerated every enqueue/drain against its counter), gate **deadlock-free on all paths incl. three the fixer hadn't enumerated**, **(A) genuinely pre-existing**, **all three tests non-vacuous**. CI on new head **1 success / 12 pending** at check time (fixer correctly cited 14/14 on the *prior* head, not the new one); monitor armed. **GH_TOKEN RESOLVED** (operator refreshed); both owed writes posted. **No blocker — merge is the only remaining step; webhook-driven.**
- **Two propagated NUMBERS corrected by recomputation, not trust:** `sizeof(CpuEvent)` is **64 B, not 56** ⇒ wrap-reachability bound is **petabyte-order, not ~2 exabytes** (~1000× off; **conclusion unchanged**, but the wrong figure had already reached the reviewer's artifact **and** a shared learning). And two wrap holes had been **collapsed into one claim**: duplicate-id is bounded by `timeline_id` aliasing (frame zone counter saturates at 2^30−1 < the 2^32 sequence period ⇒ a thread can't collide with itself inside a frame), but the **ABA hole needs only a sequence wrap and is NOT allocation-bounded** — weaker guarantee, now recorded as *considered* rather than *impossible*.
- **⭐⭐ THE KEEPER GENERALIZATION (fixer + reviewer converged — the real output of this chain):** **"present" and "passing" are not "exercising."** A skipped test, a stale binary, and a vacuous assertion are **three disguises for one bug.** The single check that catches all three: **name the defect, then name the assertion that fails when *only* that defect is reintroduced** — otherwise you have *coverage-shaped nothing*.
- **Adversarial-verification tally:** 4 defects (3 the fixer's) · 2 vacuous tests of its own · 1 masking test · 1 borrowed-macro error · 1 wrong failure-mode claim · 2 bad magnitude estimates — **every one caught before a human saw it, none by a passing test.** ~30 critique rounds, real-findings ratio stayed high ⇒ **the argument FOR the gate.**
- **✅ VERDICT EXTENDED TO `34226ac` 08-03 17:31Z — reviewer verified the follow-up head is comment-only (9 insertions, 0 deletions) RATHER THAN TAKING THE FIXER'S WORD** ⇒ its APPROVE_WITH_NITS carries without a re-review. 0 bugs across both rounds; all 4 nits addressed. CI monitor armed on `34226ac`. **Awaiting human merge; nothing outstanding on either side.**
- **⭐ The sizeof correction resolved in BOTH directions (fixer verified the reviewer's correction instead of just accepting it):** base `CpuEvent` is **56 B**, head is **64 B**, and `expected_zone_count` has **zero occurrences on `origin/main`** ⇒ **the reviewer's estimate was CORRECT FOR MAIN and invalidated by the fixer's own diff.** Neither party was sloppy — the number moved because the diff moved it. Exact figure: 2^32 × 512 KiB = **2.0 PiB**. Paired observation (the reviewer's): **the conclusion was robust to being wrong by 888×** — *precisely the condition under which a bad number is never challenged.* Learning filed: **for any number you publish, name how you obtained it** — "estimated from the field list" and "printed by a probe compiled against HEAD" are **different epistemic states.**
- **⭐⭐ WHAT ACTUALLY CLOSED #1072 (the fixer's own honest closing assessment, worth preserving):** **NOT** its original one-line `drain()` reorder. It was **(1) the author catching that `flush()` covered one of three channels** and **(2) the reviewer catching that the "existing regression guards" were `doctest::skip()`-disabled and never running.** The reorder was **necessary but nowhere near sufficient** — and **the first version looked complete and green.** That sentence is the whole case for the review chain: a fix that is green, peer-approved, and CI-clean can still be a third of the answer.
- **(historical) Status:** was LIVE — fixer implementing both suggestions; `GH_TOKEN` was 401 (pushes worked, `gh` writes did not) — **RESOLVED by operator refresh.**
- **Approver NOT dispatched. No nudge sent.** Three `synchronize` webhooks debounced — reviews were `[]` and
  every commit was our own bot mid-implementation. ⭐Liveness is read from the **pushed head**, not from a
  session list.
- **RESUME TRIPWIRES (named so they act as tripwires, not vibes):** (1) head quiet ≥15 min **and** the `ci` run on the settled head completes non-`cancelled`; (2) any new **non-bot** entry in `pulls/1073/reviews`; (3) fixer reports the redesign complete (suggestion 1 + 2 + all three design constraints + re-enabling #1076's two `doctest::skip()` guards). Churn cap ~2h → escalate to operator if still moving. **No nudge sent to the fixer** — it is visibly pushing; a ping mid-implementation is noise.
- **Full detail below, in chronological order.** Corrections and per-sync notes accumulate at the bottom; if
  this file is read truncated, this block is the authoritative summary. See
  [[project_memory_files_over_read_limit_backlog]].

**slangpy#1072** — Profiler can finalize a global frame before consuming attached cross-thread zones. Author **jkwak-work** (maintainer). Opened 2026-07-24.

- **Classification (triager, verified first-hand):** bug in newly-landed profiler PR #1063 / high / SGL C++ `src/sgl/utils/profiler.cpp` / **P1**. NOT upstream-Slang.
- **Root cause:** `ProfilerImpl::drain()` snapshots per-thread zone queues BEFORE snapshotting `sealed_frame_events` (profiler.cpp ~739-765). A zone published+sealed in the unlocked window between the two snapshots gets its frame finalized empty → nondeterministic frame stats.
- **Two CI repros at commit `5a1b34b63a78904aeb092bc6475744edc500a83c`:** (1) macOS ARM64 Debug, cross-thread test `test_profiler.cpp:371-373`, expected `[1,1]` got `[0,1]`; (2) Windows Debug py3.10, `device close settles pending frame statistics` `test_profiler.cpp:596`, empty GPU frame. **Thread-count-INDEPENDENT** — collector runs async; `pending_gpu_count` only bumped when CPU zone event consumed (profiler.cpp:932-933). One fix covers both.
- **Recommended fix (maintainer's proposal, triager-endorsed):** reorder the two snapshots in `drain()` — sealed-frame snapshot first, then queue acquire-loads, keeping consume-zones-before-frames. Memory-ordering proof holds. Keep existing cross-thread test. Alternatives (broaden locking / defer finalize) rejected — worse blast radius.
- **Chain state:** RESOLVED 07-24. Fix implemented (Approach A — reorder the two snapshots in `drain()`: sealed-frame vector under `sealed_frame_mutex` first, then per-thread queue acquire-load of `write_index`; consume order + GPU block unchanged; +14/−5). Covers BOTH repros. **Draft PR #1073** (`Fixes #1072`, branch dev/slangpy-fixer/1072, base main@5a1b34b). Tests: both guards pass (test_profiler.cpp:331, :596); profiler suite 17/17; no new sgl_tests reds. codex PLAN/CODE/OUTPUT approved; slangpy-reviewer peer-approved (confirmatory, status unchanged). Issue carries refreshed 5-bullet (comment 5066273737, now "peer-approved"); auto-closes on merge.
- **Next human action:** review + promote draft #1073 to ready, then merge. Merge OPERATOR-gated. Not upstream-Slang. (As of 07-27: #1073 promoted out of draft, CI all-green, but REVIEW_REQUIRED with ZERO reviews — blocked purely on human approval.)

## ⚠️ SCOPE ESCALATION 08-03 15:01Z — author @skallweitNV found a REAL DEEPER DEFECT; fixer conceded (line-by-line verified) + is fixing it in #1073

**The profiler's own author reviewed #1073 and would request changes — he's RIGHT, and the fixer verified line-by-line before conceding** (not reflexive agreement, not defensiveness). Defect: **`flush()` has THREE collector input channels but only ONE participates in its barrier.** `flush()` records watermarks only from each thread's ring `write_index` (`profiler.cpp:1710`); `flush_targets_satisfied()` checks only `read_index >= flush_targets[i]` (`:1616-1629`). **Neither `sealed_frame_events` NOR `pending_gpu_results` (published `:1046`/`:1289`) participates at all** → implementation does NOT meet its documented contract at `profiler.h:476` ("Block until the collector consumes events published before this call").
- **Pre-existing + INDEPENDENT of the reorder fix.** The reorder still correctly closes the reported **#1072 symptom** (guarantees ordering WITHIN a drain pass) but **cannot make `flush()` wait for a side-channel event published after the CPU watermark was satisfied.**
- **🔴 HONEST CORRECTION the fixer is making to the PR description:** #1073 does **NOT** make the device-close test deterministic; the PR body implied it did. **Consequence: the two "regression guards" CANNOT gate this race** — independently confirming, from the other direction, the coverage nit slangpy-reviewer raised, AND matching caveat (1) in the #1077 section below ("a green run does NOT validate #1073's fix").
- **Fixer committed to skallweit's suggestion 1 IN #1073:** published/consumed watermarks for sealed-frame events + GPU results, folded into flush completion. Offered suggestion 2 (explicit frame completion: expected published-CPU-zone count in marker; finalize only when ended + all expected zones arrived + `pending_gpu_count == 0`) here or as follow-up — defaulting to BOTH here unless he objects. Reply: https://github.com/shader-slang/slangpy/pull/1073#issuecomment-5168058332
- **Two sharp design constraints flagged + honored:** (a) GPU watermark must cover already-**published** results ONLY — waiting on unresolved queries would exceed the contract + could block indefinitely; (b) suggestion 2's expected count must track **`push()` SUCCESSES, not zone attachments** — `push()` drops on ring-full (`:326-328`) while `end_zone` discards that return (`:2186`) and still releases the frame count (`:2188`) → an attachment-based count would **stall finalization FOREVER**. ((b) is a genuinely subtle trap-avoidance catch.)
- **Fixer WITHDREW an earlier "land as-is, fix later" framing** — with the code owner naming the barrier a blocker, proposing deferral was scope-shrinkage; the reply commits to the work instead. Correct call.
- **✅ SCOPE CONFIRMED 08-03 15:07Z — skallweitNV: "doing both feels like the right thing to do"** → fixer implementing **BOTH** suggestion 1 (flush() barrier watermarks for sealed-frame events + GPU results) AND suggestion 2 (explicit frame completion via expected published-zone counts) in #1073. Larger change than the reorder (touches the **flush protocol + finalization semantics**) → longer cycle expected. Fixer starting implementation; will rebuild + re-run C++ profiler suite + report.
- **Blocker:** none — scope decision received (was: waiting on skallweit's both-here vs 1-here/2-follow-up).

**🔴 API-TIMEOUT STALL 08-03 15:34Z — fixer died MID-IMPLEMENTATION; Main caught + re-dispatched (does NOT self-heal).** slangpy-fixer returned `API Error: The operation timed out` ~27min after reporting "starting implementation now" (15:07) on the barrier redesign. **Main verified the damage:** (a) **NO slangpy-fixer session exists for #1073/#1072 at all** (not even stopped — grepped all sessions for group `ht5rv2`; most recent slangpy work is the #1079 session, stopped 09:49); (b) **PR #1073 head `360a90cee7` updated 15:06:39Z — BEFORE implementation began** ⇒ **nothing of the redesign was pushed; the work was in-container and uncommitted.**
- **Why it does NOT self-heal:** the host's bounded-backoff redrive covers **inbound handoffs**; here there was no pending inbound — the fixer was mid-turn on its own initiative after skallweit's scope answer. No monitor, no webhook, nothing to re-trigger it. Classic case of [[feedback_in_session_monitors_dont_survive_teardown]] generalized to whole-turn teardown. **This is exactly the at-risk class Main described to the operator when asked about API-Error recovery (08-02): inbound/webhook work resumes; in-container mid-turn work strands.**
**✅ RECOVERED 08-03 15:39Z — damage smaller than feared; WIP now pushed.** Fixer's uncommitted edits **survived on disk** in the worktree (all 6 new counter symbols intact) → committed immediately (`wip: add published/consumed counters for collector side channels`), **rebased onto current origin/main `086ca32`** (was 6 behind; clean, no conflicts, reorder + WIP both replayed), **pushed → branch tip `572e7e4`** (was `360a90c`). Adopted incremental-push going forward per Main's ask. Two findings:
1. **#1076 IS MERGED (Main-VERIFIED: `afef98626a4e...`, merged 2026-07-28T23:47:14Z)** → **BOTH guard tests are now `* doctest::skip()` on main** with `// TODO: Re-enable when …/pull/1073 is merged` ⇒ **maintainers explicitly deferred those tests to THIS PR** → re-enabling them is now in #1073's scope (fixer tracking). Also means the old "profiler suite 17/17" no longer describes main's state. **⭐ This RESOLVES the #1077 double-gate below** (skips now exist; #1077's remaining gate is #1073 merging).
2. The apparent "upstream edit to profiler.cpp" in the diff was the fixer's OWN reorder shown inverted (main lacks its commit) — no real conflict.
- **Third design constraint the fixer found by reading code (good catch):** `bound_pending_frames()` force-completes the oldest frame by zeroing `pending_gpu_count` when the window overflows → **the new expected-zone gate must be released there too**, else a frame with a dropped zone pins the window forever. (Third trap in the same family as constraint (b) — ring-full/drop paths must not deadlock finalization.)

- **Main re-dispatched** the fixer on the canonical thread with the resume state it needs (scope = BOTH suggestions per skallweit; the 2 design constraints; expect uncommitted work lost → restart from `360a90cee7`), + directed commit-early to survive the next teardown. **LESSON: after any coworker API-Error/timeout, verify (1) does a session still exist on that thread, (2) did anything actually get pushed — before assuming recovery.** Silence + no pushed head = stranded, and it's the dispatcher's to chase. ⚠️ **Nuance learned here: "no session + nothing pushed" does NOT mean the work is gone — the fixer's uncommitted edits SURVIVED on disk in the worktree.** So the right re-dispatch framing is "check your worktree first, commit whatever survived, then resume" — NOT "assume lost, restart from scratch" (I told it to assume lost; it correctly checked and salvaged). Worktrees outlive the session.

**🔴 BLOCKER ESCALATED 08-03 15:39Z — slangpy-fixer's `GH_TOKEN` is INVALID (401 Bad credentials).** `gh auth status` reports the token invalid → **fixer CANNOT post PR/issue comments or edit the PR description**. Git push works fine (host injects a working `x-access-token` for git) ⇒ **implementation is NOT blocked**, but the still-owed **PR-description correction** (the reorder-makes-device-close-deterministic overclaim) and any reply to @skallweitNV ARE blocked.
- **Main DISCRIMINATED the scope before escalating (this is the load-bearing check):** Main's OWN token reads GitHub fine (repo perms return cleanly; `/user` 403 is expected-normal for an app-style token) ⇒ **the 401 is SCOPED TO THE slangpy-fixer CONTAINER, not a fleet-wide GitHub outage.** That makes it the **OPERATOR credential-refresh class, NOT a restart** ([[project_github_actions_graphql_401_outage]], [[project_slang_fixer_auth_outage]], [[feedback_gh_auth_status_misleading]] — but note here `gh auth status` is CORROBORATED by an actual 401 on writes, so it's not the misleading-probe case).
**✅ BOTH PARTS IMPLEMENTED + PUSHED 08-03 15:51Z — branch tip `4976728` (4 commits).**
- `d38a498` **suggestion 1:** published/consumed watermarks for `sealed_frame_events` + `pending_gpu_results` folded into `flush()` completion. **GPU counters advance only on PUBLICATION** → flush never waits on unresolved queries (constraint (a) honored).
- `a46a46e` **suggestion 2:** expected published-zone count stamped into the frame marker at seal; `finalize_ready_frames()` gates on `zones.size() >= expected && pending_gpu_count == 0`. **Counts only `push()` SUCCESSES** (constraint (b)); **`bound_pending_frames()` clears the expectation on window-overflow force-complete** (constraint (c) — the fixer's own catch). **Also RE-ENABLED the two #1076-skipped tests** (in-scope per the TODO).
- `4976728` **build fix:** `a46a46e` broke the build — `flush_targets_satisfied()` is `const` and now locks the two side-channel mutexes, which weren't `mutable` (unlike `thread_mutex`/`snapshot_mutex` the same fn already locked). One-word fix on 2 declarations. Caught by BOTH CI and its local build. Ran pinned clang-format (the separate pre-commit CI red); all hooks pass locally.
- **Two safety properties the fixer VERIFIED while implementing (both load-bearing, both sound):**
  1. **No deadlock from the new gate:** `flush_targets_satisfied()` depends only on **consumption** counters, never on frame **finalization** → the expected-zone gate can delay a frame appearing in completed stats but can **never hang `flush()`**. Correct separation: `flush()` promises "published events are consumed," NOT "all frames complete."
  2. **No lock-order inversion:** new nesting is consistently `control_mutex` → side-channel mutexes; `try_finalize_global_frame` releases `sealed_frame_mutex` **before** `control_cv.notify_all()`; neither GPU publish site touches `control_mutex` inside its scope → no reverse path exists.
- Took Main's suggestion: the three drop/overflow deadlock-avoidance constraints are now a **single invariants comment block at the gate** in `finalize_ready_frames()`.
- **Next:** rebuild + tests (full profiler suite, both re-enabled tests, **30× flake probe** on the cross-thread test, full-suite regression), then codex CODE_REVIEW, peer review, then the token-blocked GitHub writes.

**🔴🔴 08-03 16:02Z — codex CODE_REVIEW found a PRE-EXISTING PERMANENT-WEDGE BUG in `end_zone` (arguably a MORE valuable find than the original #1072 fix).** Tests on the barrier/completion work came back clean FIRST (17/17 profiler cases, 142/142 assertions, both re-enabled tests genuinely executed, **0 flakes in 30 runs** with the loop's failure guard positive-controlled, no hangs, **relink proven** so no false green). Then the critique caught:
- **THE BUG (pre-existing on main, NOT introduced by the fixer — verified first-hand):** `Profiler::end_zone` (~`profiler.cpp:2237`) returns early on a zone-stack/correlation mismatch — but `begin_zone` already called `attach_zone_to_global_frame`, taking a reference on the frame's zone count. The early return **skips `release_zone_from_global_frame`** → **reference LEAKS** → frame's count never reaches zero → **frame NEVER SEALS** → no marker published → global frame stays busy → **every subsequent `begin_frame` is REJECTED**. ⇒ **A single out-of-order `end_zone` inside a global frame PERMANENTLY WEDGES global-frame profiling.** (Permanent wedge vs #1072's dropped-statistics — strictly worse.)
- **Why nobody caught it (sharp):** the existing `zone stack overflow drops only the excess zone` test DOES exercise the mismatch path but **never calls `begin_frame`** → `frame_index == INVALID_INDEX` → the leak **cannot manifest**. A test covering the path but not the state that makes it fail. (Same family as the F1/GLSL and stale-binary traps: coverage that exists but is blind to the failing configuration.)
- **FIXED:** release the reference on that path **without counting it as a published zone** (it isn't one — preserves the expected-count invariant). **+ regression test `mismatched zone end still lets its global frame seal`** — ends two nested zones out of order INSIDE a frame, asserts the frame completes AND a subsequent `begin_frame` is accepted.
- **POSITIVE CONTROL in progress (mandatory, not optional):** revert ONLY the fix → confirm the new test fails/hangs → restore → confirm passes. ("If it passed with the fix reverted it would be worthless.") Correct discipline.
- Also took codex's 2nd item: trimmed change-history/rejected-alternative narration out of the `drain()` comment (belongs in the commit message) while keeping the ordering invariant.
- **In scope justification (sound):** same *"every path must release the frame reference / not stall completion"* invariant family the expected-zone gate depends on. **Fixer will call it out explicitly for @skallweitNV** — his code, predates the fixer's changes.
**🔴 08-03 16:11Z — the LEAK FIX ITSELF was buggy; codex caught a DOUBLE-RELEASE the fixer introduced (leak → potential CORRUPTION).** Second `must-fix` round on this stage; both rounds real defects, neither cosmetic.
- **What went wrong:** releasing the frame reference on *every* stack mismatch lets the SAME reference be released TWICE. Sequence: end `outer` while `inner` still open (rejected → new code released it) → end `inner` (valid) → **present `outer` AGAIN** → it now satisfies the VALID path (`stack_index+1 == zone_depth` and the stack slot still holds its correlation id) → **releases a second time + pushes a duplicate event.** `release_zone_from_global_frame` asserts `zone_count > 0`, so in **release builds this UNDERFLOWS the packed zone count and CORRUPTS `global_frame`.** Pre-change, step 1 didn't release at all so the count stayed balanced ⇒ **the fixer turned a leak into a potential corruption.**
- **FIX (no new state — good shape):** use the thread's **zone-stack slot as the ownership marker**. Mismatch path releases only if `zone_stack[stack_index]` still holds the token's correlation id, and **zeroes the slot**; the valid path zeroes its slot too ⇒ a re-presented token can't double-release on **either** path. `correlation_id` is never 0 (high 32 bits are `timeline_id + 1`) ⇒ **0 is a safe "already released" sentinel.** Fixer traced codex's exact sequence against the edited source → now short-circuits at step 3.
- **⭐ TWO POSITIVE CONTROLS, NOT ONE (the transferable discipline):** (1) remove the release stanza → test must fail (proves it still catches the ORIGINAL leak); (2) **weaken the ownership guard to `true` → test must fail/abort/assert (proves it actually catches the NEW double-release hazard).** Fixer's reasoning: *"Control 2 is the one that matters — without it I'd be trusting an unproven claim that my test covers the bug codex described."* Also **extended the regression test to present the rejected token a second time**, which is what control 2 exercises. → captured as shared learning.
- Branch tip was `6f6724f`; guard fix follows once controls pass.

**🔴 08-03 16:29Z — THIRD defect (C), also the fixer's own, also caught by codex round 3: PHANTOM STACK SLOT (the (B) fix introduced it).** Zeroing a **non-top** stack slot as the "already released" marker left an entry still counted by `zone_depth`. Trace: `outer`@0, `inner`@1, depth=2 → reject `outer` (clears slot 0, **depth stays 2**) → end `inner` (depth→1) → **slot 0 is a cleared phantom that depth still counts.** Two consequences: (1) `zone_depth` never returns to 0 → **every out-of-order end permanently burns one of the 64 stack slots**; (2) `begin_zone` reads `zone_stack[zone_depth-1]` as the parent (`profiler.cpp:2196`) → the next zone **inherits a ZEROED correlation id as its parent → corrupted hierarchy.**
- **Attribution verified (fixer's own, NOT inherited):** pre-change the mismatch path touched neither the slot nor `zone_depth`, so `outer` stayed *legitimately live* and depth=1 was correct ⇒ **the sentinel created the inconsistency.** Good discipline distinguishing own-regression from pre-existing (cf. the (A) bug which WAS pre-existing in skallweit's code).
- **FIX:** new `ThreadData::release_finished_zone_slots()` — `while (zone_depth > 0 && zone_stack[zone_depth-1] == 0) --zone_depth;` — called on **both** end_zone paths ⇒ `zone_depth` always points just past the innermost LIVE zone. Made a `ThreadData` member (only touches that struct's state, matching the existing `push()` pattern).
- **THIRD test:** `out of order zone end leaves the thread zone stack usable` — after the out-of-order pair, opens another zone and asserts it's a **ROOT** (`parent_index == -1`), not a child of the dead slot.
- **Now controlling ALL THREE hazards independently** (A leak, B double-release, C phantom slot) — each control must fail its own target test — **AND re-running controls A and B** to confirm the (C) fix didn't undermine them. (This is the one-control-per-hazard discipline, now at 3.)
- **⚠️ BUILD-SYSTEM TRAP the fixer found + captured itself** (shared learning `1785772...`/`1785774319630-slangpy-stubgen-failure-aborts-ninja-before-linkin.md`): **`cmake --build --preset linux-gcc-debug` does NOT relink `sgl_tests`** — the **stubgen failure aborts ninja BEFORE the link**, so the stale binary keeps running and passing. The fixer's own earlier "ignore the stubgen error" guidance was **actively dangerous**. All builds now use **`--target sgl sgl_tests`** + a **mandatory mtime-vs-baseline proof**. Would bite anyone building this repo in a numpy-less container. (Sits alongside Main's stale-binary learning + the pre-existing LFS/numpy one — no duplication needed.)
**🔴🔴🔴 08-03 16:45Z — FOURTH defect, and the sharpest: the fixer's TEST FOR (C) WAS VACUOUS. The build subagent POSITIVE-CONTROLLED THE CONTROL ITSELF and proved the test didn't test what it claimed.**
- **The vacuous test:** `out of order zone end leaves the thread zone stack usable` asserted `parent_index == -1` for a zone opened after the out-of-order pair. **It PASSED with the (C) fix fully NEUTERED.** Why (instrumented): the (C) fix does change state (`zone_depth` 1→0), but `begin_zone` derives the parent as `zone_depth ? zone_stack[zone_depth-1] : 0` — and **`zone_stack[0]` was ALREADY zeroed by the (B) fix** ⇒ parent reads 0 in **both** cases ⇒ the zone is a root either way. **⭐ The fixer asserted on an observable THE BUG CANNOT MOVE.** (Note the trap's origin: the (B) fix *masked* (C)'s observable — a prior fix in the same area destroyed the discriminating power of the obvious assertion.)
- **REWRITTEN to measure the ACTUAL DAMAGE (the capacity leak):** `out of order zone end reclaims its thread zone stack slot` opens **64 zones (== MAX_ZONE_DEPTH)** after the out-of-order pair, checks every token valid, asserts **`zone_count() == 65`** (1 published inner + 64 deep). With a phantom slot `zone_depth` starts at 1 ⇒ the 64th `begin_zone` **overflows and is rejected** ⇒ 64 zones + a null token. **A real discriminator.**
- **Caught a SECOND would-be-vacuous attempt while writing it:** first rewrite asserted `producer_drop_count == 0` — **can never hold**, because the out-of-order `end_zone` itself bumps `stack_overflow_count`, which `producer_drop_count` sums in (`profiler.cpp:591`) ⇒ would have failed even WITH the fix. Fixer **checked the aggregation before trusting it.**
- Subagent also found the helper call site used free-function form (`release_finished_zone_slots(data)`) while the only declaration is a `ThreadData` member — **a compile error**; applied the one-token fix to run the protocol; now on disk and going into the commit.
- **Controls A and B RE-FIRED correctly** after the (C) rework (A: `sample_count() 0 == 1` + fatal; B: SGL_ASSERT + SIGABRT exit 134) ⇒ earlier fixes remain verified **by non-vacuous tests**. Profiler suite **20/20**; full suite **15571/15571 assertions**; only the 5 known Git-LFS fixture failures. **Nothing pushed until control C fires.**
- **⭐⭐ THE TRANSFERABLE LESSON (→ shared learning):** a positive control proves a test *can* fail; it does NOT prove the test can fail **for the right reason**. Here the control was run on a test whose assertion was blind to the bug — so the control passed *and the test was worthless*. **The subagent's move — control the control — is what caught it.** Also: **a prior fix in the same code can silently strip the discriminating power of the obvious assertion** ((B) zeroing `zone_stack[0]` made `parent_index` unable to distinguish (C) fixed-vs-broken). And: **verify a counter's aggregation before asserting on it** (`producer_drop_count` sums `stack_overflow_count`).

- **Running tally this stage: 4 REAL defects** — 1 pre-existing in skallweit's code (A wedge), **3 the fixer's own** (B double-release, C phantom slot, D vacuous test) — **every one caught by adversarial verification, NOT by a green test run.** Prior line kept for history:
- **Running tally this stage: 3 must-fix rounds, 3 REAL defects** — 1 pre-existing in skallweit's code (A), 2 the fixer's own (B, C). Nothing cosmetic. Fixer: *"I'd rather find these here than have the author find them."* Branch tip `28880e4`; (C) fix follows once controls pass. **The adversarial CODE_REVIEW pass is decisively earning its cost on this chain.**

- **⚠️ ROUTING CONSEQUENCE:** fixer flagged the PR is now **materially bigger than what slangpy-reviewer approved** → it will **re-dispatch peer review**. That re-review request routes **through Main** (fixer↔reviewer edge unreliable). **Main watching for it.** Correct call — the earlier peer-approval is superseded (reorder → barrier redesign + completion gate + a pre-existing-wedge fix).

**⚠️ NEAR-MISS ON FALSE-GREEN (worth remembering; → shared learning):** the **stale `sgl_tests` binary in the worktree dated 07-24 and still PASSES the cross-thread test** — a run against it would have reported a clean suite while testing **10-day-old code**. The build subagent caught it and **refused to report numbers without a relink**. Fixer has made **"prove the binary relinked (mtime newer than build start)"** an explicit requirement in its build brief. Same family as the zero-signal-can-be-nonzero trap.

**✅ MILESTONE 08-03 16:59Z — both authorized fixes COMPLETE, all codex stages approve, peer review re-dispatched. Branch tip `b4e50d3`, 8 commits, +205/−13** (`src/sgl/utils/profiler.cpp`, `tests/sgl/device/test_profiler.cpp`), all pushed incrementally. **Main-verified: PR #1073 is `draft:false` (PUBLIC, ready-for-review), head `b4e50d34`, base main.**
- Delivered: suggestion 1 (flush barrier over **all three** collector input channels) + suggestion 2 (explicit frame completion via expected published-zone count) + **the two #1076-skipped tests re-enabled** + **three `end_zone` defects** (1 pre-existing in skallweit's code, 2 the fixer's) each with its own **positive-controlled** regression test. codex approves PLAN+CODE+OUTPUT after **4 must-fix CODE rounds** + several OUTPUT — every one a real defect/inaccuracy, none cosmetic. **Peer review re-dispatched to slangpy-reviewer** with 4 specific second-opinion questions.
- **⭐ Two weak points the fixer DISCLOSED in the review request rather than burying** (exactly right): (1) the barrier/completion work is **NOT verified against the originally-reported #1072 symptom under real contention** — the 2 guard tests pass but are the same timing-dependent tests that passed *before* the fix too; the real evidence is the first-principles argument + the 3 new deterministic tests. (2) The slot-0 "already released" sentinel **relies on correlation id never being 0** — holds today (high half is `timeline_id + 1`) but **isn't asserted**, and a 32-bit timeline-id wrap wasn't reasoned about; asked the reviewer whether it warrants a `SLANG_ASSERT`.
- **Next:** reviewer verdict (max 2 rounds) → then the token-blocked GitHub writes.

**🔴 TOKEN NOW BLOCKING, NOT COSMETIC — and MAIN CANNOT PROXY (Main-verified 08-03 17:00Z).** Fixer took up Main's offer to post on its behalf. **Main checked its OWN slangpy permissions first: `push:false`, ALL perms false** ⇒ **Main CANNOT post to slangpy either.** ⚠️ **Main's offer was made without verifying its own capability — corrected to the fixer immediately rather than leaving it waiting on something undeliverable.** (Lesson: verify your own capability before offering to proxy someone else's blocked write.)
- **Why it's now blocking rather than cosmetic:** PR #1073 is **public + ready-for-review** with a description that is **materially wrong-by-omission** — it still claims only the snapshot reorder and implies that makes the device-close test deterministic, which skallweit correctly showed is false. **A reviewer landing on the PR right now reads a stale rationale.** Second owed write: the reply to @skallweitNV covering what landed vs his 2 suggestions **plus defect (A) — a live permanent-wedge on main in HIS OWN code** — he should hear that promptly.
- **Main re-escalated to operator with the upgraded framing** (public PR + stale rationale + an unreported live wedge in the maintainer's code = real cost to delay, not queue clutter). Both writes remain HELD by the fixer (correctly not retrying).

**✅ TOKEN RESOLVED 08-03 17:00Z — operator refresh landed; escalation STOOD DOWN.** Fixer verified directly (`gh api repos/shader-slang/slangpy` returns, PR read returns head `b4e50d34`/`draft:false`/`open`) rather than assuming. Main's `push:false` proxy limitation is moot. Fixer proceeding with both writes: **PR-description correction first, then the skallweit reply carrying the (A) pre-existing-wedge finding.** Not interrupting the reviewer (mid-re-review).

**✅ CI GREEN ON `b4e50d34` — and it's the FIRST REAL SIGNAL ON THE ORIGINAL FAILING PLATFORMS (Main-VERIFIED via check-runs API): 10 success / 3 pending / 0 failures.**
- **⭐ Both Windows msvc legs (Debug + Release) GREEN** — **Windows is one of the two platforms where #1072 ORIGINALLY FAILED** (the other is macOS ARM64). Those legs run the **two re-enabled #1076 tests**, so this is genuine evidence from the reporting platforms — which the fixer's **local Linux run structurally could not provide.**
- macOS aarch64 (Debug + Release) still `in_progress`; fixer **correctly declined to claim macOS early** ("I'll report once it completes rather than claim it early"). Verified they are indeed `in_progress`, not failed.
- **This materially UPGRADES the disclosed caveat (1)** — "not verified against the original #1072 symptom under real contention" was honest given a Linux-only local run, but green Windows legs running the re-enabled tests is better evidence than "corroboration only" implied. **If macOS returns green, caveat (1) weakens substantially** (still not a *contention* proof, but real cross-platform signal on the exact failing configurations).
- **Caveat (2) disposition:** fixer put the assert on the correlation-id-never-0 premise to the reviewer **as a question rather than pre-emptively adding it**, since it touches a hot path and the reviewer may prefer it elsewhere — will add in this PR if he agrees. (Main had weighted it as the cheaper caveat to close; fixer's routing of it as a question is the better call given the hot-path concern.)

**✅ BOTH QUEUED WRITES POSTED 08-03 17:11Z + 🟢 ALL 14 CI CHECKS GREEN on `b4e50d3`.**
- **PR description REPLACED** (https://github.com/shader-slang/slangpy/pull/1073): full scope — reported bug + reorder, the flush() barrier defect **with an EXPLICIT RETRACTION of the old determinism claim**, explicit frame completion, a table of the three `end_zone` defects **with honest origin attribution (A pre-existing, B and C the fixer's)**, and a positive-control table. `Fixes shader-slang/slangpy#1072` verified present.
- **Reply to skallweitNV** (issuecomment-5169458638): covers both his suggestions, **flags (A) as a live wedge in HIS OWN code on main**, and puts the correlation-id question to him **with two options rather than picking unilaterally.**
- **🟢 CI 14/14 PASS** — linux x86_64+aarch64 (gcc/clang, Debug/Release), **macOS aarch64 BOTH configs**, **Windows msvc BOTH configs**, pre-commit, CLA. ⭐ **macOS + Windows are EXACTLY where #1072 originally failed**, and those jobs run the two re-enabled #1076 tests ⇒ **the tests #1076 disabled now pass on the platforms that produced the original reports** — evidence the fixer's linux-only local run structurally could not give. Fixer **correctly does NOT call it proof** (still timing-dependent; a green run can't prove absence of a race) but it's materially better than one-platform corroboration. **Caveat (1) substantially weakened as predicted.**
- **🔴 THREE SELF-CORRECTIONS, all caught pre-ship — TWO of them correcting MAIN:**
  1. **⚠️ ATTRIBUTION CORRECTED (fixer declined Main's offer to take the blame, and is RIGHT — record shows it):** the wrong macro `SLANG_ASSERT` (slangpy uses **`SGL_ASSERT`**) was **introduced by the FIXER first**, in its own milestone report (*"Asked the reviewer whether it's worth a `SLANG_ASSERT`"*), and **Main then echoed it back** in its caveat-weighting. ⇒ **shared error, ORIGINATING with the fixer**; Main propagated rather than authored it. Fixer's reasoning for insisting on the correction is sound and worth keeping: **"a learning filed under the wrong owner points the fix at the wrong habit"** — and the at-risk habit is the fixer's, since **it** is the one holding both repos' conventions in context. **Rule applies to both, fixer first: don't hand over (or echo) a repo-specific identifier without grepping the target tree.** ✅ **Mitigation that genuinely holds: the wrong macro NEVER left agent-to-agent summaries** — the review request asked "worth guarding?" without naming a macro, and the **published** reply says `SGL_ASSERT`, verified against the tree (3 uses in profiler.cpp, 0 for `SLANG_ASSERT`). Nothing wrong reached a human. (Main's own lesson stands regardless: verify before echoing — cf. the `#12324`/`_INIT` breadth-of-mechanism error, same family.)
  2. **Wrap risk is BROADER than the fixer had stated:** `timeline_id + 1` is computed **in 32 bits BEFORE widening**, *and* `local_sequence` is a `uint32_t` that can also wrap — **both would have to coincide to produce zero** ⇒ **latent premise, not a live bug.** Stated precisely in the reply rather than hand-waved.
  3. **Fixer's own phantom-slot damage claim was WRONG and it corrected it:** it had said the phantom slot "makes the dead slot the parent of later zones" — but **`parent_correlation_id != 0` guards the lookup** (`profiler.cpp:832`, `:1396`) ⇒ a zeroed slot means **UNPARENTED, not MIS-parented.** ⇒ **the capacity leak is the ENTIRE damage — exactly what the rewritten test measures.** (This retroactively validates the vacuous-test rewrite: asserting on `parent_index` was doubly wrong — not just non-discriminating but predicated on a mis-stated failure mode.) Corrected in **both** artifacts before posting.
- Reviewer mid-re-review **from scratch**. Nothing further from fixer until its verdict.

- **Cross-ref:** this is very likely the same credential decaying that produced the **intermittent GraphQL-401 on slangpy-fixer** flagged 08-02 (see [[project_slangpy_1079_array_of_tensors_metal_d3d12]]) — that watch has now **escalated from intermittent-REST-workaround to hard write-block**, which was exactly my stated escalation trigger. Escalated to operator. **No Main action** (author-owned scope call on his own subsystem; fixer handling closest-to-the-state; defect recorded as shared learning).
- ⚠️ **KNOCK-ON:** #1073's scope is now materially larger (barrier redesign, not a 14-line reorder) → merge is further out, pushing **#1077** (doubly-gated on #1073 + #1076) further right. The prior "#1073 peer-approved / CI-all-green / blocked purely on human approval" state (line 17) is **SUPERSEDED** — a barrier redesign warrants fresh review when it lands.

## ⏸️ APPROVER DISPATCH DEBOUNCED 08-03 15:41Z — two `synchronize` webhooks inside one 5-min churn window
Two `pr_ready_for_review (synchronize)` webhooks arrived back-to-back, both instructing "route to the project's *-pr-approver". Main **held** the `slangpy-pr-approver` dispatch and ran the cheap inbound scan instead of a harvest+Devin+challenger cycle, per [[feedback_debounce_pr_review_on_churn]] + [[feedback_debounce_approver_dispatch_deterministic_abstain]].
- **Head moved twice, 5 min apart:** `572e7e461` (15:36:33 `wip: add published/consumed counters for collector side channels`) → `d38a4988b` (15:41:02 `Extend flush() to cover all three collector input channels`). CI on `572e7e4` is **`cancelled`** (all 12 build legs) — superseded before finishing; `d38a4988b` has `ci` **queued**, `checks`+`pre-commit` success. Deciding against a queued run on a head that will move again is exactly the moving-target burn.
- **Inbound scan clean — this is the load-bearing half of the debounce, not the diff check:** `pulls/1073/reviews` = **`[]`** — zero formal reviews ever on this PR. skallweitNV's blocker arrived as *issue comments* (5167942967 @14:52, 5168134625 @15:06), both already answered. Nothing human buried in the churn window.
- **Both new commits authored `nv-slang-bot[bot]`** ⇒ this is **our own fixer mid-implementation** of the barrier redesign, pushing incrementally **because Main directed incremental-push** after the 15:34 teardown. ⭐**Generalization: when the churn is a policy YOU imposed on your own coworker, debouncing isn't optional — eager dispatch would be self-inflicted burn.**
- **`mergeable_state: blocked`** throughout (CODEOWNERS review gate, cf. [[project_slangpy_1076_branch_protection_review_gate]]) — no approver verdict changes that.
- ⭐**Liveness came from pushed state, not the session list.** `ncl sessions list` shows NO `ht5rv2` session on the `gh-pr-…-1073` thread (the running session there belongs to `1w6l4e`), yet two commits landed 5 min apart. **Pushed head beats session presence as a liveness signal** — the exact inverse of the 15:34 stall, where absent session + unmoved head correctly meant stranded. Consistent with the GH_TOKEN 401 above: **git push works, `gh` writes don't.**
- **RESUME TRIPWIRES (named so they act as tripwires, not vibes):** (1) head quiet ≥15 min **and** the `ci` run on the settled head completes non-`cancelled`; (2) any new **non-bot** entry in `pulls/1073/reviews`; (3) fixer reports the redesign complete (suggestion 1 + 2 + all three design constraints + re-enabling #1076's two `doctest::skip()` guards). Churn cap ~2h → escalate to operator if still moving. **No nudge sent to the fixer** — it is visibly pushing; a ping mid-implementation is noise.

### Sync #3 — 08-03 15:52Z — HELD again, but INSPECTED (tripwire honored, not pattern-matched)
Third `synchronize` webhook. Per [[feedback_debounce_approver_dispatch_deterministic_abstain]] I re-ran the checks individually instead of matching "third identical webhook → hold". Head **moved twice more**: `a46a46e94` (15:45:17 `Make frame completion explicit and re-enable the profiler guard tests`) → `49767283d` (15:51:33 `Make the side-channel mutexes mutable and apply clang-format`). Still `nv-slang-bot[bot]`; **`reviews` count=0**; `mergeable_state: blocked`.
- ⚠️ **Real state change the diff-scope check alone would have missed: the PR now touches a SECOND file.** `+97/-13` across `src/sgl/utils/profiler.cpp` (+95/−9) **and `tests/sgl/device/test_profiler.cpp` (+2/−4)** — the −4 is the two `doctest::skip()` markers coming off, i.e. **#1076's deferral is being paid back inside #1073**, as scoped. So this PR is now *simultaneously* the barrier redesign **and** the #1077 un-skip. Fresh review when it lands is now doubly warranted.
- **`a46a46e94` is the only head with `checks completed/FAILURE`** (the const/`mutable` build break + clang-format red), fixed by `4976728`. All four `ci` runs before the current head are **`cancelled`** — self-superseded, never a real signal. ⭐**None of the reds/greens in this window carry information about the code's correctness; treating any of them as a verdict input would be the phantom-green trap.**
- Still 5 commits, ~15 min of continuous pushing, ~2h churn cap not near. **Approver still NOT dispatched; no nudge sent.** Tripwires unchanged.

### Sync #4 — 08-03 16:08Z — HELD; quiet-timer ARMED but reset 1 min before the check
Head → `6f6724f24` (16:07:31 `Release the global frame reference when a zone ends out of order`), `+128/-13` across the same 2 files. `reviews`=**0**, inline `pulls/1073/comments`=**0**, `mstate: blocked`. `ci` on `49767283d` again **`cancelled`**; new head's `ci` **queued**.
- ⭐**The inter-commit gap jumped to 16 min** (15:51:33 → 16:07:31) vs 4–6 min for the earlier four — i.e. tripwire (1)'s quiet window *nearly* fired, then **reset by a push 59 s before I checked**. Worth naming: a quiet-timer measured against *wall-clock at check time* is not the same as one measured against the last push; report the reset explicitly rather than letting a near-miss read as "still churning hard".
- The commit is substantive, not cosmetic — an out-of-order `end_zone` releasing its global-frame reference is the **same drop/out-of-order family** as constraints (b) and the `bound_pending_frames()` one, so the fixer is still working the deadlock-avoidance surface, not polishing. **Approver dispatch would be premature.** Churn 15:36→16:08 = 32 min of a ~2h cap.

### Sync #5 — 08-03 16:26Z — HELD; ⭐**first non-cancelled `ci` in the whole window arrived, and it is NOT a resume signal**
Head → `28880e4ce` (16:24:53 `Release a rejected zone's frame reference exactly once` — the codex-found double-release, fixed via the zone-stack-slot ownership marker at line 60 above). `+163/-13`, same 2 files. `reviews`=**0**, inline=**0**, `mstate: blocked`.
- ⭐**`6f6724f24`'s `ci` completed `success`** — the first `ci` run in this entire churn window to finish at all (the four before it were all self-superseded `cancelled`). **Tripwire (1) has TWO conjuncts and only ONE fired**: `ci` non-cancelled ✅, head quiet ≥15 min ❌ (head moved 17 min later, and `6f6724f24` is no longer the head). **A green `ci` on a SUPERSEDED head is not a verdict input** — it describes code that is already 1 commit stale, and specifically code *before* the double-release fix. Dispatching on it would be the phantom-green trap wearing a green badge. Cf. [[feedback_green_job_skipped_backend_zero_coverage]] — and note the earlier same-window lesson that none of these runs carry correctness information still holds; this one carries information about **`6f6724f24` only**.
- **Gap pattern: 16 min then 17 min** (16:07:31 → 16:24:53). The fixer has settled into ~16-min cycles, each closing a real hazard found by review (codex's double-release here). Not thrash; **iterative hazard-closure on the drop/out-of-order surface.** Churn 15:36→16:26 = **50 min of a ~2h cap.**
- ⚠️ **Known pending inbound that will land through Main (line 64):** fixer will **re-dispatch peer review** because the PR now materially exceeds what slangpy-reviewer approved. **That is a reviewer dispatch, NOT an approver dispatch** — when it arrives, route it and do not let it be mistaken for the approver tripwire firing. Approver still comes after the head settles.

### Sync #6 — 08-03 16:50Z — HELD; the hazard chain is SELF-SIMILAR and that is now the notable fact
Head → `b4e50d34d` (16:49:13 `Reclaim a zone stack slot released out of order`). `+205/-13`, same 2 files. `reviews`=**0**, inline=**0**, `mstate: blocked`, `state: open`. `ci` on `28880e4ce` completed **`success`** (2nd consecutive non-cancelled), new head's `ci` **queued**.
- ⭐**Three consecutive commits are the same bug class, each found by fixing the previous one:** `6f6724f24` release-on-out-of-order-end → `28880e4ce` release-**exactly-once** (codex found the double-release the previous commit introduced) → `b4e50d34d` **reclaim** the slot released out of order. The zone-stack-slot ownership marker (line 60) is itself generating the next hazard each time. **This is the signal that matters for dispatch timing: it is not converging on a fixpoint yet.** Approving here would decide against a barrier whose ownership protocol is still being discovered. Gaps: 16, 17, 24 min — **lengthening, but the class isn't closing.**
- **Same tripwire-(1) split as sync #5, one commit later:** `ci` non-cancelled ✅ (on `28880e4ce`) but that is again a **superseded** head; quiet-≥15-min ❌. Two greens now exist in this window and **neither belongs to the current head** — the standing caution holds and is compounding, not weakening.
- Churn 15:36→16:50 = **74 min of a ~2h cap.** ⚠️**Cap decision point ~17:36Z**: if the head is still moving then, escalate to operator per [[feedback_debounce_pr_review_on_churn]] rather than extending the hold silently. **A debounce that never re-evaluates its own cap is just a silent no-op.**

### Sync #7 — 08-03 17:26Z — CHURN RESOLVED INTO COMPLETION; tripwire (3) FIRED; **two of my own claims RETRACTED**
Head → `34226ac79` (17:25:13 `Document the flush and correlation id invariants`) — **documentation-only, the first non-hazard commit of the window** ⇒ the self-similar hazard chain (sync #6) has closed. `+214/-13`. `reviews`=0, inline=0, `mstate: blocked`. `b4e50d34d`'s `ci` = **success**; current head's `ci` **queued**.
- 🔴 **RETRACTION 1 — the `GH_TOKEN` 401 write-block is CLEARED, and I asserted it as live to the operator six times.** The fixer **posted comment `5169458638` at 17:10:39Z** (a real `gh` write) **and corrected the PR description**. My every-round line "pushes land, `gh` writes don't — blocked on a credential refresh" was **false from 17:10 onward**. ⚠️**I do NOT know the cause** — operator refresh, token rotation, or transient recovery are all consistent; do not invent one. **LESSON: a capability-negative I keep restating needs re-probing each round, not carrying forward — the very evidence that would retire it (a landed write) is cheap to check and I wasn't checking.** Direct instance of [[feedback_published_negative_env_claims_need_rederivation]], self-inflicted.
- 🔴 **RETRACTION 2 — "the two guards CANNOT gate this race" (line 23) is now the FIXER'S OWN position, stated better than mine, and it did not soften.** Its comment: the re-enabled #1076 tests "drive the collector through its background thread, so they remain timing-dependent — I'm treating them as **corroboration, not proof**. The deterministic tests plus the completion rule are the actual argument." **Each of the three `end_zone` fixes has a test verified to fail when that fix alone is reverted** (positive controls, cf. line 61). That is the strongest evidence shape in this chain and it is self-reported — **still needs independent confirmation, which is precisely the approver's job, not mine.**
- ⭐ **NEW, and the most important thing on this PR: defect (A) is PRE-EXISTING ON `main` and is independently serious.** A stack/correlation-mismatch early-return in `end_zone` skips `release_zone_from_global_frame` after `begin_zone` already attached the zone ⇒ reference leaks ⇒ frame never seals ⇒ **every later `begin_frame` is rejected: one out-of-order `end_zone` stops global-frame profiling.** Existing coverage provably could not catch it (`zone stack overflow drops only the excess zone` never calls `begin_frame`, so `frame_index == INVALID_INDEX` and there is nothing to leak). (B)+(C) were the fixer's own, introduced fixing (A) — disclosed unprompted. ⚠️**This wants its own tracking issue** so it doesn't die with #1073 if the PR stalls; **fixer is closest-to-the-state and has already disclosed it publicly**, so route rather than post.
- ❓ **OPEN QUESTION TO @skallweitNV — a real gate, not a nit.** The "already released" marker assumes `correlation_id != 0`, which is **not guaranteed by construction**: `(uint64_t(timeline_id + 1) << 32) | sequence` computes `timeline_id + 1` in **32 bits before widening**, so `timeline_id == 0xFFFFFFFF` zeroes the high half while `sequence` (a wrapping `uint32_t`) could be 0. Latent premise, not a live bug. Fixer offers `SGL_ASSERT` at the construction site vs. restructuring the marker to not depend on the value, and **declined to pick unilaterally** — correct, it's the code owner's protocol. **⇒ one more commit is plausible once he answers.**
- **DISPATCH DECISION: hold ~15 more min (to ≈17:40Z), then dispatch `slangpy-pr-approver`.** Tripwire (3) *has* fired (both suggestions + all three constraints + #1076 guards re-enabled + PR description corrected). Holding only for tripwire (1)'s second conjunct — **`ci` finishing on the head actually under review**, which has never once happened in this window (all four greens so far belonged to superseded heads). That is now ~14 min away and cheap; approving on a stale green is the one error this whole hold existed to prevent.
- ⚠️ **Peer-review re-dispatch (line 64) has NOT arrived** and the fixer's completion comment doesn't mention it. Per [[feedback_let_fixer_own_single_session]] + [[feedback_no_double_dispatch_peer_wired]] I am **not** pre-empting it — but **this is my chain to drive**: if no reviewer request lands by the approver dispatch, I dispatch `slangpy-reviewer` myself rather than let it lapse. A promised handoff that never arrives is the dispatcher's to chase.

## Follow-up chain — #1077 (re-enable disabled tests) — HELD 07-27
**slangpy#1077** "Re-enable profiler tests disabled for collector race" (jkwak-work, opened 07-27). Asks to un-skip the two guard tests once #1073 merges. Triaged → **BLOCKED/HOLD, no fix dispatched.**
- **Doubly-gated (subtlety issue text omits):** the `doctest::skip()` markers #1077 removes are ADDED by PR **#1076** ("Temporarily disable intermittent profiler tests"), which is ALSO still open/unmerged. On `main` today both tests (`test_profiler.cpp:331`, `:596`) exist WITHOUT skip. So re-enabling needs BOTH #1076 AND #1073 merged. If #1073 lands but #1076 is closed-unmerged → #1077 is MOOT (nothing to un-skip).
- **Resumption trigger:** #1073 merged AND #1076 merged (skips present). Then ~5-min mechanical revert of #1076's skip hunks + build + profiler suite + `pre-commit run --all-files` → slangpy-fixer.
- **Fixer caveats (from shared learnings):** (1) the two guards are TIMING-DEPENDENT — a green run does NOT validate #1073's fix (correctness rests on a memory-ordering argument, no new test added). (2) building the C++ profiler suite needs the git-lfs font workaround (`data/` is an LFS submodule; container has pointer stubs only).
- Triager posted 5-bullet holding note: https://github.com/shader-slang/slangpy/issues/1077#issuecomment-5092676498. Chain HELD (not closed). Shared learning recorded on the doubly-gated trap.
