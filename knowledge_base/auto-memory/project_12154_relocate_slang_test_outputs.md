---
name: project_12154_relocate_slang_test_outputs
description: "#12154 relocate slang-test generated outputs — MERGED 07-19 by jkwak (self-merge over 5× shadow ABSTAIN_POLICY, expected flow); TERMINAL"
metadata: 
  node_type: memory
  type: project
  originSessionId: fd70e7a8-3ee6-4c86-8050-24ff6a160bdc
---

shader-slang/slang PR **#12154** "Relocate slang-test generated outputs" — author **jkwak-work** (COLLABORATOR). Rewrites bare `-o` / `-separate-debug-info-output` values in slang-test directives so artifacts land beside the owning test; narrows `.gitignore` to `/tests/**`; adds CI worktree-cleanliness gates.

**Approver verdict (R1, shadow mode, ledger-only):** **ABSTAIN_POLICY** @ head `3b9dbac84023` — reason_code `CLAUSE_FAIL:no_protected_paths`. PR touches two protected CI-workflow files (`.github/workflows/ci-slang-test.yml`, `ci-slang-test-container.yml`) → deterministic protected-path clause FAILs → terminal ABSTAIN, short-circuits before challenger. Shadow mode never rounds up; nothing posted to GitHub.

**Review signal was clean/strong** (would NOT have blocked absent the policy gate): PRIMARY github-actions[bot] (production claude-code-action) = 🟡 Has issues, single 🔵 **Question** (not a 🔴); Devin structured = 0 bugs (prose over-claims, unreliable); CodeRabbit stale @af82d923, ignored. Core path-normalization logic judged correct (bounds guard, ref stability, `-o -` stdout preserve, `Path::hasPath` both-slash/`../`).

**Two items for the human maintainer (CI-workflow edits are policy-gated by design):**
1. **Description/scope Question:** util scopes to `-o`/`-separate-debug-info-output` only, but PR *description* over-claims `-dump-intermediate*` handling + a `slang-end-to-end-request.cpp` change **not in the diff**. `-dump-intermediates` is a boolean flag (no path arg) → can't be relocated by this util; still leaks to CWD under the new cleanliness gate. → fix the description OR add a `-dump-intermediate-prefix <path>` branch + unit test.
2. **check-formatting RED** at `3b9dbac` on the PR's own new file `tools/slang-unit-test/unit-test-slang-test-output-path.cpp` (trailing blank line — real, PR-caused, trivial; not gated by the relaxed policy). Author will likely push a fix → new synchronize → re-decision.

**R4 UPDATE @ `eccfc77a0732`** (settled after debouncing f165c4a→eccfc77a; distinct fresh ledger row from R1/R3 @3b9dbac): still **ABSTAIN_POLICY / `CLAUSE_FAIL:no_protected_paths`** — the two `.github/workflows/*.yml` edits remain in the diff → deterministic clause FAIL dominates, terminal, no round-up in shadow mode. jkwak's push fixed both R1/R3 items: **description/scope 🔵 Question CLEARED** (description narrowed, dump-intermediate recorded out-of-scope since no current tests use it), **check-formatting now GREEN** (trailing-blank fixed). The 3 ci_failed suites I forwarded during the push burst were all at superseded head `f165c4a`.

**Final CI at R4 head `eccfc77a` (amended after later suite `80331363618`):** ONE real failing leg — `test-windows-debug-cl-x86_64-gpu / test-slang`, single test `gfx-unit-test-tool/sharedBufferD3D12ToCUDA.internal` failing `CUDA_ERROR_ALREADY_MAPPED (208)` at `external/slang-rhi/src/cuda/cuda-heap.cpp:395` (11264/11265 passed; retried, still failed). Concurrent `check-ci` red = aggregation gate reflecting that one leg, not a second failure. **Classified: known-class GPU real-HW / inherited-CUDA-state flake — NOT real code failure, NOT infra-provisioning.** PR touches no cuda/rhi/gfx/d3d12 files; failure is in slang-rhi submodule D3D12↔CUDA interop; master intermittently red on this workflow (2 of 3 recent runs). Policy `require_ci_green=false` → not a clause input regardless. Verdict unaffected.

**Open items for the human's policy-gated look (net-additive gaps, NO 🔴, do not block merge on their own):**
- **2× 🟠 Major (CodeRabbit, approver verified vs file) on the NEW leak-detection logic in `ci-slang-test.yml`:** (1) gate uses `git status --untracked-files=all` which does NOT list `.gitignore`'d files (needs `--ignored`) → ignored root-level artifacts can still bypass the leak this PR guards; partially mitigated by narrowing `.gitignore` to `/tests/**`. (2) cleanliness check (L216) runs BEFORE "Run Slang examples"/"Run slangc tests" (L227/L237); only later check (via-glsl L272) is gated to non-macOS full-GPU release → later-step mutations unchecked on other legs.
- **5× 🟡 clarity gaps (primary claude review):** missing file-level comment; implicit `normalizeBareTestPath` contract + undocumented `..`-relative passthrough (escapes both normalization AND the CI leak check); unstated scope (only 2 of several path-writing flags normalized — e.g. `-coverage-manifest-output`, `-save-core-module`, `-depfile`; input flags like `-r` not rewritten → potential `-o`/`-r` root-vs-testdir mismatch); `++i`-skip idiom reads as possible double-increment; `.gitignore` comment half-documents design.

**R5 UPDATE @ `394aaa9c45de`** (settled after quiet-head debounce; distinct fresh ledger row R3@3b9dbac / R4@eccfc77a / R5@394aaa9c): still **ABSTAIN_POLICY / `CLAUSE_FAIL:no_protected_paths`** — `.github/workflows/*.yml` edits still in diff → terminal. Primary github-actions[bot] = 🟡 Minor, **0 correctness bugs, 3 gaps** (down from R4's 5 — jkwak addressed several clarity items; bounds-safety + CI git-clean scope re-verified). Remaining 3 🟡: header understates `Path::hasPath`/`../x` passthrough (doc); new TU still lacks file-level comment; backslash-preservation + no-parent-dir early-return untested. **The 2× 🟠 Major CodeRabbit CI-gate findings are STILL OPEN at R5** (approver re-verified vs file: `--untracked-files=all` still lacks `--ignored` at L211/L258; first cleanliness check L208 still runs before "Run Slang examples"/"Run slangc tests" L221/L231) — neither addressed this push. CI: check-formatting GREEN; sweep 22 success / 0 fail, 6 legs running; R4 CUDA-interop flake did NOT recur.

**MERGED 07-19T05:02:39Z, mergeCommit `a916653b7085`, by jkwak-work — AUTHOR SELF-MERGE.** Terminal. Approver recorded `human_verdict=APPROVED` against R5 row via join path; **join integrity verified — merged at exact R5 head `394aaa9c45de`, no drift/no push past decision head.** Calibration: our 5× ABSTAIN_POLICY / CLAUSE_FAIL:no_protected_paths across R3–R5 is **CONFIRMED-correct** — not a merge prediction, it's shadow-mode enforcing "CI-workflow edits need a human look." Merge is consistent → NOT a false-safe, NOT a false-block.

**Caveat (weak-signal, same class as [[project_12153]] self-merge):** `mergedBy=jkwak-work` = the PR author, with `reviewDecision=REVIEW_REQUIRED` (**zero independent maintainer APPROVED**). So the "human who looked" was the author himself. The 2× 🟠 Major CodeRabbit CI-leak-logic gaps (re-verified still-open at merge: `--untracked-files=all` lacks `--ignored`; cleanliness check ordered before mutating steps) **shipped unweighed** — a weak endorsement of the surfaced next-action, not validation the gaps were judged acceptable. No 🔴 was ever raised R1–R5; all open items are net-additive completeness gaps in a NEW guard, not regressions. jkwak resolved several R5 clarity gaps just before merging.

**Disposition:** No GitHub post (shadow-mode approver never posts; all review + CI signal was public for the human's look). Chain CLOSED. Related jkwak debug-info work: [[project_12147_separate_debug_info_output_block]], [[project_11983_spirv_debugfunction_wrong_cu]].
