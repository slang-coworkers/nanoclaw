---
name: project_discord_support_heartbeat_dead
metadata: 
  node_type: memory
  type: project
  originSessionId: a5790b77-160e-4f43-8acc-cc66ac7dd6c3
---

**RESOLVED 2026-07-07 ~23:18Z — heartbeat re-armed AND autonomous on-cron firing CONFIRMED LIVE.** Task `task-1783463591538-d3s5gm` (`*/5 * * * *`, verbatim CLAUDE.md guard-script) re-registered ~22:51Z. Autonomy proven by three self-advancing signals checked ~40 min later (8 cron boundaries, container idled): (a) next-fire self-advanced `22:35`→`23:20` (Main corroborated via own `list_tasks`: `at=2026-07-07T23:20:00.000Z`); (b) `.heartbeat-last-ts` self-advanced to `23:15:10Z`, past the manual `22:38:20Z` baseline, on cron cadence — a real fire ran the guard-script without an agent; (c) NO heartbeat-spawned session, which is the CORRECT result — the gate returned `wakeAgent:false` (Discord quiet, summons 6→0, CI healthy), so no wake session minted (credit-burn guardrail satisfied). **NOT the [[project_scheduler_stall_incident]] class** — next-fire advancing + state self-moving. The earlier "no fire across 22:35/40/45" was benign: `new_session` fires queue behind a continuously-`running` container mid-turn and only execute once the session idles → **heartbeat fires are NOT observable mid-turn; verify after the container idles** (the reusable lesson). Also: 6 stale June summons (all verified already-answered) backfilled with markers, `pending_summons` 6→0, closing a latent wake-loop. Autonomous Discord/CI monitoring is back after being dead since mid-May. HISTORICAL DIAGNOSIS BELOW retained for context.

---

**[HISTORICAL] slang-discord-support had NO autonomous wake since ~mid-May — its heartbeat scheduled task was unregistered. Non-button Discord events (thread @mention follow-ups, new source-channel questions) never woke it. (Opened 2026-07-07, resolved same day — see above.)**

**How surfaced:** operator (07-07) noticed two Discord support threads where a user/maintainer follow-up went unanswered:
- `1523787482578157668` (pixelsandpointers, bsdf.slang Metal `Unknown addressspace` crash) — user follow-up 07-06 21:14Z unanswered ~24h.
- `1523406940749824132` (pelagic_cormorant CB-heap crash) — maintainer **jkwaknv** asked the bot 07-06 16:42Z to file a GitHub issue; unhandled ~28h.

**Root cause (slang-discord-support's diagnosis of its own container — Main relays as its finding, not independently verified from Main's side):**
- `list_tasks` in its container → **no scheduled tasks**. The `*/5 * * * *` heartbeat pre-check documented in its CLAUDE.md is NOT registered with the scheduler.
- `memory/.heartbeat-last-ts` frozen at `2026-05-15T11:05:10Z`; `latest-report.md`/`heartbeat-log.md` last written `2026-05-14`. Heartbeat hasn't run since ~mid-May.
- The 07-06 21:14Z follow-up appears ONLY in the Discord MCP's passive `thread_replies.jsonl` — no agent inbound, no `summon_requests.jsonl` entry. An @mention follow-up does NOT mint a summon; **only the "Need help?" button does.** So the only path that could wake the bot for a non-button event is the heartbeat's periodic "new Discord messages" scan — and it's dead.
- **Failure mode = (b) never-woke (trigger/scheduling miss), NOT (a) delivered-but-ignored.** Main corroborated: discord-support's Discord session `sess-1777396566923-htyftv` `last_active` froze at 2026-07-06T20:29:42Z (the first-answer post); the 21:14 follow-up never advanced it. The 2.1.38 rebuild (07-06 15:57Z) is NOT the cause — the bot answered fine at 20:29, post-rebuild.

**Operational blast radius:** with the heartbeat unregistered, the bot only acts when (i) the summon button is clicked, or (ii) a human (Main/operator) nudges it. Every thread @mention follow-up and every new source-channel question since ~mid-May that did NOT get a button-click has been silently unmonitored (many were caught anyway because users click the button or a human maintainer replies — but the autonomous net has been down ~7 weeks). Distinct from [[project_scheduler_stall_incident]] (that froze already-registered daily tasks 07-03; this one was never registered at all since mid-May).

**Also found:** slang-discord-support's GitHub MCP is **read-only + `GH_TOKEN` invalid** — it cannot file issues or post to GitHub. Issue-filing from Discord triage must route to slang-triager. (Separate capability gap from the heartbeat.)

**Fix:** re-register the heartbeat via `schedule_task` (exact guard-script is in discord-support's CLAUDE.md; `*/5` cadence, pre-check gates cost so the agent only wakes on real Discord activity). discord-support offered to re-arm on the word. **Pending operator decision** (surfaced 07-07; recurring-cost + it's another agent's cadence, so asked rather than unilaterally armed). Once re-armed, verify `.heartbeat-last-ts` advances and a test follow-up wakes it.

**RESOLVED 2026-07-08 ~09:03Z — heartbeat re-armed AND confirmed firing autonomously.** Operator approved re-arm 07-07 22:28Z; discord-support registered `task-1783463591538-d3s5gm` (`*/5`, guard-script verified) but couldn't confirm on-cron firing within its own turn (the fire queues behind its running container — benign). I scheduled a one-shot disambiguation follow-up (`task-1783464816166-diz097`, 23:15Z, now fired+cleared) as an external occasion since a quiet-Discord fire is silent. **Confirmed at 09:00Z heartbeat: `.heartbeat-last-ts` self-advanced to 09:00:05Z — 4th consecutive autonomous wake, MCP up, 0 pending summons.** So the re-arm worked; the earlier non-fire was exactly the "queued behind running container" case, not a stall. Autonomous Discord monitoring is LIVE again after ~7 weeks dead. Fixed the latent wake-loop too (6 stale June summons that forced wake=true every fire — all verified already-answered, markers backfilled). **Standing caveat remains:** the Main scheduler-watchdog (`task-1783328238990-qikxwn`) is scoped to Main's agent group only — it does NOT cover discord-support's heartbeat, so if this stalls again nothing auto-recovers it; discord-support's "check heartbeat first each wake" habit (its `check-heartbeat-first.md`) is the only self-guard until the host-side scheduler fix lands. Related: [[project_scheduler_stall_incident]].

**POISONED THREAD — do NOT reuse `discord-support-followup-sweep-20260707` (2026-07-09).** That thread_id (which *I* created for the 07-07 sweep dispatch) is now an unroutable channel: **565 unresponded rows and growing** (150 @07-08 11:52Z → 332 @07-09 03:09Z → 565 @07-09 23:14Z). On a cron heartbeat fire discord-support can't reach the orchestrator via SendMessage (no live a2a chain), and the host `send_message` refuses to post onto this thread without an `in_reply_to`. discord-support correctly handles it — a CALM heartbeat's deliverable is its saved report files (`latest-report.md`/`heartbeat-log.md`), not an upstream ping, so it ends silently. **Implication for Main:** (a) do NOT reuse this thread_id to dispatch discord-support — open a fresh thread or use `in_reply_to`; (b) do NOT read discord-support's cron-fire silence as a problem — it's by-design for calm wakes; (c) the ONLY escalation trigger is if discord-support has a SUBSTANTIVE report it can't route (hasn't happened) — the row-count growth alone is benign accumulation, not worth re-flagging to the operator (already flagged once 07-08 11:52Z). Host-side, these accumulating unresponded sidecar/thread rows are the same class the scheduler-stall fix PR #860 addresses (seq-starvation from orphaned always-due rows) — see [[project_scheduler_stall_incident]].

**In-flight from this incident:** two repros escalated to slang-triager on 07-07 — CB-heap local-var crash, and bsdf Metal crash (verify same-cause-as-#11969 vs distinct).
- **CB-heap → FILED as #11980 (2026-07-07, Main-verified live).** Triager verdict: **NOT reproducible on ToT** (`1bd105cee`, includes #11647 merged 07-07 01:56Z) — reporter's exact cmd compiles to valid SPIR-V, exit 0; corroborates szihs. Likely already-fixed by #11647 (StorageBuffer-class rework; emitted SPIR-V now carries `SPV_KHR_storage_buffer_storage_class`) and/or #11211 (merged 06-02, related crash same surface). Cross-links #11483(per jkwaknv)/#11681/#4472 resolve. Issue ASKS pelagic_cormorant for `slangc -v` + ToT repro (open pending that). No fixer dispatched (nothing to fix until a ToT repro exists). Type/labels left unset, `reproduced` NOT applied (correct — couldn't repro), left for maintainer once version known. Main routed discord-support to post the #11980 link on the Discord thread to close jkwaknv's loop.
- **bsdf Metal crash → FILED as #11981 (2026-07-07, Main-verified live).** Triager verdict: **DISTINCT bug, NOT a dup of #11969.** Reproduced at ToT `1bd105cee` (text-only): `slangc -target metal -whole-program` on an `export`-only library (no `[shader]` entry point) with an `out`/`inout` param → `Unknown addressspace encountered` at `slang-emit-metal.cpp:1363` (same throw line as #11969, DIFFERENT producer). Root cause: `specializeAddressSpaceForMetal`→`AddressSpaceContext::processModule` (`slang-ir-specialize-address-space.cpp:359`) seeds its worklist ONLY from `IREntryPointDecoration` functions, so in library/-whole-program mode `export` funcs are never visited → their mutable-ref params keep `AddressSpace::Generic` → emitter default-arm throws. #11969 is a vertex-only stage gate in `legalizeEntryPointVaryingParamsForMetal` (different pass/file); neither fix subsumes the other → cross-linked siblings. Labels `Metal`/`bug`/`reproduced`, Type=Bug, 5-bullet triage comment posted (4909202916). **Fixer briefed** (rec: Approach A = also process exported funcs + default unspecialized mutable-ref param to `thread` addrspace; add -whole-program regression test for out+inout exported params). Chain open awaiting fixer's [Fix Report] — do NOT double-dispatch (fixer peer-wired by triager). Both Discord threads from this incident now fully run to ground.