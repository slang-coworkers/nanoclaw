---
name: project_github_actions_graphql_401_outage
description: "⚠️ FLAPPING/PARTIAL (NOT resolved) — GitHub gateway token: onset 07-16 15:00Z; ~18:00Z reads+GraphQL briefly OK; by 07-17 05:40Z re-degraded = reads/actions-READ/job-logs OK but GraphQL 401 + actions-WRITE (rerun/requeue) 'Must have admin rights' STILL DOWN ~14h. Read-capable, write-handless fleet-wide; operator must re-auth gateway cred (GraphQL + actions-write paths); NOT restart-fixable"
metadata: 
  node_type: memory
  type: project
  originSessionId: ddab6188-0f28-4222-8b37-6ae1cbb4b414
---

**⭐⭐⭐ 08-03 18:3xZ — THE WHOLE "AM I AUTHENTICATED?" FRAMING IS WRONG: INJECTION IS PER-PATH AT THE PROXY, AND YOUR OWN HEADERS ARE IRRELEVANT. Main-derived, 3-way controlled.** `HTTPS_PROXY`/`HTTP_PROXY` are set to `http://x:aoc_…@host.docker.internal:10255` (+ `NODE_EXTRA_CA_CERTS=/tmp/onecli-gateway-ca.pem`) ⇒ **every** egress — `gh`, `curl`, `urllib` alike — traverses the OneCLI gateway, which injects the credential **only on paths with a secret rule**:

```
                               HTTP  limit   body.message      ← ⚠️CORRECTED, see below
repos/shader-slang/slang-rhi   200   6000    (none)            ← rule ⇒ injected + accepted
repos/torvalds/linux           401   <none>  "Bad credentials" ← cred FORWARDED and REJECTED
repos/microsoft/vscode         401   <none>  "Bad credentials" ← same
rate_limit                     200   60      (none)            ← reached GitHub anonymously
```
⛔**THIRD RETRACTION, mine: I wrote `torvalds/linux` off as "no rule ⇒ not even proxied through" from the ABSENCE OF A HEADER — it is HTTP 401 `{"message":"Bad credentials"}` from GitHub.** A genuinely unauthenticated GET on a public repo returns **200**. So a credential *was* forwarded there and *rejected* ⇒ **"no `X-Ratelimit-*`" means the request FAILED, not that it left un-injected.** My "no-rule path" was therefore **not a control either** — same failure mode as the two probes it was correcting, one layer down (approver-caught, then Main-verified with status+body).
⭐⭐**Distinguish "the control returned nothing" from "the control FAILED." Read the BODY, not just status or headers** — a 401 body, an HTML error page, and a real anonymous 200 all look like "no data" through a header grep.
⇒ ⛔**RETRACTED (mine, and I SENT IT TO THE APPROVER): "core limit 60 = anonymous / 5000 = injected" reads a PER-PATH property as a PER-CONNECTION one.** `rate_limit` shows 60 because *that path* has no rule — it says nothing about the connection. Same session I called `.permissions`-presence insufficient and then replaced it with a check that is wrong in a *subtler* way. ⚠️**A bad recipe handed downstream is worse than one kept: correct it on the same edge you sent it.**

⛔**The approver's "no `Authorization` header at all and `.permissions` still True ⇒ presence carries no information" inference is INVALID** — its request was still proxied and **still injected** (`X-Ratelimit-Limit: 6000` on that very call). It removed a header the proxy re-supplies, so the experiment could not test what it claimed. ✅Its *conclusion* is right for a different reason: `.permissions` appears on any **public** repo regardless. ⭐**Removing your own credential does not create an unauthenticated request when a transparent proxy owns egress.** ⚠️**But my proposed replacement control (`repos/torvalds/linux`) was ALSO invalid** — it 401s (see the corrected table above). **Three probes, ONE failure mode: each assumed a condition it never checked** — *no header ⇒ unauthenticated* · *no rate-limit header ⇒ not proxied* · *this path's limit ⇒ the connection's limit*. Cf. [[feedback_published_negative_env_claims_need_rederivation]]: probe with the capability, **and verify the control is a control.**

✅**Correct probe, verified working under this outage:** `gh api -i repos/<org>/<repo>` → read `X-Ratelimit-Limit` (6000 = injected on that path) **and** `X-Accepted-Github-Permissions`. Never `rate_limit`; never `.permissions`; never a header-removal experiment.

**⭐⭐ 08-03 18:2xZ — THE DISCRIMINATOR IS THE `X-Ratelimit-*` RESPONSE HEADERS ON A WORKING REST CALL, NOT `gh api rate_limit`.** `rate_limit` **never reaches GitHub** on this connection — it returns a OneCLI error body (`{connect_url, error:"app_not_connected", message, provider}`, HTTP 401). Main-probed own token 18:2xZ:

```
gh api -i repos/shader-slang/slang-rhi | grep -i x-ratelimit
  X-Ratelimit-Limit: 6000     X-Ratelimit-Remaining: 3679
  X-Ratelimit-Used: 2321      X-Ratelimit-Resource: core
```
⇒ REST is **authenticated** (6000 tier; anonymous would be 60) and **quota NOT exhausted**. Same call: `.permissions` all-**false**, `X-Accepted-Github-Permissions: metadata=read`; GraphQL `{viewer{login}}` = **401 Bad credentials**. Three endpoints, three behaviors ⇒ injection is **path-specific**, not a single dead credential.

⛔**RETRACTED from the 18:1xZ entry below: "cleaner discriminator: `rate_limit` core limit 60 = anonymous / 5000 = injected."** That recipe is **UNEXECUTABLE during this outage** — `rate_limit` returns no numbers at all. Use the response headers. (Same failure as [[feedback_narrowing_is_not_testing_check_own_store]]: I wrote a check that cannot run.)

**✅ 08-03 18:2xZ — MAIN RE-DERIVED on its own edge (Orchestrator container), and the refutation above HOLDS with fresh numbers.** `gh api -i repos/shader-slang/slang-rhi` → `X-Ratelimit-Limit: 6000`, `Remaining: 3007`, `Used: 2993`, `Resource: core`, `X-Accepted-Github-Permissions: metadata=read`. **The OneCLI `app_not_connected` error body contains NO 6000-shaped number** — only `10254` (its own port, ×3) and `401` ⇒ **no caller could derive `Used: 6000/6000` from it.** ⇒ the babysitter read a **genuine GitHub header**; the triager's misread hypothesis is refuted a second time, independently.
⚠️ **DELTA for the operator: the exhaustion has CLEARED** (3007 remaining). The live escalation's "secondary REST rate-limit exhaustion" framing describes a **transient window, not a standing state**, and leaving it uncorrected points the operator at the wrong constraint. **Standing** problem is only: GraphQL `401 Bad credentials` + `rate_limit` unreachable (`app_not_connected`).
**Fleet-wide, NOT per-agent — all four probes reproduce on Main's edge:** `rate_limit` → `app_not_connected`; REST repo/issue/comment reads → **fine**; `gh api user` → **403 `Resource not accessible by integration`** (the signature of a **GitHub App installation token** — right lens for the 401, better than "expired credential"); `graphql {viewer{login}}` → **401**. Also confirmed: `connect_url` is emitted as **`0.0.0.0:10254`** ⇒ container-internal, **not reachable from an operator browser as-is** — must be handed over as `127.0.0.1`.
🔴 **Separately, and genuinely per-agent:** slang-fixer's critique gate **blocks read-only `gh api`** (and re-armed 3× on edits to its own private memory files). Read-only `gh api` works on **both** Main's and the triager's edges ⇒ **gate config difference, NOT the token.** It pressures toward memory-substitution exactly when verification matters — see [[project_slang_rhi_805_license_readme_mismatch]] (blocked call ⇒ UNKNOWN, not UNCHANGED). **Own escalation, mine to file.**

**❌ Triager's "the 6000/6000 reading was a misread OneCLI error body" hypothesis — REFUTED, and the refutation matters more than the hypothesis.** The OneCLI error body carries **zero numeric fields** (verified above), so no caller could derive `Used: 6000/6000` from it; and my own limit is **exactly 6000**, matching the babysitter's figure. ⇒ the babysitter's reading was a **genuine GitHub header**, and the 12:3xZ exhaustion was real. ✅Triager's *other* two corrections STAND: (1) REST quota is **not** exhausted now — the exhaustion was a transient window, not a standing state, so "secondary rate-limit exhaustion" overstates it as an ongoing condition; (3) **two distinct failure modes must not be conflated** — OneCLI `app_not_connected` (proxy-level, reconnect fixes) vs GitHub GraphQL 401 (token-level, a reconnect may NOT fix). ⚠️**Do not let the operator treat one reconnect as closing both.**

⭐`gh api user` → `Resource not accessible by integration` is the **expected** signature of a GitHub **App installation token** (an App has no `/user`) — not a bug, and the right lens for the GraphQL 401 (App-token GraphQL provisioning), not "generic expired credential." Triager-contributed.

⚠️**The connect URL is emitted as `http://0.0.0.0:10254/...` — a container-internal bind address, NOT reachable from an operator's browser.** Substitute `127.0.0.1` before sending it to a human, or the first click looks broken. (Already noted 07-22; re-confirmed by triager 18:20Z on its own edge — so it's still being emitted wrong.)

## ❌ 08-04 00:1xZ — 401 at 22:00Z, 00:00Z **and** on my own re-probe. The 20:00Z "recovery" was ONE GREEN SAMPLE, not a recovery.

**Main-verified 00:1xZ:** `graphql {viewer{login}}` → **401 "Bad credentials"**; REST control → 200 (`repos/shader-slang/slang`). Babysitter independently 401'd at 22:00Z and 00:00Z with `X-Ratelimit-Remaining 5131/6000` — so **not** rate-limit exhaustion, genuinely the credential facet.

⭐**The rule both of us converged on, and it's the durable one: ONE GREEN PROBE NEVER ESTABLISHES RECOVERY ON A FLAPPING FACET.** A single success is a sample from a distribution, not a state transition. To call recovery on a flapping subsystem you need either repeated greens across a window, or an *explanation* of what changed (operator reconnected, cron fixed). We each made the same error from opposite directions — the babysitter reported recovery at 20:00Z; I verified per-facet, correctly narrowed "outage over" to *partial*, and still relayed the GraphQL half upward as recovered. **Per-facet verification was necessary but not sufficient; a flapping facet also needs a per-TIME caveat.**

**Standing consequences while this flaps:** REST-only sweeps mandatory (`gh pr checks` would phantom-green all 75 PRs); `mergeQueueEntry` merge-queue idempotency unavailable; `evicted:[]` in the wake payload is manufactured absence ⇒ cross-check REST `merge_group` every sweep (did so 00:00Z — 3 pre-22:00Z failures #12303/#12299/#11667, **all verified `merged=true`**, nothing owed).

## ❌ 08-03 22:2xZ — **RE-REGRESSED. The 20:2xZ "recovery" held ~2h at most. I had already told the operator it recovered — corrected.**

**Main-verified 22:2xZ:** `graphql {viewer{login}}` → **401 "Bad credentials"**; `repository.pullRequest.mergeQueueEntry` → **401** (both returned real data at 20:2xZ). REST healthy (`repos/shader-slang/slang` → 200). ⇒ **this facet FLAPS on a ~2h scale.**

⚠️ **Operational consequence, and it is the dangerous one:** anyone who *selects transport* based on a stored "recovered" note gets **phantom all-green** from `gh pr checks`. ⇒ **Re-probe GraphQL at the start of every run; never carry a recovery forward across sessions or even across hours.** A recovery note is a timestamped observation, not a capability fact.

⭐**This is the 07-17 premature-recovery error recurring at a shorter timescale, and I made it despite documenting that exact lesson 2h earlier in this same file.** At 20:2xZ I correctly narrowed the babysitter's "outage is over" to a *partial* recovery — verified per-facet, found `rate_limit` and `--paginate` still broken — and still reported the GraphQL half as recovered to the operator without flagging that a flapping outage's recovery is itself provisional. **Per-facet verification was necessary but not sufficient: a flapping subsystem also needs a per-*time* caveat.** The babysitter caught the regression and corrected me; its "re-probe every run" instinct is the durable form.

## ⚠️ (superseded 22:2xZ, kept for the arc) 08-03 20:2xZ — PARTIAL RECOVERY after ~52h. GraphQL back; two REST facets still down.

Babysitter reported the outage **over**. Main-verified — and it is **partial**, which matters because the still-broken facets are the two that fail *silently*:

| probe | state |
|---|---|
| `graphql {viewer{login}}` | ✅ **`nv-slang-bot[bot]`** (was 401 for ~52h) |
| `graphql … pullRequest.mergeQueueEntry` | ✅ real data — `{position:2, state:"AWAITING_CHECKS"}` on #12299 |
| **`gh api rate_limit`** | ❌ **still 401 `app_not_connected`**, connect URL `agent_name=Orchestrator` |
| **`gh api --paginate` page 2+** | ❌ **still 100-then-401** on slang#12208's 126 check-runs |

⇒ **The GraphQL plane recovered; the OneCLI connection is still not fully healthy.** Consequences that survive the recovery:
- **The `--paginate` silent-truncation hazard is LIVE.** Keep explicit `?page=N` + `total_count`/raw-page-length reconciliation ([[feedback_gh_paginate_401s_on_page2_use_explicit_pages]]). This is the facet that reads as success, so it is the dangerous one.
- **`rate_limit` is unreadable**, so the "check `Remaining` before fan-out" mitigation adopted at 12:3xZ **cannot be executed** — the secondary rate-limit-exhaustion risk is unmitigated even though GraphQL is back.
- ⭐**The `rate_limit` core-limit discriminator (60 = anonymous / 5000 = injected) recorded at 18:1xZ is itself unavailable while `rate_limit` 401s.** A diagnostic that depends on the failing subsystem is not a diagnostic. Use `graphql {viewer}` + a REST `.permissions` read together instead.

**Operator ask NARROWED, not withdrawn:** the GraphQL half no longer needs chasing; the OneCLI GitHub connection for `agent_name=Orchestrator` still does. **Not re-escalated as urgent** — impact dropped sharply now that GraphQL and REST single-page both work.

⭐**Lesson: "recovered" needs per-facet verification, exactly like the 07-17 premature-recovery call.** A multi-facet outage recovers in pieces; confirming the loudest facet and declaring victory is how the 07-17 error happened. The babysitter checked the two facets it needed and generalized; **its own permanent-REST-recipe instinct was right for the wrong reason** — not because `gh pr checks` might phantom-green in future, but because `--paginate` is *still broken right now*.

**⚠️ 08-03 ~18:1xZ — STILL DOWN, 4th agent this window (approver). NOT re-escalated: no material delta.** slang-pr-approver reported GraphQL 401 on slang-rhi#803. Main-verified own container: `gh api graphql {viewer}` **401 Bad credentials**; raw `POST api.github.com/graphql` **401**; `gh api rate_limit` = **`app_not_connected`**, connect URL `agent_name=Orchestrator`. REST healthy (`pulls/803` head via `gh api` exit 0). ⭐**Load-bearing new datapoint: my OWN GraphQL query SUCCEEDED earlier this same session** (returned `author{login __typename}` on #803 reviews) and 401s now ⇒ **fresh mid-session onset, not a per-agent config fault** — it discriminates "outage" from "that coworker's credentials," which need opposite responses (operator re-auth vs. fix your setup). Told the approver its report is corroborated, not its problem. ⚠️`rate_limit` core shows **limit 60** (unauthenticated tier) while `.permissions` **IS present** on `repos/shader-slang/slang-rhi` — so the canonical `.permissions` probe from [[feedback_gh_auth_status_misleading]] **can read as authenticated while GraphQL is dead**; on a *public* repo the field appears regardless, so `.permissions`-presence is NOT sufficient alone. Cleaner discriminator: **`rate_limit` core limit 60 = anonymous / 5000 = injected.** Operator ask unchanged and already live from 05:00Z/12:4xZ/14:2xZ today (reconnect GitHub in OneCLI + fix App-token-refresh cron); a 5th ping adds nothing.

**⚠️⚠️ 08-03 14:2xZ — THIRD-ORDER HARM CONFIRMED: `--paginate` page-2 401 silently truncates sweeps (phantom-green).** Main-reproduced on slang#12208 (126 check-runs): `gh api --paginate .../check-runs?per_page=100` → **100 items then 401 `app_not_connected`**; explicit `?page=2` → the remaining **26**. Babysitter measured 4/4 fail vs 6/6 success. **The danger is invocation-form-dependent:** `gh api --paginate … --jq` exits **1** (detectable), but `gh api --paginate … | jq` exits **0** because the pipeline reports *jq's* status — so a sweep piping into jq gets a truncated answer with a success code, and `[]?` swallows the error doc on top. **6 of 54 non-draft slang PRs exceed 100 checks (up to 135)**; re-swept with explicit pages, **no hidden reds ⇒ no prior wrong action**, but earlier sweeps had a real phantom-green window. Fix: explicit `?page=N` + reconcile against `total_count` (page 1 supplies it) + fail loudly on mismatch. Full detail: [[feedback_gh_paginate_401s_on_page2_use_explicit_pages]]. ⚠️ The 08-01 16:05Z note below already recorded "`--paginate` returns page-1 then 401s" — **what was missed then was that it fails SILENTLY in the common pipeline form.** Recording a symptom is not the same as recording its detectability.

**⚠️⚠️ 08-03 12:3xZ — SEVERITY ESCALATED: this outage now causes SECONDARY REST RATE-LIMIT EXHAUSTION. No longer merely verification friction.** slang-ci-babysitter hit **`403 API rate limit exceeded`, `Used: 6000/6000`** (installation `122982130`) mid-task, self-inflicted by its own 12:00Z sweep: 75 PRs × paginated `check-runs` + combined statuses at `-P8`, plus a 42-PR strand scan at 2 calls each. **Causal chain: GraphQL 401 ⇒ `gh pr checks` / `gh pr view --json` phantom-green on empty stdout ⇒ every sweep MUST fall back to REST ⇒ per-sweep REST cost roughly DOUBLES ⇒ shared hourly budget exhausted.** ⇒ **standing risk on every 2h sweep, not a one-off.** Babysitter waited for `X-Ratelimit-Reset` rather than hammering (correct); mitigations adopted: check `Remaining` before fan-out, drop per-PR `reviews` for non-approval-candidates, lower parallelism. ⚠️ **The 05:00Z entry below called a rate-limit hit "unrelated, self-clears" — that was WRONG.** It is a *consequence* of this outage and is now demonstrably load-bearing (it can blind a sweep mid-flight). **Re-escalated to operator 12:4xZ as a genuine delta** (state materially changed since 05:00Z), not a reflexive re-ping. Same fix: operator reconnects GitHub in OneCLI + App-token-refresh cron. Surfacing sweep: [[project_12116_dxc_prebuilt_zip_500_fetch_flake]].

**⚠️ 08-03 10:09Z — SAME FLAP WINDOW, now confirmed on slang-triager's token too (multi-agent, consistent with App-token-refresh-cron death rather than per-agent).** Triager reports GraphQL 401 persisting "several sessions"; REST unaffected. **Concrete capability loss beyond verification friction: Issue-Type setting is GraphQL-only (`updateIssue` mutation) — it blocked setting Issue Type on slang#12313 and #12317.** Also dead: the `gh pr view` / `gh issue view --json` paths (GraphQL-backed → empty stdout → false-green risk, per the 08-01 note). Triager did all slang-rhi#805 verification via REST and said so explicitly — good practice; treat any coworker's state claims this window as REST-backed only unless they state otherwise.

**✅ REST substitution recipe (triager-authored 08-03, worth running even when GraphQL is healthy).** The load-bearing substitution is NOT the field-name mapping — it's that `gh api repos/{o}/{r}/pulls/{N}/reviews` exposes each review's **`commit_id`**. Comparing `commit_id == pulls/{N}.head.sha` proves an approval **BINDS to current head** rather than sitting stale on an earlier commit. That is a strictly stronger check than GraphQL's `reviewDecision: APPROVED`, which can't distinguish the two. Used to confirm skallweitNV's approval binds on slang-rhi#806 ([[project_slang_rhi_805_license_readme_mismatch]]).

**⚠️ RE-FLAP 08-03 ~05:00Z — GraphQL 401 / REST+search healthy (Main-verified).** The daily Slang PR-report scheduled job (`slang-pr-report/scripts/pr_report.py`, coworker `ag-1776713258088-r8pp2t`, 05:00Z weekday fire, target Discord #slang-committers 1352357976878481468) exited 1 and correctly WITHHELD (no partial post). Coworker MISDIAGNOSED root cause as "GH_TOKEN invalid → rotate token" citing `gh auth status`/`gh api user`/token-len-23 — all the known-misleading probes ([[feedback_gh_auth_status_misleading]]). Main-verified TRUE state: REST `repos/shader-slang/slang` `.permissions` object present (token injected), `search/issues` total_count=231 exit 0, `pulls?state=open` → #12312 exit 0 — reads fully healthy; **GraphQL `{viewer{login}}` = 401 "Bad credentials"**. `pr_report.py` is **GraphQL-primary** (`gh.graphql(_PR_QUERY)` L609, org preflight probes via `gh api graphql (organization)` L478-480) → that's why it fails while REST works. Same recurring OneCLI GitHub-connection flap. Fix = operator reconnect GitHub in OneCLI (connect URL) + App-token-refresh cron; NOT GH_TOKEN rotation, NOT restart. Escalated dashboard 08-03. Corrected coworker's diagnosis on its edge. (Also hit shared 5000/hr gh rate-limit mid-probe — unrelated, self-clears.)

**⚠️ RE-FLAP 08-01 10:05Z — GraphQL 401 / REST healthy AGAIN (Main-verified + babysitter-reported; scope possibly broader than Orchestrator-only this time).** babysitter sweep (msg 204) flagged GraphQL 401 "Bad credentials" while REST fully healthy — DANGEROUS because `gh pr checks`/`gh pr view --json` are GraphQL-backed → return empty stdout on 401 → a naive scan FALSE-GREENS every PR (babysitter re-ran whole sweep via REST `commits/<sha>/check-runs` = complete+correct). **Main-verified own connection:** `gh api graphql {viewer}` = 401 Bad credentials; `gh pr view 12289 --json` = HTTP 401; REST `repos/shader-slang/slang` = 200 OK; `gh run rerun` (REST) works; `rate_limit` = OneCLI `app_not_connected` `agent_name=Orchestrator`. **SCOPE CAUTION:** prior flaps (07-22/23) were reconciled as ORCHESTRATOR-only; but THIS time the babysitter reports GraphQL-401 on ITS OWN sweep too → may be BROADER (2 agents' connections) — NOT asserting fleet-wide (can't probe babysitter's cred directly), flagged as possible to operator. **Tooling impact on Main THIS SESSION: my `gh pr view --json`/`gh pr checks` verifications are UNRELIABLE until reconnect — switch to REST (`gh api .../commits/<sha>/check-runs`, `.../pulls/N`) for all state-verification.** Operator-actionable: reconnect GitHub in OneCLI (connect URL, `agent_name=Orchestrator`) + check the App-token refresh cron (babysitter's ask); NOT restart-fixable. Escalated to dashboard 10:10Z. **08-01 10:17Z — BROADER-THAN-ORCHESTRATOR CONFIRMED:** babysitter reported its OWN `gh api graphql {viewer}`=401 + `rate_limit`=`app_not_connected` + REST 200 → 2 agents' connections down this recurrence (vs Orchestrator-only in 07-22/23 flaps) ⇒ consistent with App-token-refresh-cron death (recurring facet), not per-agent. Also confirmed GraphQL-401 is **read-plane only**: merge queue functioned through it (#12263 merged 07:09:29Z via merge_group all-success; enqueue/dequeue/`gh run rerun` all worked). So during THIS outage class: REST reads + actions:write + merge-queue all UP; only GraphQL reads dead (danger = `gh pr checks/view` empty-stdout false-green → use REST `commits/<sha>/check-runs`). Operator ask stands (reconnect + fix refresh cron).

**08-01 16:05Z — ⚠️ WORSENING (Main-verified): REST now degrading too, not just GraphQL.** babysitter (msg 208) reports 4+h persistent + REST `--paginate` fully 401-failing + single-page needs 10-12 retries. Main confirmed: REST single-page `repos/shader-slang/slang` = 200 OK, but **`--paginate` returns page-1 (100 items) then page-2 401s `app_not_connected`** (partial-then-fail = paginate genuinely broken); GraphQL still 401; rate_limit = app_not_connected. So the degradation ladder this window: GraphQL fully dead → REST paginate dead → REST single-page flaky (retries). **RISK: REST is the fallback everyone (babysitter/approver/me) uses during GraphQL outages — if single-page crosses fully offline, agents go BLIND.** RE-ESCALATED to operator w/ worsening delta (not a reflexive re-ping — state materially changed since 10:10Z). Same fix: reconnect GitHub in OneCLI + App-token-refresh-cron. My own reads: single-page REST still OK, avoid `--paginate` this window.

**⚠️ RE-FLAP — actions:write down on UPSTREAM via OneCLI GitHub-connection disconnect (07-22 10:20Z, Main-verified with a REAL write, receipts in hand):** babysitter sweep (msg 6) reported `gh run rerun --failed` = 403 "Must have admin rights" on 3 upstream runs + `rate_limit` OneCLI 401. Main verified independently — NOT a misleading probe: (1) reads HEALTHY (GraphQL `viewer=nv-slang-bot[bot]`, REST `repos/shader-slang/slang` 200); (2) `gh api rate_limit` = OneCLI **`app_not_connected` HTTP 401** with connect URL `http://127.0.0.1:10254/p/jdeqknpbgutadefo/connections?connect=github&source=agent&agent_name=Orchestrator` (host emits 0.0.0.0; real dashboard 127.0.0.1); (3) **the ACTUAL legitimate rerun the babysitter wanted — `POST repos/shader-slang/slang/actions/runs/29904991503/rerun-failed-jobs` (#12174 head 20abf012, pre-test Common-Test-Setup infra flake, APPROVED) — returned 403 "Must have admin rights to Repository."** So actions:write is genuinely down on the upstream App/OAuth path. **DISTINCT FACET from this morning's 07:29Z fix:** that restored the `slang-coworkers/*` **vault USER-PAT** (fork push/PR/merge — validated via #1003); THIS is the OneCLI **GitHub OAuth *connection*** (`connect=github` for agent Orchestrator) that injects GH_TOKEN for `gh api` against upstream → disconnected. Fresh regression (reruns worked 07-19, `run_attempt=2` exists on this very #12174 run). Impact: babysitter can classify but can't rerun/requeue anything on upstream; approver can't merge-queue; #12174 legit rerun queued 0/3. **Operator-actionable, NOT restart-fixable** (per-request injection hits the same disconnected connection): operator opens the connect URL to reconnect GitHub in OneCLI. Escalated to orchestrator-dashboard + PushNotification 10:20Z.
  - **07-23 19:55Z SCOPE RECONCILIATION — disconnect is ORCHESTRATOR-AGENT-scoped, NOT fleet-wide.** The `app_not_connected` 401 connect URL names `agent_name=Orchestrator` — it's *Main's own* OneCLI GitHub connection that's disconnected, not a shared fleet cred. Proof other agents' actions:write is HEALTHY: (a) **slang-fixer fired `gh run rerun --failed` on #12202 at 19:50:01Z — verified landed** (CI run 30033260546, `run_attempt=2`, queued); (b) **slang-ci-babysitter has been firing reruns successfully all of 07-23** (msg 14 00:00Z #12182, msg 16 02:05Z, msg 18 18:05Z 2 jobs). So the 07-22 10:20Z framing "actions:write down for the bot on upstream" was OVER-BROAD — each agent has its own OneCLI GitHub connection; only Orchestrator's is down. Impact is now near-nil: Main rarely needs actions:write (comment-POST works fine, which is all merge-queue nudges need); babysitter/approver/fixer are NOT blocked. Operator ask DOWNGRADED from "urgent fleet outage" to "low-pri: reconnect Orchestrator's OneCLI GitHub when convenient." Corrected on dashboard.
  - **12:05Z path-split refinement (real writes both attempted):** the outage is **actions:write-specific**, NOT a blanket write-block — the **issue-comment REST POST path is UP**. Confirmed by landing the #12151 merge-queue nudge (`POST issues/12151/comments` → 200, comment 5045635667) in the SAME window that `rerun-failed-jobs` returns 403. Same path-specific pattern as the 07-17 06:11Z window (REST core/comments/PR-create alive, actions-write/GraphQL dead). So during this outage class: **comments/labels/PR-create via REST still work; only `gh run rerun`/`requeue`/merge-queue-enqueue (actions:write) + possibly GraphQL are dead.** Merge-queue nudges (which are comments) CAN still be posted; the actual hand-requeue still needs a human (bot can't self-enqueue regardless).

**⚠️ RE-FLAP — INVERTED FACET: git-push OK, gh-api GH_TOKEN fully dead (07-22 ~03:1xZ, Main-verified this nightly kb-sync session):** the nightly `knowledge_base` sync (this task) got through STEP 6 cleanly — `git fetch`/`reset --hard` READs 200 AND **`git push -u origin kb-sync-20260722` SUCCEEDED** (branch landed on origin, 286 files/5993+/-870). But STEP 7 (REST PR-create via `gh api -X POST .../pulls`) returns **HTTP 401 "Bad credentials"** across 3 retries, and characterization shows the **GH_TOKEN path is fully invalid**: `gh api user` = 403 "Resource not accessible by integration", `gh api repos/...` = 401 "Bad credentials", `gh auth status` = "The token in GH_TOKEN is invalid." So this flap is the INVERSE of the 07-18/07-20 variant — there git-push was 401 while gh-api reads were 200; **here git-push is healthy while the entire gh-api GH_TOKEN surface is dead.** Confirms git-push and gh-api(GH_TOKEN) are SEPARATE credentials that flap independently. STEP 7–8 (PR create + REST merge) blocked (no read-only workaround for a write). **Nothing lost:** branch `kb-sync-20260722` is durable on origin; operator can land it by re-auth'ing the gh-api/GH_TOKEN gateway cred then re-running `gh api pulls` create + `gh api pulls/N/merge` (or `gh pr create`/`merge` if that path recovers), OR just merging the already-pushed branch. Reported failing STEP 7 to orchestrator-dashboard. Operator ask: re-auth the GH_TOKEN/gateway cred (NOT restart — per-request injection hits the same dead cred).

**✅ CRED RESTORED + VALIDATED 2026-07-22 07:29Z (operator-triggered revalidation, subagent-run; ALL 3 slang-coworkers/* write paths GREEN).** Operator (dashboard msg 54396) reported the deleted `api.github.com /repos/slang-coworkers/*` USER-PAT + `github.com /slang-coworkers/*` git-push secrets were RESTORED (nv-slang-bot PAT regenerated) and asked for a validation run. Main ran a fresh kb-sync (off current `nv-coworkers` head `776d7b7e`, NOT the stale kb-sync-2026072x branches which are behind head + would've reverted #1002). Result: **git-push (exit 0, branch created) · REST PR-create (#1003) · REST squash-merge (merged:true, sha `c7948cec`) ALL SUCCEEDED.** Diff was empty (kb already current via #1002 `kb-wiki-fold-20260722` merged 07-22 06:32Z) → empty-commit probe exercised the cred paths; PII audit 0 emails. So the `slang-coworkers/*` write-credential outage (07-16 onset, flapped through 07-21) is **RESOLVED**. ⚠️ Note the auth-probe caveat still holds ([[feedback_gh_auth_status_misleading]]) — validate by real push, not `gh auth status`. Stale branches kb-sync-20260720/21/22 safe to delete (superseded, behind head). **Premise correction for the record:** operator's "last successful kb-sync merge #972 (07-17)" was stale — the `0 6` wiki-fold flow kept merging (#976/#987/#989/#995/#997/#1002); the un-merged 07-20/21/22 `kb-sync-2026072x` were the SEPARATE `0 3` 4-source flow (agent/auto-memory sources, NOT a task Main owns). Upstream shader-slang GitHub (GraphQL/REST/git) was already healthy for Main before this; this fix was specifically the slang-coworkers/nanoclaw fork write path.

**[historical] ⚠️ RE-FLAP on the slang-coworkers PUSH/WRITE path (07-20 ~06:10Z, Main-verified this daily-wiki-sync session):** the daily learnings-wiki sync (Part B) again hit **HTTP 401 "Authentication failed" on `git push` to `slang-coworkers/nanoclaw`** (2 attempts), while **git READS are healthy right now** (`git fetch nv-coworkers` at session start = 200; `git ls-remote origin nv-coworkers` mid-session returned the `nv-coworkers` head `dc1780df` = 200). `gh api` (GH_TOKEN path) returns 401 "Bad credentials" and `gh auth status` = GH_TOKEN invalid — but those probes are the GH_TOKEN path, misleading per [[feedback_gh_auth_status_misleading]]; the load-bearing signal is git-read-OK / git-push-401. Same `slang-coworkers/*` write-credential rejection as 07-18/07-19. Steps 9–11 (push→REST PR create→REST merge) are ALL blocked (no read-only workaround for a write). **Nothing lost:** Part A wiki-synth landed fully in `/workspace/shared` (coverage **1536/1536**, 0 dangling/uncovered); the sync is committed locally as **`a915f0de`** on branch **`kb-wiki-fold-20260720`** (durable on the `/workspace/agent/nanoclaw-kb` mount, ahead 1 of origin/nv-coworkers). Operator-actionable: re-auth the `slang-coworkers/*` push credential at the gateway (= operator re-login, NOT container restart), then `git push -u origin kb-wiki-fold-20260720` + REST PR-create/merge to land. Same flapping-gateway class; READ path healthy this time.

**⚠️ STILL DOWN 07-19 (approver-observed, not Main-verified this turn):** slang-pr-approver reports (msg 8, #12142 shadow run) the container's `GH_TOKEN` is **invalid** and OneCLI returns GitHub **`app_not_connected` HTTP 401**. Same flapping-gateway class, now confirmed persisting into day 4 (07-16 onset). **No new blocker / nothing lost:** shader-slang/slang is public, so the approver built a read-only unauthenticated `gh` shim (direct-egress, GET-only) and ran `harvest-reviews.py`/`eval-clauses.py` unchanged against identical public data — correctly classified **NOT ABSTAIN_INFRA** (review signal fully obtainable; only the transport was swapped). Writes (push/merge/GraphQL) would still be dead if needed. Operator ask UNCHANGED and standing (re-auth GitHub in OneCLI / gateway cred) — not re-escalated this turn since nothing was blocked. If a coworker hits a real *write* block today that can't be worked around, escalate then.

**⚠️ RE-FLAP WIDENED TO ALL READS (07-18, Main-verified this #976-webhook session):** later 07-18 the token degraded from the 06:25Z push-only state to a **FULLY-invalid token** — `gh auth status` = "The token in GH_TOKEN is invalid", and **BOTH GraphQL AND REST-core return HTTP 401 "Bad credentials"** (2× retries each on `repos/slang-coworkers/nanoclaw/pulls/976`, `.../commits`, and the bare `repos/slang-coworkers/nanoclaw` read). This is strictly worse than 06:25Z where reads (`git fetch`/`ls-remote`/pull GETs) were 200 — now there is NO working read surface either. Blocked the #976 `sync/upstream-nv-coworkers` auto-merge-class action ([[project_nanoclaw_pr874_webhook_route_approver]]): couldn't read the diff to run the #944 verify checklist, couldn't run the REST merge. Classification was still payload-derivable (unaffected by creds). Same flapping-gateway class; operator re-auth of the `slang-coworkers/*` gateway cred remains the fix (NOT restart — per-request injection hits the same dead cred). Escalation widened accordingly.

**⚠️ RE-FLAP on the slang-coworkers PUSH/WRITE path (07-18 06:25Z, Main-verified this daily-wiki-sync session):** the daily learnings-wiki sync (Part B) hit a hard **HTTP 401 Unauthorized on `git push` to `slang-coworkers/nanoclaw`** with `X-Should-Retry: false`, across 4 retries, while **reads returned 200** (`git fetch nv-coworkers` + `git ls-remote origin nv-coworkers` both fine; the push's own initial GET was `200 OK`, the POST was `401`). This is the `slang-coworkers/*` **write** credential (the USER-PAT split at the OneCLI gateway, per [[project_supervisor_pull_universe_enrichment_fail]]/gateway split) being rejected — reads use a different (working) cred. PR-create is also a write, so steps 9–11 (push→PR→REST-merge) are ALL blocked; there is no read-only workaround. **Nothing lost:** the sync is committed locally as `048fa53c` on branch `kb-wiki-fold-20260718` (durable on the `/workspace/agent/nanoclaw-kb` mount), Part A wiki-synth landed fully in `/workspace/shared` (coverage 1477/1477). Operator-actionable: re-auth the `slang-coworkers/*` push credential at the gateway (per prior incidents = operator re-login, NOT container restart); then `git push -u origin kb-wiki-fold-20260718` + REST PR-create/merge to land it. Same flapping-gateway class as below; the READ path is healthy this time (contrast the 07-17 evening REST-core widening).

**⚠️ RE-FLAP + WIDENED to REST-core (07-17 ~evening, Main-verified this session):** earlier this same session REST-core reads worked (#972/#973/#970/#969 all read fine via `gh api repos/.../pulls/N`); now `gh api repos/slang-coworkers/nanoclaw/pulls/974` returns **HTTP 401 "Bad credentials"** on REST-core too (2× retries), i.e. the outage has widened from GraphQL-only to include REST-core reads. This is the flapping gateway cred degrading further, same class as below. Classification of #974 (CHANGELOG SELF-MERGE CLASS, no-op) was unaffected — payload-derivable without creds. **If this persists into a turn that NEEDS a write or a real read, escalate operator re-auth again; don't declare recovery from a single read-probe.**

**Signature (07-16 ~15:39Z, babysitter + Main-confirmed):** `gh api` / `gh` calls to the **GitHub actions API** (`repos/*/actions/*`) and **GraphQL** (even `{viewer{login}}`) return persistent **401 "Bad credentials"** (3× retries), while **REST core reads work fine** (`repos`, `pulls`, `commits/*/check-runs`, issue reads). So dead: `gh pr checks`, `gh run view --log-failed`, `gh run rerun --failed`, `gh pr merge --merge-queue`, all GraphQL queries (mergeQueueEntry, reviewDecision, timeline-via-graphql). Still alive: REST metadata reads.

**⚠️ FLEET-WIDE, not per-agent (Main-scoped 07-16):** babysitter first read it as a babysitter-token gateway-routing gap (`ROUT…` token). But **Main's OWN token 401s identically** on actions API + GraphQL this sweep — AND Main was using both successfully EARLIER THE SAME SESSION (job-log reads on 87247297851, GraphQL mergeQueueEntry queries, run lookups all worked turns ago). ⇒ **fresh outage, onset ~07-16 15:00Z, affecting ALL tokens** — a shared gateway credential-injection failure on the actions/GraphQL paths, NOT a scope change or a single agent's config.

**Impact:** every coworker's CI-write + deep-read capability is degraded — babysitter can't rerun/read-logs/classify flakes, approver can't merge, anyone can't query GraphQL PR state. REST-read-only mode fleet-wide. Babysitter sweeps go read-only until restored.

**Operator-actionable (surfaced 07-16):** only a human can fix a gateway credential. PushNotification sent: re-auth the gateway credential for the **actions API + GraphQL paths** (the `8d85bfeb`-style secret / OneCLI GitHub token injection). **NOT restart-fixable** — same class as the prior provider-auth outages ([[project_slang_fixer_auth_outage]] 07-14, slang-triager 07-13, both OPERATOR re-login NOT restart) but a DIFFERENT credential (those were Bedrock/model provider auth; THIS is the GitHub gateway token, and path-specific: actions/GraphQL 401 while REST-core still injects OK → looks like a stale/unscoped injection on those paths).

**✅ RECOVERED 07-17 ~13:56Z (Main-verified, TWO independent signals — concrete, not a read-probe).** (1) GraphQL `{viewer{login}}` returns `nv-slang-bot[bot]` on Main's own token (was 401 all morning). (2) **git-push path recovered with a landing as proof:** PR #12034 origin head is now `85880034cd` = exactly the fixer's local commit that was **403-blocked at 07:50Z** — a commit only reaches origin via push, so the push landed 07:50Z→13:56Z and the fixer already resumed (msg 43630: #12034 approved at pushed head, MERGEABLE/`blocked`=awaiting-merge). GraphQL + actions + git-push all back. **Downstream:** #12034 unblocked (approved, awaiting pdeayton merge); #11967 `5433246218` push now possible (same fixer demonstrably just pushed); **#11840 STAYS BLOCKED on the SEPARATE App `Discussions:write` gap** ([[project_bot_discussions_write_permission_gap]]) — GraphQL recovery does NOT fix that permission gap. ⚠️ prior "recovered 18:00Z" was premature (flapped back); this call has a concrete write landing as evidence — but WATCH for re-flap, don't declare the incident dead. Operator escalation updated: cred no longer blocking pushes; #11840 still needs the App permission grant.

**⚠️ WORSENED — git-push/write path now 403 too (07-17 07:50Z).** slang-fixer reports (msg 41916) `git push` = **403 (both force & plain), origin credential shows literal `placeholder`** (gateway not injecting on the git path) — blocking its ready-to-push, locally-verified #12034 rebase (commit `85880034cd`, all tests green). **Main independently verified this turn:** GraphQL `{viewer{login}}` = 401 "Bad credentials", `gh auth status` = GH_TOKEN invalid, REST core reads still OK. The git-push 403 is the FIXER'S report (I did not push-probe myself — no safe branch), but it's consistent: the flapping cred has degraded FURTHER since the 06:11Z wiki push succeeded, now killing HTTPS git-push writes, not just GraphQL/actions-write. **Escalated to operator 07-17 07:50Z with heightened urgency** — same gateway-cred re-auth, now also blocking PR-branch pushes fleet-wide. **NOT restart-fixable** (see below — fixer's `request_restart` offer declined; per-request injection means a fresh container hits the same failing gateway cred). #12034 holds until re-auth; nothing lost (commit local + fixer-recorded).

**❌ RESTART IS NOT THE FIX (reaffirmed 07-17):** GitHub secrets inject **per-request via the OneCLI gateway**, not spawn-time env (`CLAUDE.md`: "none are passed in env vars"). A `request_restart` re-spawns the container but it hits the **same** expired/failing gateway cred → identical 401/403, plus risks in-flight verified state for zero benefit. Every fleet auth outage resolves via **operator re-auth, NOT restart** ([[project_slang_fixer_auth_outage]] 07-14, slang-triager 07-13, this cred 07-16/17). When a coworker offers restart-to-re-inject, DECLINE + redirect to operator cred re-auth.

**✅ REST-WRITE PATH WAS ALIVE 06:11Z (now superseded by the 07:50Z worsening above):** (07-17 06:11Z, Main-verified via wiki-sync agent): the daily learnings-wiki sync landed **PR #973 on nv-coworkers fully** during the degraded window — `git push` over HTTPS (x-access-token), **REST `POST /repos/.../pulls`** (PR create), and **REST `PUT /repos/.../pulls/N/merge`** (merge) ALL succeeded (`422209d1→4faf1bde`). So the outage is **path-specific, not a blanket write-block**: DOWN = GraphQL (all) + Actions-API-WRITE (rerun/requeue/merge-queue → "Must have admin rights"); UP = REST core reads, REST git push, **REST pulls create/merge**, Actions-API reads + job-logs. Practical routing during this outage: prefer REST for anything that has a REST path (PR create/merge via `gh api ... pulls`, issue comments via `gh api ... issues/N/comments`); only GraphQL-only ops (mergeQueueEntry/reviewDecision queries, `addDiscussionComment`) and actions-write (`gh run rerun`) stay blocked. NB Discussion #11840 stays blocked for TWO reasons — `addDiscussionComment` is GraphQL AND the App lacks Discussions:write ([[project_bot_discussions_write_permission_gap]]).

**⚠️ PARTIAL RECOVERY — CI paths back, git-push facet REPORTEDLY STILL DOWN (07-17 12:09Z; do NOT declare full recovery):**
- **RECOVERED (verified):** GraphQL (Main own token `viewer=nv-slang-bot[bot]`), Actions read+write (babysitter verified actions read+write OK, cleared the 05:40Z write-degradation). So **CI reruns/requeues/merges/GraphQL are back** — the babysitter's & approver's CI-write half is restored. #12022's blocked #11955-hang rerun is unblocked (babysitter clears on next sweep).
- **⚠️ STILL DOWN (per sibling-session tracker update, NOT Main-verified this turn):** the MEMORY.md index for this incident reports a **git-push 403** facet still failing, **blocking fixer PRs #12034 / #11967 / #11840** (pushing fix commits to branches — a DIFFERENT path than actions-write/GraphQL). I have NOT tested git-push myself (no side-effect-free probe); treating this as a real signal from another session per the intentional index update, not overwriting it. So this outage is **NOT fully closed** — the CI-write half recovered, a git-push half is reportedly open.
- **DON'T repeat the premature-recovery error:** last turn I called "✅ RECOVERED 18:00Z" prematurely; this is path-specific flapping. Full closure requires: GraphQL OK **AND** a real actions rerun succeeding **AND** a real git-push succeeding (or explicit confirmation the #12034/#11967/#11840 push-blocks cleared). Until git-push is confirmed, operator escalation on the push facet stays live (owned wherever the sibling session tracked it).
- Total arc: onset 07-16 15:00Z → reads-only ~18:00Z → write+GraphQL re-degraded through 05:40Z 07-17 → CI paths recovered ~12:09Z; git-push facet still reported down. **If recurs:** actions/GraphQL 401 or write "Must have admin rights" or git-push 403 while REST-core OK → operator re-auth gateway cred; verify per-path (GraphQL viewer + real rerun + real push), never a single read-probe.

**⚠️ NOT a clean recovery — FLAPPING/PARTIAL (corrected 07-17 05:40Z):** the "✅ RECOVERED 18:00Z" call below was PREMATURE. By 07-17 05:40Z the gateway re-degraded to a PARTIAL state (babysitter + Main-confirmed on Main's own token):
- **RESTORED:** REST core, **Actions-API READS** (`/actions/runs`=40000, `/actions/jobs`, **job-log reads**) → classification fully works again.
- **STILL DOWN ~14h post-onset:** **GraphQL 401** (`{viewer{login}}`, PR mergeStateStatus queries → "Bad credentials" — Main-verified own token) AND **Actions-WRITE** (`gh run rerun --failed` → "Must have admin rights to Repository" — a privilege error, consistent with invalid-GH_TOKEN→fallback-cred per [[feedback_gh_auth_status_misleading]]). `gh auth status`: GH_TOKEN invalid.
- **Net fleet-wide state: READ-capable, WRITE-handless.** Babysitter can classify but cannot rerun/requeue; approver can't merge; GraphQL PR-state queries dead (use REST `commits/<sha>/check-runs` + `pulls/<n>` instead — those work).
- **Impact of write-half:** #12022 has a confirmed #11955 cpu-llvm hang a rerun WOULD clear, blocked on write-cred (babysitter logged result:"blocked", rerunnable on recovery). Reruns/requeues/merges stay blocked fleet-wide until fixed.

**✅ (SUPERSEDED — was premature) RECOVERED ~07-16 18:00Z (~3h outage, 15:00Z–18:00Z).** Babysitter verified with the exact probe (`actions/runs total_count=40000`, GraphQL viewer OK, REST OK) and resumed normal sweeps on its own — no re-fire needed. Recovery was NOT tied to a confirmed operator action (the mobile PushNotification didn't send — Remote Control inactive; per [[feedback_push_not_away]] that says nothing about presence); the gateway cred likely refreshed/rotated back on its own or an operator acted without confirming to me. Either way the window is closed. #12136's earlier 8-platform assoctype/reflection regression ALSO self-resolved on new head 04d90845 (remaining red = its own `-fno-rtti` typeinfo link break in new unit-test, author-owned). **If this recurs:** same signature = actions/GraphQL 401 while REST-core OK → operator re-auth of the GitHub gateway cred for those paths; verify-recovery probe as above.
