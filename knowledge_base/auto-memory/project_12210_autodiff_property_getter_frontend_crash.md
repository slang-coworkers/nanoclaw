---
name: project_12210_autodiff_property_getter_frontend_crash
description: "#12210 slangc front-end crash — [Differentiable] on struct property getter, no diagnostic; regression from #5922"
metadata: 
  node_type: memory
  type: project
  originSessionId: ca1f3ab5-7512-490a-9afd-11dec729552d
---

**shader-slang/slang#12210** — `[Differentiable]` (or ANY differentiability attr) on a **property** accessor segfaults slangc in the FRONT-END with no diagnostic / no line (Windows `0xC0000005` = error 3221225477). External reporter qwerty10086, self-contained repro (autodiff property getter + CUDA/slangtorch, but crash reproduces bare).

- **Classification:** bug (crash) / high / frontend (semantic-check → autodiff decl-header) / **P1**.
- **Minimal repro:** `struct S { property V : float { [Differentiable] get { return 1.0; } } }` — no CUDA, no `__fwd_diff`, decl never used. **Subscript** accessor with `[Differentiable]` is fine → property-vs-subscript is the discriminator.
- **Root cause (CONFIRMED, symbolized bt + git-blame):** `getFuncType` (slang-syntax.cpp:1124) enters `if (as<SubscriptDecl>(parent) || as<PropertyDecl>(parent))`, then does `parent.as<CallableDecl>()` and iterates params. `SubscriptDecl` IS a `CallableDecl` (index params); **`PropertyDecl` is a `ContainerDecl`, NOT callable** → walking it as CallableDecl null-derefs at slang-ast-decl.cpp:247 (`isUsingOnDemandDeserialization`). Regression from commit **e93cb8a4d (#5922, 2024-12)** — matches reporter's v2023.4.10→v2026.12.2 window.
- **Fix (triager recommended):** **A** = gate the param-prepend branch on `as<SubscriptDecl>(parent)` only (a property contributes no params; accessor's own param loop already handles a setter's value). Plus **C** = add assert/diagnostic so out-of-contract parents fail loudly (addresses "crash with no diagnostic").
- **Extra discriminators:** even `[TreatAsDifferentiable]` and an empty `get;` in an interface crash → purely front-end, not an IR/autodiff pass.
- **Status 07-24:** triager applied `reproduced`+`regression`, set Type=Bug, posted 5-bullet verdict ([comment 5068086148](https://github.com/shader-slang/slang/issues/12210#issuecomment-5068086148)), forwarded to **slang-fixer** on canonical thread. slang-fixer ACK'd, building Approach A + hardening.

**✅ SHIPPED 2026-07-26 00:46 — draft PR #12231 (fixer msg 64270; Main-verified at HEAD `0d409b6ae3`).** OPEN/**draft**/nv-slang-bot, base master, `pr: non-breaking`, title "Fix #12210: property accessor with [Differentiable] segfaults front-end", CI dispatched, webhook-mapped. **Approach A as triager-recommended:** `getFuncType` gated the parent-param-prepend on `as<SubscriptDecl>(parent)` ONLY — PropertyDecl (a ContainerDecl, not CallableDecl) was cast to CallableDecl + null-deref'd at slang-ast-decl.cpp:247. A setter's value param lives on the accessor itself → nothing dropped. **1 file +5/−1** (`slang-syntax.cpp`) + 1 new test. **Tests:** repro SIGSEGV(139)→EXIT 0; new `tests/autodiff/differentiable-property.slang` (CPU+CUDA) 2/2; full `tests/autodiff/` **869/869**, 0 fail; `__fwd_diff` through the getter now emits clean **E41022** instead of crashing. codex CODE/PLAN/OUTPUT approve.
- **⚠️ SCOPE — crash-fix only; full property-accessor autodiff DEFERRED (fixer's key judgment call):** fixing the crash was a **3-layer cascade**; shipping Layers 1+2 (crash-gate + fwd-registration) WITHOUT the not-yet-root-caused Layer 3 would **regress bwd_diff to an internal assert-crash**. So **Layer 1 alone is the safe, complete fix for the REPORTED issue** (the segfault). Deferred layer-2 fwd + layer-3 IR analysis saved fixer-side at `/workspace/agent/patches/fix-12210-layer2-fwd-registration.md`. **✅ FOLLOW-UP FILED = #12232** (07-26, per Main ruling to file rather than let the PR note suffice — durable searchable home > buried PR note, cf [[project_11990]]-pattern): "Autodiff through a property accessor is not yet supported (follow-up to #12210 crash fix)", https://github.com/shader-slang/slang/issues/12232, labeled `Autodiff`. Framed as tracking/deferred-feature (NOT bug/commitment), cross-links #12210+#12231, captures the 3-layer cascade map + key caveat (**L2 without L3 regresses bwd to assert-crash → both must land together**), notes analysis+ready-fwd-patch exist without pasting, states may be maintainer/contributor-owned (bot won't implement). Correct scoping — didn't ship a half-fix that trades a segfault for an assert-crash.
- **⚠️ 07-31 — #12232 re-surfaced as a fresh `issue_opened` webhook + a dashboard triage ask.** It's a **self-echo** (author `nv-slang-bot[bot]`, our own filing) — NOT external work. Live state (Main-verified `github_get_issue`/`get_pull_request`): **#12232 now maintainer-picked-up — assignee `saipraveenb25`, milestone `Q3 2026 (Summer)`, label `Autodiff`, 0 comments.** #12231 (crash fix) still **OPEN/draft**, `fix/issue-12210`→master, requested reviewers `csyonghe`+`saipraveenb25`, not merged. **Triage verdict:** enhancement/feature-gap (NOT bug), subsystem Autodiff (fwd derivative-registration for property member-access path + IR reverse-mode `slang-ir-autodiff-unzip.cpp`), **P2/P3** (feature not-yet-supported; workarounds = `__subscript` differentiates today / `no_diff`). **Disposition = NO fix dispatch, maintainer-owned, park.** GitHub post HELD (redundant — issue body already IS the triage writeup; maintainer already assigned+milestoned; bot-on-bot comment = pure noise). Reported to dashboard on canonical thread `gh-issue-shader-slang/slang-12232`. RESUME only on a substantive **human** (non-bot) comment on #12232.
- **⚠️ slang-reviewer NOT ADDRESSABLE (flag):** peer review SKIPPED — fixer got "No agent named 'slang-reviewer' is currently addressable" + the routing gate blocked a fresh [Fix Review Request] without an inbound to reply to. **BUT the wiring row EXISTS** (Main-verified `ncl destinations list`: `slang-fixer ag-...vmjrwe → slang-reviewer ag-1780667168475-a9tac8`, 2026-06-05). So this is likely a **reviewer-session-down/logged-out** state (same class as prior slang-reviewer logout 07-09), NOT a missing edge ([[project_coworker_named_edge_dropped_silent_hang]]). Non-blocking here (draft, maintainer review is the real gate) — fixer will dispatch if reviewer returns. WATCH: if other chains also can't reach slang-reviewer, it's a reviewer-container health issue → may need operator re-auth. Merge OPERATOR-gated per usual. Related autodiff cluster [[project_12197_rayquery_byvalue_return_nrvo]] but distinct. regression from #5922.
