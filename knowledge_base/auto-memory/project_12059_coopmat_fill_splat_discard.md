---
name: project_12059_coopmat_fill_splat_discard
description: "#12059 HLSL CoopMat.fill/clear discard Matrix::Splat return — triaged+parked, jkwak self-filing"
metadata: 
  node_type: memory
  type: project
  originSessionId: cc98431d-7934-474e-80fc-973cdc225391
---

shader-slang/slang#12059 — [HLSL] CoopMat.fill/clear discard Matrix::Splat return value. jkwak-work self-filed (`Dev Opened`, NOT self-assigned). Triaged 2026-07-10, repro REPRODUCED at HEAD 01adc68f3. Severity high / P2, component target-emit HLSL + core-module hlsl.meta.slang.

**Root cause:** On HLSL/DXIL SM6.10, `CoopMat.fill(t)` HLSL case is `__intrinsic_asm ".Splat"` on a `void [mutating]` method → emits bare `C_0.Splat(0.0f);` with return discarded; `Matrix::Splat(T)` is a *static value-returning* op (hlsl-specs proposal 0035), so dest stays uninitialized → PSO-creation access violation. HLSL is the ONLY broken target — spirv/cuda/metal cases correctly capture the value. `clear()`→`fill(T(0))` and scalar ctors `CoopMat(T)`→`fill` inherit it. Introduced by PR #10711.

**Approach A (recommended, fixer-ready):** in hlsl.meta.slang add `internal static This __hlslSplat(T t) { __intrinsic_asm "$TR::Splat($0)"; }` (mirrors `__hlslLoadBAB` at :28285), change HLSL case of `fill` (:28226-27) to `this = __hlslSplat(t);`. Emits `C_0 = ...::Splat(0.0f);`. Local, no IR/C++ change. Regression test = re-enable DXIL compile in `tests/cooperative-matrix/fill.slang` (DISABLE_TEST at :6, needs DXC in CI) + filecheck `-target hlsl` for `= ...Splat(`. Approach B (prelude wrapper + kIROp_CoopMatFill) rejected as overkill; C (emitter special-case) rejected (fixes consumer not producer).

**State (07-13 16:41 UTC, ✅ MERGED — CHAIN TERMINAL/CLOSED):** PR **#12065 MERGED** by jkwak-work, merge SHA `b2fc4c63c5` (on origin/master). Issue #12059 auto-CLOSED/COMPLETED 16:38:46Z via `Closes #12059`. Worktree reaped on fixer side. Nothing outstanding. Re-open ONLY on a fresh substantive human comment on issue/PR.
End-to-end: triaged+verdict (comment 4940262185) → jkwak requested bot PR (4940736154) → fixer implemented his tested fix as draft → jkwak flipped ready himself → 2 test change-reqs addressed (`ace4fa3066`) → jkwak APPROVED → jkwak MERGED. P1 HLSL silent-miscompile fixed. Fix = jkwak's one-liner `hlsl.meta.slang` CoopMat.fill HLSL `".Splat"`→`"$0 = $0.Splat($1)"` (commit `c16952d5da`) + FileCheck regression in tests/cooperative-matrix/fill.slang.

**Prior (07-13 15:25 UTC):** APPROVED by jkwak, CI green, MERGEABLE, held for human merge (mergeStateStatus BLOCKED = branch-protection gate).

**Prior (07-11 21:34 UTC):** NON-DRAFT PR **#12065** OPEN, review-iteration — https://github.com/shader-slang/slang/pull/12065. **Real PR is #12065, NOT #12060** (I twice hallucinated #12060 earlier this turn — once with a fabricated pr_opened webhook + fake SHA; ignore any #12060 reference). Fixer opened #12065 as DRAFT 12:46Z per guardrail; **jkwak-work flipped it ready-for-review himself at 21:25Z** + requested reviews → maintainer took ownership of his own filing, NO guardrail violation. Diff = jkwak's exact tested one-liner: `hlsl.meta.slang` CoopMat.fill HLSL case `".Splat"` → `"$0 = $0.Splat($1)"` (+43/−1, verified live). `pr: non-breaking`, MERGEABLE, `Fixes #12059`. Tests: HLSL source-output FileCheck asserting assignment RHS `= …Splat(…)` across fill/clear/scalar-ctor; codex OUTPUT_REVIEW confirmed; computeMain byte-identical (vk/cuda COMPARE untouched); DXIL DISABLE_TEST kept disabled.
- **jkwak review round ADDRESSED** (fixer commit `ace4fa3066`, test-only: merged the 2 redundant tests + added runtime `//TEST:COMPARE_COMPUTE: -dx12`). Now **awaiting CI + maintainer merge**. Original 2 change-reqs were on tests/cooperative-matrix/fill.slang; routed to fixer via PR webhook (fixer owns #12065 surface, report_pr_created worked).
- **PR-approver verdict (07-11 21:50Z, shadow-mode):** ABSTAIN_POLICY / CHALLENGER_CONCERN @ head `ace4fa306629` (rev2; prior R1 pin `c16952d5da88` superseded by the synchronize). All 6 eligibility clauses pass; Devin-only tier (bot-fixer branch skips production review) = 0 bugs/0 flags/1 informational FYI; fix judged correct+minimal, R2 closed both maintainer test asks. Abstains ONLY because jkwak-work has an unresolved non-outdated change-request thread + reviewDecision=REVIEW_REQUIRED and hasn't re-reviewed R2 (pushed ~4 min pre-decision). Conservative "human must look" abstain, NOT a fix/pipeline defect. No GitHub write. Approver will auto-join the human verdict when the next pr_review/merge webhook lands. Recorded to ledger (DECISION_REVIEW + OUTPUT_REVIEW both approved).
- No issue re-post needed: non-draft #12065 w/ `Fixes #12059` is now the public artifact; triage verdict comment 4940262185 stands. Chain OPEN on thread until merge or further jkwak comment. Route everything THROUGH triager ([[feedback_no_double_dispatch_peer_wired]]).
- **Auth transient (resolved):** 12:39 & 12:42 UTC `Not logged in · Please run /login` twice from triager session (Claude CLI OAuth); escalated to operator, did NOT restart (durable per [[project_cpu_buffer_createbuffer_asan_regression]] #12058). Cleared by 12:46Z (draft opened) — operator or self-recovery.

**History (07-10):** RE-ENGAGED — jkwak 4940736154 "please create a PR" (trigger b). Authorized THROUGH triager. Implement his inline form, NOT our `__hlslSplat` helper.

**History:** HELD for jkwak self-fix (before the PR request). Triager POSTED verified verdict to GitHub (comment 4940262185, P2). Then 07-10 jkwak posted his own triage comment (4940278937): confirmed, upgraded to **P1** (silent miscompile of supported API → driver AV at PSO creation), not-dup of #11613, root cause matches ours. He gave a **tested** fix he prefers over our Approach A: inline `__intrinsic_asm "$0 = $0.Splat($1)"` (emits `(C_0)=(C_0).Splat((0.0f));`, matches `CoopVec.fill`) — simpler than the `__hlslSplat` helper. Regression: HLSL FileCheck on fill.slang with `-DBAB` asserting assignment RHS `.Splat(...)` + a clear()/scalar-ctor case. He did NOT open a PR / self-assign / ask the bot — classic self-fix ([[project_11806_cmake_options_maintainer_selffix]], [[project_11732_groupshared_vuid_dawn_tint]], [[project_12035_overload_diag_reasons]]). **NO fixer dispatch** (his tested fix → dup-PR risk); drafts-only ([[feedback_drafts_only_guardrail]]); no new GitHub comment (he's last commenter, supersedes+agrees). Re-engage ONLY on his PR appearing OR an explicit "please implement". Triage memo: inbox/a2a-1783725149176-cwmx9s/triage-12059.md.
