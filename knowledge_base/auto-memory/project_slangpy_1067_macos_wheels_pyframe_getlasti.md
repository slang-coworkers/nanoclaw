---
name: project_slangpy_1067_macos_wheels_pyframe_getlasti
description: "slangpy#1067 macOS wheels fail link cp311+ undefined _PyFrame_GetLasti — regression from"
metadata: 
  node_type: memory
  type: project
  originSessionId: a988d465-c4d3-4f39-aa9b-c1c56d422f8c
---

# slangpy#1067 — macOS wheels link-fail cp311+ (_PyFrame_GetLasti)

## ✅ TERMINAL — MERGED 2026-08-10T11:25:31Z by **skallweitNV**, merge commit `35e262ab73`
#1067 closed `completed`; **#1066 (CI-coverage gap) still OPEN**. Final head `4639cff88a`, 3 commits, net **1 file / +8 / −0**.
**Verdict NOT contradicted.** MINE-VERIFIED at join: blob `039be94b54` for `src/slangpy_ext/CMakeLists.txt` is **byte-identical** at the decision head `266b2072e6`, the merged head `4639cff88a`, and `main` ⇒ **zero code delta** decision→merge (3rd commit = another merge-from-main, base drift only). **Zero human signal added** after the decision (reviews still 1 = the stale empty-body APPROVED; issue comments still 1 = ours; inline 0).
⭐⭐**THE FLAGGED GAP WAS *ACCEPTED*, NOT CLOSED** → [[feedback_merged_does_not_mean_the_flagged_gap_was_closed]]. Post-merge on `main`: `wheels.yml` **still `workflow_dispatch:`-only**, `runs?branch=dev/slangpy-fixer/1067` **still `total_count: 0`**, `ci.yml:43` **still `python: ["3.10"]`**. ⭐**The last `wheels` run on `main` was 2026-04-16** ⇒ `_PyFrame_GetLasti` has still **never been compiled at 3.11+ anywhere**; the 08-10 wheels runs were dispatches on unrelated branch `manylinux-2-28`. **The risk moved from the PR to the RELEASE** — v0.44.0 remains the exposure point that #1067 was filed about.
🔴**AND THERE IS NO LEDGER ROW — there never was.** The approver's 08-03 "Ledger row keyed (…, 1068, 266b2072e621…)" was retracted 08-10: `record_decision` was denied (`APPROVAL_LEDGER_WRITERS` unset ⇒ **denies every group, fleet-wide**). `ok('Decision recorded')` proves **emission, not persistence**, and the denial is **asymmetric** (seq=5 never denied; seq=19 denied +1.3s) ⇒ silence ≠ success. Durable record = approver's `work/1068-266b2072e621/`. Full mechanism + **my outstanding operator action**: [[feedback_record_decision_ok_proves_emission_not_persistence]].

--- prior: APPROVER VERDICT (2026-08-03 19:52) — `ABSTAIN_INFRA` / `NO_REVIEW_SIGNAL` ---, mode `live_late`, policy `v0-shadow-relaxed`, at `266b2072e621`. Recorded to ledger; **nothing written to GitHub**. All 6 clauses passed, then Step 2 short-circuited on `reviewers_complete=false`: harvest exit 20 (production claude-code-action legitimately **skips bot-authored** `dev/slangpy-fixer/*`; no CodeRabbit) **AND** Devin exited 0 with a **hollow artifact** — page carried `Connect GitHub`/`Sign in`, `## Flags` empty, and the text under "AI analysis" was **our own bot's PR description echoed back byte-for-byte**. ⇒ counting it would have been **self-review laundered through a scraper**. Named pipeline defect, NOT a statement about the code.
📌**Approver's own inspection said the fix looks RIGHT** (context only, cannot substitute for the missing review): `external/nanobind` is submodule **`skallweitNV/nanobind`** (a FORK, not `wjakob/nanobind`) @`0b719c12ad`; its `cmake/darwin-ld-cpython.sym` (932 entries) carries `_PyFrame_GetBack/GetCode/GetLineNumber`, `_PyEval_GetFrame`, `_PyThreadState_GetFrame` and **no `_PyFrame_GetLasti`**, applied as a linker response file (`nanobind-config.cmake:108`). Genex configured under cmake 3.25.1 **with a `PLATFORM_ID:Linux` control** → control expanded to `LINK_FLAGS = -Wl,-U,_ControlProbe` ⇒ Darwin arm yields `-Wl,-U,_PyFrame_GetLasti`, empty off-Darwin. Causal link for the coverage gap: `profiler.cpp:22-26` gates the symbol behind `#if PY_VERSION_HEX >= 0x030b0000`.
⚠️`ci_green_on_sha` "passed" only because the relaxed policy **doesn't require green** — NOT coverage evidence. ⛔**CORRECTED — my "13 required checks + `license/cla`" was WRONG (double-counted, implies 14).** MEASURED: required contexts = **13** = **12 build jobs + `license/cla`**; check-runs on the head = 13 (12 builds + `pre-commit`, which is green but **NOT** required). Both sets happen to have cardinality 13 ⇒ the coincidence hid the bad composition → [[feedback_two_sets_same_count_different_members]]. All 13 required ARE satisfied (`license/cla` via a commit **status**, not a check-run — join against both). ✅**And "which checks are required" IS knowable**: `branches/main/protection` 403s but `branches/main` → `.protection.required_status_checks.contexts` returns all 13 (sibling-endpoint rule). I wrongly told the fixer to suppress "required". **`mergeable_state` is STILL `blocked`** — VERIFIED: all 13 required contexts `success`, `license/cla` `success`, `branches/main` classic protection reports **`required_pull_request_reviews: null`**, `CODEOWNERS = * @shader-slang/dev`. **CAUSE = the branch-protection REVIEW gate (pending `@shader-slang/dev` / CODEOWNERS), NOT CI** — this is the **already-established slangpy diagnostic** from [[project_slangpy_1076_branch_protection_review_gate]]: *`mergeStateStatus: BLOCKED` + `mergeable: true` + all checks green ⇒ suspect a review gate; the bot 403s on branch-protection endpoints so it CANNOT enumerate the exact requirement — say so, don't guess.* Same repo, same `CODEOWNERS = * @shader-slang/dev`, same shape. `repos/…/rulesets` → `[]` (exit 0, genuinely empty — not a 403), so rulesets are ruled out. ⚠️Still UNVERIFIED and 403-blocked: whether ccummingsNV's approval counts toward `@shader-slang/dev`. ⛔Do NOT name a specific failing check.
⭐⭐**RETRIEVAL MISS (mine, this session):** I re-derived this from `branches/main` + CODEOWNERS instead of checking my own store, where #1076 had already answered it 6 days earlier — and my first write of this line stated the CODEOWNERS cause as fact, then over-corrected to "unverified/indistinguishable" when the truth was "**verified 6 days ago on this very repo**". Exactly [[feedback_narrowing_is_not_testing_check_own_store]]. The trigger I should have matched: **`blocked` + green checks on a slangpy PR** ⇒ `grep -ril "mergeStateStatus\|blocked" memory/`.
⛔**Do NOT relay the approver's critique-gate diagnosis** ("ABSTAIN fast-path honored on tool-call path but not final-response path") — disproved, see [[project_critique_gate_pulls_pattern_builtin_floor]] §5th-instance: the hook is `PreToolUse`-only so it never sees a `<message>` block, and the fast-path declines whenever the abstain's prose *mentions* `WOULD_APPROVE`/`BLOCK`.

--- state (2026-08-03 19:2x) --- **#1068 FLIPPED READY** by **@fknfilewalker** (Lukas Lipp, `write` perm — NOT the fixer, NOT skallweitNV/ccummingsNV) at 19:21:45Z, immediately after pushing `266b2072e6` = *"Merge branch 'main' into dev/slangpy-fixer/1067"* (`parents=2`, web Update-branch). `draft=false`, `mergeable_state=blocked`, reviewers now requested: **jhelferty-nv, bmillsNV**. Net diff vs `main` still **1 file / +8 / -0** (`src/slangpy_ext/CMakeLists.txt`) — the one `target_link_options(… -U,_PyFrame_GetLasti)` line + 5 comment lines. **Reviewed code byte-identical**; the 47-file `compare dc6f5ef8…266b2072` list is base drift, not PR content (`ahead_by=10, behind_by=0` off the *old* commit vs `ahead_by=2` off `main`).
⚠️**ccummingsNV's APPROVED is 5 DAYS STALE (07-29T10:05:29Z) and the API HIDES IT:** `reviews[].commit_id` = `266b2072e6`, a commit dated **08-03T19:22:22Z** — after the review. Do NOT read that field as "approved at head" → [[feedback_reviews_commit_id_can_postdate_submitted_at]].
⚠️**AUTHORITATIVE GREEN STILL ABSENT — re-verified at the new head:** `wheels` is *still* `workflow_dispatch`-only, and `runs?branch=dev/slangpy-fixer/1067` → **total_count 0** (zero wheels runs ever on this branch). `ci.yml` *still* pins `python: ["3.10"]` (line 43) ⇒ the 13 in-flight `build (…, 3.10)` checks compile the **pre-3.11 `frame->f_lasti` branch** and **cannot** exercise `PyFrame_GetLasti` — green here is **0 coverage of the fix** ([[feedback_green_job_skipped_backend_zero_coverage]]). Last `wheels` run of any kind: 07-16 on tag `v0.43.1`.
Only 1 issue comment + 1 review on the PR; **0** `pulls/1068/comments` (endpoint-split checked).
**Next:** routed to **slangpy-pr-approver** (`pr_ready_for_review` → approver, never reviewer/fixer). Expect the coverage gap to drive the verdict, not the code.

--- prior state (2026-07-29) ---
RESOLVED on triage/review side — **APPROVED, held as DRAFT pending human merge**. PR **#1068** reviewDecision=APPROVED by **@ccummingsNV (human)**, mergeable=MERGEABLE, `Closes #1067` (issue still OPEN — draft doesn't auto-close). No changes requested, no open review threads.
**⚠️ NOT yet green on authoritative signal:** macOS `wheels build-only` cp311–cp314 is **workflow_dispatch-only** and will NOT auto-run on merge. Maintainer must trigger it manually + confirm green BEFORE cutting v0.44.0. Fixer surfaced this caveat on the PR.
**Next HUMAN action (on GitHub):** @skallweitNV / @ccummingsNV → run `wheels build-only` (macos aarch64 cp311–cp314), confirm green, mark #1068 ready, merge before v0.44.0. Issue cmt refreshed (comment-5116278224). CI-coverage gap = #1066.
**Ownership:** triage chain CLOSED our side. slangpy-fixer REMAINS attached to PR #1068 for synchronize/merge webhooks; reaps worktree on merge/close. Canonical thread `gh-issue-shader-slang/slangpy-1067`.

--- prior state (2026-07-16, draft opened) ---
DRAFT PR **#1068** opened by slangpy-fixer (`dev/slangpy-fixer/1067` off `main`@e5e8cb4; `Closes #1067`; `report_pr_created` DONE → webhooks route to fixer; verified isDraft=true). Peer review dispatched to slangpy-reviewer by fixer.
**Fix (fixer, matches triage Approach A + codex-approved PLAN/CODE/OUTPUT):** one line —
`target_link_options(slangpy_ext PRIVATE "$<$<PLATFORM_ID:Darwin>:LINKER:-U,_PyFrame_GetLasti>")`. No code/submodule change; 3.11+ attribution unchanged.
**Authoritative green = macOS `wheels build-only` cp311–cp314** (link failure is macOS+Py3.11+-only; NOT reproducible on Linux CI pinned 3.10 or any unit test; fixer did not run native build — no-op off-Darwin + tight disk). Local genex validated standalone; pre-commit passes.
**Guardrail posture:** stays DRAFT until human/maintainer flips ready. If @skallweitNV self-starts, draft #1068 is safe to abandon. CI-coverage gap = #1066 (out of scope). Public footprint OK: triager's 5-bullet at issue cmt #4986298306 + PR `Closes #1067`.
Report memo: fixer-side `/workspace/agent/reports/slangpy-1067.md`.

--- prior state (2026-07-15, triage) ---

**Triage verdict (P1 release-blocker for v0.44.0):** regression / high / build-system (nanobind link). On macOS, nanobind links via a curated undefined-symbol allow-list (`external/nanobind/cmake/darwin-ld-cpython.sym`) that omits `_PyFrame_GetLasti` → hard link error for Py≥3.11. cp39/cp310 take `frame->f_lasti` branch, link fine.
**Recommended fix A (triager):** one platform-guarded `target_link_options(... -U,_PyFrame_GetLasti)` line in `src/slangpy_ext/CMakeLists.txt` (~L87) — resolves at import time like sibling frame symbols already do; zero behaviour regression. Reporter's "link libpython" (C) REJECTED — nanobind deliberately avoids libpython on macOS.
**Files:** `profiler.cpp:20-27`; `CMakeLists.txt:~87`; `darwin-ld-cpython.sym`.
**Ownership:** slangpy-triager OWNS fixer wire → forwarded directly (I do NOT double-dispatch). @skallweitNV assigned-by-reporter + pinged h2h but NOT engaged (no comment/PR) → fixer DRAFTS + coordinates, does NOT race the maintainer. Stand down to skallweitNV if he starts a fix.

**What:** macOS arm64 wheel `build-only` fails at LINK time for Python 3.11–3.14 (cp311–cp314). cp39/cp310 pass. Undefined symbol `_PyFrame_GetLasti` referenced from `python_caller_site()` in `profiler.cpp.o`.

**Root cause (reporter):** `src/slangpy_ext/utils/profiler.cpp` calls `PyFrame_GetLasti()` when `PY_VERSION_HEX >= 0x030b0000`; older Pythons use `frame->f_lasti`. Symbol not resolving at link time on macOS ext-module builds (limited-API / no explicit libpython link).

**Regression from:** #1063 (Profiler), merged 2026-07-13. Blocks ALL macOS wheel publishes for Py3.11+. NOT #1002 (cp314 bump) — Linux/Windows cp314 built fine same run (run 29453450122).

**Fix options (reporter):** limited-API-safe frame offset (all platforms preferred); or link libpython; or fallback avoiding non-exported symbols on macOS.

**Acceptance:** wheels build-only green macos aarch64 cp311–cp314 + profiler caller-site attribution still works on macOS Py3.11+.

**Why it slipped CI (reporter follow-up cmt 4986213993):** (1) `wheels` workflow is `workflow_dispatch`-only — no PR runs cibuildwheel. (2) macOS PR CI (`ci.yml`) pins `python: ["3.10"]` so profiler.cpp compiles on macOS but takes the pre-3.11 `frame->f_lasti` branch — never references `PyFrame_GetLasti`. Bad symbol only appears building against Py3.11+. Would've been caught by a `wheels build-only` on main post-merge or a macOS cp313 smoke job in PR CI. Related policy gap: **#1066** (CI-coverage gap).

**Priority (cmt 4986225987):** reporter pinged maintainer **@skallweitNV** — "probably needs a fix before we do a v0.44.0". So this is a **release-blocker for v0.44.0**, not just wheel publishes. If skallweitNV self-assigns/starts a fix, that takes precedence (don't double-drive); else chain stays with us → slangpy-fixer. No bot GitHub post (human-to-human thread, bot not mentioned).

**Next:** await triager verdict → likely slangpy-fixer, unless skallweitNV picks it up. Related: profiler PR #1063, policy gap #1066.
