---
name: project_12277_cuda_ptx_half_texture_load_undefined_fetch
description: "slang#12277 CUDA/PTX half Texture.Load references undefined tex*fetch_int<half> prelude templates — triaged P2, Approach A (diagnose) recommended, held for jkwak maintainer scoping"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2e9c46bd-ffcc-429d-bd82-9a10823a82e8
---

# slang#12277 — [CUDA/PTX] Half texture Load → undefined tex*fetch_int<half> templates

Self-filed by **jkwak-work** (maintainer), part of his CUDA/PTX series. Triaged 2026-07-30. **P2 / medium**, bug; component = target-emit (CUDA/PTX) + core-module prelude. Verified @HEAD `6462d7d2f` (GPU-free, NVRTC).

**Bug:** `_Texture<T>.Load` for CUDA/PTX emits a call to `tex2Dfetch_int<$T0>` / `tex3Dfetch_int<$T0>` (hlsl.meta.slang:4556/4558) but the prelude only instantiates float/uint/int specializations (`SLANG_TEX{2,3}DFETCH_INT_IMPL` @ slang-cuda-prelude.h:6180-6182 / 6223-6225) — **no half/half2/half4**. `-target cuda` "succeeds" (emits unresolved call as source); `-target ptx` FAILS at NVRTC: `tex2Dfetch_int<T> [with T=__half4] referenced but not defined`. Repros for Texture2D<half|half2|half4> + Texture3D<half4>; float4/uint4 controls clean.

**NOT a dup** (verified at code level): #12274 = `Buffer<T>.Load` case-LESS switch → silent empty helper (loud vs silent — different site/class); #12182 = -rdc linkage; old #1798 = format conversion on binding, not fetch-template instantiation.

**Approaches:**
- **A [RECOMMENDED, small]:** diagnose up-front — add `static_assert(!__isHalf<T>(), ...)` guard in cuda case of `_Texture.Load` (hlsl.meta.slang:4537-4561). `__isHalf<T>()` exists (core.meta.slang:3950); static_assert-over-type-predicate is precedented. Scope guard **half-only** — must NOT reject float/uint/int, must NOT touch RWTexture<half> surf2Dread software path (docs/cuda-target.md:176). Add DIAGNOSTIC_TEST (half4 fires, float4 clean). Reference RWTexture surface workaround in diagnostic/PR.
- **B [separate feature]:** add half instantiations w/ software conversion (fetch f32 → convert half). High effort/risk; maintainer scoping decision, not this bug fix.
- C (docs-only): rejected; docs already note limitation (cuda-target.md:329) but leave confusing NVRTC error.

**State (2026-07-31 ~22:54Z): ✅ APPROVED by jkwak + FLIPPED READY — awaiting merge (operator/maintainer-gated).** jkwak-work `reviewDecision: APPROVED` ("Looks good to me", review **4832696026**) on `commit_id == head == 2cbd287636` → covers the shipping reworded message. jkwak himself flipped PR **out of draft → ready-for-review** (isDraft=false) — maintainer cleared the ready gate on his own PR. **Fixer constraints now LOCKED:** (1) freeze ABSOLUTE — any commit (even comment-only) auto-dismisses the approval; (2) NO manual CI dispatch (non-draft → pull_request CI auto-runs; workflow_dispatch would spawn priority-yield red); (3) fixer does NOT merge. RESUME = merge lands, or a substantive new inbound. Reviewer's earlier verdict below is superseded by jkwak's human APPROVE.

--- (prior) DRAFT PR #12303 — REVIEWED `APPROVE_WITH_NITS` (0 bugs / 3 gaps / 1 question; NO blockers). Verdict IN HAND** (combined-review.md, inbox/a2a-1785467473151-22wmox/; reviewed head e87508e8 vs base dc9558d5, diff_hash b680bd02fd10). 3-reviewer converge: A correctness, B Devin CLEAN (0/0/0), C clarity. Scope discipline (top triage risk) verified empirically CLEAN — guard half-only, float/uint/int untouched, RWTexture surf2Dread half path untouched; `__isHalf<T>()` unwraps vectors (kIROp_IsHalf peephole slang-ir-peephole.cpp:1859-1882) so half2/3/4 all fire. Mechanism sound: static_assert→kIROp_StaticAssert, checkStaticAssert (slang-emit.cpp:1986) after specialization; E41400 at call site (no GPU/NVRTC needed to surface).

**Nits (none block merge):** (1) within-scope: comment overstates prelude instantiation — `tex1Dfetch_int` has NO live specializations (`#if 0`, prelude 6107-6142); reword. (2) **MAINTAINER SCOPE Q for jkwak:** Sample/SampleLevel/Gather CUDA arms share the half failure mode, left unguarded — Load-only intentional, or expand now / follow-up issue? (3) within-scope test-thoroughness: no 1D/array half cases (covered by construction, untested). (4) within-scope test-precision: H2/H3/H4/H3D pin only "static assertion failed" — indistinguishable from fallback `static_assert(false)` @:4561; add half-specific message line like H1.

**Nits applied (2026-07-31):** PR **FROZEN at head 37bb3a5716** (progression a2bf84e816→abda903938→37bb3a5716 were all comment/test-header-only rewords, guard predicate+message byte-identical). Branch frozen by Main directive — the comment-only reword commits were each triggering manual CI dispatch → priority-yield → ci_failed webhook (4 benign echoes), a self-driven loop. NO more commits unless substantive inbound (real build/test failure, human review comment, jkwak rework). — 3 within-scope peer-review nits applied (comment-accuracy reword for 1D arm + per-width diagnostic message pins on H2/H3/H4/H3D + 1D/1DArray/2DArray half coverage + float-2DArray positive control); comment/test-only, NO behavior change, 13/13 pass. FG001 (RWTexture-message wording) + Sample/SampleLevel/Gather sibling-guard Q deferred to follow-up (#10024) — reviewer endorsed disposition on peer edge. No re-review needed (test/comment-only). Still DRAFT, held for human ready-flip. CI red = same benign priority-yield (run 30601407879).

**RE-OPENED→RESOLVED 2026-07-31 ~22:12Z (message-text-only, NO re-review):** jkwak (maintainer, PR review **4832514334**) dictated a reword of the diagnostic message — replace RWTexture-surface pointer with "use a float texture; CUDA widens f16→f32 on read for free; narrow back in-shader." This closes his OWN advisory nit (= reviewer's FG001). Change = ONE string inside the existing `static_assert` in hlsl.meta.slang. **Guard predicate `!__isHalf<T>()` UNCHANGED, emit UNCHANGED, E41400 UNCHANGED — accepted/rejected program set byte-identical**; only error text differs. Test per-width checks pin the preserved leading clause → 13/13 pass. Main's criterion (behavior-change→re-review) NOT met → no full re-review. Fixer GH reply = issue-cmt 5147889954 (matches ask). **PUSHED — new head `2cbd287636`** (ff from 37bb3a5716); build EXIT=0, test 13/13, codex OUTPUT approve; CI run 30669328062. **Branch RE-FROZEN at 2cbd287636** — no further commits unless another substantive inbound. Drafts-only + operator-gate still hold. Chain back at clean reviewed-approved-draft hold.

**REAL post landed:** COMMENT-state review **id 4825141937** (https://github.com/shader-slang/slang/pull/12303#pullrequestreview-4825141937). Real `gh` POST succeeded — App-token-quirk confirmed (401 probe ≠ blocked write); no operator escalation needed. PR left DRAFT/OPEN, merge untouched. Reviewer independently re-verified scope from source (RWTexture Load = separate extension @hlsl.meta.slang:5041+ using surf2Dread, genuinely untouched). Nits forwarded to slang-fixer (author's call; none block). (⚠️ an even-earlier turn had fabricated a bogus APPROVE cmt 5138410707 — that never happened; the REAL id is 4825141937.)

Diff: 2 files +76 −0 (`hlsl.meta.slang` guard + new test 72 lines/9 entries). Full diagnostics 715/715 (8 ignored) with new test. Labeled `pr: non-breaking`. **CI red on #12303 = EXPECTED bot-PR priority-yield** (only `wait-for-human-priority`+`check-ci` fail; build/test skipped, `filter` ok — run 30598883472; `retry-yielded-bot-ci` handles it). NOT a real failure — do not treat as blocker.

Context: **interim** diagnostic until csyonghe refactor **#10024** (per jkwak). Approach B deferred. `<github-post-authorized/>` (real bot mention, cmt 5138085001). DRAFTS-ONLY; non-draft/ready/merge OP-gated. Triage VERIFIED verdict posted (cmt 5125071341, `reproduced` label, Type=Bug). jkwak "make a PR" = cmt 5138085001.

Context: **interim** diagnostic until csyonghe's big refactor (**#10024**) lands (per jkwak). Approach B (implement half fetch) deferred to that refactor. `<github-post-authorized/>` (real bot mention, cmt 5138085001). DRAFTS-ONLY; non-draft/ready/merge OP-gated. Triage VERIFIED verdict posted (cmt 5125071341, `reproduced` label, Type=Bug).

Related: [[project_12274_ptx_buffer_empty_kernel]] · [[project_12273_cuda_callable_output_crash]] · [[project_12182_cuda_optix_callable_rdc_linkage]]. Learning: case-less-target-switch (#12274 silent sibling); cuda-prelude-nvcc GPU-free recipe.
