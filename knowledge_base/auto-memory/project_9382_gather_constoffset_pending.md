---
name: MERGED ✅ (PR #12133) — #9382 Gather const-offset over-declares ImageGatherExtended — TERMINAL
description: slang#9382 RESOLVED — backend-IR Option C; PR #12133 MERGED 2026-07-18 by jkwak-work (merge 340e3445a7a9, head fab37124a470); shadow WOULD_APPROVE CLEAN R1-R4 = MERGED-agreement VINDICATED; chain closed
type: project
originSessionId: c87f17be-487a-47d5-be99-2d349a875808
---

**2026-07-18 — MERGED, CHAIN TERMINAL.** PR #12133 merged 2026-07-18T16:12:59Z by jkwak-work (real maintainer), merge commit `340e3445a7a9`. Merged head `fab37124a470` = approver's R4 WOULD_APPROVE (CLEAN) decision commit **exactly** (zero follow-up commits between decision and merge). human_verdict=APPROVED recorded against R4 row. **Outcome: AGREEMENT / VINDICATED** — shadow WOULD_APPROVE matched human merge on the same SHA + explicit jkwak APPROVED review; held WOULD_APPROVE correctly across all 4 revisions (R1 fix → R2 cleanup → R3 version-bump 25→26 → R4 master-merge), each verified delta-safe with the ConstOffset-hoisting safety chain carried forward. No GitHub write (shadow mode throughout). #9382 auto-closed by merge (Closes #9382). Nothing further — topic file persists for reference.

**2026-07-18 — PR #12133 flipped READY_FOR_REVIEW.** Approver (slang-pr-approver) recorded **WOULD_APPROVE / CLEAN** in shadow mode (ledger-only, nothing posted) @commit 7a8bf46eab43, mode live_late. Devin-only tier (harvest exit 20 — production CI skips bot fixer branches): Devin 0🔴 / 3 non-blocking informational gaps / 0🔵; all 6 eligibility clauses PASS; challenger CLEAN. Verified load-bearing chain: `isConstantGatherOffset` accepts only shapes `maybeHoistConstructInstToGlobalScope` (slang-ir-spirv-legalize.cpp:1722) hoists to OpConstantComposite; misclassification safe-only-toward-Offset+capability. 3 documented non-blocking gaps clear conservative-lean bar: (1) spirv-asm-only test no `-target spirv -O0` validation → advisory; (2) negated-constant offset unhandled → pre-existing conservative over-declaration; (3) `__texture_gatherCmp_offset` runtime-Cmp mirror-bug → pre-existing out-of-scope. CI combined=failure NON-causal (macOS test-server IPC flake on Metal compute; 1/110 D3D12 falcor GBuffer image-diff — SPIR-V-only change can't affect D3D12); core SPIR-V/vk legs green on pinned SHA. Human jkwak-work review = COMMENTED (non-blocking).

**2026-07-18 R2 — `synchronize`, new head @`39d1df68ba3f`.** Approver re-decided (debounced ~90s settled head): **WOULD_APPROVE / CLEAN** @39d1df68ba3f, supersedes R1 (one ledger row per revision). R1→R2 delta = pure review-response cleanup: comment trim + `isConstantGatherOffset` refactored from if-chain to `switch`, verified semantics-preserving (accepts identical set; `emitImageGatherOffset` body byte-identical; no test/lua file touched). R1's ConstOffset-hoisting safety chain carries forward; 3 non-blocking gaps unchanged. CI not fully settled but compile-surface legs green (aarch64/wasm/macOS/windows builds, check-formatting, actionlint, test-slang aarch64+macOS incl. the R1-flaky macos-release-clang-aarch64 now green); reds all non-causal (check-ci/wait-for-human-priority premature orchestration checks; SlangPy test_profiler.cpp:284 CPU-timing flake, same as #12131). **Next: awaiting human merge — record_human_verdict against R2 row @39d1df68ba3f on merge/close (verify join SHA vs live GitHub first). Watch as false-safe if merges with a human CHANGES_REQUESTED intervening. NEVER auto-merge/post.**

**2026-07-18 R3 — `synchronize`, new head @`f3e931075958`.** Approver re-decided (debounced ~90s): **WOULD_APPROVE / CLEAN** @f3e931075958, supersedes R2 (one row per revision). R2→R3 delta = **single one-line change**: `slang-ir.h` `k_maxSupportedModuleVersion` 25→26 — documented minor-version bump for the new IR op `kIROp_ImageGatherOffset`, monotonic +1 matching 5 prior feature PRs, `k_minSupportedModuleVersion` unchanged (backward-compat preserved), serializer emits version automatically (no missed co-edit), purely additive (op-validity gated by stable-name id / kIROp_Unrecognized, not version number); also clears the `check-inst-version-changes.sh` CI gate. No emit/classifier/test logic touched → R1/R2 ConstOffset-hoisting safety chain carries forward. **jkwak-work's review was DISMISSED (was COMMENTED) — no active human review; dismissal ≠ CHANGES_REQUESTED, does not block.** CI not fully settled (5 longer legs + SlangPy still running) but 19 green / 0 red on all completed legs. **Next: awaiting human merge — record_human_verdict against R3 row @f3e931075958 on merge/close (verify join SHA vs live GitHub first). NEVER auto-merge/post.**

**2026-07-18 R4 — `synchronize` = MASTER-MERGE, new head @`fab37124a470`.** Approver re-decided (debounced ~90s): **WOULD_APPROVE / CLEAN** @fab37124a470, supersedes R3 (one row per revision). R4 is a master-merge commit ("Merge branch 'master' into fix/issue-9382-c", parents R3 + master aaa07fe29656) — NOT a fix change: net changed files still the same 6 fix files, net-diff hunks byte-identical to R3 (hlsl.meta.slang + slang-emit-spirv.cpp whole-file blobs differ only because master independently touched those hot files; fix's own contribution unchanged, no conflict-resolution edit). R1/R2/R3 safety chain carries forward. Devin CLEAN (0/0/5-info; items now AFFIRM the fix — "ConstOffset selection sound, subset of constant-hoisting condition"). 6/6 clauses PASS. CI 19 green / 0 red on completed legs (5 longer + SlangPy still running). **KEY: maintainer jkwak-work has now APPROVED on the R4 head (progression COMMENTED@R1 → DISMISSED@R3 → APPROVED@R4) — active maintainer approval on the current head.** **Next: awaiting human merge — record_human_verdict against R4 row @fab37124a470 on merge/close (verify join SHA vs live GitHub first). NEVER auto-merge/post.**
shader-slang/slang#9382 — `Texture2D.Gather(s, uv, int2(2,1))` emits SPIR-V `Offset` + `OpCapability ImageGatherExtended` for a compile-time-constant offset when it should use `ConstOffset` (no cap).

**FIX DELIVERED 2026-07-16 — draft PR #12133 (Main-verified @ this session).** Backend-IR Option C implemented exactly to jkwak's direction. Verified independently: PR #12133 OPEN draft, author `nv-slang-bot[bot]`, branch `fix/issue-9382-c`, `Closes #9382`, assignee+requested-reviewer jkwak-work. Mechanism (from PR body): new IR op `kIROp_ImageGatherOffset` (slang-ir-insts.lua + stable id **898**) + `__intrinsic_op` helper `__spirvImageGatherOffset` routing BOTH singular `__texture_gather_offset` `spirv` cases to it (the two `spirv_asm` blocks with hard-coded `OpCapability ImageGatherExtended;`+`Offset $offset` REMOVED); backend `emitImageGatherOffset` + `isConstantGatherOffset` (IRConstant leaf OR MakeVector/MakeVectorFromScalar of IRConstants) picks ConstOffset/no-cap vs Offset/+cap. **NO spirv_asm-block iteration** (the rejected mechanism); plural `__texture_gather_offsets` + shadow gatherCmp untouched. 3/3 variants PASS at **-O0** `SLANG_RUN_SPIRV_VALIDATION=1` (int2(2,1)+splat→ConstOffset/0-cap; runtime→Offset/1-cap); gh-5339 runtime guard + broader gather suite green; NonUniform propagation verified end-to-end (strictly better than removed asm form).

**All 3 prior PRs now CLOSED (Main-verified):** #11655 (07-08), #9410 (07-07), **#9741 (jkwak closed his OWN draft 07-16 01:24)**. jkwak's "close existing PRs" fully satisfied.

**Review/next:** 2 code-review agents + codex PLAN+CODE+OUTPUT approve, 0 correctness defects; peer review dispatched to slang-reviewer (advisory). Issue verdict refreshed in place (comment 4987047721). **UNDER ACTIVE MAINTAINER REVIEW 2026-07-17→18:** jkwak-work posted 4 review threads on #12133 → all accepted/resolved by 07-18; PR entered merge queue (not yet merged). Final shape = Option C + module-version bump **25→26**, head `f3e9310759` (fixer re-pushed → awaiting re-approval/re-queue). Interim work dies at triager tier per steer; triager forwards only terminal merge/[Fix Report] and will PATCH issue comment 4987047721 by ID on merge. **PR stays DRAFT** (drafts-only guardrail) — a maintainer flips ready → merge. Auto-closes #9382 on merge. Recoverable build hiccup during dev (`$(kIROp_...)` splice capitalization) fixed same-turn, learning shared. Benign `ci_failed` webhook = draft priority-yield (33 builds skipped), self-reruns.

**RE-OPENED 2026-07-16 — maintainer gave concrete direction.** jkwak-work commented on issue #9382 (comment 4986908901, real `@nv-slang-bot` mention): close existing PRs, then create a NEW PR on the **backend-IR approach** — "remove the function body from the frontend such as *.meta.slang files, and re-implement the behavior on the backend by adding a new IR"; the backend figures out offset constness and decides which capability to request. This IS the previously-scoped Option C. Routed to slang-triager on canonical thread `gh-issue-shader-slang/slang-9382` with `<github-post-authorized />`. **Triager progress 07-16 (msg #70):** re-triaged @HEAD 623227f86e; **slang-fixer DISPATCHED** on canonical thread (memo msg 79 + triage-9382.md carrying exact HEAD line numbers, AVOID list, -O0 gate); **GitHub artifact posted** = issue #9382 comment 4987047721. Awaiting fixer's fresh draft PR (`Closes #9382`, report_pr_created) + [Fix Report], which triager forwards up. Triager's own architecture memo independently concluded Option C is the ONLY valid path (runtime-offset support ∧ no asm-walk ∧ valid at -O0) — no design ambiguity.

**PR-close step — verified state @HEAD 623227f86e (do NOT blanket-close):**
- **#11655** (our prior draft, `fix/issue-9382` @ d5f5d1e2f9) — already CLOSED by jkwak 07-08. Branch preserved on origin.
- **#9410** (Copilot) — already CLOSED 07-07.
- **#9741** — still OPEN but **authored/assigned to jkwak-work himself** (his own stale draft). Do NOT unilaterally close a maintainer's own PR; triager will surface, not force.

**Implementation direction (fixer released, PR stays DRAFT):** first-class gather IR op + C++ emit selecting `ConstOffset`/no-cap vs `Offset`/`ImageGatherExtended` from the offset operand's constness at emit/IR time. Remove `OpCapability ImageGatherExtended` + `Offset` authoring from the two singular `__texture_gather_offset` `spirv_asm` blocks in `hlsl.meta.slang`.
- ⚠️ **AVOID (jkwak explicitly rejected):** (a) iterating instructions inside the `spirv_asm` block (old #11655 `processImageGatherOffset`); (b) the `__requireImageGatherExtended` helper (invalid at -O0).
- The classifier `isConstantGatherOffset` (IR literal OR `MakeVector`/`MakeVectorFromScalar` of literals; NOT `MakeArray`) is reusable — but must run in the backend/emit for the new IR op, NOT by walking the asm block.
- **Validation gate (hard):** 3 variants must pass `-target spirv SLANG_RUN_SPIRV_VALIDATION=1` at **-O0** (spirv-asm masks pre-fold invalid state): `int2(2,1)` + `int2(1)` splat → ConstOffset/no-cap; `int2(uv)` runtime → Offset/+cap. Regression test on all three.
- Separate out-of-scope latent bug already flagged: scalar `__texture_gatherCmp_offset` hard-codes ConstOffset on a non-constexpr offset.

**Design journey (why it landed here):**
1. Original fix = IR-legalize pass (conditional `ConstOffset`/`Offset` by constness). 3 reviewers APPROVE_WITH_NITS, 0 bugs.
2. jkwak objected to the fix LAYER — manipulating spirv_asm-block internals in a pass (novel; precedent only covers placeholder resolution).
3. jkwak's helper-only re-steer (`__requireImageGatherExtended` gating the cap, letting spirv-opt fold the operand) was **INVALID at -O0** — `Offset`-without-cap before the fold. Caught by fixer verification pre-ship.
4. -O0 finding collapsed viable options to **Option C** = first-class gather IR op + C++ emit choosing ConstOffset/no-cap vs Offset/+cap by constness (transparent auto-fix, valid at all opt levels).
5. jkwak floated the `_constoffset`-suffixed function workaround on the issue + pulled in csyonghe/skiminki. We posted one factual option-space clarification (#4835425316) + one precision follow-up (#4835497271), then held.
6. jkwak CLOSED the PR 2026-07-08 — direction is `_constoffset` (or whatever the 3 maintainers converge on), his team's call.

**Key constraint (for any re-dispatch):** Slang overload resolution is constexpr-blind, so a separately-named `_constoffset` function is opt-in — the reported `Gather(s, uv, int2(2,1))` call WON'T auto-route to it and would still over-declare the cap. Only an emit-time/IR constness check (Option C) transparently fixes the reported call. `_constoffset` authors `ConstOffset` directly so it's valid at -O0.

**How to apply:** Chain is **TERMINAL**. Fixer **HELD**. Do NOT auto-release, do NOT re-open, do NOT post to GitHub. Re-engage ONLY if a maintainer posts a concrete direction (name + mechanism + how constant offsets reach it) — that webhook re-opens the chain and re-dispatches to the fixer (per reopen rule: substantive human comment re-opens). A maintainer floating an alternative + looping peers is NOT convergence. Triager owns canonical thread `gh-issue-shader-slang/slang-9382`. **Validation gate on any re-dispatch:** 3 variants (`int2(2,1)`, `int2(1)` splat → ConstOffset/no-cap; `int2(uv)` runtime → Offset/+cap) must pass `-target spirv SLANG_RUN_SPIRV_VALIDATION=1` at **-O0** (spirv-asm masks pre-fold invalid state). Separate out-of-scope latent bug already flagged to maintainers: scalar `__texture_gatherCmp_offset` hard-codes ConstOffset on a non-constexpr offset.
