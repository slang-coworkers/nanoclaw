---
name: project_12006_e41017_extern_cpp_false_positive
description: "#12006 E41017 false positive on __extern_cpp/export __global globals — DRAFT PR #12011, jkwak soft-approved + set @expipiplus1 second-reviewer gate; held"
metadata: 
  node_type: memory
  type: project
  originSessionId: c1ceffdf-bc09-464b-9122-b9041298c6e0
---

**UPDATE 07-08 (RE-OPENED → fixer active):** jkwak-work commented [comment 4919355234](https://github.com/shader-slang/slang/issues/12006#issuecomment-4919355234) — **"Let's go with the option B."** Maintainer-go fired. Approach **B** (as chosen) = exempt in `checkUninitializedGlobals` only when global has `IRExternCppDecoration` AND export/public linkage. Dispatched `slang-triager` (dispatch owner) → `slang-fixer`, **DRAFT-ONLY** (jkwak self-assigned/self-filed; `report_pr_created`; `Fixes #12006`). Branch `fix/issue-12006`, worktree `/workspace/agent/wt-slang-12006`; helper named `isHostProvidedGlobal`.

**⚠️ FIXER FINDING 07-08 (UNVERIFIED by me — carries `-dump-ir` receipts per fixer):** fixer msg #22 reports **Approach B's export-linkage clause is a DEAD condition** — `-dump-ir` shows `__extern_cpp` ALWAYS emits its own `[export(...)]` decoration, so `IRExternCppDecoration AND (export|public)` ≡ `IRExternCppDecoration` alone → **B collapses to A**. This CONTRADICTS the triage digest (triage-12006.md L29 believed bare `__extern_cpp`-without-export existed) → must be shown with dump-ir evidence, not asserted. Fixer proposes shipping the honest single-`IRExternCppDecoration` check (matches the 3 existing inline exemptions; no dead OR-clause), and rewrote the companion test to a satisfiable discriminator: `export __global` WITHOUT `__extern_cpp` still warns E41017 (proves exemption is `__extern_cpp`-keyed, not export-broad — i.e. NOT Approach C). **Key steering (routed via triager):** jkwak chose "B" by name and the verdict comment labeled the single-check as "A", so the DRAFT PR must frame this as a maintainer either/or WITH dump-ir proof (single-check vs keep-explicit-AND-for-future-proofing), NOT a silent A-for-B swap; clean up the confusing "Approach C" label. Draft-gated → jkwak decides on review. Awaiting draft PR.

**DRAFT PR #12011 OPEN 07-08** — https://github.com/shader-slang/slang/pull/12011 (`fix/issue-12006`, worktree `/workspace/agent/wt-slang-12006`). 3 files +68: `isHostProvidedGlobal` helper (kept **explicit B** = `IRExternCppDecoration` AND export/hlslExport/public, one early-return in `checkUninitializedGlobals`) + 2 regression tests. Tests PASS; full `tests/diagnostics/` 664/664 (8 ignored), no regressions. `gThing` no longer warns; ordinary `export __global` (no `__extern_cpp`) still warns E41017. **Framing landed correctly:** PR body hands jkwak the either/or INLINE with `-dump-ir` receipts (external-linkage clause is redundant today since `__extern_cpp` always carries `[export]`; kept his named pick, offered collapse to single check) — did NOT silently swap B→A. Reviewer (`slang-reviewer`) dispatched; codex plan/code/output all approve. CI `ci_failed` webhook on #12011 = priority-yield cosmetic (all builds skipped, check-ci only on draft manual dispatch) — no action. **Gated:** ready-flip + merge operator/maintainer-authorized only. Awaiting reviewer verdict + jkwak spelling decision (single-check vs keep-explicit-AND).

**Draft-held issue footprint POSTED 07-08** (triager, fresh delta comment; fixer told NOT to dup): https://github.com/shader-slang/slang/issues/12006#issuecomment-4919976907. Triager VERIFIED #12011 live by reading it: OPEN/draft, base `master` ← `fix/issue-12006` (origin `c79e3c3`), label `pr: non-breaking`, `Fixes #12006` present, body keeps explicit-B verbatim + `-dump-ir` redundancy inline + decision handed to jkwak ("say the word and I'll simplify… the choice stays with you"), companion = **discriminator test** (not "Approach C"). `report_pr_created({repo, pr_number:12011})` **CONFIRMED called** by fixer right after opening (triager relayed) → #12011 review/CI/verdict webhooks pin to the fixer session, not orphaned. **Follow-up armed:** #12011 webhooks (fixer owns) + #12006 comments (triager owns). Chain in webhook-driven HOLD — no polling. **Next human actions:** reviewer verdict + jkwak's spelling either/or (keep explicit-B vs collapse to single `IRExternCppDecoration` check), then maintainer merge — all gated.

**UPDATE 07-08 23:53 — jkwak soft-approved + set second-reviewer gate:** @jkwak-work reviewed #12011 (COMMENTED, not REQUEST_CHANGES): *"Looks good to me. But I want @expipiplus1 to review before merging."* → approach endorsed; he tagged @expipiplus1 himself. Fixer correctly took NO action (no change requests to address, draft stays draft, no bot GitHub reply — jkwak's comment IS the footprint; fixer doesn't nominate reviewers or flip ready). He did NOT pick a spelling (explicit-B vs single-check) in the comment — that either/or is still open, may resolve with @expipiplus1's review. **Held for:** @expipiplus1 review + `slang-reviewer` pipeline (still running). Ready-flip/merge maintainer/operator-gated. Webhook-driven.

**CI 07-09 01:00 (fixer, plausible-not-verified-by-me):** real `pull_request` CI on #12011 = 11019/11020 pass. Sole failure `tests/compute/static-const-matrix-array.slang.3 syn (llvm)` on `test-windows-release-cl-x86_64-gpu` ONLY (all other platforms green). Fixer classified **flake** — diagnostic-only diff has no causal path to a static-const-matrix compute test; single-runner unrelated-test fail = textbook flake. `gh run rerun --failed` dispatched (1/3 flaky budget); no re-push/edit. ⚠️ If the rerun ALSO fails on the same test, flake theory weakens → re-examine at claim-precision per [[feedback_verify_regression_claims_at_precision]] before accepting.

---

shader-slang/slang **#12006** — `warning[E41017]` "use of uninitialized global variable" fires on `export __global __extern_cpp` globals whose value is supplied by the host at runtime (no in-module initializer by design). False positive. Filed by nv-slang-bot **at @jkwak-work's request** (jkwak self-assigned), motivated by **#11989** (blocks `-warnings-as-errors` on `cpu-com-example`). Issue Type=Bug, milestone Q3 2026. Severity low / P3.

**Triage (VERIFIED @ HEAD d8e8e1a9e, `-target cpp`, no GPU):** repro confirmed; `= {}` is not a fix (trades E41017 for E30521 interface-default-init deprecation). Root cause = `checkUninitializedGlobals` at `source/slang/slang-ir-use-uninitialized-values.cpp:1210` exempts `IRSemanticDecoration`/`IRGlobalInputDecoration`/`IRVulkanHitAttributesDecoration` (L1218-1224) but not `__extern_cpp`/`export`-linkage globals. False-negative-safe to add: diagnostic only reached when no init block AND no Store use (early-exits L1227-1232, L1242-1243).

**Approaches:** A = exempt bare `IRExternCppDecoration` (issue's 1-line hypothesis, slight over-exemption); **B (recommended)** = `IRExternCppDecoration` AND export/public linkage, matches `docs/cpu-target.md:275-278` host-set-global intent, optionally via `isExternallyProvidedGlobal` helper; C = any exported global (over-broad, NOT recommended). Load-bearing decision = predicate width A vs B → **maintainer call**.

**Routing: PARK — NO auto-fixer.** Per [[feedback_no_autofixer_jkwak_self_filed]] (jkwak self-filed + self-assigned + hinges on maintainer predicate decision). Same pattern as [[project_11806_cmake_options_maintainer_selffix]], [[project_11746_witnesstable_refactor_pending]], [[project_9062_diffptr_array_spirv_validation_parked]]. Re-engage on maintainer go / a PR / a human comment. Canonical thread `gh-issue-shader-slang/slang-12006`. Drafts-only if a fix is ever authorized.

**GitHub footprint EXISTS:** verified-triage verdict comment posted by nv-slang-bot → https://github.com/shader-slang/slang/issues/12006#issuecomment-4919328448 (confirms repro + root cause + A-vs-B maintainer call). Issue Type already `Bug`, assignee `jkwak-work` — no change needed.

**Token caveat (triager report, unverified by me):** triager's scoped `gh` token could POST the issue comment fine but got **403 "Must have admin rights"** on label-write — could NOT apply a `reproduced` label (comment states repro in text instead). This *contradicts* [[project_nv_slang_bot_readonly_incident]] which says labels work; may be a per-coworker token-scope difference vs a regression. One data point — not rewriting that memory without receipts. Watch for label-write 403 on other chains this session.

Triage memo: `/workspace/inbox/a2a-1783545742442-1bp718/triage-12006.md`.
