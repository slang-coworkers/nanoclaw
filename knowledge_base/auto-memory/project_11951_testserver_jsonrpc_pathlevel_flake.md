---
name: project_11951_testserver_jsonrpc_pathlevel_flake
description: "✅ RESOLVED #11951 Sig-B test-server JSON-RPC IPC-drop flake — root-caused as AVX-512 JIT SIGILL; fixed by #12056 (MERGED 01adc68f3); jkwak-work closed 07-15"
metadata: 
  node_type: memory
  type: project
  originSessionId: ddab6188-0f28-4222-8b37-6ae1cbb4b414
---

**⚠️ TWO LIVE FOLLOW-UP THREADS on the closed issue (07-15) — do NOT assert "#11951 fully closed" as fact:**
1. **UAF follow-up [our chain — ✅ MERGED 07-15 21:40Z].** PR #12114 MERGED to master by jkwak-work, merge commit `bf7a78ab25f4` (both files present: `slang-llvm-builder.cpp` +17, `tests/llvm/jit-teardown-debug-info.slang` +30). `closingIssuesReferences: []` — correctly did NOT touch #11951 (follow-up, no `Closes`; jkwak's AVX-512-flake close stands). UAF chain CLOSED. Historical detail:  pdeayton-nv (cmt 4977128283) proposed a *distinct latent* order-of-destruction **use-after-free** in `LLVMBuilder::generateJITLibrary()` — a SECOND bug in the same JIT path, NOT the flake. Triager CONFIRMED by static read + codex, posted finding 4977296282. slang-fixer produced **PR #12114** (HEAD `d94b9ae85c`, +43/−0: reset borrower members before ORC ThreadSafeModule move + regression test `tests/llvm/jit-teardown-debug-info.slang`). **NOW NON-DRAFT (maintainer flipped it ready, not the bot) + DUAL-approved: juliusikkala 09:48Z + jkwak-work 14:04Z, both on HEAD `d94b9ae85c`;** reviewDecision=APPROVED, MERGEABLE, held only on human merge. Devin clean (0 bugs), clarity drift-free. Correctly NO `Closes #11951` (follow-up; jkwak's flake-close stands). Round-1 CHANGES_REQUESTED was on the root-cause *explanation* only (real mechanism: context freed INSIDE `generateJITLibrary` when `jit->initialize()` materializes a module with `llvm.global_ctors`, before builder dtor — verified vs LLVM 21.1.2; declaration order NOT the crux); fix logic unchanged, re-approved. Debug-info-gated (`-g`) → never caused the default `syn (llvm)` flake. Triager delta comment 4979243693 credits juliusikkala + links #12114. **Next: maintainer flips draft→ready+merge (drafts-only gate; not bot's).**
2. **AVX-512 fix-gap question [babysitter's surface, deferred-to-human].** A bot session posted 07-15 08:19Z (cmt 4978396335) arguing the ORIGINAL AVX-512 flake may have a residual fix-gap despite #12056 — a genuine FAILED with the disable active, mechanism unproven — explicitly left to the human to decide on reopen. Babysitter owns this; not acting on it. See the babysitter-playbook nuance below (workflow-staleness vs genuine gap).

---

**✅ RESOLVED / TERMINAL (2026-07-15) — for the ORIGINAL flake only.** jkwak-work closed #11951 (stateReason COMPLETED, cmt 4975629716) pointing at **PR #12056 "Apply AVX-512 JIT workaround to all CI tests" — MERGED 2026-07-10, commit `01adc68f3` confirmed in master.** Root-cause fix (maintainer option A), NOT a quarantine/mask. **Mechanism (triager-verified with in-tree receipts):** on virtualized hosts incl. Windows runners, LLVM host-detection over-reports AVX-512 → JIT emits `kmovd`/masked `vmovss` → physical host rejects → `EXCEPTION_ILLEGAL_INSTRUCTION` → slang-test test-server CHILD terminates → parent sees the JSON-RPC `waitForResult()/hasMessage()` drop. Fix activates in-tree `disableAVX512ForJIT` helper (source/slang-llvm/slang-llvm-jit-shared-library.cpp) by exporting `SLANG_DISABLE_AVX512=1` UNCONDITIONALLY in ci-slang-test.yml:99 — generalizes the pre-existing Linux-CPU-only #11062 workaround to all CI tiers incl. windows-debug-gpu. Options B (quarantine) / C (reduce server-count) retired as unnecessary. Triage had the mechanism right (child crash, not RPC-layer bug) and correctly ruled out the "harden IPC retry" dead-end; precise AVX-512 cause needed a virtualized-Windows repro we couldn't produce. Merge-queue disruptor cleared at source. Reopen only on a fresh substantive human comment.

**⚠️ OPERATIONAL CONSEQUENCE — babysitter playbook FLIPS (Main-verified 07-15 04:1xZ):** as of #12056-merge (07-10) + #11951-close (07-15), Sig-B is **fixed at source**, so the babysitter's "rerun Sig-B under cap, known open flake" playbook is STALE for its single biggest bucket. New rule relayed to babysitter: a Sig-B fingerprint (`*.slang.3 syn (llvm)` JSON-RPC `waitForResult()/hasMessage()` drop) is **no longer routine** — it's now a SIGNAL to interpret. **Nuance I verified:** today's Sig-B hits (#12105, #12064) are on bases that ALREADY CONTAIN `01adc68f3` (compare=ahead) with heads pushed post-fix (07-15 00:45-00:53Z) — yet still showed the fingerprint at 04:13Z. Two explanations, and the babysitter must distinguish: **(a) workflow-file staleness [most likely, benign]** — `pull_request` runs use `ci-slang-test.yml` from the PR **head tree**; a branch whose base pointer advanced past the fix but never merged the fix's workflow change into the branch still lacks the line-99 export → **rebase/merge-base clears it** (verify: did that run actually export `SLANG_DISABLE_AVX512=1`?); OR **(b) genuine fix-gap [reopen signal]** — if the run DID have the export active and Sig-B still fired, the fix doesn't cover that path → flag for #11951 reopen. Don't reflexively rerun-under-cap anymore; check fix-active-in-run first.

**⚠️⚠️ POSSIBLE FIX-GAP — (b) determined, hedged follow-up authorized to jkwak (07-15 04:19Z, Main-verified crux):** babysitter ran the (a)/(b) diagnostic on the 2 reruns → **both land on (b), NOT (a):**
- **#12105** (branch `fix/mimalloc-malloc-free`): head-tree `d50a386239` CONTAINS `SLANG_DISABLE_AVX512=1` at `ci-slang-test.yml:99` byte-identical to master; failing run **29376935541** (Main-verified: `pull_request` event, head `d50a386239` = **ahead of `01adc68f3`** = post-fix tree) **echoed `export SLANG_DISABLE_AVX512=1` ACTIVE** in the Test Slang step (babysitter read log line 3169, 02:02Z) — yet Sig-B `static-const-matrix-array.slang.3 syn (llvm)` still dropped at 02:38Z. Fix present + active + path still crashed = (b).
- **#12056 is workflow-ONLY** (ci-slang-test.yml + ci-slang-coverage-test.yml, no C++; the `disableAVX512ForJIT` helper it activates is the pre-existing #11062 in-tree binary code, long present) → **no lagging binary for a rebase to pick up** → "(a) rebase clears it" CANNOT apply when the export is already active. Genuine fix-gap, not stale-tree.
- **HONEST EVIDENCE LIMIT (babysitter self-flagged, I concur):** the parent-side `waitForResult()/hasMessage()` drop is GENERIC — consistent with AVX-512 SIGILL but NOT provable from the drop alone (no `0xc000001d`/illegal-instruction string surfaces; child dies silently — confirmed absent in #12105's 16.7k-line log). So the mechanism is unproven: EITHER the env var isn't reaching the JIT on the **windows-debug-gpu tier** specifically, OR the child death has a different cause sharing the fingerprint.

**DECISION (Main):** surface to jkwak as a **hedged factual follow-up comment on the (closed) #11951** — NOT a bot-reopen. His close (00:52Z), his call to reopen. Frame exactly as babysitter proposed: "#12056's export was active on failing run 29376935541 yet the LLVM-synth `.slang.3 syn (llvm)` path still dropped on windows-debug-gpu — either the env var isn't reaching the JIT on that tier, or the child death has a different cause; worth a look before declaring #11951 fully closed." Babysitter posts (closest-to-state, established #11951 occurrence-poster, ungated). Bot does NOT reopen the issue itself. If jkwak agrees → HE reopens. #12064: same head-tree state but failed-attempt log gone (rerun rolled to attempt 3) → only head-tree + fingerprint, no run-echo; cite #12105 as the receipt.

**⚠️⚠️⚠️ RECEIPT MAY BE CONTAMINATED — correction ARMED (07-15 06:00Z, Main):** the fix-gap flag above rests on ONE receipt (#12105 run 29376935541); the other witness #12064 is GONE. New evidence materially weakens it:
- **#12064 CLEARED on rerun (attempt 3 green).** It was author=LDeakin, a Flat-decoration fix (`#10909 SubgroupLocalInvocationId`) — **nothing to do with allocator or AVX-512.** A rerun-clears = plain flake, NOT a fix-gap witness. Removed from the escalation.
- **#12105 is an ALLOCATOR-REPLACEMENT PR** ("Use mimalloc for Slang-owned malloc/free on Windows", frankie-yan-nv; 17 files incl. slang-allocator.h, slang-mimalloc.cpp, slang-zip-file-system.cpp, slang-deflate-compression-system.cpp, unit-test-allocator.cpp, unit-test-file-system.cpp). Its 04:10Z rerun dropped a contiguous `slang-unit-test-tool/*.internal` batch = **`defaultAllocator`, `scopedAllocation`, `compression`, `fileSystem`, `zip*`, `repro*`** — which **maps almost 1:1 onto the PR's own changed files.** Strong signal this is a **PR-caused debug-only mimalloc heap fault** (debug-gpu red, RELEASE + both -gpu-rhi PASS = exactly a debug-only heap-check trip), NOT the generic AVX-512 fix-gap. Reproduced across 2 consecutive attempts (flake would likely clear).
- **So my hedged comment 4976834356 to jkwak may point at a confounded PR.** The comment WAS hedged ("or the child death has a different cause") — which literally anticipates this — but if #12105 is mimalloc-caused, the #12105 receipt is contaminated and shouldn't stand as fix-gap evidence.

**OPEN DISCRIMINATOR (determines whether the receipt survives):** did run **29376935541** (the one cited to jkwak) drop on **`static-const-matrix-array.slang.3 syn (llvm)`** (a constant-fold test, NO allocator → CLEAN fix-gap receipt) or on the **allocator `.internal` batch** (CONTAMINATED)? Babysitter's 04:19Z msg framed 29376935541's drop as the `syn (llvm)` test; its 06:00Z msg describes the allocator-batch drop (possibly a DIFFERENT attempt). Asked babysitter to separate these.

**✅ DISCRIMINATOR RESOLVED — RECEIPT CONTAMINATED, RETRACTION FIRED (07-15 06:19Z):** babysitter pulled all 3 attempts of run 29376935541 (attempts API): `SLANG_DISABLE_AVX512=1` active on all (00:23Z/02:00Z/04:14Z); FAILED set on EVERY attempt = exclusively the allocator/repro/zip `.internal` batch (14 tests, maps 1:1 to #12105 changed files); **`static-const-matrix-array.slang.3 syn (llvm)` + `-vector-array` PASSED on all attempts, never failed.** Babysitter's 04:19Z `syn(llvm)` framing was a MISREAD (grepped test name → caught `passed test:` line, not `FAILED test:`). So: BOTH fix-gap witnesses VOID (#12064 cleared=flake; #12105=own mimalloc debug-heap fault); **NO evidence `SLANG_DISABLE_AVX512=1` fails to reach the JIT.** My comment 4976834356 was factually wrong.
- **Main GREEN-LIT the retraction (07-15 06:1xZ):** babysitter EDITS 4976834356 in-place (last commenter) — retracts both receipts, attributes #12105's batch to its own mimalloc change, **#11951 stays CLOSED, no AVX-512 tier bug to chase.** Verification bar for retracting an over-claim < making one; conservative/self-correcting direction; I'd independently confirmed the file-overlap half. Left a wrong fix-gap claim on jkwak's just-closed issue = worse error than retracting.
- **#12105 mimalloc debug-gpu fault is SEPARATE + real** (APPROVED PR, jkwak WOULD_APPROVE'd per [[project_12105_mimalloc_windows_malloc_free]]) → babysitter's author-steer (frankie-yan-nv check the debug-gpu unit-test-server crash) proceeds on ITS next-sweep discriminator (fresh push clears→flake; same-batch re-drop→author-owned), NOT on #11951. Keep the two issues clean.

**⚠️⚠️⚠️ FIX-GAP IS REAL — CLEAN RECEIPT (07-15 08:05Z, Main-verified from raw job log; REVERSES the "no bug to chase" conclusion):** merge_group run **29390282163** (job `test-windows-release-cl-x86_64-gpu/test-slang`, evicted APPROVED PR **#12064**): **`export SLANG_DISABLE_AVX512=1` present** (05:29:16Z) YET `FAILED test: 'tests/compute/static-const-matrix-array.slang.3 syn (llvm)'` on a **genuine FAILED line** (+ `gh-5900.slang.1 syn (llvm)` dropped) with JSON-RPC `waitForResult()/hasMessage()` drop. **#12064 = LDeakin Flat-decoration fragment-shader fix — UNRELATED to LLVM-synth constant-fold compute** → this is the clean, non-PR-related receipt the #12105 evidence never was (per [[feedback_signature_grep_passed_vs_failed]]: escalation needs a receipt on a test unrelated to the PR's change — #12064 IS that). windows-**release**-gpu (the retracted #12105 confound was debug-gpu) → **Sig-B spans BOTH GPU tiers with the export active.**

**⚠️ MY RETRACTION OVER-CORRECTED (meta-lesson):** the 06:23Z retraction was RIGHT that the #12105 receipt was contaminated (allocator PR, allocator tests) — but WRONG to conclude "#11951 stays closed, no AVX-512 tier bug to chase." Killing a contaminated receipt ≠ disproving the hypothesis; it just left it UNPROVEN. Correct retraction would've been narrow: "#12105 receipt withdrawn; fix-gap neither confirmed nor denied — need a clean receipt." Now #12064 IS the clean receipt → fix-gap CONFIRMED at claim-precision.

**MECHANISM STILL UNPROVEN (hold the hedge):** generic parent-side `waitForResult/hasMessage` drop; NO `0xc000001d`/illegal-instruction string surfaced in this job's log either. So EITHER #12056's `SLANG_DISABLE_AVX512=1` isn't fully suppressing the JIT path on GPU tiers, OR a second cause shares the fingerprint. Fix-gap = FACT; AVX-512-specifically = still inferred.

**RE-SURFACE to jkwak (07-15, Main-directed, babysitter posts):** a **NEW comment** (not edit — edits don't NOTIFY, and he needs to see this to reconsider his close; substantive new evidence contradicting the prior conclusion justifies the notify-post over anti-stacking hygiene). Framing OWNS THE ARC honestly: "correcting my retraction above — it was right about #12105 (that WAS its own mimalloc), but a clean receipt on the UNRELATED #12064 shows `syn (llvm)` genuinely FAILING with the export active, evicting an approved PR → a Sig-B fix-gap does appear real; mechanism unproven; your call whether to reopen." Bot does NOT reopen — jkwak's issue, his decision. Present evidence, defer decision, don't editorialize a hard "REOPEN."

**2nd CLEAN RECEIPT (07-15 16:05Z, Main-verified from raw log):** run **29419385541** (#11979 merge-group, `test-windows-release-cl-x86_64-gpu`) — `export SLANG_DISABLE_AVX512=1` ACTIVE (13:59:26Z), `FAILED test: 'static-const-matrix-array.slang.3 syn (llvm)'` genuine FAILED line (14:11:39Z), no SIGILL string. **#11979 = jvepsalainen-nv examples-only PR — unrelated to LLVM-synth compute** → 2nd non-PR-related receipt. So now **TWO independently-verified clean receipts (#12064 + #11979), both windows-release-gpu, both export-active, both unrelated PRs** + a #12109 head-check 3rd datapoint (babysitter-reported, not Main-reverified). Pattern, not anomaly. (Each verified per [[feedback_signature_grep_passed_vs_failed]] trifecta — export-active + FAILED-line + unrelated-PR — after the 07-15 #12105 misread.)

**Escalation already LIVE with jkwak** ([comment 4978396335](https://github.com/shader-slang/slang/issues/11951#issuecomment-4978396335), posted 08:20Z). ~8h, no maintainer response yet (his call, no SLA). **Directed babysitter to EDIT 4978396335 in-place** (GraphQL updateIssueComment per [[feedback_github_comment_hygiene]] — REST PATCH 403s, GraphQL OK same token) to ADD the #11979 corroborating datapoint — this is ADDITIVE evidence strengthening the SAME live recommendation, NOT a contradiction needing a fresh notify-post, so edit-in-place is correct (don't stack a 3rd bot comment). Still jkwak's reopen decision; bot does not reopen.

**⚠️ DATAPOINT-COUNT DISCIPLINE (07-15 20:00Z) — escalation rests on the 2 CLEAN RUN-LEVEL receipts ONLY (#12064, #11979); do NOT inflate the count:**
- **#12111** = head-tree export check, NOT a run-level in-log echo → weaker corroborator, do NOT add to comment 4978396335 (head-tree ≠ run-echo).
- **#12109** (pdeayton-nv "SpecializationWorkList using scratchData") = babysitter counted it as a 4th datapoint, but #12109's KNOWN failure across all prior sweeps is its OWN **`-fno-rtti` typeinfo LINK break** in its new `unit-test-specialization-work-list.cpp` (author-owned, PR-related → FAILS the "unrelated-PR" trifecta prong). Main-checked run 29380400512 test-slang-gpu job: `syn(llvm)` lines were `passed test:` with export active; did NOT surface a genuine `FAILED syn(llvm)` line. So #12109 is likely CONFLATED with its own RTTI break — same class of error as the retracted #12105 receipt. **Do NOT count #12109 as a Sig-B fix-gap unless a genuine FAILED `syn(llvm)` line distinct from its RTTI link break is shown.**
- **Lesson reinforced:** every fix-gap datapoint must independently pass the trifecta (export-active + genuine FAILED-line + test unrelated to the PR's own change) before it counts. Two strong receipts > four mixed ones; diluting with weak/conflated datapoints repeats the #12105 credibility hit.

**#11951 NET: fix-gap CONFIRMED real — TWO clean verified run-level receipts (#12064, #11979) despite #12056; escalation live with jkwak (comment 4978396335); mechanism (AVX-512 residual vs second cause) unproven; awaiting his reopen decision. Do NOT re-escalate per-sweep — evidence is sufficient; only act on jkwak's response or a STRANDING (non-self-recovering) eviction.**

**Historical context below.**

---

**#11951** — recurring CI flake, the dominant `.slang.3 syn (llvm)` JSON-RPC IPC-drop ("Sig-B"): test-server child drops the IPC connection (`computeTrivialD3D12 sendCall` → `waitForResult()/hasMessage()`, 1 FAILED, **zero** compile/assert/device-loss) under `-server-count 8` on Windows GPU runners. Tracked Infra issue, labels `CI`/`Infra`, **assigned to jkwak-work** (maintainer, 07-08 17:07Z). Convergence lead with [[project — #11955 CPU-SIGSEGV on same `.slang.3 syn (llvm)` boundary]] posted 07-08 16:17Z (not frame-confirmed).

**07-10 06:00Z — GENERALIZED to path-level (my generalization-watch trigger fired):** the identical fingerprint hit a **2nd distinct test file** `static-const-vector-array.slang.3 syn (llvm)` (was only `static-const-matrix-array`), run 29068393170. Two trivial `static const` array constant-fold tests, same synth/JIT+IPC path → root cause is the **LLVM-synth test-server JSON-RPC path itself under `-server-count 8`, not any one test's codegen.** Strengthens harden-the-IPC-path fix direction over per-test quarantine (quarantine just moves the drop to the next `.slang.3 syn (llvm)` in queue). Babysitter posted the evidence to [#11951 comment 4932530933](https://github.com/shader-slang/slang/issues/11951#issuecomment-4932530933), notifying jkwak-work.

**Disposition (07-10):**
- **NO bot fixer chain.** Deep infra, high blast radius, maintainer-assigned → jkwak-work drives. Do NOT auto-dispatch a fixer. Babysitter role holds: surface + route + rerun-under-cap.
- **Not an operator escalation yet.** Reruns-under-cap is the working workaround; maintainer owns fix; issue-comment notifies assignee.
- **Throughput cost:** #12030 evicted from merge queue 07-10 04:09Z by same Sig-B drop; left unrequeued (stale SHA 824e24f2→12ab082d, bot enqueue 403 per [[project_nv_slang_bot_readonly_incident]] / #11675, needs author/GitHub-auto requeue).

**Escalation thresholds (ARMED — babysitter will flag as separate operator-prioritization line):** either (a) a **3rd** distinct `.slang.3 syn (llvm)` file catches the fingerprint, OR (b) Sig-B **merge-queue evictions cluster** (2+ PRs in one sweep). At that point it becomes an operator prioritization call regardless of maintainer assignment. Babysitter owns the CI-health surface + occurrence-posting; I hold the escalation-decision state.
