---
name: project_12274_ptx_buffer_empty_kernel
description: "#12274 [PTX] typed Buffer<T>.Load silently emits empty CUDA/PTX kernel — triaged, parked"
metadata: 
  node_type: memory
  type: project
  originSessionId: bf28cc43-ed22-4083-b4ab-072df74f26be
---

**shader-slang/slang#12274** — `[PTX] Buffer access silently compiles to an empty kernel`. Author + self-assignee: jkwak-work.

**07-30 20:10Z — PR #12289 (fix/issue-12274) has a CROSS-REPO REGRESSION (routed to slang-fixer).** Bot PR "Fix #12274: diagnose typed Buffer<T> access on CUDA/PTX instead of silent empty kernel" (non-draft, REVIEW_REQUIRED). Babysitter re-classified + Main-verified: the PR's OWN new diagnostic **`E36107` "unavailable features in entry point"** now fires on slangpy's downstream `tests/device/test_buffer.slang:30 copy_buffer_uint` (typed `Buffer<uint>` on `DeviceType.cuda`) → **4 tests fail (byte_address_buffer + structured_buffer_uint × {128,1024}) on BOTH linux+windows, deterministic** (slangpy run 30564544350). Sole red (44 pass/2 skip/1 fail = SlangPy Tests). NOT a flake, NOT rerunnable (bot gateway slang/*-scoped anyway). Tension: the diagnostic is arguably CORRECT (typed Buffer<T> genuinely broken on CUDA = the #12274 bug) but rejects code slangpy's test relies on. Recent #12289 comments = fixer/jkwak on diagnostic DESIGN (pointer+size limitation), NOT the slangpy break → fixer likely unaware. Decision (fixer's, w/ jkwak): narrow diagnostic scope, OR update slangpy test_buffer.slang + bump pin, OR coordinate both-repo. Merge OP-gated.

**07-30 20:15Z RESOLVED-DIRECTION: diagnostic CORRECT, fix is slangpy-side (Option 2). Routed to slangpy-triager.** Fixer investigated + Main-verified the crux: `test_buffer.slang` is ONE module with 3 compute entrypoints (`copy_byte_address_buffer`, `copy_buffer_uint` typed-Buffer, `copy_structured_buffer_uint`); #12289 correctly rejects `copy_buffer_uint` on CUDA (E36107), but Slang fails the ENTIRE module-load on one bad entrypoint (E39999→E40003) → the CUDA-clean siblings become unreachable = the 4 failures are COLLATERAL, not typed-Buffer failures. **Main-verified decisive fact:** slangpy `test_buffer.py:~76` ALREADY `pytest.skip("CUDA does not support Buffer/RWBuffer resources")` for buffer_uint → NO test relied on silently-broken behavior → Option 1 (narrow diagnostic) correctly REJECTED (would contradict slangpy's own stated position). **Fixer posted scoping analysis to jkwak (#12289 comment 5135787108); report at /workspace/agent/reports/slang-12274-slangpy-regression.md (fixer's FS).** slang-side #12289 needs NO change; merge OP-gated. **slangpy fix (routed to slangpy-triager, thread gh-slangpy-test_buffer-cuda-modulesplit):** keep CUDA-unsupported `copy_buffer_uint` from sinking shared module-load — split into own .slang / guard-out on CUDA / per-entrypoint compile — **GATED on #12289 merging** (today slangpy's pinned slang still loads the module; only #12289's E36107 via pin-bump breaks it), so restructure+pin-bump execute together on #12289 merge. Secondary (surface-only, NOT acting): one unsupported entrypoint failing whole-module-load is a long-standing Slang import sharp-edge, pre-#12289, noted to jkwak.

**07-30 20:16Z — precise slangpy-side spec (from fixer, for slangpy-fixer when gate opens):** 4 failing tests (linux+win, slangpy run 30564544350): `test_buffer[{128,1024}-byte_address_buffer-DeviceType.cuda]` + `test_buffer[{128,1024}-structured_buffer_uint-DeviceType.cuda]`. Files (repo shader-slang/slangpy): shader `slangpy/tests/device/test_buffer.slang` (1 module, 3 compute EPs; only `copy_buffer_uint` ~L28-41, typed `Buffer<uint>` src @L32); test `slangpy/tests/device/test_buffer.py:55` (already skips buffer_uint-on-cuda @:76-77). Fix options: (a) split copy_buffer_uint→own .slang [cleanest], (b) guard-out on CUDA, (c) per-EP compile. **Gate (Main-held, stricter than fixer's framing):** fixer notes module-split is pin-independent/"safe to fix slangpy first" — TRUE it won't break current CI, but I hold execute-gated on #12289-merges-AS-A because the OPEN A/B fork (jkwak wants Approach B = support Buffer-on-CUDA) would MOOT the split entirely if it lands. So design-now, execute (split+pin-bump) only on my signal "#12289 merged WITH diagnostic A". No slangpy issue# filed yet; repro = the cross-repo CI run. Fixer's full analysis = its local report slang-12274-slangpy-regression.md (not on my FS; ask fixer to paste sections if needed).

**07-30 20:26Z — slangpy-triager memo done + forwarded to slangpy-fixer (memo: /workspace/inbox/a2a-1785443200194-kmsx0w/triage-slang12289-slangpy-cuda-buffer.md).** Classification: regression (predicted, upstream-gated) / med→P1-when-A-merges / tests+build(pin) / CUDA / Upstream-Slang=no. **Recommended mechanism (b): guard `copy_buffer_uint` with `#ifndef __TARGET_CUDA__`** (2-line, test_buffer.slang:26-42; `__TARGET_CUDA__` injected pre-capability-check shader.cpp:488/496; in-repo precedent blit.slang:61-86). (a) split-module works but more surface; (c) harness per-EP rejected (wrong altitude). **⚠️ Main-verified GATE SHARPENING:** slang pin `external/CMakeLists.txt:85` `SGL_SLANG_VERSION "2026.12"` downloads a RELEASE TARBALL (L87 `releases/download/v${VER}/`), NOT a SHA → pin bump needs #12289 in a slang RELEASE TAG, not just merged-to-master. **Two-stage gate:** (1) #12289 merges AS-A → guard edit safe to land (pin-independent, won't break current CI); (2) #12289 ships in a slang release ≥ that tag → THEN bump pin. If B lands: drop guard + optionally un-skip test_buffer.py:76. **GitHub footprint DECISION (Main):** NOT filing a slangpy tracking issue now — regression is predicted/contingent on A, and the #12289 slang-thread I own already carries the public cross-repo footprint (fixer comment 5135787108). File slangpy issue (or fold into fix PR) only WHEN #12289 merges-as-A (regression becomes real-on-next-release). Resume: I signal slangpy-fixer/triager on #12289 A/B-resolution + merge + release-availability.

**07-30 22:15Z — #12289 A/B fork LIKELY RESOLVING TO A (approved, NOT merged — signal HELD):** `reviewDecision=APPROVED` — **csyonghe APPROVED the Approach-A diagnostic @20:13:59Z** (head da929bb6, E36107 still present = still the diagnostic, NOT a B/Buffer-on-CUDA impl). jkwak's earlier B scope-override was COMMENTED (not a formal block); bot flagged the slangpy break to jkwak @20:12. So A is winning (maintainer approval of the diagnostic as-is), BUT still OPEN/BLOCKED = NOT merged. **Signal HELD** — my gate is "#12289 MERGED as A", and approval≠merge + jkwak's B-request not explicitly conceded. Do NOT fire Stage-1 to slangpy-triager yet. WATCH: if it merges with E36107 intact → fire "merged as A" (Stage 1 guard + footprint); if jkwak reopens B and it pivots → fire "merged as B" (drop guard). babysitter re-flagged the regression @22:10Z (head da929bb6 unchanged) → told babysitter it's OWNED+GATED, log-as-left, don't re-flag.

**07-30 22:40Z — ⚠️ GATE INVERTED (Main-verified): SlangPy Tests is REQUIRED → guard is a PREREQUISITE to merge #12289, NOT a follow-up.** GraphQL `isRequired(pullRequestNumber:12289)` = **`SlangPy Tests: FAILURE required=TRUE`** (+ check-ci required=SUCCESS; all test-slang jobs required=FALSE). So #12289 (mergeStateStatus=BLOCKED) **CANNOT merge while SlangPy Tests is red.** Dispatch mechanism (`slangpy/.github/workflows/ci-latest-slang.yml`): repository_dispatch job `actions/checkout@v6` checks out slangpy test source from **main** + builds slang from #12289's PR ref (`slang-checkout-mode: pr`, `slang-ref`). ⇒ **the ONLY way to green SlangPy Tests = land the guard on slangpy MAIN**, which then makes the dispatch (slangpy-main test_buffer.slang guarded + #12289 slang w/ E36107) load clean → SlangPy Tests green → #12289 unblocks → merges as A. **CORRECTED ORDERING:** guard is NOT gated on "#12289 merged"; guard lands FIRST (it's the unblock). Guard safe on current pinned-slang too (guard PR's own CI builds slang=master, no E36107; buffer_uint-on-CUDA already skipped → guard is behavioral no-op → green). Pin-bump (Stage 2) STILL waits for #12289 in a release tag. If jkwak pivots to B: revert guard + unskip test_buffer.py:76. **Re-routed corrected gate to slangpy-triager: land guard NOW (prereq), don't wait for #12289 merge.**

**07-30 22:40Z — MAINTAINER-AUTHORIZED slangpy PR (Main-verified jkwak comment 5136975309 directly): jkwak-work @22:36:31Z "can you make a PR on slangpy repo for this?"** = explicit go-ahead to open the slangpy fix PR (fixer relayed + ack'd on #12289 cmt 5136981531, will cross-link the slangpy PR#). Converts slangpy fix from designed→AUTHORIZED-GO. Combined w/ required-check finding (guard=prereq to unblock #12289) → slangpy-fixer opens the Guard PR NOW. Posture: jkwak asked for "a PR" → open it (non-draft OK per maintainer request — but merge still maintainer/OP-gated; slangpy-fixer doesn't self-merge). slangpy-fixer→ reports PR# → I relay # to slang-fixer for #12289 cross-link. Repo-separation held: slang-fixer does NOT cross into slangpy; slangpy-fixer owns the slangpy PR.

**07-30 23:20Z — slangpy Guard PR #1083 UP (Main-verified), cross-linked to #12289.** https://github.com/shader-slang/slangpy/pull/1083 "Guard typed Buffer<uint> test entry point from CUDA compilation" — nv-slang-bot, non-draft, OPEN, base main, **+6/-0 one file** (`test_buffer.slang`); diff verified = exact `#ifndef __TARGET_CUDA__ ... #endif` guard around `copy_buffer_uint` w/ comment ref `shader-slang/slang#12289`. commit 25004c5. Fixer codex CODE/OUTPUT/PLAN approve; triager-side codex APPROVE no-must-fix (1 non-blocking advisory: WGPU not also guarded — pre-existing, WGPU∉DEFAULT_DEVICE_TYPES, out of scope). slangpy-reviewer verdict pending. Local L40S: CUDA byte_address+structured siblings PASS, buffer_uint skipped, 0 fail; PR multi-backend CI green-so-far (2 jobs still running). **Relayed #1083 to slang-fixer to cross-link on #12289** (jkwak requested + watching). **Fixer lane ENDS at green+ready (no self-merge); human (jkwak/slangpy maint) merges #1083 → #12289 SlangPy Tests re-fires green → #12289 unblocks.** MERGE-NUDGE-IF-STRANDED = Main's (watch #1083 green-ready-unmerged). Stage-2 pin-bump = Main's "in release vX" signal (still deferred). B-contingency: revert guard + reeval unskip test_buffer.py:76.

**07-30 23:20Z — slangpy-reviewer verdict on #1083: APPROVE_WITH_NITS** (0 bugs/0 gaps/0 Q / 1 optional non-blocking clarity nit = add cross-ref between the .slang guard and test_buffer.py:76 CUDA skip). send_file-only to fixer (no github-post-authorized marker → correct, no GH post). **De-risking completeness evidence (reviewer swept ALL .slang):** `test_buffer.slang` is the ONLY module affected by the E36107 whole-module-load abort — the one other bare-`Buffer` hit (unused global in `test_shader_cursor.slang`) passed 6/6 on CUDA in run 30582422093 → **#1083 alone greens #12289's required SlangPy Tests check** (no hidden 2nd module needing a guard). Fixer may ship as-is (nit optional). This confirms the merge-nudge I own will actually unblock #12289 once #1083 merges. Verdict flows reviewer→fixer→triager→me via normal [Triage Resolution]; this was a parallel status FYI.

**07-30 23:20Z — #1083 FULLY GREEN + at human merge gate; cross-link POSTED on #12289.** All 15 checks green (linux/macos/windows × debug/release + pre-commit + license/cla), non-draft, MERGEABLE, `mergeStateStatus=BLOCKED` = branch-protection awaiting a slangpy-maintainer review-approval (green-ready clock STARTS ~23:20Z). slang-fixer posted the cross-link on #12289 (comment 5137262390: links #1083, frames it as the prereq greening the required SlangPy Tests check, E36107 diagnostic stays as-is). **MERGE-NUDGE now LIVE but NOT yet due** — #1083 just went green; nudge only if it sits green-ready-unmerged long enough to strand jkwak's #12289 (per #12122/#12152 multi-hour pattern), NOT now. Note: triager msg 184 said "reviewer verdict pending" — stale/race; reviewer already returned APPROVE_WITH_NITS (msg 182), positive, changes nothing. Resume: #1083 merges (→#12289 unblocks) / #1083 stranded-green-ready (→I nudge maintainer) / #12289 in release (→my Stage-2 signal) / B-pivot.

**07-30 23:25Z — #1083 2nd commit `37a6942` (comment-only) + repro now CONFIRMED-FACT.** (1) slangpy-reviewer empirically reproduced the 4-sibling collateral vs #12289 run 30582422093 → mechanism + "#1083 alone suffices" are confirmed fact, not predicted (upgrades the earlier hedge). (2) #1083 now 2 commits, still one logical change: 25004c5 guard (byte-unchanged) + `37a6942` COMMENT-ONLY (adopts reviewer's optional nit = paired "must be removed together" cross-ref comments on .slang guard ↔ test_buffer.py:76 skip, documents B-contingency coupling in-source). Reviewer APPROVE_WITH_NITS, nit adopted, no round 2; all gates clear (codex×3 + peer + triager-codex). **CI RE-RUNNING on 37a6942** (first run was 15/15 green) → #1083 momentarily not-fully-green → **merge-nudge clock resets to when the new run settles green; do NOT nudge while CI re-running.** Merge-gate state unchanged (BLOCKED=awaiting maintainer approval).

**07-31 ~01:18Z — #1083 CONVERGED TO MERGE-READY (maintainer LGTM + nit resolved; approver WOULD_APPROVE R2).** Sequence: (1) synchronize R1 `37a6942` (comment-only) → approver WOULD_APPROVE. (2) jkwak-work reviewed **"Looks good to me."** (state=COMMENTED, review 4824527046) — positive LGTM, not a formal approve. (3) jkwak left ONE inline nit (discussion r3687323763): *"Remove this comment that is inconsistent to the verbosity of the rest of the if branches"* → routed to **slangpy-fixer** (MODE=pr-review-fix, github-post-authorized). (4) Fixer removed the 2-line cross-ref comment on the CUDA `buffer_uint` skip branch in **test_buffer.py:77-78** (commit `a31e8af`, `test_buffer.py +0/-2`, back to baseline; Black-clean, comment-only, no build), replied on thread r3687346117. **Kept** the substantive E36107/#12289 rationale comment in `test_buffer.slang` (not flagged, documents non-obvious why). (5) synchronize R2 `a31e8af88768` → approver **WOULD_APPROVE** (mode live_late; full PR now 1 file `test_buffer.slang` +7/-0; guard structure + CUDA skip ordering intact; 6/6 clauses, Devin 0 findings). **State: OPEN + MERGEABLE, human-gated; jkwak's LGTM + nit both satisfied.** Merge-nudge (Main-owned) now LIVE: nudge only if #1083 sits green-ready-unmerged long enough to strand (per #12122/#12152 multi-hour window). On #1083 merge → #12289 SlangPy Tests re-fires green → #12289 unblocks. Stage-2 pin-bump still deferred to "#12289 in a release tag."

**07-31 ~01:25Z — #1083 FORMALLY APPROVED by jkwak-work (branch-protection review gate SATISFIED).** review 4824709174, state=**APPROVED**, body *"Looks good to me. This PR avoids the invalid shader compilation when targeting CUDA."* Preceded by jkwak resolving the `test_buffer.py` inline-nit thread (housekeeping = satisfied with `a31e8af`). This is the STRONGEST positive signal + clears the review-required branch-protection gate that held #1083 BLOCKED. **#1083 now merge-ready in the strongest sense — merge is jkwak's/slangpy-maint's action (human-gated); fleet does NOT self-merge.** Approver stamping APPROVED against R2 ledger row (agreement scoring; was COMMENTED at R1). Merge-nudge (Main): watch for #1083 green-ready-APPROVED-unmerged strand; nudge only per multi-hour window. **On #1083 merge → #12289 SlangPy Tests re-fires green → #12289 unblocks → merges as A.** Stage-2 pin-bump still deferred to "#12289 in a release tag."

**07-31 ~01:26Z — ✅ #1083 MERGED (jkwak-work). PREREQ CLEARED for #12289.** slangpy main now carries the `#ifndef __TARGET_CUDA__` guard on `copy_buffer_uint`. **Follow-up (actionable NOW):** #12289's required **SlangPy Tests** check is still red at its last run — it does NOT auto-re-fire just because slangpy main changed; it needs a RE-RUN (repository_dispatch checks out slangpy main + builds slang from #12289 PR ref → now loads clean → green). ⚠️ Must NOT push to #12289 (auto-dismisses csyonghe's APPROVED). So re-run = human merger's action (jkwak) or merge-queue; signaled slang-fixer (owner of #12289) to advise/nudge jkwak to re-run SlangPy Tests (not push). On green → #12289 unblocks (mergeStateStatus BLOCKED→clear) → merges as A. **Stage-2 pin-bump** still deferred to "#12289 lands in a slang RELEASE TAG" (pin at `external/CMakeLists.txt:85` downloads a release tarball, not a SHA) — Main's signal to slangpy-fixer when that release ships. #1083 chain TERMINAL (merged); approver mining merge for calibration.

**07-31 00:46Z — jkwak actively asking clarifying Qs on #12289 (NOT stranded — do NOT nudge yet).** jkwak asked (cmt 5137843376) "why was SlangPy test working fine with Buffer for CUDA if we're disabling it?"; fixer answered (cmt 5137851606) w/ empirical proof: `copy_buffer_uint` was ALWAYS pytest.skip-ped (:76-77) so never ran on CUDA; the shared module compiled pre-PR only because the #12274 bug was SILENT (empty body, exit 0), letting the genuinely-CUDA-supported siblings pass; PR converts silent→E36107→whole-module abort→sibling collateral; #1083 guard restores module load. Fixer verified pre-PR(0err+empty)/post-PR(E36107) directly — consistent w/ all confirmed facts, no contradiction. **Merge-nudge timing implication: jkwak's understanding gates his merge, and he's actively asking Qs → #1083-green-ready-unmerged is HELD-PENDING-MAINTAINER-UNDERSTANDING, NOT stranded-by-inattention. Nudging now = premature noise into a live maintainer convo. Nudge only if it goes quiet+unmerged for the multi-hour #12122/#12152-style window AFTER jkwak's questions resolve.** A/B still a live maintainer fork (csyonghe APPROVED A; jkwak asked for B + is now watching the red @cmt 5136936142) — guard unblocks A and is trivially revertible if B wins, so landing it now is low-risk + high-value (unblocks jkwak's approved PR).

Compiling `Buffer<float4>` read → `RWStructuredBuffer<float4>` write for `-target ptx`/`cuda` succeeds with NO diagnostic but silently omits the load+store → PTX entry is just `ret;`. Silent miscompile + missing-diagnostic.

**Root cause (confirmed @HEAD 6462d7d2f, compile-only/no-GPU):** `Buffer<T>` = `_Texture<T,__ShapeBuffer,...>`. `_Texture.Load`'s `__target_switch` (hlsl.meta.slang:19361-19375) has NO `cuda` case; a case-less switch on the active target emits an EMPTY helper body `_Texture_Load_0(){}` (not a diagnostic), NVVM strips the dead ops. `Load` already carries `[require(glsl_hlsl_metal_spirv, ...)]` which *excludes* cuda.

**Triage verdict:** bug / medium / P2 / target-emit CUDA-PTX + core-module. Recommended fix = **A: fail loudly with a diagnostic** (enforce the existing capability at the CUDA call site; mirrors #6304 WGSL resolution; reporter explicitly accepts a clear diagnostic). Approach B (implement CUDA typed-Buffer load) = HIGH-risk feature work, needs GPU verify, may re-hit the PTX `tex.1d` limit that got the prelude impl `#if 0`'d.

Resembles **#6304** (WGSL Buffer/RWBuffer silent empty shader) but NOT a dup — no prior PTX/CUDA issue.

Triager applied `reproduced` label + Issue Type=Bug (left human's `Dev Opened` untouched), posted 5-bullet on issue (comment `5124885609`). Fix scope caveat: don't over-reject the StructuredBuffer family (real pointers on CUDA).

**RE-ENGAGED 2026-07-30:** jkwak-work commented "make a PR" (comment `5126152243`) → FIX AUTHORIZED, Approach A (diagnose), dispatched to **slang-fixer** (DRAFT-only, merge OP-gated) on thread gh-issue-shader-slang/slang-12274.

**Fixer refinement (@HEAD `7c58a326b1`):** true root = `__ShapeBuffer`'s *type-level* `[require(cpp_cuda_glsl_hlsl_metal_spirv)]` at `hlsl.meta.slang:572` admits cuda; a case-less `__target_switch` on `Load` infers an EMPTY capability set so the method-level require never enforces. WGSL already excluded there → clean `E36107`; cuda/cpp fall through to empty body. **Fix = drop `cuda`** → `[require(cpp_glsl_hlsl_metal_spirv)]` (exact WGSL precedent PR **#6585**, labeled `pr: non-breaking`). `-target ptx` routes via the cuda atom so one edit covers both. **Kept `cpp`** in-scope: CPP has a real `Buffer<T>` prelude type (`slang-cpp-types.h:103`), its gap is Approach-B, out of the cuda-only authorized scope. Scope-safe verified: StructuredBuffer family + `Texture2D.Load` unaffected. Regression test `tests/diagnostics/cuda-typed-buffer-unsupported.slang` (asserts `E36107` for cuda/ptx).

**DRAFT PR #12289** opened @ HEAD `bbfa0ed370`, branch `fix/issue-12274`, base master. Label `pr: non-breaking` (per #6585 precedent). Tests: repro 2/2; tests/diagnostics 708/708; tests/cuda 64/64; tests/hlsl-intrinsic 64 pass +1 pre-existing unrelated (scalar-bf16 vk). Two incidental-param test swaps preserve unrelated cuda coverage. **slang-reviewer 3-reviewer pass (A correctness / B Devin / C clarity) IN FLIGHT** — file-only (no github-post-authorized marker). **Merge OP-gated; stays DRAFT.**

**MAINTAINER SCOPE-OVERRIDE 2026-07-30 (~15:18):** jkwak-work left a review comment on #12289 (state: commented): *"I think Buffer should be supported for CUDA/PTX. Please implement it to support it."* = **Approach B**, which my dispatch forbade. Fixer surfaced rather than pivoting (correct).
- Feasibility (fixer read-only): `Buffer<T>`=`_Texture<T,__ShapeBuffer,...>`, binds on CUDA as `CUtexObject` (texture obj, not ptr); read needs `tex1Dfetch`. `tex1Dfetch_int` impl is `#if 0`'d @`prelude/slang-cuda-prelude.h:6107-6142` ("1D not supported via PTX"); `docs/cuda-target.md:133` confirms CUDA=tex1Dfetch only, linear-mem only. So B = design fork, both branches non-trivial + GPU-verify-gated (NO GPU here): (1) texture-backed = re-enable/fix the `#if 0` tex.1d inline-PTX (may be fundamentally PTX-blocked); (2) pointer-backed rebind = change CUDA binding CUtexObject→ptr+size like StructuredBuffer (larger emitter change, drops typed-format-conversion semantics).
- **ORCH DECISION (sequenced, autonomous):** engage jkwak on-thread NOW with the binding design decision + no-GPU caveat; KEEP #12289 diagnostic as interim; **NO B code until jkwak picks a binding**. Re-authorize the specific draft attempt after his pick; re-gate (possibly loop operator) before committing heavy unverifiable build cycles. Diagnostic PR = insurance if B proves infeasible.
- **Merge OP-gated; stays DRAFT.** Awaiting: jkwak binding pick + reviewer verdict on the current diagnostic.
- Fixer posted design-fork question to jkwak on #12289 (comment `5132779779`): both bindings + no-GPU/best-effort-draft caveat, leaning pointer-backed.
- **SCOPE RESOLVED 2026-07-30 (~16:50):** jkwak converged **back to Approach A** (comment `5133751931`): *"If the functionality of Buffer cannot be natively supported by CUDA, I think it makes sense to go with an error message; for now."* Fixer confirmed the technical reality on-thread (comment `5133766571`): `Buffer<T>`/`RWBuffer<T>` = HLSL 1D typed-buffer → `CUtexObject` → needs `tex1Dfetch` which PTX doesn't provide; pointer-backed route is a separate feature effort. **The E36107 diagnostic (#12289) IS the agreed fix — no B code, no build cycle.** Sequenced hold validated (interim held = final outcome). Fixer offered jkwak wording tweaks. Remaining: reviewer verdict on diagnostic (in flight) → human flip ready/merge (OP-gated).
- **2026-07-30 (~17:09):** jkwak left one inline finding (*"Remove this comment; too much implementation detail"* on the 2-line why-comment above `[require]`); fixer removed it in commit **`97ab19e`** (pure comment deletion, behavior-neutral; `[require]` self-documents), replied comment `5133938950`. Code-push per review finding, not OP-gated. **jkwak flipped PR to NON-DRAFT himself** (human/maintainer action, NOT a bot drafts-only breach — same pattern as [[project_12265_byteaddressbuffer_atomic_shared_type_mutation]]); fixer did NOT flip. CI: `ci_failed` on 97ab19e = fixer's own redundant `workflow_dispatch` priority-yield (run 30564537587); real `pull_request` runs = SUCCESS (cosmetic red, [[project_bot_pr_priority_yield_red_run]]). **Reviewer 3-pass runs against `bbfa0ed370`; HEAD now `97ab19e`, delta = comment-only/behavior-neutral → verdict stays valid, no re-dispatch.** HOLDING: reviewer verdict → human merge (OP-gated).
- **2026-07-30 (~20:14): #12289 APPROVED by maintainer csyonghe** (review `4822869229`). Approval commit `97ab19ec` == current HEAD → **live/valid, not stale**. Non-draft; `mergeStateStatus: BEHIND` (branch behind master). Fixer HOLDING correctly: NO push (any commit incl. update-branch auto-dismisses csyonghe's approval per standing order; if rebase needed to clear BEHIND that's the human merger's action); ready/merge OP-gated. **Remaining: human merge only.**
- ⚠️ **OPEN THREAD — slangpy collateral:** fixer asserts a "slangpy-side collateral fix is with orch to route to slangpy-fixer" — needs slang pin bump after #12289 lands, independent of the merge. **Orch has NO details in this chain** (no what/where/fix). Asked fixer to send specifics before routing to slangpy-fixer. Do NOT fabricate ownership; do NOT drop.

Final fixer report: 4 files +23/−3; edit landed `hlsl.meta.slang:574`; 3 codex gates (PLAN/CODE/OUTPUT) all APPROVE; webhooks mapped (report_pr_created done). Latest CI `ci_failed` webhook = benign priority-yield (only `wait-for-human-priority`+`check-ci` "fail", builds skipped) — NOT a real failure ([[project_bot_pr_priority_yield_red_run]]). NOTE: **#12274 ≠ #12273** — #12273 (CUDA callable OUTPUT-param crash) is covered by PR #12280; this (#12274, typed-Buffer silent miscompile) = PR #12289. Distinct roots. Related: [[project_12192_e55215_constantbuffer_no_source_location]] (CUDA/CB emit), [[project_12273_cuda_callable_output_crash]] (CUDA crash cluster).