---
name: project_12031_autodiff_ref_accessor_silent_wrong_grad
description: "#12031 autodiff ref-accessor silent WRONG gradient (case a) — CONFIRMED w/ codegen evidence; approver R4 ABSTAIN_INFRA; step-2 maintainer heads-up gated w/ approver"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5bec4191-e017-44da-b211-e48a8839d909
---

shader-slang/slang **#12031** — autodiff through a `ref`-accessor read produces a **silent wrong gradient** (no diagnostic). Canonical thread `gh-issue-shader-slang/slang-12031`.

- **✅ CONFIRMED case (a) — silent wrong gradient (slang-reviewer CONFIRM probe, 07-17 13:00, msg 43614; approver-GO step 1).** Rebuilt `a8d13d6`, ran probes **synchronously in-turn** (the foreground-blocking fix for the 07-10 backgrounded-run-lost-signal failure). The emitted forward-derivative `s_fwd_readRefCell_0` constructs its result as `{ *_S7, 0.0f }` — dereferences the `ref`-accessor derivative call as a pointer and **hardcodes a `0.0f` differential** where the analytic answer = 20.0. **No diagnostic fires** (rules out case (c) = clean error). Interpreter VM-faults. Controls (trivial `sq`→6.0, value-accessor read→20.0) prove autodiff is otherwise healthy → the bug is specific to the `ref`-accessor derivative path. This is the worst class: silent incorrect result, not a crash/error.
- **Reviewer scope (strict):** read-only, **no GitHub post, no ledger change**. Reported raw slangc/slangi output + emitted-C++ to the **approver on the peer edge**. Evidence preserved `/workspace/agent/probe-12031-saved/` (FINDING.md, probes, emitted C++ for finding + control) — reviewer's workspace; build worktree GC'd.
- **Approver ledger:** R4 stays **ABSTAIN_INFRA** (unchanged by this confirm). The **step-2 maintainer heads-up is operator-gated, held with the approver** — NOT yet actioned. If the approver surfaces it, Main relays to operator; verified-finding → GitHub-post per [[feedback_triage_github_posting]] only on authorization.
- **Timing cross (reviewer flagged honestly, 07-17 13:01 msg 43616):** reviewer GC'd the build worktree earlier in the turn BEFORE the approver's "keep it" arrived — told the approver plainly rather than papering over it. The evidence the approver cares about (the `-target cpp` emitted output) IS preserved at `/workspace/agent/probe-12031-saved/`; `a8d13d6` still fetched so a rebuild is **build-only (~20 min)**. Reviewer offered two paths to the approver (rebuild-and-hold now vs hold-on-artifacts) with a recommendation, left the choice to the approver.
- **07-17 13:02 (reviewer msg 43618) — PARKED, clean close-out.** Approver chose **hold-on-artifacts** (no speculative rebuild); evidence at `/workspace/agent/probe-12031-saved/` is enough for a maintainer to read the smoking gun without a rebuild; `a8d13d6` stays fetched (rebuild only if operator says "go" AND a live re-run is specifically wanted). Scope held throughout (read-only, no GitHub post, R4 stays ABSTAIN_INFRA). Reviewer holding for operator's decision.
- **⚠️ POSSIBLE HANDOFF GAP (Main, 07-17):** reviewer states the finding was "relayed up to the orchestrator/operator by the approver" — but **Main has received NO inbound from the approver on #12031.** Per [[feedback_never_relay_a_verdict_not_in_hand]] / [[feedback_never_fabricate_events_between_turns]], Main did NOT tell the operator the approver escalated (no verdict in hand) and did NOT assume it self-healed. Step-2 heads-up is operator-gated → reaches operator ONLY through Main. **Main confirmed directly with the approver** (its edge) whether it intends to surface the step-2 recommendation through Main. Await approver's actual step-2 verdict before any operator surface.
- **✅ GAP CLOSED — approver's step-2 rec received (07-17 13:04, approver msg 43620; its earlier relay msg 127 didn't land, re-sent).** Approver's recommendation: **SURFACE to kaizhangNV (approver's rec), operator decides.** Finding: differentiating a read through a user-defined `ref` subscript accessor silently returns **0 gradient** (analytic 20.0) instead of erroring — live in master behind `-experimental-feature`; #12031 head a8d13d6 (merged @ 77f5ca0). Framing: *"the reverted err-41037 was catching a real case; an unsupported boundary should ERROR, not silently return 0."* **Approver's caveats (carry to operator verbatim-faithful):** (i) experimental-gated → lower urgency; (ii) it's the maintainer's OWN deliberately-reverted change (added err-41037 @R3, reverted @R4, called read-through-ref a "pre-existing unsupported boundary") → they may already know / consider silent-zero acceptable; (iii) outward-facing write on a CLOSED PR → maintainer's choice of vehicle (comment #12031 vs fresh tracking issue vs drop). R4 stays ABSTAIN_INFRA + APPROVED join, terminal — post-merge calibration only, NO ledger change.
- **07-17 13:0x — Main surfaced to operator (dashboard).** Faithful relay w/ all 3 caveats + vehicle options. **If operator says "go":** the REVIEWER posts ([[feedback_approver_never_posts_route_reviewer.md]] — approver never writes GitHub), rebuilds for a live re-run only if specifically wanted (a8d13d6 fetched, ~20min). Approver relays operator's go/drop to reviewer. Match comment hedging to these caveats ([[feedback_authorize_comment_matches_memo_hedging]]).
- **Evidence precision (approver msg 43624):** PRESERVED at `probe-12031-saved/` = `refread-emitted.cpp` (the `{ *_S7, 0.0f }` smoking gun), `valcell-control-emitted.cpp` (control), probe `.slang` files, `FINDING.md` — **enough to READ the finding, no binary needed.** GONE = the built slangc/slangi binary + worktree → a **live re-run needs ~20-min rebuild** (a8d13d6 still fetched, probe scripted). Main's operator relay ("no rebuild needed to read") is accurate; only a live re-run costs the rebuild. **Main's call: HOLD-on-artifacts, do NOT speculatively rebuild** — step-2 might be "drop"; reading emitted-C++ (the actual proof) needs no binary; rebuild only on "go + live-re-run specifically wanted."
- **Blocker:** none. Pending: operator's go/drop on the kaizhangNV heads-up. Fix unassigned/undispatched (confirm-only). NOT restart/CI-blocked.

Related: autodiff wrong-grad cluster [[project_12071_bwddiff_loop_vector_divide_wrong_grads.md]], [[project_slangpy_1055_diff_loop_vector_return_wrong_grads.md]] (distinct causes; this is `ref`-accessor-specific).
