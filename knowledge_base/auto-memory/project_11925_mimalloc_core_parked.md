---
name: project_11925_mimalloc_core_parked
description: "#11925 use mimalloc for Slang core — RELEASED: author directed Mechanism B + default-ON-shared-Windows; fixer dispatched for full impl"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1e96e659-006b-4466-8e0b-4575b390fdfb
---

**RELEASED 2026-07-03:** @pdeayton-nv (dev-opener) @-mentioned the bot ([issuecomment-4873379378](https://github.com/shader-slang/slang/issues/11925#issuecomment-4873379378)) and resolved BOTH parked judgment calls himself: (i) default **ON for shared Windows** (trivially flippable to OFF later — keep it a one-line change), (ii) **Mechanism B** (explicit choke-point conversion + allocator function-pointers/wrappers, NOT Approach C shim-only). Author-ownership blocker inverted (he wants the bot to do it). Fixer dispatched via triager for FULL Mechanism B. Directives: convert ALL core `::malloc`/`::free`/`::realloc` sites (StandardAllocator slang-allocator.h:41,45 + aligned + memory-arena/free-list/blob/rtti-util/offset-container per digest below) + new/delete shim TU; LEAVE slang-io.cpp:695,705 `::free` (OS realpath buffers); audit for half-converted paths (mi_malloc↔::free = crash — the whole risk of B); guard on `SLANG_USE_MIMALLOC` default-ON-shared-Windows (mirror WIN32 gate CMakeLists.txt:211-215), reuse existing vendored clone. Guardrails: DRAFT PR only (never ready-flip), `report_pr_created` (fix/issue-11925 has NO prefix-route fallback → mapping is the only webhook path), post 5-bullet on issue since draft ≠ issue footprint, brief ack to author OK (he asked directly). Awaiting draft PR #.

**Stale-state race (2026-07-03 6:50):** after acking stand-down, fixer emitted empty pings (msgs 28/30/32) — auto-route-hook / [[project_taskless_fixer_review_cc_loop]] pattern; container has since EXITED (no running fixer container, no fixer session on 11925 thread — self-limiting burst, not a live loop). Triager's #18 said "chain stays parked" — STALE, superseded by the RELEASE above. Corrected the triager: chain released, forward Mechanism B as the single dispatch (triager owns handoff, Main does NOT double-dispatch directly). Escalation: if fixer resumes empty-looping on the live session instead of working, Main `ncl groups restart`s the fixer (cross-group op, triager can't) then triager re-forwards onto fresh session. Empty pings must NOT be replied to (feeds ack loop). slang-fixer group id = ag-1780667166439-vmjrwe.

**Re-dispatched cleanly (2026-07-03 7:00, triager msg 42):** empty-loop escalation did NOT fire — fixer working. Triager caught + corrected a scope discrepancy: fixer's earlier narrowed plan was shim-only/default-OFF, but author wants FULL Mechanism B + default-ON-shared-Windows (broader). Fresh author ack posted ([issuecomment-4873518743](https://github.com/shader-slang/slang/issues/11925#issuecomment-4873518743)); corrected full-scope forwarded to fixer as single dispatch. AWAITING Fix Report (draft PR #). Chain ACTIVE.

**Author status-ping (2026-07-07 16:19Z, [issuecomment on #11925]):** @pdeayton-nv @-mentioned bot "do you have a draft PR ready?". Routed to triager to get fixer status + answer author on GitHub.

**CORRECTION 2026-07-07 ~23:23Z — NOT a dropped chain; a HELD chain with intact WIP. Fixer LIVE, never stalled.** Triager queried the fixer: it was deliberately *parked* on 07-03 (Decision B), real WIP intact + validated (4 files, plumbing built end-to-end on Linux, UNPUSHED — which is why HEAD showed no branch/PR). **No restart needed** (correct — a restart would have wiped the validated work; endorsed the triager's hold). **Scope conflict resolved from ground truth:** fixer surfaced a genuine contradiction (its parked scope = narrowed/new-delete-only/default-OFF vs. the full-Mechanism-B/default-ON dispatch) and refused to silently ship either. Triager resolved against the author's OWN written directive ([issuecomment-4873379378](https://github.com/shader-slang/slang/issues/11925#issuecomment-4873379378): "default ON for shared Windows … Mechanism B), explicit conversion") → unambiguously **(b) full Mechanism B + default-ON**; the narrowed version was the fixer's earlier self-narrowing, never the author's ask. Parent (Main) re-confirmed (b). **Triager directed the fixer** to execute (b) on its live worktree: rebase onto current master `33f9ed0cea`, keep validated plumbing + flip default-ON, treat explicit-conversion pairing audit as the gating step (blessed two danger sites: `ScopedAllocation::detach()` ownership boundary, and slang-io.cpp realpath `::free` stays CRT), draft-only, `report_pr_created` mandatory. Chain RE-ACTIVATED, awaiting fixer's draft PR #. So the "dropped chain" framing below was wrong — the WIP simply never reached a pushed branch because the chain was correctly parked, not lost. Lesson (fleet-wide learning saved by triager): resolve park/re-engage scope drift from the authoritative artifact (author's written directive), not from a downstream agent's self-narrowed restatement.

**[SUPERSEDED framing] DROPPED CHAIN — flagged 2026-07-07 ~23:07Z (Main verified at HEAD, operator flagged it) — CORRECTED above to held-not-dropped.** Operator asked "what happened to #11925? you indicated a PR was forthcoming, one never linked." Verified: **NO `fix/issue-11925` branch (GitHub 404), NO PR anywhere referencing #11925, issue still OPEN.** Last issue comments: pdeayton-nv 07-03 06:47 (the directive) → bot 07-03 06:59 ("Proceeding to implement…") → **pdeayton-nv 07-07 16:19 "do you have a draft PR ready?" — UNANSWERED ~7h.** So after publicly committing to implement Mechanism B on 07-03, the fixer produced nothing pushed and the chain went silent. Note the 07-03 dispatch coincided with the messy park→proceed-past-park→stand-down→release→re-dispatch sequence (empty-ping bursts, fixer container exits — see the stale-state-race + re-dispatch entries above) AND with the scheduler-stall day ([[project_scheduler_stall_incident]]); likely the re-dispatched Mechanism-B work either never took hold on a live session, stalled, or produced only local uncommitted work that was never pushed (and `fix/issue-11925` has NO prefix-route fallback, so the author's follow-up webhook may not have routed to any fixer session either). **Re-driving 2026-07-07 ~23:07Z:** dispatched triager (owns the fixer handoff — Main does NOT double-dispatch directly) on canonical thread to (a) get REAL fixer status (live session? local uncommitted work? or dead), (b) answer pdeayton-nv HONESTLY on GitHub now (closest-to-the-state; comments not gated) — in-progress vs restarting-from-scratch, NO fabricated readiness, NO rushed half-audited PR (mi_malloc↔::free pairing audit is the whole risk). If fixer is dead/looping, Main `ncl groups restart`s it (cross-group, triager can't) then triager re-forwards full Mechanism-B scope onto the fresh session. Draft-only, `report_pr_created` mandatory. Awaiting triager report.

**RECOVERED + MOVING 2026-07-07 ~23:46Z (triager report, msg 18776).** Chain was NOT dead — fixer rebased clean onto master `33f9ed0cea`, ran the `mi_malloc`↔`::free` pairing audit BEFORE touching any site (exactly the crash-safe discipline required), and returned a SAFE/UNSAFE partition instead of silently narrowing or shipping a crasher. Triager re-verified both UNSAFE exclusions at source (didn't take on faith):
- **`slang-blob.h ScopedAllocation`** — `attach()` contract requires a `malloc`'d ptr; its 2 callers attach **miniz-heap** buffers (`tdefl_compress_mem_to_heap`, `mz_zip_writer_finalize_heap_archive`). Converting `deallocate()`→`mi_free` = cross-allocator free of a miniz ptr = heap corruption. EXCLUDED (correct).
- **`slang-rtti-util.cpp _mallocArray`** — buffers cross into `List::attachBuffer/detachBuffer` (List frees via its own allocator). Isolated conversion splits ownership. EXCLUDED (correct).
- SAFE subset = `slang-allocator.h`, `slang-memory-arena.cpp`, `slang-free-list.cpp`, `slang-offset-container.cpp` + global `operator new` (alloc-and-free both internal → crash-free).

**Main DECISION: CONFIRM (i) — safe subset now as DRAFT, exclusions documented; do NOT override to (ii).** Reasons: (a) crash-safe by construction; (b) faithful to author's "explicit choke-point conversion" intent for what Slang owns end-to-end; (c) **the blob.h/miniz-heap exclusion overlaps [[project_11924_miniz_alloc_wrappers]]** (#11924 / PR #11934, maintainer-APPROVED draft — the dedicated miniz-owned-buffer free/alloc-pairing chain) — folding miniz ownership into #11925 risks colliding with #11924's work; keep the boundary in its own chain; (d) incremental draft lets pdeayton-nv weigh in (mirrors #11917/#11919 slice pattern). Triager's (i) is a correctness call it owns; I'm affirming, not overriding. **Author-answer timing:** pdeayton-nv's 07-07 16:19 ping is ~7h stale — post the honest GitHub answer WITH the draft PR link the moment it's up (one clean comment, avoids double-post); if the draft is >~30-60min out, post an interim "in progress, draft shortly" now rather than leave him hanging longer. Awaiting fixer draft PR # (+ `report_pr_created`). **Thread note:** triager's report arrived mis-tagged on thread `discord-bsdf-metal-addrspace-crash` (should be `gh-issue-shader-slang/slang-11925`) — flag to keep #11925 reports on the canonical thread for per-issue observability.

---

shader-slang/slang#11925 (pdeayton-nv, "Dev Opened") — add a CMake option to route **Slang core**'s allocations through mimalloc; asks default-on for shared Windows, opt-in static. Prior art #8158→#8419 (MERGED, SPIRV-Tools scope), refined #8460/#8676.

**Load-bearing verified finding (HEAD f4975a7f8):** the issue's "reuse the existing dep" premise is NOT turn-key. SPIRV-Tools' shim `mimalloc-new-delete.h` overrides only C++ `operator new/delete`, NOT the scattered `::malloc`/`::free`/`::realloc` sites in Slang core (StandardAllocator slang-allocator.h:41,45; slang-memory-arena.cpp, slang-free-list.cpp, slang-blob.h, slang-rtti-util.cpp, slang-offset-container.cpp). `::`-scoped calls bypass mimalloc's macro override. Partial conversion → `mi_malloc`↔`::free` mismatch = UB/crash (same mixed-allocator hazard that kept SPIRV-Tools Windows-only per #8419). slang-io.cpp:695,705 `::free` on OS realpath buffers must STAY `::free`.

**Two genuine judgment calls → fixer HELD, defer to maintainer/author:** (1) default-on-shared-Windows policy swaps the allocator for every host process embedding slang.dll; (2) mechanism **A** (Windows mimalloc-redirect.dll: complete, low source-churn, process-global, ships extra DLL, Windows-only) vs **B** (portable guarded choke-point conversion behind `SLANG_USE_MIMALLOC`, no host side-effects, exhaustive-or-crash). Approach C = scaffolding-only draft (CMake option + shared-dep plumbing) if a maintainer wants a starting point.

**State (2026-07-03):** triager posted deferring verdict [issuecomment-4872987846](https://github.com/shader-slang/slang/issues/11925#issuecomment-4872987846), set Type=Feature, and relayed [STAND DOWN] to the already-dispatched fixer (no fix action). Chain PARKED. Author is an active NV build contributor (18 merged PRs) → may self-implement.

**Fixer proceeded past park (2026-07-03):** despite the STAND DOWN, slang-fixer started implementing, but *narrowed scope* to a scaffolding-only draft (Approach C): opt-in `SLANG_USE_MIMALLOC`, default OFF, Windows-shared-only, routes only `operator new/delete` through the proven SPIRV-Tools shim, NO `::malloc`-site edits, ELF/static/default-policy flagged as maintainer follow-ups. Default-OFF = output-neutral for all existing builds. Triager held it, leaned B. **Main decision: B — fixer FULLY stands down, no draft/no branch push, nothing to GitHub.** Rationale: park is about author-ownership (dev-opened issue, likely self-implements), not technical risk; the GitHub comment posed the scaffolding draft as an *offer* contingent on author/maintainer acceptance — delivering it unprompted preempts the offer + risks stepping on pdeayton-nv's work. The narrowed-scope design is PRESERVED locally so Approach C can be re-dispatched instantly IF the author/maintainer accepts the offer. This is the [[project_autoroute_hook_pressures_parks]] pattern — park is authoritative over the auto-route hook.

**Reopen triggers:** maintainer directs the scaffolding draft (→ re-dispatch fixer, Approach C, design ready), a self-implementing PR from @pdeayton-nv (webhook), or a substantive human comment. Do NOT auto-dispatch the fixer without a maintainer/author go. Related: [[feedback_triage_github_posting]], [[project_11806_cmake_options_maintainer_selffix]], [[project_autoroute_hook_pressures_parks]].
