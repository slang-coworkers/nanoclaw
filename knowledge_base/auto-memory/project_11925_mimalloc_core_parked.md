---
name: project_11925_mimalloc_core_parked
description: "#11925 use mimalloc for Slang core — RELEASED: author directed Mechanism B + default-ON-shared-Windows; fixer dispatched for full impl"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1e96e659-006b-4466-8e0b-4575b390fdfb
---

**RELEASED 2026-07-03:** @pdeayton-nv (dev-opener) @-mentioned the bot ([issuecomment-4873379378](https://github.com/shader-slang/slang/issues/11925#issuecomment-4873379378)) and resolved BOTH parked judgment calls himself: (i) default **ON for shared Windows** (trivially flippable to OFF later — keep it a one-line change), (ii) **Mechanism B** (explicit choke-point conversion + allocator function-pointers/wrappers, NOT Approach C shim-only). Author-ownership blocker inverted (he wants the bot to do it). Fixer dispatched via triager for FULL Mechanism B. Directives: convert ALL core `::malloc`/`::free`/`::realloc` sites (StandardAllocator slang-allocator.h:41,45 + aligned + memory-arena/free-list/blob/rtti-util/offset-container per digest below) + new/delete shim TU; LEAVE slang-io.cpp:695,705 `::free` (OS realpath buffers); audit for half-converted paths (mi_malloc↔::free = crash — the whole risk of B); guard on `SLANG_USE_MIMALLOC` default-ON-shared-Windows (mirror WIN32 gate CMakeLists.txt:211-215), reuse existing vendored clone. Guardrails: DRAFT PR only (never ready-flip), `report_pr_created` (fix/issue-11925 has NO prefix-route fallback → mapping is the only webhook path), post 5-bullet on issue since draft ≠ issue footprint, brief ack to author OK (he asked directly). Awaiting draft PR #.

**Stale-state race (2026-07-03 6:50):** after acking stand-down, fixer emitted empty pings (msgs 28/30/32) — auto-route-hook / [[project_taskless_fixer_review_cc_loop]] pattern; container has since EXITED (no running fixer container, no fixer session on 11925 thread — self-limiting burst, not a live loop). Triager's #18 said "chain stays parked" — STALE, superseded by the RELEASE above. Corrected the triager: chain released, forward Mechanism B as the single dispatch (triager owns handoff, Main does NOT double-dispatch directly). Escalation: if fixer resumes empty-looping on the live session instead of working, Main `ncl groups restart`s the fixer (cross-group op, triager can't) then triager re-forwards onto fresh session. Empty pings must NOT be replied to (feeds ack loop). slang-fixer group id = ag-1780667166439-vmjrwe.

**Re-dispatched cleanly (2026-07-03 7:00, triager msg 42):** empty-loop escalation did NOT fire — fixer working. Triager caught + corrected a scope discrepancy: fixer's earlier narrowed plan was shim-only/default-OFF, but author wants FULL Mechanism B + default-ON-shared-Windows (broader). Fresh author ack posted ([issuecomment-4873518743](https://github.com/shader-slang/slang/issues/11925#issuecomment-4873518743)); corrected full-scope forwarded to fixer as single dispatch. AWAITING Fix Report (draft PR #). Chain ACTIVE.

---

shader-slang/slang#11925 (pdeayton-nv, "Dev Opened") — add a CMake option to route **Slang core**'s allocations through mimalloc; asks default-on for shared Windows, opt-in static. Prior art #8158→#8419 (MERGED, SPIRV-Tools scope), refined #8460/#8676.

**Load-bearing verified finding (HEAD f4975a7f8):** the issue's "reuse the existing dep" premise is NOT turn-key. SPIRV-Tools' shim `mimalloc-new-delete.h` overrides only C++ `operator new/delete`, NOT the scattered `::malloc`/`::free`/`::realloc` sites in Slang core (StandardAllocator slang-allocator.h:41,45; slang-memory-arena.cpp, slang-free-list.cpp, slang-blob.h, slang-rtti-util.cpp, slang-offset-container.cpp). `::`-scoped calls bypass mimalloc's macro override. Partial conversion → `mi_malloc`↔`::free` mismatch = UB/crash (same mixed-allocator hazard that kept SPIRV-Tools Windows-only per #8419). slang-io.cpp:695,705 `::free` on OS realpath buffers must STAY `::free`.

**Two genuine judgment calls → fixer HELD, defer to maintainer/author:** (1) default-on-shared-Windows policy swaps the allocator for every host process embedding slang.dll; (2) mechanism **A** (Windows mimalloc-redirect.dll: complete, low source-churn, process-global, ships extra DLL, Windows-only) vs **B** (portable guarded choke-point conversion behind `SLANG_USE_MIMALLOC`, no host side-effects, exhaustive-or-crash). Approach C = scaffolding-only draft (CMake option + shared-dep plumbing) if a maintainer wants a starting point.

**State (2026-07-03):** triager posted deferring verdict [issuecomment-4872987846](https://github.com/shader-slang/slang/issues/11925#issuecomment-4872987846), set Type=Feature, and relayed [STAND DOWN] to the already-dispatched fixer (no fix action). Chain PARKED. Author is an active NV build contributor (18 merged PRs) → may self-implement.

**Fixer proceeded past park (2026-07-03):** despite the STAND DOWN, slang-fixer started implementing, but *narrowed scope* to a scaffolding-only draft (Approach C): opt-in `SLANG_USE_MIMALLOC`, default OFF, Windows-shared-only, routes only `operator new/delete` through the proven SPIRV-Tools shim, NO `::malloc`-site edits, ELF/static/default-policy flagged as maintainer follow-ups. Default-OFF = output-neutral for all existing builds. Triager held it, leaned B. **Main decision: B — fixer FULLY stands down, no draft/no branch push, nothing to GitHub.** Rationale: park is about author-ownership (dev-opened issue, likely self-implements), not technical risk; the GitHub comment posed the scaffolding draft as an *offer* contingent on author/maintainer acceptance — delivering it unprompted preempts the offer + risks stepping on pdeayton-nv's work. The narrowed-scope design is PRESERVED locally so Approach C can be re-dispatched instantly IF the author/maintainer accepts the offer. This is the [[project_autoroute_hook_pressures_parks]] pattern — park is authoritative over the auto-route hook.

**Reopen triggers:** maintainer directs the scaffolding draft (→ re-dispatch fixer, Approach C, design ready), a self-implementing PR from @pdeayton-nv (webhook), or a substantive human comment. Do NOT auto-dispatch the fixer without a maintainer/author go. Related: [[feedback_triage_github_posting]], [[project_11806_cmake_options_maintainer_selffix]], [[project_autoroute_hook_pressures_parks]].
