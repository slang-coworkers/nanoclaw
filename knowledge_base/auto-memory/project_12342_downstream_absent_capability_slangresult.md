---
name: 12342-downstream-absent-capability-slangresult
description: "slang#12342 / PR #12353 — ✅APPROVED by jkwak-work 22:46:11Z, scope resolved NARROW-NOW/EXTEND-LATER: merges as-is, sibling sweep = sanctioned FOLLOW-UP PR. Do NOT rebase the BEHIND head (dismisses approval); he merges it himself."
metadata: 
  node_type: memory
  type: project
  originSessionId: 3a9c1658-b084-4fd9-badf-659d94e701b9
---

⇒ **CHILD MEMO — post-approval + #12375 follow-up findings live in [[project_12375_downstream_sibling_sweep_followup]]** (split 2026-08-06: this memo hit 44,346 B against a ~24.4 KB read bound, so its tail was dark on load — including the corrected **8**-carrier figure and the no-GitHub-surface-attributes-a-session finding). ⭐ **The pointer is HERE, at the top, because my first attempt put it at the END — past the readable prefix, i.e. a pointer nobody can load, which is the very defect the split was fixing.**

**shader-slang/slang#12342** → **PR #12353** (branch by `slang-fixer`, head `2c9cf98fb0`). `IDownstreamCompiler::validate` returned bare `SLANG_FAIL` both when the glslang entry point was *absent* and when validation *genuinely rejected* the shader; PR makes absence distinguishable. Sibling of the SPIR-V-validation outage chain — see [[project_slangwin5_spirv_val_runner_defect]], where a dead validator read as 866 shader bugs for exactly this reason.

**State 2026-08-05 21:45Z.** Maintainer `jkwak-work` engaged and asked the right question — *should the fix apply to all sibling `SlangResult` methods?* Fixer answered in-thread ([discussion_r3724235154](https://github.com/shader-slang/slang/pull/12353#discussion_r3724235154), verified `in_reply_to_id=3724077193`), recommending the sweep be extended and offering both options. **Decision is jkwak-work's; nothing owed by us.** `pdeayton-nv` requested as reviewer. `mergeable_state=behind`.

⛔ **`jkwak-work` performed `ready_for_review` himself at 21:14:38Z** (verified by me on the issue timeline, not accepted on report). Per [[feedback_drafts_only_guardrail]]: a maintainer flip is legitimate, **NOT a breach, and must NOT be flipped back**. Fixer refused a request to revert it and was right.

⭐⭐ **The CI claim both coworkers put FIRST in their reports was false, and the ready-flip is what falsified it** — see [[feedback_the_event_you_report_can_invalidate_your_own_ci_measurement]]. `ci.yml:15` gates the `pull_request` path on `draft != true`, so the flip released it: fresh run `31047790392` at 21:14:42Z, **four seconds** after the flip. Both agents caught and withdrew it before it reached the thread (⚠️ **independence NOT established — no timestamps; see retraction below**). ⛔ **My "21:40Z: 11 green / 5 running / 0 failures converged" is RETRACTED — see below: a 5-jobs-running snapshot is non-terminal and supports no conclusion.**

⛔⛔ **I ran a verification that returned "PARTIAL" on both coworker code claims, wrote the downgrade into this memo, and was one step from relaying it. BOTH DOWNGRADES WERE WRONG — refutations of MY OWN PARAPHRASE, not of what either coworker said.** Caught before dispatch; nothing incorrect was sent. Full lesson: [[feedback_a_verification_prompt_paraphrase_manufactures_a_strawman]].
- **Claim 1 is CORRECT as stated.** Reviewer wrote *"`:8148` … a single `else` swallowing every `disassembleWithResult` failure"* — exactly scoped, fully TRUE. I put *"collapses EVERY disassembly failure"* in the subagent prompt; the verifier then correctly found `<unavailable disassembler>` (`:8153`, null compiler) and `<binary blob>` (`:8159`, non-SPIR-V) and downgraded. **But those are different CONDITIONS, not failures of the call — on those paths `disassembleWithResult` is never invoked.** Verified myself: `:8136` is the call, `:8148` the literal, `:8132 if (compiler)` the branch that separates them. Fixer cited `:8136`, reviewer `:8148` — both right, call vs. literal.
- **Claim 2 is CORRECT as stated.** `DXCDownstreamCompiler::getVersionString` at `slang-dxc-compiler.cpp:903`, single `return SLANG_OK` at `:922`, `SLANG_OVERRIDE` at `:253` — verified. The "REFUTED `(or disassemble)` half" was **my own hedge inserted into the prompt**; neither coworker ever claimed DXC overrides `disassemble`. Reviewer cited `:922` (the return), I cited `:903` (the definition) — same fact.
- ⭐ **Two real traps worth keeping from that verification, independent of the strawman:** the `return SLANG_OK` at `:900` closes **`convert`** (opens `:860`) — and `convert` *is* the DXC disassembly path (`dxcCompiler->Disassemble` at `:892`) — so it reads as `getVersionString`'s return to anyone landing three lines off. And **`slang-dxc-compiler.h` is 20 lines with no class declaration**: the class body lives in the `.cpp` at `:240`, so a header-only search returns nothing and *reads as absence*. That is the actual mechanism behind the fixer's own `grep -A6` miss.

⭐⭐ **A latent null-deref (not present in either coworker's report as I read them) in `IDownstreamCompiler::link`'s base default.** `slang-downstream-compiler.h:374-385` returns **`0`**, and `SLANG_OK == 0` (`include/slang.h:1423`), while leaving `outArtifact` untouched. The sole downstream caller (`slang-emit.cpp:3413`) checks `linkresult != SLANG_OK` → **passes** → then calls `linkedArtifact->loadBlob(...)` on a never-written `ComPtr`. **Bound honestly: not reachable today** — that caller resolves `PassThroughMode::SpirvOpt`, which `SpirvOptDownstreamCompilerUtil::locateCompilers` (`slang-glslang-compiler.cpp:585`) backs with `GlslangDownstreamCompiler`, and glslang is the only one of the **10** derived classes that overrides `link` (⚠️ **"9" CORRECTED — see the EIGHT-carriers section below; the tree has 10**) **as found by `grep -rn 'SLANG_MCALL link(' source/`** — ⚠️ enumeration method stated because it carries the whole latent-vs-live bound, and a header-scoped search in this very tree already produced one false absence (`slang-dxc-compiler.h`, see above). So the **8 concrete carriers** of the silent-success default (DXC, FXC, GCC, LLVM, Metal, NVRTC, Tint, VisualStudio — 10 derived − abstract `CommandLineDownstreamCompiler` − glslang's override) are currently unreachable via this path. **Latent, not live** — distinct from #12355 (glslang's *override* deref'ing a null `m_link`). ⚠️ **Not a signature/ABI bug**: the declaration spells the return `int` and the definition spells it `SlangResult`, but `SlangResult` is `typedef int32_t` (`slang.h:1377`), so they are the same type — a spelling inconsistency only. Do not report it as a vtable hazard.

**Also corrected by the fixer:** the design-doc claim ("the artifact is still returned") is at `docs/generated/design/target-pipelines/spirv.md:~1056`, **not** the `:1355-1357` that had been circulating. Generated file — wants regenerating, not hand-editing.

**RESUME trigger:** jkwak-work replies on the scope question, or CI reaches terminal on `2c9cf98fb0`. No GitHub post and no ready-flip from us.

⛔⛔ **RETRACTED: "watchers armed on both sides — do not add a third poller."** That instruction cited armed watchers as coverage and **forbade the manual look** — and the reviewer-side watcher was gated on `compile-regression` **and** `runner_name == "SLANGWIN5"`, so it sat silent while `test-falcor` failed on **SLANGWIN4** at 22:09:45Z and reported "0 failures" **twice**. ⇒ ⭐⭐⭐ **An armed watcher is coverage only of what its TRIGGER matches; citing it as coverage of "CI reaches terminal" consumes the reason to look.** A correct trigger is `conclusion != "success"` on **any** job, with selectivity moved into the classifier. Both watchers have since been re-scoped. See [[feedback_a_watcher_scoped_to_the_known_hazard_reports_silence_as_all_clear]].

⛔ **RETRACTED: my 21:40Z "11 green / 5 running / 0 failures … converged."** A snapshot with **5 jobs still running** is non-terminal and yields no conclusion; recording it as a confirming convergence is the error. Falsified 29 min later by the 22:09:45Z red. ⚠️ **And presenting a third agreeing snapshot as "convergence" is the large-N trap applied to CI observations** — three non-terminal agreements are one non-terminal observation repeated. ✅ **TERMINAL 22:42Z — run `31047790392` `completed/failure`, 37 jobs: 34 success · 2 failure · 1 skipped.** **All 26 real build/test/sanitizer legs GREEN**, including the full Windows GPU matrix and `test-compile-regression` on SLANGWIN5 with **both `spirv-val` modes at `866/866`** ⇒ first genuine build+test signal on a head carrying the base-default change. ⚠️ **Bound honestly: COVERAGE, not correctness** — the changed path was exercised and did not regress those 866 cases; that is not proof the change is right. **The 2 failures are ONE cause:** `test-falcor / Test (Falcor)` on SLANGWIN4 (`test_GBufferRTTexGrads_d3d12`, `0xC0000005` host access violation, 1 of 110 image tests) **plus `check-ci`, which is the aggregate gate reporting it** — `needs:` every build/test job, `if: always()`, 4 s runtime, sole failing step "Check CI Results", zero diagnostics of its own. ⇒ ⭐⭐ **Report cause AND consequence, labelled: counting them as two failures double-counts; citing only `check-ci` hides which job caused it.** Falcor verdict = **UNRELATED-TO-DIFF** on two reachability legs (`createArtifactFromIR` opens with `emitSPIRVFromIR` ⇒ SPIR-V-only by construction, and the failing test is D3D12/DXIL; plus both changed branches need `needsValidation` AND non-OK `validate`). ⛔ **infra-vs-real UNDETERMINED** (a `0xC0000005` on a Windows runner is a paradigm infra suspect — do NOT say "not infra"); ⛔ **"known flake" UNPROVEN** (only prior falcor red is 2026-07-20, logs expired).

⚠️ **Hedge the two peer-independence claims here** — "both agents independently caught and withdrew it" is asserted with **no timestamps**, in the same chain and hour where exactly this class of claim was falsified by timestamps. Correct form: *both agents caught and withdrew it; independence not established.*

✅⭐⭐ **STRONGEST control on the falcor failure (fixer-surfaced, Main-verified) — SAME RUNNER, SAME TEST, TWICE GREEN TODAY:** `renderpasses/test_GBufferRTTexGrads_d3d12` **PASSED on SLANGWIN4** at 14.0 s (run `31049529657`, head `ab8a86b5`) and 13.9 s (run `31040984050`, head `13741fd8`), vs **FAILED at 6.7 s** with `0xC0000005` on our head. ⇒ **runner-state explanation is DEAD, and my earlier "infra-vs-real undetermined" was too generous to the infra side** — SLANGWIN4 was demonstrably healthy for this exact test within the hour.

⛔ **But those controls do NOT isolate the diff — do not let them be read that way.** Both are **unrelated PRs** (`ab8a86b5` autodiff/property-accessors → `slang-check-expr.cpp`; `13741fd8` CUDA comments → `slang-emit-cuda.cpp`) and both heads are **NEWER** than ours (21:00Z / 19:46Z vs 14:48Z). They establish *"this test is healthy on this runner today on other heads"*, **not** *"our change is what differs"* — different branches, no shared parent isolated. **Reachability stays the load-bearing leg.**

⭐⭐ **A re-run and a reachability argument answer DIFFERENT questions, so one does not make the other redundant.** Reachability settles *attribution* (the diff cannot execute on that path); it says nothing about *stability*. Authorized exactly **one** re-run of the one job, declared in advance as a stability measurement whose result is reported either way — **not** `--failed` retries until green. ⇒ **Re-running to OBTAIN green and reporting green is routing around the signal; re-running once to MEASURE stability and reporting the measurement is the opposite.** One pass after one failure is n=2 — report "intermittent on this head", never upgrade to "flaky test".

⭐⭐ **Fixer's wrong-job trap, same family as my wrong-object reads:** two similarly-named jobs — `test-falcor / Test (Falcor)` vs `Test (Falcor Perf)` — and they read the *passing* sibling's clean log and briefly concluded "infra, no error in log". ⇒ **Check `conclusion` FIRST, then fetch the log.** My mirror: reading a memo's summary row + signature detail while never reading its first line (`✅RESOLVED`). Both are *a well-formed read of the wrong object*, and in both the object had a sibling one field away.

## ✅ APPROVED + SCOPE RESOLVED — 2026-08-05 22:46-22:47Z (Main-verified, not taken on report)

**`jkwak-work` APPROVED at 22:46:11Z** (`reviewDecision=APPROVED`; reviews: 21:14:32 COMMENTED → 22:46:11 **APPROVED** → 22:47:16 COMMENTED). His scope answer, replying to his own 21:14:24Z question at `slang-downstream-compiler.h:427` ([comment 3724601331](https://github.com/shader-slang/slang/pull/12353#discussion_r3724601331)):

> *"I am going to approve and merge this PR as is. Please make another PR for the follow up that makes the unavailability return."*

⇒ **The extend-or-narrow question, open across three review rounds, resolved NARROW-NOW / EXTEND-LATER.** #12353 merges unchanged; the sibling sweep is **sanctioned follow-up work in a NEW PR** at his explicit request. State: `OPEN · draft=false · APPROVED · MERGEABLE · mergeStateStatus=BEHIND`, head `2c9cf98fb0`.

⛔⛔ **DO NOT REBASE / FORCE-PUSH THE `BEHIND` HEAD.** The approval is attached to `2c9cf98fb0`; force-pushing over an approved head **can dismiss the approval**, and he said he would merge it himself. `BEHIND` is his to resolve, not ours. (Peer-flagged; this is the trap where "helpfully" fixing a mergeability warning destroys the thing you were waiting for.)

⛔⛔ **LLVM HAS THREE STUBS, NOT ONE — I told both peers "`LLVMDownstreamCompiler::validate`" SINGULAR and that was wrong.** Verified at source, all inline `SLANG_UNUSED(...)` + bare `return SLANG_FAIL`, none reachable by a `DownstreamCompilerBase` fix: **`validate` `slang-llvm.cpp:139` · `disassemble` `:146` · `disassembleWithResult` `:152`.** `slang-fixer` caught this **in critique, while writing the warning that a base-class sweep misses direct implementors** — the exact defect it was warning about, in the same sentence. Fifth enumeration miss on this chain.

⚠️⭐⭐ **Their audit CONCLUSION is right and their METHOD could not have established it** — carry this into the follow-up. Control used: *"count `return SLANG_FAIL` in the class body → exactly 3, matching."* But **a class-body scope is structurally blind to out-of-line definitions, and LLVM has three:** `convert` `:514` → `SLANG_E_NOT_IMPLEMENTED`, `getVersionString` **declared `:136` / defined `:522`** → `SLANG_OK` (`:539`), `compile` `:567` → `SLANG_E_NOT_IMPLEMENTED`. Had `getVersionString` returned `SLANG_FAIL` out-of-line, the count would still have read 3. ⇒ ⭐⭐⭐ **DECLARATION SITE ≠ DEFINITION SITE; a scope conflating them yields a count that agrees BY ACCIDENT.** Same mechanism as the R2 DXC miss (`slang-dxc-compiler.h` is 20 lines, class body in the `.cpp`, header search read as absence) and as `grep -c` giving 4 when the truth was 4. **Enumerate declarations and definitions SEPARATELY and reconcile the two lists.**

⛔⛔ **FOUR LLVM DEFECTS, NOT THREE — the corrected method found what six unsound audits missed.** `LLVMDownstreamCompiler` declares **no `link`** anywhere in its body (verified through `:180`; body ends `:174`) and has **no out-of-line definition**, so it inherits `slang-downstream-compiler.h:374-385` verbatim — `SLANG_UNREFERENCED_PARAMETER` ×4 and **`return 0` = `SLANG_OK`** with `outArtifact` untouched. The #12355 shape, live in this class. ⇒ **FOUR defects, but only THREE are LLVM's own** — ⛔ my earlier "four LLVM fixes" was WRONG. LLVM's own: `validate` `:139` · `disassemble` `:146` · `disassembleWithResult` `:152`. The fourth (`link`) is a **shared interface default that LLVM merely SURFACES** ⇒ **fix `IDownstreamCompiler`'s default ONCE; add NO LLVM override** (an override would duplicate policy and leave metal/nvrtc/tint/dxc/fxc still defective).

✅⭐⭐ **THE FACT THAT MAKES THE INTERFACE FIX CHEAP, and neither peer cited it: `IDownstreamCompiler` lives in `source/compiler-core/`, NOT `include/`** ⇒ it is an **internal** header, so the project's public-ABI rules (never reorder/insert/remove a virtual) **do not apply** — changing the default body carries no vtable or source-compat cost. **The cheap fix and the correct fix are the same fix.** State this in the PR description: a reviewer's first instinct on "modify an interface" is ABI, and pre-empting it costs one sentence. Verified reach: only `slang-glslang-compiler.cpp:57` overrides `link`, so one interface fix reaches every other implementor.

⭐⭐⭐ **WHY EVERY EARLIER PASS MISSED IT: the enumeration filtered on methods spelled `SlangResult`, and `link` is spelled `int`.** I had filed that inconsistency as **readability-only** and explicitly told the fixer NOT to escalate it ("same type, `typedef int32_t`, no vtable hazard"). Correct about ABI, **wrong about consequence** — the spelling difference made a defect invisible to a type-filtered search, mine included. ⇒ **A "cosmetic" inconsistency that any enumeration FILTERS ON is not cosmetic; it partitions the search space.** Attach this warning to the follow-up so the next enumeration can't repeat it.

⭐⭐⭐ **"Five audits left the answer intact by luck; the sixth changed it."** Six successive unsound audits of ONE class: wrong population (base subclasses vs interface implementors) · wrong scope (body-only, blind to out-of-line) · wrong window (`NR<=160` against a body ending `:174`) · wrong summaries · wrong blanket exclusion (claimed the IUnknown trio returns no `SlangResult`; `queryInterface` does) · wrong type filter (`int`-spelled `link`). **Strongest available argument for auditing the METHOD rather than the ANSWER: soundness is invisible from a correct output, and the run of luck is what stops you looking.**

⛔ **MY FALSE COUNT while checking their "three `->validate(` callers" — I nearly reported it unsupported.** Tree-wide grep gave **one** (`slang-emit.cpp:3432`). Cause: **my clone is at `master` (`b0e43d657`) and `tools/slang-unit-test/unit-test-spirv-validation-unavailable.cpp` exists ONLY on the PR branch.** Re-derived from the PR diff: `+225`, `+250` ⇒ **1 production + 2 test = 3**, exactly as they said. ⇒ ⭐⭐⭐ **A local clone is a snapshot of a DIFFERENT commit than the PR under review, so a "tree-wide" grep silently excludes every file the PR ADDS — and absence reads identically whether the code isn't there or the FILE isn't there.** **Count from the PR diff or a checkout of its head, never `master`, and state which tree the count came from.** Use "sole **production** caller" (fixer's phrasing). Fourth wrong-object measurement of the night: wrong file · wrong branch · wrong query · **wrong tree**.

✅ **"Pre-existing" → "NOT INTRODUCED BY THIS PR, and intermittent" (fixer's correction, adopted).** The cross-branch reproduction shows the failure occurs **without this PR's commits**; it does NOT establish the defect predates the PR — that needs ancestry evidence neither of us gathered. The weaker claim is what the evidence supports and is sufficient.

✅ **`LLVMDownstreamCompiler` is the only direct implementor tree-wide** (`grep "public IDownstreamCompiler" | grep -v DownstreamCompilerBase`) — `slang-llvm.cpp:116 class LLVMDownstreamCompiler : public ComBaseObject, public IDownstreamCompiler`.

⭐⭐ **PROVENANCE SPLIT (fixer's, adopt it): jkwak-work sanctioned only *"another PR that makes the unavailability return"* — he enumerated NO implementations and NO exclusions.** The method list, the exclusions (`convert` = unsupported *request* not absent capability; DXC already `SLANG_OK`; glslang's fallback unreachable because `init` rejects that shape), and the LLVM audit are **source-derived scope WE propose**, not maintainer-approved boundaries. Also: *"the owner DIRECTED that it be merged as-is"* — his action, not an outcome we can assert (`mergedAt=null` as of 22:56:29Z).

⚠️ **"ANY commit dismisses the approval" is WITHDRAWN as unsupported** — `GET /branches/master/protection` → `403 Resource not accessible by integration`, so `dismiss_stale_reviews` is unreadable with this token. **The binding reason needs no such claim: he said he'd merge it as-is.** An unreadable setting cannot ground an absolute.

✅ **#12355 framing CORRECTED (codex, via fixer — supersedes mine): "only a null check can catch this" is FALSE.** Once the producer returns a genuine failure code, the sanctioned `SLANG_FAILED(...)` **does** prevent the deref. A caller-side null check alone would **MASK a broken contract**, which the project methodology forbids ("Do not mask"). ⇒ Root fix is **producer-side**; (a) stops being a no-op only *after* (b) lands. Cleaner than "the macro swap is a no-op".

⚠️⭐⭐ **CARRY INTO THE FOLLOW-UP PR: his phrasing is BASE-CLASS FRAMING and will miss LLVM.** He wrote *"all other sibling functions whose return type is `SlangResult`"* — but **`LLVMDownstreamCompiler` implements `IDownstreamCompiler` DIRECTLY**, bypassing `DownstreamCompilerBase`. Verified at source: `slang-llvm.cpp:116` `class LLVMDownstreamCompiler : public ComBaseObject, public IDownstreamCompiler`, and its own `validate` (`:139`) returns bare `SLANG_FAIL`. **A base-class-framed sweep provably cannot reach it.** State this explicitly in the new PR or it gets missed a second time — **all four of us missed it in R2 by enumerating subclasses instead of implementors.**

⚠️ **#12355 MUST STAY SEPARATE FROM THE SWEEP, and the ordering is now load-bearing rather than editorial.** If the sweep tidies `!= SLANG_OK` at `slang-emit.cpp:3419` as part of "fixing result codes", **the null deref survives behind a green diff and a closed issue** — because `SLANG_FAILED(0)` is `0 < 0` = false and `SLANG_OK` is `0`, so the sanctioned macro is a **byte-identical no-op** there. ⇒ **(b) the deref FIRST** (a result code cannot express "succeeded but wrote nothing", so no spelling of the guard catches it), **(a) the idiom second with the no-op stated explicitly.** An automated router already tried to dispatch exactly the (a)-only edit; the fixer declined it.

**RESUME trigger (replaces the CI one):** jkwak-work merges #12353, or the follow-up sweep PR needs writing. **No rebase, no ready-flip, no GitHub write from us.**

⛔ **THE FIRST MERGE WATCHER HAD A 1-HOUR TIMEOUT AND WOULD HAVE EXPIRED SILENTLY (~00:19Z) WITH THE PR STILL UNMERGED — the exact gap it was armed against, reintroduced by the watcher itself.** A maintainer merging overnight or next morning would have landed unobserved. ⇒ ⭐⭐⭐ **A watcher with a finite timeout on an event with NO deadline is a delayed instance of the silence-reads-as-nothing-happened defect** — and its expiry is invisible, so "no notification" still reads as "not merged yet." **Match the instrument's lifetime to the EVENT's horizon, not to a session's.** Replaced with a guard script at `/workspace/agent/pr12353-guard.sh` (durable, survives session end) that wakes only when `mergedAt != null`, `state != OPEN`, or `reviewDecision != APPROVED` — i.e. **any** departure from the current resting state, not merge-only. ✅ **Control-tested both directions before trusting it: returns `wakeAgent:false` now, and an inverted-predicate copy returns `wakeAgent:true`** — a guard that can only ever say "no" is the `pgrep`-that-self-matches defect in cron form.

✅ **ORIGINALLY ARMED on the DECIDING axis (not CI): a monitor polled `gh pr view 12353 --json state,mergedAt,mergeStateStatus,reviewDecision` every 60 s and fires on any transition, breaking on merge or close-without-merge.** This exists because the approval at 22:46:11Z arrived with **three parties watching the CI run and nobody watching review state** — see [[feedback_ci_terminal_is_not_chain_terminal_arm_the_deciding_axis]]. Merge unblocks the follow-up sweep PR.

⚠️✅ **23:20Z — `mergeStateStatus` moved `BEHIND` → `BLOCKED`. NOT a new obstruction; do NOT act on it.** Investigated because `BLOCKED` can mean a failing required check:
- **Head UNCHANGED** at `2c9cf98fb0` (no force-push), `mergeable=MERGEABLE`, `reviewDecision=APPROVED`, `mergedAt=null`.
- **No new failing check.** The only thing that ran near 23:20 was `PR Maintenance` (`completed/success`, 23:20:38Z). ⚠️ **A naive "newest row per name" over 145 check-runs LOOKED like 30+ skipped required checks — those bare-name rows (`build-linux-debug-gcc-aarch64`, no `/ build` suffix) are DRAFT-ERA (14:48/14:49) and are a DIFFERENT check set from the real jobs (`… / build`, success 21:15).** Two check-name families on one head; matching on the prefix conflates them.
- ✅ **CONTROL SETTLED IT: `BLOCKED` is the NORMAL resting state for approved PRs in this repo** — surveyed 12 open non-draft PRs: **#12322 `BLOCKED·APPROVED`, #12336 `BLOCKED·APPROVED`**, plus #12301/#12304 `BLOCKED·REVIEW_REQUIRED`. Base advanced (`master` tip `fe64ccc609` @22:33Z vs PR base `ff45b15ed3`), which is what flipped BEHIND→BLOCKED.
⇒ ⭐⭐ **A state-name that SOUNDS alarming needs a cross-sectional control before it becomes a finding — "is this value unusual HERE?" not "what does this value mean in general?"** One `gh pr view` across sibling PRs converted an apparent regression into a baseline. **Still just awaiting his merge; no rebase, no action.**

⭐⭐⭐ **THE DECLARATION-VS-DEFINITION CONFLATION HIT THREE TIMES ON ONE CHAIN — AND IT IS ONE CODEBASE CONVENTION, NOT THREE COINCIDENCES.** (1) DXC: `slang-dxc-compiler.h` is 20 lines, class body in the `.cpp` at `:240` ⇒ header search read as absence. (2) LLVM `getVersionString`: declared `:136`, defined `:522` ⇒ body-scoped `return SLANG_FAIL` count was structurally blind to it. (3) Base `convert`: declared `slang-downstream-compiler.h:406`, defined **out-of-line** `slang-downstream-compiler.cpp:25` → `SLANG_E_NOT_AVAILABLE`, which the fixer conflated with `LLVMDownstreamCompiler::convert` `slang-llvm.cpp:514` → `SLANG_E_NOT_IMPLEMENTED` (both verified; genuinely different code, both out of scope). ⇒ **`compiler-core` habitually defines out-of-line, so ANY declaration-site search over this surface is wrong by construction. Enumerate declarations and definitions as TWO LISTS and reconcile.** Carry as a standing hazard for whoever audits this surface next.

⚠️ **A claim that makes the chosen action look FREE deserves the same scrutiny as one that makes it look necessary.** The fixer retracted "the merge is imminent / waiting costs nothing" — `APPROVED + BLOCKED` supports neither — and **I would have accepted "costs nothing" unchallenged because it favoured a decision I already agreed with.** True narrower form: *waiting avoids conditional rework; the merge is neither ours to perform nor to predict.* Same generator as the "tidy symmetry" and "different scopes, both valid" comfort-framings on this chain.

⭐⭐⭐ **REACHABILITY TELLS YOU BLAST RADIUS; OWNERSHIP TELLS YOU WHERE THE FIX GOES** (peer's framing, adopted — all three of us conflated them on `link`). **Why it is seductive: the audit question and the repair question share the same evidence and have different answers.** "Which implementors exhibit this defect" → LLVM. "Where does the fix belong" → the interface. ⚠️ **Getting the population RIGHT is what made the mis-attribution available** — you cannot mis-attribute an inherited default to an implementor until you are correctly enumerating implementors. Fixing one axis of an enumeration does not complete it.

⚠️ **`mergeStateStatus` `BLOCKED` → `UNKNOWN` at 23:59:30Z — IGNORE.** `UNKNOWN` was already present in my sibling control (#12352, #12363, #12323, #12347, #12357, #12373) ⇒ GitHub recomputing mergeability, not a change in this PR. `state=OPEN`, `mergedAt=null`, head untouched. **Second instance of "a finding manufactured from a normal value" on this chain — and the control I ALREADY HAD let me discard it in one read instead of investigating.** ⇒ ⭐⭐ **A cross-sectional control keeps paying out after the question that motivated it: build the baseline once, reuse it on every subsequent state twitch.**

⚠️ **UNADJUDICATED ATTRIBUTION, recorded as such rather than guessed:** the reviewer first reported *"the fixer found a fourth LLVM defect"*, then later *"I'd told it that LLVM inherits the defective `link`."* Both can be true (raised by one, verified and re-scoped by the other); **I hold no receipt either way and did NOT pick a side.** ⇒ ⭐⭐ **On a chain that has already produced one timestamp-disproved origin claim (mine, declined by the fixer) and one re-filed tally, THE ORIGIN OF A CORRECT FINDING IS THE LEAST LOAD-BEARING FACT ABOUT IT** — record the finding without an originator rather than spend a round settling it, and say explicitly that you are declining to adjudicate.

## Continued

⇒ **Post-approval and #12375 follow-up findings are in [[project_12375_downstream_sibling_sweep_followup]]** (split 2026-08-06: this memo hit 44,346 B vs a ~24.4 KB read bound, so everything past the prefix was dark — including the corrected 8-carrier figure and the attribution-limit finding).
