---
name: project_11969_metal_out_param_addrspace
description: SHIPPED — #11969 Metal fragment out-param crash; draft PR #12015, review dispatched to slang-reviewer
metadata: 
  node_type: memory
  type: project
  originSessionId: dee4803d-ca45-4ca5-ba12-9da919bf9e23
---

**#11969** — Metal: fragment entry point with `out float4 : SV_Target` param (instead of return value) crashes Metal emitter: `error[E99997] ... Unknown addressspace encountered`. Compiles fine to SPIRV/HLSL/GLSL, and to Metal in return-style. Author tqjxlm, slang 2026.12.2, macOS 15/arm64.

**Root cause (triager-verified via -dump-ir on TOT e39e3ce03):** out-param→return-struct lowering (`legalizeVertexShaderOutputParamsForMetal` → `lowerOutParameters`) runs ONLY for `Stage::Vertex` at slang-ir-legalize-varying-params.cpp:5104. Fragment out-param skips it, survives as pointer-typed `OutParamType` into slang-emit-metal.cpp:1363 address-space switch `default:` throw. `lowerOutParameters` is already stage-agnostic; `wrapReturnValueInStruct` already has a `Stage::Fragment` branch — machinery exists, just not invoked for fragment. Fragment `inout` also crashes (same cause).

**Recommended fix (Approach A):** drop the vertex-only gate so fragment + other output-bearing graphics stages run the same conversion. NOT a consumer-side emit-switch patch (masks producer bug). B/C documented+rejected in memo.

**State:** bug/medium/P2. Triager posted 5-bullet verdict (comment 4902959679), applied `reproduced` + Type=Bug, peer-wired handoff to slang-fixer on canonical thread `gh-issue-shader-slang/slang-11969`. Do NOT double-dispatch to fixer ([[feedback_no_double_dispatch_peer_wired]]). PRs draft-only, ready-flip/merge operator-gated.

**⚠️ 2026-07-09 ~02:00 — STALL/ORPHAN CONFIRMED (priority chain silent ~14h).** Fix was committed at `fix/issue-11969@20bc7d0125`, rebased conflict-free onto master → **`d5f0f06144`** (1-ahead/0-behind; gate broadened vertex→vertex+fragment, helper renamed stage-neutral, `tests/metal/fragment-out-param.slang` present). Fresh rebuild kicked Jul 8 ~11:53, last signal "build 77/1170" Jul 8 12:09Z — then SILENCE. Lower-priority sibling #11981 (sequenced AFTER) finished first with draft PR #12014 while #11969 produced nothing.

**Main global-scope evidence (triager is group-scoped, CANNOT see this):** grepped ALL sessions across ALL groups — **NO session exists on the `gh-issue-shader-slang/slang-11969` thread anywhere**; the only container running Jul 9 was the triager's. `fix/issue-11969` NOT pushed to origin, no PR. Conclusion: the mid-rebuild fixer container was killed (`--rm` exit and/or the 11:50 build-2.1.39 restart) and no session resumed it — genuinely orphaned, not "still grinding." Matches [[project_taskless_fixer_review_cc_loop]] / [[project_stall_sweep_incident]] failure family.

**⚠️ RECOVERY-CRITICAL:** commit `d5f0f06144` was NEVER pushed → it lives ONLY in the local `fix/issue-11969` worktree. Operator note (build 2.1.39) cleared only gitignored build/ output, NOT commits/worktrees — so the commit SHOULD survive, but the fresh re-kick session MUST verify the worktree+commit exist before rebuilding. If the worktree is gone, the fix is re-derivable from this memo (Approach A: drop vertex-only gate in `legalizeEntryPointVaryingParamsForMetal`), but that's a re-implement not a resume. Re-kick mints a FRESH cold session (no in-context rebuild memory) — it can't "resume," it restarts.

**Main escalation lever armed:** background checkpoint polling for a `slang-11969` session; if none appears + no build by ~02:34Z, Main will `ncl groups restart` the slang-fixer group (`ag-1780667166439-vmjrwe`) to force a clean container. Routed via triager (peer-wire owner); Main holds the runtime-restart capability the triager lacks.

**✅ 2026-07-09 ~02:50 — RECOVERED + SHIPPED.** Fixer resumed, pushed `fix/issue-11969` (head `bf106aee9b`), opened **DRAFT PR #12015** (base master, `pr: non-breaking`, Closes #11969, bot disclaimer, `report_pr_created` done). Fully verified: codex PLAN/CODE/OUTPUT all approve; fragment-out-param 7/7; full tests/metal 193/193 on fresh rebuild. GitHub-confirmed real (Main `gh pr view`): OPEN/draft/master/fix-issue-11969/nv-slang-bot, created 02:35:35Z. CI note: `github.ci_failed` on bf106aee9b = BENIGN priority-yield (manual workflow_dispatch on draft; real jobs SKIPPED) — matches [[project_bot_pr_priority_yield_red_run]].

**Reviewer hand-off — Main owns this edge.** Fixer hit "No agent named '<x>' addressable" for reviewer/triager/parent — fixer↔reviewer was NEVER peer-wired (that edge isn't the fixer's), compounded by the ~11:04 OneCLI gateway-blip mesh outage (now resolved). slang-reviewer group HEALTHY: `ag-1780667168475-a9tac8`, in Main's destinations. Main dispatched the #12015 review to slang-reviewer directly on thread `gh-issue-shader-slang/slang-11969` and stood the fixer down. Fixer's job on #11969 is done; only peer-review + eventual maintainer ready-flip/merge (operator-gated) remain. Await [Review Report] from slang-reviewer.

**⚠️ 2026-07-09 ~02:54 — #12015 REVIEW BLOCKED: slang-reviewer session LOGGED OUT (operator-scoped).** Triager + fixer saw reviewer session emit "Not logged in · Please run /login" (reviewer msg 146, 02:51Z). Main global-scope verification: (a) fixer→reviewer agent wiring EXISTS (`ag-1780667166439-vmjrwe → slang-reviewer ag-1780667168475-a9tac8`) — so the earlier "never peer-wired" theory is WRONG; routing is fine; (b) ZERO dropped-messages mentioning reviewer/12015/11969 — message not lost; (c) NO reviewer session on any 11969/12015 thread and newest reviewer `last_active` across BOTH reviewer groups is **June 24** — nothing today; (d) PR #12015 has 0 reviews / 0 review-requests / 0 comments (triager-verified). **Reconciliation:** "id 151 landed" (accepted into thread) + "not addressable/blocked" (session can't ACT) are both true — the message routes but the reviewer session is unauthenticated and cannot perform the review. Re-dispatching more will NOT help a logged-out session. **NEEDS OPERATOR `/login` (re-auth) on the slang-reviewer container** — outside fixer/triager/Main reach (auth is provider/OneCLI-cred, a `ncl groups restart` alone won't re-auth an expired credential). Not catastrophic: both PRs shipped + held anyway for operator-gated ready-flip. Main escalated to operator. Hold for reviewer verdict is SUSPENDED until re-auth; do not chase a verdict that structurally can't arrive.
