---
name: project_11984_debugsource_bom_pending
metadata: 
  node_type: memory
  type: project
  originSessionId: 887ca12c-7f18-4a98-a8ef-7291ba649c14
---

shader-slang/slang#11984 (pdeayton-nv): `-g2` embeds a `#line`-referenced file's `DebugSource` via a fresh raw-byte disk re-read (`slang-lower-to-ir.cpp:9564-9583`, taken because a `#line`-named path has no loaded `SourceFile`), bypassing `SourceFile::setContents`'s canonical BOM-strip (`slang-source-loc.cpp:619-663`). A UTF-8 BOM (`EF BB BF`) survives into the `OpString` text while line/column data stays BOM-free → 1-code-point misalignment. Producer-side bug; SPIR-V consumer (`emit-spirv.cpp:2136`) innocent.

**DRAFT PR LANDED = #12055 (2026-07-10 ~20:44Z; Main-verified at HEAD).** https://github.com/shader-slang/slang/pull/12055 — OPEN, **draft** (guardrail intact, fixer did NOT flip ✓), author nv-slang-bot, branch `fix/issue-11984 @ bc278df1b6`, `Fixes #11984`, label `pr: non-breaking`, **report_pr_created called** (verified — no orphaned-webhook risk). Fix as scoped (Rec A, producer-side): extracted canonical raw→BOM-free-UTF-8 decode into a shared static `SourceFile::decodeContentBlob`, routed `getOrEmitDebugSource`'s fresh-read fallback through it (was embedding raw bytes → BOM survived, misaligning DebugSource text vs BOM-free line/col by 1 code point). 5 files +88/−27. Tests: repro `tests/spirv/debug-source-bom.slang` 1/1 with revert-drill proven (unfixed baseline emits `OpString "\xEF\xBB\xBF…"`, FAILS 0/1); `tests/spirv/` 502/502; DebugSource suite 5/5. codex PLAN+CODE+OUTPUT all approve (0 must-fix); **peer review dispatched to slang-reviewer by the fixer.** CI red = benign priority-yield (check-ci + wait-for-human-priority only, builds skipped) [[project_bot_pr_priority_yield_red_run]]. **Next:** Reviewer A verdict + human review; draft stays draft (ready/merge operator/maintainer-gated).

**MAINTAINER APPROVED + FLIPPED READY 2026-07-10 22:09Z (Main-verified at HEAD — NO breach).** jkwak-work reviewed "Looks good to me" → **APPROVED** (reviewDecision=APPROVED, review 4675278562) AND flipped #12055 **ready-for-review himself at 22:09:28Z** (timeline event actor=jkwak-work). So PR is now non-draft/OPEN/MERGEABLE/APPROVED. **NOT a drafts-only breach** — the MAINTAINER un-drafted it, not the bot (verified via issue timeline, matches [[feedback_drafts_only_guardrail]]); the fixer correctly held draft + surfaced upward, did NOT self-flip. The fixer's "stays a DRAFT" report (msg 24646/24648) is STALE by seconds (jkwak flipped it right after approving). **No operator ready-flip decision to surface** — the maintainer already did the flip, and merge is jkwak's to take at his discretion (he has rights). NOT merged yet (mergedAt=null). Bot must still NOT merge (operator-gated). slang-reviewer peer verdict (A/B/C) still running — fold in when it lands but maintainer APPROVE is the stronger signal. Next terminal state = jkwak merges (→ fixer reaps worktree) or a further review comment. **Reporter loop:** pdeayton-nv asked "draft PR ready?" 07-10 03:00Z (triager gave honest "not yet" then) — the triager (owns this thread) should now surface the #12055 link to close that loop. Do NOT double-dispatch ([[feedback_no_double_dispatch_peer_wired]]).

Triage (07-07): Type=Bug + `reproduced` set, verified 5-bullet posted ([comment 4910063798](https://github.com/shader-slang/slang/issues/11984#issuecomment-4910063798)), memo `triage-11984.md` (3 approaches). Reporter pdeayton-nv asked "draft PR ready?" 07-10 03:00Z; triager replied honestly (no PR public yet, [comment 4931638255](https://github.com/shader-slang/slang/issues/11984#issuecomment-4931638255)).

**2026-07-15 22:57 — REVIEW ROUND ADDRESSED, awaiting pdeayton-nv re-review (fixer msg 37942).** pdeayton-nv (reviewer, described by fixer as now holding merge authority) left 2 inline comments; both addressed in commit `0a1da47291` + replied on-thread. Changes: (1) `decodeContentBlob` `rawContentBegin` → `const Byte*` (getBufferPointer returns `void const*`; kills a -Wcast-qual warning across all 3 call sites); (2) dropped redundant `-O0` from the test directive. +2/−2, **no behavior change**. Verify: signature-read across 3 call sites + clang-format-clean + codex OUTPUT_REVIEW re-approved. Did NOT rebuild locally (build/ reclaimed id=20; disk 97%/9G-free, a ~7G build risks re-filling shared volume) — non-draft push auto-triggered full `pull_request` CI (head `0a1da47291`, in-progress) covering the real build+test. **Push auto-dismissed jkwak-work's earlier APPROVED (APPROVED→REVIEW_REQUIRED) — expected, not a regression;** pdeayton must re-review his two requests before merging. Next: webhook-driven — pdeayton APPROVE+merge → fixer reaps worktree + reports; further comment → address; real CI failure → triage. Bot does NOT merge (operator gate).

**2026-07-15 23:02 — APPROVED at HEAD, bot work complete, holding for merge (fixer msg 37944).** pdeayton-nv (reviewer + delegated merge authority) re-reviewed and **APPROVED** commit `0a1da47291` (= current head, review `commit_id == headRefOid` — not stale) at 23:01:37Z. `reviewDecision: APPROVED`, `MERGEABLE`, both inline requests accepted. CI green so far; full build matrix (Linux/macOS/Windows/sanitizer) in-progress, zero failures. **Merge delegated to pdeayton by jkwak-work; bot does NOT merge (operator gate).** Next: webhook-driven — MERGED/CLOSED → reap `wt-slang-11984` + `active-work/slang-11984`, report; late CI failure → triage; further comment → address. No GitHub post (approval + CI are the footprint).

Third of the DebugSource cluster, same reporter/subsystem, distinct causes: [[project_11982_debugsource_dup_import]] (dup DebugSource for imported module) and #11983. Drafts-only guardrail applies to any fixer PR.
