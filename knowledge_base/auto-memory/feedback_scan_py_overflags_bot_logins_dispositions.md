---
name: feedback_scan_py_overflags_bot_logins_dispositions
description: "scan.py over-flags needs_nudge — coderabbit/slangbot absent from bot_logins + pull-universe omits dispositions; verify, don't blast"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: c155d051-5657-4d95-bac6-caa24f4a3f29
---

Supervisor tick 87 (2026-07-15): `scan.py` flagged **102 needs_nudge / 7 escalate** out of 167 in-flight — ~100× the verified steady-state (tick 67 fired 0/31, tick 86 fired 1). All 102 resolved to NOT-our-ball on inspection. **0 nudges was correct.**

Two structural false-positive sources, both in the tooling, not the chains:

1. **`pull-universe.sh` does not inject prior-state `disposition` into chain payloads.** `scan.py`'s bot-last carve-out (`we_owe_next_step`, reads `chain["disposition"]`) therefore can't park maintainer-driving / human-debate / reviewer-gated / stood-down chains. Fix each tick: read `memory/supervisor-state.json`, map `{thread_id: disposition}`, inject into `uni['chains'][tid]['disposition']`, re-run scan. Drops flags but NOT enough (the `ball==ours` path ignores disposition by design).

2. **`scan.py` default `bot_logins` = only `nv-slang-bot`.** So `coderabbitai` and `slangbot` review comments read as "human spoke last → awaiting_us" even when we pushed after. ~15+ chains/tick are pure coderabbit-last false positives.

**The decisive verification test** (apply to every flagged row before nudging): a flag is a false positive if EITHER (a) the last comment author matches `coderabbit|slangbot|nv-slang-bot|[bot]`, OR (b) our_last_outbound/our_last_push ≥ last comment time (we already answered), OR (c) the carried disposition contains a parking token (`maintainer-driving|human-debate|external-pr|reviewer-gat|awaiting-pickup|stood-down|advisory|held|blocker|escalat|auto-retry|lingering session`). After all three filters, the genuine survivors are typically <5 and each still needs a per-chain look (maintainer-assigned? approved-pending-merge? stale sub-thread on a CLOSED issue?).

**Why 0 nudges is usually right:** in steady state maintainers (jkwak-work, szihs, jhelferty-nv, skiminki-nv, pdeayton-nv, jvepsalainen-nv) spoke last *because they own the chain* — we correctly stood down. A maintainer's last word on a chain they're assigned to is a handoff, not an owed reply. See [[feedback_dead_promise_check_assignee_before_rewake]], [[feedback_holding_echoes_are_noise]].

Blind-firing scan's raw `needs_nudge` would be a spam incident that re-wakes dozens of settled/parked chains. The skill's "`needs_nudge=true` is authoritative" MUST-rule assumes scan had dispositions + full bot_logins; when it doesn't (current pull-universe), the supervisor's judgment call is mandatory.

**Tick 89 (2026-07-18) — root causes localized to exact lines + a THIRD filter:**
- Bug #1 line: **`pull-universe.sh:106`** `bot_logins = {"nv-slang-bot[bot]", "nv-slang-bot"}`. It stamps `is_bot:false` on `coderabbitai` (97 cmts this tick), `github-actions` (67), `slangbot` (9), `CLAassistant` (7), `copilot-swe-agent`, `copilot-pull-request-reviewer`, `claude`. Every downstream `is_bot` flows from this. Correcting it in the payload before scan: raw **100 → 82**.
- Bug #2: **`scan.py` `classify()`** applies `HUMAN_OWNED_DISPOSITION` only on the `ball=="human"` path (`we_owe_next_step`), NOT on `ball=="ours"`. A maintainer commenting last on their own assigned issue flags `awaiting_us` despite a parked disposition. Note the token list `HUMAN_OWNED_DISPOSITION` is also narrow — real dispositions like "awaiting-classification", "maintainer<->maintainer design cross-ref", "BLOCKER…escalated to operator", "held for direction" contain NO matched token, so widen the match or check `issue.assignees` directly.
- **THIRD filter (new this tick, decisive):** a flagged chain with a **running container** right now (`ncl sessions … container_status=='running'` on that gh-issue thread) is being actively worked — NOT stalled. Tick 89 this removed 6 (#11591/#11595/#11944/#12133/#12149/#8125). scan can't see live container state, so add it to the verification test alongside (a)/(b)/(c).
- **FOURTH check:** for the tiny genuine-looking survivors, `gh issue view N --json assignees` — a human maintainer assignee ⇒ stand-down (their last word is a handoff). Tick 89: #12062/#12071/#1052/#844 all had maintainer assignees; only #11476 (concrete unanswered ask, fixer dark) + slangpy#1070 (NEW bot-filed, never triaged) were genuine.
- Tick 89 funnel: raw 100 → bot-fix 82 → +disposition/assignee/running filters → **1 nudge + 1 triage dispatch**. Reported the 82-vs-1 gap transparently to dashboard per fails-loud invariant (did NOT report clean). See [[feedback_deadpromise_check_assignee_before_rewake]].

**Tick 91 (2026-07-18 12:00Z) — same pattern, raw 104 → verified 0.** Batch-fetched GitHub last-commenter + PR state for ALL 104 flagged rows (not a sample). Funnel: **48 bot-last** (coderabbit/nv-slang-bot/slangbot last on issue or PR — R4 leave-alone), **29 maintainer-driving/parked** dispositions, **~22 maintainer-assigned issues or ext/maintainer-authored PRs** awaiting human review (only 1 bot-PR #11595 = WOULD_APPROVE merge-gated), **5 CI-anchor/fresh** (12137/12145 babysitter-owned, slangpy#1070 GPU-wedge 0-cmts, samples#54 ext-PR clean, 11568/recovery-2 on CLOSED issue → archived). **0 genuine our-ball dead-fixer stalls → 0 nudges (correct).** scan.py STILL leaks bot-last (48) + maintainer-disp (29) into `action='nudge'` — its own contract says these never should. The fails-loudly reconciliation (`sent!=must_nudge`) therefore fires every tick and is meaningless until scan.py's `needs_nudge` gate excludes bot-last + human-owned-disposition rows. Surfaced the 104→0 gap loudly on dashboard; did NOT `ask_user_question`-block (no operator decision needed to proceed; recurring known FP). **The right durable fix is a scan.py patch, not a per-tick judgment call** — recommend routing to nanoclaw/coworker-infra.

**Tick 94 (2026-07-20 00:07Z) — same pattern, raw 11 → verified 0; NEW crash + fix documented.**
- **NEW crash this tick:** `pull-universe.sh` writes `our_last_outbound` in NAIVE `YYYY-MM-DD HH:MM:SS` (space-sep, no Z) for 73 chains (ncl outbound created_at stored naive-UTC). scan.py's `parse_ts` returns a naive datetime for those → `TypeError: can't compare offset-naive and offset-aware` in `compute_last_activity_by_us` `max()`. **Fix:** normalize the payload before scan — regex `^(\d4-\d2-\d2)[ T](\d2:\d2:\d2)(\.\d+)?$` → `\1T\2\3Z` (values ARE UTC). Do this on the raw universe JSON, then re-run.
- After normalize, raw flagged **11 awaiting_us**; ALL 11 bot-last FP (coderabbitai ×10 on #11475/11667/12094/12095/12119/12125/12127/12141/12142/12136-jvep-review, CLAassistant ×1 on slangpy#1054). is_bot re-stamp (162 comments, BOT_RE=`coderabbit|slangbot|nv-slang-bot|[bot]|CLAassistant|github-actions|copilot|^claude$|devin`) → **must_nudge 0**. Dispositions ALREADY injected by pull-universe now (129 chains carry `disposition`) — bug #1 from tick 89 is fixed in tooling; only bot_logins widen (bug in pull-universe.sh:106) remains.
- **Both durable fixes belong in pull-universe.sh:** (1) widen `bot_logins` set; (2) emit ISO-Z timestamps for `our_last_outbound`/`our_last_push`, not naive. Until then the per-tick normalize+restamp is mandatory or scan.py CRASHES (not just over-flags).

**Tick 96 (2026-07-21 12:00Z) — raw 86 → verified 1 genuine / 0 escalate. Skill was RE-SYNCED 07-21 06:30, reverting tick-95's inline scan.py patches — all three fixes had to be re-applied.** Funnel: (a) re-fixed `parse_ts` naive→aware crash (same as tick 94); (b) corrected `is_bot=True` for github-actions/coderabbitai/CLAassistant on 153 comments (86→71); (c) **lifted the HUMAN_OWNED gate to the TOP of `classify()`** so it fires on `ball==ours` AND `ball==none`, not just the bot-last `we_owe_next_step` path — this is the fix for bug #2 that ticks 89/91 only diagnosed. Return `awaiting_human` (never `silent`) so the escalate counter doesn't trip (71→38); (d) widened `HUMAN_OWNED_DISPOSITION` with descriptive tokens the state file actually uses: `maintainer-owned`, `lingering session`, `superseded`, `maintainer-assigned` — but DELIBERATELY NOT `awaiting_human`/`pr_open` prefixes (a FRESH human ask must override a stale self-label) (38→29); (e) per-chain live verification of 29 → **1 genuine: #11476** (pdeayton fresh @nv-slang-bot ask 07-21 05:09Z, our last reply + tick-95 nudge both PREDATE it, fixer STOPPED) + 28 FP. Refreshed the 28 verified dispositions into payload → **must_nudge=1 honestly** (sent==must_nudge, fails-loud satisfied). All 33 `test_scan.py` pass — the tests never exercised `ball==ours`/`ball==none`, which is why the gate gap survived. **Durable upstream fix owed** (edits revert every skill re-sync): pull-universe.sh bot_logins+ISO-Z+is_bot; scan.py parse_ts + HUMAN_OWNED-gate-on-all-paths; add a `ball==ours` maintainer-driving test case.
