---
name: slang-rhi-backend-chains-index
description: LIVE chains — slang-rhi + backend-codegen (Metal/CUDA/HLSL-SPIRV). Most rows were spilled from MEMORY.md 2026-08-04 (3rd spill) and are gated on a NAMED human; the "added directly" section at top may hold rows where OUR step is pending (currently #12349). Each keeps its RESUME trigger verbatim.
metadata: 
  node_type: memory
  type: index
  title: Slang RHI / backend-codegen live chains
  tags: 
    - slang
    - slang-rhi
    - metal
    - cuda
    - live-chain
    - spillover
  originSessionId: 7c60dd16-8d5c-4bb3-b934-5056a88a40a4
---

# Slang RHI / backend-codegen live chains

Spilled from `MEMORY.md` on 2026-08-04 at the index floor, under the standing
rule that **the only real lever at the floor is spillover, not deletion**. These
are **LIVE** rows, not parked ones — each is gated on a named human, which is
why they were the lowest-priority live rows to move rather than the least true.

⚠️**Rows here retain full force.** A row's presence in a child file is about
index bytes, never about its status. Re-read the linked memo before acting on
any of them — a stale alarm outlives the thing it alarms about.

## Rows added directly here (not spilled)

- ✅**[rhi#824 parallel pipeline creation — MERGED, approver's OPEN_GAP premise RESOLVED, gap EVAPORATES](project_slang_rhi_824_nvapi_option_window_premise_resolved.md)** — merged 08-10T13:59:27Z (self-merged, at approver's pinned head `6e60945d03f1`). `ABSTAIN_POLICY:OPEN_GAP` hung on ONE premise: does `SetCreatePipelineStateOptions` affect graphics/compute PSOs? **Resolved raytracing-only** — 2 independent `nvapi.h` mirrors (flag block `diff`s IDENTICAL, no `_V2` struct): every flag reads *"Change whether **raytracing pipelines** are created with…"*, and **NVRHI sets these flags ONCE in its device ctor, never resets, no mutex** ⇒ harmless-by-construction in NVIDIA's own RHI. **Nothing live on `main`; correct verdict at decision time was WOULD_APPROVE.** ⚠️Docs + reference-impl oracle, NOT execution — CI *compiles* NVAPI but reports `SKIPPED (Device does not support NVAPI)`. ⚠️`gh api repos/NVIDIA/nvapi` → **401 `Bad credentials` on my edge** while `slang-rhi` reads 200 in the SAME shell and `search/code` on that repo works ⇒ third-party mirrors were forced, so the mirror-equality check is load-bearing. Also MINE-VERIFIED: **CodeRabbit publishes a green `success` commit status with `reviews[] count=0`** (rate-limit notice lives only in `issues/comments`, 13:46:01Z) ⇒ any review-row harvest scores it absent while the green check implies it passed. 🔴**Joined a FALSE ABSTAIN / LOSS** (approver's own scoring; it declined my exculpatory *"right at decision time"* framing — only the outcome feeds accuracy). ⛔**One leg of that loss is VOID: the self-merge carries ZERO bits** — merged 13m50s after open, `reviews[]=0`, no bot presence (approver never posts), decision landed 14:05Z = **6 min AFTER the merge**, so the author could not have seen the finding. Loss stands on the resolved premise alone. Inverse of [[feedback_merged_does_not_mean_the_flagged_gap_was_closed]]. ⚠️**Doc-quote leg rests on MY edge alone** — approver confirmed the NVRHI oracle from source and reproduced the 401, but every `nvapi.h` it can reach predates these flags and my mirrors 404 on its fetch path. 🔴**Ledger: TWO denied appends (decision 14:05Z + join 15:07Z) ⇒ replay decision+join per PR; replaying the decision alone lands an unjoined abstain that reads as an open hold instead of a recorded loss.** 5th+ consecutive denial, `APPROVAL_LEDGER_WRITERS` unset fleet-wide — my outstanding operator action.

- 🔵**[rhi#811 shader-object-layout cache UAF](project_slang_rhi_811_shader_object_layout_cache_uaf.md)** — **R2 TERMINAL 08-05: ABSTAIN_POLICY `CHALLENGER_CONCERN` @ `2a3524d8`. ✅5th webhook 08-10 HELD, no re-dispatch** (head `b4ca3c8c`, **5 for 5** moves): ahead_by 6 but **core fix BYTE-IDENTICAL — `src/device.*` not in the delta at all**; only PR-owned commit is `3ef27be1` `+1/-0` closing CR's last 🟡 (`REQUIRE(secondShaderObject != nullptr)`), rest is upstream main (#813/#814/#815/#819). **All 3 readiness tripwires NEGATIVE, checked individually:** WIP comment `5192349256` **still standing verbatim, `created_at == updated_at`** (a real check here — CR's summary comment WAS edited in place) · last `ReviewRequestedEvent` still `coderabbitai`/**Bot**, zero User-actored since the author's own removals · ⛔**`skallweitNV`'s main-merge is a BATCH SWEEP, not engagement — identical `Merge branch 'main'` on their OWN #598 2m18s earlier** ⇒ ⭐⭐⭐**a maintainer action is engagement-with-THIS-PR only if it isn't also hitting their other PRs in the same minute; check the neighbours before reading intent** (contrast rhi#802 where the same operation WAS genuine — discriminator is the sweep, not the operation). 🔴**AUTHOR-SILENCE ESCALATION FIRED: WIP note 4d17h old, author last active `3ef27be1` 08-06 ⇒ ~3d13h silent; a green finished-looking PR parked under a 5-day WIP label. Reported upstream 08-10.** ⏱️CI run `31364419295` `queued` (read the RUN, not the 23-check count). ⛔PIN `2a3524d8`; reject verdicts on `5d00ad51`/`e062d03f`/`4c020aeb`. ⭐**Every harness input said approve — 6/6 clauses, CI fully GREEN (run `31013368950`, all 4 wrapper-bearing Debug legs executed+passed, 7 backends), CR reviewed head clean, 0🔴, both nits cleared — and it abstained on the WIP declaration alone: `WOULD_APPROVE` claims READY-TO-MERGE, artifacts establish only SOUND, and only the author speaks to readiness.** 🔴**Policy gap RESOLVED — the proposed `author_declared_ready` clause is DEAD ON BOTH LIMBS and the approver proved it (shared learning `1785942491533`): "non-draft" is an ENTRY PRECONDITION (webhook-driven ⇒ passes on every PR the tier sees; `gh pr view 811 --json isDraft` → false ON THE WIP PR), and "no author WIP comment" is a JUDGMENT read that would evaluate `unevaluable` ⇒ spurious ABSTAIN_INFRA fleet-wide. #811 is its own refuting control: the clause would have PASSED. ✅NOTHING for the operator to sign off.** ⛔⭐⭐⭐**MY defect: I routed it upstream as an open operator item TWICE without probing it — a gap stated by the tier that OWNS the mechanism felt pre-verified. A FORWARDED ASK IS STILL AN ASK: name the input that makes it FAIL · find a trigger-present control (⛔if your motivating case PASSES the clause, it isn't the fix) · check ENTRY PRECONDITIONS ("how did this PR reach me?") · check evaluability against the tier's data. One `--json isDraft` call, available from report #1.** 🔵**Re-dispatch ONLY on: author says ready / AUTHOR (⛔not bot — `coderabbitai[Bot]` auto-re-requested `bmillsNV` TWICE) re-requests / human maintainer engages. A bare `synchronize` is NOT readiness — he pushed a re-architecture 13min AFTER declaring WIP.** ⛔Don't re-run to `NO_REVIEW_SIGNAL`. 🔴**My "requested_reviewers empty" was STALE (re-added 14:07:36Z) and I declined the approver's "true when you stamped it" charity** ⇒ [[feedback_a_multi_probe_turn_has_a_window_not_a_timestamp]] (**a multi-probe turn has a WINDOW, not a timestamp**; **don't inherit a peer's unverified ABSOLUTION when it helps you** — symmetric half of R1). ⛔⭐⭐⭐**THEN BOTH OF US OVER-RETRACTED — the real lesson: SELF-ACCUSATION IS A DILIGENCE SLOT, the least-audited one, since nobody audits a claim whose only victim is the claimant.** I wrote "the proof was in my own output" — FALSE, my turn never held the 14:07:36Z change point (only enough to know the fact was UNVERIFIED, never FALSE; ⭐**a self-criticism needing info you didn't have is a story, not a lesson — and the weaker claim is the useful one because it's actionable at ship time**). They wrote "both timestamps were printed in your dispatch (id 16)" — FALSE, only 14:08:49Z was; **decisive internal check: asserting `requested_reviewers` EMPTY is logically incompatible with holding an event that ADDS a reviewer.** ⚠️**Accepting a peer's over-retraction isn't politeness — it propagates a false fact about YOUR OWN artifact into THEIR process file, the one place you're authoritative and they can't check.** Their surviving core: at msg 24 they held both numbers and didn't subtract ⇒ real rule is **cross-check a NEW finding against facts already standing in the conversation** (integrating across sources), not "read the reply more carefully." ⭐⭐⭐**Chain's best artifact lesson: the SAME PR had two endpoints lie in OPPOSITE directions — R1's `📥 Commits` header said "reviewed" while rate-limited; R2's `pulls/811/reviews` says "not reviewed" when it had been (a clean pass is an EDIT to the summary comment). Only the rate-limit marker got both right.** ⛔**`fetched==total_count` is a TRUNCATION guard, NOT completeness — read `20==20`, later grew to 21; use `actions/runs/<id>.conclusion`.** ⭐**An event that changes state is not evidence about INTENT until you check `actor.__typename`** (approver's catch, authorship→ACTORSHIP). Prior R0 detail: dispatched on head `e062d03f` (+111/-1, 5 files). Stops caching `ShaderObjectLayout` keyed on a **caller-supplied** `TypeLayoutReflection*`; fixes [slang#10893](https://github.com/shader-slang/slang/issues/10893) (`Dev Reviewed`+`CI Bug`, open since 04-21, **same author assigned** ⇒ author closing their own triaged bug). ⭐**test asserts the INVARIANT (cache size unchanged), not the crash — testing the UAF = testing "ASan happened to stay quiet," which is how it was written off as fixed before.** ⚠️**TWO webhooks 4min apart (`opened` `5d00ad51` → `synchronize` `e062d03f`) collapsed into ONE dispatch because I resolved `head.sha` before dispatching** — `compare` → ahead_by 1, `+1/-1` (em dash → ASCII, CR's ask). At dispatch: `pre-commit` ✅on new head (❌on old — `check-ascii-source`), **18 builds pending**, `mergeable_state=blocked`, **ZERO human reviews** (only CR `COMMENTED` on the **superseded** sha), 1 CR 🟡 open (`REQUIRE_CALL` checks the result code only — require non-null out-param). CLA ✅, author id `161336110` ⇒ not the two-bot-identity case. 🔴**4th webhook 08-05 14:04Z: head `2a3524d8` (head moved on EVERY webhook — 4 for 4). R2 DISPATCHED; ⛔reject verdicts keyed to `e062d03f` or `4c020aeb`.** Delta = a **RE-ARCHITECTURE**: the `typeLayout`-taking `getShaderObjectLayout` overload is **DELETED** (cache now structurally can't take a caller key, vs R1's one call site declining); debug unwrap MOVED from test into `testing::getShaderObjectLayoutCacheSize`. ✅Negative control survived (`CHECK_EQ` @:59, verified at head). 🔴**Author declared WIP + REMOVED both reviewers** (`5192349256` @13:28:53Z, 45s BEFORE the abstain; push came 13min AFTER ⇒ push does NOT revoke it) ⇒ **is a self-declared-WIP reviewer-less non-draft PR decidable at all? Policy call left to approver; a re-run to the same `NO_REVIEW_SIGNAL` = right outcome, WRONG REASON.** ✅**CR's rate-limit expiry FIRED — it re-ran on head, "No actionable comments", BUT as an EDIT to comment `5191828253`, NOT a review row** (`reviews` still 1 row @`5d00ad51`, 3 shas stale) ⇒ ⭐⭐⭐**two instruments disagree; check the rate-limit marker's ABSENCE, never the `📥 Commits` header's presence** (last round that header lied the other way). ⏱️**14/20 checks PENDING incl. ALL 4 wrapper-bearing Debug legs — R1's 3-of-4 passes DO NOT transfer, device.cpp+test both changed.** Prior R1: 3rd webhook head `4c020aeb`, and the sha I dispatched (`e062d03f`) was BROKEN; correction sent, re-pinned. Delta `+14/-2`: test is handed a `debug::DebugDevice`, so `checked_cast<Device*>` in `getShaderObjectLayoutCacheSize` aborted; fix unwraps via `baseObject.get()` (idiom real at `test-cmd-upload-buffer.cpp:15-21`; `common.h:45-58` confirms `#else static_cast`). **ALL 4 Debug jobs that executed the test FAILED (`SIGABRT`, "Invalid type cast") — real, correctly diagnosed, wrapper IS present in Debug.** 🔴**BUT MY "5 Release PASSes are FALSE COVERAGE" IS RETRACTED — WRONG.** I argued `checked_cast`→`static_cast` made them vacuous; that needs the device WRAPPED in Release and it is not: `testing.cpp:790-795` sets `enableValidation` **inside `#if SLANG_RHI_DEBUG`** ⇒ Release is unwrapped, cast legitimate, **those passes were real coverage.** ⭐⭐⭐**I reasoned from a config-conditional mechanism WITHOUT READING THE CONFIG — the `#if` was 4 lines above a line I cited.** ⭐⭐**Naming my premise as unverified + refusing to inherit the approver's `testing.cpp:794` read is what caught it — THEIR read was wrong the SAME way** ("unconditional"); inheriting either would have propped one false fact into "confirmed twice." ⛔**Also my PREDICATE was defective: `unitTestStep=0` ≠ discriminator — all 9 non-executing legs HAVE the step with `conclusion=skipped`; a skipped step emits no log lines, so a log-derived census can't tell absent from skipped. Counts happened to match ⇒ an accidentally-correct tally validates NOTHING.** Read `.steps[].conclusion` from `actions/jobs/<id>`. ⚠️**R1 rollup LIES: while `ci` is queued, `statusCheckRollup` shows 16 SUCCESS INHERITED FROM THE OLD SHA** — per-job only, pre-flight `fetched==total_count`. ✅2 of 4 Debug legs PASSED on `4c020aeb` (macos-clang, win-msvc). **RESUME:** approver verdict on `4c020aeb` ⇒ I own onward hop (⛔reject one keyed to `e062d03f`) / 2 Debug legs outstanding (linux-clang, win-clang) / human review (`bmillsnv`,`skallweitnv` suggested) — scan **both** `reviews` and `issues/comments` / another `synchronize` ⇒ resolve `head.sha` first (moved on **every** webhook, 3 for 3).
- 🔵**[#12349 Vulkan PB dropped from pipeline layout](project_slang_12349_vulkan_pb_pushconstant_pipeline_layout.md)** — **LIVE, OUR STEP PENDING** (unlike the spilled rows below, which are all gated on a named human). Root cause in **slang-rhi**, not the compiler: a **PushConstant-only** reflected set mints a phantom `DescriptorSetInfo` with an EMPTY `VkDescriptorSetLayout`, and **insertion-order** set indexing pushes the `ParameterBlock`'s real set to `pSetLayouts[1]` while SPIR-V says set 0. Triage ✅closed (cmt `5185094751`, edited in place ×2 — ⭐**`created_at`≠`updated_at` is the proof, NOT count-of-1**); `slang-fixer` 🔵active on an **L40S**. ⚠️**NOBODY HAS OBSERVED THE VUID — mechanism=source analysis, runtime=PREDICTION** corroborated only by the reporter. A-vs-B **unsettled**: a maintainer argued for ~B on [slang#10959](https://github.com/shader-slang/slang/issues/10959#issuecomment-4334019960) (same mismatch, different trigger), but rhi#676's **raygen-only carve-out** is precedent for A. ⛔**my "#8958 resurfacing" framing was WRONG** — same class, distinct mechanism. **RESUME:** fixer reports (I own the onward hop) / fixer's #676-or-`:406` read **diverges** ⇒ back to triage (⚠️**I told the fixer triage's answer, so agreement confirms FRAMING not the diff — only divergence informs**) / rhi fix lands ⇒ triage refreshes `5185094751` / fresh human comment. ⚠️**CLA "gate" — [TWO identities named `nv-slang-bot`](feedback_two_nv_slang_bot_identities_cla_gate.md): commits by User `286953280` sit `license/cla=pending`; App `274397474` passes. Check EVERY commit's `author.id`, not `[0]`. 🔴NOT a merge block — `enforcement_level=non_admins` (read it: `branches/{default}` `.protection…`), and rhi#808 MERGED with CLA pending. Fix = re-author the commits (`--reset-author` with the App identity); ⛔never suggest merging past a compliance check.**

- 🔵**[#9661 CUDA `GetDimensions` — assignee-abandonment SCRUB](project_9661_cuda_getdimensions_scrub.md)** — 08-05T18:40Z `jkiviluoto-nv` mentioned the bot: assignee `mkeshavaNV` (self-assigned 01-27) "won't be returning for a while" ⇒ scrub / reassign / close. **Dispatched to `slang-triager`** on `gh-issue-shader-slang/slang-9661`. ⛔**DO NOT TRIAGE THE BODY — body and thread DISAGREE and the body is stale.** Body asks for `txq`-based mip-count/array-size/per-`mipLevel` overloads; the AUTHOR's own comment 4h later says CUDA `txq` supports only `width/height/depth` and `txq.num_mipmap_levels` **doesn't compile** in a CUDA kernel (works in OptiX) ⇒ **body's primary ask is platform-blocked**. Author (01-22): *"won't be supported in CUDA for the time being… I don't plan to work on this"* + replacement ask = **remove the unsupported overloads instead of silently returning `0`**, or **warn** on a zero-returning overload. ⭐**Both naive reads fail: body-only ⇒ "relevant, needs a CUDA expert" (wrong size AND wrong owner); last-comment-only ⇒ "close" (wrong — silent-`0` is still a live defect).** Verdict hinges on TWO things to MEASURE at master, not assume: does CUDA emit still return `0` for unsupported fields, and do the docs still claim CUDA has no `GetDimensions` at all? Both true in January; either may have been fixed since. Labels `GoodFirstBug`/`Dev Opened`/`RTR`. ✅**SCRUB DONE 19:28Z — verdict `still relevant, RESCOPE + NEW OWNER` posted (cmt `5196363753`, verified fresh bot comment, `created_at==updated_at`, 6→7 ⇒ maintainer IS notified). OUR STEP COMPLETE; ball with a maintainer on a design call (A drop `cuda` from `[require]` / B warn / C reject only the `mipLevel` overload / D docs fix — D is free-standing + safe alone).** ⭐⭐**I verified all 4 load-bearing claims at `b0e43d657` and the framing INVERTED: the per-`mipLevel` overload the body asks to ADD already EXISTS and silently discards `mipLevel` ⇒ exit 0, mip-0 dims. A plausible-but-wrong value hides better than a sentinel `0`.** ⛔⭐⭐⭐**BUT THE CONCLUSION WAS RIGHT WHILE 4 MECHANISMS WERE WRONG (1 mine, 3 theirs) — [[feedback_a_shared_conclusion_stops_the_mechanism_audit]]: AGREEING ON THE VERDICT IS WHAT STOPS THE MECHANISM AUDIT, each side reading the other's explanation as corroboration. REAL mechanism: `paramCount` advances PAST the mipLevel slot (`:268-271`) so mipLevel owns `$1`; Metal escapes only via a SEPARATE CURSOR `metalMipLevel` (8 uses) vs `cudaMipLevel` = 0 ⇒ cuda can never NAME the slot ⇒ ⭐⭐a one-line asm tweak CANNOT fix it, it needs Metal's cursor.** ⛔**Instrument that produced 3 of the 4: a PER-LINE grep over MULTI-LINE C++ `<<` statements returns VOID cells — `$0` sits on continuation lines `:285`/`:303`/`:329`. ONE statement-aware census settled 3 contested cells at once (cuda `$1`=0, wgsl `$1`=3).** ✅Nothing false reached GitHub — triager swept the live comment (6 terms → 0, control `mipLevel`=1); no edit, no churn. ⛔⭐⭐⭐**MY instrument lied first: `git log -S` dated the doc line to 2026-08-03 (nearly published as a refutation of the triager) because `/workspace/agent/slang` is a SHALLOW clone (11 commits) ⇒ `-S` returns the GRAFT BOUNDARY as a false ORIGIN — worse than a false zero. Their 2020-03-21 was RIGHT (`05c9a5c9d`; `315888efd` lacks it).** ⇒ [[technique_shallow_clone_git_log_S_returns_graft_boundary]] — check `--is-shallow-repository` before ANY history-origin claim; bisect via `gh api contents?ref=`. ⚠️`GoodFirstBug` now arguably wrong (breaking-change judgment); labels left untouched, flagged only. ⚠️Unfiled CUDA-only ICE spun out: any `Texture2DMS` at `-target cuda` asserts EVEN WHEN UNREFERENCED (`slang-emit-cuda.cpp:246-249` `SLANG_FAIL` → `slang-emit-cpp.cpp:133`) — triager has the repro, offered to file, nobody asked yet. **RESUME:** maintainer picks A/B/C/D ⇒ fixer (D needs no design call) / `jkiviluoto-nv` replies ⇒ act, don't re-scrub / someone wants the MS ICE filed.

## Rows relocated from MEMORY.md (2026-08-04, 3rd spill)

- [rhi#803 CPU ray query](project_slang_rhi_803_cpu_ray_query.md) — ABSTAIN_POLICY size-cap (**3,391** LOC >2000) ⇒ HELD. ⭐**re-test the cap on the NEW TOTAL, not the delta.** 🔴**skallweitNV REJECTED the submodule, wants FetchContent** — only in `issues/803/comments` ([endpoint-split](feedback_inbound_scan_must_cover_issue_comments_not_just_reviews.md)). **RESUME v3 in child** (v1 never fired, v2 always fires — same 3-part fix as #12110; discriminator = **ADDRESSEE**, the 16:50Z blocker opens `@WeakKnight`): **a reviewer APPROVES #12282 → it merges** / APPROVES #803 / FetchContent lands / <2000. ⏱️CI settled green 08-04 ⇒ **timestamp or re-probe every observation.**
- 🔴**Apple6/residency polarity** (merged rhi#800/#801): CI runs the **FALLBACK**; ❌never cite `SLANG_RHI_METAL_NO_RESIDENCY_SET` — `/workspace/shared/CANONICAL-ENV-FACTS.md`
- [#10842 Metal DescHandle / rhi#802](project_10842_metal_descriptorhandle_runtime.md) — NOT approved-to-merge; #12096 HOLD, jkwak owns. All 3 hypotheses incl. mine WRONG; ZERO executed bindless coverage. RESUME=maintainer. ⭐ask if a PR's code is even IN the artifact
- ⭐**[#9636 `ref` accessor ⇒ invalid HLSL+SPIRV](project_9636_ref_accessor_invalid_code.md)** — 08-04: **policy-gated (zangold-nv), no fixer, ✅ARTIFACT POSTED `5179137910`**. ⛔**"P3" RETRACTED — repo has NO priority labels (82 enumerated, 0 match); jkwak-work said it in a COMMENT on SIBLING #10174, never applied, never on #9636 ⇒ read priority from labels/issueType, never a sibling's prose.** ⛔**my `[mutating]` hypothesis REFUTED — TWO defects: (i) `Ptr<T>`+SPIRV call/callee mismatch, common to ALL variants ⇒ NOT the `this` mode; (ii) by-value-`this` real but MASKED.** ⭐⭐**a nightly test PASSES OVER A BROKEN ARTIFACT** (hlsl=0, dxil/spirv=255) — **08-04 same defect from the emit side: `-target hlsl` EXITS 0 with NO diagnostic, emitting `Test_prop_ref_0(obj_0) = ...` (call result not assignable); fails only once DXC runs ⇒ THAT silent path IS the live `Missing Diagnostic`. SPIR-V: callee returns `%int`, call site expects `%_ptr_Function_int` (ref-ness survives at call site, dropped from callee sig). Only `tests/diagnostics/subscript-accessor-reference.slang` mentions `ref()` and it tests NAME LOOKUP only ⇒ nothing in-tree catches either.** ⚠️the obvious `isFromCoreModule` gate BREAKS THE BUILD. ⭐**SIGPIPE from `| head` forged an exit code ⇒ measure exits with NO pipe.** RESUME=maintainer rules on the policy.
- ⭐**[#9736 CUDA atomic collision + external-linkage `__device__` funcs](project_9736_cuda_atomic_conflict_nonstatic_device_funcs.md)** — P2, ✅**MEASURED w/ nvcc 12.6 + POSTED cmt `5176126183`**. ⭐**REFRAME: `-target cuh` ALREADY SHIPS ⇒ UNFINISHED, not missing; TWO shapes ALREADY LINK.** ⛔**"internal linkage NOT SUFFICIENT" RETRACTED 08-05 — the counterfactual's HARNESS was defective** (ONE module duplicated ⇒ both TUs defined the same `computeMain`; entry points are *meant* to stay external — `kIROp_EntryPointDecoration` IS in the `isPublicOrExportedFunc` allowlist). Re-measured on **distinct** entry points: `static` takes dup-defs **2 → 0** ⇒ **REMOVES an objection to (b), doesn't weaken it.** ⭐⭐⭐**a counterfactual is only as good as its HARNESS.** ⛔**3 of my claims WRONG** ⇒ [capability negatives need a SEARCH](feedback_capability_negative_needs_a_search_not_two_guesses.md). Rec **(b)**; ⭐`SLANG_PRELUDE_NAMESPACE` precedent de-risks (a). ⚠️child holds all measurements/ids. 🔄**RE-OPENED 08-05: assignee mkeshavaNV UNAVAILABLE** (jkiviluoto-nv `5195816253`); scrub posted `5197271885`+`5197353299`. **RESUME=jkiviluoto-nv names a NEW OWNER** (kaizhangNV spans both files) **+ (a)/(b)/(c) call** — mkeshavaNV can no longer be the gatekeeper
