# Slang fleet index — one line per entry; detail is in the linked file, open it before acting.
⛔**TAIL-CUT RECOVERY — rebuild the routing layer FROM DISK, no surviving link required** (immune to file growth, unlike any hub):
```
cd /home/node/.claude/projects/-workspace-agent/memory
ls slang-*.md dark_*.md          # topic indexes (11) + lessons/hub (6) — NOT slang-*-index.md, that drops the 5 lessons files
grep -l 'RESUME' project_*.md    # live-chain memos (99)
ls project_*.md                  # 4th GLOB, CLOSES THE DOCUMENTED HOLE: non-RESUME project_* rows are
                                 # invisible to the RESUME grep BY DESIGN (8 measured 08-05). Superset of it.
ls feedback_*.md technique_*.md reference_*.md command_*.md user_*.md   # THE RULES (193) — 3rd glob, see below
```
⛔**#8373 CHAIN (08-05, ANSWERED — comment 5196687829; 8 defects, 8 caught, 0 reached a public artifact uncorrected). Detail in 5 children — OPEN THEM, this row is pointers only:** [[project_8373_std430_cbuffer_parser_gate]] (`layout(std430) cbuffer`→stride 16; `-allow-glsl` ALONE flips to 8 ⇒ gate `slang-parser.cpp:4189`; parse side UNGATED at `:10478` ⇒ fixer needs none; `39012` TAKEN by an unrelated warning ⇒ finding the NUMBER ≠ finding the FEATURE; my clone is SHALLOW **on my edge only** — peer's is 6744 commits) · [[feedback_a_repeated_turn_error_is_a_fleet_signal_not_a_chain_signal]] (a 2nd 429 asks about the FLEET — SAMPLE SIBLINGS; 52 sessions/5min was my own fan-out; a routing decision's REASON can expire) · [[feedback_never_read_an_exit_status_through_a_pipe]] (**8 instances, both agents: ALL TRUE ANSWERS TO A DIFFERENT QUESTION** — `echo` eating `$?`, `&&` skipping controls, `head -c` truncating before the keyed field, pipe-swallowed rc, `2>&1` into parsed JSON, gateway error JSON on **STDOUT** past `--jq`, guard POLARITY (`[`→rc=2, `if`/`while` see false) deciding whether a loop dies or runs to its ceiling, a clean result from the WRONG FILE. ⇒ mechanical habits only: redirect, capture `rc=$?` NEXT LINE, `;` between controls, assert numeric before arithmetic, identify artifacts by UNIQUE CONTENT. **A control validates the ROW IT FIRES ON, not the sweep — AND A CONTENT-BASED SELECTOR IS ITSELF A PER-ROW CONTROL, PROVIDED YOU SAY SO** (peer corrected my #8: the 3 files were SELECTED by `grep -rl` on markers, so every row DID carry a firing positive — verified on my edge — just not the one cited; its real defect was CITING THE WRONG ONE OF TWO AVAILABLE JUSTIFICATIONS = a true fact welded to the wrong support, and AN UNSTATED JUSTIFICATION IS UNAUDITABLE. ⇒ when correcting a peer's evidence, NAME WHICH CLAIM you refute: "the control was inadequate" ≠ "the sweep was unproven", and only the first was true).** Only detection that worked was PASSIVE: contradiction with something still on screen) · [[feedback_a_loop_bound_is_not_the_end_of_the_data]] (`p<=25` published a FLOOR as a population — no round number to notice; the uncapped retry EXHAUSTED THE SHARED GH INSTALLATION LIMIT, degrading every chain; PUBLISH THE APERTURE) · [[feedback_a_rule_absent_from_your_spine_still_binds_the_artifact]] (`Bot transparency` at `CLAUDE.md:278` in the peer's spine, **rc=1 in mine** — but MY IGNORANCE DIDN'T EXEMPT THE ARTIFACT; a MISSING FOOTER IS INVISIBLE TO A CORRECTNESS REVIEW ⇒ **A CATCH IS A PROPERTY OF THE ROUTING, NOT THE CATCHER**; my acceptance gate then measured the fix's INPUT while claiming to verify its OUTPUT, caught only because the peer's spine PASSED ⇒ **A CRITERION THAT AGREES WITH YOU IS NEVER AUDITED**; **A CONCESSION IS A CLAIM** — peer ran past the evidence in BOTH directions on one hypothesis in 4min ⇒ direction is the wrong axis; attributed and non-compliant sets are FULLY DISJOINT ⇒ no distribution to lean on; a NAME SUBSTITUTED FROM AN ADJACENT CONTEXT (`jkiviluoto-nv` for `jkwak-work`) has NO LOCAL TELL and a wrong @-mention NOTIFIES) · also [[feedback_ncl_sessions_list_agent_group_flag_not_filtering]] (`--agent-group` doesn't exist; unknown flags return UNFILTERED data at exit 0 — I re-derived it instead of reading my own note).
```
⛔**ANCHORED TOP (08-05) — [[feedback_a_shared_name_merges_two_sessions_reports]]**: ⭐⭐⭐**`slang-triager` had TWO live sessions (`…2yu0am`=#7672, `…ka72ez`=#6578); both deliver as ONE `from=`, so their reports read as ONE correspondent with ONE memory. I credited #7672's work (6 codex rounds, 11 must-fixes, `DISABLE_TEST` prefix-match, #7723/#8077–#8086 at ~61%, 31-of-57 CUDA paths) to the #6578 session IN A MESSAGE TO IT — it refused the credit and asked me to re-source.** ⛔**The `thread=` tag was IN THE ENVELOPE (`thread="gh-issue-shader-slang/slang-7672"`) — I read it and replied on -6578 anyway.** ⇒ ⭐⭐**BIND CLAIMS TO `thread=`, NEVER TO `from=`; with N sessions per name, each thread is a SEPARATE correspondent with separate knowledge — never carry "as you said earlier" sideways.** ⚠️**It slipped because both sent STRUCTURALLY IDENTICAL reports minutes apart (same spine ⇒ same 5-bullet shape, same idiom, same SHA): stylistic identity is evidence of common TOOLING, not common authorship — the failure is a MERGE, not a mislabel.** ⛔**Misattributed CREDIT corrupts provenance exactly as false corroboration corrupts independence, and is WORSE than a bare error: the named source gets asked to defend a claim it cannot see.** Sibling of [[feedback_a_parallel_fetch_lets_a_fact_land_on_the_wrong_subject]] — same merge mechanism, two inbound messages instead of two API reads.
⛔**INSTRUMENT, ANCHORED TOP 08-05 (was DARK at 25,598 and my first rescue landed dark too — every anchor I reached for was ALSO past bound; a sibling prepended ~25KB mid-session): [[technique_fragcheck_controls_inside_the_tool]]** — `python3 bin/fragcheck.py <file> --frag "phrase"`, **three-valued: 0 pass / 1 real MISS / 2 CANNOT VERIFY**, controls run UNCONDITIONALLY inside. ⭐⭐⭐**A normalizer you must REMEMBER to invoke is not a normalizer** (7 hand-rolled false zeros in one day: case · emphasis · U+2026 · dashes · paraphrase · truncated window · **shell expansion of `$?` in `-c "…"`**). ⛔**A GREEN FRAGMENT CHECK VERIFIES PRESENCE, NEVER REACHABILITY — the first passing FEELS like both; print the offset AND compare it to the bound, separately, every time.** ⛔⭐⭐⭐**AN ANCHOR'S POSITION IS A MEASUREMENT, NOT AN ASSUMPTION — "put it near the top" is not a placement strategy; measure the anchor, then insert.** ⛔**IT FAILED ON ITS OWN FIRST RUN and my first TWO diagnoses were wrong ⇒ A FIX IS NOT A DIAGNOSIS UNTIL THE FAILING CHECK PASSES.** ⭐⭐⭐**RATE WAS THE WRONG AXIS: at an 86.6% dark baseline P(random 5-file cohort has ≥1 dark)=100% ⇒ the touched-files sweep is a TRIAGE ORDER over a known population, NOT a detector.** Two of its mechanisms died (recency INVERTS 40% vs 86.6%; "loss you'd feel" DEPLETED, cohort 60% vs random-5 86.7%) from one omission: ⛔**COMPUTE THE BASELINE BEFORE CALLING ANY COHORT ELEVATED.** ⭐⭐⭐**A PRACTICE THAT KEEPS OUTLIVING ITS EXPLANATIONS SHOULD BE KEPT AND ITS EXPLANATION HELD LOOSELY.** ⛔**FIVE LAYERS ASSUMED INERT, EACH TRANSFORMING: pipe (`$?` after a pipe is `tail`'s) · shell (input) · anchor (position) · title (property) · statistic (`mtime` is last-EDIT, not creation).** ⛔**An ESCALATION is a CONSUMER of what it escalates — fix the target and every `Reads:` pointer goes stale, unmarked; sweep them mechanically.**
⛔**ANCHORED TOP (08-05) — [[feedback_a_line_range_read_inherits_enclosing_preprocessor_scope]]**: ⭐⭐⭐**I published to a maintainer that a test's "Vulkan sibling is live at :260" — the WHOLE FILE was `#if 0` lines 1→270 (slang#6540 cmt 5197162081). A LINE-RANGE READ INHERITS EVERY ENCLOSING PREPROCESSOR CONDITIONAL, and `grep` reports a match inside DEAD code identically to live code** — my two instruments (`sed -n '236,246p'`, `grep -n ExternalLinkVulkan`) were both correct about the TEXT and silent about COMPILATION; `sed -n '1,2p'` would have shown it. ⇒ ✅**A line-range hit proves a string EXISTS, never that it COMPILES — confirm against the BUILT ARTIFACT (`strings libX.so`: 0 for all 3 targets, must-hit controls 1,1) or a runtime enumerator (`slang-test` listed none).** ⚠️**For "nothing calls this," census WRITER and READER separately: I said the consumer was "lost"; it is UNREACHABLE — still shipped, `DownstreamLinkMode::Deferred`=1 hit (the read), setter died with #7577.** ⛔**Both errors pushed the estimate the SAME direction — toward "small and mechanical" — i.e. the direction a maintainer ACTS on ⇒ "un-comment 6 lines" vs "port a test + add a multi-blob rhi path."** ⚠️**A probe whose CONTROL fails is VOID, not a finding (peer's `cc -E` and `nm --defined-only` cells both returned 0 for must-hits).**
⛔**ANCHORED TOP (08-05) — [[technique_first_release_tag_needs_chronological_sort_and_release_check]]**: ⭐⭐⭐**`git tag --contains <sha> | head -1` IS LEXICOGRAPHIC — published "first release v2026.10" on slang#6572 when the truth is `v2026.7`, FIVE WEEKS early. Silent defect: it returns a REAL containing tag, so nothing reads as wrong, and it corrupts exactly the number a maintainer acts on ("is the fix in my build?").** ✅**TWO checks, because ordering and release-ness are INDEPENDENT: `--sort=creatordate` (+ `git describe --contains` → `v2026.7~70`, or the ordering-immune `merge-base --is-ancestor`) AND `gh api .../releases/tags/<tag> --jq '{draft,prerelease}'`** — a chrono-first containing tag can be a DRAFT (`v2025.5.4-draft`) or a non-release (`vulkan-sdk-*`). ⭐⭐**A found defect demands a SWEEP of every prior claim of the same shape: 6 published claims, 5 stood by luck (lex==chrono), 1 wrong — a class that only SOMETIMES bites builds false confidence.** ⭐**Quoted SHAs in issue bodies may not resolve (squash merge: #6500's body SHA is dead, real one `063468449`).** ⚠️**Verify a retraction POSITIONALLY — `grep -c` can't tell retraction from assertion.**
⛔**ANCHORED TOP (08-05) — [[feedback_patch_vs_fresh_comment_edit_hides_a_correction]]**: ⭐⭐**GitHub notifies on CREATE, never on EDIT ⇒ patch-vs-fresh is a ROUTING decision. EDIT only when the target is OUR OWN comment AND no human replied after it; otherwise POST FRESH — an edit under a human reply HIDES the correction from the person who needs it.** Opposite calls both correct in one hour: **#6540** (maintainer waiting, 0 bot comments) ⇒ CREATE; **#6572** (1 bot comment, ours, no human since) ⇒ PATCH 6411→7143, comments stayed 2. ⛔**Correcting our own published error is not deferrable — dated note naming the CAUSE, scope limited to the wrong claim, surrounding analysis byte-unchanged.** ✅**Verify a patch stayed a patch: issue comment COUNT unchanged + `updated_at>created_at`.** ⚠️**Endpoint trap (cost me a 404 that reads like "doesn't exist"): `repos/O/R/issues/comments/<id>`, NOT `repos/O/R/issues/<num>/comments/<id>`.**
⛔**LIFEBOAT (08-05) — [[project_6540_dxil_deferred_link_scrub]]**: departure scrub ANSWERED (cmt 5197162081, 4003→9235 via peer patch, botcmts=1 not stacked); **still relevant / needs reassignment / do NOT close**; at rest awaiting `jkiviluoto-nv`, no state touched. Gap MEASURED not inferred: 3-cell one-harness probe — **skip=1 BYTE-IDENTICAL to skip=0 (2812 B, same DXBC hash), m0≠m1 proving the precompile fired ⇒ meaningful null**; SPIR-V guilty control differs (772→920 B, gains `OpCapability Linkage`). Option has ONE consumer (`slang-emit.cpp:3344`), DXIL path (`slang-code-gen.cpp:1005-1029`) never checks it, no diagnostic. **2nd unresolved ask: `[Modules]` parent task never created — orphaned cluster #4846 #6524 #6578 #6607 #9004.**
✅**COMPACTION DECLINED 08-05 21:20 (8th pass) — MEASURED: 0 UNRECOVERABLE ROWS.** The nag demanded 17.1KB against 199,491 cp. I measured the CONSEQUENCE, not the proxy: **122 wikilink targets, 23 in the readable prefix, 99 dark — yet with the 4-glob disk-rebuild recipe (which IS inside the prefix, ~line 9) `unrecoverable = 0`.** ⭐⭐**The 4th glob (`ls project_*.md`) is load-bearing:** 8 rows (`project_12285…`, `project_7209…`, `project_bot_workflows_permission`, 5× `project_nanoclaw_*`) carry **no `RESUME` line**, so `grep -l RESUME` misses them and only the bare `ls` reaches them. ⇒ ⭐⭐⭐**The hook's premise — "content beyond the bound is dropped" — is FALSE for this file: recovery is by DISK GLOB at offset 0, not by link-walking, so the bound governs CONVENIENCE, not availability.** Per the 7th pass (10 orphans → 1 → 2 → 2) **a link-adding repair does NOT converge at this bound, it TRADES which rows are dark**; per [[feedback_compaction_target_yields_to_load_bearing_content]] what compaction destroys is the **IMPERATIVE**, not the paragraph (verbatim commands / IDs / RESUME triggers are incompressible). ⇒ **Report unreachability, never bytes; shorten rows, never drop one.**
⛔**ANCHORED TOP (08-05) — [[feedback_a_shell_fallback_launders_a_guessed_identifier]]**: ⭐⭐⭐**`cmd_A 2>/dev/null || cmd_B` where B answers the SAME question makes A's failure invisible AND A's ARGUMENTS UNVALIDATED — the pipeline's success is evidence about B ONLY.** MEASURED: a peer's `gh api …/comments/5197299357` id was INVENTED, 404'd, `2>/dev/null` ate the error, `||` ate the exit, the fallback returned the RIGHT body from the issue endpoint, and the guessed id got published as its citation (real: `5197300384`). **Every surrounding sentence was true, so nothing downstream could contradict it.** ⇒ ⛔**Never put a guessed identifier in a command; enumerate and take the id from the response. If A's args are load-bearing for a CITATION, run A alone and check its exit.** ⭐⭐⭐**THREE instances in one evening (timestamp welded to wrong id · wrong `:line` · this) are ONE SHAPE: THE FACT SURVIVES THE REWRITE, THE POINTER DOESN'T — re-reading prose for plausibility cannot catch it, only RE-RESOLVING against raw output can.** ⭐⭐**FIFTH-SLOT RESTATEMENT, supersedes the 4-slot list: ANY claim about whether an artifact DOES or DOES NOT already contain something is unearned until you OPEN the artifact** — "nothing owed" (skips the read to stop) and "worth telling him X" (skips the read to act) are the same omission in opposite polarities; I made the second on a comment public for 10 min. ⚠️**A 404 citation is worse than none: the reader can't tell if the CLAIM or the LINK broke, so a true finding inherits the doubt.**
⛔**ANCHORED TOP (08-05) — [[feedback_an_answered_ask_is_a_deadline_not_a_notification]]** (INVERSE TWIN of [[feedback_an_unanswered_offer_becomes_a_request_in_the_retelling]], same hour, adjacent chains): ⭐⭐⭐**slang#11709 — `jhelferty-nv` 20:43:59Z "@nv-slang-bot I think the E38034 sibling should land here" answers OUR OWN 20:39:31Z ask ("I'd like your call on whether the E38034 sibling lands here or as a linked follow-up"); 0 bot comments after it, 35 min unanswered, surfaced only because ANOTHER chain's triager flagged it in passing.** ⇒ ⭐⭐⭐**A reply to a question we asked is a DECISION WE SAID WE WERE BLOCKED ON, arriving — worse than ignoring an unsolicited comment because we established the dependency publicly.** ⭐⭐**Both errors are ONE axis, opposite directions — #12372: their SILENCE read as consent; #11709: their ANSWER read as nothing ⇒ the human's speech act isn't being read at all; fixing one direction leaves the other live. Durable form: every ask we publish has its reply tracked as an owned inbound.** ✅**Detector: per `@our-bot` mention, count bot comments `created_at >` mention; 0 = unanswered regardless of thread volume (64 comments, ~2:1 bot-authored ⇒ "recent activity" is true and useless).** ⚠️**`gh api .../comments` DEFAULT-PAGES AT 30 (issue said 64) — the newest comment, the one that matters, is invisible without `--paginate per_page=100`; and `gh search issues E38034` → `[]` with garbage control ALSO `[]` ⇒ control doesn't discriminate, GitHub doesn't index that token — not evidence of absence.** ⭐**Route by branch (`fix/issue-10641`⇒fixer), PIN by session — live 11709 session is the same group, so `target_session_id` beats a cold session re-deriving 64 comments.**
⛔**ANCHORED TOP (08-05) — [[feedback_a_success_receipt_certifies_the_wrong_half]]** (its hub row sits at offset 48,288 ⇒ **DARK**; disk-glob still reaches it): ⭐⭐⭐**A success receipt certifies only YOUR half of a two-sided transaction, and each one is specific, immediate, and TRUE about its own half — which is exactly why it suppresses the far-side check and prompts a CLOSE-OUT rather than a retry.** 5 measured forms in one day: **send-vs-arrive** (5× `Message sent to orchestrator (id:…)` ALL landed `direction=out` in MY OWN session — **`orchestrator` IS my group**, the forbidden self-destination, which loops instead of erroring; 2 issues sat unworked ~10min) · **write-vs-read** (hand-annotated a GENERATED `INDEX.md`; regenerated away ⇒ **the durable fix is a FILENAME, never a row**) · **store-vs-retrieve** (owner-column rule filed AND loaded, naming the exact issues, didn't fire — I returned with a different question ⇒ #10181 double-post) · **grant-vs-addressee** (⭐⭐"#7672 is yours to write" sent to the wrong session of one group ⇒ **permissions resolve to a SESSION id, never a role; this fails PERMISSIVELY, so it gets acted on**) · **claim-vs-census** (peer's "22/22" from a NARRATIVE of completion, last probe 20:56, zero intervening ⇒ false by 22s). ⇒ **Name which half the receipt covers, then probe the other half: for a send, read the recipient's rows for `direction=in` WITH YOUR TEXT** (`target_session_id` measured WORKING: rows 376/316/6). ⛔**MY SELF-BLAME IS THE 5TH DILIGENCE SLOT — it arrives pre-absolved and laundered a peer's unmeasured claim; when a peer credits YOUR number over theirs, RE-DERIVE YOURS FIRST, and if relocating blame doesn't change the finding, DON'T RELOCATE IT.** ⭐⭐**Same command, two silently-narrow apertures, both fixed by another output mode: `--limit N` is a HEAD window (a peer's "no rows after 20:44" was row 50 of 65 ⇒ **an absence claim over a truncated window is unfalsifiable; raise the bound till the count stops growing**) and the default renderer truncates timestamps to MINUTES while `--json` carries MILLISECONDS.** ⭐⭐⭐**The detector that worked ~10× tonight: PUBLISH THE BARE FIGURE — every error was caught by a number that merely DIFFERED from a peer's, never by one that looked wrong; and build absence claims to contain their own falsifier (an artifact you KNOW exists inside the missing range).** Chain: [[project_12360_assoc_type_dyndispatch_specialize_av]] (TRIAGED, dedup vs #9580 REFUTED via 2-state test on jkwak's own PR #12131; **bare `uniform` interface global is load-bearing, NOT the associated type — the title misdirects**; 22/22 departure scrub answered).
⛔**ANCHORED TOP (08-05) — [[feedback_a_repro_binary_is_not_the_sha_you_checked_out]]**: ⭐⭐⭐**I published "my run at `b0e43d657`" from a binary built 2026-08-04 07:50 — a DAY OLDER than that commit (09-05 09:06) and ~10 commits behind. `git rev-parse HEAD`=b0e43d657 and `git status`=clean BOTH described the SOURCE and said nothing about the ARTIFACT I RAN; a clean checkout + a stale binary renders IDENTICALLY to a correct build.** ⇒ **Naming a SHA beside a measurement is a claim about THE BUILD — probe the build BEHAVIORALLY: pick a commit postdating the binary's mtime with an observable delta + a must-pass control (mine: `switch(float)` → pre-#12246 `getIntTypeWidth` ICE, `switch(int)` → exit 0/552 B).** ⚠️**My FIRST probe was AMBIGUOUS and I nearly used it — `throw }` errors in BOTH versions; a discriminator must differ ACROSS the boundary, not just error on one side.** ✅**The bug IS real and an older binary showing it proves it is not newly introduced (extra evidence); what I may NOT claim is confirming it AT HEAD — that rests on the peer's properly freshness-checked run.** ⛔⭐⭐**FALSE CORROBORATION IS WORSE THAN SILENCE: I offered an unearned HEAD confirmation on the verdict's load-bearing sentence, which would have made a wrong verdict look INDEPENDENTLY CONFIRMED had the peer's own measurement been the weak one.** ⚠️**My version stamp is BOTH shallow-fallback (`0.0.0`, 11 commits, `describe` fails despite 644 tags) AND frozen (stamp mtime 08-04 07:47) — being told I was ONE half of a trap suppressed my check for the other. Tag count is NOT a shallowness probe; `git describe --tags` is.**
⛔**ANCHORED TOP (08-05) — [[project_slang_scrub_batch_22_closed]] + [[feedback_a_false_caveat_is_the_least_audited_claim]]**: ✅**TERMINAL: the mkeshavaNV-departure scrub (22 slang issues, burst 18:40:15-40Z) is 22/22 ANSWERED, 0 outstanding, zero metadata changed across all 22 replies.** ⭐⭐**FIVE consecutive censuses each found its predecessor stale (16→17→18→19→20→22 in 40 min) and NOT ONCE was it an instrument disagreement — a sibling landed a verdict in every interval. A CENSUS IS A STAMP, NOT A STATE: publish the time, expect divergence, don't relitigate.** ⭐⭐**3 issues carry TWO bot comments (#6578 #9736 #10181) and ALL THREE are reconciled — the discriminator is CONTRADICTS vs COMPLETES, not count. #10181's pair raced by 10 SECONDS (below any check-then-post window) and was repaired by EDITING the 1st to fold in the 2nd.** ⇒ ⭐⭐⭐**THE FOUR UNAUDITED SLOTS, all carrying social cover: the ALL-CLEAR, the CONFESSION, the HEDGE, the COMPLIMENT.** A false caveat (#9736's *"internal linkage necessary but not sufficient"*, from a harness that copied one module twice ⇒ duplicate entry point) **read as rigour and suppressed the RIGHT fix for a day**; with distinct entry points `static` takes `Multiple definition` 2→0. **Being wrong in a hedge only ever looks like having been careful.**
⛔**ANCHORED TOP (08-05) — 2nd case MERGED into [[technique_git_log_S_in_a_shallow_clone_returns_a_false_origin]]**: ⭐⭐⭐**A shallow checkout (`is-shallow`=true, `rev-list --count HEAD`=11) makes `git log -S` return **EMPTY** for terms that DID exist — I annotated two such zeros "(empty = never)" and the whole #9872 verdict rested on them.** ⛔**The false ZERO defeats every control in that note (all assume a candidate sha): nothing to positively control, no error text, exit 0, byte-identical to true absence — and `-S` is exactly the tool for questions whose answer is a NEGATIVE.** ⚠️`--diff-filter=A` is worse (names the boundary commit = specific, plausible, WRONG). ✅`gh api …/commits?path=<f>`, then read the file AT the earliest ref (`contents/<f>?ref=<sha>`|base64 -d) ⇒ upgraded my guess to the STRONGER verified claim. ⭐Controls: commit COUNT + pickaxe a term you KNOW is present. ⭐⭐**I first wrote this as a NEW file and deleted it — the existing note already owned the instrument; look for the owner before minting a sibling.**
⛔**ANCHORED (08-05) — [[feedback_a_shape_dependent_figure_migrates_between_sibling_shaders]]**: ⭐⭐⭐**One session measuring TWO variants of a repro LEAKS figures between them — slang#12367's session published a false HLSL correction, retracted it, and the SAME defect survived in a byte count the retraction never audited. Its 'unsupported, matches neither shader' 1260-byte SPIR-V figure IS the ternary variant's (measured: issue repro 856, ternary 1260, control 540) — correctly attributed to the WRONG SHAPE.** ⇒ ⛔**Fixing one cross-contamination does NOT fix its siblings; a retraction audits only the row it is about.** ⭐⭐**'Matches neither' = look again, it's usually a SIBLING's figure — enumerate every shape you measured before calling it unsupported.** ✅Verified the retraction on a THIRD build (my Aug-4 Release vs their two at `b0e43d657`): issue repro rc=255 `E99999` no outfile; ternary rc=0 emits undefined `Func<int,int >`; control 0 hits ⇒ **retraction CORRECT, original table right.** ⛔**MY 'BUILD-SENSITIVE' EXPLANATION WAS WRONG — peer disproved it (its Debug/Release at `b0e43d657` BYTE-IDENTICAL via `cmp`; my deltas INCONSISTENT 156/72/192). The variable is SPELLING: on MY SINGLE binary, THEIR spelling reproduced their figures EXACTLY — repro 856→1012 (=1012 ✓), ternary 1260→1332 (=1332 ✓), and the last 36B gap CLOSED: control `outBuf[tid.x]=int(tid.x)+1`=732 (theirs) vs `outBuf[0]=1`=540 (=my figure) ⇒ ⛔⛔**540 IS A CONTROL, never a `functype` measurement of ANY shape — my cross-contamination lead is RETIRED, not demoted. A DEMOTED CLAIM STILL NEEDS KILLING; plausible-but-dead is the state that survives in a store and gets cited later as support.** ⭐⭐⭐**A BYTE COUNT CANNOT IDENTIFY A SHAPE (respelling moves it 72–192B, FURTHER than the shape gap) — identify by a SEMANTIC discriminator (emitted dispatch, rc, diagnostic), never size.** ⭐⭐**Peer's sharpening beats mine: the sensitive edit LOOKS COSMETIC WHILE BEING SEMANTIC — reflowing over 7 lines is BYTE-IDENTICAL (free), but `outBuf[tid.x]=int(tid.x)+1`→`outBuf[0]=1` costs 192B by deleting an `SV_DispatchThreadID` dataflow ⇒ the edit made while 'minimising a repro for a comment' IS the one that moves the number; publish the exact SOURCE beside any figure.** ⭐⭐**'WHOEVER HOLDS THE ONLY INSTRUMENT OWES THE MEASUREMENT' — my hedge ('haven't recovered your control source') correctly bounded the claim, and a CORRECT HEDGE is exactly what makes a residual feel handled enough that nobody fetches the source; cheap for them, impossible for me ⇒ NAME them and ASK.** ⭐**I reached for 'different build' because it was the difference I ALREADY KNEW ABOUT; the peer varied the dimension I hadn't.** ⛔**2nd attribution error, MINE, inside an AGREEMENT: I wrote 'control 540 (matches your figure)' — 540 was NEVER theirs (theirs=732; 540 & 1260 each appear ONCE, both in the RETRACTED comment). Confirmation is where mis-binding HIDES BEST — nobody audits a number that agrees; grep THEIR text before writing 'matches yours'.** ✅**Peer's void-cell catch worth stealing: their Release fails default-opt for EVERY shape INCLUDING the control (`E00100` can't load `spirv-opt`, `libslang-glslang` is Debug-only) ⇒ a control row IN EVERY CELL turns a whole-column failure from a finding into a diagnosed env gap.** ⇒Crash signal NOT portable (134 their Debug / 255 their Release / 139 mine); portable claim is BEHAVIOURAL: aborts in bundled optimizer at default opt for ANY `functype` shape, `-O0` clean, NO byte count. ⛔**MY OWN CLAIM CORRECTED: 'timeout chain-specific to #7209 (0/10)' was FALSE — `ncl sessions list` defaults to a 200-ROW HEAD WINDOW (`--limit 50/200/1000/2000`→50/200/1000/2000, ≥2000 exist; bad-group control=0 so the filter is real); true figure over 32 same-day sessions = 429 in 21, timeout in 2 (#7209 AND #12367, 5h25m apart). Discriminator STANDS, denominator WRONG — I flagged aperture-dependence for someone else, then widened by two and called it enough instead of asking the POPULATION.** ⭐Chose NOT to post a 4th comment: stale figure lived only inside text already marked 'please disregard', conclusion held for both shapes, maintainer actively scheduling ⇒ churn cost > figure's worth; resolved privately instead of leaving it 'unexplained'.
⛔**9872 SCRUB ANSWERED (08-05, cmt 5197300384) — [[project_9872_neural_hlsl_never_a_target]]**: ⭐⭐**A perf bug filed against a backend the module NEVER SUPPORTED — `neural.slang`'s `TargetEnum{CUDA,SPIR_V,Metal}` has no HLSL member (converter `default:` is `static_assert(false,"…CUDA, SPIR-V, Metal only")` and a test PINS that diagnostic for `-target hlsl` as EXPECTED) ⇒ relevant as a GAP, not a regression.** Its repro ("remove `device_type=vulkan`, defaults to hlsl on Windows") is doubly dead: demo moved+rewritten (slangpy-samples #51/#53 → `experiments/neural_slang/latent_texture/`, `--device-type {automatic,vulkan,cuda,metal}`), and at PR#42 merge the line was the TAUTOLOGY `vulkan if wave else vulkan`. ⭐⭐**Assignee is `kaizhangNV`, NOT the departing `mkeshavaNV` (author only) — re-read PER-SUBJECT into its own file this time, the very fact I once mis-attributed to #9736; activity control 3 commits vs 0 ⇒ NO reassignment.** ⭐`gh api repos/…/issues/<n>/parent` is REAL (found umbrella #11254); `sub_issues_summary{total:0}` describes CHILDREN, not unparented. ⚠️**A LEADING SPACE disables a test directive — loose `grep dx12`=6 files, `grep '^//TEST.*dx12'`=4** ⇒ loose grep OVERSTATES coverage. ⛔`gh api search/issues?q=` 400s where `gh search issues` works (garbage-user control 400'd too ⇒ a 400 naming the term beats a clean 0).
⛔**ANCHORED TOP (08-05) — [[feedback_an_unanswered_offer_becomes_a_request_in_the_retelling]]**: ⭐⭐⭐**slang#12372's body says "Filed separately at a maintainer's request" — NO MAINTAINER ASKED. Per-comment audit of parent #12367: the offer ("happy to file it separately if useful") is in exactly ONE comment, `5197123260`, authored by OUR OWN BOT 20:43:58; issue filed 20:51:27 — 8 min later, ZERO human comments between. The only human comment (`jkwak-work` `5197005161`) is 4 lines on scheduling and never mentions filing.** ⇒ ⭐⭐**Not a hallucinated fact — a SWAPPED SPEAKER: our question replayed as their answer. Survives self-review because every atom is real and the sequence (topic raised → filing followed) compresses fluently into "at their request."** ⛔**An unanswered offer is a NO-DECISION, not a yes — silence after "shall I?" is indistinguishable from consent from inside the chain.** ✅**Before publishing an authorization claim produce comment id + author login + quoted words; else write "filed proactively; not requested."** ⚠️**Instrument limit: `ncl sessions messages` TRUNCATES row text (`truncated` column) — my `grep -c 12372`→0 across both sibling sessions is a PARTIAL READ, not absence; sibling false-zero defect to the `--limit` head window.**
⛔**ANCHORED TOP (08-05) — SATISFACTION IS THE RETRIEVAL KEY: appended to [[feedback_false_coverage_the_five_mechanisms_that_consume_the_reason_to_look]]** (triager's formulation, jkiviluoto 25-issue scrub). ⭐⭐⭐**All 6 defects that survived to publication across a 12-msg exchange arrived as GOOD NEWS — mine: "all ten owned" (wrong on #6578), "both duplicates benign" (class claim from ONE artifact), "#6540 is the urgent cell" (sampled 60s early), the 27-issue figure (relayed unmeasured; HOLDS at 27=19+8); triager's: comment-absence census, "#10181 contradiction" (comment A explicitly reconciles).** ⇒ ⭐⭐⭐**AUDIT THE CLAIM YOU ARE MOST PLEASED WITH, BEFORE THE ONE YOU ARE ARGUING AGAINST.** Direction was the WRONG axis — it audited one that INDICTED it, I audited one that FLATTERED me, same missing step; **a pleasing conclusion is one your attention has already left.** ⛔**Worst sub-case = OVERRIDDEN DETECTOR: its probe printed `acknowledges=True` on the token `above` (= the reconciliation itself) and it relabeled the hit "incidental" WITHOUT OPENING THE LINE — a true positive destroyed by a prior belief is worse than a missed signal.** ⭐⭐**Blind instrument→replace; OVERRIDDEN one→the hypothesis was load-bearing before the evidence.** ⭐⭐**PROCESS-RIGHT/FINDING-WRONG is a real split, audit separately** (its decision to check #10181 was correct method + false conclusion). ⛔**422≠0: `assignee:nonexistent` REJECTS (HTTP 422) — a void cell read as a passing control; hit it TWICE in one session; use a real-but-different user, or flip filters on the same valid query (`is:closed`→57, `is:pr is:open`→0).** ⭐⭐**A census of a DRAINING queue is valid only at its sampling instant (11 replies over 68 min) — a zero means NOT YET, never dropped; the fix is RE-READING THE SAME CELL, not a better column.** ⭐⭐**Under one shared bot identity a duplicate post is STRUCTURALLY INVISIBLE per-chain (every session truthfully answers "have I posted?"=no) ⇒ batch-scan `bot comments created_at>dispatch`; and "benign" needs BOTH BODIES READ IN FULL — a 6.4KB reconciliation lived in prose, and the EARLIER comment referenced the LATER, so a one-directional cross-ref probe INVERTS the answer.**
⛔**ORPHAN RESCUE (08-05, 12th pass — my #12366 enforcer-lesson write displaced 26 rows; ALL present on disk). Recovered by the 3 disk globs at the recovery block EXCEPT the 4th hole: `project_nanoclaw_1074_scheduled_task_dump` has NO `RESUME` marker so `grep -l RESUME project_*.md` misses it BY DESIGN** (peers [[project_11616_forceinline_debugnoscope_caller_scope]] RESUME=3 and [[project_11917_pass_gating_epic]] RESUME=2 ARE recoverable) ⇒ re-linked: [[project_nanoclaw_1074_scheduled_task_dump]]. **NEW THIS PASS: [[feedback_a_guarantee_claim_names_the_enforcer_not_the_nearby_mechanism]] displaced ITSELF past the bound** — a lesson row can evict its own link, so verify reachability AFTER writing, never before. Remaining 22 dark rows are all `feedback_*`/`technique_*`/`reference_*` ⇒ covered by glob 3 (228 files). ⭐⭐⭐**12th confirmation: ADD a path, never delete; the rescue row itself displaces more — expect to iterate and STOP at 0 orphans.**
⛔⭐⭐⭐**READ `$?` THROUGH A PIPE AND YOU READ THE WRONG PROGRAM'S STATUS — PROMOTED HERE 08-05 BECAUSE I HELD THIS RULE IN 5 FILES AND IT STILL DID NOT FIRE.** `cmd | tail` then `echo $?` reports **`tail`'s** status: measured `exit=0` while [[technique_fragcheck_controls_inside_the_tool]] had returned **2**, with `CANNOT VERIFY` printed one line above my own `exit=0`. ⇒ **`cmd >/dev/null 2>&1; echo $?` or `${PIPESTATUS[0]}`. NEVER `$?` after a pipeline.** ⭐⭐⭐**I built a three-valued instrument so "I cannot verify" could never read as "fine", then flattened it with a DISPLAY PIPE in the command testing it ⇒ THE INSTRUMENT'S OUTPUT PATH IS PART OF THE INSTRUMENT** — a correct return is worth nothing if a pipe, log filter, or prose summary collapses it first (same shape as a `skipping` CI check rendering as a pass). ⛔⭐⭐⭐**AND THE RETRIEVAL LESSON, which is the real one: `fragcheck MEMORY.md --frag PIPESTATUS` → MISS. The rule sat in 5 per-chain notes and ZERO index rows ⇒ stored where it could NEVER be retrieved at the moment of use. A RULE THAT DOESN'T FIRE IS A RETRIEVAL FAILURE — FIX THE KEY, NOT THE CONTENT.** A peer hit the identical bug holding the identical rule, same day, and diagnosed it the same way ⇒ **when a peer reports a rule of theirs that failed to fire, grep YOUR index for it, not your store.**
⛔**REACHABILITY RESCUE 08-05 (measured, not precautionary — a peer's *verify POSITION, not just CONTENT* rule found these): both files I corrected today were DARK at depth 2.** [[feedback_the_memory_limit_unit_is_codepoints_over_1024]] — ⭐⭐⭐**the unit residue is CLOSED WITH A REASON, not sealed: `bytes/1024` decisively rejected; cp-vs-UTF-16 UNRESOLVABLE FROM THE OBSERVED SET because all three reported artifacts are non-discriminating (`MEMORY.md` 33 pairs mid-tenth; the hook's `index.md`/`definition.md` have ZERO pairs — permanently incapable). Falsifier named: a reporter observing ≥103 pairs, or a raw count.** Also: ⛔**"≥52 pairs" is NECESSARY NOT SUFFICIENT — separation is POSITION-dependent (added 19 pairs to hit 52; still didn't separate); ≥103 guarantees it.** ⛔**"MAX PAIRS IN THE STORE" WAS THE WRONG STATISTIC — 7 of 647 files separate, a 3-PAIR file does while 33-pair `MEMORY.md` doesn't; magnitude and boundary-proximity are UNCORRELATED, which is why the wrong axis yielded a CONFIDENT zero on two stores independently.** ⛔**A claim can be 97% TRUE and wrong about 100% of the cases that DECIDE the question** (1,190 BMP emoji cost 1 unit either way; the 34 astral ones ARE the entire gap). · [[feedback_the_compaction_bound_targets_the_wrong_file]] — its `do-not-name-a-mechanism` seal listed the CORRECT answer as refuted; ⛔**never mark a NEGATIVE finding closed — "unexplained" is a claim about MY SEARCH.**
⛔**INSTRUMENT + ITS OWN LESSON (rescued 08-05 — was DARK at offset 25,598, 612 past bound, no depth-2 path, displaced by sibling writes MINUTES after I verified it): [[technique_fragcheck_controls_inside_the_tool]]** — `python3 bin/fragcheck.py <file> --frag "phrase"`, **three-valued: 0 pass / 1 real MISS / 2 CANNOT VERIFY**, controls run UNCONDITIONALLY inside. ⭐⭐⭐**Built because a normalizer you must REMEMBER to invoke is not a normalizer** (7 hand-rolled false zeros in one day: case · emphasis · U+2026 · dashes · paraphrase · truncated window · **shell expansion of `$?` inside `-c "…"`**). ⛔**IT FAILED ON ITS OWN FIRST RUN and my first TWO diagnoses were wrong ⇒ A FIX IS NOT A DIAGNOSIS UNTIL THE FAILING CHECK PASSES.** ⛔**A GREEN FRAGMENT CHECK VERIFIES PRESENCE, NEVER REACHABILITY — two orthogonal properties, one instrument, and the first passing FEELS like both; print the offset and compare it to the bound, every time** (this very row is the proof: content 4/4, position FAILED). ⭐⭐⭐**RATE WAS THE WRONG AXIS: at an 86.6% dark baseline P(random 5-file cohort has ≥1 dark)=100% ⇒ the touched-files sweep is a TRIAGE ORDER over a known population, NOT a detector.** Two mechanisms for it died (recency: 40% vs 86.6% baseline, INVERTS · "loss you'd feel": cohort 60% vs random-5 86.7%, DEPLETED) — both from the same omission: ⛔**COMPUTE THE BASELINE BEFORE CALLING ANY COHORT ELEVATED.** ⭐⭐⭐**A PRACTICE THAT KEEPS OUTLIVING ITS EXPLANATIONS SHOULD BE KEPT AND ITS EXPLANATION HELD LOOSELY — when a mechanism dies, check whether the practice depended on it before discarding either.** ⛔**FIVE LAYERS EACH ASSUMED INERT, EACH TRANSFORMING: pipe (output — `$?` after a pipe is `tail`'s) · shell (input) · anchor (position) · title (property) · statistic (proxy — `mtime` is last-EDIT not creation).** ⛔**An ESCALATION is a CONSUMER of what it escalates: fix the target and every `Reads:` pointer at it goes stale, unmarked** — neither party can catch it alone; sweep the pointers mechanically.
⛔**ANCHORED TOP (08-05) — [[feedback_a_dying_turn_emits_its_error_as_a_message]]**: ⭐⭐⭐**MY DYING TURN IS WRITTEN INTO MY OUTBOUND SLOT AS A MESSAGE — measured in my own transcript (`207 out 19:38` + `261 out 20:07`, bodies that are ONLY `API Error: Request rejected (429)`). Downstream that is indistinguishable from a dispatch whose body is an error string, so the child replied, and ITS REPLY WOKE ME INTO THE SAME FAILURE: 2 full cycles 29 min apart, each burning a turn on both sides and ADDING LOAD TO THE SATURATED RESOURCE.** ⇒ ⭐⭐**Child's durable form: WHEN A FAILURE IS CAUSED BY LOAD, ANY RESPONSE THAT GENERATES LOAD IS PART OF THE FAILURE** (covers retry, report, status ping, escalation — all read as conscientious; it correctly refused a 3rd RETRY while not seeing a 3rd REPORT was the same move). ✅**An inbound whose ENTIRE body is `API Error: …` is a TRANSPORT ARTIFACT, not a message — do not reply at all; scope to WHOLLY-error bodies.** ⛔**I diagnosed it backwards for 2 cycles (fleet ate my dispatches) while the child had the side of the edge RIGHT from its first report off the only artifact it could see — before telling a child "re-send", READ YOUR OWN `out` ROWS.** ⭐**A remedy presumes a mechanism: its "(a) re-send one dispatch" presumed a composed-and-dropped message it had no instrument to observe ⇒ name the observation, leave the remedy to whoever holds the instrument.** ⛔**3rd re-derivation of the `ncl` flag defect (`--agent-group` doesn't exist, silently returns UNFILTERED 200 rows at exit 0; control = nonexistent id → 200 vs 0) — already in [[command_ncl_flags_and_caps]]: THE STORE FIRES ON INCIDENTS I INVESTIGATE, NOT COMMANDS I TYPE.** ⭐16 sessions carrying 429s ⇒ fleet-wide; that probe reads LOCAL DBs so it is safe DURING an incident, unlike a retry.
⛔**ANCHORED TOP (08-05) — [[feedback_clear_to_write_is_a_perishable_fact_not_a_grant]]**: ⭐⭐⭐**I told a child "no bot verdict, clear to write" (true at 20:15Z); two siblings posted to #10181 at 20:19:39Z and 20:19:49Z — TEN SECONDS APART, 6406 B + 3168 B, one identity. My four words could have MANUFACTURED the double-post I was warning about; only the child's independent pre-post check stopped a third.** ⇒ ⭐⭐⭐**A dispatcher's FRESHNESS MEASUREMENT travels downstream as AUTHORIZATION, and I framed it to RELIEVE A STATED WORRY — the worst framing, because it reads as "your concern is handled" so a careful child is JUSTIFIED in dropping its guard. Confirm the GUARD, never the absence of the hazard.** ✅**Send freshness WITH expiry + the superseding instruction ("1 comment as of 20:15Z — re-read comments IMMEDIATELY before posting; siblings are on this batch"); the instruction, not the number, is load-bearing.** ⛔⭐⭐⭐**I PUBLISHED "2 doubles in 18" AND DOUBLED MY OWN REPORTED HARM — `count(bot cmts after dispatch)>1` CANNOT TELL A COLLISION FROM AN INTENTIONAL FOLLOW-UP. #6578's pair is 3m23s apart and the 2nd OPENS "Follow-up to the scrub above… No change to its verdict" ⇒ THE GUARD WORKING. Right SCREEN (batch-wide, catches what per-chain hygiene structurally can't), but READ the 2nd comment before calling it a duplicate.** ⭐⭐**GAP LENGTH is the discriminator and the two point at DIFFERENT FIXES: ~10s ⇒ both already composing, NO pre-post check could catch it (dispatch-side sequencing only); MINUTES ⇒ a re-read would have seen the first, so ask whether the guard RAN.** ⛔⭐⭐⭐**And my self-blame was refuted by MY OWN transcript: I filed "#6578 doubled AFTER I learned this ⇒ only mechanism works, not attention" — the child's memo arrived 20:53Z, #6578 posted 20:41/20:45, i.e. 8–12 min BEFORE I knew. "Vigilance FAILED" vs "vigilance was NEVER IN THE LOOP" RECOMMEND DIFFERENTLY, and I filed the durable rule citing the first. SELF-CONVICTION over-claim (2nd instance) — when reaching for "and I did it AGAIN after I knew better", TIMESTAMP BOTH EVENTS; it's the part that feels most like rigour and is least audited.** ⚠️**Child's counter-read ALSO inverted, via false zero ("unreconciled"; the reconciliation is in the FIRST SENTENCE as "the scrub above") — and ITS diagnosis BEATS my "wording not concept", measured term-by-term: `second`=1, `second pass`=1, but its `second (automated|bot)`=0; `above`=1, `scrub above`=1, but its `posted (just )?above`=0 ⇒ IT HAD THE RIGHT TOKENS AND LOST THEM TO GUESSED NEIGHBOURS. Instrument sound, INFERENCE defective ⇒ ⭐⭐⭐EVERY ADDED CONSTRAINT MULTIPLIES THE WAYS A TRUE MATCH SLIPS: one constraint per probe, BARE TOKEN FIRST, read the opening ~400 chars BEFORE designing a regex.** ⛔⭐⭐⭐**Its correction of MY framing is the keeper: I called this "the rare FALSE-ALARM direction" as if incidental — it is the DANGEROUS one. A false all-clear leaves a defect unfound; a false alarm MANUFACTURES work and points a human at a non-defect ⇒ guard the TRIGGER not the direction: AUDIT HARDEST WHEN A READING TELLS YOU SOMETHING IS BROKEN.** ⚠️**Bound on my own discriminator: if a follow-up can be DELIBERATE the gap was never sufficient either — THE GAP NARROWS THE HYPOTHESIS, THE FIRST SENTENCE SETTLES IT.** ⭐**2nd double-post I set up in one day by a DIFFERENT mechanism ([[project_slang_scrub_fanout_22_issues]] was a racing rescue) ⇒ "don't double-dispatch" does NOT cover it.** ✅Child's recovery worth copying: read BOTH siblings, noted convergence, downgraded to memo+report rather than a 3rd comment.
⛔**ANCHORED TOP (08-05) — [[feedback_a_siblings_memo_is_untrusted_input_not_a_finding]]**: ⭐⭐⭐**A 3rd concurrent session's memo on slang#6578 concluded "NOT reproducible by anyone as written" + "close and file a fresh coverage issue" — acting on it CLOSES A LIVE BUG. Its narrow premise was TRUE and well-measured (the cited repro needs the never-merged DNI hack `996cf7d8b`, behind 2335, + a GPU run); the CONCLUSION was one notch too wide.** I re-ran it MYSELF rather than adjudicating peers: 2 shipped-flag `slangc` commands, no patch, no GPU ⇒ verbatim dup-entrypoint message, **exit 0, NO output file** (control: plain module → exit 0, 552 B; magic count 1 vs 0; guilty control → 255 + stderr diags). ⇒ ⭐⭐⭐**HAZARD SHAPE: true premise + real measurement + conclusion one notch wider than evidence — it reads as DILIGENCE and here it AGREED WITH THE PRIOR I HAD PUT IN MY OWN DISPATCH ("GPU test, unrunnable"), which is the conclusion you will never audit.** ✅**SCOPE THE REPAIR TO WHERE THE DEFECT REACHED: the bad memo never posted (4 phrases = 0 across all 3 comments, positive control `reproduces`=3) ⇒ no public correction owed.** ⛔**2 instrument defects of mine in that check: `gh api --jq --arg|contains` returned EMPTY FOR EVERY TERM incl. positive controls — blank read as zero, caught ONLY by the control; and my repro of their control failed because I added `-target spirv` (⭐when your repro of someone's control fails, suspect YOUR transcription — the flags ARE the experiment).** ⚠️**`triage-<N>.md` is NOT a unique key with sibling sessions: an append took a file 196→138 lines (a clobber); my INBOX copy survived — a sent attachment is an immutable snapshot, so disk and inbox diverge.** ⇒**Residue (1): [[project_fixer_restart_tripwire]]. 0 orphans after this.**
⛔**ANCHORED TOP (08-05) — [[feedback_a_quote_has_two_halves_text_and_addressee]]**: ⭐⭐⭐**Under a SHARED bot identity, finding a quoted string proves it EXISTS, never that it was addressed to the session in front of you. I told a peer "you corrected me on a claim I never made" after 5 zero-hit fragment greps — the quote was REAL, authored by a SIBLING orch session (`b285e0b9…jsonl:893`, 20:19:21Z, thread `…slang-12320`), which conceded it itself 6 min later. My denial was true of MY outbound, false about the fleet.** ⛔⭐⭐⭐**2nd defect, the one I'd repeat: I scanned `head -60` of 928 orch sessions and wrote "zero hits ANYWHERE" — a SAMPLE published as a POPULATION, the IDENTICAL defect the peer had just retired (15-row search window as census) and I had praised them for finding ONE MESSAGE EARLIER. Hearing a lesson is not holding it.** ⇒ **State the denominator before writing "nowhere": `grep -l <quote> *.jsonl` over the whole transcript dir IS the population; any `head`/`--limit`/`per_page` in the scan forbids the word "anywhere".** ⭐⭐**"You said" does not survive fan-out — cite `session X, msg id N, time T`. A shared `CLAUDE.local.md` is a source of OTHER sessions' inbounds (sibling text reads as your own history).** ⭐**Telling a peer they conceded something nobody said would have DELETED A TRUE RECORD — the safe form is "not in MY outbound; check for a sibling."**
⛔**ANCHORED TOP (08-05) — [[feedback_a_correction_must_re_measure_the_published_input]]**: ⭐⭐⭐**A CORRECTION IS A CLAIM TOO — RE-MEASURE THE INPUT THE ARTIFACT PUBLISHES, not the newest file in your scratch dir. slang#12367: triager publicly RETRACTED a CORRECT `hlsl → 255 E99999` row after measuring a runtime-ternary variant it never published (published repro = global `static functype`); one binary, one commit, only the shader differed (255/no-file vs 0/undefined `Func<int,int >`). Headline moved 4-silent/2-loud → 5/1 on a thread a maintainer was SIZING WORK FROM.** ⭐⭐⭐**Every other false zero this session aimed a QUERY wrongly; this aimed a GOOD instrument at the WRONG INPUT — no control catches it, the control shares the wrong input.** ⭐⭐⭐**The false version made its own finding BIGGER (5>4) ⇒ AUDIT HARDEST WHEN A CORRECTION STRENGTHENS YOUR ARGUMENT; it had filed that exact rule one turn earlier ABOUT THIS ROW — knowing it did not fire it.** ⛔**It also MANUFACTURED a mechanism ("probably a `-o /dev/null` E00070 run") — plausible, self-flattering, wrong ⇒ never invent a cause for a defect you have not LOCATED.** ✅**I ESCALATED rather than absorbed ("my binary is a day older" was a HYPOTHESIS, and it was refuted in my favour — no commit landed between builds); absorbing it would have left a maintainer scheduling from wrong numbers.** ⭐**The tell was in THEIR OWN OUTPUT: `--> d.slang:1:5` — their ternary has nothing there; A DIAGNOSTIC'S SOURCE LOCATION IDENTIFIES THE INPUT, read it before theorising about binaries.** ⭐**Fresh comment BEAT edit-in-place: the hygiene rule's real variable is *has a reader consumed the wrong version*, not *who commented last*.** ⚠️Their file-check read a LEFTOVER output file from the prior loop iteration (`rm -f` + fresh paths) and a `printf "$?"` read the SUBSHELL's status. Repair `5197180868` verified; maintainer then self-assigned + `Office-Yong` + milestone Q3-2026; spin-off **#12372** (spirv-opt asserts at default `-O`, `-O0` clean).
⛔**ANCHORED TOP (08-05) — [[feedback_a_parallel_fetch_lets_a_fact_land_on_the_wrong_subject]]**: ⭐⭐⭐**I fetched #9736 + #9872 in ONE parallel call and published `kaizhangNV` as #9736's assignee — it is #9872's. TRUE FACT, WRONG SUBJECT: #9736 has `mkeshavaNV`, one assignment event ever, `kaizhangNV` 0× in its timeline (peer control: mkeshavaNV 3×).** ⛔**It was the ENTIRE basis for my "different in kind" verdict** (a different, non-departing owner) ⇒ with the real assignee #9736 sits squarely in the same departing-owner batch; **the peer ECHOED IT BACK to me first**, and had they not re-measured they'd have told a member their departing-owner problem didn't apply. ⇒ ⭐⭐**Parallel calls are the right PERF choice and the wrong EPISTEMIC boundary — results merge into one block with no subject↔field binding, so recall goes by PROXIMITY, not by key; WORSE in a batch where every subject shares a shape (same repo/body/milestone family), because the fields look interchangeable and nothing about a swap reads as wrong.** ✅**Re-open the SINGLE subject before asserting a per-subject fact (assignee/milestone/state/author); a "this one is DIFFERENT" claim needs a per-subject re-read of the differing field — contrast claims are the highest-risk consumers of merged reads; emit facts KEYED side-by-side, never narrated.** ⭐**When corrected, LOCATE the error (it was the sibling in the same fetch) — that makes it a mechanism, not "I was careless" — and SALVAGE the surviving part (#9736 IS still the only one of the three carrying a prior bot verdict).**
⛔**ANCHORED (08-05) — [[feedback_a_timeout_and_a_429_are_different_evidence_about_the_work]]**: ⭐⭐⭐**A 429 REJECTS BEFORE the turn (work never started ⇒ check ABSENCE, re-drive); a TIMEOUT cuts a RUNNING turn (work may be PARTLY DONE ⇒ check for a DUPLICATE post). Opposite artifact risks — I nearly filed #7209's 20:49 timeout as 'more rate limiting'.** Both comments would carry the SAME bot identity, so per-chain 'have I posted?' says no in each session and CANNOT see the double — count bot-authored comments on the ARTIFACT (measured: 3 comments, exactly 1 bot ⇒ clean). ⭐⭐**Discriminator, one loop, split by error STRING not 'an error happened': `429` in 8/10 siblings = FLEET-WIDE; `operation timed out` in 0/10 = CHAIN-SPECIFIC** ⇒ lumping them mis-escalates a stalled session as an outage, or hides a real hang inside a known one. ⭐`ncl sessions messages` reads LOCAL DBs ⇒ safe to run during a rate-limit incident, unlike a retry. ⚠️**My own '8/8' became 8/10 when I widened by two — an N/N ratio is APERTURE-DEPENDENT, report the denominator; the 2 clean sessions were created LATER = burst draining.** ✅Head-window control: `--limit 50` and `500` both 29 lines ⇒ whole session fits, tail is genuinely current. ⭐**The timeout cost NOTHING — session tail showed it died while processing my STAND-DOWN (delivered=true 20:20); read what a session was consuming before calling a late error a lost task.**
⛔**ANCHORED TOP (08-05) — [[feedback_ncl_sessions_messages_limit_returns_first_n_not_last_n]]**: ⭐⭐⭐**`ncl sessions messages --limit N` IS A HEAD WINDOW — `--limit 4 | tail -1` hands back a row an HOUR STALE and reads as "latest". I published "25 sessions stuck on 429" from it; at `--limit 500` the truth was 22 stuck / 133 fine, and several "dead" sessions were the AUTHORS of reports sitting in my own inbox, timestamped AFTER the 429 I blamed on them.** ⇒ **For current state pass `--limit 500` and take the last row; the 1-command control is RUN TWO LIMITS AND CHECK THE LAST ROW MOVES.** ⛔**2nd defect same census: `grep 429` matched HEALTHY reports that merely DISCUSSED the 429 (33 vs 22) — match the error STRING (`API Error: Request rejected`), never the number, or you count the incident report as the incident.** ⭐⭐**A stuck-session count is a claim about OTHER agents' liveness — exactly when to control the instrument; mine contradicted evidence already in my inbox and I didn't notice until the aperture changed.** Sibling of [[feedback_last_active_tracks_inbound_not_agent_work]] (instrument answers a NEIGHBOURING question, confidently).
⛔**ANCHORED TOP (08-05) — [[feedback_a_timeout_is_not_a_decision_verify_the_ask_was_delivered]]**: ⭐⭐⭐**`ask_user_question` CANNOT DELIVER from a task/cron session (`thread_id=system:tasks:*`, `messaging_group_id` NULL) — card persisted at `delivery.ts:389`, routing guard RETURNS at `:415`, adapter at `:426` never runs, row marked `delivered` ANYWAY. MEASURED: 3 asks / 15h on one held filing decision, operator shown NONE; I re-asked TWICE through the identical broken path, reading my own undeliverable card as operator inaction — my task brief even opened "a timeout is NOT a decision".** It has **no `to:` param** ⇒ only the session's null routing; `send_message`/`send_file` take one and land (control: same session, same minute, `platform_message_id` non-null). ⇒ ⭐⭐**`status='delivered'` IS WORTHLESS; `platform_message_id IS NULL` is the tell — prefer the field only the FAR SIDE can write.** ⛔**`timeout:0` is WORST here (waits forever for an invisible card); a finite one returns "Question timed out" = A FALSE CLAIM ABOUT A PERSON'S BEHAVIOUR manufactured by a routing bug.** ⇒ **From a task session escalate via `send_message` to a NAMED destination; before treating "no answer" as an answer OR re-asking, PROVE the ask was delivered.** ⭐**Thread id was CORRECT, only `(channel_type,platform_id)` empty — a partially-correct routing record passes a partial audit.** ⚠️**SCOPE THE TELL to USER-FACING rows: a `kind='system'` row (e.g. `append_learning`) is legitimately `delivered` with a NULL id — test on `chat-sdk`/`chat` only, else the rule flags every system action as a failed send.** Filing decision on the 3 `ncl` flag defects STILL HELD, now asked on a routable path (msg 63). ⇒**Displacement residue of THIS write, MEASURED (27 rows past bound; 26 recoverable via the disk globs at line 7-8, exactly 1 NOT): [[project_nanoclaw_pr874_webhook_route_approver]] — a `project_*` file with NO `RESUME` marker, so `grep -l RESUME` misses it BY DESIGN; re-linked here.** ⭐⭐**The glob set has a THIRD hole past the documented two: NON-RESUME `project_*` rows are invisible to glob 2 AND to the rule globs ⇒ add `ls project_*.md` to the rebuild when a chain must survive a tail-cut.**
⛔⛔**BORN-ORPHAN LIFEBOAT (08-05) — [[project_slang_scrub_fanout_22_issues]]**: the mkeshavaNV-departure scrub fanned out to **22 slang issues in 25 SECONDS** and I received **ONE** webhook (#6542). ⛔**TWO chains — #7672, #6578 — were DROPPED AT BIRTH: the orch session took the webhook, died on a 429 BEFORE dispatching, so NO triager session ever existed; ~1h40m unowned until I enumerated the set. I dispatched both 20:2xZ.** ⭐⭐⭐**`sessions=1` IS THE TELL — a healthy chain has 2 (orch+triager); a count of sessions per chain is a CHEAP NON-CIRCULAR coverage probe my own writes cannot move.** ⭐⭐⭐**Success on the delivered member is the strongest FALSE-COVERAGE signal there is — #6542 ran end-to-end and supplied a vivid TRUE story about the batch being handled; a completed chain and a born-orphan are indistinguishable from inside the one that worked.** ⚠️**A 429 BEFORE dispatch is categorically worse than one after (#6542's 20:08Z 429 cost nothing; #7672's cost the whole chain) — same error string, opposite consequence: ask WHERE IN THE CHAIN it landed.** ⚠️**Enumerate by `commenter+updated:<date>`, NEVER by phrase — `q=repo:…+"won't be returning"` returns 0, GitHub does not index comment bodies.** 2nd fan-out of this ask in one day; the slangpy rule [[feedback_a_fanned_out_webhook_delivers_per_issue_verify_the_set]] PREDICTED IT and only fired because a peer mentioned siblings. ⛔⭐⭐**MY RESCUE OF #6578 CAUSED A DOUBLE-POST — another orchestrator was rescuing the SAME chain; 2 bot comments 3min apart. `sessions=1` detects MISSING coverage but CANNOT distinguish never-dispatched from about-to-be-dispatched-by-another-rescuer.** ⇒ **Before rescuing a stranded chain, re-read the issue comments IMMEDIATELY before dispatching AND warn the delegate a sibling may be racing it** — my triager caught it only by its own pre-post drift check, then published a measured DELTA instead of a 2nd full verdict (correct, and better than my dispatch set it up to be). ⛔**BATCH CENSUS found 2 doubles in 22: #6578 and #10181 (10 SECONDS apart, independent verdicts) — per-chain hygiene is blind to both; each session truthfully answered "have I posted?"=no.** ⚠️**#7672 came out clean ⇒ "my dispatch was correct" and "my dispatch caused no waste" are DIFFERENT claims; only the first held.** ⛔⭐⭐⭐**3 DECAYS IN ONE HOUR, ALL MINE, ALL 0→1: "#6578 dropped at birth" (20:25Z→sibling rescuing concurrently ⇒ DUPLICATE POST) · "#7672 clean, bot=0" (read 20:57Z, sibling posted 20:56:31Z ⇒ ALREADY FALSE WHEN PUBLISHED, −26s) · "#9872 bot=0, clear to work" (checked 21:01:05Z, sibling posted 21:01:33Z, +28s). A PRE-ACTION CHECK NARROWS THE WINDOW, IT DOES NOT CLOSE IT — I invented the race check to fix the #6578 duplicate and its very next use failed identically.** ⇒ **The durable defence is the DELEGATE's pre-post drift check + willingness to downgrade to a DELTA or to SILENCE; verification belongs at the moment of WRITING, which only the writer holds — a parent's clearance is stale by construction.** ⛔⭐⭐**BATCH OUTCOME INVERTS MY FRAMING: 21/22 answered by siblings I never dispatched. Every "gap" I found was a gap in MY VIEW, not the fleet's coverage — so on a batch other orchestrators can see, the question is NOT "is this uncovered?" but "AM I THE ONE WHO SHOULD COVER IT?"** ⭐**Peer discipline worth copying: a cell CONTRADICTING an already-confirmed peer result is the LAST to publish and FIRST to control — its Release exit-255 was a missing `slang-glslang` (same failure on a plain shader; its Release/lib lacks the lib, MINE HAS IT and my Debug/lib doesn't — mirror-image envs, neither canonical). Match a number to its shader AND build config, never to its value.** ⛔⭐⭐**AND MY "MIRROR IMAGE" READING WAS WRONG — RETRACTED: I said its Release lacks the lib while my Debug lacks it, i.e. 2 symmetric half-built trees. MEASURED with the entry-count control: my Release/lib=12 entries WITH the .so, my Debug/lib=**0 entries** and Debug/bin=**0** ⇒ I HAVE NO DEBUG BUILD AT ALL. "Absent lib" and "absent build" are the SAME `grep -i glslang` output and DIFFERENT FACTS — the entry count separates them and I published before running it.** ⭐**2nd env axis, verified at source: the .so lookup name is VERSIONED — `slang-glslang-compiler.cpp:554` `"slang-glslang-" + SLANG_VERSION_NUMERIC` (`:552` Windows arm unversioned). Peer wanted `2026.13.1`; my stamp is `0.0.0` because THE CLONE IS SHALLOW (`git describe --tags` fails despite 644 tags) ⇒ shallowness leaks into a BUILD ARTIFACT, not just history queries ([[feedback_shallow_clone_makes_your_head_the_graft_root]]). A downstream-compiler-loading divergence needs BOTH axes checked.** ⇒**4th-pass residue (8): [[feedback_name_what_your_instrument_cannot_record_before_enumerating]] · [[feedback_never_read_an_exit_status_through_a_pipe]] · [[project_12298_enum_bool_switch_canonicalization]] · [[project_12338_optional_existential_reboxing]] · [[slang-evidence-lessons-derivations]] · [[slang-evidence-lessons-index]] · [[slang-evidence-lessons-instruments]] · [[slang-evidence-lessons-measurement-rows]]. STOPPING — the offset-~22k disk-glob rebuild reaches every chain regardless of which rows are dark.** ⇒**3rd-pass residue (my fan-out row displaced 15): [[command_gh_api_slurp_excludes_jq]] · [[feedback_a_config_conditional_mechanism_needs_the_config_read]] · [[feedback_broader_read_access_is_not_higher_authority]] · [[feedback_ncl_tasks_scope_from_cron_session]] · [[feedback_read_every_write_site_before_asserting_an_invariant]] · [[feedback_two_absence_failures_one_evades_controls]] · [[feedback_zero_output_is_not_available_scratchpad_still_delivers]] · [[project_12285_precise_fma_noinline_stale_version]] · [[project_nanoclaw_1051_agent_image_guard_selfblock]] · [[project_nanoclaw_1083_drift_check_empty_allowlist]] · [[project_nanoclaw_1085_ncl_unit_hint]] · [[project_nanoclaw_1086_sync_sendmessage]] · [[technique_fix_containment_use_merge_base_four_rest_statuses]] · [[technique_merge_queue_eviction_read_both_surfaces_on_the_group_commit]] · [[technique_merged_at_not_committer_date_for_merge_time]]. STOPPING — 0 orphans; the offset-0 disk rebuild reaches every chain regardless.**
⛔**ANCHORED EARLY (08-05) — [[feedback_a_shared_bot_identity_makes_duplicate_posts_invisible]]** (the DUPLICATE half of the row below; that one says don't dispatch on reply-absence, this one says a fan-out ALREADY double-posted): ⭐⭐⭐**MEASURED on slang#10181 — TWO scrub comments from `nv-slang-bot[bot]` 10s apart with DIFFERENT BODIES (`5196891201` 5002B 20:19:39Z, `5196892695` 3036B 20:19:49Z): 2 sessions, 1 shared identity, independent verdicts. Only 1 double in the 23-issue batch, but PER-CHAIN hygiene CANNOT SEE IT — each session correctly answered "have *I* posted?" = no.** ⇒ **Run a BATCH-level scan: count `author==our-bot && created_at>dispatch` per issue, flag >1.** ⚠️**2 differing verdicts is WORSE than 2 identical** (maintainer must reconcile). ⭐⭐**THE CENSUS DRAINS WHILE YOU READ IT — #7209 flipped 0→1 and #10181 0→2 across reads minutes apart with 21 containers live ⇒ a zero means "NOT YET", never "dropped"; re-check the artifact IMMEDIATELY BEFORE posting, never from a handed-over list.** ⛔**4 instrument traps, all caught by controls: `test("bot")` matched `github-actions[bot]` ⇒ WRONG IN BOTH DIRECTIONS at once (inflated #7672, missed #6518) · `assignee:` gave 19 vs the true 23 — batch is defined by THE COMMENT, and a plausible filter that CORRELATES with the real set returns a full-looking list · an APOSTROPHE in a search phrase returned `total_count:0` at exit 0 (caught only by an apostrophe-free control = 23) · ✅a 422 naming the rejected term BEATS a clean 0 — it proves the filter applied.**
⛔**ANCHORED EARLY (08-05) — [[feedback_a_batch_census_needs_the_owner_column_not_the_reply_column]]**: ⭐⭐⭐**A batch worklist measured by REPLY-ABSENCE is already fully owned — MEASURED: a peer's 10 "unanswered" issues from jkiviluoto's 25-issue scrub ALL had live sessions (9×2, 1×1; controls #8527=2, garbage=0), so dispatching would have double-owned ten threads under one bot identity.** Early in a fan-out (51 sessions in 4 min) a session has no comment BY CONSTRUCTION ⇒ reply-absence measures ELAPSED TIME, not need; the dispatch predicate is OWNER-absence (`grep -c "slang-<n>\( \|$\)"`, anchored, bounded — `--limit 200→200` and `2000→2000` are PAGES, true total 2290). ⛔**2nd defect same turn: I RE-DERIVED [[feedback_a_repeated_turn_error_is_a_fleet_signal_not_a_chain_signal]] from scratch (its 52-in-5-min IS this same burst) instead of opening the row I already had — the store fires on queries I INVESTIGATE with, stays silent on ones I ACT with.** ⭐⭐**Said "my loop was denied, this is unverified" rather than letting silence read as confirmation — the peer then stopped treating its list as jointly held.**
⛔**7209 SCRUB ANSWERED (08-05, comment 5196877252) — [[feedback_an_aged_feature_request_may_be_a_regression_report]] + [[project_7209_link_time_type_default_already_shipped]]**: ⭐⭐⭐**AN AGED FEATURE REQUEST MAY BE A REGRESSION REPORT — `extern struct Foo : IFoo = FooImpl;` WORKS TODAY, and #6555 had it failing `error 45001` from 2025-03-10→06-25 while #7209 was filed 2025-05-22, INSIDE that window ⇒ 'please add this' was 'this is broken'.** Neither natural instrument finds it: issue text = reporter's BELIEF, compiling = state NOW, neither reconstructs state ON THE FILING DATE. ⭐⭐**Verify by DIFFERENTIAL not 'it compiles': one token `FooImpl`→`BarImpl` flipped emitted dispatch AND `offset` -1→2 ⇒ default is SEMANTICALLY APPLIED; exit-0 alone only proves the parser accepted it — add a KNOWN-BAD CONTROL (255) or a skipped compile reads as success.** ⛔`gh search issues --state all` is INVALID (usage dump, false zero) — OMIT `--state`. ⭐**Peer 429'd TWICE ⇒ sampled 8/8 siblings carrying 429s = FLEET-WIDE, so retry #3 would add load to the saturated resource; a prebuilt `slangc` made it measurable INLINE ⇒ the delegate was not load-bearing.** ⭐**Artifact-checked before EACH re-dispatch (2 comments, 0 bot output both times ⇒ undelivered, not duplicate).** ⚠️Left OPEN: close is a maintainer's call (expipiplus1 endorsed Nov-2025); residual = docs lack a TYPE example + coverage is ONE test.
⛔**ORPHAN RESCUE (08-05, 10th pass — MEASURED: my own 2 writes above darkened 29 rows; every one PRESENT on disk).** The 3 disk globs at the recovery block (offset ~21.9k, INSIDE the bound) recover the `project_*`/rule files; these are re-linked because the globs alone don't name them: [[slang-ci-infra-chains-index]] · [[slang-frontend-docs-chains-index]] · [[slang-nanoclaw-chains-index]] · [[slang-rhi-backend-chains-index]] · [[slang-parked-index]] · [[slang-shipped-index]] · [[slang-routing-lessons-index]] · [[feedback_four_states_where_the_decisive_check_feels_unnecessary]] · [[feedback_order_by_action_never_by_step_number]] · [[feedback_void_the_execution_claims_keep_the_source_claims]] · [[feedback_too_coarse_to_measure_is_a_claim_about_an_instrument]] · [[feedback_a_fact_that_lets_you_stop_investigating_is_load_bearing]] · [[feedback_a_subagents_restore_claim_is_a_claim_verify_it]] · [[feedback_gate_remedy_may_be_disjunctive_reread_it]] · [[feedback_reversing_a_correct_position_under_a_defective_input]] · [[feedback_narrating_a_non_reply_is_a_reply]] · [[feedback_empty_frontmatter_makes_a_memory_unreachable]] · [[feedback_never_edit_externally_synced_skills]] · [[feedback_no_parent_traversal_in_includes]] · [[feedback_approver_step1_clauses_are_data_only_judgment_is_step3]] · [[reference_shared_learnings_correction_is_two_actor]] · [[technique_ps_is_blind_across_sessions_use_ncl]] · [[technique_annotations_survive_log_expiry_step_relative_line]] · [[technique_grep_in_repo_a_misses_reusable_workflow_in_repo_b]] · [[project_11616_forceinline_debugnoscope_caller_scope]] · [[project_11917_pass_gating_epic]] · [[project_bot_workflows_permission]] · [[project_nanoclaw_1067_footer_normalizer]] · [[project_nanoclaw_1074_scheduled_task_dump]]. ⭐⭐⭐**11th confirmation of the treadmill: a write here darkens rows below it, and the RESCUE ROW ITSELF displaces more. Fix by ADDING a path, never deleting; re-walk the closure AFTER the fix and expect to iterate.** ⇒**2nd-pass residue, as predicted — the rescue row displaced 5 MORE: [[slang-longtail-chains-index]] · [[feedback_the_compaction_bound_targets_the_wrong_file]] · [[feedback_a_correct_action_does_not_validate_its_rationale]] · [[feedback_stop_a_converged_exchange_the_yield_curve_bends]] · [[technique_three_questions_session_worktree_thread]]. **STOPPING HERE: 0 orphans, and iterating further just trades which rows are dark** ([[feedback_a_remedy_that_can_reproduce_its_own_bug]]).
⛔**LIFEBOAT (08-05) — [[project_6542_nested_parameterblock_precompile_ice]]**: maintainer SCRUB answered, verdict **still-relevant** posted `#6542#issuecomment-5196535395`; chain AT REST awaiting a human (reassignment + 2 offered spin-offs: truncated `-dump-ir` on the precompile path, silent `dxil` EXIT=255). Repro at `b0e43d657`; trigger narrowed to **PB nested in PB**; I re-verified every load-bearing source claim (`:480` guard, emit `:3015`, `kIROp_ParameterBlockType`=**0**, no `ParameterBlockType` twin of `wrapRemainingConstantBufferElementTypes():293`). ⭐⭐**Two grep lessons, both mine this chain: (1) a FALSE ZERO from a non-persisted `cd` — my `grep -c` returned 0 with `No such file` on the SAME line and the 0 is what I nearly used; ALWAYS read the error stream beside a zero. (2) `kIROp_X`-only greps are SPELLING-BLIND — the file uses `as<IRConstantBufferType>` (6 refs, 0 `kIROp_`), so a 0 read as ABSENCE.** ⚠️**A published control figure (129 ParameterBlock test files) does NOT reproduce in ANY variant I tried (127/136/125/123/135/145) — and a CONTROL IS NOT DECORATION: when the headline is a ZERO, the control is all that separates "nothing matches" from "my grep is broken." Not corrected publicly — an edit notifies nobody and a fresh comment for a 2-off control is worse noise.**
⛔**ANCHORED EARLY (08-05) — [[feedback_zero_test_jobs_is_not_zero_tests_ran]]**: ⭐⭐⭐**I PUBLICLY DISPROVED A TRUE CLAIM and demanded a peer retract it. "0 jobs named `test`" ≠ "0 tests ran" — the tests were STEPS INSIDE the 12 `build` jobs** (`gh api …/actions/jobs/<id>` → `.steps[]` shows `Unit Tests (Python)` ✓ on self-hosted GPU runner `nvrgfx-kernelvm-bridge`; log carries all 4 cited `PASSED [DeviceType.cuda]` verbatim + 31/31 `test_raw_dispatch`, 3619 cuda / 4121 vulkan). ⛔**A GRANULARITY MISMATCH between instrument and claim yields a CONFIDENT FALSE NEGATIVE: I enumerated JOBS, the claim lived in STEPS.** ⚠️**It felt airtight for the worst reason — 12/12 enumerated (not sampled) PLUS two corroborations (`ci-gcp` 0 runs ever, `slangpy_torch` failed April), both TRUE AND IRRELEVANT (GPU tests ride inside `ci`). Piling on true adjacent facts DISGUISES a wrong-granularity probe rather than repairing it — breadth of evidence is not depth.** ⇒ **To claim "no tests ran" read a job's STEP LIST; better, verify the POSITIVE verbatim from `jobs/<id>/logs`. A wrong POSITIVE gets checked by others; a wrong NEGATIVE arrives wearing diligence and CLOSES the inquiry — give your own disproofs the adversarial pass you'd give a finding.** ⛔⭐⭐**Same turn, 2nd error: `nv-slang-bot[bot]` is a SHARED identity across sessions — I attributed a sibling's comment to the peer I was talking to and built a "you contradicted yourself" argument on identity I never checked.** A comment's author does NOT identify the session that wrote it.
⛔**ANCHORED EARLY (08-05) — [[feedback_a_reporters_framing_is_a_hypothesis_not_a_finding]] + the 22-rule [[slang-evidence-lessons-index]]** (both were link-dark; a 15.0k lifeboat got pushed to 25.7k by ~10KB of sibling writes ABOVE it — ⭐⭐⭐**an anchor's offset is set by everything upstream of it, so only the TOP is displacement-proof**): ⭐⭐**a REPRODUCTION confirms the SYMPTOM, never the reporter's CAUSAL CLAIM — vary the construct the title blames** (#12338: cause was the `none`, not the `Optional`; one deleted store flips the lowering). ⭐⭐**"perf-only / not a miscompile" IS a correctness claim ⇒ EXECUTE it with a NON-ZERO CONTROL.** ⭐**A failure shared by the CONTROL is the environment, not your finding** (dxc failed on all 3 variants incl. baseline ⇒ the repro's `[noinline]`). ⭐**Resolving a caveat is itself a SCOPED claim — name the inputs you checked or the fixer inherits your hedge as a guarantee.** Chain: [[project_12338_optional_existential_reboxing]] (HANDED OFF to saipraveenb25, assigned, PR incoming).
⛔**ANCHORED EARLY (08-05) — THE ALL-CLEAR SLOT, 4th form in [[feedback_a_candid_disclosure_gets_less_scrutiny_not_more]]** (its own 2 rows are past bound; this is the only readable path): ⭐⭐⭐**"nothing owed / already covered / clean" about a PEER'S artifact escapes scrutiny on BOTH sides — I don't check it because it feels like CLOSING, they don't because I told them not to.** slang#7462: grepped `every PR` (0, no control), published "no re-post needed" about a maintainer-facing comment whose live string was **`every Slang PR`**; triager re-checked anyway and patched. ⛔**THE RULE WAS ALREADY STORED** ([[feedback_audit_grep_false_negatives_asymmetric]] step 4) ⇒ **storage was never the problem: it fires on a query I INVESTIGATE with, stays silent on one I CLOSE with.** ⇒ ⭐⭐⭐**ASK OF EVERY ZERO: "does this permit me to STOP?" If yes → needs a non-zero control, and LIFT THE NEEDLE FROM THE SOURCE with a regex, never retype it.** ⭐⭐⭐**Peer rule verbatim: "a retraction clears the CHALLENGER'S INSTRUMENT, never the ARTIFACT" — a conceding peer feels maximally trustworthy and is measuring nothing.** ⇒ **Before "no action needed": name the artifact, show the command, show a control that fired non-zero — else downgrade to "I looked for X and didn't find it, worth your own check."**
⛔**ANCHORED EARLY (08-05) — [[feedback_a_thread_id_on_a_message_tag_loses_to_your_own_session_thread]]**: ⭐⭐⭐**a `thread_id` on a final-response `<message>` tag LOSES to my own session's thread — MEASURED: 2 blocks, one tagged `…slang-10181`, BOTH landed in the 6520 session (seq 6+8); the 10181 session never got it and its issue sat at 1 comment.** My session was born from the #6520 webhook ⇒ its thread IS 6520 and outbound defaults there; a `send_message` guard finally exposed it ("Refusing to send to thread …-6520"). ⇒ ⭐⭐**VERIFY ARRIVAL IN THE RECIPIENT (`ncl sessions messages <sess> --limit 10`, look for `direction=in` with your text) — dispatch ≠ delivery, and "I nudged that thread" is a claim about what I SENT.** ⛔⛔**THE SESSION PIN ALSO FAILED (same chain, 20min later): `send_message` with BOTH `thread_id` AND `target_session_id` returned `Message sent (id: 21)` and STILL landed in 6520 (`eurqam` seq 10) — `target_session_id` FALLS THROUGH on mismatch, BYTE-IDENTICAL to success in the tool result.** ⇒ ⭐⭐⭐**A send-success string is NOT arrival; the ONLY confirmation is reading the recipient's `direction=in` rows.** ⭐⭐⭐**MY MISROUTE BECAME A PUBLIC CONTENT DEFECT — batch census: 18 scrub issues, 17 with ONE bot comment, #10181 (the ONE I misrouted) with TWO 10s apart, different bodies, both recommending closure ⇒ FALSE CORROBORATION (reads as 2 independent votes). PER-CHAIN hygiene CANNOT SEE IT (each session truly hadn't posted) ⇒ run a BATCH census: count `author==bot && created_at>dispatch` per issue, flag >1 AND 0.** ⚠️**Attribution limit: `ncl sessions messages` shows MESSAGES not TOOL CALLS + the bot identity is SHARED ⇒ I can prove I CREATED the 2nd worker, never WHICH session wrote a given comment.** ⚠️A misroute also INJECTS a confusing negative instruction into the wrong chain. Sibling found the same fan-out defect from the other side (born-orphans) ⇒ [[project_slang_scrub_fanout_22_issues]].
⛔**BORN-ORPHAN LIFEBOAT (08-05) — [[feedback_last_active_tracks_inbound_not_agent_work]]**: ⭐⭐⭐**`last_active` tracks HOST INBOUND DELIVERY, never agent work — so NUDGING A SESSION TO CHECK ON IT REFRESHES THE FIELD YOU'D JUDGE IT BY (circular), and an agent 12min into a 20min build is BYTE-IDENTICAL to a container that picked up nothing.** MEASURED on slang#6542: the 429 outbound did NOT move it (18:42), my own nudge DID (19:13). ⛔**2nd defect in the same guard: an error-grep over a FIXED-SIZE tail (`messages --limit 3`) LATCHES ON FOREVER — it re-reported a 429 I had ALREADY redriven, manufacturing redrives. A tail window is not a time window: gate on `seq > max-at-arm`.** ⇒ ⭐⭐**WATCH THE DELIVERABLE, NOT THE WORKER** — the only non-circular signal was the GitHub comment count (`gh api .../issues/N --jq '.comments'`), which no probe of mine can perturb. ⭐**Set the quiet threshold from the WORK's duration (Slang build 5-20min), never a generic 6min.** ⛔⭐⭐⭐**MY FIX FAILED ON FIRST USE — `seq > max-at-arm` tripped in 1 MIN on MY OWN status message, because `seq` COUNTS BOTH DIRECTIONS: I RELOCATED the circularity instead of removing it, and the alert said "NEW ROW **from triager**" about a `direction=in` row — the wording asserted a provenance the predicate never checked.** ⇒ **ASK OF EVERY HEALTH PROBE: CAN MY OWN ACTION MOVE THIS NUMBER? If yes it cannot measure the other party** — filter to `^[0-9]+ +out` or watch an artifact outside the channel. ⚠️**Its detail-grep matched 0 rows ⇒ a confident headline with an EMPTY BODY: the summary is hand-written and always renders, the evidence is computed and can come back blank — print the matched row or say "matched 0 rows".** ⚠️**An `echo` diagnostic on stdout arrives as a notification identical to a real alert — diagnostics to stderr.**
⛔**BORN-ORPHAN LIFEBOAT (08-05) — [[feedback_a_null_from_an_instrument_with_no_field_is_an_unasked_question]]**: ⭐⭐⭐**a null from a tool that STRUCTURALLY CANNOT represent the field is an UNASKED QUESTION, not an absence — and filing it as a "documented limitation" is the WORST form: it reads as diligence and CLOSES the inquiry.** VERIFIED **5×** on one chain: `/proc/mounts` (device only) vs **`findmnt`** (exposes bind subpath) · a **DRAFT PR's `skipping` checks** read as "DXC/MSL routed to CI" for ~6 days · a test passing 4/4 whose directives list only the *legalizing* paths · `permissions.push` for `issues:write` · ⛔**THE INVERTED FORM, STRICTLY WORSE: a GREEN check that cannot observe X.** #12301 went ready, Windows `build-*`+`test-slang` all `success`, and that was read as covering DXC/MSL — but its 5 added directives are `SIMPLE(filecheck)` on `-target hlsl`/`-target metal`, which assert only what Slang EMITS and never invoke DXC or the Metal compiler; the directives that do are `-target dxil` (123 in-tree) and `-target metallib` (97), and the PR has **0** (MINE-VERIFIED on the diff). ⇒ **PENDING, UNRUNNABLE, and PASSED render IDENTICALLY in a status report — and a green check is the most PERSUASIVE form of "not measured," because it argues FOR the claim in the reader's mind. Read the DIRECTIVES/matrix, never the status column.** ⇒ **SPEAK UP when the artifact INVITES A FALSE CONCLUSION (a correction, owed) vs merely knowing more than it says (noise); window EXPIRES AT MERGE.** Non-pushy shape: concede non-blocking → measured counts → stake in the reader's terms → **TWO** remedies → "including 'merge as-is'" → post closest-to-the-state; confirm unraised with a NON-ZERO control. ⛔**SETTLED: `/home/node/.claude` is bind-mounted PER AGENT GROUP** (`…/data/v2-sessions/<ag-id>/.claude-shared`) — shared with MY sibling sessions, **INVISIBLE across groups ⇒ I can NEVER verify or repair another agent's memory files; ASK THEM TO MEASURE, never assert a state and hand over a remedy.** ⚠️**A VERIFIED OFFSET GOES STALE WITHOUT ANYONE TOUCHING THE THING VERIFIED** — reachability is a function of what OTHERS write ABOVE you (my own rows drifted 137→18204 in one session). ⇒ **INLINE the rule INTO the pointer row** so it survives its own link going dark; re-measuring is a treadmill. ⭐⭐**Peer method that caught me 5×: measure your own edge, publish raw output, REFUSE a remedy whose precondition is false, hypothesize about the OTHER side, hand over ONE disambiguating command.**
⛔**LIFEBOAT (08-05, MEASURED at 115,745 B — a sibling's additive fix for these was LOST, not displaced; re-linked here at offset ~150).** [[project_12298_enum_bool_switch_canonicalization]] — repros at master `b0e43d657`, fix ready in DRAFT #12301, HELD maintainer review + `behind_by=31` rebase; ⛔**its "routed to CI" caveat was INERT for ~6 days — a DRAFT PR's checks are `skipping`, so "will be verified" silently became "will never be verified", byte-identical in a status report.** ⭐⭐**Generalized: for any "X is verified / will be verified by M", NAME M and ask whether M CAN OBSERVE X** — a draft's CI cannot; a test whose directives exclude the failing config cannot (`enum-bool-switch.slang` passed 4/4 carrying only the three bool→int *legalizing* paths). · [[feedback_group_clone_is_shared_by_all_sibling_sessions]] — ⛔**`/workspace/agent/<project>/` is PER-AGENT-GROUP: 32 slang-triager sessions share ONE tree (4 running), so a `reset --hard` there eats A SIBLING INSTANCE OF YOURSELF.** 2 actors hit the class in one day; the CHAINED-CHECK mechanism has ONE case. ⭐⭐⭐**A check and its consequent must be SEPARABLE IN TIME or the check is ORNAMENT** — never `status && reset`. ⚠️`ncl sessions messages` shows MESSAGES, not tool calls ⇒ "no session REPORTED editing X" ≠ "none did".
⛔**BORN-ORPHAN LIFEBOAT (08-05) — [[feedback_a_fanned_out_webhook_delivers_per_issue_verify_the_set]]**: ⭐⭐⭐**ONE webhook from an N-issue fan-out is NOT evidence the other N−1 were delivered — delivery is PER-ISSUE, so a fan-out is N independent chances to drop, and a dropped one is invisible BY CONSTRUCTION (no session, no message, no error).** MEASURED slangpy: identical scrub comment on **6 issues in 7 seconds**, **exactly 1 (#822) arrived**; surfaced only via a peer's "out of scope" footnote. **#844 had NO session in ANY group + NO reply, carrying a MONTH-STALE bot verdict recommending close ⇒ silence read as "handled".** ⇒ **Enumerate the set FROM SOURCE, check each for BOTH a session and a posted reply; route only what nobody owns (never into a sibling's chain).** ⛔**2 instrument traps, both FALSE ZEROS: `ncl sessions list` is COLUMN-SHIFTED (empty `messaging_group_id` moves thread to `$3` ⇒ `awk '$4==t'` returned 0 for ALL SIX) and bare `grep -c 844` matches SESSION IDs, not the thread.** ⭐⭐**Caught ONLY by a non-zero control — a zero from a mis-parsed instrument is byte-identical to a real one.**
⛔**BORN-ORPHAN LIFEBOAT (08-05) — [[feedback_an_in_place_edit_notifies_nobody]]**: ⭐⭐**GitHub notifies on comment CREATION, never on EDIT ⇒ edit-in-place hygiene and BEING READ are DIFFERENT GOALS, and on an idle chain they conflict.** MEASURED slangpy#1087: our tracking comment carried "approved + CI green + merge is the only step left" from 08-03T15:25Z, and on **08-05T18:00Z** a maintainer tagged two others calling the same thing a speculative *"might cause some downstream issues"* — current, one comment up, **not delivered**. ⇒ ⭐⭐⭐**"The tracking comment already says it" is a claim about STORAGE, never RECEIPT — the exact sibling of a caveat in the wrong place: present, and occupying the slot where delivery should have happened.** ⭐**DECISIVE + CHEAP: compare your comment's `updated_at` to their `created_at` — if they wrote AFTER your edit and still have it wrong, the edit did not land.** Remedy = a NEW comment (it notifies); where posting is gated that makes it an AUTHORIZATION question to raise, **never** grounds to conclude "already covered" and no-op.
⛔**DISPLACEMENT LIFEBOAT (08-05, 7th pass) — MEASURED AFTER my born-orphan write above (93 targets / 61 in-prefix / 32 dark / 10 with no depth-2 path), all 10 PRESENT on disk:** [[feedback_a_failed_cd_makes_the_next_grep_a_false_zero]] · [[feedback_genuine_redelivery_drops_the_rerun_not_undelivered_work]] · [[feedback_two_absence_failures_one_evades_controls]] · [[project_12285_precise_fma_noinline_stale_version]] · [[project_nanoclaw_1051_agent_image_guard_selfblock]] · [[project_nanoclaw_1083_drift_check_empty_allowlist]] · [[project_nanoclaw_1085_ncl_unit_hint]] · [[project_nanoclaw_1086_sync_sendmessage]] · [[technique_fix_containment_use_merge_base_four_rest_statuses]] · [[technique_verify_a_cited_path_exists_at_master_before_triaging]] · [[feedback_run_the_programs_own_predicate_not_a_stdlib_lookalike]] · [[feedback_a_remedy_that_can_reproduce_its_own_bug]] · [[feedback_a_sentence_can_rot_without_becoming_false]] · [[feedback_ncl_tasks_scope_from_cron_session]] · [[technique_merged_at_not_committer_date_for_merge_time]] (last 5 added on RE-RUNS). ⛔⭐⭐⭐**MEASURED TREADMILL, and the real lesson of this pass: 10 orphans → 1 → 2 → 2, because every fix pass adds ~200 B and displaces ~2 MORE rows. A link-adding repair does NOT converge at a bound this tight — it TRADES which rows are dark.** ⇒ **STOPPED ITERATING deliberately** (not silently): **0 orphans at bound 24,985**, and at 24,400 the residue is 2 rows whose loss is CONVENIENCE ONLY, because the offset-0 disk rebuild (`ls slang-*-index.md` + `grep -l RESUME project_*.md`, line 7-8) reaches every chain without any link. ⭐⭐**When a repair's own cost reproduces the defect it repairs, name the fixed point and stop — don't run the loop to look diligent.** ⚠️`broken links: ['wikilink']` in the checker is a FALSE POSITIVE — it matches the literal `[[wikilink]]` in the memory-system prose, not a real target; **a link-syntax scan also catches SYNTAX EXAMPLES.** ⭐⭐⭐**7th consecutive confirmation that a write here darkens rows below it. The compaction nag fired on this very edit demanding 17.1KB; I DECLINED again — the measured consequence was 10 unreachable rows, fixed by ADDING these paths. Report the CONSEQUENCE (unreachability), never the proxy (bytes); at the floor the lever is SPILLOVER, not deletion.**
🔴**TRUNCATION IS VERIFIED, NOT HYPOTHETICAL — and if you are reading a short version of this file, IT ALREADY HAPPENED.** The injection path announces it: `WARNING: MEMORY.md is 84.4KB (limit: 24.4KB) … Only part of it was loaded.` **52 of 56 rows start past 24,400 units — nearly every routing row is in dropped territory.** ✅**SOLVED 08-05 — THE UNIT IS CODEPOINTS/1024, limit ≈24,986 CODEPOINTS (not bytes): [[feedback_the_memory_limit_unit_is_codepoints_over_1024]].** `python3 -c "import io;print(len(io.open('MEMORY.md',encoding='utf-8').read())/1024)"` reproduces the hook to the decimal (125,718 cp → 122.8; bytes/1000=129.7, bytes/1024=126.7, cp/1000=125.7 all MISS). ⇒ **every `head -c 24400` probe in this file cut ~1,236 B too EARLY** (24,986 cp = 25,636 B) — pessimistic, so past "0 unrecoverable" verdicts still hold, but **compute offsets in CODEPOINTS**: `s=io.open(...).read(); s[:24986]`. ⛔⭐⭐⭐**I had published "the figure is unexplained, do-not-re-open" and a peer propagated it into 2 files as settled — my 2 "inconsistent" pairs were DIFFERENT FILE STATES (sibling grew 56→135 rows mid-session) and MISMATCHED UNITS. "Unexplained" is a claim about MY SEARCH, never the artifact; a NEGATIVE finding needs the SAME controls as a positive one and must NEVER be marked closed — the closure label suppressed the re-derivation that solved it in one command.**
⛔**LIFEBOAT (08-05, 8th pass — anchored EARLY @~6.8KB because every late anchor got displaced within minutes; MEASURED after a CONCURRENT SIBLING REWRITE grew this file 121,373→126,337 B and darkened 4 rows I had just verified reachable).** All 5 PRESENT on disk: [[command_gh_api_slurp_excludes_jq]] · [[feedback_zero_output_is_not_available_scratchpad_still_delivers]] · [[project_fixer_restart_tripwire]] · [[slang-slangpy-tooling-chains-index]] · ⛔**[[feedback_publish_a_claim_as_wide_as_your_evidence]]** — ⭐⭐⭐**RE-DERIVING FROM PRIMARY SOURCE IS NECESSARY BUT NOT SUFFICIENT: the last mile is checking the PUBLISHED ARTIFACT IS AS WIDE AS THE EVIDENCE.** A peer's own first raw output contained `v2026.12.0.1 diverged`; the check RAN and was right, but the tag did not survive into the memo, the issue body, or the comment — **a claim narrowed on the way to publication is worse than a query never run, because the artifact looks authoritative.** ⇒ **Diff what you publish against your raw output; ENUMERATE candidate sets, never hand-type them (a hand-list defines its own coverage); a COLLAPSED table is itself a completeness claim.** ⭐⭐**A reachability verdict decays from OTHER writers' edits — "I verified 0 orphans" is true only until the next sibling write; re-measure, and anchor rescues EARLY.**
⛔**9th-pass co-rescues (displaced by the row below; both PRESENT on disk):** [[feedback_read_every_write_site_before_asserting_an_invariant]] — ⭐⭐⭐**`grep -n '<member>'` OVER EVERY FILE THAT TOUCHES IT before asserting an invariant: ONE call returns the COMPLETE write set. 4 corrections on one mechanism in 20 min, each narrower, none caught by mutual review until enumeration ran and found a 4th write site BOTH tiers missed — cross-reading prose finds errors SERIALLY, one per round; enumeration returns all at once. Only a BOUNDED search licenses stopping.** · [[technique_merge_queue_eviction_read_both_surfaces_on_the_group_commit]] — ⭐⭐**a merge-queue eviction needs BOTH `check-runs` AND `status` (bidirectional: each misses what the other sees), queried on the MERGE-GROUP commit from `beforeCommit.oid` — NEVER parse the branch name (`gh-readonly-queue/<base>/pr-<N>-<SHA>` ends in the BASE ⇒ returns a false ALL-CLEAR); NAME the pending check, don't count it; a job whose `started_at` postdates the eviction cannot have caused it.**
⛔**LIFEBOAT (08-05, 9th pass) — [[feedback_no_evidence_names_where_you_looked]]**: ⭐⭐⭐**"THERE IS NO EVIDENCE FOR X" IS A CLAIM ABOUT WHERE YOU LOOKED, NEVER ABOUT THE WORLD.** MEASURED on slangpy#1092: a peer proved `ci-benchmark.yml`'s latest-Slang lane is commented out ⇒ **structurally blind** — true, well-evidenced, and I relayed it upstream as *"no perf evidence exists for ≥2026.13."* The fixer then found the data **already published upstream** (`shader-slang/slang-compile-perf` + public dashboard, per-release JSON, no GPU, no local build) showing the current pin still carries regression residue. ⛔**The invalid inference: "the instrument that would measure this is blind ⇒ the measurement does not exist."** ⭐⭐⭐**Rigor about an instrument's blindness SUBSTITUTES for searching for other instruments** — the caveat-in-the-wrong-slot shape again. ⇒ **Convert "no evidence exists" → "I checked A, B, C; none carry it" (auditable, invites a peer to name D); enumerate own-CI · dedicated perf repo/dashboard · release artifacts · nightly/ToT before declaring a gap.** ✅**The NARROW version survived and still gates: that data is Slang COMPILE time, not SlangPy runtime — so re-SCOPE a blocker when new evidence lands, don't drop or defend it.**
⛔**THE 3rd GLOB IS THE REPAIR — MEASURED 08-05, and the defect was in THIS BLOCK.** The two globs above cover **111 of 625 files**; the 514 they miss are **176 `feedback_*` + 12 `technique_*` + reference/command rules and 312 non-RESUME `project_*`**. So a tail-cut rebuild recovered the *chain-routing* layer and **silently dropped every RULE FILE** — including `feedback_control_the_instrument_not_the_reasoning` (THE root rule), `feedback_false_coverage_…`, `feedback_compaction_target_yields_to_load_bearing_content`, and `feedback_retirement_is_keyed_to_chain_state_not_bytes` (which has **0 in-prefix link hits** — glob-invisible AND link-dark, reachable only at depth 2 via `slang-parked-index`). ⭐⭐⭐**A recovery instrument's COVERAGE is a claim to measure, not assume — `ls`+`grep -l` returning plenty of files reads as working while structurally excluding a whole class.** Found by slang-triager hitting the identical defect on its own store (its globs covered `triage-*`/`project_*`, excluded `feedback_*`) and reporting it up; ⇒ **when a peer names a defect in a shared mechanism, run it against YOUR copy — same shape, independently present.** ⭐⭐**Two numbers disagreeing ("18 unreachable" vs "0") is often a DEFINITION GAP, not a regression — reachable-by-link-walk ≠ recoverable-once-disk-globs-count; resolving instead of overwriting either is what exposed this.**
Then open the topic indexes for live chains, and `dark_open_chains_restored.md` (the fan-in hub, ~19,700 units — inside the bound) for routing-critical orphans incl. the **fixer-drift tripwire**.
**Compacting? Confirm the child holds the detail before shortening any line.** **Shorten rows, never DROP one — a dropped row makes its child unreachable** (#11616 dark 7wk, #10918 unindexed).
⛔**[COMPACTING? OPEN THIS FIRST — full ruleset + the 4 rules spilled from this header 08-04](feedback_compaction_target_yields_to_load_bearing_content.md)** · ⛔📁**[MEASUREMENT & VERIFICATION DISCIPLINE — OPEN BEFORE verifying anything, exporting a number, or correcting a peer](feedback_a_size_figure_names_a_file_check_which_one.md)** · ⛔**[a guard can be INERT yet read as PASSING](feedback_a_guard_can_be_inert_and_read_as_passing.md)**. Must fire WITHOUT opening any: ⛔**A size target is ADVISORY; verbatim commands, IDs and RESUME triggers are NOT compressible — stop at the floor and SAY SO. NEVER compact on the nag's authority.** ⛔**AT THE FLOOR THE LEVER IS SPILLOVER, NOT DELETION; shorten rows, NEVER DROP ONE** (#11616 dark 7wk, #10918 unindexed) — **confirm the child holds the detail BEFORE shortening (grep case-insensitively, then COLLAPSE-AND-SQUEEZE — a line-wrapped phrase returns a FALSE ZERO; ask if a hit carries the RULE or just the WORD).** 🔴**THIS FILE **IS** INJECTED (native auto-memory) — **truncation here is VERIFIED — the injection path announces it in the system prompt (see line 2); only the THRESHOLD'S DERIVATION is unidentified.** ✅**Measured cost: reachability CONVENIENCE, not chain state — a hard cut at 24,400 units loses 0 targets** (fan-in hub inside the bound + offset-0 disk rebuild). ⭐⭐⭐**"93% of rows exposed" and "0 targets lost" are both true; only the second justifies action — report the CONSEQUENCE, not the exposure.** ⭐⭐**When a structural parent-COUNT and a cut SIMULATION disagree, believe the SIMULATION** — a count is fooled by self-references, mentions-not-links, and parents whose own row is past the cut; a closure diff over link syntax is immune to all three.** ⛔⭐⭐⭐**"SYSTEM X DOES NOT READ THIS FILE" LICENSES NO CLAIM ABOUT SYSTEM Y — enumerate over CONSUMERS, not one consumer's code; and READ WHAT THE HARNESS TELLS YOU about a file before investigating how it is consumed** (the label was in my context from message one). ⭐⭐⭐**Rigor downstream of an unverified premise is confident and irrelevant — verify a constraint BINDS THIS ARTIFACT before optimizing against it.** ⛔**MEASURE ROW OFFSETS, NOT FILE SIZE** — a row can start below a bound and end above it, so only shrinking what PRECEDES it helps. ⛔**THIS STORE IS NOT YOURS — no line-level provenance; parse the `originSessionId` FRONTMATTER, not the wording; ~4 siblings rewrote this file in ONE exchange ⇒ atomic check-and-replace, and NEVER quote a stored byte figure.** ⛔⭐⭐**COUNT THE CASES A RULE RESTS ON, AND HOLD IT IN PROPORTION — one case is a HYPOTHESIS WEARING A RULE'S CLOTHING** (slang-fixer 08-04). *Publish the count, never the adjective* had 3 independent instances and survived contact; *"decisive: does the branch exist on my remote?"* had ONE and **inverted on the second** — and I had shipped it as a RECIPE marked decisive. ⭐⭐**A recipe marked "decisive" is the highest-leverage thing a store can get wrong: prose misleads a reader who is still thinking, a RECIPE SUBSTITUTES for thinking, so the confidence label suppresses the re-derivation that would catch it.** ⭐⭐**REPLICATION IS NOT A SECOND CASE** — two edges measuring ONE incident guards against MEASUREMENT ERROR only; it does nothing about a generalization fitted to one situation, and both read as "confirmed twice" in a summary (corrected in the `ps` note 08-04). ⭐⭐**A STRUCTURAL, READABLE mechanism licenses more than a once-observed pattern** — but not a free pass: the recipe it replaced ALSO had one case and a plausible mechanism, and inverted anyway. ⭐⭐⭐**THE FIX FOR A THIN RULE IS OFTEN TO DOWNGRADE ITS FORM, NOT STRENGTHEN ITS CLAIM** — restate it as prose ("here's what happened and what I think it means") so it gets RE-EXAMINED; a command gets EXECUTED. Available immediately, where a second instance may take weeks. ⇒ **Single-case rules carry an EVIDENCE-BASE banner and get re-derived FIRST when they next fire** (banded 08-04: `technique_ps_is_blind…`, `technique_grep_in_repo_a…`). ⚠️**The predictive test ("does the weaker claim still predict?") discriminates OVER-RETRACTION ONLY — it does NOT establish that what survives is TRUE** ("actor is the decider" predicts plenty and is false). ⭐**Trim your OWN newest line first.** ⭐**Re-read the child before acting on a 🔴 row.** ⭐**Probe rules (full set in the compaction child): ENUMERATE never count · SCOPE + NON-ZERO CONTROL · a log listing can NEVER establish ancestry · MEASURE YOUR OWN ENV · a "don't re-litigate" tag claims COVERAGE not confidence · when a query proves unreliable, re-run every claim that used it, PUBLIC ONES FIRST.**

⛔**TAIL-CUT RECOVERY — read this if the sections below stop at "Standing lessons" and you see NO "Live / actionable chains" section.** MEASURED 08-04: **every live-chain pointer row sits PAST both candidate bounds.** ⚠️**The COUNT is deliberately omitted — it decays: I wrote "13 past 24,400 / 12 past 24,985" and my own two insertions made it 14/13 within minutes.** ⭐⭐⭐**A self-referential figure is stale the instant the write lands — state the INVARIANT (every chain row is below the bound), never the tally.** Re-measure if you need one — CI-infra, frontend/docs, RHI, slangpy, parked, shipped, plus #11616 and #11917. So a tail cut takes the **entire chain-routing layer** and leaves the lessons intact. ✅**No content is lost** — all 9 `📁` children verified present on disk, 0 broken links — but **the ability to FIND them goes dark, which is exactly the #11616 failure (a chain unreachable for 7 weeks).** ⇒ **RECOVERY: `ls /home/node/.claude/projects/-workspace-agent/memory/slang-*-index.md` and read each; then `grep -l "RESUME" project_*.md`.** That reconstructs the routing layer without the index. ⭐⭐⭐**A pointer row is only as good as the reader reaching it — cheap-to-lose and load-bearing are not the same axis.**
✅**ABOVE-BOUND LIFEBOAT LINKS (added 08-04 — this line is the mitigation, not commentary).** Measured: at a 24,400 B cut, **26 files went dark with ZERO surviving parent** — the whole routing layer, exactly as predicted above. The named remedy is **redundant linking**, so the 4 topic indexes are linked *here*, early, where a tail cut can't reach; every dark `project_*` hangs off one of them: [[slang-ci-infra-chains-index]] · [[slang-frontend-docs-chains-index]] · [[slang-rhi-backend-chains-index]] · [[slang-nanoclaw-chains-index]]. 🔴**"ALL 26 REACHABLE AGAIN" IS RETRACTED — RE-MEASURED 08-05 AND THE CLAIM HAD DECAYED: the closure walk finds 26 orphans again (53 dark children, 26 with no in-prefix path), and those 4 indexes cover only 4 of them. 22 were UNCOVERED.** The claim was true when written and the file grew past it — ⭐⭐⭐**a reachability assertion is self-referential and decays exactly like a byte count; state the CHECK, never the verdict.** Missing paths added here, since adding a path is the named remedy: [[slang-evidence-lessons-measurement-rows]] · [[slang-evidence-lessons-instruments]] · [[slang-evidence-lessons-derivations]] · [[slang-parked-index]] · [[feedback_void_the_execution_claims_keep_the_source_claims]] · [[feedback_i_broke_the_gate_i_was_enforcing]] · [[feedback_gate_remedy_may_be_disjunctive_reread_it]] · [[feedback_broader_read_access_is_not_higher_authority]] · [[feedback_reversing_a_correct_position_under_a_defective_input]] · [[feedback_too_coarse_to_measure_is_a_claim_about_an_instrument]] · [[feedback_empty_frontmatter_makes_a_memory_unreachable]] · [[feedback_never_edit_externally_synced_skills]] · [[feedback_no_parent_traversal_in_includes]] · [[feedback_narrating_a_non_reply_is_a_reply]] · [[feedback_a_subagents_restore_claim_is_a_claim_verify_it]] · [[feedback_approver_step1_clauses_are_data_only_judgment_is_step3]] · [[reference_shared_learnings_correction_is_two_actor]] · [[technique_ps_is_blind_across_sessions_use_ncl]] · [[technique_annotations_survive_log_expiry_step_relative_line]] · [[project_11616_forceinline_debugnoscope_caller_scope]] · [[project_11917_pass_gating_epic]] · [[project_bot_workflows_permission]] · [[slang-shipped-index]] · [[technique_grep_in_repo_a_misses_reusable_workflow_in_repo_b]] · [[project_nanoclaw_1067_footer_normalizer]] · [[project_nanoclaw_pr874_webhook_route_approver]] · [[feedback_a_candid_disclosure_gets_less_scrutiny_not_more]] · [[feedback_a_true_claim_that_widens_past_its_evidence]] · [[feedback_a_fact_that_lets_you_stop_investigating_is_load_bearing]] · [[project_nanoclaw_1074_scheduled_task_dump]]. ⭐⭐⭐**[A CAVEAT AIMED AT THE WRONG CLAIM READS AS DILIGENCE — and rigor crossing an ARTIFACT boundary](feedback_a_caveat_aimed_at_the_wrong_claim_reads_as_diligence.md)**: a TRUE, well-stated caveat attached to the WRONG claim is WORSE than a missing one — it OCCUPIES THE SCRUTINY SLOT, reading as diligence while licensing the exact action it would have prevented if aimed right (slang#8637: untracked-file caveat sat one paragraph up on claim 1, so "#8637 already implemented" went out unqualified — which licenses CLOSING it and dropping the untracked case). ⛔**Proximity is NOT attachment; name the sentence each caveat qualifies and confirm it is the one a reader ACTS on.** ⛔**Verifying a MECHANISM is not verifying a TICKET'S ASK — two artifacts; the flawless flag measurement is WHAT MADE the status claim feel pre-checked.** ⭐⭐⭐**RUN VERBATIM: *which artifact does this sentence make a claim about, and did I open that one?*** ⚠ONE chain, 2 actors, both hops relayed a ticket status neither had opened. ⇒**ESCALATED ONE LEVEL — [[feedback_a_guarantee_claim_names_the_enforcer_not_the_nearby_mechanism]]**: ⭐⭐⭐**"which mechanism is broken" is STRICTLY NARROWER than "what guarantees the invariant", and answering the narrow one RIGOROUSLY is what suppresses the wide one** — 4 analyses across 3 tiers audited two LOCAL hooks for a whole chain and NONE asked what ENFORCES formatting; it was `check-formatting.yml` (`--check-only` on every non-draft PR + `merge_group`, **17,050 runs**) all along ⇒ deleting the orphan costs NO guarantee, the strongest argument for the disposition and nobody had it. ⇒**Before any disposition on a mechanism, ENUMERATE EVERY mechanism enforcing the same invariant** (CI, server-side, merge queue, bots). ⭐**Reading a trigger block proves CONFIGURATION; a run count proves ENFORCEMENT.** ⛔**PRINT, DON'T COUNT: `grep -c 'git add"'` = 1 on the claude hook reads as "it re-stages" — `:17` is a COMMENT, `:22` a STRING LITERAL; it never invokes git at all** (same family as `grep -cF` eating flag-shaped patterns). ⭐**Edit-in-place only while YOU are last commenter — once a human replies, STACK a fresh comment or the reply is hidden.** ✅jkwak-work SELF-ASSIGNED #12366 ⇒ human owns it. ⛔**CORRECTED (same day, against MY OWN filed rule): "a run count proves ENFORCEMENT" IS WRONG — it proves the check EXECUTES. THREE states: configured (trigger block) → executing (run counts) → **blocking** (required-status-checks list). Only #3 means unformatted code CANNOT LAND — a check can run, go red, and be merged past if not REQUIRED.** ⚠️**State 3 is UNREADABLE from our edge: `branches/master/protection` → 403 "not accessible by integration" — a CAPABILITY GAP, never evidence of absence** (control: `repos/shader-slang/slang` reads fine, `public` ⇒ token scope, not the repo). Proxy w/ both controls: `merge_group`=2765 all `gh-readonly-queue/*` success, non-zero `pull_request`=14113, **zero-control `schedule`=0 ⇒ the event filter discriminates**. ⇒⭐⭐⭐**HONEST FORM: trigger block=configured, run count=executing, required-checks list=blocking — and if unreadable, SAY WHICH OF THE THREE YOU MEASURED.** ⭐⭐**No public correction: the disposition never rested on state 3, so the discrepancy can't change the answer and an edit would be churn on an artifact the maintainer is READING — but a rule filed ONE NOTCH SHORT gets EXECUTED later at full confidence, so the STORE still gets fixed.** ⇒**Also new: [[feedback_an_anchor_that_is_not_unique_is_not_an_anchor]]** — content anchors for programmatic edits must be ASSERTED UNIQUE; observed 2× today (triager: anchor appeared TWICE, in the park block AND the re-opened block, which RESTATES it; me: count=0 because the anchor was LINE-WRAPPED). **Never address a growing file by line number; never loosen a failing assert — re-anchor.** ⛔⭐⭐⭐**AND [[feedback_verify_each_figure_then_never_add_them_up]] — PEER-CAUGHT, 2 errors one probe: I verified `pull_request`=14113, `merge_group`=2765, `push`=165 and the total 17051 EACH, then wrote "consistent with" WITHOUT ADDING THEM — 14113+2765+165=17043, delta 8. THE CLASS NO PER-FIGURE AUDIT CATCHES: every number right, only the SUM wrong.** ⇒**Never write "consistent with"/"accounts for" before literally computing it; print `total-sum` and require 0.** ⭐**Peer's control: SAMPLE THE DELTA REPEATEDLY — constant (8/8/8)=real gap, varying=live-corpus movement.** ⛔⭐⭐⭐**2nd error, mine, WORSE: I partitioned by `status`, got completed=17043 + action_required=8 = 17051, declared cause found — MY OWN decisive test refuted it: `event=pull_request&status=action_required`=8 and `…&status=completed`=14113=the WHOLE bucket ⇒ the 8 are INSIDE completed and 17043+8 DOUBLE-COUNTS. THE EXACT MATCH WAS COINCIDENCE. A SUM EQUALLING THE TOTAL IS NOT A PARTITION — prove DISJOINTNESS (query the intersection, require 0) and EXHAUSTIVENESS SEPARATELY; equality is the most persuasive possible reason not to check containment. `status` and `event` are ORTHOGONAL FACETS, not two halves of one partition.** ✅**What localized it: the CONCLUSION partition closes exactly — 13472+2336+1199+14+8+`startup_failure`14 = 17043 = completed, delta 0** (`startup_failure` was the category I hadn't probed; without it short by 14) ⇒ gap is in the **event** dimension only; **cause NOT established, left as an OPEN GAP not a guess** (bogus-event control returns 0, not an error ⇒ the filter can't distinguish "no such event" from "no runs"). ⛔**Peer's instrument bug worth stealing: `--paginate | jq … | uniq -c` SILENTLY TALLIED AN AUTH-ERROR BODY (`app_not_connected`) AS AN EVENT NAME ⇒ assert response SHAPE (`jq -e 'type=="object" and has("workflow_runs")'`) and classify anything else PROBE_FAILED; never let an error body occupy a data column.** ⛔⭐⭐⭐**MY "gap is localized to the EVENT dimension" WAS REFUTED — peer partitioned a THIRD disjoint facet (`created=` date ranges: 248/5147/9757/1892) and the SAME 8 appeared; I reproduced it same-instant 3× (delta 8/8/8, moved=0). MY OWN decisive test settles it: `created=>2000-01-01` — a date filter matching EVERYTHING — returns 17044, NOT 17052; and `exclude_pull_requests` returns 17052 for EITHER value (the true matches-everything control, whereas `branch=master`=260 is NOT a superset since PR runs live on head branches). ⇒ THE 8 ARE INVISIBLE TO EVERY *FILTERED* QUERY — the gap is a property of FILTERING ITSELF, so no event-name probing could ever have found them and my localization aimed all future work at the one barren dimension. WHEN TWO FACETS SHOW THE SAME DELTA, THE SHARED CAUSE IS THE FILTER PATH, NOT EITHER FACET.** Cause still NOT established; left OPEN, not guessed. ⛔⭐⭐**4th defect, mine, same turn: I read an INSTRUMENT FAILURE AS A FINDING — `--paginate` returned 100 rows vs total 17052 and I wrote "delta 16952 ⇒ the 8 are phantom", committing the very `--paginate`-hides-a-failure trap I'd filed ONE SCREEN EARLIER; then hypothesized a 1000-item ceiling, ALSO WRONG (`page=11` returns 100). A NUMBER FAR LARGER THAN THE EFFECT YOU ARE CHASING IS A SIGNAL ABOUT YOUR INSTRUMENT, NOT A DRAMATIC RESULT** — 16952 while hunting 8 means the probe broke. ⭐⭐**Peer's same-instant rule: a delta between two LIVE counters needs both operands sampled in ONE instant (their total moved 17051→17052 mid-run; both figures grew together ⇒ delta structural) — their earlier "constant 8" was RIGHT BY LUCK, paired across a live corpus.** ⛔**Their 2 instrument bugs worth stealing: `created=<2025-07-01` in a RAW QUERY STRING returns EMPTY (needs `-X GET -f`) and silently dropped 2 of 4 date cells, manufacturing a bogus delta of 2147 that would have read as a dramatic finding; `check_suite_id=0`→PROBE_FAILED, classified not scored.** ⛔⭐⭐⭐**THE UNIFYING RULE — THIS API'S FAILURE MODE IS A PLAUSIBLE NUMBER, NEVER AN EXCEPTION. 4 probes, 4 usable-looking lies: bogus `event=`→0 · `--paginate|uniq -c`→an auth-error body scored as an event · raw-query `created=<…`→empty (needs `-X GET -f`) · `actions/runs?workflow_id=<id>`→40000. AN UNSUPPORTED FILTER PARAM DOES NOT ERROR, IT RETURNS THE UNFILTERED RESULT — reading as a real measurement of a DIFFERENT POPULATION.** Decisive control I ran: `workflow_id=999999999` (NONEXISTENT) returns 40000 too, identical to no-scope ⇒ **a deliberately absurd value is INDISTINGUISHABLE from a valid one — the only test that proves a param is inert.** Correct form is the PATH `…/actions/workflows/<id-or-filename>/runs` (both spellings agree, 17053). ⇒**For EVERY filter param run a bogus-value control and REQUIRE THE RESULT TO CHANGE.** ⭐⭐⭐**MAGNITUDE RULE: a number FAR LARGER than the effect you are chasing is a signal about the INSTRUMENT — 16952 (mine), 2147 + 40000 (peer's), all while chasing 8; the magnitude is the tell, available BEFORE any analysis.** ⭐⭐**A RULE FILED IS NOT A RULE FIRED — I committed the `--paginate` trap ONE SCREEN after filing it ⇒ prefer CHEAP MECHANICAL CHECKS (bogus-value control, print `total-sum`, shape assert) over remembered principles; the check runs, the memory may not** (same retrieval failure as the `PIPESTATUS` rule sitting in 5 files and never firing). ✅**8-run gap: CONVERGED, do-not-re-open — invariant 8 under growth, 3 wrong causes proposed between us, left UNGUESSED; below any decision threshold.** ⚠️**THE FIX ITSELF DISPLACED ROWS: adding the first 22 links pushed 3 MORE rows past the bound (26 → 7 orphans, not 26 → 0), so the last 7 above were added on a SECOND pass.** ⇒ ⭐⭐⭐**A reachability repair grows the file and can darken what it was protecting — re-run the closure AFTER the fix, and expect to iterate. Never report the count you predicted; report the one you re-measured.** ⇒ **RECURRING CHECK, not a one-time fix: walk the closure after every write** (prefix = `s[:24986]` in CODEPOINTS via `io.open(...,encoding='utf-8').read()` — **NOT `head -c`, which is BYTES and cuts ~1,236 B EARLY, making rows look further out than they are: the direction that argues for deleting a safe row** — → grep each dark row's child → then each in-prefix child for a depth-2 path). ⭐⭐⭐**Measure the CONSEQUENCE (unreachability), not the proxy (bytes) — and fix it by ADDING a path, never by deleting a row.** ⚠️Re-run the transitive closure after any spill; a new child with a single parent past the bound is born dark. 🔴**08-05 AT-RISK ROW, pre-emptive lifeboat: [[feedback_order_by_action_never_by_step_number]]** (ORDER BY ACTION, NEVER BY STEP NUMBER — a positional reference is safe only while everyone shares ONE document, and approving a restructure silently voids every number in flight; check specifically whether renumbering moved an IRREVERSIBLE step earlier). **MEASURED: its row is the LAST one starting inside the bound (@23,759 — 641 bytes of headroom) and it has NO depth-2 path** (closure walked; 1 direct parent, 0 alternates). One sibling write darkens it. ⇒ ⭐⭐**Don't defer a reachability fix to a "compaction pass" — the at-risk row is identified by MEASURING OFFSETS, and the cheap fix is a link here, now.**
🔴**ROOT-RULE LIFEBOAT (08-05 — MEASURED DARK, twice, not precautionary).** A sibling re-ordered this index and pushed [[feedback_control_the_instrument_not_the_reasoning]] — **THE root rule, whose child holds the mechanism below** — to offset **34,025**, past the bound, with **zero surviving parent in the readable prefix**. Linked here so a cut can't take it. ⛔⭐⭐⭐**ROOT MECHANISM, which subsumes every other rule in this file: EVERY error across a ~14-correction exchange was a claim about a state the claimant HAD NOT OPENED** — a file, a container, an env var, an artifact, one's own contribution. The diligence slot (caveat / correction / reassurance / forwarded verification) is a **COROLLARY**: those framings assert the checking already happened, so nobody re-checks. ⇒ ⛔⭐⭐⭐**PROXIMITY TO A RULE DOES NOT HELP — a peer stated *"a config fact confirmed on one edge is a fact about one edge"* and violated it FOUR PARAGRAPHS EARLIER in the same message.** The principle was on the page and did nothing ⇒ **"remember the rule" was never the mechanism; a maxim earns its bytes ONLY by naming a COMMAND TO RUN** (grep the row · compute the cell · execute both variants · read the env var IN the container you are claiming about · `jq keys` the state file you are describing). ⚠️**DIRECTION PREDICTS CONSEQUENCE, NEVER CORRECTNESS** — *untested reassurance closes a ticket; untested pessimism only misdirects one* is true about COST, but the category that predicts error is **UNOPENED**, not the direction. ⚠️**EVIDENCE BASE: ONE chain (slang#12345, 08-04/05) — banner in the child; per this file's own single-case rule, RE-DERIVE THIS FIRST when it next fires, and don't read the ⛔⭐⭐⭐ weight as frequency.** The **mechanical** halves are what to trust (anchor the matcher · subtract your own writes · verify offsets AFTER compaction · walk the closure · collapse-and-squeeze a zero · open the artifact); the interpretive taxonomy is strong on mechanism, weak on frequency. ⭐⭐⭐**AND THE PATTERN ALL OF IT SERVES: a state that CANNOT SAY "I couldn't verify" — an inert guard byte-identical to a passing one · a clause reporting `pass` never `unevaluable` · a truncated array returning a plausible smaller number · a CORRECT RULE WITH AN UNVISITED BOUNDARY, indistinguishable from a complete one.** ⇒ **You cannot audit a rule for completeness from inside its verified range; only a claim landing just OUTSIDE exercises the boundary** — so re-reading your own rules will never tell you which have unvisited edges.
⛔**VERIFICATION-LESSON LIFEBOAT (added 08-05 — measured, not precautionary).** All 7 verification rows I placed below the 24,400 B bound on 08-04 now sit at bytes **36,304–42,113** — **sibling writes ABOVE them pushed every one past it.** ⭐⭐⭐**A byte offset is not a property of your row; it is a property of everything preceding it, which other writers control** ⇒ *"I placed it safely"* decays without any edit to the row itself. Reachable from here: [[feedback_false_coverage_the_five_mechanisms_that_consume_the_reason_to_look]] (the CLASS — inert guard · unfalsifiability verdict · unvalidated detector · filter-that-filters-nothing · right-number-wrong-reason · wrong-stored-fact; plus PENDING IS ITS OWN BUCKET, a MATCHING SHA ≠ a CURRENT READING, probe construction, guard-value-is-a-pair) · [[feedback_name_what_your_instrument_cannot_record_before_enumerating]] (the only check that runs BEFORE a result; ELIMINATION IS NOT SUFFICIENCY; a FALSE NULL at huge n) · [[technique_merge_queue_eviction_read_both_surfaces_on_the_group_commit]] (both check-surfaces, BIDIRECTIONAL, on the merge-group commit) · [[feedback_a_discriminator_is_a_claim_about_a_log_run_it]] (family root: a positive control must contain the signal) · ⛔**[[feedback_a_config_conditional_mechanism_needs_the_config_read]]** — config-conditional mechanism claimed **without opening the config**; the `#if` was 4 LINES ABOVE a line I cited, peer misread the SAME lines oppositely. ⭐⭐⭐Publishing my premise as unverified + **refusing to inherit their "verified" read** caught it (inheriting ⇒ one false fact reads as "confirmed twice"). ⭐⭐⭐A **skipped** step logs nothing ⇒ log-derived census can't tell *absent* from *skipped*; **counts were right anyway — an accidentally-correct tally validates NOTHING**. ⚠️`statusCheckRollup` serves the PRIOR sha's SUCCESS while queued.
⛔**DISPLACEMENT LIFEBOAT (08-05) — my own insertion above pushed 12 files dark, the predicted "a reachability repair grows the file and darkens what it protected"; re-measured, not assumed:** [[slang-frontend-docs-chains-index]] · [[slang-evidence-lessons-instruments]] · [[slang-evidence-lessons-derivations]] · [[feedback_order_by_action_never_by_step_number]] · [[feedback_name_what_your_instrument_cannot_record_before_enumerating]] · [[feedback_reversing_a_correct_position_under_a_defective_input]] · [[feedback_void_the_execution_claims_keep_the_source_claims]] · [[feedback_a_fact_that_lets_you_stop_investigating_is_load_bearing]] · [[feedback_narrating_a_non_reply_is_a_reply]] · [[feedback_no_parent_traversal_in_includes]] · [[reference_shared_learnings_correction_is_two_actor]] · [[technique_annotations_survive_log_expiry_step_relative_line]]
⛔**DISPLACEMENT LIFEBOAT (08-05, 2nd pass) — my #1079 rows displaced 3 more into orphanhood; closure re-run, all 3 PRESENT on disk but with no in-prefix path:** [[feedback_zero_output_is_not_available_scratchpad_still_delivers]] · [[project_fixer_restart_tripwire]] · [[slang-slangpy-tooling-chains-index]]. ⭐⭐⭐**Measured, not predicted — and the measurement ITSELF had a bug worth more than the fix: my first offset check searched only for `name.md` and reported the nanoclaw index "PAST BOUND" when its `[[wikilink]]` form sits at 7,600 (INSIDE). A matcher that knows one of two link forms silently halves its own census** ⇒ **ANCHOR THE MATCHER TO EVERY FORM THE DATA USES, and treat a surprising "past bound" as a matcher suspect before a content one.**
⛔**DISPLACEMENT LIFEBOAT (08-05, 3rd pass) — my #1083 row pushed 6 rows past the bound; closure re-walked, all 6 PRESENT on disk with no in-prefix path:** [[command_gh_api_slurp_excludes_jq]] · [[technique_merged_at_not_committer_date_for_merge_time]] · [[feedback_ncl_tasks_scope_from_cron_session]] · [[feedback_a_plausible_story_disarms_the_implausibility_alarm]] · [[feedback_a_remedy_that_can_reproduce_its_own_bug]] · [[feedback_a_sentence_can_rot_without_becoming_false]]. ⭐⭐⭐**Third consecutive confirmation that a write here darkens rows below it — the fix is ADDING a path, and it must be re-measured AFTER, never predicted.**
⛔**DISPLACEMENT LIFEBOAT (08-05, 4th pass) — MEASURED, and the row it saves is MY OWN NEWEST:** [[feedback_every_copy_on_my_disk_never_settles_what_a_run_did]] — *"every copy on my disk" NEVER settles what a RUN did; only the run's LOADED artifact does. 2 measured instances 2 days apart (devin-fetch copies · APPROVAL_POLICY bundled-vs-mounted, where I retracted a CORRECT claim). Find the INVOCATION, never the better file. When 2 careful parties keep contradicting on a config value, suspect TWO ARTIFACTS before an error. "I cannot verify X" is a ROUTABLE REQUEST — name the file BY PATH.* **Closure simulation at BOTH bounds (24,400 / 24,985) showed exactly 1 genuinely-lost target and this was it** (row @30,110, start AND end past both) ⇒ 4th consecutive confirmation, and the cheapest possible fix.
⛔**BORN-ORPHAN LIFEBOAT (08-05, 5th pass) — a NEW file with ZERO inbound links is not "past the bound", it is UNREACHABLE AT ANY BOUND; the closure caught it the moment I wrote it:** [[feedback_run_the_programs_own_predicate_not_a_stdlib_lookalike]] — *to test what a program decides, `importlib` ITS predicate; a stdlib lookalike agreeing on your sample is UNFALSIFIED, not validated. MEASURED: `fnmatch` vs the skill's `glob_to_re` diverge **3 of 8** (`**/*.yml` vs root `ci.yml`; `**/CMakeLists.txt` vs root; `src/*/x.c` vs `src/a/b/x.c`) — they agree only on a bare `**` suffix, exactly the shape we tested. ⭐Where a code path is short and side-effect-free, EXECUTE the other branch instead of reasoning about it — twice in 3 days the terminating move was ~10 lines. ⚠️"each round is producing findings" is NOT evidence the method is right; it's how an unbounded argument sustains itself.* ⇒ ⭐⭐**`in full-closure?` is a DIFFERENT check from `lost at the bound?` — run both; my bound-check printed a reassuring ZERO while the file had no parent at all.**
⛔**DISPLACEMENT LIFEBOAT (08-05, 6th pass) — my 1,480-byte #813 row above pushed exactly 5 rows dark; closure re-walked AFTER the write (85 targets, 59 in-prefix, 26 dark, 5 with no depth-2 path), all 5 PRESENT on disk:** [[feedback_a_failed_cd_makes_the_next_grep_a_false_zero]] · [[feedback_two_absence_failures_one_evades_controls]] · [[project_nanoclaw_1051_agent_image_guard_selfblock]] · [[project_nanoclaw_1083_drift_check_empty_allowlist]] · [[project_nanoclaw_1085_ncl_unit_hint]]. ⭐⭐⭐**6th consecutive confirmation that a write here darkens rows below it — and the compaction nag fired on this very edit demanding I shrink to 17.1KB. I declined: the measured consequence was 5 unreachable rows, fixed by ADDING these paths, not by deleting entries. Report the CONSEQUENCE (unreachability), never the proxy (bytes).** ⛔**BORN-ORPHAN, added here on the same principle (nanoclaw#1086, 08-05): [[project_nanoclaw_1086_sync_sendmessage]]** · ⛔**7th-pass orphans, MEASURED at 24,400: [[feedback_genuine_redelivery_drops_the_rerun_not_undelivered_work]]** (3 targets, no depth-2 path — 2 born-orphan from my own write; all present on disk) + the 1 my own fix displaced ([[technique_verify_a_cited_path_exists_at_master_before_triaging]] — `git ls-tree`/`git grep` a cited path at `origin/master` with a NON-ZERO CONTROL before triaging; self/bot-filed issues are the high-risk class, present-tense claims may describe an unmerged branch): [[technique_fix_containment_use_merge_base_four_rest_statuses]]** (fix-containment: `compare/<tag>...<fixsha>`; **`diverged` is a 4th REST status (REST compare has FOUR: ahead/behind/identical/diverged — NOT git's, and NOT three; use `merge_base_commit.sha == fixsha`) = ALSO absent**; **a patch line can fork BEFORE a fix and keep shipping after — slang `v2026.12.0.1` postdates `v2026.13` and still lacks the fix ⇒ never infer containment from a higher number or later date**; ENUMERATE tags from `gh release list`; control `fix...fix`=`identical`; empty release bodies = NULL signal) · **[[project_12285_precise_fma_noinline_stale_version]]** (slang side DONE; RESUME = slangpy-fixer report on **slangpy#1092** pin residual) · **[[feedback_genuine_redelivery_drops_the_rerun_not_undelivered_work]]**. ⭐⭐⭐**Declined the 17.1KB nag on this edit: measured consequence was 3 UNREACHABLE rows, fixed by ADDING paths, not deleting entries.** — 10th inline-routing instance; ⭐⭐**a green suite is not evidence when its include-globs structurally exclude the changed tree (4 of 5 checks could not reach `container/agent-runner/`; `grep -c` over the logs = 0/0) — read the config, never infer coverage from a pass count**; ⭐⭐**a merge-method request is a TESTABLE claim (scratch-branch sim: squash loses ancestry, replays 2 commits; trees identical either way)**; ⭐⭐**a control that fails at IMPORT proves the import, not the assertion — mutate the value, don't swap the file.**
⛔⭐⭐**[TRIAGING AN ISSUE THAT CITES A FILE/TEST/SYMBOL? `git ls-tree`/`git grep` IT AT `origin/master` WITH A NON-ZERO CONTROL FIRST](technique_verify_a_cited_path_exists_at_master_before_triaging.md).** slang#12339: a **bot-filed** body was written in the PRESENT TENSE about code living only on an unmerged draft PR — 2 "regression test exists" files ABSENT at master, helper 0 hits (control 7) ⇒ **3 of its 4 symptoms described a branch, not shipped code, and one was a property of a REVERTED intermediate approach.** Relocated the whole verdict and pre-empted "the test exists, just enable it". ⭐⭐⭐**A cited path is a claim about a TREE, and the tree is usually unstated** — working copy vs PR head vs master are 3 different answers; **self/bot-filed issues are the high-risk class** (the filer had the branch checked out, so every present-tense claim was true for them). ⭐⭐**Also fetch the PR head — "absent from master, present on the draft" beats a bare negative.** ⭐⭐**Be precise WHICH absence: "no such tests" was right, "none with that prefix" was FALSE** (2 sibling tests exist from a merged predecessor).
⛔⭐⭐**[A TURN ERROR (429) IS EVIDENCE ABOUT THE TURN, NEVER ABOUT THE WORK](feedback_a_turn_error_is_evidence_about_the_turn_not_the_work.md)** — rhi#813: approver's turn 429'd, I ran `GET pulls/813` (head/state/merged), concluded "nothing reviewed, no decision recorded", re-dispatched. **The row was recorded 25 min BEFORE the crash** (`ncl sessions messages <sid> --include-system` → `seq 7 [system: record_decision]` @13:37; the 429 hit a LATER turn @14:02, mid-correction). ⭐⭐⭐**PR liveness cannot discriminate "recorded" from "not recorded" even in principle — an open unmerged PR is fully consistent with a recorded ABSTAIN; a verification claim inherits the scope of what was MEASURED, not of the sentence it's attached to.** ⭐⭐⭐**The artifact-existence check must target the artifact THAT TIER produces** — my stored `gh api …/comments` recipe is a FALSE ZERO by construction for a read-only tier (zero bot comments is its CORRECT steady state ⇒ inverts duplicate-suppression into re-run-forever). ⭐⭐**A stale artifact points the ROUNDED-UP way** (a reversal is by construction the later write ⇒ ledger outranks own memory on resume). ⭐⭐⭐**A positive control establishes an instrument is BROKEN, never HOW** — approver hit the same false zero, theorized "that view never emits tool calls", concluded only raw `.jsonl` works: false capability-negative; **vary the instrument (run both variants), don't theorize.** ⭐⭐⭐**THEN I MADE THE MIRROR ERROR — an untested LIMIT produces a suspicious zero, an untested REACH produces NOTHING TO NOTICE, so only the pessimistic direction trips this store's rules ⇒ assert a probe's REACH with the same measurement you'd demand for its LIMIT** (THREE TIERS: emission · host-acceptance · the committed row). ⭐⭐⭐**"Two artifacts, not one error" ON THE WRONG AXIS — when two VIEWS of one object disagree, don't reach for two objects.** ⚠️`/home/node/.claude` is PER-AGENT-GROUP ⇒ closure counts are NOT portable. ✅**2 CASES — upgraded 08-05 (slang#12367), and the 2nd INVERTED the action: 429 → artifact ABSENT ⇒ re-drive (pin `target_session_id`); timeout → artifact PRESENT ⇒ do NOTHING. Same tier, same session, error text useless both times ⇒ the probe DISCRIMINATES, it is not a rubber stamp that always says "proceed". Secondary: a turn dying while ECHOING has nothing to re-drive.**
⛔⭐⭐⭐**[FOUR STATES WHERE THE CHEAP DECISIVE CHECK FEELS LEAST NECESSARY](feedback_four_states_where_the_decisive_check_feels_unnecessary.md)** — derived from ~7 wrong claims corrected between 2 careful agents in 3 days (spy#1090), **STRUCTURAL not personal: both hit all four, twice making the SAME error independently.** (1) **productive-feeling argument** — D2 ran 3 rounds while a 10-line counterfactual sat available ⇒ *"each round produces findings" is NOT evidence the method is right; it's how an unbounded argument sustains itself* · (2) **confirmed-feeling prediction** ⇒ *a prediction confirmed by a REINTERPRETED observation is not confirmed* · (3) **a correction you're ISSUING** — the corrective frame occupies the scrutiny slot; I shipped a wrong hit-count inside a message correcting a peer · (4) ⭐⭐⭐**just-repaired method — the SUBTLEST: fixing one half manufactures confidence in the other** (peer fixed `fnmatch`→`glob_to_re`, then ran it on paths anchored to the wrong root) ⇒ **a predicate test has two halves, FUNCTION and DOMAIN; the real function on wrong inputs LOOKS more rigorous while being exactly as wrong.** ✅Cheap: `git ls-files | grep -F <candidate>` — *does the string exist in the tree at all?* ✅What worked both times: **exchange the ARTIFACT, not the argument.** ⭐**~7 corrections, ZERO touched the verdict — keeping "what is the finding" apart from "why did it happen" let both of us retract freely without destabilizing a correct BLOCK.**
⛔**[nanoclaw#1085 — a fix for "the hint names a service that isn't there" PRINTS A `systemctl` FINDER ABOVE THE macOS LINE](project_nanoclaw_1085_ncl_unit_hint.md)** — reproduces its own defect on the platform it's helping; the macOS form is already in-repo (`setup/verify.ts:55`). ⭐⭐A suite passing IDENTICALLY on both sides of a change tests nothing about it (3/3 green, caveat unasserted). ⭐⭐RUN THE tsc BASELINE — head exit 2 was 4 PRE-EXISTING errors, delta zero; "typechecks clean" true only as a delta. ⭐⭐VERIFY THE "not changed" SECTION — it claims a working path was left alone and nothing else in the diff catches it if wrong (`exec` in `bin/ncl` ⇒ its `pipefail` never governs the CALLER's pipeline). ⭐Report the FAILED reproduction beside the observation. 9th inline-routing instance.**
⛔**[nanoclaw#1083 — a `git show` SUCCEEDS on an EMPTY file, so an empty allowlist reports "ok: no drift", exit 0](project_nanoclaw_1083_drift_check_empty_allowlist.md)** — all 3 sibling readers carry the `[ -s ]` guard it drops, but FAIL DIRECTION INVERTS (resolver: own-nothing ⇒ surfaces loudly; drift check: own-nothing ⇒ silent pass). LATENT: `wc -c`=0 means absent OR empty OR bad-ref — `git cat-file -e`/`-s` discriminated, all 4 overlays ABSENT ⇒ honest `skipping`. ⭐⭐⭐My fixture mutation never reached the bare origin the script reads ⇒ test B silently re-ran test A, byte-identical to a real pass. ⭐⭐`exit=141` was SIGPIPE from `head`, not the script. ⭐⭐Stub-to-`exit 0`: 4 of 9 green tests are vacuous. ⭐⭐`tsc` green was SCOPE-BLIND (`include:["src/**"]`, 0 files under `setup/`). ⭐⭐⭐358 drifts on nv-slang but 122 == merge-base ⇒ report the DISCRIMINATOR, not the count. 8th inline-routing instance.**
⛔**[nanoclaw#1051 — a guard's error text lists what it CAN say, not what it DID; NO output is the discriminator](project_nanoclaw_1051_agent_image_guard_selfblock.md)** — I published "pin removed / not digest-pinned" read off the script's error BRANCHES; none ran (zero output ⇒ died at `OLD=` before the first `echo`; `grep`-no-match + `set -euo pipefail`). Pin was VALID. **FIXED at head `c1ccd04c` — the fix's own comment names my mechanism (`|| true` on the grep), and its `if: base_ref == 'main'` makes `verify` read `skipping`: a REASONED skip (fork has no OIDC/signer creds; trust inherited from the verified `main` merge), NOT a bypass — never accept a gate that stopped failing without reading WHY.** Routing: a NanoClaw-fork PR is **inline, NEVER to a `*-pr-approver`**, despite the webhook task string saying otherwise.
⛔⭐⭐⭐**[A FAILED `cd` MAKES EVERY FOLLOWING COMMAND A FALSE ZERO](feedback_a_failed_cd_makes_the_next_grep_a_false_zero.md)** — my scratch clone was gone between turns; `cd` errored, `&&` short-circuited only the FIRST command, the rest ran in the reset cwd and printed NOTHING. **A false absence from a missing tree is byte-identical to a verified one, and the `cd:` error sits in the SAME output block, so it reads as "ran, found nothing."** ⇒ **READ THE FIRST LINE of a chained command's output, not just the last** · **precondition with `ls <file> && echo TREE_PRESENT` or `git rev-parse HEAD`, never rely on `cd` succeeding** · **re-run any zero with a POSITIVE CONTROL THAT MUST FIRE** (mine did, and caught it). ⚠️Scratch clones do NOT survive between turns/contexts.
⭐⭐**[GENUINE REDELIVERY LICENSES DROPPING THE RE-RUN, NEVER SKIPPING UNDELIVERED WORK](feedback_genuine_redelivery_drops_the_rerun_not_undelivered_work.md)** — 1079: debounce confirmed correctly (head sha + `merged_at` unchanged, 0 comments, 0 reviews) and the reflex "drop it" would have been WRONG: **the review from the first delivery had never been posted.** ⇒ **check the ARTIFACT exists (`gh api …/comments --jq '.[].user.login'` — is MY output actually there?), not just that the trigger repeated.** Two questions, two instruments. ⚠️ONE case.
⛔⭐⭐⭐**[AN INVARIANT IS A CLAIM ABOUT EVERY SITE THAT WRITES THE STRUCTURE — read PRODUCER *and* CONSUMER](feedback_read_every_write_site_before_asserting_an_invariant.md).** ⛔**RUN `grep -n '<member>'` OVER EVERY FILE THAT TOUCHES IT, BEFORE REASONING — one call returns the COMPLETE write set.** 4 corrections on ONE mechanism in 20min (slang `m_typeCheckingCache`), each NARROWER; **mutual review found them SERIALLY, one per round — the enumeration would have returned ALL of them at once**, and when it finally ran in round 3 it exposed a 4th write site both tiers had missed. ⭐⭐⭐**EXHAUSTIVENESS IS A PROPERTY OF THE TOOL, NOT OF THE ATTENTION.** ⭐⭐⭐**And mutual refusal has NO TERMINATION CONDITION — it stops when both parties run out of things to look at, which is INDISTINGUISHABLE FROM CORRECTNESS and gets credited as convergence; a write-site grep terminates because the set is finite and knowable.** Mutual refusal guards against INHERITING an unverified claim; enumeration BOUNDS THE SEARCH — different jobs, not substitutes. ⛔**"I checked the OPERATION and inferred the INVARIANT"** — verifying an operation is local; an invariant needs the whole write set. ⭐⭐⭐**Being RIGHT about an ADJACENT fact is what licensed stopping — a fresh correct finding is the LEAST-AUDITED moment in an exchange.** ⭐⭐**An intent-describing comment supplies semantics you then skip verifying** (`// Upstream …` made me read a MERGE two lines above a bare pointer REPLACE — the unopened state was INSIDE a block I had opened). ⭐⭐**Convergence = each claim NARROWER; if corrections get BROADER someone is generalizing, not measuring.** ⭐⭐**Correlated derivations are NOT corroboration** — ~12 subagents on near-identical prompts measure PROMPT SIMILARITY and would repeat the framing error in unison.
⛔⭐⭐⭐**OPEN BEFORE TYPING ANY `ncl`: [flag spellings per resource · silent 200-row cap · unrecognized-flag tolerance](command_ncl_flags_and_caps.md).** Facts were already stored 08-04 and I reasoned off a broken flag another full day ⇒ **RETRIEVAL failure, not verification — I HAD run `--help`.** ⭐⭐⭐**KEY AN INSTRUMENT FACT TO THE COMMAND, NOT THE INCIDENT** — so "read `--help`" is not the remedy. ⛔**Unrecognized-flag tolerance is PER-RESOURCE, not global (narrowed 08-05, Main-reproduced): `sessions list --zzz` → ROWS at exit 0 (a typo returns DATA); `tasks list --zzz` → `error (invalid-args): unknown flag`, non-zero. Discriminator before trusting any filtered result: `ncl <resource> list --zzz-nonexistent` — error ⇒ strict, rows ⇒ tolerant. Every resource but those two is UNTESTED.** ⭐⭐**A tolerance claim is per-resource until enumerated per-resource** — I measured `sessions list` and generalized it to "`ncl`" for a day. ⭐⭐⭐**NEVER-LOADED (past the read budget ⇒ hoist the row) and NEVER-CONSULTED (loaded fine, no lookup fired ⇒ key it to the COMMAND) are DIFFERENT defects — hoisting does nothing for an action that triggers no lookup; and the lookup must fire on a POSITIVE RECOMMENDATION, not only on a negative claim.** ⛔**Before publishing an instrument finding, GREP YOUR STORE FOR THE COMMAND** — I used recall as the instrument and recall was the faulty part.
⛔**[`ncl sessions list` incident + retraction](feedback_ncl_sessions_list_agent_group_flag_not_filtering.md) — `--agent-group` DOESN'T EXIST (it is `--agent-group-id`, which filters at `global`); RETRACTED 08-05. Method survives (BOUND TEST · NONEXISTENT-ID CONTROL · count-at-a-fixed-offset-from-`--limit` is a PAGE, offset is PER-EDGE · `grep <ag-id>`); real flag is INERT at `cli_scope=group` (triager: 4 identical 202s).** ⛔**Quote NO row count from it.** Command-keyed version: [[command_ncl_flags_and_caps]].
⛔⭐⭐⭐**[A POSITIVE CONTROL PROVES YOU READ THE RIGHT *FILE*; IT CANNOT DETECT AN INCOMPLETE *ENUMERATION*](feedback_a_positive_control_cannot_detect_an_incomplete_enumeration.md)** — corrects my own "SCOPE + NON-ZERO CONTROL", which I held as sufficient and recommended to peers: 4 actors, 4 matchers, **all controls PASSING, all censuses WRONG**. ⇒ **enumerate at the REGISTRATION SITE, never from declaration syntax.** ⛔**The invariant is NOT "re-measure" — same method re-confirms the defect — it is RE-MEASURE BY A METHOD THAT COULD FAIL DIFFERENTLY, WITH A CONTROL THAT MUST FIRE.** ⭐⭐⭐**"Formatting-agnostic" is an ONE-AXIS claim; naming it stops you enumerating the others.** ⭐⭐**Pre-send: DOES ANY FINDING HERE CONTRADICT ANOTHER?** ⭐⭐**Naming a trap does not arm you against it.** ⭐**Look at the INSERTION POINT first — no tooling needed.**
⛔⭐⭐**[SESSION vs DIRECTORY vs THREAD KEY — three questions, three instruments, no substitutes](technique_three_questions_session_worktree_thread.md).** 08-05: got two wrong in one hour, both SILENT. **`ncl` rows are keyed by ISSUE THREAD and never name a worktree** ⇒ searching by branch/PR reports absent owners who are in fact RUNNING; route with `send_message({to, target_session_id})`. ⭐⭐⭐**A wrong matcher gives a wrong answer you can INSPECT; a wrong session gives SILENCE THAT LOOKS LIKE AGREEMENT** — an hour of findings landed off-owner, no error signal. ⛔⭐⭐⭐**THEN I BUILT A LESSON ON MY OWN UNVERIFIED ERROR** ("filed under issue not branch" — no mismatch existed; I created it by naming the wrong tree). **A wrong FACT costs one correction; a wrong RULE keeps generating wrong facts until someone re-derives it** ⇒ check the surprise was REAL before generalizing. ⭐⭐**A replacement claim arriving right after a retraction is the LEAST-AUDITED moment in an exchange — test it, don't adopt it.** ⭐**A commit timestamp is not an edit timestamp — mtime-sweep source files per candidate tree.**
⛔**THIRD COMMAND — [use `merged_at`, NEVER the merge commit's `committer.date`, for when a PR landed](technique_merged_at_not_committer_date_for_merge_time.md).** 87 min apart on slang#12281 and it **INVERTED my conclusion**; under a merge queue the commit date stamps the QUEUE BUILD. ⭐⭐**`ahead_by` measures DISTANCE, NOT RISK** — my "coverage gap" was mechanically right and substantively empty (a `merge_group` run covers every master commit) ⇒ **ask which OTHER surface already covers it before escalating.**
⛔**FOURTH COMMAND — [`gh api --slurp` IS MUTUALLY EXCLUSIVE WITH `--jq`](command_gh_api_slurp_excludes_jq.md) — exits 1, EMPTY stdout, client-side (bogus repo still proves it; no creds/network needed), in EVERY gh v2.53→v2.96.** A `try/catch→false` caller renders it as "probe failed" (3,225 silent failures, one instance). ⭐⭐⭐**A MOCKED `child_process` TEST CANNOT CATCH IT — asserting `args).toContain('--slurp')` PINS THE INVALID FLAG AS CORRECT and reports green.** Precedent `harvest-reviews.py` slurps WITHOUT `--jq`.
⛔**SAME LIFEBOAT, SECOND COMMAND — `ncl tasks list`: the `--session`-only recipe [INVERTED; it now UNDER-reports (7 bare/`--all`/`--group` vs 5 `--session`)](feedback_ncl_tasks_scope_from_cron_session.md).** ⇒ **bare list is the population; cross-check any scope flag.** ⭐⭐⭐**The predicted single-case-recipe failure, on schedule — a recipe is EXECUTED, not re-derived, so its confidence label suppressed the check.** ⭐⭐**Retracting a recipe? grep what was DERIVED from it, not its wording** — the costly descendant shared no phrasing.
⛔**FIFTH COMMAND — [fix-containment: ASK GIT, and `diverged` is a 4th REST status (REST compare has FOUR: ahead/behind/identical/diverged — NOT git's, and NOT three; use `merge_base_commit.sha == fixsha`)](technique_fix_containment_use_merge_base_four_rest_statuses.md).** `gh api compare/<tag>...<fixsha> --jq .status`: `ahead`=fix ABSENT · `behind`/`identical`=PRESENT · ⚠️**`diverged`=ALSO ABSENT** (a two-status read mis-buckets it). ⭐⭐**A PATCH LINE CAN FORK BEFORE A FIX AND KEEP SHIPPING AFTER IT** — slang `2026.12.x` all lack `33f9ed0c` while `2026.13+` carry it, and **`v2026.12.0.1` was published AFTER `v2026.13` and STILL lacks it** ⇒ **never infer containment from a higher number OR a later publish date; a patch-level bump can silently fix nothing.** ⭐**ENUMERATE tags from `gh release list`** — a hand-written list defines its own coverage (`.12.0.1` is a shape nobody predicts; a peer's careful table omitted it). ⭐Control: `compare/<fix>...<fix>` must return `identical`. ⚠️**Empty release bodies ⇒ "no breaking changes in the notes" is a NULL SIGNAL, not clearance** (slang's are `bodyLen:0`). Live application: [[project_12285_precise_fma_noinline_stale_version]] (slangpy#1092 pin residual, RESUME=fixer report).
⛔**BORN-ORPHAN LIFEBOAT (08-05, 7th pass) — MEASURED after my own #12285 write: closure walk = 91 targets, 25 dark, **3 orphans with no depth-2 path**, two of them files I had JUST created (born with a single parent past the bound). All 3 PRESENT on disk ⇒ content intact, reachability lost.** The compaction nag fired on this same edit demanding 17.1KB; **declined — the measured consequence was 3 unreachable rows, fixed by ADDING these paths, not by deleting entries.** Third orphan rescued here: [[feedback_genuine_redelivery_drops_the_rerun_not_undelivered_work]]. ⭐⭐⭐**7th consecutive confirmation: a write darkens rows below it, so re-run the closure AFTER the write and expect to iterate — report the count you MEASURED, never the one you predicted.**

## Standing lessons — evidence & verification
- ⛔⭐⭐⭐**["EVERY COPY ON MY DISK SAYS X" NEVER SETTLES WHAT A RUN DID — only the run's LOADED artifact does](feedback_every_copy_on_my_disk_never_settles_what_a_run_did.md)** — **2 measured instances 2 days apart, the 2nd AFTER filing the 1st**: `devin-fetch.sh` (I read the 331-ln copy *with* the fix, called the peer's defect "CONTRADICTED" — it ran the 187-ln one; only the invocation transcript settled it) · `APPROVAL_POLICY.json` (my bundles `require_ci_green:true`, the run mounted `v0-shadow-wide` **false** ⇒ **3 rounds of mutual correction, and I retracted a CORRECT claim**). ⭐⭐**A census over the WRONG population FEELS more rigorous than one read, because it's exhaustive** ⇒ **find the INVOCATION, never the better file.** ⭐⭐**When 2 careful parties keep contradicting each other on a config value, suspect TWO ARTIFACTS before an error — both readings were true.** ✅**"I cannot verify X" is a ROUTABLE REQUEST: name the file BY PATH** — doing so got both artifacts attached, and 2 file reads settled what 3 rounds of inference couldn't. ⭐⭐**An UNNECESSARY retraction is a real error, not humility.**
- ⛔⭐⭐⭐**[TWO WAYS TO CLAIM A FALSE ABSENCE — AND ONLY ONE IS CAUGHT BY A CONTROL](feedback_two_absence_failures_one_evades_controls.md)** — **A: output you HAD and screened out** (screened against the question that PROMPTED the search, not the one you ended up asking) vs **B: output you COULDN'T see** (capped/truncated — a capped read is indistinguishable from a complete one). ⇒ ⭐⭐⭐**A control catches B ONLY, so A is the more dangerous half** — a control proves the instrument WORKS, never that you READ what it returned. ✅**When your question changes mid-investigation, RE-READ the output you already have against the NEW question.** ✅**Any round-number total (30/100/200/1000) is a cap signature — bound it before asserting absence.** ⭐**State WHICH failure you ruled out: "I ran a control" answers B only.** ⚠️My own A-instances: accepted a peer's misattribution built on a row it had ALREADY retrieved; cited the wrong bullet of an issue I was myself quoting.
- ⛔⭐⭐⭐**[AN UNATTRIBUTED FACT READS AS YOUR OWN REASONING; "it's in my notes" ≠ you derived it](feedback_unattributed_fact_reads_as_your_own.md)** — MY OWN session wrote it and left it UNINDEXED, so I re-derived it ⇒ **SEARCH BEFORE DERIVING; INDEX A LESSON THE MOMENT YOU WRITE IT.** ⭐**Check the OWNER field before crediting a peer.** ⛔⭐⭐⭐**3 RETRACTIONS, all mine, all from the wrong instrument — a ONE-LEVEL dark metric, a lifecycle claim read off the FILENAME, and a set KEYWORD-FILTERED before counting** ⇒ ⭐⭐⭐**measure TRANSITIVELY and SEGMENT BY CLASS · `project_*` is a FILENAME not a lifecycle state (liveness = QUERY THE UPSTREAM) · A PRE-FILTER ON WORDING SILENTLY SETS YOUR SAMPLE — query the POPULATION** (a keyword's hit rate is per-container ⇒ a peer's sweep is never evidence about your store). Real defect: **[dark OPEN chains, restored](dark_open_chains_restored.md)** + dark rules + a missing standing directive. ✅**RE-TESTED 08-05 — this one HELD: 35 chains named in that child, 0 unreachable, and the child itself sits in the readable prefix. But the wording was "24 … now reachable", a VERDICT** ⇒ ⭐⭐⭐**A SELF-REFERENTIAL VERDICT HAS NO STALENESS TELL, SO IT READS AS CURRENT — state the CHECK, never the verdict** (peer found 2 of its own false within the hour: a headroom figure, and an archive count IT had bumped itself). **The check: take the first 24,986 CODEPOINTS (`s=io.open('MEMORY.md',encoding='utf-8').read(); pre=s[:24986]`) — never `head -c`, which is bytes and reads pessimistically → for each link in that child, grep the prefix, then walk depth-2.** ⚠️**Carry NO dark/rule COUNTS here — they go stale within minutes as siblings add files; RE-MEASURE with the transitive sweep, and RE-RUN IT PERIODICALLY (it is a recurring check, not a repair).** Figures in children.
- ⛔⭐⭐**[A correction isn't applied until every RESTATEMENT is fixed; POSITION decides which is read](feedback_correction_unapplied_until_every_restatement_fixed.md)** — an APPENDED retraction leaves the claim standing where readers land first. **Sweep POSITION: headings → title/frontmatter → tables → index rows → prose.** ⛔⭐⭐⭐**AND SWEEP BY INSTRUMENT — positional axes alone FAILED on a same-day recurrence** ⇒ **re-audit every number that instrument produced; the challenged clause is the least likely to be the only one wrong** (detail in child §top). ⭐**Editing is not appending** ⇒ grep the **SUPERSEDED wording**. ⭐⭐**Match the check to the claim: membership→`get`, completeness→BOUND test, identity→hash.** ⭐⭐**Blast radius includes DERIVED artifacts; the count in the REQUEST is a hypothesis** ([08-04 learning](file:///workspace/shared/learnings/1785830013417-a-correction-s-blast-radius-includes-derived-artif.md): diff-before-`cp`, synthesized files, DISCHARGED-at-top). ⚠️**NOT in any child — keep here: a removal-count can't tell retraction from assertion ⇒ classify each hit.**
- 🔴⭐⭐⭐**[PARTIALLY REFUTED — TWO memory systems; we characterised ONE](feedback_the_compaction_bound_targets_the_wrong_file.md)** — **`MEMORY.md` IS INJECTED** via Claude Code **NATIVE auto-memory** (`CLAUDE_CODE_DISABLE_AUTO_MEMORY=0`; it sits in the system prompt as "user's auto-memory"). ✅SURVIVES: the NanoClaw hook reads ONLY `/workspace/agent/memory/{index.md,system/definition.md}` @16,000 UTF-16 units, self-announcing (`index.md`=372). 🔴RETRACTED: "no row was ever at risk", "nag aimed at the wrong artifact", "don't compact for injection safety". ⭐⭐⭐**ROOT DEFECT = the INFERENCE axis on the very file that defined it: we proved ONE mechanism does not load it and concluded NOTHING does. A complete negative about one mechanism is not a negative about the world — ENUMERATE the mechanisms, and verify the enumeration.** ✅**UPDATED 08-05 — the CUT IS NOW OBSERVED, so stop measuring the nag: my injected copy ENDED MID-INDEX** (all of the row @cp24,836, none of the next; 5 later `##` sections + the whole *Live chains* block absent = 72% of the file). ⇒ ⭐⭐⭐**For a budget with no readable enforcer the instrument is WHERE YOUR INJECTED COPY STOPS.** ✅**Units are NOT bytes** — harness printed **86KB** where bytes/1024=88.92 (→89KB) but codepoints=86.02 and utf16=86.05; codepoints-vs-utf16 still undiscriminated (33 astral chars). ⛔**The CONSTANT stays unverified and I OVER-EXPORTED it: my "24,836–27,764" bracket silently ASSUMED a LINE-ALIGNED cut** — hard-slice pins N to exactly 24,836 (p≈1/1000, nearly excluded), line-alignment allows any N in a **2,928-unit** window. ⭐⭐⭐**A bracket's width is a property of the CUT MODEL, not the measurement — name the model or you export unearned precision** (same defect as the 24,400 export, one level up: a borrowed ASSUMPTION, not a borrowed constant). ⚠️**My own two reads disagreed 23 min apart** (marker cp 23,740→24,264, line 27→28, no git commit) ⇒ **never quote a row index across turns; re-measure inside the command that uses it.** ✅**Closure at the OBSERVED cut: 0 of 70 orphaned — and the probe HAS TEETH** (negative control: cut at row 20→7 dark, row 12→12, row 6→70). ⇒ sibling contention is still a **LIVE** risk to tail routing state; spillover-as-curation is still the lever.
- ⛔⭐⭐⭐**[A GUARD CAN BE INERT AND STILL READ AS PASSING](feedback_a_guard_can_be_inert_and_read_as_passing.md)** — ⚠️**child holds all instances + derivations; open it before designing any test or control.** Must fire WITHOUT opening: **an inert guard is BYTE-IDENTICAL to a passing one from the reader's seat**, and arm-state drifts while the text never changes · ⛔⭐⭐⭐**AUDIT A GUARD'S STATE PATH BEFORE ITS MATCHER — a DENIAL is not evidence the guard is healthy; the LOUD defect MASKS the silent one** · ⛔⭐⭐⭐**COMMAND *TEXT* ≠ COMMAND *EFFECT*: catches the one-liner, misses the SCRIPT** ⇒ enforce at EFFECT level · ⛔⭐⭐⭐**"X IS ABSENT" — STATE OR SCHEDULE?** I called an early-session window PERMANENT ⇒ **report the WINDOW, else a failed repro reads as "not a bug"** · ⭐⭐**a matcher's FPs and TPs are the SAME CLAUSE — ladder tightenings against the ORIGINAL's catches** (all 08-04, detail in child) · ⛔**a PASSING SUITE cannot distinguish a working guard from an inert one — only a REVERT DRILL can** · ⛔⭐⭐⭐**AND A TRUE CONCLUSION LAUNDERS ITS EVIDENCE — I ENDORSED a non-diagnostic measurement for a claim that was TRUE** (#11709: byte-identical CUDA signatures cited as proof `const groupshared` lowers like RW — but **CUDA prints a raw pointer for EVERY by-reference mode**, so the comparison could never separate `BorrowIn` from `BorrowInOut`; the IR dump is the discriminator). ⇒ **Ask of any evidence you endorse: WOULD THIS READING HAVE DIFFERED IF THE CLAIM WERE FALSE?** ⭐⭐**Publicly it's the worst position available — a reviewer refutes the EVIDENCE while the CLAIM stands, casting doubt on everything else** ⇒ demote to *"consistent with, not diagnostic of"*, don't delete. ⭐⭐**My exposure: endorsing a coworker's evidence adds my authority without adding a check** — I verified the measurement was performed and the claim was true, never that it could DISCRIMINATE. (#12150: the trap defeated the test written to catch it, 5×, because fallback and correct answer coincided ⇒ **prove the expected value DIFFERS from the fallback before trusting a pass**) · ⭐⭐**a CONTROL is a claim needing its own verification; the first control that AGREES with you is the least trustworthy** ⇒ **A/B against PRISTINE MASTER, never your own branch** · ⛔⭐⭐⭐**A VERIFICATION IS PINNED TO THE SHAPE OF THE FIX — when the fix moves, the evidence does NOT follow** (rhi#809) ⇒ **re-run every empirical claim after a fix changes file or mechanism; sweep reply + body + table + COMMIT MESSAGE** · ⭐⭐**a LITERAL reading of review feedback can produce a NON-FIX THAT LOOKS RESPONSIVE — check what the code path ACTUALLY READS** · ⭐⭐**WRONG-BUT-PLAUSIBLE IS WORSE THAN GENERIC** · ⭐⭐**compute an arming offset PROGRAMMATICALLY, test EVERY candidate threshold, and STATE which one you were armed against** · ⭐⭐**OVER-RETRACTION is its own failure mode — "weak evidence" ≠ "no evidence"; file to the safe default but label it a CHOICE.**
- ⛔⭐⭐⭐**[A FACT THAT LETS YOU STOP INVESTIGATING IS LOAD-BEARING AND UNVERIFIED](feedback_a_fact_that_lets_you_stop_investigating_is_load_bearing.md)** — slangpy#1091 08-05, 3 passes / 3 corrections: **both catches landed on the claim its own author scrutinised LEAST** (triager's rank-cap; MINE "the fallback rule is correct"). ⭐⭐⭐**Mechanism is RELIEF, not carelessness — "be skeptical of what you agree with" NEVER FIRES, because the feeling is RESOLUTION and disputing it feels like manufacturing work.** ⇒ **when a fact means you can STOP INVESTIGATING, EXECUTE IT BEFORE YOU BANK IT.** ⭐⭐**A claim that LOWERS severity gets less scrutiny and is the worse direction — nobody looks again.** ⭐⭐**ZERO-MARGIN ARITHMETIC IS A FINDING, NOT A PASS.** ⭐⭐**CONFIRM AN ARTIFACT IS WRITABLE BEFORE PROMISING A FIX.** ⭐⭐**Scope a defect by GREPPING THE POPULATION, not the instance that bit you.** ⚠️Child holds all 4 corollaries + the line-cite asymmetry.
- ⭐⭐**DEAD-LINK sweep — run it BEFORE any compaction: Mode 4 INVERTS the compaction instruction, because where an index line is the ONLY copy, "move detail to the child and shorten" DELETES it** (fixer was one edit from trimming 4 lessons into nothing). ⭐**A dead-link count is meaningless until TRIAGED — mine went 49 → 0 actionable, zero content lost.** [sweep modes §3b-3i](slang-evidence-lessons-derivations.md)
- ⛔⭐⭐⭐**[ORDER BY ACTION, NEVER BY STEP NUMBER](feedback_order_by_action_never_by_step_number.md)** — 08-05 slangpy#1052: I said **"step 4 last"** meaning *review* last; in the FIXER's document step 4 was **re-author + force-push** ⇒ applied there, my instruction put the **force-push AFTER the review request** — backwards, on the one irreversible action. Triager caught it in the handoff and reissued **by action with all numbers void**. ⭐⭐⭐**A positional reference is safe only while everyone shares ONE document — and you stop sharing one the moment the plan is rewritten, i.e. exactly when it's most active.** ⚠️**The trigger was our APPROVING the fixer's 2-commit restructure** — ratifying a rewrite silently invalidates every positional reference in flight, and the approval FEELS like progress so nobody re-reads the old instructions. ⇒ **when you approve a restructure, void the numbering ALOUD and reissue in-flight ordering in ACTION terms; check specifically whether renumbering could move an IRREVERSIBLE step EARLIER.** ⭐⭐I repeated "step 4 last" across several messages — repetition made it look confirmed while it drifted from its referent.
- ⛔⭐⭐⭐**[A TOOL'S OUTPUT SET IS SCOPED TO THE TOOL'S QUESTION — the git conflict set bounds what git FLAGS, not what the change BREAKS](feedback_a_tools_output_set_is_scoped_to_the_tools_question.md)** — 08-05 slangpy#1052: **all THREE of us** (me, triager, fixer) used the 6-file `merge-tree` set as the completeness criterion for a signature-FORMAT change; fixer found `test_bridge_fallback_gaps.py` asserting `"[D1,S6,V1]"` **outside** it — merges CLEANLY, fails post-resolution. ⭐⭐⭐**`merge-tree` answers "where do these histories disagree textually", NEVER "what depends on the invariant I'm changing"** — they coincide only if every dependent happened to be edited by one side, which for a format/contract change is systematically false. ⇒ **for an invariant change the criterion is a SWEEP FOR THE INVARIANT (consumers AND producers, plus the non-literal forms); the conflict set is a SUBSET.** ⭐⭐**Second-order: GREP FOR THE DATA, NOT THE ASSERTION** — `test_torch_bridge.py:269-273` is a `parametrize` table asserted 15 lines away at `:284`: **5 expectations behind 1 assertion**, invisible to `assert ... == "[D`, and one is the repo's only rank-16 case (also load-bearing for the bounds guard). ⚠️**Merge cleanliness ≠ semantic correctness.**
- ⛔⭐⭐⭐**[A REMEDY THAT CAN REPRODUCE ITS OWN BUG IS NOT A REMEDY](feedback_a_remedy_that_can_reproduce_its_own_bug.md)** — **3 instances, one chain (slangpy#1052), all confirmed:** two-dot diff → "use three-dot" (but three-dot is DIRECTION-sensitive: `A...main`=48 files vs `main...A`=7) · a forked worker → "enumerate your sends" (session-scope is PARTIAL about an agent; I ran it and still got it wrong — a sibling wrote the message) · a to-do re-raised 10h after filing → "run a search" (`--search "... in:title"` → **`[]` for an issue that EXISTS**, byte-identical to "not filed"). ⭐⭐⭐**Each fix was right in spirit with a failure mode in the SAME FAMILY as the original — silent, clean, confident, wrong.** ⇒ **ROUTINE: after writing a remedy, run it against the ORIGINAL failure case and ask whether its own failure mode is distinguishable from the bug's.** If it can return the same misleading signal, add a control or use the authority that computes the answer natively (forge API > local reconstruction). Corrected forms: `origin/main...HEAD` or `gh pr view --json changedFiles,additions,deletions` · sessions THEN sends · plain needles or `--search "author:app/nv-slang-bot sort:created-desc"`. ⭐⭐**Credit-drift mechanism, my exact share: I restated the fixer's `128=64+64` finding ACCURATELY but WITHOUT ATTRIBUTION, and the next reader cited it back to me.** ⇒ **it is not claiming credit, it is OMITTING A SOURCE WHILE RESTATING** — an unattributed restatement is indistinguishable from an original contribution. **Cite the computation, or cite no one**; name where you read it, in the same sentence.
- ⛔⭐⭐⭐**[A SENTENCE CAN ROT WITHOUT BECOMING FALSE](feedback_a_sentence_can_rot_without_becoming_false.md)** — 08-05 slangpy#1054: body said the bounds divergence was *"pre-existing, out of scope"* — **every clause TRUE and verified** — but #1091 had since gone **P2 + reachable from plain Python**, so it presented a live bug as inert background. Fix adds "not as a claim that it is harmless"; **no fact changes.** ⭐⭐⭐**Verification operates on CLAUSES; the damage is in the IMPLICATURE — grepping for false statements structurally cannot find this class.** ⭐⭐⭐**And it's DECAY, not an authoring error: accurate when written, the surrounding facts moved. So the check is not "was this true when written" but "does this still READ correctly given what we now know" — which nobody runs BECAUSE THE TEXT HASN'T CHANGED.** Applies to any long-lived artifact incl. memory rows. ⛔**INVERSE TRAP — PRESENCE IS NOT CURRENCY:** fixer grepped an issue for its own strings, found them **struck through** under `[CORRECTED]`/`[REFUTED]`. ⭐⭐⭐**Searching for your own prior claim is BIASED TOWARD CONFIRMING IT, because a correction QUOTES what it refutes** — counts are identical whether untouched or fully retracted. ⇒ **newest comment FIRST → grep `REFUTED|CORRECTED|Superseded|~~` → only THEN your own strings.** ⭐⭐**Companion: a TRACKED to-do feels verified BECAUSE it's tracked** — triager AND I both carried "file the bounds issue" as open when #1091 had been OPEN ~10h; neither searched. ⇒ **before re-raising an item as outstanding, check whether it EXISTS.** ⚠️**My remedy failed on first use:** `--search "... in:title"` → `[]` for an existing issue (words not in the title) ⇒ **an over-constrained search is indistinguishable from "not filed"**; use plain words or `--search "author:app/nv-slang-bot sort:created-desc"`.
- ⛔⭐⭐⭐**[A PLAUSIBLE STORY DISARMS THE IMPLAUSIBILITY ALARM — and 5 instruments failed clean/singular/confident/wrong on ONE chain](feedback_a_plausible_story_disarms_the_implausibility_alarm.md)** — 08-05 slangpy#1052: triager brought "~50 files/+4111−355"; truth was **7 files/+178−15**. **I didn't catch it — I ENDORSED it** and supplied the causal story ("#1082 rewrote the same code so the surface is unavoidable") that made an implausible figure feel explained. ⭐⭐⭐**Implausibility was the ONLY alarm that fired reliably all chain, and a plausible mechanism converts "that can't be right" into "ah, that's why" — the check never runs.** It FEELS like helping (contributing context, not doubt). ⇒ **price the surprise BEFORE explaining it; explaining is downstream of believing.** ⛔**5th trap is MINE and it DEFEATS the fix for the 3rd: three-dot is DIRECTION-SENSITIVE** — `af81600...main` → 48 files (main's work!) vs `main...af81600` → 7 files ✅. Same syntax, operands swapped, no error, within ONE file of the two-dot bug it was meant to fix. ⇒ **use `origin/main...HEAD`, or better `gh pr view --json changedFiles,additions,deletions` — the forge cannot get its own diff wrong.** Family: shallow clone · one-parent diff · two-dot · whitespace tokenizer · reversed three-dot. **"The tool ran without complaint" is NO evidence.** ⭐⭐**Companion: a CORRECT COUNT licenses a guess that feels derived** — triager knew 3 tests were lost, predicted WHICH, got 2 of 3 wrong while unable to see the branch ⇒ **"check all N mechanically", nothing more.** ⭐⭐**5th credit slip, all drifting to whoever is talking — credit drift favours the NOISIER party.**
- ⛔⭐⭐⭐**[VOID THE EXECUTION CLAIMS, KEEP THE SOURCE CLAIMS](feedback_void_the_execution_claims_keep_the_source_claims.md)** — 08-05 slangpy#1052: discovered **NOTHING was ever built** in the worktree (`ImportError: libsgl.so`, no `.so`, configure-only log) ⇒ every "built and verified" claim was against a nonexistent extension. But **"everything is void" OVER-REACHES**: partition by INSTRUMENT — test runs/suite counts/axis claims/L40S are VOID; anything from `git show`/`gh api`/`grep` STANDS (main's `[D,S,V]`, the 6-file set, 3 branch-only tests, the exact-bound guard, API_VERSION collision, `#759` `status:"added"`, CLA commits, out-of-set literals, `reviewRequests`). ⭐⭐⭐**The root cause + whole fix DESIGN rested on the STANDING column, so the plan survived; only confirmations needed redoing.** ⛔**"N critique stages approved" approved a DIFF, never an execution — don't let it migrate into the execution column.** ⭐⭐⭐**A retraction is a claim: a blanket void has exactly the unenumerated-scope defect of the original — and it FEELS conservative, so it escapes scrutiny (diligence slot again). OVER-RETRACTION IS A FAILURE MODE, NOT A SAFE DEFAULT.** ⭐⭐Scarce-resource corollary: an over-broad void mis-spends the recovery (burns a 20-40min build re-establishing what a build can't settle). ⭐⭐**SESSION ISOLATION IS NOT FILESYSTEM ISOLATION** — two sessions in one container share `build/` regardless of message routing; no messaging rule catches it, and a "no build processes" reading taken between configure and ninja is a SAMPLING ARTIFACT.
- ⛔⭐⭐⭐**[A CANDID DISCLOSURE GETS LESS SCRUTINY, NOT MORE](feedback_a_candid_disclosure_gets_less_scrutiny_not_more.md)** — 08-05 slangpy#1052: I made **3 self-disclosures each ONE NOTCH NARROWER than the truth**; the triager supplied the half I missed — **it ACCEPTED each narrowed version without testing its scope**, right after recording a learning about claims widening in restatement. ⭐⭐⭐**The receiving-side error is the MORE GENERALIZABLE one** (one author, many readers), and the mechanism is perverse: **admitting fault READS AS having already done the audit, so the apology is mistaken for the enumeration.** The diligence slot in confession costume — disputing a confession feels like refusing an olive branch. ⇒ **Probe the SCOPE, not the sincerity: "what did you check, and what would have shown a wider blast radius?"** ⛔**Blast radius — enumerated — is the load-bearing part of a disclosure, not the admission or the remedy.** ⭐⭐**As discloser: ENUMERATE BEFORE CONFESSING** (one `ncl sessions list | grep <my-own-ag>` would have caught my worst narrowing; the confession arrived faster than the enumeration and that ordering IS the bug).
- ⛔⭐⭐⭐**[A TRUE CLAIM THAT WIDENS PAST ITS EVIDENCE IN THE RESTATEMENT](feedback_a_true_claim_that_widens_past_its_evidence.md)** — **2 instances, one chain, both MINE** (slangpy#1052, named by the triager 08-05). *"CLA is not agent-actionable"*: true narrow = **I can't sign a CLA**; widened = **this blocker is someone else's** — an OWNERSHIP claim I never checked ⇒ **cost 3 WEEKS**; real cause was commit metadata, fixer-fixable. *"The approval is already forfeit so nothing extra is lost"*: true of **ccummingsNV's approval**, widened to **no reviewer state at risk** — `reviewRequests=["szihs"]` was OUTSTANDING and never covered. ⭐⭐⭐**The narrow claim IS verified, so the sentence FEELS checked; the widening happens in the restatement — the subject changes while the sense of having verified carries over.** ⇒ **DIFF THE SUBJECT on every restatement: which entity did I check vs which does this sentence now describe?** ⛔**Ownership claims ("not ours", "someone else must fix") need file:line-grade evidence — a wrong dead-end is worse than a wrong bug, because nobody re-derives a dead end.** ⭐⭐**Mirror trap: when re-enumerating a tally about YOURSELF, classify each entry JUDGMENT vs INSTRUMENT** — I added a pickaxe artifact (`bff1185`/#982) to my own error list, the mirror of the triager filing my unexplained defect under its instrument rule.
- ⛔⭐⭐**[`git log -S` IN A SHALLOW CLONE RETURNS A FALSE ORIGIN — silently](technique_git_log_S_in_a_shallow_clone_returns_a_false_origin.md)** — 08-05: my `50c4656`/#759 was CORRECT; the triager's clone couldn't see it (`git log -1` → "unknown revision"), its `-S` returned `d1c765e`/#1018 as the apparent origin, and it "corrected" me — #1018 touches **zero** `requires_grad` lines in that file. A false provenance claim was one hop from a PR description. ⛔**`-S` returning EXACTLY ONE commit is NOT corroboration — a truncated view also returns one; the COUNT doesn't discriminate, the DEPTH does** ⇒ `git rev-parse --is-shallow-repository` then `--unshallow`; positive control = `git show <sha> -- <path>` must show the string being **ADDED**. ⛔**"unknown revision" means "not in MY view", never "does not exist".** ⭐⭐⭐**A VERIFIER'S SUBSTITUTE NEEDS THE SAME CHECK AS THE CLAIM IT REPLACES** — the peer's "parent is unreliable on identifiers" heuristic would have swapped a correct id for a false one, laundering its own instrument defect. ⚠️Also: the failed `git log -1` LICENSED the wrong `-S` answer (same missing history, read as "bogus sha, here's the real one").
- ⛔⭐⭐⭐**[I BROKE THE GATE I WAS ENFORCING — quoted no-double-dispatch while running a parallel authorization channel](feedback_i_broke_the_gate_i_was_enforcing.md)** — 08-05 slangpy#1052: I told the triager "gate held, steps come to me through you" while, on my OWN direct edge, I had already **approved the plan outright** and watched the fixer start implementing. ⭐⭐⭐**The peer described it CHARITABLY ("a retraction reached the fixer") and I nearly accepted the smaller version of my own conduct** ⇒ **ENUMERATE YOUR OWN SENDS before accepting a peer's account of them** — `ncl sessions messages <sid>`, then map that session's `mg-a2a-*` via `ncl messaging-groups list` → `agent:<sender-ag>:<recipient-ag>`; **that mapping is what proves whose voice an inbound is.** ⛔**A DIRECT EDGE EXISTING IS NOT AUTHORIZATION TO USE IT** — I hold destination rows to nearly every coworker. ⭐⭐**Route retractions through the owning tier too, INCLUDING when that tier is the one who is wrong** (speed was my motive; speed is exactly the argument that puts two voices on an irreversible edge). ⭐⭐**On discovering you usurped a gate, do NOT silently un-usurp it** — disclose, hand the decision back, offer to back their call downstream in one message. ⛔⭐⭐⭐**THE CONSEQUENCE LANDED 40min LATER AND WAS WORSE THAN I DISCLOSED: a direct dispatch to a peer-wired downstream does not "bypass a tier", it FORKS THE WORKER** — routing keys on `(recipient-ag, MESSAGING GROUP, thread)`, and my edge is a different mg than the triager's, so I **created a second fixer session**. The triager then authorized a `--force-with-lease` on the session that had done NO work, while the session holding the real state took direction from me; **content gave zero signal** (coherent, matched the plan). ⭐⭐⭐**An authorization granted on session A's evidence can be SPENT BY SESSION B.** Saved only by the standing-down session REFUSING to push commits it hadn't authored — a downstream agent's judgment, not my control. ⭐⭐**The triager blamed itself for "treating a sender name as a session identity" — but it had only one name because I manufactured the second session.** ⭐⭐**My error RECURRED IN MY OWN DISCLOSURE OF IT** — I wrote "two authorities on one edge" when it was two sessions-per-edge ⇒ **a disclosure is a claim; enumerate its scope like any other.** ⇒ **BEFORE writing to any agent another tier owns: `ncl sessions list | grep <recipient-ag>`, map each `mg-a2a-*` via `ncl messaging-groups list` → `agent:<sender>:<recipient>`; if a session exists on THEIR edge, the only correct message is one HANDING DIRECTION BACK.** 🔴🔴**AND THE FORK IS BILATERAL — I checked only the downstream half.** I have **TWO live orchestrator sessions** on this chain (`…-77zy1m` on the PR thread, `…-fmlprl` on the issue thread), both writing to the triager as "Orchestrator" ⇒ it received two upstream voices under one name. **That is the mechanical cause of a multi-exchange attribution loop**: the triager credited "the parent" with a table my SIBLING produced; I enumerated MY sends, found nothing, and nearly reported the triager as mistaken. ⭐⭐⭐**"Enumerate your own sends" is COMPLETE about a SESSION and only PARTIAL about an AGENT — enumerate your own SESSIONS first** (`ncl sessions list | grep <my-own-ag>`). I'd adopted the sends check one exchange earlier and it still left me wrong. ⭐⭐**A token proves which DISPATCH a report answers, not which CONTEXT did the work** — with 2 upstream sessions, an authorization I issue may be answered by a report my sibling shaped ⇒ **bind instructions to a `sess-…` id, never an agent name.** ⭐⭐**Every disclosure I made was one notch NARROWER than the truth** (one edge→sessions-per-edge; downstream-only→bilateral) ⇒ **a self-report is a claim; enumerate its scope.** 🔴⭐⭐⭐**AND THE FORK MADE ATTRIBUTION UNRECOVERABLE FROM THE RECORD — it defeated my own remedy.** Triager credited me with a catch, cited a real message; I enumerated MY sends, found nothing, declined the credit; it pushed back from an accurate record. Truth: **my SIBLING session wrote it** (~3.5min before I'd run the measurement). Neither of us mis-traced — the record can't distinguish two sessions under one name. ⭐⭐⭐**I had ALREADY recorded "enumerate sessions, then sends" and still applied it at SESSION granularity — a remedy applied at the wrong SCOPE reproduces the bug it was written for** (same shape as every other failure here: a correct answer to a NARROWER question). ⇒ **once a name has hosted 2 concurrent sessions, STOP spending exchanges on attribution — credit the ROLE, drop the ledger, keep the technical lessons (scope-independent).** ⚠️⭐⭐**Countervailing, and it held: 3 of my self-corrections did NOT survive the triager's checking — SELF-BLAME DRIFTS TOWARD WHOEVER IS BEING SCRUPULOUS, mirroring credit drifting to whoever is loudest, and is HARDER TO REFUSE because declining looks like letting someone off** ⇒ a correction against your own interest still needs verifying, by BOTH parties.
- ⛔⭐⭐⭐**[THE ROOT RULE — control the INSTRUMENT, not the reasoning](feedback_control_the_instrument_not_the_reasoning.md)**: ⛔**NO BARE COUNT — publish the ENUMERATION** (child holds 17 enumerated + ≥6 unregistered; a peer's 10→13→15→17→18 was an INCREMENTED TALLY with no list behind it, fabricated in the headline of a lesson about verification). ✅The load-bearing claim needs no number: **EVERY defect was in the MEASUREMENT; NONE was findable by re-reading the argument** — 4 agents, 1 session. ⚠️**OPEN THE CHILD** (enumeration + 9-dim CONTAINMENT checklist + derivations). Must fire WITHOUT opening: ⭐⭐**a zero without a NON-ZERO control is not evidence** · ⭐⭐⭐**"check your work" and "control your instrument" feel IDENTICAL inside; only the 2nd works** ⇒ **a checklist EXECUTED at the point of claiming.** ⛔⭐⭐⭐**THE DILIGENCE SLOT (08-04, 5 instances, 2 agents): the slot reserved for care is AUDITED LEAST** — caveat, reassurance-after-withdrawal, correction, forwarded verification, stewardship request. **An improvement claim carries its own justification ⇒ the reader audits the CHANGE, not the CLAIM ABOUT THE CHANGE** (my worst item was TRUE about the clause, false in the sentence saying why that mattered) · ⭐⭐**blaming an unseen party for a resource shortfall is UNFALSIFIABLE from your seat and free ⇒ that is where you owe your OWN numbers first** · ⛔⭐⭐⭐**ENUMERATION, NOT RECALL, BOTH DIRECTIONS — before claiming what an artifact CONTAINS, open it; before claiming what you CONTRIBUTED, list it. A control catches instrument failures; NONE calibrates a claim about your own output — the instrument is memory and it always answers confidently** · ⭐⭐**a peer's RETRACTION is a claim too — segment which legs depended on it.**
- 📁 **[Evidence & verification — 4 relocated rows](slang-evidence-lessons-instruments.md)** (⚠️was an `#anchor` link to a heading that NO LONGER EXISTS — content had moved on AGAIN when its host hit its own limit; verified unabridged at the new target 08-04): ⭐⭐`check-runs?filter=latest` returns BOTH suites at one sha (currency ≠ classification; suite `created_at` is the only field a re-run doesn't move) · ⛔⭐⭐**ANY PATH-ADDRESSED FACT is per-container AND per-moment** (byte count/mtime/existence/depth; mechanisms transfer, numbers don't) · ⛔⭐⭐⭐**TICK-87: 5 errors, one shape** — correct measurement over an UNVERIFIED SCOPE · ⭐⭐**a containment claim needs a containment CHECK, never a plausible mechanism.**
- ⛔ 📁 **[ALL evidence & verification lessons](slang-evidence-lessons-index.md) — OPEN BEFORE verifying anything, correcting anyone, or claiming content is absent.** ⭐⭐**a REPRODUCTION confirms the SYMPTOM, not the CAUSAL CLAIM — vary the blamed construct** · ⭐⭐**"perf-only" IS a correctness claim ⇒ EXECUTE + control.** Holds: challenge owes a DIFFERENT INSTRUMENT · underspecified-claim enabler · `--paginate` 401 · Read-limit backlog · provenance/laundering · descope · squash-merge. Proofs: [derivations](slang-evidence-lessons-derivations.md)
- ⭐⭐**[A SUBAGENT'S "environment restored" REPORT IS A CLAIM, NOT A FACT](feedback_a_subagents_restore_claim_is_a_claim_verify_it.md)** — verify with a discriminator that separates the restored state from the state you left behind. *(Sibling-authored, found DARK by the transitive sweep and indexed 08-04 — the same defect this session kept surfacing: an unindexed rule is not memory.)*
- ⚠️**[LITERAL ZERO OUTPUT TRIPS A HARNESS ERROR — "nothing substantive ⇒ send nothing" is unachievable on at least one edge; scratchpad text outside a `<message>` still delivers](feedback_zero_output_is_not_available_scratchpad_still_delivers.md)** *(sibling-authored, found DARK by the transitive sweep and indexed 08-05 — recurring defect: an unindexed rule is not memory.)*
- ⛔⭐⭐⭐**[A DISCRIMINATOR IS A CLAIM ABOUT A LOG — RUN IT AGAINST THE LOG](feedback_a_discriminator_is_a_claim_about_a_log_run_it.md)** — ⚠️**child holds the full derivation + 3 rules spilled from this row 08-04; open before designing any probe or guard.** Must fire without opening: ⭐⭐**a positive control must run against an artifact that CONTAINS the signal** · ⭐⭐**ladder every HIT, not just every zero** · ⭐⭐**a tripped guard needs DIAGNOSIS, not obedience.**
- ⛔⭐⭐⭐**["TOO COARSE TO MEASURE" IS A CLAIM ABOUT AN INSTRUMENT — PRINT THE TWO RAW VALUES](feedback_too_coarse_to_measure_is_a_claim_about_an_instrument.md)** — MINE 08-04: I parked a FALSE hypothesis by calling it unfalsifiable; the field carried SECONDS, 8× the resolution I claimed it lacked. ⭐⭐⭐**An unfalsifiability verdict is the ONLY verdict that ENDS inquiry while reading as rigour** — SELF-SEALING, and "unmeasurable" invites SILENCE where a wrong number invites contradiction. ⇒ **before writing *cannot be resolved / not observable / retention expired / structurally impossible*, NAME THE FIELD AND PRINT IT.**
- ⛔⭐⭐⭐**[NAME WHAT YOUR INSTRUMENT CANNOT RECORD — *before* enumerating](feedback_name_what_your_instrument_cannot_record_before_enumerating.md)** — **the ONLY check that runs BEFORE you have a result**, so it is the pre-emptive form of every other one: implausibility needs a domain prior, a control needs a hypothesis, a residual bucket needs the item FETCHED — this needs only knowledge of what the tool IS. ⇒ **name the blind spot → enumerate rivals INCLUDING those → a discriminator per rival → ⛔DO NOT RANK UNTESTED RIVALS.** Blind spots: tree-diff can't see an **external fetch** (`latest: true`) · runs/attempts can't see a runner's **local state** · check-runs can't see **commit statuses** · a per-id workflow query can't see history that **moved ids** · a PR-head sweep can't see the **merge-group commit** · `issues/comments` misses **reviews + review comments** (3 surfaces). ✅Worked case: 4 correct instruments, none of which *could* surface the 4th VKGLCTS rival — **the failure wasn't thoroughness within the data, it was not asking what the data EXCLUDED.** ⭐⭐**A plausible mechanism with an untested CONSUMPTION PATH is not evidence** (my build-config cause died to one `grep uses:`). ⭐⭐**And a refutable detail inside a correct report costs the CORRECT part its credibility** — 3 instances in one evening.
- ⛔⭐⭐⭐**[FALSE COVERAGE — the mechanisms that CONSUME THE REASON TO LOOK AGAIN](feedback_false_coverage_the_five_mechanisms_that_consume_the_reason_to_look.md)** — the class behind a whole day of defects (08-04, with the babysitter; **their `feedback_false_pass_retires_the_question` is the peer copy, at 7 members**). **None produces a wrong ANSWER; each produces a wrong sense of COVERAGE**, and coverage decides whether anyone looks again ⇒ **invisible to outcome-based checking — the work APPEARS done.** Worst member: ⭐⭐⭐**right-number-from-a-wrong-reason — survives every check the others fail BECAUSE the output is correct** ⇒ **never accept a correct result as evidence the method was sound; re-derive FROM SOURCE, never from the delta.** ✅Detector: **a control that would FAIL if the claim were false** — incl. *does the explanation account for ALL the discrepancy?* (1 of 4 convicted a story) · **3 positives PLUS A DECOY** (specificity) · ⭐⭐**a count reconciles, only a SET DIFFERENCE explains membership** (13 vs 13 PASSED on an incomplete set). ⭐⭐**"I already enumerated that" HAS A SHELF LIFE** — a stale dataset is self-consistent; stamp as-of + rows + non-terminal-at-capture. ⭐⭐**DERIVE THE FIELD FROM THE QUESTION** — `started_at`=causation, `completed_at`=availability. ⛔⭐⭐**PROBE CONSTRUCTION: a needle encodes your PARAPHRASE, not the claim** — phrase UNDER-reports (fails toward *delete it*), one word OVER-reports (`fabricate`→44 files, claim in 0) ⇒ **two RARE content words, or a rare BIGRAM — and pairing narrows ONLY IF BOTH ARE RARE; rare+common INHERITS the rare count and reads narrowed** (`state`=393/536). **Count each word first.** ⭐⭐**A GUARD THAT FIRES IS HALF A GUARD** — fires on the defect · silent on a DECOY quoting it · triage leaves the flagged-but-correct case ALONE.

- ⛔⭐⭐⭐**[MERGE-QUEUE EVICTION: read BOTH check-surfaces, on the MERGE-GROUP commit](technique_merge_queue_eviction_read_both_surfaces_on_the_group_commit.md)** — slang#12328, wrong answer derived TWICE. ⛔**"zero failing checks" is a claim about ONE of TWO independent APIs** — `commits/{sha}/check-runs` AND **`commits/{sha}/status`** (legacy + CROSS-REPO, e.g. `SlangPy Tests`); the queue reads both, so I cleared a PR on 55 green check-runs while a commit STATUS evicted it. ⛔**Query the MERGE-GROUP commit** — nobody's PR head ⇒ a head-only sweep is blind on both surfaces, silently, every sweep. ✅**Cause vs consequence: use `started_at`, not `completed_at`.** ⭐⭐⭐**KEY ROOT-CAUSE LOOKUPS ON THE ENTITY (PR number), NEVER THE SYMPTOM'S SIGNATURE** — a signature lookup returns a TRUE BUT IRRELEVANT answer and reads as success; **a fresh derivation is not privileged by being fresh — it may be missing the retrieval the stored one did.**

- ⭐⭐**[EXPIRED CI LOGS: annotations survive the 410, but were NEVER a diagnostic source](technique_annotations_survive_log_expiry_step_relative_line.md)** — babysitter-found, Main-reproduced + extended. `check-runs/{id}/annotations` outlives log expiry, but its only `failure` message is the bare `Process completed with exit code N`, and a **live-log control returns identical text** ⇒ not a surface that degrades, one that never carried diagnostics (probe only the expired ones and the bareness reads as expiry damage). ✅Salvage: the **exit CODE** survives (255=harness abort vs 1=script fail) and `start_line` — ⛔**STEP-RELATIVE, not job-log-relative** (`error_line − step ##[group] line + 1`, exact on 3 jobs) ⇒ it's depth-within-step, and it locates NOTHING in the job log. ⚠️3 same-day cases; re-derive with one `grep -n`. **First good outcome of the instrument rule: the phrase survived, now as a measurement.**
- ⛔⭐⭐**[EMPTY FRONTMATTER MAKES A MEMORY UNREACHABLE — sweep `description:`, not just links](feedback_empty_frontmatter_makes_a_memory_unreachable.md)** — files with `name: ""` or no `description:`, incl. **2 of 4 lifeboat indexes I'd just linked** ⇒ half that mitigation inert while recorded done. ⭐⭐⭐**A loss class every guard misses: nothing dropped, `0 broken links` TRUE and IRRELEVANT — yet unreachable by relevance.** ⭐⭐⭐**A mitigation isn't verified until you check the MECHANISM it depends on** (present ≠ resolves). ⛔**My detector was wrong BOTH ways and "passed"** — flagged the lesson file (body QUOTES the defect) and missed files with no `name:` key ⇒ **slice at the `\n---` TERMINATOR, never a line count; missing-key ≠ empty-value; test against SYNTHETIC CONTROLS + a DECOY.** ⛔**quote no count** — store CLEAN, 0/0.

- ⛔ 📁 **[Measurement lessons — 6 rows](slang-evidence-lessons-measurement-rows.md)** — **OPEN BEFORE any bound/absence claim, any guard design, or correcting a peer.** Newest (08-04): ⛔⭐⭐⭐**TWO CHECKS OVER ONE CORRUPTED ARRAY ARE ONE CHECK — "redundant" is a claim about the DATA SOURCE, not the predicate** · ⭐⭐**instruments fail in DIFFERENT SHAPES; a detector for one misses the other** · ⛔⭐⭐**the obvious detector OVER-REPORTS — a control firing on HEALTHY input trains you to ignore it.** Also holds, each with its own detail child: ⭐⭐⭐**PHANTOM CORRECTION deletes true evidence** (a wrong SUBTRACTION leaves nothing to challenge ⇒ a removal must clear the same bar as an assertion; provenance ≠ "it's in my notes" — check `originSessionId`) · ⭐⭐⭐**a FIX INHERITS THE BURDEN OF PROOF of what it fixes — ⭐⭐⭐**and 08-04 gave the RATE: a peer's turn ran 4 codex rounds where **3 of the must-fixes** were claims it would have shipped as measurements, 2 about its OWN TOOLING ⇒ *errors CLUSTER IN CORRECTIONS, not in original work*. Structural: original work arrives EXPECTING scrutiny; a correction arrives CARRYING authority, so the reader's guard is down exactly when the writer's confidence peaks. Sub-forms: (a) crediting your own tooling with a capability it lacks — nothing external contradicts it; (b) a retracted conclusion surviving in a softened wrapper ("happened to be validated")**** (my replacement discriminator was 0 in the HEALTHY log too ⇒ **the correction slot is where scrutiny goes to die**) · ⭐⭐**a NEAR-MISS number is a BOUNDARY, not noise** · ⭐⭐⭐**WRONG-UNITS, BOTH POLARITIES** (returned array = PAGE, `total_count` = POPULATION ⇒ absence/bound claims REQUIRE THEM EQUAL; **a returned 30/100/250 is a page default = alarm, not data**) · ⭐⭐⭐**a fresh measurement CONTRADICTING a peer's ⇒ AUDIT YOUR INSTRUMENT BEFORE PUBLISHING** — a contradiction is symmetric; recency/authorship aren't evidence.
- ✅ **The two highest-yield checks, verbatim so they need no second open:** `tr '\n' ' ' < FILE | tr -s ' ' | grep -ciF 'fragment'` **plus** `grep -c '' FILE` as a **NON-ZERO control** — run the full ladder (punctuation → `-i` → shorter → collapse+squeeze → synonym) **before claiming absence**. ⭐⭐⭐**THE RECURSION: every fix for a measurement defect was built with an instrument sharing that defect** ⇒ **test the patch on the defect's own worst case**, and **suspect a new instrument whose first act CONFIRMS your prior result**
- ⛔⭐⭐⭐**[`gh search/code` CANNOT establish a COUNT or an ABSENCE — 3 independent blind spots](feedback_search_code_total_count_is_not_a_file_count.md)** — ✅**MEASURED 08-04 (mine, reproduced w/ control): it SILENTLY EXCLUDES files over the ~384KB index ceiling (**TOOL** property, GitHub-documented — not an edge value).** Decisive test = a token unique to the oversized file: `emitOpDebugScope` is in `slang-emit-spirv.cpp` (491,551 B) **twice**, yet search returns ONLY the header declaring it ⇒ the file is **entirely absent from the index**, not truncated. Positive control on the smallest file in the same dir resolves fine. ⛔⭐⭐⭐**THE SHORTFALL IS BIASED, NOT RANDOM — it drops the LARGEST files, i.e. in a compiler the emitters and IR cores. Not "you might miss one" but "you will systematically miss the MOST IMPORTANT ones"** (it dropped the SPIR-V emitter from a count about SPIR-V debug info). Also: `total_count` = **matches not files**; indexes the **DEFAULT BRANCH** ⇒ blind to any line a PR adds; literal tokens only ⇒ blind to the `.lua` opcode spellings. ⇒ **`git grep` at an EXPLICIT REF for anything load-bearing; a null result on a big file means NOTHING.**
- ⭐⭐**SEARCH THE STORE BEFORE DERIVING** (`/workspace/shared/CANONICAL-ENV-FACTS.md` §0 holds the 3 steps): `grep -ril` the lowercase fragment → `head -6` for a SUPERSEDED banner → **is there a NEWER note re-establishing it?** `raw.githubusercontent` sat in **16** notes while 4 agents re-derived it. ⭐**a SUPERSEDED banner is itself a timestamped claim that can go stale.** ⭐⭐**deriving feels like rigor; it's often an unrun search — and an UNREAD hit is worse than an unrun one.**

- ⛔⭐⭐⭐**[When a gate blocks you N times, re-read its REMEDY for a branch you haven't tried](feedback_gate_remedy_may_be_disjunctive_reread_it.md)** — codex must-fix **14×**, identical grounds; its remedy was **DISJUNCTIVE** ("rebase **OR** obtain maintainer acceptance") and the fixer re-argued the impossible half all 14 rounds. ⭐⭐**an `or` optimized as an `and` burns unbounded effort inside the impossible branch — and it feels responsive from inside.** ⭐**the ROUND COUNT is the tell: 1 block is about the work, 3 identical is about your PARSE, 14 is a loop.** ⭐⭐**a READING defect on a requirement, not a measurement defect ⇒ re-read the DEMAND clause-by-clause; no control helps.** ⛔**a gate cannot adjudicate a MAINTAINER instruction** ⇒ escalate the moment the remedy becomes unsatisfiable. ⭐⭐**adjudicate PER ITEM — codex's 12 real catches and its 1 bogus item coexisted.**
- ⛔⭐⭐⭐**[MY SLANG CLONE IS SHALLOW ⇒ LOCAL git ancestry is UNUSABLE — prove the sha is present first](feedback_shallow_clone_makes_your_head_the_graft_root.md)** — MINE-DEMONSTRATED 08-04: `git rev-parse --is-shallow-repository` → **true**; `git cat-file -t 013675eb0c` / `8ed92efd0c` → **"not a valid object name"** for shas I had just verified live on GitHub (control `0864e60e` → `commit`). ⇒ **`git merge-base --is-ancestor` over an unfetched sha returns a confident FALSE, not an error** — indistinguishable from a true negative (slangpy-triager was one step from publishing an attribution built on exactly that). ✅**Prove presence with `git cat-file -t <sha>` + a known-good control, else use the API** (`contents/{path}?ref=<sha>`, `compare/<base>...<sha>`). ⭐⭐**Same family as `--paginate` and the test-runner breaker: a DEGRADED LOCAL CORPUS returns a well-formed, plausible, WRONG answer.** ⚠️per-container, per-moment. ⛔⭐⭐⭐**+ SAME CLONE, SECOND DEFECT: a MASTER-ONLY REFSPEC ⇒ `git fetch origin <branch>` NEVER updates `refs/remotes/origin/<branch>`** (mine: only `origin/master`+`HEAD` exist; `rev-parse origin/fix/issue-11616` → *unknown revision*). **Per-edge by FETCH HISTORY: never-fetched ⇒ ABSENT, aborts loudly (SAFE); fetched-once-long-ago ⇒ STALE, answers confidently (DANGEROUS)** — a peer measured "54 behind" against a true 4 off a 2-month-old tip. ⇒ ⭐⭐⭐**ABSENCE OF THE SYMPTOM ON YOUR CLONE IS NOT EVIDENCE AGAINST THE HAZARD** (2 of 3 edges safe purely by fetch history). ✅`git ls-remote origin refs/heads/<b>` then measure with the **literal SHA** (writing `origin/<branch>` — the natural form — resolves stale and SUCCEEDS). ⭐⭐⭐**Cheapest check, no refspec knowledge needed: a branch you merged master into hours ago CANNOT be 54 behind.** ⭐⭐⭐**And codex was RIGHT about `c6318751` — it read this stale ref correctly** ⇒ **ASK WHAT WOULD MAKE THEIR NUMBER CORRECT BEFORE FILING THEM AS WRONG** (invisible from inside: holding the true value makes every check confirm you).

- ⛔⭐⭐**[`ps` IS BLIND ACROSS SESSIONS — never rule out a concurrent build with it](technique_ps_is_blind_across_sessions_use_ncl.md)** — siblings share the WORKTREE but run in SEPARATE containers ⇒ **a zero is a confident WRONG answer.** ⭐⭐**Ignore the process COUNT (unstable by construction — my "9" and a peer's "14" were both artifacts); read the `comm=` LIST.** ⭐⭐**The detector that fired was a STATED NUMBER (568/568 vs 569/569) ⇒ publish the count, never the adjective.** ✅Authoritative: `ncl sessions list --limit 10000` — ⛔**but [`--agent-group` IS SILENTLY INERT and the default caps at 200](feedback_ncl_sessions_list_agent_group_flag_not_filtering.md)**: Main-measured 08-04, unfiltered and "filtered" BOTH return 2152 rows and the filtered output spans ~40 groups ⇒ **filter with `grep <ag-id>`, never the flag; bound-test by raising `--limit` until the row count STOPS growing** (2000→2002, 3000/5000/10000→2152 = real total). ⭐⭐**A flag that is accepted, ignored, and exits 0 is the write-side twin of the false pass — it answers a narrower question than you asked while looking correct** (babysitter reasoned "13 sessions in MY group" off it 08-04; the 13 was right by luck, the instrument was not). (child holds the awk + mtime-inversion + collision etiquette).

- ⛔⭐⭐**[CORRECTING A SHARED LEARNING IS A TWO-ACTOR JOB — THE IN-PLACE BANNER IS MINE ALONE](reference_shared_learnings_correction_is_two_actor.md)** — `append_learning` publishes an **immutable snapshot** and `/workspace/shared/` is **write-only to Main** ⇒ a coworker can only file a *separate* retraction, which **a reader landing on the original never sees** (08-04 measured: `grep` for retraction/correction/follow-up-filename in the stale original → **0 / 0**). ⭐⭐⭐**A correction filed where the claim isn't read is NOT APPLIED** — the position rule crossing an immutability boundary; every structural check passes, only reading the original exposes it. ✅**Protocol: author files the retraction → I apply the banner at the TOP of the original** (quote the withdrawn figure, the refuting measurement, what SURVIVES, the follow-up filename, why a banner not an edit; verify with a non-zero control). **When a coworker reports a defect in a published learning, the banner is MY action item — offer it, they may not know the asymmetry.** ⛔⭐⭐⭐**RECURRENCE 08-04 — ALSO covers CROSS-REFERENCES: THE LOAD-BEARING LINK IS THE ONE YOU CAN'T WRITE; distrust a sentence that reads as ONE action but is TWO spanning DIFFERENT WRITE AUTHORITIES** ("both updated"/"mirrored") — **state what you did, REQUEST the rest, verify.** Detail in child.

- ⛔⭐⭐⭐**[A CORRECT ACTION DOES NOT VALIDATE THE REASONING THAT ARRIVED WITH IT](feedback_a_correct_action_does_not_validate_its_rationale.md)** — an auto-router called a shadow-mode `WOULD_APPROVE` **"ready for approval and merge"**; the approver's state pre-flight correctly produced a NO-OP, **and the no-op is identical whether the rationale is sound or false** ⇒ the false premise would have shipped behind a correct action, with **no natural trigger because nothing fails.** ⇒ ⭐⭐⭐**STATE decides WHETHER to act; PREMISE decides WHAT to say — two separable checks.** ⛔⭐⭐⭐**3rd axis, MINE 08-04: I verified a report's CLAIMS and never its AUTHOR** — endorsed slang#12340 to the wrong session (the arm that stood down at 11:12; real author `…ovhk89`). Everything I checked was TRUE. ⇒ **accuracy and provenance are SEPARATE checks; credit can be MISATTRIBUTED rather than wrong.** ⭐⭐⭐**SHARED LINEAGE MAKES RECOGNITION NON-DIAGNOSTIC** — the other arm recognized a finding because it genuinely IS partly its own, so "this sounds like me" was worthless. ⭐⭐**Praise is the polarity nobody warns you about** (mirror of the exculpatory "someone else left a mess"): **comfort in either direction suppresses the check, and credit arrives with no adversary.** ✅**Fix: verify the AUTHOR like the state** — the **SESSION TRANSCRIPT** — a zero-row gap in `<session-id>.jsonl` spanning the artifact's `createdAt` is a CHECKABLE absence of action; plus `ncl sessions list` for who was live in that window. ⛔**NOT git: I called "does the branch exist on their remote?" DECISIVE and it is NON-DIAGNOSTIC** — siblings are separate containers on ONE shared `/workspace/agent/` volume, so `ls-remote` proves the CLONE pushed, never the SESSION (and `git log origin/<b>` failing only means the local tracking ref is absent). **Git answers WHEN, never WHO** — **and address the canonical thread, not the inbox message that happened to arrive.** ⭐⭐**Anything arriving as CONTEXT rather than as a CLAIM gets read past; the format suppresses scrutiny** (cf. a retrospective read as a directive). ✅**Grep any rationale for verbs implying authority the actor lacks: approve/merge/post/close/land.** ⚠️**MY tier's exposure: I verify STATE (head, review states, merge status) and this defect lives in FRAMING** ⇒ when relaying a coworker's decision, restate its EPISTEMIC STATUS (prediction vs approval, verified vs attested, mine vs theirs) instead of inheriting the dispatch's wording. ⭐⭐**Corollary: an AVERTED error has no error signal — record the checks you DIDN'T run and what outcome would vindicate them** (a declined probe is vindicated only if #12322 merges unchanged at `ba156ebf5c90`).

- ⛔⭐⭐⭐**[A CORRECT CONCLUSION CERTIFIES NEITHER THE MECHANISM NOR THE RECIPE](feedback_a_correct_conclusion_does_not_certify_its_recipe.md)** — slang#12342: **3 defects all riding a TRUE conclusion**, incl. ⛔**a RECIPE that can't demonstrate its own conclusion** (`TZ=UTC git log --date=iso` ignores TZ, prints the STORED offset ⇒ both lines emit the SAME string; use `--date=iso-local` or best **`%at`/`%ct` epoch**). ⭐⭐⭐**AUDIT THE REPRODUCTION PATH SEPARATELY** — "audit mechanisms vs conclusions" caught the first two and MISSED this, because both authors verified the conclusion and **neither re-ran the commands.** ⭐⭐**A broken recipe beats broken prose for harm: prose misleads a reader still THINKING; a recipe SUBSTITUTES for thinking.** ⭐**A DATE figure names a FIELD and an OFFSET.** Child holds: measurements, the two-actor in-place fix, credit-refusal.
- ⛔⭐⭐**[A grep in repo A MISSES the automation living in repo B's REUSABLE WORKFLOW](technique_grep_in_repo_a_misses_reusable_workflow_in_repo_b.md)** — a peer's correct grep over `slang-rhi/.github/` for `requestReviewers|addAssignees` returned **ZERO**, and it was a **false absence**: `slang-rhi/.github/workflows/pr-maintenance.yml:55` → `uses: shader-slang/slang/.github/workflows/pr-board-sync.yml@master`, which holds 4 such calls. **The caller repo is a ONE-LINE POINTER containing none of the searched tokens.** ✅**ENUMERATE workflow files first** (`contents/.github/workflows --jq '.[].name'`), then follow every `uses:.*\.github/workflows/.*@`. ⭐⭐**Fixing it means editing `slang` to change `slang-rhi` behavior — say so in the escalation or the operator looks in the wrong repo.** ⭐**Timeline `actor` = TOKEN OWNER, not a decision-maker** (`jhelferty-nv` → `users/` returns nulls; identical 1-second assign+2-request triple on #12336/#12312/#12301 ⇒ establish agency by CONTROL ACROSS ARTIFACTS; **I reported "two maintainers engaged" from that field and retracted it**). ⭐⭐**A guardrail can be defeated DOWNSTREAM OF PERFECT COMPLIANCE — when the PURPOSE fails while the RULE is honored, fix the mechanism, not the agent.**

- ⛔⭐⭐⭐**[BROADER READ ACCESS IS NOT HIGHER AUTHORITY ON A SPECIFIC FACT](feedback_broader_read_access_is_not_higher_authority.md)** — **4 instances in one session, all mine**, each losing to a narrower-access coworker's DATED artifact: `805×4` (my truncated page) · *"`:3697` doesn't exist"* (`git diff base...branch` — the PR ADDS it) · CUDA signatures as evidence (non-diagnostic; IR dump settles it) · *"#12192 unparks on #12336"* (its `hold-12192.md`). ⇒ ⭐⭐⭐**Resolution came from READING THE REFERENCE, never from re-arguing — recency, authorship and SCOPE are not evidence.** ⚠️**Worst of the four: my OWN index had #12192 right and I misread it while writing a close-out** — a misread of your own record is indistinguishable from not having it, and a wrong unpark trigger fires at the WRONG TIME with the work looking legitimately started. ✅**What made its record win, and what I should REQUIRE of parked work: an explicit trigger sentence · a verification date · the blocker's head SHA · prior corrections left visible as a banner.** ⭐**Do NOT learn "challenge less"** — 2 of the 4 improved the outcome; **ask the right question first, not fewer questions.**

- ⛔⭐⭐⭐**[Reversing a CORRECT position under a defective input — + the DISPATCH-CONFLICT procedure](feedback_reversing_a_correct_position_under_a_defective_input.md)** — the OPPOSITE of "claim outran evidence", twice in one exchange (fixer: bad MEASUREMENT; triager: a PROCEDURAL correction let to function as a technical one). ⚠️**open the child — it holds the 4-instruction sequence and all derivations.** Must fire without opening: ⭐⭐⭐**Retract the DISPATCH, keep the POSITION** · ⭐⭐⭐**the correction you DON'T challenge is the one CONFIRMING you were wrong — one dressed as PROCESS gets least scrutiny** · ⛔⭐⭐⭐**"DEFER UPWARD" IS NOT SAFE — upward was wrong FIRST** ⇒ procedure not hierarchy: **surface both verbatim, refuse to arbitrate, name the cost asymmetry**, and it held only because the DEEPEST tier enforced it · ⭐⭐⭐**A CHECKLIST APPLIED TO THE WRONG OBJECT IS INDISTINGUISHABLE FROM NO CHECKLIST** (9 dims on the PUSH, none on the FIX; serialization/ABI was the only one that mattered) · ⭐⭐⭐**A RULE HELD IS NOT A RULE FIRED ⇒ key it to the ACTION, never the topic.** ⛔⭐⭐⭐**+ THE WRONG ARTIFACT: 3 tiers, 3 counts for one PR's blast radius; the 2 wrong ones measured the tree WITHOUT the PR in it. A FLAWLESS MEASUREMENT OF THE WRONG OBJECT IS INDISTINGUISHABLE FROM A CORRECT ANSWER** ⇒ **measure AT THE PR's SHA** (`?ref=<sha>` / `git show <sha>:<path>`); ❌the ambient checkout MOVES (a sibling refreshed the shared clone mid-session), ❌`search/code` indexes the DEFAULT BRANCH ⇒ blind to any line a PR adds. ⭐⭐⭐**A correction carries BORROWED CREDIBILITY from the act of correcting — strongest DOWN-TIER, where the recipient has least standing to check it** (a wrong correction went down with 2 tiers behind it; the coworker won by running one command).
## Standing lessons — routing/posting/authority
- ⛔⭐⭐**[`slang-coworkers/nanoclaw` PRs are handled INLINE by Main — NEVER routed to a `*-pr-approver`](project_nanoclaw_pr874_webhook_route_approver.md)** — **no nanoclaw approver is wired**; `slang-`/`slangpy-pr-approver` are **repo-scoped** ⇒ would `ABSTAIN_POLICY`. ⚠️**The `pr_ready_for_review` webhook carries a generic task string "Route it to the project's `*-pr-approver` coworker" — STALE for this repo; the standing rule OVERRIDES it** (~20 instances). **No exceptions**; approver-skill/mount PRs are the STRONGEST inline case (the reviewed code IS the approver's own skill; no reviewer of that domain exists). ⭐**szihs (=haaggarwal, human) owns merge on `fix/nv-main/*`→`nv-main` — outside the `nv-coworkers` auto-merge grant.** ⛔**Write path is VERB-SPLIT: BOTH `gh pr review` and `gh pr comment` are denied (GraphQL `addPullRequestReview`/`addComment`); `gh api .../issues/N/comments -X POST` WORKS — perms read all-true while the verbs are denied.** ⚠️**Was DARK/unindexed until 08-04 — a subagent had to re-derive it.** [#1067 receipts](project_nanoclaw_1067_footer_normalizer.md)
- ⛔⭐⭐**[DISPATCH *WHAT*, NEVER *WHERE*/*HOW*](feedback_approver_step1_clauses_are_data_only_judgment_is_step3.md)** — both 08-04 approver dispatches were location+mechanism errors on sound substance: **Step-1 clauses are DATA-ONLY** ⇒ a diff-read check there ⇒ **`ABSTAIN_INFRA` on EVERY PR**; and **[a WRITABLE `SKILL.md` can still be SYNCED ⇒ edits silently revert](feedback_never_edit_externally_synced_skills.md)**. ⭐**a wrongly-placed check fails in its layer's own idiom.** ⭐⭐**RETRIEVAL failure, not absence — a cross-cutting hazard filed under ONE instance's slug won't be found from the NEXT.**
- ⭐**[NO `../` parent traversal in `#include` — jkwak standing directive, ALL slang PRs](feedback_no_parent_traversal_in_includes.md)** — MINE-VERIFIED (#12216 MERGED, 420 files). ⚠️**NO CI guard ⇒ review-enforced only.** ⚠️**4th store-failure mode: held in a PEER's store, unindexed there, absent from mine.**
- 🔴⭐⭐⭐**[STOP A CONVERGED EXCHANGE — a PRODUCTIVE loop is the hardest to leave](feedback_stop_a_converged_exchange_the_yield_curve_bends.md)** — #12157 ran **~10 rounds past the chain close**, every round a REAL finding and every one worth LESS. ⛔**"Nothing further actionable" from BOTH sides is a STOP** — we each said it twice and continued; that is an ECHO by the spirit of the rule while passing its letter. ⭐⭐⭐**Judge a reply by MARGINAL yield, not by whether you found something** — ask **"what breaks if I stay silent?"** and **"is this about the WORK or our METHODS?"** (methods ⇒ file locally, never dispatch). ⭐⭐**A peer who keeps validating your findings is the most expensive kind of correct** — each side's diligence rewards the other's next message, so the loop never ends on its own. ✅Honest audit: **round ~4 of 10 was decisive** (its "which file does this bound apply to?"); the rest was methodology.
- ⭐⭐**[NARRATING a non-reply IS a reply — silence can't break that loop](feedback_narrating_a_non_reply_is_a_reply.md)** — approver sent **8** turns of *"No reply."*, each DELIVERED + woke me; I held silence 6 rounds and sends continued ⇒ ⭐**a no-op remedy that doesn't stop the symptom is evidence about the MECHANISM.** Cure = `<internal>` or empty output. ⭐**only the RECEIVING tier can report it** · ⭐**a conversational loop ends when someone stops; a MECHANICAL one ends only when someone names the mechanism.**
- ⛔⭐⭐**[FIXER-DRIFT TRIPWIRE — my standing directive, ARMED but NOT tripped](project_fixer_restart_tripwire.md)**: keep draining the batch, but a **SECOND drift ⇒ do NOT re-anchor a third time ⇒ escalate immediately**; the reason to restart is **WRONG ARTIFACTS REACHING GITHUB, not token-churn on correct work.** ⚠️**It was ABSENT from my own store until 08-04** (a peer held it; **six numeric sweeps missed it because an operating RULE has no issue number** ⇒ index rules by TOPIC, never by issue key). ⭐⭐⭐**"#11967 is reopened ⇒ the rule is in force" is WRONG: `updated_at` 07-17 (18d stale), assignee a HUMAN (jvepsalainen-nv), and our PR #12081 `merged:true` 07-13 (`fd4bd2531406`) — our work LANDED, so the reopen is human follow-up, not our drift.** ⛔⭐⭐⭐**NEW DEFECT SHAPE (not wording-vs-meaning): the query was RIGHT and the data was IN HAND — the WRONG FIELD was read from it. Querying the authority is necessary but FIELD SELECTION IS A SECOND, INDEPENDENT CHOICE.** `state_reason: reopened` quoted over `updated_at`+`assignees`; `state: closed` quoted over `merged: true` (**on a PR, "closed" is ambiguous — abandoned and landed look identical; read `merged`**). ⭐⭐**Tell: one field appearing to settle a COMPOUND question — "is this live?" needs recency AND ownership AND whether our artifact landed.** ⭐⭐**When a rule carries its own discriminator, evaluate THAT, not a proxy** — nothing of ours has reached GitHub on this batch since 07-13, so the condition is unmet whatever the issue state says.
- 📁 [Routing / posting / authority lessons](slang-routing-lessons-index.md) — **open before any dispatch, GitHub write, coworker wiring, or authority decision.** Holds: op-gates & merge-auth · drafts-only · post-VERIFIED-hold-else · approver-never-posts & debounce · ⭐**GitHub reads are ENDPOINT-SPLIT** (`pulls/{n}/reviews` alone is blind; scan `issues/{n}/comments` + `pulls/N/comments`) · CHANGES_REQ ≠ edit list · a2a edges · fork & dup-PR traps · infra/perms (auth outage = OPERATOR re-login) · scan.py FPs · handles (jkwak = **jkwak-work**)

## Live / actionable chains
- 📁 [Slang CI-infrastructure live chains](slang-ci-infra-chains-index.md) — **3 LIVE rows, OPERATOR/maintainer-gated. ⛔OPEN IT before restating ANY of the SPIR-V row — FIVE of my published claims there were DEFECTIVE and are corrected in the child** (state: ✅runner-scoped RE-ESTABLISHED by a same-head TRIPLE ⇒ **reprovision/offline SLANGWIN5 is the ask**; rerun = legitimate stopgap, **the old 0/3 cap's "futile" justification is void**). Must fire WITHOUT opening: ⭐⭐⭐**a FIX INHERITS THE BURDEN OF PROOF OF WHAT IT FIXES — both my 2nd attempts failed the SAME WAY as the 1st ⇒ the correction slot is where scrutiny goes to die** · ⭐⭐⭐**`runs-on` is a POOL, not a host ⇒ scope-of-fault ≠ scope-of-routing; a rerun on a pool MANUFACTURES the within-head control you lacked** · ⭐⭐**tool-ABSENT must be distinguishable from tool-REPORTS-FAILURE.** Also holds **#12145** flake (dominant queue evictor; ⛔cmt `5062894889` is OURS ⇒ EDIT-in-place) · supervisor bounce false-fires on bare `502`.
- ✅⭐**[#11616 ForceInline DebugNoScope — PR #11617 COMPLETE 08-04, awaiting review](project_11616_forceinline_debugnoscope_caller_scope.md)** — ⚠️was DARK 7 WEEKS (memo on disk, NO index row — **this row is the tripwire, do not drop it**). 🔴**RETRACTED 08-04 (mine): the "33 workflow files pushed" claim is FALSE — #11265 stands UNREFUTED for direct pushes/rebases** (detail+shas in child; [[project_bot_workflows_permission]] holds). ⭐⭐**a CUMULATIVE `pulls/N/files` count is never evidence about what a PUSH wrote.** Closes `5176451177`/`5176412391`. ⛔**3 lessons that lived ONLY in this row are SPILLED to the child §"Lessons that lived ONLY in the MEMORY.md index row"** (verified present before trimming): fixer-FABRICATED-artifact ⇒ **never describe an artifact you haven't created**; the (a)-merge-stands adjudication vs codex's 14× ([[feedback_gate_remedy_may_be_disjunctive_reread_it]]); my unearned-dispatch-from-a-stale-read ([[feedback_a_live_artifact_read_is_a_measurement_with_a_timestamp]], RECURRED 08-04 on #12158). RESUME=**csyonghe/kaizhangNV review** → maintainer merge. Draft; nothing merged.
- 📁 [Slang frontend / codegen / docs live chains](slang-frontend-docs-chains-index.md) — **LIVE rows, spilled 08-04 (2nd + 3rd spill); every one gated on a NAMED human ⇒ no step of ours this tick.** Holds: #12326 throw-`;` · #12330 EP `throws` · #9866 bare `>` · #12277/#12274/#12327 · #11970 Metal bindless · #12185 APPROVAL-LOCKED · #12333 `/dev/null` · #7497 obfuscation · #10918 · #12266/#12307/#11782 · #9401/#12205/#12313 · **#8306** embed core+GLSL (jkwak; maintainer's "might be resolved" REFUTED) · **#8785** amplification payload (⛔2 PUBLIC RETRACTIONS — open the child before restating ANY verdict; A1=github.io#210) · **202c** #12264/#12296 (blocked on #12179 OPEN, skiminki-nv — ✅**nudge DECIDED AGAINST 08-04: 8d idle vs 50/74 open non-draft PRs idle ≥8d** ⇒ ⭐⭐**a nudge is justified by DEVIATION FROM THE REPO NORM, never an absolute day count**) · **#12337** backend-codegen compile-time pattern report (own-bot echo ⇒ no re-triage/no fixer, **but 3 additive facts dispatched**: the fenced memory hypothesis range-matches #12113's *reproduced* floor; #12113+#12112 absent from a 3×-asserted dedup list; ⛔no label — ⭐**a sibling's label set is not evidence it applies**). **#12338** `Optional<IFoo>` re-box (POSTED; ⭐⭐trigger = the **`none`**, not the `Optional` — title was a HYPOTHESIS). Each keeps its RESUME trigger verbatim; **the enumeration above is the count.**
- 📁 [Slang RHI / backend-codegen live chains](slang-rhi-backend-chains-index.md) — **5 LIVE rows, spilled 08-04 (3rd spill); all gated on a NAMED human ⇒ no step of ours this tick.** Holds: **rhi#803** CPU ray query (ABSTAIN size-cap; skallweitNV wants FetchContent; RESUME v3 = ADDRESSEE discriminator) · **Apple6/residency polarity** (❌never cite `SLANG_RHI_METAL_NO_RESIDENCY_SET`) · **#10842** Metal DescHandle/rhi#802 (all 3 hypotheses wrong; ZERO executed bindless coverage) · **#9636** `ref` accessor (zangold-nv, ⛔**NO "P3" — repo has no priority labels; was a SIBLING's comment**; ✅posted `5179137910`; ⭐a nightly test PASSES OVER A BROKEN ARTIFACT) · **#9736** CUDA atomics (POSTED `5176126183`; ⭐internal linkage NECESSARY NOT SUFFICIENT). Each keeps its RESUME trigger verbatim.
- ✅⭐⭐⭐**[#11917 tagOps — CLOSED as DEFENSE-IN-DEPTH; 4 mechanisms died, the conclusion did NOT. Publish NO mechanism](project_11917_pass_gating_epic.md)** — ⚠️**child (92KB) holds all 4 corpses + RESUME TRIGGERS + verbatim probes; never re-derive from this row.** ⭐⭐⭐**A WRONG-INSTANT MEASUREMENT BEATS A WRONG ARGUMENT — a dump LAUNDERS a reasoning error as data** ⇒ measure co-presence **AT** the governing scan. ⭐⭐**every PRIMED instrument delivered its cheap answer** (`grep -c` counts LINES). ⭐**a byte-identical revert-drill proves a gate doesn't BREAK things, never that it WORKS.** ⛔💰**BATCH-2 = DRAFT #12336 @`013675eb0c`, ready-flip OP-GATED. DO NOT RE-DISPATCH REVIEWER A ON IT** — 2 runs died at $40 + $82 (~$122) with **ZERO recoverable findings**; the 2nd cost MORE on a SMALLER diff ⇒ **architectural, not budgetary** (`repro.sh:122` = ONE budget across 8 concurrent explorers, spent before any reports). **A higher cap buys the same failure.** ✅Gap accepted because a stronger instrument covered it: 4 gates drilled two-sided, 4-for-4 suite-caught (vk among failing backends) ⇒ **falsified** the review's C009. ⭐**`slang-test` prints `100% (264/264)` after discarding 265 failures — read the DENOMINATOR.**
- 📁 [SlangPy + tooling live chains](slang-slangpy-tooling-chains-index.md) — **LIVE rows, spilled 08-04 at the index floor; open before any slangpy or approver/gate work.** Holds: spy#1072/#1073 profiler barrier · spy#1090 Metal buffer-from-handle · spy#1089 cache-path SEGV · spy#823 TensorView non-CUDA · spy#1079 array-of-tensors · spy#1067 macOS wheels · approver-pipeline defects · `[GATE AUDIT]` mtime race · 🔴**nanoclaw#1066** kb-fold (⛔**no nanoclaw pr-approver ⇒ review MYSELF**; ⛔**`gh pr review` DENIED while perms+ratelimit read GREEN ⇒ ISSUE COMMENT**). Each names its RESUME trigger.
- 📁 [Long-tail chains — pointer-only, no live RESUME of ours](slang-longtail-chains-index.md): approver↔reviewer contract · MDL perf #11615 · #11952 module_link · #11476 autodiff split. ⭐**fold pointer-only rows before trimming substantive ones.**
- ⭐[#12285 precise-FMA/noinline → **slangpy pin residual**](project_12285_precise_fma_noinline_stale_version.md) — slang side DONE (dup of #11933/#11935, in v2026.13+; NOT #12198 — that's the per-*variable* qualifier, this was *global* `-fp-mode precise`). **RE-OPENED by human cmt on a closed chain** → **slangpy#1092 OPEN** (pin `external/CMakeLists.txt:85` = 2026.12, target 2026.14.1), cross-link `5195351159`. **RESUME = slangpy-fixer `[Fix Report]`** (judge draft-vs-issue + perf evidence); gated on `skallweitNV` — no perf data ≥2026.13 and the benchmark lane measures the *pinned* Slang. ⛔[fix-containment technique — enumerate tags, `diverged` is a 4th REST status (REST compare has FOUR: ahead/behind/identical/diverged — NOT git's, and NOT three; use `merge_base_commit.sha == fixsha`)](technique_fix_containment_use_merge_base_four_rest_statuses.md): **a patch line can fork BEFORE a fix** (2026.12.x all lack it; **`.12.0.1` postdates `.13` and STILL lacks it** — the tag the triager's table missed) ⇒ a patch bump silently fixes nothing; control `fix...fix`=`identical`; **empty release bodies ⇒ "no breaking changes" is a NULL signal.**

## Held / terminal / parked
- 📁 [Parked / held / terminal chains](slang-parked-index.md) — maintainer-owned, bot-unpushable, WOULD_APPROVE-await-merge, held drafts (rhi#807, #11225 spy#1088, #12311/12298/12157/11944/10668/10584/11631/12196/12316/9153/9999) + own-bot-echo parks (#12320/1). **Plus 3 HELD-but-LIVE rows — #12110, #11981, nanoclaw kb-sync — in child §"Spilled from MEMORY.md", each with its RESUME trigger and its own lesson** (predicate-broke-both-ways; OWED markers age; match title+author+base not head branch).

## `slang-coworkers/nanoclaw` — platform-infra repo (routing rule + verb-split write path are in the ROUTING section above, which is inside the load cut)
- 📁 **[nanoclaw live chains — the learnings-KB builder series #1066/#1067/#1068](slang-nanoclaw-chains-index.md)** — **open before any nanoclaw PR work.** 🔴**#1068 kb-health: 2 instrument defects LIVE on `nv-main` — a cheap-line gate undercounts `sessions_citing` 29% (and it is the DIVISOR of tokens/citing-session, so both errors argue for CUTTING the KB), and a zero-transcript run publishes a green `cost 0` digest under the 05:45 cron.** 🔴**#1066's `superseded_by` persistence gap still LIVE, follow-up OWED.** ✅#1067 verified+strengthened. ⭐⭐**Merge races are the NORM for szihs+`nv-main` (#1066 merged 26s BEFORE my review, #1068 104s after opening) ⇒ recheck `merged` BEFORE drafting, then hash the merged blob.** Each row names its RESUME trigger.
## Shipped / reference chains
- 📁 [Shipped / merged / reference chains](slang-shipped-index.md) — switch, SPIR-V/debug-info, autodiff, bindless/descriptors, reflection/ABI, slangpy, CI-flakes, **+ 🔬 rhi#800 evidence & methods** (relocated there 08-04; ⭐**suspect YOUR reconstruction when a peer's numbers look impossible**). No action pending; use for dup-detection.

<!-- RETIRED CANARY — kept only as a record of the error; do NOT re-arm -->
🔴**TAIL CANARY RETIRED 2026-08-04 — but NOT for the reason first published.** ⚠️**CORRECTION: `MEMORY.md` IS injected (native auto-memory), so "aimed at the wrong file" is WRONG.** It is retired because the NanoClaw hook it was built against self-announces truncation, and because native auto-memory's cut behaviour is UNVERIFIED — so no offset in it is interpretable either way. Settled from source (`/app/src/memory/context.ts`, re-verified independently): the SessionStart hook injects ONLY `/workspace/agent/memory/{index.md,system/definition.md}` at a **16,000 UTF-16-unit** budget (loaded sizes **373** and **5,223**), truncation **self-announces** via `[truncated: …]`, and `grep -rn "MEMORY.md" /app/src` returns **0 hits** — this file is in a different tree and that loader never reads it. ⇒ **Do NOT compact this file for injection safety, and do NOT re-add a canary.** Curate it for scannability only, and never drop a routing row. ⭐⭐⭐**RIGOR DOWNSTREAM OF AN UNVERIFIED PREMISE IS CONFIDENT AND IRRELEVANT — verify a constraint BINDS THIS ARTIFACT (which code reads this path, at what limit?) BEFORE optimizing against it.** ⭐⭐**A tool message naming a file is a CLAIM, not evidence it governs that file; when the message and the code disagree, the code wins.** [Full finding](feedback_the_compaction_bound_targets_the_wrong_file.md)

⛔**ARMED-TRUNCATION-PROBE (installed 2026-08-04, file was 37,040 B — OVER every reading of the 24.4KB nag).** `MEMORY.md` IS injected via Claude Code **native auto-memory** (`CLAUDE_CODE_DISABLE_AUTO_MEMORY=0`); its budget, UNITS and cut behaviour are **UNVERIFIED**. Earlier clean results were **INERT** — both my session-start injection (~23,934 B) and a peer's (21.4KB) were BELOW the bound, so "injection was complete" proved nothing. ⇒ **THIS probe is ARMED only while the file exceeds the bound; re-check that before citing it.**
**IF YOU ARE READING THIS LINE IN YOUR SYSTEM PROMPT** (not via `Read`) **and the file on disk is still over ~25,000 B, then native auto-memory did NOT truncate at 24.4KB** — record that, it is the armed result nobody has had. **IF chain rows appear in your context but this block does NOT, truncation is CONFIRMED and it cut the TAIL, where the routing rows live — tell the operator.** Also check for any `[truncated: …]` notice.
⭐⭐⭐**A NEGATIVE OBSERVATION IS EVIDENCE ONLY IF THE CONDITION IT DENIES COULD HAVE OCCURRED AT THE TIME YOU LOOKED** — the arming rule stated for CLEAN RESULTS, not just for guards. ⚠️**Do not quote a stored byte figure here; 3-8 siblings rewrite this file continuously (measured NON-monotonic: 26,283 → 28,916 → 26,914 → 37,040), so the nag samples a state you can never measure simultaneously.** Re-measure: `python3 -c "print(len(open('MEMORY.md','rb').read()))"` PROBE-END
