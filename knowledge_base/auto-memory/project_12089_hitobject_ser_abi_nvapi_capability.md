---
name: project_12089_hitobject_ser_abi_nvapi_capability
metadata: 
  node_type: memory
  type: project
  originSessionId: edfa7fc6-9dea-4059-a70a-5dcc9b3f8f63
---

**PR #12089** "HitObject: single source-of-truth SER ABI via explicit `nvapi_hit_objects` capability", author **szihs (COLLABORATOR/maintainer)**, opened → ready_for_review 2026-07-14. Body: `Fixes #11903`. NOTE: #11903 + our bot PR #11907 already MERGED/CLOSED 2026-07-10 (see [[project_11903_hitobject_sm69_nvapi_pending]]) — so #12089 is a **maintainer-authored cleaner re-do** of the SER-ABI approach, NOT our chain. Do NOT dispatch fixer/reviewer; `pr_ready` → approver only ([[feedback_webhook_dispatch_by_event]]).

**Mechanism (per approver's read, unverified):** new `nvapi_hit_objects:hlsl_nvapi` capability atom; HLSL type emitter + every HitObject `__target_switch` consult ONE ABI decision via concrete-atom `implies()` on resolved caps; native 2-arg `Invoke` `static_assert`s under NVAPI. Same capability-atom pattern prior chain converged on.

**Approval verdict (2026-07-14, shadow mode, ledger-only, NOTHING posted — [[feedback_approver_never_posts_route_reviewer]]):** **ABSTAIN_POLICY / CHALLENGER_CONCERN** @ head `15ae9279357064990707d80a937fd82b607d0707`. Three human-must-look signals blocked auto-approve:
1. Real build+slang-test CI (ci.yml) **never ran on this head** — only `pull_request_target` jobs green; the 2 new `.slang` tests + core-module ABI change are CI-UNVERIFIED.
2. PR **CONFLICTING / ~228 commits behind master** (diverged, non-mergeable — likely why `pull_request` CI never dispatched).
3. Fallback-tier review only: no production `github-actions[bot]` review (check-run stuck `in_progress` ~48min), Devin timed out (30m); CodeRabbit's one relevant finding = minor test-convention nit (`diag=CHECK` vs `filecheck=CHECK`).

Not BLOCK (no verified bug); not ABSTAIN_INFRA (valid CodeRabbit review harvested, pipeline completed).

**Rev1 next action was: rebase + let CI run. DONE.**

**RE-DECIDED 2026-07-14 ~10:49 on settled head `ce42d01f` (szihs pushed 5 revs in a churn storm: 15ae927→4c26b4d2→0990f618→c83a96d0→ce42d01f; approver re-anchored each push, waited 10-min quiet + real CI complete before parsing; Main forwarded first synchronize + one re-anchor, then went SILENT on the storm per debounce standing-guidance — approver's loop re-polls live head itself).** Verdict: **ABSTAIN_POLICY / CLAUSE_FAIL:ci_green_on_sha** (supersedes rev1 `15ae927` ledger row). Shadow mode — recorded, NOTHING posted.

**Both rev1 policy blockers CLEARED:** PR now MERGEABLE (rebased, no longer ~228 behind) and real `pull_request` CI (ci.yml build+slang-test) dispatched+completed. **But CI is RED** — decisive failure is **PR-caused**: `check-cmdline-ref` failed because new atoms `nvapi_hit_objects` / `cuda_glsl_nvapi_hit_objects` were added but `docs/command-line-slangc-reference.md` NOT regenerated (byte-exact diff gate; approver read the failing job log). `test-falcor` + SlangPy Tests also red (falcor likely external flake). All 9 builds + ~20 test-slang/rhi configs PASSED.

**REAL CODE REGRESSION (approver-independently-verified, would block even if CI green):** `slang-emit-hlsl.cpp:1997` — bare `hlsl_nvapi` + pre-SM6.9 profile now hits `SLANG_UNEXPECTED` (compiler ABORT) instead of a clean diagnostic → regression on a formerly-supported config (the exact edge flagged in rev1 investigation). Production `github-actions[bot]` review now PRIMARY-tier + non-stale (targets ce42d01f): 🔴 1 bug / 3 gaps. Devin ran on this head, no additional bugs.

**Rev2 next action was: regen cmdline-ref + fix SLANG_UNEXPECTED + close gaps. Both hard blockers DONE by rev3.**

**RE-DECIDED 2026-07-14 ~16:47 on rev3 settled head `54a064e9` (single push this time; 10-min quiet + real CI complete + fresh production review posted before parsing).** Verdict: **ABSTAIN_POLICY / CLAUSE_FAIL:no_protected_paths** (3rd ledger row: rev1 `15ae927` CHALLENGER_CONCERN, rev2 `ce42d01f` ci_green_on_sha, rev3 `54a064e9` no_protected_paths).

**Both rev2 blockers CLEARED (approver code-verified):** (1) `docs/command-line-slangc-reference.md` regenerated → `check-cmdline-ref` passes, combined status GREEN; (2) rev2 🔴 `SLANG_UNEXPECTED` abort FIXED — emitter now emits clean diagnostic **error 55215** + `dx::HitObject` fallback, w/ new regression test using exact `sm_6_5 + hlsl_nvapi` repro.

**NEW rev3 blockers:**
- **Enum driver (protected path):** rev3 adds `source/slang/CMakeLists.txt` (a `slang-fiddle-generate` target for a `--parallel` fiddle race) — policy-protected build path → terminal Step-1 clause fail. Production review itself questions whether this CMake change belongs in a capability PR.
- **NEW verified 🔴 (would BLOCK even if protected-path+CI clean):** on `-profile sm_6_5 -capability hlsl_nvapi`, the `ReorderThread` `__target_switch` selects native `case hlsl:` → emits SM6.9-only `dx::MaybeReorderThread` where pre-PR it emitted `NvReorderThread`. Only the **no-`HitObject`** overload `ReorderThread(uint, uint)` **SILENTLY** regresses (nothing fires the new 55215 diagnostic); the two `HitObject`-taking overloads fail cleanly via 55215. Approver verified in code; CI misses it (no test hits that path); Devin found nothing.

**RE-DECIDED 2026-07-15 ~15:21 on rev4 settled head `ecd1e5e4` — REBASE-ONLY, no code change.** Verdict: **ABSTAIN_POLICY / CLAUSE_FAIL:no_protected_paths** again (4th ledger row: rev1 CHALLENGER_CONCERN, rev2 ci_green_on_sha, rev3+rev4 no_protected_paths). rev4 = rev3 rebased onto master (delta is almost entirely a master merge + unrelated PRs); PR's own code byte-identical to rev3. **None of the rev3 next-actions acted on:** (1) `source/slang/CMakeLists.txt` still present, no sign-off (SAME terminal driver); (2) no-`HitObject` `ReorderThread(uint,uint)` byte-identical (rev3 🔴 unfixed); (3) 3 gaps still open.

**Deltas this rev:** `ci_green_on_sha` PASSES — combined-status API (license/cla, CodeRabbit, SlangPy Tests) = `success`. [CORRECTION 15:34Z: earlier "test-falcor didn't flake" was premature — CI finalized ~15:32Z AFTER the 15:13Z record. `test-falcor` DID flake late (recurring external image flake; unit+Perf passed) and `check-ci` aggregator failed downstream of it. BUT both are check-runs, NOT combined-status contexts → `ci_green_on_sha` (reads combined-status API) remains a correct PASS, decision UNCHANGED, no re-decision.] Production review dropped 🔴→🟡 (no 🔴 this rev, 3 gaps + 1 question) — but its lead 🟡 **enumerates MORE of the mixed-ABI class** (vindicates approver's rev3 "enumerate parallel no-operand sites" learning): `MakeMiss(RayFlags,…)` / `FromRayQuery` at `slang-emit-hlsl.cpp:470`, `hlsl.meta.slang:24821/24843/24859` still emit `dx::HitObject::…` into an `NvHitObject` under `nvapi_hit_objects`. Plausible-trigger OPEN_GAP → wouldn't approve even with green CI.

**RE-DECIDED 2026-07-15 ~18:27 on rev5 settled head `d6c2114d` — SUBSTANTIVE ("drop out-of-scope fiddle CMake change; address review nits").** Verdict: **ABSTAIN_POLICY / CHALLENGER_CONCERN** (5th ledger row: rev1 CHALLENGER_CONCERN, rev2 ci_green_on_sha, rev3+rev4 no_protected_paths, rev5 CHALLENGER_CONCERN).

**BIG PROGRESS — protected-path blocker CLEARED:** rev5 removed `source/slang/CMakeLists.txt` entirely → **all 6 eligibility clauses PASS for the first time in this chain.** ABI fixes verified complete in code (single-ABI no-mix invariant holds by construction): native-only `MakeMiss(RayFlags)`/`FromRayQuery` are `[require(hlsl,ser_dxr)]`-only → unreachable under `nvapi_hit_objects`; no-`HitObject` `ReorderThread(uint,uint)` is require-split; 2-arg `Invoke` has E41400 `static_assert`. Production `github-actions[bot]` review is 🟡 (3 gaps, 1 question), **no 🔴**.

**NEW blocker (the enum driver) = verified downstream COMPILE-BREAK, not a protected path:** `test-falcor` fails `error[E41011]: __target_switch has no compatible target` on `{sm_6_6 + hlsl_nvapi}`. This is the PR's breaking change reproduced against a real consumer — Falcor compiles SER shaders with `sm_6_6 + hlsl_nvapi`, and the re-gating leaves that formerly-working profile matching NEITHER `case nvapi_hit_objects:` (needs the explicit new atom, NOT implied by `hlsl_nvapi`) NOR `case hlsl:` (gated on `ser_dxr`/SM6.9). Corroborating: user-guide `docs/shader-execution-reordering.md` (untouched by PR) is STALE — doesn't document that `hlsl_nvapi` alone no longer selects NVAPI HitObject ABI. Other 2 CI fails ARE infra (x86_64-cpu segfault in unrelated `library/` test; aarch64 apt/ports "Common Test Setup").

**PROCESS CATCH (important):** approver initially mis-classified the Falcor fail as external flake (as it had noted on rev2–rev4) — DECISION_REVIEW critique CAUGHT it: reading the log showed E41011 on the exact `sm_6_6+hlsl_nvapi` surface this PR re-gates → PR-caused, not flake. Verdict stands (ABSTAIN) but on corrected evidence. NOTE on prior revs: falcor was NEVER the terminal driver on rev2 (ci_green_on_sha/cmdline-ref) / rev3–rev4 (no_protected_paths) — those had stronger drivers, so the mis-classification changed no earlier verdict; it only becomes operative at rev5 where all clauses pass.

**rev5 next action was: re-gate ReorderThread etc + resolve Falcor break + update SER doc.**

**RE-DECIDED 2026-07-17 ~17:41 on rev6 settled head `fbbe0c40` — FIRST BLOCK IN CHAIN.** Verdict: **BLOCK / RED_BUG** (6th ledger row: rev1 CHALLENGER_CONCERN, rev2 ci_green_on_sha, rev3+rev4 no_protected_paths, rev5 CHALLENGER_CONCERN, rev6 RED_BUG). First-filter: substance changed only COSMETICALLY — the one rev6 commit is a **pure rename** `nvapi_hit_objects`→`nvapiHitObjects` (+[EXT] tag already present); ABI gating byte-identical modulo identifier → all 6 clauses PASS, protected-path gone.

**NEW verified 🔴 (enum driver) — production review escalated 🟡(rev5)→🔴(rev6):** four NVAPI-only HitObject **LSS accessors** — `GetSpherePositionAndRadius`, `GetLssPositionsAndRadii`, `IsSphereHit`, `IsLssHit` (`hlsl.meta.slang` ~24239-24333) — were NOT re-gated onto `nvapiHitObjects`. Their `case hlsl:` emits NVAPI method-call intrinsics, so on `-profile sm_6_9 -capability hlsl_nvapi` the HitObject **type** emits native `dx::HitObject` while these methods emit NVAPI accessors → **mixed-ABI HLSL** — the exact #11903 defect class the PR eliminates for the other ~18 arms, left uncovered for the LSS subset. Approver confirmed code-shape + reachability (`raytracing_lss_ho` real; methods `require(hlsl)`-callable regardless of type ABI); codex independently verified. Attributable: PR flips default type to native on `sm_6_9+hlsl_nvapi` without re-gating these methods → matched(pre-PR) becomes mixed(post-PR).

**Persistent (corroborating, not enum driver):** rev5 `test-falcor` break STILL present — `E41011 __target_switch has no compatible target` on `{sm_6_6 + hlsl_nvapi}` (rename didn't touch gating). Stale `docs/shader-execution-reordering.md`. (x86_64-cpu/aarch64 CI = infra; combined status success; 2 Windows GPU checks in-progress at decision, immaterial.)

**Next author action (szihs): (1) re-gate the four HitObject LSS accessors onto `nvapiHitObjects`-vs-native (or diagnose) so they can't emit NVAPI accessors on native `dx::HitObject`; (2) resolve persistent `sm_6_6+hlsl_nvapi` break (migrate consumers to `nvapiHitObjects` or add back-compat); (3) update SER user-guide doc.** NOTE: production `github-actions[bot]` review ALREADY posted the 🔴 publicly on the PR — human-observability surface carries it; approver stays shadow-mode ledger-only ([[feedback_approver_never_posts_route_reviewer]]); NO nv-bot post, NO operator card (maintainer-owned PR, szihs sees own CI+posted review). Re-decidable on next `synchronize`. Chain terminal for now; reopens on PR webhook / substantive human comment. `pr-12089-awaiting-join` memory on approver side records human verdict when merge/review join lands.
