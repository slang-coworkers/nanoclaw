---
name: project_slangpy_1059_float3_cuda_perf
description: "slangpy#1059 float3-loop CUDA ~2.9x-slower codegen perf; dual-track — B docs draft PR"
metadata: 
  node_type: memory
  type: project
  originSessionId: 557ad10f-4d2f-4242-8bf3-798d654f8dd3
---

**slangpy#1059** — `float3` loop arithmetic runs ~2.9x slower on the **CUDA backend** than identical `float4`/scalar code; Vulkan shows no penalty. Root cause **upstream in shader-slang/slang**. SlangPy transparent to vector types (no code lever) → Track C (slangpy-layer rewrite) ruled out.

**⚠️ ROOT CAUSE CORRECTED (slang-triager, GPU-free codegen evidence @ HEAD 8f0c3515d):** NOT float3 layout/alignment (that + Metal PR #10031 packed-vec3 precedent are a *different, unrelated* buffer-stride concern). Real mechanism = **swizzle-base duplication**: a multi-component `.rgb`/`.xyz` READ is emitted by the C-family emitter (`slang-emit-cpp.cpp:1642-1719`, inherited by CUDA via `slang-emit-cuda.h:44`) as `float3{base.x, base.y, base.z}`, **re-evaluating the base once per component**. When base is a folded texture/buffer load, the fetch emits 3×. Proof: hot-loop `tex2Dfetch` count f4_all=**1**, f3_loop=**3** (≈2.87×), f3_epi=**1**. SPIR-V=single `OpVectorShuffle`, HLSL=native `.xyz` → the backend asymmetry. Affects **CUDA AND CPU/C++**. Explains reporter's uncharacterized trigger boundary: base=cheap local → fast; base=expensive helper expr → slow. Original slangpy-triager float3-layout diagnosis SUPERSEDED. Fix path: A (emit-side: bind base to temp once for elementCount>1 when base folded/non-trivial) or B (IR-side use-aware materialization). Severity medium/P2. slang-triager memo: `/workspace/agent/memory/triage-slangpy-1059-cuda-swizzle.md`.

**Dual-track (parent = me owns cross-project routing):**
- **Track B (DONE — awaiting human merge):** SlangPy CUDA perf/gotcha doc note. Draft **PR #1060** (OPEN, isDraft:true, docs-only: `docs/src/basics/cuda_performance.rst` + `docs/index.rst`; title "multi-component swizzle CUDA performance gotcha note"), body `Fixes shader-slang/slangpy#1059`. Re-pushed 07-13 with BOTH corrections (swizzle-base mechanism prose + slang#12073 backfill) + reviewer nits in one revision. **slangpy-reviewer APPROVE_WITH_NITS → upgraded to full ✅ APPROVE (0 findings)** after nits folded (nested-inline-literal header mis-render ×2; WA3 rewritten to show base triplication); codex re-critique green. Reviewer independently re-verified corrected mechanism vs slang#12073 (GPU-free .cu 1/3/1 + DeepWiki), logging a correction to its own round-1 pass that had certified the wrong "why" → corrected mechanism now confirmed by 3 parties (slang-triager, slangpy-triager, reviewer). APPROVE is advisory/non-gating for human merge. Triager verified artifact: mechanism + CUDA+CPU/C++ scope correct, zero wrong-attribution residue (the one "12-byte" hit is the doc saying layout is NOT the cause). **NEXT HUMAN ACTION (operator-gated per [[feedback_github_writes_operator_authorized]] + [[feedback_drafts_only_guardrail]]): promote draft #1060 → ready → merge (auto-closes #1059).** Low-urgency docs-only; GitHub carries the trail; do NOT auto-promote. slangpy-triager holding, surfaces only further state change (merge / new review / CI).
- **Track A (FILED upstream 07-13):** slang-triager filed **shader-slang/slang#12073** (https://github.com/shader-slang/slang/issues/12073), cross-linked both ways with slangpy#1059; Type=Bug, labels bug/cuda/reproduced. No slang dup existed. #12073's own `issue_opened` webhook self-drives its triage→fixer chain — slang-triager deliberately did NOT manual-dispatch slang-fixer (would double-dispatch). I confirmed letting it self-drive.

**OPEN DEPENDENCIES I OWN (07-13):**
1. ✅ upstream issue# = **#12073** — relayed to slangpy-triager for doc-link backfill in PR #1060.
2. **⚠️ Doc-note correctness:** slangpy PR #1060's doc note was written on the *superseded* float3-layout mechanism. Workaround still holds, but the *explanation* (float3 12B/4-byte alignment / register-alloc) is WRONG — real cause is swizzle-base re-eval. Flagged to slangpy-triager to have fixer correct the doc's mechanism text, not just backfill the link. Sibling-distinct from the slangpy autodiff chains ([[project_slangpy_1052_autograd_cache_grad_bit]], [[project_slangpy_1055_diff_loop_vector_return_wrong_grads]]).
