---
name: project-12157-ir-version-check-required-status
metadata: 
  node_type: memory
  type: project
  originSessionId: f47a5b63-d46a-4309-a3ce-97f5b86becd4
---

**#12157** (shader-slang/slang) — "Make the IR-instruction version-bump check a required status check". Bot-filed follow-up requested on PR #12133 (which added `kIROp_ImageGatherOffset`, needed a manual 25→26 bump after the advisory comment fired).

**Problem:** The `⚠️ IR Instruction Files Changed` advisory (marker `<!-- slang-ir-version-check -->`) can't be a *required* branch-protection check. Two design gaps confirmed @HEAD c6a261068:
1. `extras/check-inst-version-changes.sh` emits `::warning::` + `exit 0` on the needs-bump path → the `pull_request`-event step always reports success.
2. The poster job in `.github/workflows/check-ir-version.yml` runs on `workflow_run` (default-branch ctx) → reports no PR-head status → branch protection can't require it.

**Triage:** enhancement (CI/infra) / low / CI / P3. Verdict posted as issue comment 5013932488.

**Recommended fix (Approach A):** add `--enforce` (exit 1) mode to `check-inst-version-changes.sh` + a dedicated `pull_request`-event job wired into the existing `check-ci` aggregate (ci.yml:642). That aggregate is the single required check, so adding the job to its `needs` makes it required automatically. Mirrors precedents check-cmdline-ref + Check-Stable-Names. Keep the advisory comment as the human-friendly explanation.

**Files:** extras/check-inst-version-changes.sh (bot-committable); .github/workflows/ci.yml new job + check-ci.needs (NOT bot-committable — workflows-permission wall); source/slang/slang-ir.h:2260-2261 (constants).

**⚠️ Split delivery:** script change is bot-committable; the ci.yml portion must be posted as a maintainer-applied diff (assignee jkwak-work). See [[project_bot_workflows_permission]].

**State (07-19):**
- Triaged (verdict comment 5013932488 on issue) → forwarded to slang-fixer on canonical thread `gh-issue-shader-slang/slang-12157`.
- Fixer SHIPPED bot-committable part: `extras/check-inst-version-changes.sh` +27, opt-in enforce mode (`CHECK_ENFORCE=1`/`--enforce` → `::error::`+`exit 1` on needs-bump; default byte-identical, advisory comment flow untouched). 6 synthetic-git scenarios PASS; shfmt clean; codex PLAN/CODE/OUTPUT approve.
- ci.yml `check-ir-version-bump` job + `check-ci.needs` edit delivered as ready-to-apply **diff in PR body** (bot can't push workflow YAML).
- **Draft PR #12158** open, labeled `pr: non-breaking`. Peer review dispatched to slang-reviewer (verdict pending). Draft-CI = benign priority-yield (builds skipped), not a real failure.
- **Maintainer action required (jkwak-work):** apply the embedded ci.yml diff to make the check required.
- Chain: orchestrator → slang-triager → slang-fixer. Do NOT double-dispatch to fixer (triager owns the wired handoff). Awaiting reviewer verdict + maintainer application.
- Observability CONFIRMED (triager [Triage Resolution] verified PR independently): issue footprint LIVE — fixer posted draft-held 5-bullet (issue comment **5014066686**) on top of triage verdict (5013932488). Enforce mode fires AFTER advisory warning+artifact upload → `⚠️ IR Instruction Files Changed` comment byte-identical (avoids Approach-B pitfall). Only open observability item: fixer to confirm `report_pr_created(#12158)` (nudge msg out, in_reply_to=10).
- Chain stays OPEN until #12158 merges.
- **07-19 expipiplus1 @-mention (issue comment 5018630322)** — ORIGINAL AUTHOR of #7821 answered the intent question: check was **conservative/advisory on purpose** because "changes can be made which do not require version bump, for example reordering the file or adding comments." → LIVE design concern for #12158: an enforce gate keyed on "`.lua` IR-inst file changed + constant NOT bumped" would **false-positive on benign edits** (reorder/comment) that need no bump. Routed to slang-triager on canonical thread: (a) ack Ellie on-issue, (b) assess whether `check-inst-version-changes.sh` distinguishes real instruction-addition from cosmetic file-touch — if not, enforce mode needs hardening (fixer owns #12158). Genuine mention → posting authorized.
  - **DETERMINATION (triager msg 30): CASE (b) REAL GAP.** Re-read script at PR head **cd0715be3** (advanced past 5e770366c5, same enforce commit): enforce path keys purely on **filename** (`git diff --name-only` → `INST_FILES_CHANGED` → `exit 1` if constant not bumped), zero hunk-content inspection → reorder/comment-only `.lua` edits WOULD false-positive & block a benign PR. Ellie vindicated.
  - Triager posted to issue (**comment 5018667237**, fresh comment since Ellie was last commenter — notifies her): confirmed real gap w/ specifics, said #12158 owner will harden before enforcing ships. PR stays draft.
  - Hardening HANDED to slang-fixer on canonical thread w/ concrete in-tree discriminator: a real new instruction appends a NEW entry to append-by-ID `slang-ir-insts-stable-names.lua` (verified #12133 appended `["imageGatherOffset"] = 898`); reorder/comment edits append none. Gate enforce on "added stable-names diff lines introduce a new opcode entry"; keep advisory warning broad. Serialization branch flagged as same false-positive class.
  - Chain HELD on fixer hardening (supersedes prior reviewer-verdict hold — gap must be fixed before enforcing gate is sound).
- **07-20 jkwak-work @-mention (issue comment 5024407855) — DESIGN PIVOT (authoritative, he's assignee+maintainer):** points at doc `docs/design/ir-instruction-definition.md` describing when the version var must increment. Wants: (1) more robust logic to check when bump required, keep the comment system as fallback for undecidable cases; (2) a dedicated checking step in **`ci-slang-{build,build-container}.yaml`** (BOTH — addresses the container/non-container split triager flagged); (3) verification in **C++, not shell**; (4) a C++ utility tool that loads the two LUA files and computes whether a bump is required. → This supersedes the shell-script approach in #12158. Routed to slang-triager on canonical thread: read the doc, assess C++-differ feasibility, pivot the fixer (currently shell-hardening). **Split shifts:** the C++ tool = bot-committable (new source util); the `ci-slang-build{,-container}.yaml` wiring = maintainer diff (workflows-perm wall). Genuine mention → posting authorized.
- **07-20 jkwak-work follow-up (issue comment 5024428753):** direct Q — "can you inspect how `k_minSupportedModuleVersion` works?" Citing doc `ir-instruction-definition.md?plain=1#L125` ("Increment both `k_min...` and `k_max...`"), asks whether `k_minSupportedModuleVersion` should bump to match `k_max` on a **backward-compat-BREAKING** change. Distinction the differ must encode: additive/new-inst = bump `k_max` only; breaking = bump BOTH min+max. `k_min`@slang-ir.h:2260=4, `k_max`=26. Folded into triager dispatch (same thread) — inspect `k_min` semantics (what rejects a module below it) + reconcile w/ doc L125. Posting authorized.
- **07-20 FEASIBILITY + DOC-RULE report (triager msg 40, @master HEAD 6a244fee2):**
  - **Doc bump rule** (`docs/design/ir-instruction-definition.md`): ADDITIVE (new inst / new flag not affecting existing / new OPTIONAL operand) → bump **k_max ONLY** (doc L191-195). BREAKING (remove inst / change semantics / change min-operand-count or types / rename) → bump **BOTH k_min+k_max** (doc L123-127, L197-202).
  - **⭐ SHARP FINDING — k_min/k_max are ADVISORY DOC ONLY, NOT enforced at load (PROVEN via exhaustive grep + deserializer read):** only use sites in all of source/ = definition+static_assert (slang-ir.h:2260-2262), m_version default-init (:2292), CLI diagnostic print (slang-options.cpp:4010-4011). Deserializer (slang-serialize-ir.cpp:790-834) gates ONLY on `serializationVersion != kSupportedSerializationVersion` (:813); loaded m_version deserialized (:722) but NEVER compared to k_min/k_max. Real runtime backcompat = stable-name sys + `_foundUnrecognizedInstructions → SLANG_FAIL` (:831-832), NOT the version window. **⇒ bumping k_min has NO operational effect today.** Answer to jkwak: breaking change need NOT bump k_min for runtime effect now; "bump both" is doc convention, unenforced. DESIGN FORK (jkwak's to decide): (a) treat k_min as documentation, differ RECOMMENDS per doc; or (b) make k_min a real gate = SEPARATE feature (add `m_version < k_min → SLANG_FAIL` to deserializer), OUT OF SCOPE for the CI-check PR.
  - **C++ differ FEASIBILITY = HIGHLY FEASIBLE (infra verified):** (1) lua-embed vendored external/lua/onelua.c, slang-fiddle already embeds lua_State → mirror; (2) CMake `generator()` macro tools/CMakeLists.txt:19 (reg :79-80) → new tool tools/slang-ir-version-check/ + 1 generator() line; (3) ref algorithm extras/check-ir-stable-names.lua (flatten+diff) → port to C++; (4) best signal = append-by-ID stable-names.lua (new key=additive→k_max; missing key=breaking→k_min+k_max; identical=reorder/comment→no bump); insts.lua only for operand-count. NOT mechanically decidable (→ KEEP advisory fallback, as jkwak wants): pure semantic-meaning change, some optional-operand cases, Fiddle-internal C++-only changes.
  - **Committable split:** tool source tools/slang-ir-version-check/* + generator() line = **BOT-COMMITTABLE**; dedicated steps in ci-slang-build.yml AND ci-slang-build-container.yml = **MAINTAINER DIFF** (workflows-perm wall; addresses container/non-container split — container currently has NO version step).
  - **MY DECISION (sign-off to triager):** proceed with option (a) — dispatch fixer to build the design-locked core NOW (C++ tool scaffold + ADDITIVE path: new stable-name key → recommend/enforce k_max bump), **DEFER the k_min/breaking-change branch** until jkwak answers the doc-only-vs-real-gate fork; #12158 stays DRAFT (rework shell→C++). Triager to post k_min inspection to issue now, presenting BOTH k_min options as jkwak's call (not decide it).
- **07-20 BUILD PROGRESS + scope-narrowing (triager msg 52):** fixer wrote `tools/slang-ir-version-check/` (embeds lua à la Fiddle, one generator() line), reverted shell `--enforce`, branch `fix/issue-12157` clean @ master 6a244fee2 + C++ tool only. Additive hard-gate ENFORCED (new stable-name key ∧ k_max not bumped → exit1); removed/renamed → recommend, exit0; fail-closed on unreadable inputs.
  - **Narrowing:** v1 keys on STABLE-NAMES key set alone (+ k_max int from slang-ir.h); defers "operand-count-change detection from insts.lua" (a named piece of my point-3 detect+recommend set) to a documented future extension.
  - **Verified rationale (triager, not a shortcut):** insts.lua `process()` (L3428-3447) hardcodes `stable_names_file` + `loadfile()`s it at a RELATIVE path → loading a base-revision insts.lua standalone reads working-tree stable-names (or fails on CWD), NOT the base → genuinely fragile. Stable-names file is self-contained & diffs cleanly base-vs-PR. So add/remove/rename robustly covered; only same-key operand-count changes invisible to stable-names diff.
  - **MY DECISION: ACCEPT the narrowing.** (1) trimmed piece is recommend-only (exit 0), not the hard gate → no false-positive/enforcement risk; (2) hard additive gate (the #12133 incident, core ask) fully covered by stable-names + k_max; (3) operand-count breaking changes rare + already caught by "Check Stable Names Table" job + advisory comment. **Condition:** gap must be documented explicitly (tool help/output + PR body) as a stated known-limitation — no silent cap (triager already instructed fixer). Operand-count detection → follow-up, gated behind solving base-insts.lua load-fragility.
- **07-20 VERIFY-AT-HEAD DONE (triager msg 58, #12158 @ 2565211fad, read pushed source not report):** OPEN/DRAFT, `Closes #12157`, `pr: non-breaking`, 4 files +521. Shell `--enforce` cleanly reverted (check-inst-version-changes.sh byte-identical to master). C++ enforce logic correct: additive hard-gate (added-keys ∧ no-removed ∧ k_max-not-bumped → `::error::`+exit1); removed/renamed checked FIRST → note+recommend exit0 (rename never hard-blocks); NO k_min read, NO deserializer touch (deferral honored); `readMaxModuleVersion` strips comments + matches the ASSIGNMENT (ident-boundary + `=` not `==` + digit) → ignores comment/static_assert/default-init decoys; fail-closed on all bad input. Wrapper materializes base rev via `git show origin/$base_ref:` into tmpdir (sidesteps insts.lua load-fragility), pull_request-only, fail-closed. CMake mirrors Fiddle lua-include; lua embed = onelua.c. **Both my conditions MET:** operand-count gap documented in tool `--help` AND PR body ("Known gap (deferred)"); CI maintainer diff in PR body for BOTH ci-slang-build.yml + ci-slang-build-container.yml (container step closes x86_64-debug gap). codex PLAN/CODE/OUTPUT approve; 2 real CODE bugs caught+fixed pre-report (rename false-block; readMaxModuleVersion comment-mention misread).
  - **⚠️ MAPPING CAVEAT:** triager called `report_pr_created(12158)` from ITS session → re-mapped #12158 PR-webhooks to triager (fixer owns PR). Triager dispatched fixer to reclaim (msg 63, last-write-wins) + forwards any webhook meanwhile. Correction on triager↔fixer edge; DON'T double-dispatch. Until fixer reclaims, reviewer/CI webhooks on #12158 route to triager not fixer. **RESOLVED (msg 60): fixer has reclaimed the mapping** — #12158 webhooks route to fixer again.
- **07-20 CROSS-CHAIN BLEED FLAG (triager msg 64):** fixer status msgs ("ID-based plan / re-push / both fixes green / slang-test compiling main.cpp") don't match #12157's C++-differ work → likely another issue's status landing on this edge (fixer driving multiple sessions). Triager sent disambiguation Q (its msg 71), holding #12157 position (verified @ 2565211fad, held reviewer verdict, NO resolution forwarded). Triager owns disambiguation on triager↔fixer edge — DON'T double-dispatch. Pending fixer answer: (a) if 2565211fad superseded by an "additive-bypass fix" → head moves, triager re-verifies before upstream forward; (b) if those msgs belong to another issue → disregard for #12157.
- **07-20 REOPENED by review (triager msg 66) — verify-at-head @ 2565211fad now STALE.** Reviewer verdict = REQUEST_CHANGES, two REAL findings on the C++ tool; head being reworked same branch (fresh SHA pending). Triager NOT forwarding until it re-verifies new head from scratch (not trusting prior pass).
  - **Finding 1 (build-wiring blocker):** tool does NOT build in normal debug CI — `generator()` only wires it under `all-generators`, which nothing in `cmake --workflow --preset debug` requires → wrapper's binary `find` empty → **exit 1 on EVERY PR**. PR-body "built via all-generators dependency" was WRONG. Fix: `add_dependencies(slang-test slang-ir-version-check)`.
  - **Finding 2 (additive-gate BYPASS, correctness):** `removedKeys>0 → return 0` early-return lets an unrelated new instruction ride in on any PR that also removes/renames (no k_max bump → exit 0) → defeats the #12133 case. Fix = **ID-based**: added NAME is genuinely-new (enforce) iff its stable-ID not in base ID set; ID-reuse rename stays advisory; NO early return.
  - **Triager's disclosed MISS:** its verify-at-head passed a tool with finding-2's hole — read the branches in isolation, missed the remove+unrelated-add COMBINATION; and accepted finding-1's PR-body build claim rather than proving the debug build emits the binary. Gate (reviewer+codex) caught both. Lesson → shared learnings.
  - #12158 stays DRAFT (correct). Chain OPEN, held on fixer rework + fresh head SHA → triager re-verifies from scratch → then upstream forward. Two deferred follow-ups (k_min gate on jkwak a/b; operand-count detection) unchanged.
- **07-19 review round 1 (fixer msg 20):** slang-reviewer caught a **fail-OPEN** bug — enforcing gate could pass GREEN on a missing bump if merge-base absent from a shallow checkout (`git diff ... || echo ""` swallowed the failure). Fixer fixed to **fail-closed** (uncomputable diff → `::error::`+`exit 1`); advisory mode still byte-identical; ci.yml maintainer diff bumped to `fetch-depth: 0`; folded reviewer nit C001. 8 synthetic scenarios pass (2 new fail-closed); shfmt clean; codex re-approve. Re-pushed **HEAD 5e770366c5**. Awaiting reviewer's final combined verdict. Draft gate holds — no ready/merge.
- **07-19 jkwak-work @-mention (issue comment 5014083539)** — real bot mention, 3 Qs. ANSWERED by triager on-issue (comment **5014106406**, closest-to-the-state), verified via git:
  - Warning implemented by **PR #7821** "Add CI to check ir module versioning", author **@expipiplus1**, merged 2025-07-22, reviewed/merged by **@k3wlbuddy**. Advisory-by-design from the FIRST commit (`::warning::`+`exit 0` script + `on: workflow_run` poster both present initially; poster later renamed `comment-ir-version-check.yml`→`check-ir-version.yml` in #11828). Never flipped enforcing→passive.
  - Intent record is SILENT — #7821 has empty body, no linked issue, single-line commit, no review text. Told jkwak plainly (didn't invent intent).
  - Who to ask: @expipiplus1 (author, owns intent) primary; @k3wlbuddy secondary.
