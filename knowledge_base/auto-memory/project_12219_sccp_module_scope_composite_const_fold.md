---
name: project-12219-sccp-module-scope-composite-const-fold
description: "#12219 SCCP: fold module-scope vector/composite constant exprs before SPIR-V emit — triaged+scoped, PARKED"
metadata: 
  node_type: memory
  type: project
  originSessionId: f1d7131c-25f1-4b0d-a50b-9a32585f50b6
---

# #12219 — SCCP: fold module-scope vector/composite constant expressions before SPIR-V emission

Split-off from review of PR #12186 (fix for [[project-12185-bindless-texture-nv-desc-handle-nonimage]]) at **@pdeayton-nv**'s request. Filed by nv-slang-bot. Label: bug.

**What:** ICE (E99997 "Unhandled global inst in spirv-emit") on valid module-scope `static const` vector/composite initializers built from float / `bit_cast` sources. Original framing was narrow (DescriptorHandle constant initializers, uint2/uint64 rep casts); **pdeayton-nv broadened scope 07-24** to the general case: *fold module-scope vector/composite constant exprs containing construction, numeric conversion, bit-casting, selection, and representation-wrapper ops before emission.*

**Root cause (verified file:line, master HEAD 5281ccc66):**
- `isEvaluableOpCode` (`slang-ir-sccp.cpp:113`) omits `MakeVector`/`MakeVectorFromScalar` + the 4 `Cast*DescriptorHandle*` rep ops.
- scalar/packed-float eval gate (`slang-ir-sccp.cpp:1026`) blocks vector-valued bitcast/cast/construct.
- Fix layer = **SCCP**, not emit. `applySparseConditionalConstantPropagationForGlobalScope` runs at global scope before emit but can't collapse these.

**Triage (slang-triager, 07-24):** EMPIRICALLY REPRODUCED on master, compile-only/no-GPU — both cases incl. capability-off. Type=Bug, `reproduced` applied. Verdict comment id **5072813775**. Title generalized + body rewritten (reproducers kept, proposed-work item 1 widened). Nuance: naive unused-const repro masked by DCE (must consume the constant); #12186's emit-time walker/assert is ABSENT on master → **proposed-work item 3 (retire the walker) is #12186-gated.**

**State: FIXER DISPATCHED (07-24).** @pdeayton-nv gave the operator go-ahead (comment 5097444164): *"please work on a PR for this issue first, which should then make the PR for 12186 simpler."* This **inverts the dependency** — #12219 now LEADS, #12186 follows. Routed authorization through slang-triager (chain owner, holds `triage-12219.md` briefing w/ approaches A/B/C) to hand off to slang-fixer on thread `gh-issue-shader-slang/slang-12219`.

**Fixer scope = items 1-2 ONLY** (the broadened general fold): extend `isEvaluableOpCode` (sccp.cpp:113) to admit MakeVector/MakeVectorFromScalar + composite/select/representation-wrapper ops; widen scalar/packed-float eval gate (sccp.cpp:1026) to cover vector-valued bitcast/cast/construct. That IS what "makes #12186 simpler." **Item 3 (retire #12186's `tryGetConstantDescriptorHandleBits` walker/assert) is explicitly NOT this PR** — walker absent on master anyway; whole point is #12186 never needs it. Guardrails: draft PR, `Fixes #12219`, `report_pr_created` on open, merge operator-gated.

**PR #12263 OPEN (draft) 07-29 13:11Z** — slang-fixer's fix, branch `fix/issue-12219`, `Fixes #12219`, +185/−0 across 2 files (all in `slang-ir-sccp.cpp` + 1 regression test `tests/spirv/gh-12219-fold-global-descriptor-handle-const.slang`). Assignees saipraveenb25; reviewers saipraveenb25+ncelikNV. Implements items 1–2: member `isEvaluableOpCode` admits MakeVector/MakeVectorFromScalar + 4 `Cast*DescriptorHandle*`; new helpers `getConstantVectorElement`/`evalVectorUnary`/`evalDescriptorHandleToUintCast`; `mapInstToFoldedVector` convergence memo. Item 3 (retire #12186 walker) correctly OUT of scope.

**3-reviewer pass complete 07-29 14:13Z → REQUEST_CHANGES (1 bug + 1 gap).** slang-reviewer combined A(correctness)+B(Devin)+C(clarity) @head 82dfaf08449f, diff sha ba2c1c427ec2, drift==0. Report file: `/workspace/inbox/a2a-1785334402526-g1fga4/combined-review-12263.md`.
- 🔴 **BUG** (`slang-ir-sccp.cpp:987`): `getConstantVectorElement` splat branch reads `getScalarValue()`→`getOperand(2)` on a **1-operand** `MakeVectorFromScalar` → debug SLANG_ASSERT / release OOB read. Reachable via module-scope splat cast e.g. `static const uint3 g=(uint3)float3(3.0)`. Verified from source: Lua schema declares 3 operands but every builder emits count=1 and every existing consumer uses `getOperand(0)`; PR is the sole `getScalarValue()` caller. Fix: read `getOperand(0)`. A conf-90 + security-reviewer confirmed; C flagged same location (C001) as "please confirm"; Devin missed it. **Fixer EMPIRICALLY REPRODUCED** `assert failure slang-ir.h(711)` on Debug — bug is demonstrated, not just source-inferred.
- 🟡 **GAP**: only `CastFloatToInt` vector fold is tested; CastIntToFloat/IntCast/FloatCast(+Constexpr) & splat path unexercised — a wrong element-type/evaluator pairing would silently mis-fold. Add GPU-free INTERPRET value-check + splat case.
- **DescriptorHandle round-trip hook (pdeayton sensitivity): SOUND** — `isTypeEqual` guard correctly gates identity-cancel; widths always match on master (guard always-true today; C003 asks assert-if-impossible w/ #12186 pointer — clarity, not a bug).

**Routing:** reviewer forwarded verdict directly to slang-fixer (direct wired edge) AND reported to parent(Main). Loop closed by reviewer; Main did NOT re-dispatch (no-double-dispatch). No GitHub post on draft (fix-chain relay, no `<github-post-authorized/>` marker); no operator escalation (op gate is at merge). **Fixer on Round 1 of 2**, will re-request review after pushing; reviewer gave R2 tips (DCE-resistant splat test; use `SLANG_ASSERT` not `RELEASE_ASSERT` for C003 fall-through). Main's edge closed.

**R2 verdict 07-29 15:24Z (head d44d91b5c9): REQUEST_CHANGES — R1 splat bug FIXED+verified, but a NEW 🔴 bug found.** The fix marks `MakeVector → Constant(inst)`, making a `Constant` lattice value wrap a **non-`IRConstant`**; `bit_cast<scalar>(constVector)` then null-derefs in `evalBitCast` — compiler-crash regression, reachable at BOTH global + function scope. Verified from source; `bit_cast<uint>(uint16_t2(1,2))` is valid Slang. Plus 4 test/coverage gaps. **Fixer owns Round 3** — guard the invariant break pass-wide (not one consumer) + add `bit_cast<scalar>(constVector)` test. Reviewer forwarded combined A/B/C report + verdict directly to slang-fixer on canonical thread (loop closed by reviewer's direct edge; Main did NOT re-dispatch). Reviewer will re-review on new head. No blocker/no human decision.

⚠️ **Concurrency reliability note (07-29 15:24Z):** the concurrent #12262 (#10668) review tripped Reviewer A's shared-tmp integrity guard with a FALSE-POSITIVE; reviewer proved A reviewed the correct #12263 diff before trusting it. Addresses the #12263 side; does NOT explicitly clear whether #12262's in-flight review was contaminated in reverse. **Main TODO: when #12262 verdict lands, verify its `diff_hash` matches head `04764dd7a2` before trusting.** Root fragility = two `/slang-pr-review` passes sharing tmp state concurrently. **Reviewer clarified: review CONTENT is never corrupted — only the guard's comparison; each run reviews via live `gh pr diff` + captures its own `<run_dir>/pr-diff.reference`. Disambiguator: trust each run's OWN footer `reviewed: <sha> · diff sha256 <hash>`; locate run dir by `prompt.txt` `PR NUMBER:` line, NOT mtime.** So #12262 check = its result-block `diff_hash` should equal `gh pr diff 12262` @ 04764dd7a2. Learning captured by reviewer in `/workspace/shared/learnings/` ("Reviewer A INTEGRITY-FAIL can be a false positive from concurrent runs sharing tmp/pr-diff.patch"). **Main AUTHORIZED reviewer to file a hardening proposal to slang-skills.** Proposal delivered 15:33Z (`/workspace/inbox/a2a-1785339204104-3iewd1/proposal-slang-skills-concurrent-run-isolation.md`). ⭐ **Reviewer correctly REJECTED Main's literal "rename to `tmp/pr-diff-<PR>.patch`" idea** — it'd break byte-equivalence with production (`validate.sh`), diverge from live `REVIEW.md` the CLI reads, + blind the guard. Better root-cause fix: **port Reviewer C's existing git-worktree isolation** (`run-clarity.sh:95-154`) to Reviewer A (`compose-and-run.sh`) — rebind `REPO_ROOT` to a per-run `wt-<PR>-review-<TS>` worktree (GC-reapable), so `tmp/pr-diff.patch` stays byte-identical name but private location; also fixes the `.git/index.lock`/checkout race the rename missed. Single shell file, 4-pt test plan (validate.sh 5/5, concurrent-isolation, cleanup, guard-still-fires). **Main AUTHORIZED reviewer to open the DRAFT PR to `shader-slang/slang-skills` directly (shell-only, no YAML, within drafts-only guardrail) if it has write; else hand patch to Main for operator routing. `report_pr_created` required; merge operator-gated. Not operator-interrupted now (non-blocking; merge gate = operator decision point).** Discipline note: trust the specialist's root-cause analysis over a parent's symptom-fix. **RECURRENCE 07-29 17:29Z: the shared-tmp false-positive INTEGRITY-FAIL fired AGAIN on R3** (concurrent PR-12265 clobbered `tmp/pr-diff.patch`; reviewer verified A reviewed correct head 278f362112 via footer before trusting). **Now 2 of 3 rounds** — concrete recurrence, not theoretical. Main gave operator a frequency update (upgrading the earlier "queue-when-convenient" framing) but still non-blocking (each instance caught + worked around by reviewer's footer-hash check). **07-30 04:19Z: FIRED AGAIN** — #12275-R2 review's Reviewer-A INTEGRITY-FAIL was a false-positive from the concurrent #11917-batch3 review (`pr-20260730T035154Z`) clobbering shared tmp/pr-diff.patch. Main independently diff-hash-verified #12275-R2 clean (live gh pr diff == report hash, correct file set) → confirmed false-positive, verdict trusted. Recurrence count now spans multiple session-days; the worktree-isolation proposal in the operator queue is the fix. NOT re-pinging operator per-recurrence (framing already upgraded to "routine collision whenever 2 reviews overlap"); each instance remains caught by the reviewer's footer-hash + Main's diff-hash check. Escalation is live in the queue. **ROUTING: bot has NO write to `shader-slang/slang-skills` (Main verified all perms false: admin/maintain/pull/push/triage all false) + repo not cloned locally → neither reviewer NOR Main can open the draft PR. Target path VERIFIED exists (`skills/slang-pr-review-runner/scripts/compose-and-run.sh`, 10075 bytes). Main handed proposal to OPERATOR (dashboard) 15:35Z for routing — needs a human with slang-skills write to apply §3 insertion + §5 test plan. Non-blocking (both PRs self-driving); parked awaiting operator.** #12263 R2 bug CONFIRMED SEGFAULT (EXIT=139) by fixer; on Round 3. **R3 approach PEER-VERIFIED sound by reviewer (15:38Z consult):** fixer's load-bearing uniqueness claim — "bit_cast is the only scalar-result evaluable op taking a vector operand" — confirmed from source against the full evaluable-op set; guard covers both aggregate shapes (MakeVector + MakeVectorFromScalar). Reviewer offered optional forward-looking hardening (document/centralize the consumption-side invariant so a future non-element-wise scalar-result op can't silently break it) — fixer's call, non-blocking. Await fixer R3 push → reviewer re-review on new head.

**⚠️ SCOPE NARROWED MID-REVIEW 07-29 15:44Z by maintainer @pdeayton-nv (PR comment 5120069648).** New scope = fold constant **integer bit-casts** between scalar↔vector of equal total bit width ONLY (required: uint2↔uint64_t for DescriptorHandle; signedness variants; uint16_t2↔uint32 if natural). **DEFERRED to follow-up:** the element-wise numeric FloatCast/IntCast/castFloatToInt folding AND general aggregate folding — i.e. MOST of the current PR diff comes OUT. Side-benefit: this cleanly resolves the R2 evalBitCast crash (bit_cast now folds the vector operand instead of null-derefing) — so R3 rework + re-scope collapse into one.
- **FOLLOW-UP OBLIGATION (do not drop):** #12219's ORIGINAL motivating repro `(uint2)float2(3.0,4.0)` is a float→int vector cast → relies on the now-DEFERRED castFloatToInt fold → **narrowed PR #12263 will NOT cover it.** Fixer flagged this on-thread + offered to file the float→int case as a follow-up issue. PENDING maintainer's answer to fixer's open question: bit_cast-only (also drop the DescriptorHandle round-trip cancel) vs. keep the round-trip. Fixer replied to maintainer (comment 5120132340) + reviewer directly; reviewer told HOLD R3.
- **WATCH:** when narrowed PR #12263 merges, confirm the deferred float→int/aggregate work is filed as a tracked follow-up (fixer offered) so #12219's original ICE repro isn't silently left unresolved. Chain self-driving; fixer owns rework, no blocker.
- **⚠️ R3 CORRECTNESS RISK (reviewer flagged 15:46Z):** the narrowed `bit_cast(vector↔scalar)` fold shifts the risk profile from *crash* (R2 null-deref) to *SILENT WRONG VALUE* — the fold's bit-packing/component-order convention MUST match the runtime/SPIR-V `OpBitcast` semantics, or a module-scope constant folds to a wrong-but-valid value with no error. Reviewer will require a **value test** pinning this on re-review. R3 HELD at fixer's request; reviewer recorded hold + scope-redirect in its memory (guards against compaction re-reviewing superseded head `d44d91b5c9`); re-reviews on new head when pinged.

**REWORK COMPLETE 07-29 16:39Z — new head `278f362112`, PR RETITLED "fold module-scope integer scalar/vector bit_cast in SCCP", R3 review REQUESTED (fixer→reviewer direct; R3 IN FLIGHT).** Scope now = fold constant integer bit_cast scalar↔vector equal-total-width (uint2↔uint64_t for DescriptorHandle + signedness variants + uint16_t2↔uint32); earlier element-wise numeric fold DROPPED. ✅ **Silent-wrong-value risk ADDRESSED:** value test pins exact bit values + runtime agreement (the reviewer-flagged OpBitcast component-order concern). repro folds to OpConstantComposite + SPIR-V-valid (aborts on master); full tests/spirv 557/557; codex PLAN/CODE/OUTPUT approve. Loop closed by fixer's direct edge — Main did NOT re-dispatch.

**R3 VERDICT 07-29 17:29Z (head 278f362112): APPROVE_WITH_NITS — 0 bugs, TRIPLE-CONVERGED (A+B+C all clean).** R2 bit_cast crash resolved correct-by-construction; scope matches pdeayton narrowing exactly. Reviewer SOURCE-VERIFIED the bit-lane packing convention matches SPIR-V `OpBitcast` (the correctness crux) + confirmed it's self-pinned by the runtime-agreement value test → silent-wrong-value risk CLOSED. Remaining: 1 test-coverage gap (splat `MakeVectorFromScalar` fold path untested — add `bit_cast<uint64_t>(uint2(7u))`) + advisory clarity/doc nits. **Next: fixer adds splat test (+ optional doc nits) → ready-for-review flip → merge (operator-gated).** Reviewer forwarded combined A/B/C to fixer; loop closed by direct edge; Main did NOT re-dispatch. Correctness = sound; PR is nits-away-from-mergeable.

**R3 FINAL 07-29 17:44Z — splat test + doc nits APPLIED, new head `2de4569fbd` (+295/−2, 1 source file + 2 tests).** Splat coverage gap closed (`bit_cast<uint64_t>(uint2(...))`); bit-lane convention element0=LSB cross-checked vs runtime bit_cast in-shader; tests/spirv 557/557; codex PLAN+CODE+OUTPUT approve. **Chain closed on fixer side — AWAITING HUMAN MAINTAINER MERGE (operator-gated; fixer will NOT flip ready/merge).** CI red = benign draft-dispatch priority-yield (build/test skipped). Fixer re-engages only on maintainer comment (webhook-driven). Triager armed to refresh footprint + forward final [Triage Resolution] on merge. Head arc: 82dfaf08(R1)→d44d91b5(R2 superseded)→278f3621(R3 rework)→2de4569fbd(R3 final w/ splat test).

**⚠️ CHAIN EVOLVED BEYOND THIS TOPIC FILE via other sessions (see MEMORY.md index for live state).** Per index: approach pivoted **SCCP→peephole** (pdeayton directed the SCCP fold reverted), now at **R7 clean @6f60947794** per index / head `31cc9261b1` per fixer msg 78488 (07-30 20:05Z). Latest fixer round: 2 more pdeayton-nv PR comments handled — rigorous audit ran all 60 `DescriptorHandle`/`DescriptorHeap` tests = **746/746 pass, exactly 1 affected sub-test** (`desc-heap-direct-index.slang.4` WGSL) proven a **correctness IMPROVEMENT not regression** (peephole cancels heap handle uint2 round-trip → WGSL array index now scalar `_slang_resource_heap[tid.x]` matching the HLSL check, vs prior invalid vec2 index; causation proven — sole functional change is the peephole, none of 11 ahead-of-HEAD master commits touch that path). Added `tests/spirv/gh-12219-fold-global-descriptor-handle-bridge.slang` (cross-width as<IRBitCast> bridge both directions, SPIR-V-val passes). codex CODE+OUTPUT approve; pdeayton resolved a pr_review_thread positive. Draft, OP-gated merge; ⚠️ float→int follow-up still owed (per index). **Main NOT actively driving this chain** (other-session-owned; Main's active role was R1–R4 SCCP-fold review relay, superseded by the peephole pivot). Fixer owns, webhook-driven, awaiting maintainer merge.

**⚠️ MAINTAINER-DESIGN-CONFLICT 07-30 20:43Z (fixer HOLDING correctly, not picking between maintainers):** @csyonghe inline-questioned whether `findDescriptorHandleRoundTripSource` is more complex than needed ("just return inner operand") — in direct TENSION with @pdeayton-nv who just asked to ADD the cross-width uint2↔uint64 cases that same logic supports. Fixer replied inline (r3686117681): same-width branch already IS "just return inner operand" (all #12219 strictly needs); cross-width branch can't (type mismatch) so it recognizes an existing `bit_cast` bridge + returns pre-bridge source. Offered clear scope choice — same-width-only (defer cross-width to #12186) OR keep cross-width per pdeayton — did NOT unilaterally decide. **Two core maintainers must resolve scope on GitHub; fixer holds (if same-width-only: drop bridge branch+test ~5min; if cross-width: no change).** No Main action (maintainer-owned scope call; correct closest-to-the-state handling — surfaced the tension to both rather than picking). Not blocked, just awaiting maintainer convergence. Mirrors the [[project_12266...]]/#12266 skiminki-vs-csyonghe design-fork pattern: fixer neutral, holds code, awaits maintainer convergence.

**✅ SCOPE FORK RESOLVED 07-31 01:02Z — pdeayton decided SAME-WIDTH-ONLY (fixer msg 80194).** Maintainer resolved the tension decisively: keep ONLY the same-width `unwrap(wrap(x)) -> x` fold (uint2 + uint64), inline per-case with `as<IRCastUInt2ToDescriptorHandle>` + `getValue()`. **This ALSO satisfies csyonghe's complexity concern** — both maintainers converged on the simpler form (cross-width deferred to #12186). Fixer narrowing: rewrote the 2 peephole cases inline, DELETED `findDescriptorHandleRoundTripSource` helper + the cross-width bridge test; acked both maintainers (PR comment 5137947704). Debug rebuild in flight (~20min subagent). The 3 remaining tests (reproducer + values + bindless) are all same-width round trips → still abort on master, fold with narrowed peephole. On green: re-run gh-12219 + descriptor suites, PR-body rewrite (drop cross-width/#12186 framing), codex CODE+OUTPUT, push, draft CI, report. Neutral-hold pattern worked — surfaced the fork, both maintainers aligned on the simpler answer. Fixer owns; WATCH-only for Main; merge OP+maintainer-gated.

**✅ NARROWING COMPLETE + GREEN 07-31 01:09Z, head `f307900bef` (fixer msg 80196).** Peephole = 2 inline same-width cases (`CastDescriptorHandleToUInt2(CastUInt2ToDescriptorHandle(x))->x` + UInt64), helper deleted, cross-width bridge test removed. Combined PR diff: peephole +20 lines, 3 same-width tests (const/bindless/values) + WGSL check fix — minimal. Verify: 3 tests pass; **tests/spirv 558/558**; cross-width shape now correctly ABORTS again (E99997) — proving the narrowing dropped EXACTLY cross-width coverage (deferred #12186), nothing more (good scope-precision check). codex CODE+OUTPUT approve. Amended+force-pushed; PR body rewritten one pass (cross-width framing removed); draft CI dispatched (benign priority-yield). Replies posted (5137947704 ack, 5137999252 green-confirm). Awaiting maintainer merge; draft, OP-gated. ⚠️ float→int follow-up still owed per index.

**✅ pdeayton APPROVED + FLIPPED NON-DRAFT 07-31 ~07:06Z, then cleanup-push dismissed it (Main-VERIFIED via GitHub API).** pdeayton approved on head `f307900bef` + flipped PR non-draft (maintainer's action, not bot — guardrail intact), then asked for a stale-reference sweep. Fixer found+fixed ONE real item (`values.slang` test comments/identifiers said "bit_cast-bridged" cross-width-era → renamed `gDirect`/`gBitCast` + reworded both-are-same-width; issue #12219 Fix section rewritten to the 2 same-width rules, dropped cross-width/#12186/SCCP wording), pushed `f9fbdf92b8`. **Main verified: pdeayton's approval on f307900bef now shows DISMISSED (07-31 07:06:10Z) — dismissed by the cleanup push, EXACTLY as expected** (any push dismisses; cleanup was pdeayton's own request). Current: `draft:false`, head `f9fbdf92b8`, `mergeable:true`, review_decision=null (REVIEW_REQUIRED). Fixer flagged in reply 5140315176 it's a comments-only delta, ready for re-approve; does NOT flip ready/merge. Real (non-draft) `pull_request` CI now genuinely builds — queued, fixer monitoring. (Fixer also mistakenly fired a redundant manual workflow_dispatch = the benign priority-yield red; logged lesson.) **Awaiting pdeayton re-approve (comments-only, should be quick) + full CI green → maintainer merge.** Chain effectively approved-modulo-a-cleanup-redraw; near-terminal.

**✅ RE-APPROVED 07-31 07:33:40Z — pdeayton APPROVED on CURRENT head `f9fbdf92b8` (Main-VERIFIED via GitHub API; review pullrequestreview-4826398401).** The individual APPROVED review is on the current head (NOT stale/dismissed) — the cleanup push's dismissal is fully reversed. All pdeayton + csyonghe thread comments resolved (narrowing, switch/test/issue updates, audit, stale-comment sweep). PR non-draft, mergeable. **⚠️ Honest nuance:** aggregate `review_decision` via REST API shows `null` (fixer read `reviewDecision:APPROVED` via GraphQL `gh pr view`) — REST/GraphQL representation difference OR a still-pending 2nd requested reviewer (csyonghe was requested; his thread comments resolved but he used COMMENT state, not formal Approve — same "verbally-positive-but-no-formal-approve-flag" pattern seen on #11961/#12111). Load-bearing fact holds regardless: **maintainer (pdeayton) approval LIVE on current head, mergeable, non-draft.** **TERMINAL-PENDING-MERGE: awaiting only CI green (real non-draft build running) + human merge press.** Fixer correctly holding — no push (would re-dismiss), no flip/merge (OP+maintainer-gated). float→int follow-up still owed per index.

**Reviewer re-pinned verdict to approved head 07-31 07:38Z:** APPROVE_WITH_NITS/0-bugs now verified against the real approved head `f9fbdf92b8` (not stale); same-width narrowing SOUND (matched-op-pair fold enforces type-identity structurally; only untested #12186 forward-support dropped; all 3 regression tests still fold). ⚠️ **ONE MERGE-MECHANICS ITEM: PR is `behind` → needs a REBASE before merge** (reviewer flagged, correctly scoped as fixer's concern NOT correctness). TENSION: a rebase push would re-dismiss pdeayton's fresh approval (the recurring push-dismisses-approval bind on this chain) — so the merge likely needs the maintainer to either merge-with-update-branch (GitHub can rebase+merge without dismissing) or re-approve post-rebase. Fixer owns the rebase decision; correctly not pushing unprompted. Reviewer done from its side (re-engages only on real CI failure or new review request).

**✅✅ [Fix Report — final] 07-31 09:00Z — CHAIN CLOSED ON FIXER SIDE; MAINTAINER-MERGE-ONLY (Main-VERIFIED via GitHub API).** Main confirmed at head `f9fbdf92b8`: `mergeable:true`, `mergeable_state:blocked`, check-runs **29 success / 1 skipped / 0 failure** (CI GREEN — earlier Falcor-600s-timeout + git-submodule-checkout-lock flakes cleared on rerun), **pdeayton APPROVED review STILL LIVE on current head** (verified 1 match on f9fbdf92b8). The `blocked` state is SOLELY branch-protection's "up-to-date before merge" (17 commits behind master, NO conflicts) — clears via the maintainer's one-click "Update branch"/admin-merge, which preserves the approval (do NOT bot-rebase: would dismiss approval + trigger full rebuild). Final change: narrow same-width DescriptorHandle round-trip peephole (2 inline cases, no helper) + 3 same-width regression tests + 1 WGSL check update; SCCP approach + cross-width bridge fully removed (deferred #12186). tests/spirv 558/558. All maintainer/reviewer comments across every round resolved; pdeayton approved + slang-reviewer 0-bug verdict both pinned to this head. **TERMINAL-PENDING human merge only.** ⚠️ float→int follow-up still owed per index (deferred numeric-cast fold — file when #12263 merges, tracks alongside the #12186 cross-width deferral).

**Triager [Triage Resolution — review milestone] 07-29 17:48Z:** tier-consolidated (NOT a dup of reviewer's direct verdict). Confirms review COMPLETE, draft @ 2de4569fbd, `closes #12219`, awaiting **human merge ONLY** (merge auto-closes #12219 via Fixes link). Footprint comment (verdict "held pending review + merge", still accurate): https://github.com/shader-slang/slang/issues/12219#issuecomment-5118314585 — triager HOLDING interim GitHub refresh until merge per no-mid-review-churn steer; PR body carries R3 approval. **On merge: triager re-reads narrowed diff → refreshes issue footprint to final shape → forwards final resolution.** CI red = benign draft priority-yield.

**CI note 07-29 19:10Z (aged/force-run 30476592931, REAL builds this time):** the ONE red = `tests/hlsl-intrinsic/scalar-bf16.slang` (vk+cuda) — **PRE-EXISTING + orthogonal to #12263, NOT this PR's regression.** Fixer verified 4 ways: (1) fails identically on clean master @ 71a3f7e71a; (2) `evalIntegerVectorBitCast` BAILS on bf16 (non-integer result) — spirv-asm confirms fold leaves this test's IR unchanged; (3) failing CHECK line is ENVIRONMENT-dependent (CI line 7=`2`=sizeof(BFloat16); local L40S line 10=`3`) → GPU/driver bf16 numeric discrepancy, not deterministic compiler output a fold could cause; (4) own 2 gh-12219 tests 2/2, full tests/spirv 557/557. **Main corroborated (verify-at-precision):** active owned bf16/FP8 problem-space exists (open #10938/#10750/#12042, owners jvepsalainen-nv + skiminki-nv), and NO tracking issue found for this specific `scalar-bf16` failure — consistent w/ pre-existing-but-untracked. Non-regression evidence (env-dependent CHECK line) is a strong shape-match. **Disposition: red NOT attributable to #12219; fixer correctly not touching test / not re-dispatching; merge stays op-gated.** ⚠️ WATCH-ITEM (bf16 owners' domain, NOT #12219): `scalar-bf16.slang` may be an untracked GPU-dependent flake — a maintainer merging #12263 must know the red is benign (triager close-out carries this). Not Main's to file mid-review without a build-env confirm of "fails on clean master".
- **FOLLOW-UP STILL OWED:** #12219's original motivating repro (`(uint2)float2(3.0,4.0)`, float→int) falls under the DEFERRED numeric-cast work — narrowed PR does NOT cover it. Fixer flagged on-thread + offered to file follow-up; PENDING maintainer's word. Narrowed PR DOES fully fix the integer-bit_cast form (DescriptorHandle uint2↔uint64 case pdeayton named). ⚠️ On merge: confirm follow-up filed so original ICE repro isn't silently left open.
- **Arc note:** 2 peer-review rounds caught 2 real bugs the fixer had introduced (R1 splat OOB, R2 bit_cast segfault) — both fixed; fix materially stronger. Adversarial review earned its keep here.

**⚙️ WHOLESALE REWORK → PEEPHOLE (07-30 02:02Z, maintainer-directed) — R5 APPROVE_WITH_NITS.** Between R3-final (SCCP fold @ 2de4569fbd) and now, @pdeayton-nv directed a **complete mechanism change**: the SCCP implementation is **fully reverted to master**; the fix is now a **targeted peephole in `slang-ir-peephole.cpp`** cancelling the DescriptorHandle round-trip. R1–R4 verdicts all MOOT (different code). Reviewer ran a FRESH 3-reviewer pass on the new mechanism @ head `9fb9f158fd`.
- **R5 verdict: APPROVE_WITH_NITS — 0 bugs (triple-verified A+C+reviewer's own source read), 1 test gap, 1 doc question.** Peephole is cleaner than the SCCP versions; **the entire R2 bug-class can't recur** (no lattice changes).
  - **Gap:** uint64 fold branches untested. Reviewer A refuted the "needs GPU" excuse with in-tree precedent `gh-9916.slang` (uses compile-time `-capability spvBindlessTextureNV`) — reviewer verified. Recommends adding the uint64 test before operator merge; **fixer's call** (live code, no coverage).
  - **Doc question:** doc says bidirectional bridge but code matches inner-side only — a missed-opt, NOT a wrong fold.
- **Next-action: none blocking.** Fixer decides on the uint64 test; ready-flip + merge stay operator-gated. Reviewer forwarded verdict + reported to parent(Main); **chain closed on reviewer side.** Main did NOT re-dispatch (fixer owns the test decision; no-double-dispatch).
- **⚠️ INTEGRITY-FAIL false-positive — 3RD OCCURRENCE (R2, #12262-mirror, now R5).** This round a concurrent **PR-12280** run clobbered the shared tmp. Reviewer verified the correct diff each time via the run's own footer/reference. Strengthens the parked worktree-isolation hardening proposal (handed to operator 07-29 15:35Z; bot lacks slang-skills write). **Main gave operator a frequency bump 2→3.** Evidence now robust per reviewer. Still non-blocking (each instance caught + worked around by footer-hash check).

Head arc: 82dfaf08(R1)→d44d91b5(R2)→278f3621(R3 rework)→2de4569fbd(R3 final SCCP)→9fb9f158fd(R5 peephole, SCCP reverted per pdeayton)→**e6edddbe1e(R5 final + uint64 test + clarity nits)**.

**✅ [FIX REPORT — R5 FINAL] 07-30 02:22Z — head `e6edddbe1e`, AWAITING HUMAN MERGE (op-gated).** Fixer closed the one actionable R5 gap (added the uint64 fold-branch regression test = bit_cast-in-chain texture handle under `spvBindlessTextureNV`; aborts on master, folds after fix) + applied clarity nits. PR #12263 draft, `Fixes #12219`, +144 across 1 source file (`slang-ir-peephole.cpp`) + 3 tests. codex PLAN+CODE+OUTPUT approve; **tests/spirv 558/558** (+1 vs R3's 557 = the new uint64 test). CI red = benign draft-dispatch priority-yield (build/test skipped). **No re-review needed:** the head advanced by a test-only + comment addition closing the exact reviewer-recommended gap on unchanged peephole source — reviewer pre-blessed adding the test ("fixer's call, I recommended it"). **Chain closed on fixer side pending human merge; fixer re-engages only on maintainer comment (webhook).** Triager armed to re-read final diff → refresh issue footprint → forward final [Triage Resolution] on merge (footprint comment 5118314585, "held pending review + merge", still accurate; PR body carries R5 approval).

**✅ REVIEWER FINAL [Report] 07-30 02:24Z — chain closed on reviewer side.** Reviewer did a **delta-verify from source (NOT a fresh pass)** on the test+doc delta over an already-APPROVE PR — matches Main's "no re-review needed" call. R5 APPROVE_WITH_NITS stands, all nits/gap/question closed. Reviewer confirmed the uint64-branch test is **genuinely discriminating** (avoids the false-test trap): same-width round-trip emits fine on master via transparent forwarding; the fixer's bit_cast-in-chain texture-handle shape produces the surviving global bitcast that aborts on master and folds ONLY through the new branch. No action from review chain; awaiting operator/maintainer merge on the draft. Reviewer re-flagged the 3× shared-tmp INTEGRITY-FAIL → worktree-isolation proposal (already in operator queue via Main #77045). ⚠️ FOLLOW-UP STILL OWED (see above): #12219's original float→int repro `(uint2)float2(3.0,4.0)` falls under DEFERRED numeric-cast work — narrowed peephole does NOT cover it; confirm follow-up filed on merge so original ICE repro isn't silently left open.

Prior state (superseded): PARKED at triaged+scoped; fixer not dispatched pending operator "say the word".

Canonical thread: `gh-issue-shader-slang/slang-12219`.

**✅ R6→R7 delta 07-30 19:03Z (reviewer, id 78464) — head `6f60947794` (was e6edddbe1e).** Maintainer post-approval comments addressed; **correctness DONE.** APPROVE_WITH_NITS, 0 bugs, all findings closed across **six rounds**. Reviewer did delta-verify from source (NOT fresh pass) + closed loop with fixer; did NOT re-send an identical rollup (behavior-preserving delta = duplicate report avoided — correct).
- **R7 source-verified:** the switch-rewrite is behavior-preserving; the replaced value-test was **false-green** (vector `==` → `bool2`, `CHECK: 1` matched the *result code* not the intent) and its removal is strictly correct.
- **Bit-lane coverage nuance (honest scope boundary):** reviewer championed bit-lane coverage for the R3 **SCCP** approach, but for the **peephole** approach the lane split is `OpBitcast`'s spec behavior (not this PR's code) → compile-only coverage is a **defensible scope boundary**, not a gap. Different concern for different mechanism.
- **State UNCHANGED from parent's view:** peer-review-clean; awaiting operator/maintainer merge on held draft #12263. Chain closed on reviewer side; nothing actionable. Head arc extends: e6edddbe1e→**6f60947794**. ⚠️ FOLLOW-UP float→int `(uint2)float2(3.0,4.0)` still owed on merge (unchanged).

**[FIX REPORT confirm] 07-30 19:08Z (fixer, id 78466) — head `6f60947794`, terminal pending op-merge.** Fixer's parent-edge report confirms the R7 milestone (same as reviewer 78464). Specifics: **4 files +156** — `slang-ir-peephole.cpp` new helper `findDescriptorHandleRoundTripSource` cancelling `CastDescriptorHandleToUintN(CastUintNToDescriptorHandle(x))→x` incl. the `bit_cast`-bridged cross-width form (covers #12186) + 3 `tests/spirv` SIMPLE tests. **SCCP fully reverted** (`slang-ir-sccp.cpp` = 0-diff vs master). `tests/spirv` 558/558; reproducer aborts E99997 on master, compiles+passes SPIR-V validation after. The **3 maintainer @pdeayton-nv comments resolved:** (a) `if(as<>)`→`switch(getOp())` rewrite; (b) broken INTERPRET test→compile-only SPIR-V SIMPLE; (c) #12219 title+body + PR body refreshed. codex CODE/PLAN/OUTPUT approve. CI red = benign draft priority-yield (only `wait-for-human-priority`+`check-ci`; 33 downstream skipped). **No outbound: same milestone as 78464 (no duplicate operator report), fixer not asking for anything (no echo). Merge stays op-gated; drafts-only guardrail intact.**
