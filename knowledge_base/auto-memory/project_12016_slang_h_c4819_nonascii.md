---
name: project_12016_slang_h_c4819_nonascii
description: "#12016 slang.h non-ASCII em-dashes trip MSVC C4819 — triaged, fixer dispatched (draft PR incoming)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6a3bef7c-5e33-4873-8b00-b822c94d91d8
---

**#12016 — Non-ASCII comments in `include/slang.h` trigger MSVC C4819.** Public header has 3 U+2014 em-dashes at slang.h:1184/4242/4248 (only non-ASCII bytes in `include/`). Under MSVC `/source-charset:.932 /WX` → C4819 promoted to C2220 error; `/utf-8` avoids it. Portability regression (header was ASCII-clean before 5ead59ffd5 & 2d6971c309 — latter our own bot).

**Verdict:** bug / low / P2 / public-header. Author pdeayton-nv (maintainer). Root cause verified at HEAD 5d5183617 (== reported version). Rec fix: replace ` — ` → ` - ` (spaced ASCII hyphen, matches file style, comment-only, **zero ABI impact**).

**State (07-09):** DRAFT PR **#12018** open + verified. Initially +3/−3 (slang.h only); **scope widened at reporter's request** (~07:42Z) — pdeayton-nv left informal PR comment "looks good... could you also review all public headers for similar issues"; fixer swept shipped headers. Now branch tip **0317683577**, **3 files +10/−10**: include/slang.h (3 em-dashes) + prelude/slang-cpp-prelude.h (1) + prelude/slang-cuda-prelude.h (5 em-dashes + 2 U+2192 arrows). Preludes install into include/ & #include into generated CPU/CUDA output → same C4819, legit expansion, one PR. All comment-only across all 3 — **zero code, zero ABI**. Triager verified ASCII-clean at tip (`git grep -P '[^\x00-\x7F]' include/ prelude/` empty). `Closes #12016`, `pr: non-breaking`, MERGEABLE, codex PLAN/CODE/OUTPUT all APPROVE (0 must-fix).

**✅ TERMINAL (07-09 ~08:07Z) — APPROVED + READY, awaiting human merge.** Triager verified via GraphQL at 08:01Z: maintainer **pdeayton-nv submitted a formal APPROVE review** (state=APPROVED, 08:01:22Z) AND **flipped the PR ready himself** (08:01:30Z). #12018 now **non-draft, OPEN, reviewDecision=APPROVED, MERGEABLE**, `Closes #12016`, 3 files comment-only (zero ABI). Review gate genuinely satisfied.
- **Non-draft is NOT a drafts-only breach** — verified pdeayton-nv (human maintainer) did the ready-flip, not the fixer (per feedback_drafts_only_guardrail: "non-draft bot PR ≠ breach if MAINTAINER flipped (verify who)").
- **Reviewer /login blocker MOOT for #12016** — human approval takes the bot peer-reviewer leg off the critical path. Go-signal protocol I set up is now unnecessary (no bot re-dispatch needed). **BUT the reviewer /login issue STILL STANDS for #11969** (separate chain) — my operator escalation remains valid, narrowed from 2 chains to 1.
- **Two fixer overstatements caught+corrected by triager this chain (verify-before-relay wins):** (1) "reporter approved" at 07:36Z was informal (reviewDecision was REVIEW_REQUIRED) — the 08:01Z one is the REAL formal APPROVE; (2) "held as DRAFT pending human ready+merge" went stale — pdeayton-nv already flipped ready.
- **✅ SHIPPED (07-09 ~11:33Z) — CHAIN CLOSED, TERMINAL.** PR #12018 merged into master (squash `135c935183`, confirmed on origin/master); issue #12016 auto-closed `completed` at 11:31:22Z via `Closes` link. Triager verified merge commit + issue state via git + API directly. Final fix: 3 files comment-only +10/−10 (slang.h 3 em-dashes + slang-cpp-prelude.h 1 + slang-cuda-prelude.h 5 em-dashes + 2 U+2192 arrows) → include/ + prelude/ fully ASCII, resolves C4819/C2220 under non-UTF-8 charsets. Approved by 2 maintainers (pdeayton-nv formal + skiminki-nv); merged by jvepsalainen-nv. Bot peer-reviewer never dispatched (/login) but moot — human approvals + merge satisfied gate. One pre-merge red = infra flake (JSON-RPC test-server drop on unrelated test; codegen-equivalent platforms green), reran + merged.
- **Carry-forward (NOT owned by this chain):** fixer flagged Approach-B follow-up on the PR — a CI/lint guard rejecting non-ASCII bytes in shipped headers (slang.h regressed twice in 2 months, once via our own bot). Left for maintainer consideration.
- **Reviewer /login note:** was moot for #12016; escalated for #11969 (separate chain). **CLEARED (07-09 ~15:19Z):** post-update (build 2.1.40) slang-reviewer is addressable again — two fresh review sessions running at 15:15Z (#12023, #12020, container running), vs the 07:01Z "not addressable" failure. My operator escalation for a reviewer re-auth is now moot. #11969's own chain owns any review re-dispatch (its review session went idle 03:13Z — not Main's to drive; supervisor/chain-owner handles). GitHub trail complete: triage verdict cmt 4922278031 + fixer fix-drafted footprint cmt 4922409261 + scope-widening delta cmt 4922781564 (bot token create-only → fresh incremental comment, not edit-in-place; 403 PATCH known) + reporter's PR reply cmt 4922741672. Type=Bug + `regression` label set. `reproduced` NOT applied (C4819 needs Windows+MSVC+CP932; bytes confirmed present regardless). Verified deterministically: `grep -rP '[^\x00-\x7F]' include/` empty on branch, clang-format byte-identical, zero ABI. E2E MSVC compile left to CI (draft → `pull_request` CI skips, expected).

**⚠️ BLOCKER — peer review NOT done.** slang-reviewer was unreachable ("/login") when the fixer tried → fixer→reviewer edge failed; fixer saved a retry TODO. Triager has NO reviewer destination, didn't re-dispatch. Main (07-09 ~06:53Z): routed retry THROUGH triager → fixer as a live reachability probe (memory showed reviewer re-authed ~03:13Z on #11969, so worth probing before a false-alarm escalation).

**Retry FAILED (07-09 ~07:01Z):** fixer re-sent [Fix Review Request] on canonical thread → runtime returned **"No agent named 'slang-reviewer' is currently addressable"** — same negative as ~06:50Z. Reviewer genuinely unreachable now (probe negative twice). → **Main escalated to operator via PushNotification for a reviewer `/login`** (07-09 ~07:01Z). Chain HELD safely (fix verified + codex PLAN/CODE/OUTPUT all APPROVE, 0 must-fix; nothing at risk). Did NOT post to GitHub (internal infra state, not issue-substance; public trail already complete). Did NOT restart reviewer group (recurring auth `/login` issue per memory; restart wouldn't re-auth + would disrupt other reviews). Did NOT dispatch reviewer directly (fixer owns that edge via triager). **Go-signal protocol:** operator re-auths → Main signals triager → triager pings fixer to re-dispatch → [Review Report] flows up → maintainer ready-flip + merge (both gated). NEXT: await operator `/login` confirmation, then release the go-signal down the triager→fixer edge.
