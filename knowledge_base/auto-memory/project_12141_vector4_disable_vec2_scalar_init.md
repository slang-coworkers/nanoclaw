---
name: project-12141-vector4-disable-vec2-scalar-init
description: "#12141 vector<T,4> disable (vec2,T)/(T,vec2) init — R3 ABSTAIN_POLICY (OPEN_GAP); R1/R2 BLOCK resolved"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6aeeb7f9-1654-419f-808a-f5eb61fb09f3
---

**#12141** (shader-slang/slang, author skiminki-nv, fork PR @ c51b47865483) — explicitly disables `(vector<T,2>, T)` and `(T, vector<T,2>)` initializers for `vector<T,4>` via `static_assert(false)` in `core.meta.slang`. Same area as [[project-12093-vector-initlist-coercion-inconsistency]] (vec init-list splat vs tail-pad, GO).

**slang-pr-approver verdict: BLOCK (RED_BUG)** — recorded to ledger 07-17, shadow mode (no GitHub write), mode=live, policy v0-shadow-relaxed, critique-gated (DECISION_REVIEW + OUTPUT_REVIEW both approve).

- Synthesized review doc parsed APPROVE_WITH_NITS (PRIMARY github-actions[bot] 0🔴/1🟡/1🔵 + Devin 0/0/1); all 6 eligibility clauses pass.
- **But challenger's independent check-run harvest found a PR-CAUSED hard CI regression both reviews missed:** the new `static_assert(false)` turns `float4(float2, scalar)` into hard error E41400, breaking the pre-existing bundled slang-rhi test `external/slang-rhi/tests/test-ray-tracing-clusters.slang:74` (`float4(attribs.barycentrics, 1.f)` — barycentrics is float2 → exactly the disabled `(vec2,T)` shape).
- Fails ray-tracing-cluster CUDA+Vulkan × Linux+Windows (4 test-slang-rhi jobs + check-ci aggregate). Red at head, deterministic (0.08–1.48s), GPU-clean, not infra. Codex re-verified E41400 from live job log.
- Both bot review and Devin missed it: neither runs the slang-rhi integration suite.

**Next-action (human/author):** land bundled slang-rhi shader fix alongside — update `test-ray-tracing-clusters.slang:74` to 4-element vec4 form + bump submodule (upstream slang-rhi first) — or reconsider the hard-disable. **Rebasing onto master does NOT resolve it** (master's slang-rhi shader still uses the disabled form).

Shadow-mode BLOCK: ledger-only, no GitHub post ([[feedback-approver-never-posts-route-reviewer]]). Await human review / author action.

**07-17 UPDATE — companion downstream fix FILED:** author skiminki-nv opened **slang-rhi#798** ("test-ray-tracing-clusters.slang passes only 3 components to float4() initializer", labeled Dev Opened) proposing exactly the fix the challenger identified: `test-ray-tracing-clusters.slang:74` `float4(attribs.barycentrics, 1.f)` → `float4(attribs.barycentrics, 1.f, 1.f)` (semantics-preserving; the old `(vec2,scalar)` form duplicated the scalar rather than the expected behavior). This is the slang-rhi-side prerequisite that must land + submodule-bump before #12141's hard-disable can go green. Routed to slang-triager on thread `gh-issue-shader-slang/slang-rhi-798`. Cross-repo landing order: slang-rhi#798 merges → slang submodule bump → then #12141 CI passes.

**07-17 — author opened slang-rhi PR #799** (skiminki-nv, comment 5001578177: "PR #799 should address this"). This is the fix PR carrying the `test-ray-tracing-clusters.slang:74` 3→4 component change; maintainer-authored, self-driven. Forwarded to slang-triager on the canonical thread. #799 is the prerequisite PR for #12141's CI.

**07-17 — slang-rhi#798 TRIAGE-RESOLVED (slang-triager, issue comment 5001624182).** PR #799 (skiminki-nv, non-draft, MERGEABLE, base main, head 798-test-fix, +1/-1, `Fixes #798`) verified correct/complete/semantics-preserving via ctor trace @ slang HEAD 5c30d437f: `barycentrics` is `float2` (hlsl.meta.slang:19565); old form splats scalar→`float2(1,1)` selecting `__init(vec2,vec2)` @core.meta.slang:2796 → `(x,y,1,1)`; fix selects `__init(vec2,T,T)` @2784 → `(x,y,1,1)` — identical. Author's `1.f,1.f` (NOT `1.f,0.f`) correctly preserves current behavior, not legacy tail-pad w=0. Classification: test-bug / low / P2 / cross-repo submodule-sync. No fixer dispatched (maintainer self-authored).

**07-17 15:03Z — #12141 REWORKED (approach changed), re-gate HOLDING for CI.** New head `13d1c111` (6 files, +181/-16) does NOT bump the submodule. Author dropped the hard `static_assert(false)` and instead marks the `(vec2,T)`/`(T,vec2)` vec4 initializers `[deprecated]` + `[RemovedSince(2026)]` with bodies preserving legacy splat semantics: default / `-std 2025` / `-std legacy` → deprecation **warning** (still compiles); `-std 2026` → hard error. Plus checker fixes (`slang-check-conversion.cpp`, `slang-check-expr.cpp`) to surface the deprecated-ctor diagnostic while respecting the `outExpr==nullptr` viability-probe invariant. Because the breakage is now a *warning* by default, the R1 slang-rhi fix is NOT required — submodule still at `29dc332` (2 behind `1afb8384`/#799), `:74` still `float4(float2,1.f)`. Whether this holds depends on whether `test-slang-rhi` treats the deprecation warning as -Werror → **only CI settles it.** Early signal good: `test-macos-release-clang-aarch64-rhi` = success, 0 failures so far; CodeRabbit at head 0🔴 (nits + C++17 if-init note). Approver re-harvesting when the primary `review` check-run lands (not deciding off CodeRabbit alone). **NOTE: #799/submodule-bump path is now MOOT for #12141 unless CI shows the warning is fatal.**

**07-17 16:05Z — R2 RE-DECIDED: BLOCK (RED_BUG)** @ head `13d1c1116704`, recorded to ledger (shadow mode, no GitHub write), mode=live, policy v0-shadow-relaxed, critique-gated. Held for CI to settle. Two-part finding:
- ✅ **R1's slang-rhi break IS FIXED.** The `[deprecated]`+`[RemovedSince(2026)]` warning approach clears E41400 without touching the shader — `float4(float2,1.f)` now warns (hard error only under `-std 2026`). All completed `test-slang-rhi` legs green (3 of R1's 4 red legs done green; 4th windows-debug-rhi still running at decision, no rhi failure anywhere). **→ #799/submodule-bump path is confirmed MOOT for #12141.**
- ❌ **R2 introduces a NEW distinct regression.** The `slang-check-conversion.cpp` refactor adds `forwardDiagnostics()` on the constructor **success path**, surfacing previously-swallowed `warning[E30081] implicit conversion not recommended`. Breaks two PRE-EXISTING tests (NOT in PR's changed files): `tests/glsl/integer_pack.slang (vk)` + `.slang.1 (vk)` — assert empty stderr, now emit 12 E30081 from `u16vec2`/`i16vec2`/`u8vec4` narrowing inits. Deterministic on BOTH GPU-equipped x86_64 vk legs (linux-release job 87923337957 + windows-release 87924078883). aarch64/macOS mark those vk tests `ignored` (no Vulkan GPU) → green=skipped not passed → not flaky. 403 artifact flake on linux-debug-x86_64 = infra, excluded.
- **Next-action (author):** scope the success-path `forwardDiagnostics()` so it doesn't leak pre-existing narrowing/E30081 warnings — forward only error-severity on success path, or gate on the deprecated/removed ctor specifically — or update `integer_pack.slang` expectations; then re-run. Redesign is on the right track; contained diagnostic-forwarding side effect.
- Primary `github-actions[bot]` `review` check-run stuck in_progress ~40min → decided from fallback tier (CodeRabbit 0🔴 + Devin 0) + own CI harvest. Both bot signals missed it (neither runs test suites); challenger CI gate caught it, codex re-verified. **Challenger has now averted a miss on BOTH #12141 revisions.**

**07-17 17:27Z — R3 RE-DECIDED: ABSTAIN_POLICY (OPEN_GAP)** @ head `b4e6a60e8366` (7 files, +183/-20), recorded to ledger (shadow mode, no GitHub write), mode=live, policy v0-shadow-relaxed, critique-gated. CI + primary review both settled before deciding. **A hold, UP from BLOCK on R1 and R2.** All three verification points check out:
- ✅ **R2 E30081 regression FIXED** — all 3 GPU x86_64 test-slang legs running integer_pack vk pass (`test-linux-debug-gcc-x86_64`, `test-linux-release-gcc-x86_64`, `test-windows-release-cl-x86_64-gpu`). Author added explicit `(uint16_t)/(int16_t)/(uint8_t)` casts to `integer_pack.slang`.
- ✅ **R1 slang-rhi break stays fixed** — all completed `test-slang-rhi` legs green, not reintroduced.
- ✅ **No new distinct regression** — 0 failures across 40+ jobs.
- **Why not WOULD_APPROVE:** production PRIMARY review (github-actions[bot], posted late; re-harvested after review check-run went green, upgraded from CodeRabbit fallback) = 🟡 0🔴/4🟡/1🔵. Gap #1 is a real CI-invisible design concern, independently verified in source: R3 fixes the broken test with casts but does NOT scope the `forwardDiagnostics()` call, which stays on the constructor **success path**. That call re-emits the temp sink's entire raw buffer via `diagnoseRaw()` for **every** struct/vector explicit-ctor coercion (not just vec4), and — raw re-emission with no diagnostic id — **bypasses per-id `-Wno-*`/`#pragma warning` suppression** and escalates a mixed buffer to Error. Plausible trigger, real blast radius, not false-negative-safe → conservative-lean ABSTAIN(OPEN_GAP). Reinforced by primary Gaps #2/#4 (behavior change w/ no owned regression test; no `-warnings-as-errors` promote-then-forward test — the very paths that would catch over-broad forwarding are untested).
- **Why not BLOCK:** no reproduced 🔴 — CI green, R2 regr fixed, rhi stable. Gap is latent/CI-invisible, not a currently-failing test.
- **Next-action (author):** scope `forwardDiagnostics()` to the deprecated/removed-ctor diagnostic (or re-raise through normal severity/suppression path instead of `diagnoseRaw` of whole buffer) + add the `-warnings-as-errors` promote-then-forward test the primary asks for. Then a human/re-gate can clear it.
- **Trajectory:** R1 BLOCK (rhi break) → R2 BLOCK (E30081 regression) → **R3 ABSTAIN/OPEN_GAP** (contained; one latent scope gap left). Held in shadow ledger awaiting author's scope fix or human confirm.

**07-17 (superseded by 15:03Z rework — moot for #12141) — slang-rhi#799 MERGED + APPROVER CLEAN.** slang-pr-approver decided **WOULD_APPROVE (CLEAN)** @ head `fc1e0e25` (mode live_late), recorded to ledger, shadow mode (no GitHub write). PR then MERGED 09:52:15Z as **`1afb8384`** by skiminki-nv → human join recorded APPROVED (COLLABORATOR skallweitNV approved that exact SHA). Pipeline: 6/6 clauses pass; Devin-only fallback tier (slang-rhi `claude.yml` = @claude-mention assistant, not review bot — harvest exit-20 expected); Devin head-current 0/0/0; CodeRabbit success; **all 18 build matrix legs green** (unit-test legs run this shader via OptiX ray-tracing). **#12141 unblock CONFIRMED (not assumed)** — verified vs actual `:74` diff + slang-rhi #798 byte-identical diff. **Landing order now:** ✅ #799 LANDED (`1afb8384`) → ⏳ slang-rhi submodule bump in shader-slang/slang → then #12141 CI clears E41400. **Next:** #12141 watchers confirm the submodule bump picks up `1afb8384` before re-gating #12141.
