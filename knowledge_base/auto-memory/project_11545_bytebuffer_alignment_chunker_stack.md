---
name: project_11545_bytebuffer_alignment_chunker_stack
description: "#11545 ByteAddressBuffer alignment-chunker series — [3/3] PR #11803 rebased onto [2/3] #11595 per jkwak; non-redundancy established; await jkwak review"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5bec4191-e017-44da-b211-e48a8839d909
---

shader-slang/slang **#11545** ByteAddressBuffer alignment/access series — the #11564 monolith split into a **stacked PR series** driven by slang-fixer. Canonical thread `gh-issue-shader-slang/slang-11545`.

- **Stack:** [2/3] = **PR #11595** (branch `fix/issue-11591`), [3/3] = **PR #11803** (slice-4, the widest-aligned-chunk optimization). [3/3] stacks on top of [2/3]. Related monolith issues #11564/#11596 (closes still on **operator hold**).
- **2026-07-16 07:11 — [3/3] non-redundancy established + rebased (fixer msgs 38904/38908/38910).** jkwak questioned whether [3/3] #11803 was redundant with [2/3] #11595. Fixer verified **at HEAD (not memory): NOT redundant.** [2/3] head has **0** chunker symbols — its not-wide-aligned path goes straight to `emitLegalSequenceLoad` (full per-component scalarize); [3/3] adds **12** chunker symbols inserting a widest-aligned-chunk middle before the scalar fall-through. Concrete delta: `LoadAligned<float4>(loc,8)` → four scalar `.Load<float>` on [2/3] alone vs two `.Load<float2>` with [3/3]. (= point 5 of the #11564 monolith.) Fixer replied w/ evidence + deferred scope to jkwak (issuecomment-4989078836).
- jkwak **accepted** the explanation, asked fixer to **rebase [3/3] onto [2/3]** for easier review. Done: slice-4 `8fa9726bb3`→`386c55056d` onto `fix/issue-11591`@`2f488df6d1` (now master-merged). Clean replay, no conflicts; content intact (12 chunker symbols, 2 doc blocks, 2 tests; 4-file diff +281/−10). `--force-with-lease`, PR head confirmed `386c55056d`, CI re-dispatched (run 29479076668, draft). Confirmed on-thread (issuecomment-4989173063).
- **✅ 2026-07-19 02:56 — [3/3] RETARGETED to master; approval gate restored (fixer msgs 48116/48118).** After jkwak's ~6-flip base-alignment arc, [2/3] **#11595 MERGED to master `c6a2610682`** (~02:35Z) as the natural-alignment revert (see [[project_11595_byteaddressbuffer_align_block]]). jkwak flagged [3/3] #11803 showed "ready for merge" WITHOUT approval — **root cause: non-draft (jkwak readied it) on base `fix/issue-11591`, an UNPROTECTED fix branch → no CODEOWNERS gate → GitHub showed it mergeable.** Fixer rebased slice-4 onto master (`386c55056d`→`1cfb3602cd`, clean, 4-file/+281−10, 12 chunker symbols intact) + **retargeted base `fix/issue-11591`→`master`**. Verified: `reviewDecision=REVIEW_REQUIRED` restored (gate back); real `pull_request` CI auto-fired (run 29670960590, incl. format check — catches clang-format the fixer couldn't run locally); did NOT manual-dispatch (non-draft auto-runs; manual = cosmetic red). Replied on-thread (issuecomment-5013899850). Correct stacked-PR-post-parent-merge handling (avoids [[project_stacked_pr_shared_base_clobber]]).
- **🔴 2026-07-19 03:21 — [3/3] shadow-approver BLOCK (RED_BUG) @`1cfb3602cd9b` (settled head, 15-min quiet-head debounce honored).** ready_for_review + 2× synchronize (rebase-to-master, single commit). Source = Devin-only fallback tier (harvest exit 20 — fixer PR, production claude-code-action review skips bot-authored PRs; Devin exit 0 head-current). All 6 eligibility clauses PASS under relaxed policy — RED_BUG is challenger evidence, not a clause fail. **Two reproduced host-independent test-slang FileCheck failures (multiple aarch64 legs, run 29670960590):** (1) the PR's OWN test `byte-address-buffer-chunked.slang:41` — store-chunking emits only ONE `float2` store where the test asserts CHECK-COUNT-2; (2) a **pre-existing UNTOUCHED** test `byte-address-buffer-consistency-11591.slang:34` regresses (CHECK-COUNT-8 `OpLoad %float` → `OpLoad %v2float`), green on master. Devin independently flagged 🔴 at `slang-ir-byte-address-legalize.cpp:784`. Ledger row written; shadow-mode (no GitHub write, no reviewer/fixer dispatch by approver). **→ Main routed finding to slang-fixer** on canonical thread for fix (chunker store-side + update invalidated SPIRV test); approver re-decides on next synchronize.
- **SERIES STATUS:** [1/3] **#11594 MERGED** · [2/3] **#11595 MERGED** (`c6a2610682`) · [3/3] **#11803 OPEN on master — BLOCKED (RED_BUG, CI red), fixer owns fix**. Webhook-driven from here.
- **✅ 2026-07-19 03:49 — #11564 + #11596 CLOSED by jkwak directly (fixer msgs 48122/48124); operator-hold close item RESOLVED.** #11564 CLOSED unmerged 03:47 "in favor of #11803" (planned Option-A superseded); #11596 CLOSED 03:47 (pre-Y-merge dup, superseded by #11595). Maintainer closed them himself → no operator green-light needed (the hold was on the BOT closing). Fixer posted closing ack on #11564 (issuecomment-5014104638), closed nothing itself. [[feedback_github_writes_operator_authorized]] honored.
- **✅ RECONCILED — fixer ON THE RED-BUG (03:54, fixer msg 48126; Main-verified 8 test-slang jobs FAILING at `1cfb3602cd9b`).** Fixer's earlier "awaiting CODEOWNERS" read was STALE; corrected — it picked up the fix from jkwak's 03:51 comment + Main's reconciliation (handoff landed). CI-log ground truth (linux-release job 88151244423) matches approver+Devin: (1) **store chunker bug** — emitted HLSL has ONE `buffer_0.Store(0U,float2(...))` where `Store<float4>(0,…,8)` must emit TWO (bytes 0-7 + 8-15); real defect. (2) **consistency-11591** `CHECK-COUNT-8 OpLoad %float`→`OpLoad %v2float` — fixer to confirm correct-new-load-chunking (update test) vs load-side over-reach. (3) **chunked.slang:41** — a stray `` `CHECK: Store` `` in a PROSE COMMENT parsed as a live directive (test bug, separate). Fixer root-causing the store path in `slang-ir-byte-address-legalize.cpp` **against source (NOT assuming Devin's line 784)** — good discipline. No local build (disk/time) → reasoning + CI verify (same posture that landed [1/3]/[2/3]). Will fix code + reconcile CHECKs, re-push; approver re-decides on synchronize. ETA next update after fix.
- **⚠️ 2026-07-19 04:00 — HYPOTHESIS SHIFT: fixer's source-trace says store code may be CORRECT; failures possibly TEST-ONLY (fixer msg 48128; building to ground-truth, NOT settled).** Careful trace of `emitLegalChunkedVectorStore` @HEAD `1cfb3602cd` shows it SHOULD emit TWO float2 stores for `Store<float4>(0,…,8)` (offset 0→elems 0-1, offset 8→elems 2-3; loop advances `done += width`). Fixer's new read: the CI log CAN'T confirm "only one store" — the `chunked.slang:41` failure is a **stray-prose `` `CHECK: Store` `` directive** (FileCheck parses the backtick-quoted words in the fixer's own comment as a live pattern), which fails FIRST and MASKS the real `CHECK-COUNT-2` on line 42 (never ran). Devin's cited `:784` is inside the LOAD chunker call, not the store path. So either **(a)** store code fine + BOTH failures test-only (stray directive + a correct-behavior CHECK-update on consistency-11591, whose text still describes slice-2's pre-chunk scalarize), or **(b)** a real store defect the trace missed. **Fixer building slangc (~20min subagent, disk healthy 44G) to compile both tests + count actual `.Store`/`OpLoad` — fix follows VERIFIED output, not guessing.** ⚠️ Do NOT relay "confirmed store defect" as fact — that was the initial read (approver/Devin/Main); the fixer now contests it pending real compile. [[feedback_never_relay_a_verdict_not_in_hand]].
- **✅ 2026-07-19 04:17 — RESOLVED: both failures TEST-ONLY, chunker codegen CORRECT (fixer msg 48132; proven by BUILD, codex OUTPUT_REVIEW=approve/independently rebuilt).** Hypothesis (a) confirmed via live slangc build at HEAD. **Store chunker NOT buggy** — `Store<float4>(0,…,8)` emits exactly TWO float2 stores (offsets 0 & 8); the "one store" was FileCheck's fuzzy hint after the stray-directive stall; Devin's `:784` is the LOAD chunker's arg, misattributed to the store path. Fixes (tests-only): (a) `chunked.slang:41` backtick `` `CHECK: Store` `` in prose comment parsed as live directive → 0-match stall masked real `CHECK-COUNT-2` → reworded; (b) `consistency-11591:34` `CHECK-COUNT-8 OpLoad %float` was slice-2 pre-chunk scalarize, slice-4 chunks promise-8 float4 → `CHECK-COUNT-4 OpLoad %v2float` + refreshed stale "scalarized" prose. All 3 byte-address tests verified green by hand-replaying every CHECK vs raw slangc emit (local FileCheck/slang-test silently ignores filecheck= tests → hand-replayed). Pushed head **`b17148b141`** on `fix/issue-11593`, **no code change**. Replied jkwak (issuecomment-5014198546). Non-draft-on-master → auto pull_request CI (no manual dispatch); real conclusion + CODEOWNERS review webhook-driven.
- **CALIBRATION (both right):** Main's nudge relaying approver+Devin "real store defect" correctly surfaced the LIVE red the fixer's stale "awaiting CODEOWNERS" read had missed — but the CAUSE was test-only, not a code bug. Fixer's value-add: **proved it test-only with a build rather than churning the chunker on an unverified symptom.** Confirms [[feedback_never_relay_a_verdict_not_in_hand]] (the "store defect" was an inference, not verified — softening it in memory before it hardened was right) + reconcile-when-framing-contradicts-verified-state (the nudge was warranted; a real 8-job red must not sit behind a wrong "review-waiting" belief).
- **⚠️ 2026-07-19 04:27 — PARALLEL-SESSION COLLISION (handled cleanly); head advanced `b17148b141`→`76a840fc50` (fixer msgs id26/id28).** A **parallel slang-fixer session** (same bot identity) pushed the identical tests-only fix `b17148b141` while THIS session was building; this session **verified `b17148b141`'s content matched its own independently-derived fix and did NOT re-push over it** (avoided [[project_stacked_pr_shared_base_clobber]]/[[project_fork_reentrancy_phantom_codriver]]), then added ONE comment-only follow-up `76a840fc50` (codex must-fix: reword a change-history comment → stable invariant). Origin HEAD now `76a840fc50` on `fix/issue-11593`. Root cause of the 2nd session: RED re-triage on an in-flight fixer PR woke a second session (jkwak's 03:51 GitHub comment webhook + Main's reconciliation) racing the a2a-dispatched one. **Mitigation = single-owner E2E [[feedback_let_fixer_own_single_session]]; the reporting session (id26/28) is the effective owner — do NOT wake another fixer session for #11803.** Fixer recommends routing future RED re-triages to a single owner.
- **SERIES (consolidated):** [1/3] #11594 MERGED · [2/3] #11595 MERGED (`c6a2610682`) · [3/3] #11803 OPEN on master (tests-only fix, head `76a840fc50`, awaiting CI + CODEOWNERS) · monolith #11564 CLOSED · dup #11596 CLOSED.
- **🔴 2026-07-19 04:48 — [3/3] shadow-approver R2 BLOCK (RED_BUG) @`76a840fc50c0` — BASIS CHANGED; NEW, CI-INVISIBLE fxc-path claim (approver msg id34; NEW ledger row, R1 row @1cfb3602 stands separately).** R1's CI-red WAS the 2 test-authoring bugs → **fixer's tests-only fix confirmed: the 4 aarch64 test-slang legs are now GREEN at settled head.** But R2 rests on a DIFFERENT defect in code **byte-identical to R1**: fresh Devin 🔴 at `legalize.cpp:777`, source-trace-verified — partially-aligned vector byte-address loads/stores (e.g. `LoadAligned<float4>(loc,8)`) on **fxc/DX≤5.0 HLSL** now emit templated `.Load<float2>`/`.Store<float2>` (**fxc-UNCOMPILABLE**) where master scalarized to the fxc-compatible `uint`+`asfloat` path. **CI-invisible** — new tests use `-target hlsl/spirv`, not `cs_5_0`. Approver's suggested code-fix (not tests): route sub-vector chunks through the uint+bitcast path (or scalarize) when `useBitCastFromUInt` set + add a `-profile cs_5_0` regression test. ⚠️ **Approver states NOT empirically compiled** (no prebuilt slangc) — trace + deepwiki + fxc-era test + Devin re-flag only. Orthogonal to the fixer's modern-target emit check (z/w-not-dropped) which was correct as far as it went. **→ Main routed to fixer to GROUND-TRUTH via build (same build-to-confirm that resolved R1), framed as approver's claim NOT fact** [[feedback_never_relay_a_verdict_not_in_hand]]. Fixer's "work complete" was scoped to modern targets; fxc path reopens legitimately.
- **✅ 2026-07-19 04:54 — R2 fxc-path regression EMPIRICALLY CONFIRMED by fixer build (fixer msg id40); approver + Devin VINDICATED.** Fixer compiled at chunker commit `-target hlsl -profile cs_5_0` (`useBitCastFromUInt=true`): `LoadAligned<float4>(loc,8)`+`Store<float4>(0,a,8)` now emit templated `.Load<float2>` + vector `Store(...,float2(...))` — exactly what `slang-emit.cpp:2063` says "Fxc and earlier dxc versions do not support". **Confirmed regression vs master:** same compile on fully-scalarized promise-4 case emits fxc-compatible `Load(loc)` uint + `asfloat`; pre-chunker [2/3] `c6a2610682` routed partial-aligned float4@8 through `emitLegalSequenceLoad` (scalarize→uint+asfloat, fxc-OK) via `!isWideAccessAligned`. **Root cause:** [3/3] chunker replaced that branch w/ `emitLegalChunkedVectorLoad`/`Store`, whose `float2` sub-chunks are wide-aligned → hit `emitSimpleLoad` → raw templated vector access, never reaching the `useBitCastFromUInt` bitcast branch (which only covered the now-unreachable whole-vector fall-through). CI-invisible (new tests `-target hlsl/spirv`, not `cs_5_0`). (fxc Windows-only, can't run downstream DXBC compile here — but the `useBitCastFromUInt`-for-DX≤5.0 mechanism is the project's own authoritative statement that templated `.Load<T>` is fxc-incompatible.) This is the R1-pattern PAID OFF in the other direction: R1 inferred-defect evaporated under build; R2 inferred-defect CONFIRMED under build — build-to-confirm resolved both. **Fixer PROCEEDING to fix: route chunker sub-chunks through uint+bitcast (or scalarize) when `useBitCastFromUInt` set + add `-profile cs_5_0` regression test. Will re-push.** This is a REAL code fix (source change), unlike R1's tests-only.
- **✅ 2026-07-19 05:15 — R2 fxc regression FIXED + pushed `ef3ba2cd46` on `fix/issue-11593` (fixer msg id46).** Real SOURCE fix (root-cause, minimal): in `slang-ir-byte-address-legalize.cpp`, guard BOTH chunker call sites — when `m_options.useBitCastFromUInt` set, fall back to `emitLegalSequenceLoad`/`emitLegalSequenceStore` instead of `emitLegalChunkedVector{Load,Store}`; each scalar then routes through existing uint-load/store + `asfloat`/`asuint`. Scoping: `useBitCastFromUInt` set ONLY for HLSL DX≤5.0 (fxc) + WGSL, WGSL scalarizes upstream → guard changes ONLY fxc path; modern targets keep point-5 chunking. +regression test `tests/compute/byte-address-buffer-chunked-fxc-11803.slang` (`-profile cs_5_0`) → fxc path no longer CI-invisible. **Verify (rebuilt slangc):** cs_5_0 float4@8 now scalarizes `Load()`+`asfloat`/`Store(...,(uint)asuint)` — no `Load<`, no `float2`/`float4`; modern HLSL unchanged (Load<float2>×2 etc); SPIRV/consistency-11591/chunked-widths/align-error all unchanged. FileCheck not run locally (not built) — direct-emit token counts. codex PLAN+CODE+OUTPUT approved. **→ Approver dispatched (Main msg 45) for R3 re-decision vs settled head; debounce instructed.** [[feedback_never_relay_a_verdict_not_in_hand]] — "fixed" is fixer's build-verified finding; CI-green + approver R3 is the confirmation gate.
- **📌 ADJACENT LATENT (out-of-scope, NOT fixed, on record):** whole-aligned `float4`@16 on fxc ALSO emits templated `.Load<float4>` — but **master did too** (via `isWideAccessAligned`→`emitSimpleLoad`), so PRE-EXISTING, not a [3/3] regression. If maintainers want fxc fully templated-load-free that's a separate latent fix. Fixer correctly scoped it out of #11803.
- **2026-07-19 05:33 — title/desc refreshed + rebased; head `ef3ba2cd46`→`dbfb03c88b` (fixer msgs 48136/48138).** jkwak asked to refresh title+desc → title "[3/3] ByteAddressBuffer: chunk partially-aligned accesses at the widest aligned width"; desc rewritten 5-part to true delivered state (now covers the fxc/DX≤5.0 scalarize fallback + `byte-address-buffer-chunked-fxc-11803.slang` cs_5_0 test the old body predated). Rebased onto fresh master (was BEHIND), force-pushed **`dbfb03c88b`**; clean 6-file three-dot diff, chunker (12 symbols) + fxc guard (9 sites) intact. mergeState=BLOCKED=awaiting CODEOWNERS (gated, not conflict). Replied jkwak (issuecomment-5014563137).
- **⚠️ SYSTEMIC — parallel-session overlap flagged AGAIN "for the board" (2nd this chain).** Fixer re-flags: two slang-fixer sessions (same bot id) advancing the SAME branch/worktree `wt-slang-slices` — converged fine here (peer's fxc fix verified legit/on-branch, rebased to consolidate), but this is the shared-worktree collision class ([[feedback_let_fixer_own_single_session]] / [[feedback_red_retriage_single_owner_routing]] / [[project_fork_reentrancy_phantom_codriver]]). **Cause: a webhook (jkwak comment) + an a2a dispatch (Main's red-bug reconciliation) about the same in-flight branch each minted a fixer session.** **Main dispatch-hygiene takeaway:** when a RED re-triage or comment concerns an in-flight fixer PR, pin to the existing owning session (`target_session_id`) rather than let a fresh one mint. NOT escalating (converged, no harm; mid-flight restart would risk live #11803 state per [[feedback_benign_ack_loop_dont_restart_if_live_chains]]) — apply the pin-to-owner discipline on the NEXT #11803 dispatch.
- **⏳ 2026-07-19 05:42 — approver R3 lean = ABSTAIN_POLICY (OPEN_GAP), NOT yet recorded (approver msg id58).** Head re-pinned `ef3ba2cd46`→`dbfb03c88b70` (byte-identical rebase; legalize.cpp/hlsl.meta/fxc-test all identical blobs) — never recorded transient head, debounce restarted, CI re-triggered. **R2 fxc finding RESOLVED — triple-confirmed:** (1) source — guard before chunker in both branches → fxc scalarizes uint+asfloat, chunker unreachable; (2) CI — new `-profile cs_5_0` test `byte-address-buffer-chunked-fxc-11803.slang` (`CHECK-NOT: Load<`) PASSED on a green test-slang leg; (3) fresh head-current Devin marks old `legalize.cpp:777` 🔴 **"Resolved"**. **But fresh Devin surfaced a NEW valid gap** at `hlsl.meta.slang:471`: the PR-added `@remarks` still says chunking applies "on every target, including HLSL (e.g. two `.Load<float2>`)" — now **FALSE for fxc/DX≤5.0** after the fix (scalarizes, emits neither). Real PR-introduced public-API-doc overstatement (doc-precision gap, not code defect). Lean ABSTAIN_POLICY: a live PR-introduced inaccuracy withholds a clean pass (shadow never rounds up), but no functional defect → not BLOCK. next-action: qualify the `@remarks` for fxc/DX≤5.0. Awaiting 15-min quiet on `dbfb03c` + core legs terminal-green (a genuine CI fail flips it) → critique gates → record. ⚠️ NOT a recorded verdict [[feedback_never_relay_a_verdict_not_in_hand]] — hold routing the doc-fix until approver records, so I don't reset its debounce mid-flight.
- **🔴 2026-07-19 06:01 — R3 RECORDED = BLOCK (RED_BUG) @`dbfb03c88b70` (approver msg id60; NEW ledger row; R1 @1cfb3602 + R2 @76a840fc stand separately).** Quiet-head debounce confirmed 05:47Z, head re-verified at record. **R2 fxc CODE regression RESOLVED — triple-confirmed** (source guard + cs_5_0 test passed green leg + Devin marks `:777` "Resolved"). 6/6 clauses pass. **Sole remaining blocker = doc-precision gap the fix itself introduced:** `hlsl.meta.slang` lines **471–475 AND 6463–6467** — PR-added `@remarks` say chunking "applies on every target, including HLSL (e.g. two `.Load<float2>`)" — now FALSE for fxc/DX≤5.0 (fix scalarizes → emits neither `.Load<float2>` nor `.Load<float4>`); no fxc caveat in file. Devin flags under ## Bugs; approver-confirmed vs source. next-action: qualify the `@remarks` for fxc (`useBitCastFromUInt` scalarizes to uint+asfloat/asuint). CI @`dbfb03c`: aarch64+macOS test-slang GREEN (incl. fxc test); x86_64/windows/wasm builds + sanitizer running, no failures. **→ Main routed doc-fix to fixer, PINNED to owning session `sess-1784431404501-dk6smp` (Main msg 65) per [[feedback_red_retriage_single_owner_routing]] — first application of pin-to-owner; avoids minting a fresh fixer session.** Approver re-decides on synchronize. **VINDICATION of hold-discipline (approver msg id68):** approver first DRAFTED this as ABSTAIN_POLICY (OPEN_GAP) on "doc-precision not code" — its DECISION_REVIEW gate CAUGHT it as an improper downgrade (policy = any verified 🔴 ⇒ BLOCK, no doc exemption; severity-clear only for 🟡) → corrected to BLOCK before recording. Confirms [[feedback_never_relay_a_verdict_not_in_hand]]: Main held the "lean ABSTAIN" (msg id58) as NOT-a-verdict and did NOT route on it — the lean was wrong and self-corrected. Approver's "lean" is genuinely provisional; its own gates can flip it. WAIT for RECORDED verdict.
- **✅ 2026-07-19 06:09 — R3 doc-precision blocker FIXED + pushed `77907d69a5` on `fix/issue-11593` (fixer msg id72); doc-only, no code.** Both `@remarks` blocks in `hlsl.meta.slang` (~471–477 and ~6463–6469) reworded: "applies on every target incl. HLSL" → "most targets, including modern HLSL" + explicit exception (fxc/DX≤5.0 has no templated `.Load<T>` → partially-covered access scalarized to `uint`+`asfloat`/`asuint`, not chunked). No "every target" text remains. Behavior-neutral (doc comments stripped pre core-module compile); confirmed slangc still loads core + fxc repro still emits scalar Load+asfloat/asuint. codex CODE+OUTPUT approved. **→ Approver dispatched (Main msg 71) for R4 re-decision vs settled head `77907d69a5`; debounce instructed.** Sole R3 blocker; R2 fxc code fix stays resolved.
- **✅✅ 2026-07-19 06:35 — R4 = WOULD_APPROVE (CLEAN) @`77907d69a59006f3` (approver msg id82; NEW ledger row; R1/R2/R3 rows stand separately). CHAIN CONVERGED.** Quiet-head debounce confirmed 06:21Z, head re-verified. 6/6 clauses pass. R3 doc 🔴 CLOSED — both `@remarks` blocks (`hlsl.meta.slang:470-478, 6464-6472`) now "most targets, incl. modern HLSL … exception is fxc/DX≤5.0 … scalarized to uint+asfloat/asuint rather than chunked"; grep "every target"/"all HLSL" EMPTY; matches verified code. R4 DOC-ONLY: `legalize.cpp` (00f3cf07) + fxc cs_5_0 test (7ffa385f) byte-identical to R3 → R2 code fix stays fixed+guarded. **Fresh Devin STILL lists `hlsl.meta.slang:471` 🔴 — approver REFUTED it vs settled-head source (overstatement no longer exists; same Devin-lag seen R3→ef3ba2).** Devin marks `legalize.cpp:777` "Resolved", no new findings. CI 06:28Z: 13 relevant legs GREEN / 0 fail — all 4 core test-slang (incl. fxc cs_5_0) + Linux/macOS/wasm builds green; pending Windows/sanitizer/x86_64 variants (doc-only can't regress vs R3). Optional non-blocking nit: author could scope the "exception" to HLSL explicitly (Metal/WGSL scalarize via other options) — NOT a defect. **CHAIN ARC: R1 BLOCK (test-authoring CI-red, false correctness flag) → R2 BLOCK (verified fxc code regression) → R3 BLOCK (fix introduced doc 🔴) → R4 WOULD_APPROVE (doc closed, code preserved).** Awaiting human join.
- **SERIES (terminal-pending-merge):** [1/3] #11594 MERGED · [2/3] #11595 MERGED (`c6a2610682`) · [3/3] **#11803 shadow WOULD_APPROVE CLEAN @`77907d69a5`, awaiting CODEOWNERS/human merge** · monolith #11564 CLOSED · dup #11596 CLOSED.
- **Blocker:** none. [3/3] #11803 shadow-CLEAN; webhook-driven — awaiting human join (CODEOWNERS review + merge). Maintainer-gated merge INTACT; bot flips/merges nothing. If a human comment/CHANGES_REQUESTED lands → re-open via owning fixer session `sess-1784431404501-dk6smp` (pin per [[feedback_red_retriage_single_owner_routing]]).

Note (07-16): fixer fat-fingered a stray a2a `noop` to slang-pr-approver (id 155/156); approver no-op'd + courtesy-noticed; fixer correctly sent nothing back (no echo). Non-event.
