---
name: project_8125_empty_struct_cuda_infllight
metadata: 
  node_type: memory
  type: project
  originSessionId: ffbf244c-faeb-4852-aa5d-4149319d75a5
---

**#8125** — empty struct in a module's public/exported interface makes the C-like (CPU/CUDA) emitter emit a real 1-byte member while reflection reports size 0 → offset mismatch → SIGSEGV (CPU) / `CUDA_ERROR_ILLEGAL_ADDRESS` (slangpy repro). Assignee/maintainer @jkwak-work; overlaps #7612.

**Authorized 2026-07-08:** jkwak commented "please create a new PR" (issuecomment-4910435580). Triager dispatched fixer for a **draft** PR (`Closes #8125`), canonical thread `gh-issue-shader-slang/slang-8125`. Held for fixer's [Fix Report] (PR # + CI).

**⚠️ LANDMINE — do NOT re-recommend the global approach.** My original authorization brief (from the pre-#11657 2026-06-17 re-triage) described "a focused IR transform removing empty-struct fields + rewriting all uses (FieldExtract→DefaultConstruct, trim MakeStruct, drop stores)." That is **exactly PR #11657's global `removeEmptyStructFields` pass, which CI REJECTED** (broke `Conditional`/`Optional` dyn-dispatch → `layout-conditional-field.slang.4 (cpu)` `non-simple operand(s)!`) and **jkwak CLOSED today**.

**Corrected direction (jkwak's explicit steer, now in fixer's hands):** confine the fix to the existing empty-type legalization — `IREmptyTypeLegalizationContext::isSimpleType`, `slang-ir-legalize-types.cpp:4058` — reconciling the retained-public empty member with the size-0 reflected layout, keeping `layout-conditional-field.slang` green. Reuse the 5-shape test preserved on `origin/fix/issue-8125 @ 3e2492d7fa`.

**Why:** relaying my stale brief as fact would have re-sent the fixer down the rejected path. Triager surfaced the conflict (per [[feedback_admin_standing_rules_precedence]]) — verify a coworker's premise against latest GitHub state before treating a "designed fix" as current.

**Update 2026-07-09 — REDIRECT to adopt PR #10788, not a fresh draft.** Maintainer @jhelferty-nv work-ordered `@nv-slang-bot` (PR #10788 comment 4919480835) to *finish the stale Copilot draft #10788* (branch `copilot/fix-empty-structs-handling`, same-repo, base master) — "adopt the approach; don't restart." #10788's approach = jkwak's exact legalization-layer steer (`isSimpleType()` always legalizes empty structs on non-Metal). Routed THROUGH triager (owns peer-wire fixer edge) on canonical thread; NO fresh `fix/issue-8125` draft. jhelferty's 5 tasks: (1) merge origin/master, (2) confirm layer + CPU/CPP affected, run cpp-compiler + reflection/layout suite, (3) prefer runtime layout test (COMPARE_COMPUTE/-cpu) over filecheck, (4) rewrite body 5-part fixing wrong "Fixes #6308" → **#8125**, (5) `./extras/formatting.sh` + `pr: non-breaking` label.

**Hiccup:** fixer initially DECLINED (msg #32) conflating jhelferty's work-order with the earlier informational jkwak-names-#10788 webhook — "not my chain, different-bot-authored." Triager corrected the inverted premise + re-dispatched. If fixer declines again same grounds → escalate to Main.

**#10788 state as of 2026-07-09 00:45Z (Main-verified):** OPEN, **non-draft** (jkwak-work flipped 00:43Z — MAINTAINER, no breach), MERGEABLE. But fixer work NOT yet landed: body still old checklist (no `Fixes #8125`), no `pr: non-breaking` label, no bot comment, CI RED (test-slang linux/macos aarch64 + rhi + check-pr-label) on the unmodified stale draft. Holding for fixer [Fix Report].

**Mechanics watch:** #10788 authored by `app/copilot-swe-agent`. Fixer must verify push access to the foreign-app branch; if blocked → carrier-PR fallback [[project_fork_pr_carrier_fallback]] + report_pr_created.

Drafts-only guardrail holds [[feedback_drafts_only_guardrail]] — fixer keeps it a draft-state deliverable but PR is already non-draft via maintainer, so do NOT re-draft; ready-flip already done by jkwak, MERGE stays operator/maintainer-gated [[feedback_github_writes_operator_authorized]]. #11657 global-pass landmine still active.

**Update 2026-07-09 ~19:06Z — MAINTAINERS NOW CO-DRIVING #10788; likely YIELD.** Two synchronize webhooks fired. Neither was fixer work — both were maintainers pushing to the branch: (1) jkwak-work merged master @17:36Z; (2) **csyonghe pushed HEAD `dc4cde29` @19:05Z "try to see if this breaks, if not this is the principled fix."** STILL zero nv-slang-bot commits (~19h since re-dispatch). Pattern = maintainer self-fix/co-drive (cf [[project_12025_wavetangledvector_diff_subscript]], [[project_11806_cmake_options_maintainer_selffix]]). **Main HELD reviewer routing on both** — reviewing mid-experiment ("try to see if this breaks") is premature; and blind fixer code-push would clobber csyonghe's HEAD ([[project_stacked_pr_shared_base_clobber]]). Forwarded to triager to set fixer posture: strong read = YIELD code-driving to maintainers; body-rewrite(#4)+label(#5) are non-colliding metadata the bot *could* still do if still wanted, but confirm with maintainer activity first. Do NOT route to slang-reviewer until maintainers settle the approach + fixer status reconciled. If fixer push-blocked/declined-again → escalate to Main.

**STALL SUSPECTED 2026-07-09 ~14:02Z (operator asked "is a fixer working on #10788?").** Main-verified at HEAD: #10788 OPEN/non-draft/MERGEABLE but **NO bot activity in ~13h** — body still the stale Copilot checklist (no `Fixes #8125`, still mis-references #6308), label = `Office-Yong` only (no `pr: non-breaking`), last comment = jhelferty's 07-08 21:43 work-order with NO bot reply, branch head `51b5703d4b` unchanged (updated 07-08 00:55Z). NO dedicated fixer OR triager session on the `gh-issue-shader-slang/slang-8125` thread (`ncl sessions list` both groups). So jhelferty's 5-task adoption order has NOT visibly progressed. **Likely cause:** the **2.1.39 prod restart (07-09 11:50Z) reset sessions mid-flight** (same event that pre-empted the 4-chain batch) and/or the earlier decline-hiccup (msg #32) left the chain without a live driver — the adoption never re-took after the reset. Re-driving through triager (owns fixer peer-wire — NO direct Main→fixer double-dispatch [[feedback_no_double_dispatch_peer_wired]]) on canonical thread: re-establish jhelferty's 5 tasks on a fresh fixer session, verify branch-push access to the copilot-authored branch (carrier fallback [[project_fork_pr_carrier_fallback]] if blocked), `report_pr_created`. Mechanics unchanged (adopt #10788, not a fresh draft; jkwak's legalization-layer steer; #11657 landmine). Answered operator: yes-dispatched-but-stalled, re-driving.

**RECONCILED 2026-07-09 ~17:4xZ (Main global-scope session inspection).** Triager escalated (msg #21306) with the caveat it CANNOT see the fixer's sessions (group scope shows only its own). Main (global scope) resolved the load-bearing fact: **`ncl sessions list --agent-group ag-1780667166439-vmjrwe` (slang-fixer) shows ZERO sessions on any 8125/6308/10788 thread — never created — while the fixer is DEMONSTRABLY ALIVE** (live running sessions on 12025 @17:32Z, 7878 @16:49Z, 12004 @16:36Z). So the "silence" is NOT a dead/logged-out fixer and NOT a fixer refusal — the triager's 3 dispatches never materialized a fixer session (restart-storm casualties: 2.1.39 @11:50Z + 2.1.40 @15:1xZ). The ONLY #10788-thread session (`sess-1783547008504-h2x7xz`, `gh-pr-shader-slang/slang-10788`) belongs to **Orchestrator/main** (jhelferty's work-order webhook landed in Main's PR session, a DIFFERENT thread than the triager's issue-thread dispatches → never converged). jkwak's 17:36:07Z commit **VERIFIED as routine master-merge, not takeover** (branch head has zero nv-slang-bot commits, body still stale #6308, `Office-Yong` only, no `pr: non-breaking`). #8125 OPEN assignee jkwak-work (RTR/reproduced/Dev-Opened); #10788 OPEN non-draft mergeable, 0 bot comments, unchanged. **Decision: authorized triager to do ONE targeted fresh-session dispatch — NOT thrash** (prior 3 were infra resets, not fixer ignoring work) and NOT a Main→fixer double-dispatch (edge stays with owner). **NO operator surface** — jkwak-merge is benign, no human decision blocked; clear mechanical next step. Bot has 1 comment on #8125 (06-17 re-triage id 4732754411) — EDIT-in-place if a status post is needed, not a new comment.

**✅ RE-ENGAGED 2026-07-09 ~18:38Z (Main session-pin recovery) — corrects the "never created" reconcile.** Triager escalated the 4th dispatch as a genuine engage-failure (msg #21328) and handed the global-scope look to Main. Root cause found via `ncl sessions list --agent-group <slang-fixer> --thread-id gh-issue-shader-slang/slang-8125` (the **plain grep missed it — 200-row list is recency-capped**, so line-31's "ZERO fixer sessions ever created" was WRONG): **the fixer's #8125 session `sess-1781713231768-httwp6` EXISTS** (on the triager↔fixer a2a edge `mg-a2a-1780672222602-epsn3s`), it was just **stopped/parked** — last inbound seq #50 (00:45Z jkwak "closing #11657 in favor of #10788"), fixer stood down at seq #53 (00:47Z) treating it as informational, received NOTHING after. So neither "never created" nor "broken edge" (edge routes fine for other issues) — the session was parked and the triager's later dispatches weren't landing on it. **Fix: Main `send_message(to=slang-fixer, target_session_id=sess-1781713231768-httwp6, thread_id=gh-issue-shader-slang/slang-8125)`** — pin took (session flipped stopped→running, last_active 00:45→18:38:09Z, NO duplicate fall-through session minted). Brief consolidated: adopt #10788, jhelferty's 5 tasks (task 1 done via jkwak's 17:36Z master-merge), **scope removal to C/CUDA emit path ONLY** (the #11657 landmine — production review-bot github-actions flagged it verbatim on #10788: discussion_r3553689204/r3548138446 = fires for all non-Metal incl SPIR-V slang-ir-spirv-legalize.cpp:2989 + AD-2.0 slang-emit.cpp:2401), nested/array empty-struct case (r3553690984), runtime COMPARE_COMPUTE/-cpu test (r3553693865), isStructEmpty doc-comment (r3553694886), body→5-part w/ `Fixes #8125`, `pr: non-breaking` + formatting. Push not gated (non-draft maintainer-flipped); merge stays gated. **Report flows fixer→triager** (chain stays triager→fixer; Main woke as one-time recovery only). Await fixer [Fix Report] → triager rolls up.

**[superseded] STILL STALLED after 2nd restart 2026-07-09 ~15:2xZ — re-driving ONCE MORE, NOT a fixer-group restart.** The 14:07Z triager re-dispatch never visibly took (no fixer/triager session on the 8125/10788 thread in `ncl sessions list`), and the **2.1.40 restart (15:1xZ) killed the triager's 60-min monitor** before it could fire/escalate. #10788 unchanged at HEAD (`51b5703d4b`, body stale, no `Fixes #8125`, no `pr: non-breaking`, no bot engage-ack to jhelferty). **Deliberately NOT `ncl groups restart`-ing the fixer group** — it has LIVE running sessions on OTHER chains (12016, 12019 per session list); a group restart would kill that in-flight work (blast radius). Instead re-driving through the (alive) triager one more time + fresh monitor. **Escalation ladder for THIS chain:** if this 3rd drive also fails to engage (no branch move / no bot reply within ~monitor window, absent another restart), the next step is a TARGETED wake of a fresh fixer session on the 8125 thread — NOT a whole-group restart — and if that's not possible, surface to operator that #10788 needs manual attention (maintainer-watched, jhelferty work-ordered + skallweit asked status). 3rd restart this session tripped the [[project_scheduler_stall_incident]] daily-task orphan bug again — re-armed wiki/relCI/nightly 15:35/15:40/00:00.

**REVERSED then RE-CORRECTED — MAINTAINERS OWN the branch; the bot NEVER pushed to it. STAND FIXER DOWN. (2026-07-09 ~23:20Z.)**
- **My first re-check (23:14Z) made a FALSE-POSITIVE error I then relayed to the operator:** I queried `gh api commits?sha=copilot/fix-empty-structs-handling` (returns full branch ANCESTRY) and saw "9 nv-slang-bot commits" → wrongly concluded "the bot engaged, its work is in the branch." **Triager caught it.** Those 9 were **master's unrelated bot commits pulled in by jkwak's 17:36 master-merge**, not #8125 work — mirror image of the earlier false-negative. [[feedback_verify_regression_claims_at_precision]] applies to authorship too: use the PR-scoped endpoint, not branch ancestry.
- **AUTHORITATIVE (verified `gh api pulls/10788/commits`, 5 commits total):** 3× `Copilot`/copilot-swe-agent[bot] (the ORIGINAL April draft: 13de43b3/cd2ce4ee/51b5703d), jkwak-work master-merge 7181864a (17:36), csyonghe dc4cde2965 (19:05, "try to see if this breaks, if not this is the principled fix" = current tip). **ZERO nv-slang-bot commits.** So the bot NEVER pushed to #10788 — my earlier "stalled / head 51b5703d unchanged / no bot activity" reads at 14:02Z & 15:2xZ were actually CORRECT; the branch head only moved (51b5703d→dc4cde29) via MAINTAINER activity (master-merge + csyonghe's own commit).
- **Accurate story:** the bot's #8125 contribution = **triage/root-cause + repro**, held as UNUSED patches on the fixer side (never pushed). csyonghe's `dc4cde29` is his **own independent, more-principled producer-side fix**, NOT built on bot work. Maintainers are landing the right fix themselves.
- **ACTION: fixer stood down** (triager confirms it YIELDed 19:07, pushed/posted nothing, patches preserved; no live monitor to disarm). Do NOT push — bot push on csyonghe's live experiment = clobber ([[project_stacked_pr_shared_base_clobber]]). Cosmetics (`Fixes #8125`/label) moot under maintainer control. **Reopen only on explicit maintainer re-ask or their next branch move (webhook).** #10788 non-draft/OPEN/MERGEABLE, `Closes #8125`.
- **Corrected the operator** (I'd relayed the 9-commits falsehood minutes earlier). Lesson: I compounded stale reads then over-corrected with a wrong-endpoint query — the triager's precision catch fixed it. Verify authorship via `pulls/<n>/commits`, never `commits?sha=<branch>` post-merge.

**RE-OPENED 2026-07-15 ~21:52Z — jkwak NEW minimal direction, supersedes csyonghe/#10788 yield.** @jkwak-work commented on #8125 (issuecomment-4985636048): create a NEW PR that simply removes the 4-line `if (as<PublicModifier>(modifier)) { builder->addPublicDecoration(inst); }` block in `source/slang/slang-lower-to-ir.cpp` ("should be enough; if CI fails, investigate/resolve as needed"), AND "close #10788 after reviewing the code and failed tests." This is the explicit-maintainer-re-ask reopen trigger the 07-09 stand-down named. Re-dispatched THROUGH triager (owns fixer peer-wire; pinned triager parked session sess-1781712604031-jmsiuc, in_reply_to=50) — msg id 57.
- **New fix ≠ csyonghe's #10788 approach ≠ #11657 landmine.** jkwak's is producer-side: drop the PublicDecoration at lowering so `isSimpleType` legalizes the empty public struct naturally (NO isSimpleType change). csyonghe's dc4cde29 changed isSimpleType directly. #11657 global-pass still to avoid.
- **⚠️ SCOPE RISK flagged to fixer:** removing `addPublicDecoration` for ALL `PublicModifier` is broader than empty-struct — PublicDecoration governs linkage/visibility/export for every public decl. Watch CI for linkage/reflection/cross-module/export regressions, not just the repro. jkwak pre-authorized CI iteration.
- **#10788 close = DO NOT auto-close.** Maintainer-co-driven (csyonghe "principled fix" dc4cde29; jhelferty-nv+bmillsNV requested reviewers). jkwak-preferring-lines-removal-over-csyonghe = maintainer-vs-maintainer design call; bot must not press Close. Directed fixer to post superseded-by comment on #10788 tagging @jkwak-work+@csyonghe, DEFER actual close to a maintainer. Main holds close pending operator/maintainer confirm. [[feedback_github_writes_operator_authorized]] NEVER auto-close.
- #10788 verified 07-15: OPEN, non-draft, Copilot-authored, base master, MERGEABLE, 0 nv-slang-bot commits (bot never pushed to it). Await fixer [Fix Report] → triager [Resolution].

**Fixer re-dispatched, holding for [Fix Report] (2026-07-15 22:01Z, triager msg #58).** Triager refreshed master HEAD `8e3f9163d`; confirmed jkwak's target block. ⚠️ **Compile gotcha (triager caught):** removing ONLY jkwak's 4 quoted lines leaves a dangling leading `else if` → won't compile; faithful fix ALSO converts that `else if`→`if` (correct realization of intent, note in PR body — NOT scope creep). Triager also told fixer to run the #11657 `layout-conditional-field` canary.

**⚠️ STALLED ~2 DAYS — re-dispatch never produced; operator nudged (07-17 21:31Z, dashboard msg 43672 pointing at jkwak's 07-15 ask).** Main-verified at HEAD `3649fb98`:
- **NO new PR exists** for jkwak's minimal-removal direction (search: only old closed #11657/#8257; no bot comment on #8125 after jkwak's 21:52 ask). The two fixer re-dispatch sessions (07-15 22:02) both **parked with NO output** (`ncl sessions list --thread-id gh-issue-...-8125` → 4 sessions, none active, newest 22:02:41Z).
- **Target block STILL in master** — `slang-lower-to-ir.cpp:1434-1436` (`if (as<PublicModifier>(modifier)) { builder->addPublicDecoration(inst); }`) unremoved → the fix genuinely never landed.
- **jkwak did his half:** CLOSED #10788 himself 07-16 19:36Z ("Copilot not responding"). So the "close #10788" task is DONE; only the "create the new minimal-removal PR" half fell through.
- **#8125 still OPEN**, assignee jkwak-work.
- **Likely cause:** same restart-storm/parked-session family as #11476 — the 07-15 re-dispatch landed on a session that parked before producing (mirrors line-31/35 stall pattern). Cred outage 07-16→13:56Z 07-17 also blocked pushes in the window.
- **RE-DISPATCHED through triager 07-17** (owns fixer peer-wire; NO direct Main→fixer double-dispatch [[feedback_no_double_dispatch_peer_wired]]) on canonical thread `gh-issue-shader-slang/slang-8125`, `<github-post-authorized />`: fresh fixer session, jkwak's exact minimal removal (drop the 4-line PublicDecoration block + fix the dangling `else if`→`if`), run the #11657 `layout-conditional-field` canary + broader linkage/reflection/export CI (SCOPE RISK: PublicDecoration governs linkage/visibility for ALL public decls, not just empty-struct — jkwak pre-authorized CI iteration), draft PR `Fixes #8125`, `report_pr_created`, `pr: non-breaking` + formatting. Cred recovered so push works now. Await fixer [Fix Report]→triager [Resolution].

**2026-07-19 05:18Z — jkwak STATUS-CHASE on GitHub (issuecomment-5014528010): "is there a PR for this issue as requested?" GROUND TRUTH = NO.** Main-verified: `gh pr list --search "8125"` EMPTY; only fix/issue-8125 branch PR = CLOSED #11657; issue timeline links only old #411/#8257 — **no new minimal-fix PR exists.** The 07-15 triager→fixer re-dispatch (msg #58) did NOT land: fixer's #8125 session `sess-1781713231768-httwp6` frozen at 2026-07-10 18:55 (before the re-dispatch); triager's own session advanced (07-17) so triager got it but its dispatch to the fixer never materialized a live fixer session — 2nd non-landing (same failure class as 07-09 restart-storm). **#10788 now CLOSED (07-16 19:42Z)** — jkwak's earlier "close #10788" ask is resolved; do NOT re-address. Re-drove through triager pinned to its live session sess-1781712604031-jmsiuc; escalation if fixer still doesn't engage = Main one-time session-pin recovery on fixer (precedent, chain stays triager→fixer). Honest status must go to jkwak (no fabricated PR).

**2026-07-30 19:29Z — jkwak 2nd PUBLIC STATUS-CHASE (issuecomment on #8125: "can you share your update about the coming PR?"); GROUND TRUTH still = NO PR (~4th non-landing).** Orch-verified at master HEAD: `gh pr list --search 8125` shows only CLOSED (#11657 rejected global-pass, #8257 copilot draft, #10788 CLOSED 07-16 by jkwak). Target block STILL in master `slang-lower-to-ir.cpp:1434-1438` (the 4-line `if (as<PublicModifier>(modifier)) { builder->addPublicDecoration(inst); }`, followed by dangling `else if (as<HLSLExportModifier>...)` at :1438). Fixer #8125 session sess-1781713231768-httwp6 + triager sess-1781712604031-jmsiuc BOTH parked since 07-19 07:13Z — bot's 07-19 05:22Z "picking up now" promise produced nothing in 11 days. Bot has 1 honest status comment on #8125 (07-19, no fabrication). Re-drove THROUGH triager pinned sess-1781712604031-jmsiuc (in_reply_to=88, msg #101; NO direct Main→fixer double-dispatch) with TWO tasks: (1) post honest GitHub status to jkwak NOW (`<github-post-authorized />`, no fabricated PR), (2) drive fixer to actually produce the minimal-removal draft (remove 4 lines + `else if`→`if` compile fix, `Fixes #8125`, report_pr_created, `pr: non-breaking`+formatting, layout-conditional-field canary + broad linkage/reflection/export CI watch). Guardrails restated: #10788 CLOSED drop adopt/close; #11657 global-pass landmine; producer-side lowering removal ONLY (no isSimpleType). If fixer parks again → escalate to Main for one-time fixer session-pin recovery (07-19 precedent). Await fixer [Fix Report]→triager→Main.
  - **Triager executed cleanly (07-30 19:42Z, msg #100).** Verified master HEAD `350f83ed6`; target block confirmed `slang-lower-to-ir.cpp:1434-1437` + real `else if`→`if` gotcha at :1438. Honest GitHub status POSTED (fresh comment `5135464218`, nv-slang-bot 19:40Z — Orch-verified landed, NO fabricated PR, owns that jkwak's 07-19 promise didn't materialize). Fixer dispatched with full spec+guardrails on canonical thread. NOTE: triager reported `send_message` MCP couldn't resolve peer names this session → used `<message>`-block delivery instead (worked).

**2026-07-31 02:14Z — jkwak 3rd PUBLIC CHASE (issuecomment "share your update on the PR?"); fixer PARKED AGAIN post-dispatch → MAIN SESSION-PIN RECOVERY #2 (~5th non-landing).** The triager's 19:42Z dispatch DID land — fixer session sess-1781713231768-httwp6 woke to last_active 19:42Z — but produced ZERO output (no PR, no comment) and re-parked. Orch-verified 02:14Z: still NO PR (`gh pr list --search 8125` only CLOSED #11657/#8257/#10788); target block still in master. This is the exact failure the armed escalation ladder names. Main pinned the fixer DIRECTLY (send_message target_session_id=sess-1781713231768-httwp6, in_reply_to=24, msg #111) with a FULLY SELF-CONTAINED brief (jkwak's 4-line removal + `else if`→`if` compile fix, branch fix/issue-8125, DRAFT, `Fixes #8125`, report_pr_created, `pr: non-breaking`+format, layout-conditional-field canary + broad CI watch, all guardrails) PLUS a **HARD no-silent-park requirement**: if it can't produce the PR this turn (build/push/cred blocker) it MUST reply to Main with the exact blocker + post honest interim status to jkwak — a silent park is the specific failure this kills. Told triager (pinned sess-1781712604031-jmsiuc, in_reply_to=102, msg #113) DO NOT re-dispatch (would double onto live fixer session); keep holding; fixer may report to Main directly this time. **If fixer parks a 6TH time → escalate to OPERATOR that #8125 automation is stuck & needs manual attention** (jkwak has now chased 3× publicly; this is credibility-costing). Await fixer [Fix Report]/blocker.

**2026-07-31 02:18Z — ROOT CAUSE FOUND (fixer interim reply msg #116, honest per the hard requirement — NOT a park).** The string of "silent non-landings" was NOT refusal or dead sessions: the fixer's 19:42Z turn DID the work (fix applied, repro test written, interim status posted to jkwak) but the **session was REAPED MID-BUILD — ninja killed at 616/1187** — so nothing reached parent and no PR opened. That is the mechanism; earlier stalls plausibly the same (reap during long slangc builds, not parked-idle). On-disk state fixer verified intact this turn:
  - Fix DONE: `slang-lower-to-ir.cpp` — 4-line `if(as<PublicModifier>){addPublicDecoration}` removed, following `else if(HLSLExportModifier)`→plain `if`; `grep PublicModifier`→gone. Matches jkwak exactly. Did NOT touch isSimpleType, did NOT reintroduce #11657 pass.
  - Test DONE: `tests/bugs/empty-struct-parameter-block.slang` (5 shapes; CPU COMPARE_COMPUTE + CUDA text-emit CHECK).
  - Mechanism verified empirically (base slangc): non-public empty struct is ALREADY dropped from CUDA emit on master; only `public` one retained via IRPublicDecoration that isSimpleType keeps. Fix makes public match the already-correct non-public case.
  - **Branch = `fix/issue-8125-v2`** (off master `350f83ed63`), NOT plain `fix/issue-8125` (that's CLOSED PR #11657's branch — reuse would resurrect it). **Main CONFIRMED keep v2** (msg #121); no rename. Load-bearing routing hook = `report_pr_created`, NOT branch name.
  - Build RESTARTED detached+monitored (~573 targets left, cached, ETA ~10-15min). Fixer owns the jkwak PR-link post once open; Main covers jkwak-facing until then (don't double-post). Main armed own PR-open monitor (task bm37dfa70, ~39min, watches any 8125 open PR). Await fixer [Fix Report] w/ PR#+CI.

**2026-07-19 07:13Z — MAIN SESSION-PIN RECOVERY done (3rd non-landing).** After 07:06/07:08 restarts killed the triager's escalation monitor, Main verified: still NO #8125 PR; fixer's #8125 session `sess-1781713231768-httwp6` frozen at 07-10 (never got the triager's 05:21Z re-dispatch). Fixer GROUP is alive (session on #11988 @07:07Z). Triager's 05:22Z honest GitHub status comment WAS posted (no fabrication — good). Main pinned the parked fixer session directly (`send_message target_session_id`, in_reply_to=26, msg #85) — woke stopped→running @07:13Z. Full current brief carried: jkwak minimal fix (remove 4-line addPublicDecoration block @ slang-lower-to-ir.cpp:1417-1420 + `else if`→`if` compile fix), fresh draft fix/issue-8125, Closes #8125, report_pr_created, layout-conditional-field canary, broad-CI watch; **#10788 is CLOSED (07-16) — drop adopt/supersede tasks.** Told triager (msg #89) NOT to re-dispatch (would double onto live session); chain stays triager→fixer; fixer [Fix Report]→triager→Main [Resolution]. Await real PR.
