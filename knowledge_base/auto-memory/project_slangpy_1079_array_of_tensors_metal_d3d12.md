---
name: project_slangpy_1079_array_of_tensors_metal_d3d12
description: "slangpy#1079 array-of-tensor params — Metal wrong-results + D3D12 device-removal; triaged→fixer, drafts-only, Metal escalation routes through Main"
metadata: 
  node_type: memory
  type: project
  originSessionId: a4a1751c-a713-4c56-adb1-93f9860a283f
---

# slangpy#1079 — Arrays of tensor params: Metal wrong results, D3D12 device removal

Follow-up tracking issue filed by nv-slang-bot while carrying #996 → **#1078**. #1078 added array-of-tensor + tensor-in-struct-vectorize tests, **skipped on Metal & D3D12** (ref this issue); pass on Vulkan/CUDA. Tracks re-enabling once backends fixed. Repro: `slangpy/tests/slangpy_tests/test_array.py::{test_array_of_tensors_read, test_array_of_rwtensors_write, test_array_of_difftensors_read, test_array_of_rwdifftensors_write, test_vectorize_struct_with_tensor_array, test_2d_mapped_vectorize_struct_with_tensor_array}`.

**Two INDEPENDENT defects** (root-caused from source; no Metal/D3D12 HW at triager — fixer confirms on-device):

1. **D3D12 device removal — root cause PINNED (high conf).** `Tensor.zeros(usage=shader_resource)` → `tensor_zeros()` calls `tensor->clear()` **unconditionally at creation** (`src/slangpy_ext/func/tensor.cpp:442`), ignoring usage flag. D3D12 `clear_buffer` needs a UAV → `ClearUnorderedAccessViewUint` on shader_resource-only buffer fails → `RemoveDevice`, poisons whole worker. VK/CUDA clear via transfer ops → pass. Root is **creation**, not dispatch (contra issue narrative). slang-rhi ref `external/slang-rhi/src/d3d12/d3d12-command.cpp:437-445`.
   - **APPROVED fix (plan reviewed+re-verified by triager @HEAD, msgs 16/22):** Approach **A** = zero non-UAV buffers via `BufferDesc.data` init-upload (NO UAV added). Chokepoint = `Tensor::clear()` (zeros/zeros_like/with_grads all route through it; zeros_like forwards usage → difftensors_read grad path trips too).
   - **Approach C caveats (msg 22):** `SlangPyError` does NOT exist in the codebase (only in AGENTS.md) — use existing error types. Approach C is already structurally satisfied by the `TypeError` at `tensorcommon.py:140-143`; the ask-#2 regression test must exercise the **array**-of-read-only→`RWTensor[4]` mismatch (not just scalar) and assert device-survival — let the test decide whether a separate `resolve_dimensionality` guard is even needed.
   - **⚠️ Load-bearing constraint:** read-only tensor *arrays* legitimately need shader_resource-only; **adding a UAV renames the element type → breaks array resolution on ALL backends** (array.py compares element types by `full_name` string). So the fix must zero the buffer another way, NOT add UAV.
   - **Guard location CORRECTED** by the plan: belongs in `tensorcommon.resolve_dimensionality:353-370`, **not** `tensor.py:184` (triager's original triage pointer was wrong; correction confirmed).
2. **Metal wrong results — subsystem call PENDING (UNCONFIRMED).** Same kernel correct on VK+CUDA, wrong on Metal → hypothesis = upstream **Slang Metal codegen** (echoes CLOSED slang#7606). Tensor-in-struct subset (`[1,5,1,1]`) may be slangpy marshalling-offset; **open PR #1045** targets that path — check first.

**Classification:** bug / high / P2 / functional-API + nanobind (+ likely upstream Slang) / Metal + D3D12.

**State (2026-07-29):** Triaged → fixer implemented Defect 2 → **DRAFT PR #1080** up (stacked on #1078, base `dev/slangpy-fixer/carrier-996`; `report_pr_created` done → webhooks route to fixer; 5-bullet posted on issue for draft-held observability). Fixer verified VK+CUDA green + 3 new regression tests (incl. array-of-read-only→RWTensor[4]); fix = zero read-only storage via copy-path upload at `Tensor::clear()` chokepoint (no UAV). **Ask #2 already satisfied** by existing clean raise on read-only→writable, path-dependent (triager source-verified msg 30): **array** path = `ResolveException` (`callsignature.py:29`, re-raised `calldata.py:229/237/387` on element full_name mismatch); **scalar** path = `TypeError` (`tensorcommon.py:140-143`). BOTH fire in Phase-2 kernel-gen before any GPU op → no device loss either path → NO guard added (would be dead code). Neither is the nonexistent `SlangPyError`. **Reviewer verdict = APPROVE_WITH_NITS** (0 bugs, 2 should-change; both addressed) → PR #1080 @ `98394f3`, still draft. Reviewer caught a real PR-introduced regression: fixer's original `unordered_access`-only guard would make a non-`device_local` `read_back` tensor throw where CUDA/Metal previously cleared → **fixed by gating copy fallback on `device_local`** (other memory types keep original path → zero behaviour change) + added `clear(cmd)` encoder-branch test. VK+CUDA green; codex re-approved. Next = maintainer review + **D3D12 CI/Windows run** to confirm RemoveDevice gone (can't repro on Linux) → re-enable 2 D3D12 read skips. Triage 5-bullet on issue ([comment 5123382519](https://github.com/shader-slang/slangpy/issues/1079#issuecomment-5123382519)). D3D12 clear-on-non-UAV = shared learning. **Metal (Defect 1) NOT started** — separate track. On-device D3D12 + Metal confirmation pending HW (flagged in PR body).
**Defect 2 COMPLETE on fixer side (msg 38):** reviewer updated verdict = effectively **APPROVE** (0 bugs, 0 gaps, 1 non-blocking perf nit); PR #1080 @ `98394f3` draft, all critique stages approve, VK+CUDA green, 4 regression tests (array case + encoder-branch). Fixer will NOT self-ready (promotion = human call); handed back to Main for maintainer-facing decision.

**OPEN — operator-gated:** promotion of draft #1080 → ready-for-review is a HUMAN call (drafts-only guardrail + stacked on draft #1078 + no D3D12 HW here so RemoveDevice-fixed is source-confirmed only, needs D3D12 CI/Windows run for empirical proof). Do NOT self-promote. Metal Defect 1 still not started (separate track). #1079 stays open until BOTH backends green + per-backend skips re-enabled.

**[Triage Resolution] DELIVERED (msg 40) — chain at terminal-rest on coworker side.** Triager OUTPUT_REVIEW critique gate passed (3 rounds; codex caught miscited line + 2 overclaims, corrected). Gap 1 source-reconciled @ PR head `98394f3`: device_local gate = (bug surface: non-UAV) ∩ (copy-path-valid: CopyDestination present) — confirmed `Tensor::clear()` gate `src/sgl/func/tensor.cpp:412-438` + Buffer ctor adds CopySource|CopyDestination only for device_local `src/sgl/device/resource.cpp:121-122`. Issue #1079 refreshed in place (PATCHed settled bot comment 5124281724 w/ consolidated 5-bullet, no dup). Full artifact = triager's `/workspace/agent/reports/triage-resolution-1079.md`.

**Main owns the remaining hop — HELD (operator-gated), NO forcing decision now:** promote draft #1080 → ready + trigger D3D12/Windows CI is naturally gated behind draft **#1078** landing (#1080 stacked on it; #1078 = in-review carrier for #996). RESUME trigger = #1078 merges OR operator authorizes promotion. On green D3D12 CI → re-enable 2 D3D12 read skips. Metal Defect 1 = separate escalation track through Main→slang-triager (plain-array subset) / PR #1045 first (tensor-in-struct). #1079 stays OPEN until BOTH backends green + per-backend skips re-enabled. GitHub trail complete (primary observability satisfied) → no operator ping forced.

NB: fixer's Fix Reports (msgs 28/36/38) arrived in Main inbox directly (tier-skip past triager); Main relayed substance back down to triager (owning tier for roll-up), did not reply on fixer edge.

**🔀 APPROACH PIVOT 07-31 09:53Z — ccummingsNV REQUESTED CHANGES → fixer pivoted copy-zero → raise-on-misuse (head `1424924`, draft).** Maintainer ccummingsNV objected the copy-path zeroing (`98394f3` approach) is **too expensive for large tensors** (full-size host alloc + upload-heap staging + GPU copy per clear) — he's right. Fixer pivoted per his steer:
- **NEW approach:** `Tensor::clear()` now **RAISES** on read-only (non-UAV) storage ("clearing is a write; requires unordered_access") instead of zeroing. Rejects misuse BEFORE any GPU op → invalid UAV clear never issued (D3D12 device-removal fixed) AND directly satisfies issue ask #2 (clean exception, not device loss) — cleaner than the original two-part copy fix.
- **Behavioral consequence:** `Tensor.zeros(shader_resource)` / `zeros_like` now RAISE (were silently zeroing/attempting). Read repro tests updated to build read-only inputs via `Tensor.empty()`+`copy_from_numpy`. All codex stages re-approved; `test_array.py` 44 passed/2 skipped VK+CUDA; pre-commit clean. Replied on ccummingsNV's review thread.
- **⚠️ SUPERSEDES the reviewer's earlier APPROVE_WITH_NITS→effectively-APPROVE** (that verdict was on the copy-path approach @ `98394f3`, now ABANDONED). The pivot materially changed the fix (copy-zero → raise); if peer re-review is wanted it's a fresh pass on `1424924` — but this is maintainer-review-round work (ccummingsNV driving), webhook-driven, separate from the slangpy-reviewer budget. NB the stale slangpy-reviewer package-install requests in the operator queue were for building THIS PR — even more clearly moot now (approach changed, review superseded).
- **Next:** maintainer re-review of pivot + D3D12/Windows CI for empirical device-removal-gone confirmation → re-enable D3D12-skipped read tests. Blocker: none actionable by fixer; on-device D3D12 verify pending HW/CI. #1079 stays OPEN. Draft; promotion/merge operator+maintainer-gated (fixer does not self-ready).

**✅ ccummingsNV ROUND-2 review addressed 08-02 11:59Z, head `f9b1b14` (still draft).** 5 inline comments: (1) shortened `clear()` comment to one line; (2) removed "device must still be alive" survivor assertions (he flagged overkill); (3) removed the D3D12 skip from the 2 read tests — read-only inputs now use `Tensor.empty()` (no clear-at-creation → removal trigger gone). **His "why not just use a writable tensor?" on the read tests → fixer verified EMPIRICALLY:** a writable input FAILS resolution against a read-only `Tensor[]`/`DiffTensor[]` param (RWTensor≠Tensor by name on the array path) → those 2 deliberately keep read-only inputs (validate the read-only-array shape, the exact case his review body exempts). Documented one line each; offered to convert to `RWTensor[]` write-style if he prefers. codex re-approved; test_array.py 44 pass/2 skip VK+CUDA. Awaiting his response on the read-test Q + D3D12 CI. Webhook-driven; maintainer-review-round.
- ⚠️ **INFRA NIT (fixer flagged, WATCH not escalate):** `gh` GraphQL endpoint INTERMITTENTLY 401'ing in fixer's container (`gh pr view`/`gh pr comment` fail); REST (`gh api .../issues/comments`) works → fixer routing PR reads/comments through REST. NOT blocking. Cross-ref [[project_github_actions_graphql_401_outage.md]] (prior GitHub-gw GraphQL 401 outage = operator re-login, NOT restart). Per [[feedback_missing_artifact_not_outage_until_push_confirmed]] + [[feedback_gh_auth_status_misleading]]: intermittent + REST-works + non-blocking + single-coworker = NOT yet operator-actionable. WATCH: if it becomes persistent OR spreads to other coworkers OR blocks a push → escalate to operator (GitHub PAT / OneCLI GitHub credential re-login). Single graceful-degrade-via-REST instance = below escalation bar.

**Main guardrails set (msg 9):** any PR = **draft-only** pending review + on-device confirmation; Metal "upstream-Slang" is a hypothesis until fixer reproduces on-device; if confirmed upstream, **escalation routes through Main → slang-triager**, NOT filed directly on shader-slang/slang by slangpy-triager. See [[feedback_verify_before_relaying]] pattern — verify diagnosis before cross-repo escalation.
