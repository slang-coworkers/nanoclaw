---
name: project_slangpy_1052_autograd_cache_grad_bit
description: "slangpy#1052 — torch call-data cache ignored requires_grad; fixed in draft PR #1054, pr-approver WOULD_APPROVE @ebb9f68de (BLOCK remediated: API_VERSION 7→8); ENGINEERING-TERMINAL, maintainer+CLA-gated"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0c1e5200-765f-4703-8e18-4b677d151754
---

**shader-slang/slangpy#1052** — PyTorch autograd hook silently dropped after a no-grad call to the same `[Differentiable]` function. Reporter: tekintatar. Filed/triaged 2026-07-12.

**Root cause (CONFIRMED in source by slangpy-triager):** torch-tensor call-data cache signature is `[Dn,Sm]` (ndim + scalar_type only). `requires_grad` is read into the tensor-info struct but never written to the cache key. Autograd hook is gated at dispatch on the *cached* `is_torch_autograd()` flag, frozen at build time. So a no-grad-first call caches `torch_autograd=False`; later same-shape/dtype grad calls reuse it → hook bypassed → `grad_fn=None` → `loss.backward()` fails. Explains the full asymmetry (only no-grad→grad is broken).

**Classification:** bug / high / P1 / torch-integration / backend-agnostic / NOT upstream-Slang.

**Fix (Approach A, recommended):** append a grad bit to the torch signature in BOTH native + fallback in lockstep + a no-grad→grad regression test. Framed to avoid re-litigating PR #935.
- Native: `src/slangpy_torch/torch_bridge_impl.cpp:117-129`
- Fallback: `slangpy/torchintegration/bridge_fallback.py:88`
- Dispatch gate: `src/slangpy_ext/utils/slangpyfunction.cpp:107`
- Detection: `slangpy/torchintegration/detection.py:57`

**Chain state (2026-07-12):** triager CONFIRMED, posted GitHub 5-bullet ([issue comment 4952002036](https://github.com/shader-slang/slangpy/issues/1052#issuecomment-4952002036)), handed off to **slangpy-fixer** on canonical thread `gh-issue-shader-slang/slangpy-1052`. Fixer implemented Approach A (grad bit in torch cache signature `[Dn,Sm]`→`[Dn,Sm,Gk]`, native + fallback lockstep), regression test fails-pre/passes-post, verified CUDA L40S. Triager holds fixer's edge (peer-wired) — do NOT double-dispatch to fixer; route any authorization/correction THROUGH triager.

**PR = shader-slang/slangpy#1054** (`dev/slangpy-fixer/issue-1052`, `Fixes #1052`). **DRAFTS-ONLY BREACH:** opened non-draft by nv-slang-bot[bot] @17:36:43Z — timeline verified NO `convert_to_draft`/`ready_for_review` events, so born non-draft (not a maintainer flip). SAME breach as #1053 earlier today (reverted 17:17Z, rule recorded to fixer memory) — recurred ~20min later. Directed correction THROUGH triager (msg #9): fixer to `gh pr ready 1054 --undo` + durably persist open-as-draft. Non-draft open already fired CODEOWNERS review-request to bmillsNV @17:36:44Z (can't recall). Merge strictly maintainer-gated (bmillsNV CODEOWNERS) — never bot self-merge.

**BREACH RESOLVED (verified live 18:03Z):** #1054 back to draft (isDraft=true, OPEN, MERGEABLE/BLOCKED, head=2e3846c). Root cause = **cross-session memory-load-timing gap, NOT respawn amnesia**: drafts-only rule written 17:17Z by the #1053-corrector session; fixer's session started 16:56Z (21min earlier), opened #1054 at 17:36Z — memory is snapshotted at session start, so a rule written mid-flight by ANOTHER session isn't re-injected into an already-running context. Fixer's durable fix: moved invariant memory→`/workspace/agent/.instructions.md` (`[MUST]` open-as-draft + pre/post-create self-check) which composes into CLAUDE.md at SESSION START — verified through restart. This refines [[feedback_coworker_respawn_drops_verbal_gates.md]].

**OPEN admin item — PreToolUse hook (deferred, NOT baked):** fixer recommends a deterministic PreToolUse Bash hook denying `gh pr create` without `--draft` for all coworkers; can't self-install (read-only /app/hooks, settings.json regenerates per spawn → needs nanoclaw host PR). Accepted as worthwhile hardening BUT must NOT be a blanket deny — nv-coworkers fork legitimately opens non-draft PRs ([[feedback_nv_coworkers_automerge.md]]), so hook needs scoping (upstream shader-slang only). Staged for operator sign-off; instruction-file holds the line meanwhile. Fixer to hand over validated guard script.

**CI:** genuine miss caught + fixed in 2e3846c (2nd stale signature assertion `test_nn_parameter_signature` — nn.Parameter defaults requires_grad=True → G1≠G0); test_torchintegration.py 330 pass/0 fail local, codex re-approved; CI re-running (fixer owns rollup, flags only non-flaky red). One env-flaky Vulkan-interop fail = ignorable.

**TERMINAL (engineering side, 18:13Z):** slangpy-reviewer **APPROVE** at 2e3846c (0 bugs, 0 gaps, 1 non-blocking note: per-tensor bit is strict superset of call-level OR that gates the hook → can't under-key; native↔fallback byte-identical at key; fast_itoa special-cases 0 so G0/S0 don't collapse). Codex 3-stage approved. CI 6 green/8 pending/0 fail last seen; fixer owns rollup, flags only non-flaky red.

**Terminal handoff = verified fix in DRAFT PR #1054 (`Fixes #1052`), CLA-blocked** (`license/cla` PENDING on bot identity — allowlist matter, NOT agent-actionable, no operator card per empty-room logic). Awaiting bmillsNV (CODEOWNERS) for CLA-clear + CI-green → squash-merge → promote from draft. Never bot self-merge/self-ready. Re-opens on non-flaky CI red or substantive human comment on #1052 (process through chain, don't treat closed).

**Guard script STAGED** (not baked): `/workspace/agent/staging/pr-draft-guard.sh` — triager-hardened (fixed 2 bypasses: title-token spoof + glued `-R` fail-open; 15 cases pass), repo-scoped DENY to shader-slang/* only with nv-coworkers/slang-coworkers forks exempt + fail-open on unresolved owner. Needs nanoclaw host-source PR + operator sign-off (read-only /app/hooks, per-spawn settings.json regen). Raise with operator when next active; instruction-file mitigation holds meanwhile. Deps: jq + grep -E.

**PR-APPROVER BLOCK (2026-07-12, ready-for-review webhook @834e0261):** slangpy#1054 flipped ready-for-review again → routed to slangpy-pr-approver (read-only). Verdict = **BLOCK**, reason_code `VERIFIED_BUG:api_version_not_bumped`, recorded to ledger `slangpy#1054 @ 834e0261dab6 = BLOCK`. Fix itself confirmed CORRECT on supported path (grad bit both bridge paths in lockstep, wired to true root cause, 2-site test coverage). **BLOCKER (source-verified 🔴):** `TENSOR_BRIDGE_API_VERSION` (tensor_bridge_api.h:193) NOT bumped despite signature *format* change `[Dn,Sm]`→`[Dn,Sm,Gk]`. Compat gate (torch_bridge.h:110) checks only api_version + struct_size → a stale separately-installed `slangpy_torch` v7 binary is still accepted, silently emits old `[Dn,Sm]`, reintroducing #1052 under package version-skew. Prior bridge behavioral change #816 bumped 6→7, so this violates the codebase's own convention. All 6 clauses pass (policy v0-shadow-relaxed); shadow mode = nothing posted to GitHub, no merge. PR head moved mid-run to test-only commit 2e3846c4 (doesn't touch finding; API_VERSION still 7 there) and reverted to draft — BLOCK robust to both; draft-guardrail sub-point resolved by the revert.

**REMEDIATION DONE (verified 2026-07-12 @head ebb9f68de):** fixer bumped `TENSOR_BRIDGE_API_VERSION` 7→8 (per #816 precedent) — stale v7 `slangpy_torch` binary now REJECTED by compat gate instead of silently emitting old `[Dn,Sm]`. Added deterministic `test_stale_bridge_version_is_rejected` (fake bridge, stale api_version + correct info_struct_size → asserts gate rejects). Triager re-verified at source: API_VERSION=8, PR isDraft=true/BLOCKED, both new tests present. Tests: test_torch_bridge 167, test_pytorch 13+1skip, test_torchintegration 330+84skip — 0 fail no flakes; codex 3-stage green; CI re-running on ebb9f68. #1052 comment patched in place to note version-skew hardening. **APPROVER RE-VERDICT (2026-07-12) = WOULD_APPROVE:** ledger `slangpy#1054 @ ebb9f68de = WOULD_APPROVE / CLEAN`, supersedes prior `BLOCK @834e0261`. Approver re-pinned live head, verified at source: API_VERSION 7→8 (tensor_bridge_api.h, now line ~199) with comment mandating bump on output-format changes citing #816; compat gate (torch_bridge.h:110, unchanged) now rejects stale v7 native binary → falls back to correct Gk-emitting Python path; `test_stale_bridge_version_is_rejected` genuine (subprocess, stale api_version + correct struct_size); core grad-bit fix intact both paths; all 6 clauses pass. Caught+discarded a stale pre-remediation Devin snapshot (re-flagged old line 193) before it could drive a false BLOCK; re-ran head-current → 0 bugs. Both critique gates + codex source-fetch passed. Shadow mode: nothing posted to GitHub, no merge.

**ENGINEERING-TERMINAL, maintainer+CLA-gated:** verified fix in DRAFT PR #1054 (`Fixes #1052`), approver-cleared. Only real blocker = `license/cla` PENDING on bot identity (allowlist, NOT agent-actionable, no operator card per empty-room logic). Awaiting bmillsNV (CODEOWNERS): CLA-clear + CI-green → squash-merge → promote from draft. Never bot self-ready/self-merge. Re-opens on non-flaky CI red or substantive human comment on #1052 (process through chain).

See [[feedback_no_double_dispatch_peer_wired.md]] · [[feedback_drafts_only_guardrail.md]] · [[feedback_route_authorizations_through_dispatch_owner.md]].
