---
name: project_11983_spirv_debugfunction_wrong_cu
description: "#11983 imported SPIR-V DebugFunction wrong CU scope — TERMINAL: PR #12148 MERGED 0864e60e63 (8 files/+135/−9, NOT the 13/+289 we published); issue closed"
metadata: 
  node_type: memory
  type: project
  originSessionId: 86e6c30b-d893-4630-97a4-0cb2792894f2
---

**shader-slang/slang#11983** — imported `DebugFunction` records reference an imported module's `DebugSource` but wrongly use the entry-point file's `DebugCompilationUnit` as parent scope. bug / low / target-emit SPIR-V debug-info / P3 (valid SPIR-V, runs fine; debugger/tooling correctness only).

**✅✅ TERMINAL — MERGED 2026-08-04 (Main-verified live via API, not relay).** PR #12148 `merged=true`, merge commit **`0864e60e635ef39d4c25e5e57747d909f1c05edd`**, final head **`72be35c1ac`**. **⛔ SHIPPED DIFF = 8 files / +135 / −9 — NOT the "13 files / +289/−7" this memo published below.** The announced figure was the *initial* head `bf2ce70aeb`; pdeayton + csyonghe tightened it over 7 commits (scope cut at `4ccab1cc8`, comment fixes, the `Dictionary<IRDebugSource*,IRDebugCompilationUnit*>` retype at `30a9eca0e`). **Mechanism unchanged** (`IRDebugFunction` optional trailing `getParentScope()` bound at the producer, emitter reads it, `findDebugScope` fallback). slang-triager caught our published claim was stale and **patched verdict cmt `5006919359` in place** (count stayed 5 — edited, not stacked), naming the delta rather than quietly restating new numbers. The superseded numbers are left in the shipped-entry below **as history, explicitly marked** — do not cite them as current. `#include`/`#line` CU scoping remains deferred to **#12150** (open).

**⭐ Reusable fact this chain established — the bot CAN push a merge commit carrying `.github/workflows/*`.** Final head `72be35c1a` (parents `30a9eca0e` + `d9353c090`, pushed by `nv-slang-bot[bot]` 08-03T17:06:33Z) carried **12 `.github/` files, 8 of them `.github/workflows/*.yml`** (`ci.yml`, `ci-slang-test.yml`, `ci-slang-test-container.yml`, `cmake-options-build{,-container}.yml`, `pr-board-sync.yml`, `release.yml`) and **the push succeeded**. This is a live counter-example to the 2026-06-04 PR #11265 blocker ("App lacks `workflows` write permission ⇒ any push introducing workflow-file changes is rejected, even verbatim from master"). ⚠️**Do NOT over-generalize:** #11265 was a **force-pushed rebase**, this was a **merge commit**; whether the App gained the permission or the block never covered merges is **UNVERIFIED**, and those predict differently for a future rebase. What is measured: a bot merge-from-master push carrying workflow files landed 08-03. See [[feedback_no_parent_traversal_in_includes]] for the other review-enforced-only rule class.

**🔄 NEW REVIEW ROUND 08-03 16:51Z (POST-approval; the retype push DISMISSES pdeayton's approval — expected, HE requested the change).** pdeayton-nv asked to **retype the debug-source→CU map to `Dictionary<IRDebugSource*, IRDebugCompilationUnit*>`** (strong typing). Fixer building in `wt-slang-11983` (~20min); **replied on-thread FIRST rather than gating a human-facing reply on the build** (correct — PR #12148 `discussion_r3705985175`).
- **To pdeayton:** doing the strong typing, leaving `IRDebugCompilationUnit` alone, `#include` ownership stays deferred to **#12150**; flagged the push will dismiss his approval.
- **To csyonghe's typing question:** conceded **no good reason** — the map was `IRInst*` only to match what `IRBuilder::emitDebugSource`/`emitDebugCompilationUnit` return.
- **⭐ To csyonghe's shared-header CORRECTNESS challenge (the substantive one) — answered with a real in-code argument, not a deflection:** the ambiguity **cannot arise in this map**. `SharedIRGenContext` is **per-TU** (`slang-lower-to-ir.cpp:15397`) ⇒ `module a`/`module b` never share a map; within one TU a CU is created only for `!isIncludedFile()`, immediately for that one source (`:15461-15465`) ⇒ **1:1 by construction**. `common.h` is an include ⇒ gets a `DebugSource` but **NO CU** ⇒ **never a key**. Fixer then **agreed csyonghe's operand-list idea is the better representation for #12150**, where the relation genuinely stops being a function. (Good shape: defend the invariant where it holds, concede the design where it doesn't.)
- **Code change** (`slang-lower-to-ir.cpp`, **+16/−13, behavior-identical**): map retyped; `cast<>` at the 2 creation sites (invariant guaranteed by construction; precedent `slang-ir-insts.h:3547`); `as<IRDebugSource>` at the read site (decoration-operand lookup ⇒ a miss degrades to the existing null-scope fallback); declaration comment tightened so it no longer overpromises a general source→CU relation. **Did NOT touch `IRDebugCompilationUnit` or builder return types** (outside the ask). clang-format 17 clean.
- **⚠️→✅ REBASE CAUGHT AND SWITCHED TO MERGE 08-03 17:05Z (Main flagged pre-push; fixer confirmed no force-push happened).** After the retype push, master had moved **+77 commits** and **PR #12202's `-debug-info-include-source` edits the SAME `emitDebugSource` lines** ⇒ real conflict. Fixer rebased **locally** and was about to **force-push**. Main flagged it against jhelferty's standing directive ([[no-force-push-under-review]] (shared: `1785520732879-on-a-pr-under-review-incremental-commits-merge-mas`)): a rebase rewrites every SHA ⇒ **pdeayton + csyonghe lose their "what changed since my last review" diff** on a 13-file PR with 4+ rounds and both actively engaged. Main explicitly said **don't unwind if already pushed** (would compound churn) — but **remote was still at `30a9eca0e6`, rebase purely local** ⇒ caught in time at zero cost. Fixer switching to: reset local to pushed `30a9eca0e6` (backup branch holds it) → **`git merge origin/master`** → resolve the same #12202 conflict **keeping BOTH changes** → rebuild → **normal fast-forward push, no force, no SHA rewrite**. Obsolete build subagent stood down. **Fixer will state the both-changes-kept resolution EXPLICITLY on-thread** (Main's ask — a conflict resolution inside a shared function is exactly what a reviewer wants confirmed, not inferred).
- **✅ MERGE COMPLETED 08-03 17:09Z — head `72be35c1ac` (rebase head `387910b25d` DISCARDED).** Original SHAs **PRESERVED** (`30a9eca0e6`, `1cd2262b60`, `f8a69f737b`) ⇒ **fast-forward push, no force, reviewer diffs INTACT.** Conflict resolution identical either way: master's #12202 `-debug-info-include-source` and the retype both edited the same `emitDebugSource(...)` call → **kept BOTH** (master's `(debugInfoLevel >= Standard || includeSource)` condition **and** the `cast<IRDebugSource>(...)` wrapper). Verified all 4 retype sites + master's `includeSource` (decl `:15456`, use `:15465`) present, **zero conflict markers, 0 behind master.**
- **Fixer caught + corrected its OWN false alarm:** initially thought the merge reverted master's `external/spirv-tools` bump; **checking ancestry properly showed the opposite** — `b707790a89` (on its branch) is **OLDER** than master's `0d6fd73ca7`, its branch never touched the submodule, and the merge **correctly adopted master's newer pin from #12184.** No regression. (Good instinct: verified ancestry rather than trusting the diff's appearance.)
- **⭐ Will post on-thread that history was NOT rewritten after all** — correcting its earlier "this push will dismiss/rewrite" warning so pdeayton + csyonghe **expect their diffs intact**. Important: a stale "history was rewritten" warning left standing would make reviewers distrust a view that is actually fine.
- Filed a shared learning: **`git reset --hard` under a running build yields a bogus `BUILD_EXIT` with an empty log** — which is exactly what the first (discarded) build hit ⇒ **tooling artifact, NOT a code failure.**
- **✅ SHIPPED 08-03 17:25Z — merge `72be35c1ac` pushed FAST-FORWARD (`30a9eca0e6..72be35c1ac`, no force, no SHA rewrite).** PR `mergeable: true` (was `dirty`); `mergeable_state: blocked` = **awaiting the re-review the push dismissed, NOT a failure.** CI auto-triggered on `pull_request`; fixer did NOT manually dispatch (non-draft — correct).
- **Final change:** 1 file, **+16/−13** — map retyped to `Dictionary<IRDebugSource*, IRDebugCompilationUnit*>`; `cast<>` at the 2 creation sites (type guaranteed by construction), `as<IRDebugSource>` at the lookup (a miss degrades to the existing fallback); declaration comment tightened. **`IRDebugCompilationUnit` untouched; `#include` ownership left to #12150** per pdeayton's explicit scope ruling.
- **⭐ Verification specifically targeted at the conflict risk:** merged-tree debug build clean (**1281/1281**, zero error/FAILED); `tests/spirv/` **563/563** + 12 ignored (was 526 pre-merge, **+37 from master**); this PR's own 4/4. **Ran all 7 of master's #12202 `-debug-info-include-source` tests explicitly → 11/11 sub-cases pass** = direct proof the conflict resolution preserved #12202 (that's the exact code the conflict was in — the right thing to prove rather than assert). codex OUTPUT_REVIEW approve, **prompted to REFUTE rather than confirm** — independently verified the net diff vs master is only the 8 #11983 files, #12202's files are byte-identical to master, and the submodule-pin ancestry.
- **Two on-thread replies posted:** the correctness answer to csyonghe, and the both-changes-kept note (Main's ask). Plus it **corrected its own earlier "history was rewritten" warning on-thread** — important, since a stale warning would make reviewers distrust a diff view that's actually intact.
- **Fixer flagged its own course-correction plainly** (rebase→merge, caught pre-push) and walked back **two** of its own intermediate claims: the submodule "revert" (its pin was OLDER; merge correctly adopted master's #12184 pin) and the SHA-rewrite warning (SHAs didn't change). Good honesty hygiene.
- **✅ RE-APPROVED 08-03 21:07Z — pdeayton-nv APPROVED at head `72be35c1ac8e` == HEAD (Main-VERIFIED).** Not the push-dismissed one (that was at `1cd2262b60`, now shows DISMISSED). `mergeable: true` (was `dirty`). **CI green on this head:** `CI | pull_request` success, **52 check-runs = 45 success / 7 skipped, zero non-passing**; required `pr: non-breaking` label present. Round-5 asks all closed (map retyped; `IRDebugCompilationUnit` **zero delta vs master**; `#include` still deferred to #12150; #12202 conflict resolution held — its 11 test sub-cases pass).
- **🔴 WHAT'S ACTUALLY GATING IT — resolved by Main (the fixer couldn't see it; token 401/403 on GraphQL + branch-protection):** the residual `mergeable_state: blocked` is an **UNMET REVIEW REQUIREMENT, not "awaiting merge."** Main verified: the PR **still lists `saipraveenb25` and `jkwak-work` as requested reviewers and NEITHER has submitted any review** — the only `APPROVED` is pdeayton's at head; all other review objects are `COMMENTED` (csyonghe + pdeayton at the old `1cd2262b60`, plus bot comments). `branches/master` returns no readable `required_pull_request_reviews` even to Main's token, so the exact rule count is unverifiable, but **green CI + two outstanding review requests + `blocked`** makes the review gate the live explanation. ⇒ **Next human action is a review from `saipraveenb25` or `jkwak-work`, not a merge press.**
- **⭐ FIXER SELF-CORRECTION (2nd this session, both caught by checking not asserting):** it had reported the residual `blocked` as "branch protection awaiting the human merge" — **an inference it never verified**; codex refuted it by pointing at the two requested reviewers. Retracted. **General form recorded: `mergeable_state` tells you THAT a requirement is unmet, never WHICH one.** (First correction was the submodule pin, where it claimed a regression that was the opposite.) ⚠️ **Main should have caught this earlier — I had accepted the "awaiting merge" framing without checking the reviewer list, which I could read and the fixer could not.**
- **Bot: no further pushes** (any commit dismisses the approval), **no merge** (operator-gated). **Merge webhook is the trigger to start #12150** (deferred `#include`/`#line` CU-ownership follow-up) off fresh master + reap this worktree; its plan + restore patch are already staged from the earlier scope cut.
- **Guardrails respected:** PR is **non-draft** ⇒ fixer will NOT manually dispatch CI (push auto-triggers) and will NOT touch `gh pr ready`/merge. **Expect pdeayton re-approval needed after the push** — the recurring push-dismisses-approval bind, correctly ANTICIPATED here rather than discovered after the fact. ⇒ **the 07-20 APPROVED state below is SUPERSEDED by this round.**

**Root cause (triager blame-confirmed):** `slang-emit-spirv.cpp` resolves every DebugFunction's parent scope through ONE module-global slot (`findDebugScope`); a post-pass added by #10907 (fixed #10906) unconditionally pins that slot to the entry-point CU → mis-scopes imported funcs. Each DebugFunction already carries the correct source via `getFile()`; only the derived scope operand is wrong.

**Approach A rejected (reporter caught the hole, triager VERIFIED @5c30d437f):** A = key scope by `DebugSource→CU` map, resolve by `getFile()` in `emitDebugFunction`. But `emitDebugSource` runs per-file (slang-lower-to-ir.cpp:15407) while `emitDebugCompilationUnit` is gated `!isIncludedFile()` (:15418) → an `#include`'d file gets a DebugSource but NO CU, so `cuForSource[getFile()]` misses for include-defined functions. Nuance: `import` ≠ `#include` (`setIncludedFile` only on preprocessor path) so A would still fix the *reported* import symptom but leave the include case unscoped.
**Conceded Approach B** = explicit scope/CU operand on `IRDebugFunction` at IR-gen — sounder, matches repo's store-canonical-at-producer methodology.

**🔄 RE-OPENED FOR IMPLEMENTATION (07-17 17:25).** Reporter pdeayton-nv (assignee) handed the issue back: cmt [5005714436] "Please implement Approach B, now that 11982 is closed." Standing "don't auto-implement while contributor self-drives" order LIFTED by the reporter's own request. slang-fixer implemented **Approach B**.

**✅ SHIPPED 2026-07-17 19:45 — draft PR #12148 (fixer msg 43668; Main-verified at HEAD).** OPEN/**draft**/nv-slang-bot, base master@3649fb982, head `bf2ce70aeb`, `Fixes #11983`, label `pr: non-breaking`, `report_pr_created` done. Title "Fix #11983: scope SPIR-V DebugFunction to its own module's compilation unit". **⛔SUPERSEDED FIGURE — 13 files (5 src + 8 test), +289/−7 was the INITIAL head `bf2ce70aeb`; the MERGED diff is 8 files/+135/−9. Do not cite this number.** Approach B as designed: optional trailing scope operand (owning DebugCompilationUnit) on `IRDebugFunction` bound at IR-gen; emitter reads it, falls back to `findDebugScope` when absent; `#include`-defined functions resolve to their includer's CU via a **TU-anchored SourceView walk** (TU manager first, then parents → deterministic TU-first). Internal IR only — no `include/`/`prelude/`/`.meta.slang` change (non-breaking). Verify: local suite 76/76 PASS (2 ignored) — spirv debug/source/include/inline; import + include + multi-CU-include + `.slang-module` serialization round-trip all verified against emitted SPIR-V (the round-trip risk this memo flagged was exercised). codex PLAN+CODE approve. **Peer review dispatched to slang-reviewer (in flight); no reviewer pre-request per policy (pdeayton-nv is assignee, their requested approach).** Draft CI red = benign priority-yield ([[project_bot_pr_priority_yield_red_run]], run 29608109010) — builds skipped, not a code failure. Draft-held; ready-flip/merge maintainer/operator-gated; bot flips nothing. Chain ACTIVE (was HOLD → implementing → shipped-draft).

**Review round 1 addressed (07-17 21:43, fixer msg 43678).** pdeayton-nv (assignee) requested a **behavior-preserving rename** for future lexical-scope support: `getScope()`→`getParentScope()`, builder arg `scope`→`parentScope`, operand 5 documented as the function's lexical parent (currently always the owning CU). Pushed head **`f2d0526529`** ("Address review: rename DebugFunction scope operand to parentScope", additive FF). Operand stays optional + 5-operand legacy-compatible + plain `IRInst*`; NO lexical-block IR / no active-scope change (as reviewer asked). 6/6 local tests still pass; acked confirmed-done on-thread. No peer re-run (behavior-preserving). CI red = benign priority-yield (run 29615529761).

**Review round 2 addressed — SCOPE CUT (07-17 23:06, fixer msg 43688).** pdeayton-nv asked to NARROW the PR. Fixer scoped it to **non-included module sources (the reported `import` case) only** — removed the include-hierarchy walk (`findIncludingNonIncludedSourceFile` + second pass) + the 5 include-specific test fixtures. **Deferred include work → NEW follow-up issue #12150** (not dropped). `#include`/`__include`/`#line` sources now keep null `parentScope` → module-global fallback (unchanged behavior, now documented). Kept: `parentScope` operand + propagation + serialization + SPIR-V consumption + import & separate-comp tests; strengthened separate-comp test to anchor on each function's own DebugFunction record + assert distinct module/consumer CUs. Head **`4ccab1cc85`**, diff cut 13 files/+289 → **8 files/+131** (at head `4ccab1cc85`; MERGED final is 8 files/**+135**/−9 after two later comment/retype rounds — cite the merged figure, not this one). 74/74 local pass; acked on-thread. No peer re-run (behavior-preserving for kept path). CI red = benign priority-yield (run 29619718183).

**Review round 4 addressed — comment discipline + factual correction (07-20 19:05, fixer msg 50394; comment-only, head `1cd2262b60`).** pdeayton-nv flagged (1) comment wording + (2) a factual claim. Fixes (both comment-only, commits f8a69f737b + 1cd2262b60): **(1) Comment discipline** — removed change-history ("unchanged from before this change") + PR-pointer ("see the issue linked", "pending #12150") phrasing; comments now describe code as-is (fixer filed a shared learning). **(2) Factual correction** — pdeayton asked "is DebugFunction emitted at Minimal?"; fixer verified vs compiler: **`-g1` emits ZERO NonSemantic DebugFunction/CU/DebugSource records** → the emit-side comment wrongly listing Minimal as a fallback was removed. Codex-caught subtlety (kept correct): emit-spirv has TWO `emitDebugFunction` callers — global switch (gated >Minimal) + func-definition path (ungated but returns null at Minimal, no module debug scope); IR-gen DOES create a 5-operand `IRDebugFunction` at `-g1` (null parentScope), so IR-level comments correctly still mention Minimal. Confirmation build green, 4/4 local pass. Answered both questions on-thread.

**✅ APPROVED 2026-07-20 19:20 (fixer msg 50396) — assignee pdeayton-nv, at HEAD.** `pullrequestreview-4738176805`, PR now **non-draft**. Verified live: reviewDecision=APPROVED, approval `commit_id == head 1cd2262b60`, not dismissed. **All FOUR review rounds resolved:** R1 rename→`parentScope`; R2 scope-cut + follow-up #12150; R3/R4 comment audit + Minimal-emit accuracy. Non-draft → `pull_request` CI runs automatically (fixer stopped manual dispatch = drafts-only). Fixer will NOT push again (would auto-dismiss approval) and will NOT merge (operator-gated). **On #12148 merge webhook:** fixer reaps worktree + active-work sentinel, then starts deferred follow-up **#12150** (include/`#line` CU ownership) off fresh master. **Next: maintainer MERGE (operator/maintainer-gated).** Terminal = merge → reap + #12150 kickoff.

**Review round 3 addressed — comment/PR-desc audit (07-20 18:07, fixer msg 50386).** pdeayton-nv asked the fixer to re-verify its source comments + PR description were still accurate after the round-2 scope cut. Audit found two stale comments left by the cut, fixed: (1) emit-spirv fallback comment now lists the `#include`'d/`#line`-remapped-source case (not just Minimal/legacy) as an absent-`parentScope` reason; (2) `getParentScope()` doc dropped the now-wrong "included" claim (included functions get no parent scope in this PR — deferred to #12150). Builder/map/creation-site comments, separate-compilation test comments, and full PR description verified already-accurate (no leftover include-hierarchy/removed-fixture claims). Head **`f8a69f737b`** ("Address review: refresh comments left stale by the scope cut"), 4/4 local tests pass, fixer replied on-thread with audit result. CI red again benign priority-yield. Still draft-held; webhook-driven; **Next:** pdeayton review/approve → maintainer merge; #12150 held pending #12148 merge.

**Approach-B edit surface (triager re-confirmed at HEAD 3649fb982):** IRDebugFunction operands slang-ir-insts.h:2719; builder slang-ir.cpp:3554; `fixUpDebugFuncType` clone that would drop a new operand slang-ir.cpp:872; IR-gen site slang-lower-to-ir.cpp:14680; CU gate `!isIncludedFile()` :15443. **Load-bearing risk:** new scope operand must round-trip through IR-blob serialization for precompiled-module imports (#12034 already exercised that path for DebugSource — reuse the test shape).

**Prior HOLD (07-17 10:19, superseded above):** was a deliberate close while the contributor self-drove the design; reporter's Approach-A objection re-verified @5c30d437f8 (include'd files get `IRDebugSource` at :15419 but NO `IRDebugCompilationUnit` → `cuForSource[getFile()]` misses); fixer retracted its original triage assumption.

**Stale attempt — CLEANED UP (07-17):** slang-fixer's superseded Approach-A WIP (branch `fix/issue-11983` @ `e35f89ec7b`) had survived the worktree-dir removal in the base clone's `.git`. codex CODE_REVIEW caught the fixer's "code is gone" claim was FALSE; fixer **deleted the unpushed/stale/unsound WIP branch** and corrected the wording across artifacts. Filed a shared learning on the worktree-dir-removed-but-branch-survives-in-.git gotcha. (Truthfulness correction — good catch; the branch was never pushed, no GitHub footprint.)

**Sibling:** #11982 **CLOSED/completed; PR #12034 MERGED (commit 3649fb982, now HEAD).** #12034 touched only `getOrEmitDebugSource` (DebugSource dedup, ~9665-9742) — does NOT overlap Approach B's edit surface and leaves the CU-scoping bug live.

GitHub trail: reporter objection cmt 5001685503; triager verified reply cmt 5001804231; triage verdict cmt 4909851905. Triage memo `triage-11983.md`. Canonical thread `gh-issue-shader-slang/slang-11983`.
