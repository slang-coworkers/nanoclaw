---
name: project_11917_pass_gating_epic
description: shader-slang/slang
metadata: 
  node_type: memory
  type: project
  originSessionId: d426803e-6b4c-4725-a21a-7ca38bb18994
---

**#11917** "Avoid running backend IR passes when they cannot apply" — compile-time perf epic on `RequiredLoweringPassSet` / `linkAndOptimizeIR` (`source/slang/slang-emit.cpp`). ~55 of ~80 backend passes run unconditionally (full-module walks) even when their IR feature is absent. Author pdeayton-nv.

**BATCH-2 RE-ACTIVATION 2026-07-28 (pdeayton-nv, epic driver, issuecomment-5098415750).** pdeayton asked to gate 4 more passes — `validateAndRemoveAssumeAddress` + the tag/union trio `lowerUntaggedUnionTypes`/`lowerTagInsts`/`lowerTagTypes` — WITH an implementation plan that UN-PARKS the B/C-risky tail WITHOUT a rescan (jkwak rejected rescan 07-14): (a) new booleans in `RequiredLoweringPassSet`, direct-opcode-family scan in `calcRequiredLoweringPassSet`; (b) move initial scan to immediately before `validateAndRemoveAssumeAddress`; (c) **make every tagged-union opcode conservatively imply `untaggedUnion`/`tagOps`/`tagType`** (safe-superset covering the in-window synthesis by `lowerTaggedUnionTypes` after the last scan). This is the safe-superset mechanism, NOT rescan — respects jkwak's steer. **Triager trace-verified at HEAD 15863db48 (07-28), all 4 GREEN, dispatched slang-fixer on canonical thread.** validateAndRemoveAssumeAddress=A-safe (AssumeAddress front-end-only @slang-lower-to-ir.cpp:5823; scan-relocation moves scan#1 ~10 lines earlier @:1038, 18 flag-consumers stay downstream @:1048+ → not stranded). Trio: pre-scan-producer fact re-confirmed at HEAD (typeflow specialization in linkIR, before scan#1; matches merged #11961 + comment @slang-emit.cpp:1600-1604). **Precision note handed to fixer (safety-critical):** the tagOps scan arm MUST enumerate ALL FOUR tag insts — `GetTagForSuperSet`/`SubSet`/`MappedSet` are pre-scan-only, NOT covered by the tagged-union implication; gating on `GetTagOfElementInSet` alone = stale-FALSE miscompile. Constraints: drafts-only, non-closing (`Addresses #11917`), bot flips/merges nothing. Awaiting fixer [Fix Report] + draft PR#; triager rolls up + posts closest-to-the-state. This reactivates the parked tag/union family from "B/C-risky, needs design mechanism" → "gateable via pdeayton's safe-superset implication."

**Framing (triager, HEAD-verified):** incremental epic, NOT a one-shot. Batch-gating all ~55 is a miscompile hazard — a stale-FALSE flag (feature present, flag unset) skips a needed pass. Autodiff (#11474/open PR #11476) is the canonical trap. Approach **A** (per-pass gating, one profiled pass at a time, first PRs restricted to passes where stale-FALSE is structurally impossible) chosen over B (fix staleness/rescan) and C (shared opcode-presence index).

**EMERGING PATTERN 2026-07-14 (load-bearing for epic outlook):** the LATE-pipeline passes are turning out mostly B/C-RISKY (trigger synthesized in-window → no naive gate): `legalizeMatrixTypes` (matrix, in-window emitCast) and now the whole tag/union family (family (a): lowerUntaggedUnionTypes/lowerTagInsts/lowerTagTypes — `lowerTaggedUnionTypes` @:1532 CREATES their trigger ops AFTER the last scan :1452, verified anchors @HEAD a9c6ff78e4). The genuinely-naively-gateable wins were the EARLY passes (#11920 append/consume, #11961 tagged-union-type) — both MERGED. The tail increasingly needs a RESCAN design (Option 1: fresh rescan after the synthesizing pass, gate on rescanned flags — extra walk only when the rare feature is present), NOT naive flag-gates. So pdeayton's "12-pass batch" is reframing from "12 quick gates" → "a few early gates + a design-gated tail." The B/C-risky set (matrix + tag/union family + parked B/C pair legalizeResourceTypes/legalizeEmptyTypes) LIKELY CONVERGES ON ONE design answer (rescan-vs-producer-maintenance) — triager holding as a bundled design item to surface to jkwak/pdeayton once the batch trace is complete, NOT piecemeal. NOTE subtlety: matrix is already on a per-pass IN-PASS-SCAN resolution path via #11987 (pdeayton's shallow global-insts scan) — so the "converged design" may be "adopt the #11987 in-pass-scan pattern for all B/C-risky passes" rather than a new shared rescan point; that's the fixer+maintainers' design call, not Main's.

**BATCH TRACE COMPLETE-ISH 2026-07-14 (families a+b done, c in flight) — verdict: overwhelmingly B/C-risky.** All at HEAD a9c6ff78e4 (last scan :1452):
- Family (a) tag/union (lowerUntaggedUnionTypes/lowerTagInsts/lowerTagTypes) → ALL 3 B/C-risky, no PR (lowerTaggedUnionTypes :1532 synthesizes their triggers in-window).
- Family (b) legalize (legalizeEmptyArray/legalizeVectorTypes/legalizeUniformBufferLoad) → ALL 3 B/C-risky, no PR. Strongest: generateAnyValueMarshallingFunctions :1584 (in-window) builds getVectorType(uint,numUints) with numUints=(size+3)/4 → 4-byte field → numUints==1 → genuine 1-vector = exact legalizeVectorTypes trigger, and IRBuilder::getVectorType has NO count-1→scalar collapse → real stale-FALSE. legalizeUniformBufferLoad = closest-to-gateable (no in-window whole-buffer-load producer found) but not provable vs inlining/simplify reshaping → conservatively held.
- Family (c) FIRED (last family): lowerLValueCast / processLateRequireCapabilityInsts / lowerSumVectorMatrixInsts / removeRawDefaultConstructors + lowerReinterpretOptional broad→narrow tightening. checkForOptionalNoneUsage held out (diagnostic-safety question separate). lowerLValueCast + processLateRequireCapabilityInsts were EARLIER profiled A-safe (front-end-only triggers, sub-1%) → these are the likely naive-gateable singletons in (c).
- **TALLY: early passes gated cleanly (#11920 + #11961 MERGED); late passes overwhelmingly B/C-risky (matrix + tag/union + legalize = 7 passes needing rescan). Batch converging on: a handful of naive-gateable singletons at most (from family c) + ONE maintainer RESCAN design decision covering ~all late passes (matrix + tag/union + legalize families + parked B/C pair).** After family (c), triager brings the full A-safe-vs-needs-rescan ledger to Main + maintainers for the design decision — THAT is the next real decision point, and it's the maintainers' (rescan vs shared-index vs producer-maintenance vs adopt-#11987-in-pass-scan-everywhere).

**BATCH COMPLETE 2026-07-14 — full ledger + converged design ask.** Family (c): 3 A-safe → draft PR #12088 (Main-verified draft/non-closing/base-master; gates lowerLValueCast + lowerSumVectorMatrixInsts + processLateRequireCapabilityInsts; each A-safe confirmed in source — lowerLValueCast front-end-only trigger, lowerSumVectorMatrixInsts producer finalizeAutoDiffPass :1389 BEFORE scan :1452, processLateRequireCapabilityInsts front-end + diagnostic-only false-skip=no-op; revert-drill byte-identical, codex approve, peer review → slang-reviewer, fixer owns round max-2). 2 B/C-risky no-PR: removeRawDefaultConstructors (in-window synth), lowerReinterpretOptional-tightening (ReinterpretOptional synth in-window by specialization → narrowing broad→narrow INTRODUCES stale-FALSE the broad optionalType flag masks; broad is safe ceiling, tightening NOT shippable).

**FINAL BATCH LEDGER:** GATED/SHIPPED = 5 (lowerAppendConsumeStructuredBuffers #11920 MERGED, lowerTaggedUnionTypes #11961 MERGED, + lowerLValueCast/lowerSumVectorMatrixInsts/processLateRequireCapabilityInsts in draft #12088). B/C-RISKY/no-PR = 8 (tag/union ×3, legalize ×3, removeRawDefaultConstructors, lowerReinterpretOptional-tightening). lowerReinterpretOptional already-gated (broad optionalType). checkForOptionalNoneUsage = separate diagnostic-safety question, still HELD.

**THE CONVERGED MAINTAINER DESIGN ASK (real epic outcome):** the late-pipeline tail is uniformly un-gateable by naive flags (a later pass synthesizes exactly the trigger IR). ONE small RESCAN (recompute requirement flags after the last synthesizing pass, gate on fresh values) cleanly covers ALL of it: tag/union + legalize + removeRawDefaultConstructors + lowerReinterpretOptional-tightening + legalizeMatrixTypes(#11987) + parked legalizeResourceTypes/legalizeEmptyTypes. Triager posted rescan-vs-shared-index design question to jkwak+pdeayton on the issue. **NOW MAINTAINER-GATED DESIGN, not more batch dispatch.**

**jkwak REJECTED RESCAN 2026-07-14 (issuecomment-4965434216):** "We don't have a plan to run another re-scan at the moment. Please do not go with the idea of rescan." DECISIVE — the rescan (Approach B) that was proposed to cover the ENTIRE B/C tail is OFF THE TABLE. Consequence: the 8 B/C-risky passes are largely UN-GATEABLE cheaply; the covering-mechanism plan is dead. Does NOT close the issue, does NOT reject shipped work (#11920/#11961 merged + #12088 in review all stand). RESPECT IT — do NOT re-propose rescan or float a heavy alternative against his stated preference for pipeline simplicity. The one remaining live possibility = pdeayton's PER-PASS IN-PASS shallow-scan (#11987 pattern: local early-out INSIDE a pass, NOT a shared re-walk) — jkwak is ALREADY adjudicating that exact pattern on the #11987 PR thread, so it resolves THERE; do NOT force a cross-thread clarification. Epic settles to: cheap gates SHIPPED (2 merged + 3 in #12088), B/C tail PARKED absent an acceptable low-cost mechanism, tail's fate tracks #11987's in-pass-scan outcome. checkForOptionalNoneUsage diagnostic-safety micro-question still separate/held. **#12088 APPROVED 2026-07-14 by jkwak** (Main-verified: reviewDecision=APPROVED, "Looks good to me" @04:45:31; jkwak flipped it ready HIMSELF @04:45:40 — timeline-confirmed, bot did NOT self-flip; mergeable_state=blocked=awaiting maintainer merge press, merged=null bot holding). jkwak left 3 affirming inline comments each validating one gate's producer against current source (lowerLValueCast/processLateRequireCapabilityInsts/lowerSumVectorMatrixInsts) — independent maintainer confirmation of the A-safe traces, no changes requested. So all 3 family-(c) A-safe gates are maintainer-approved → combined with #11920+#11961 merged = the ENTIRE genuinely-gateable set (5 passes) shipped/approved. slang-reviewer peer round still running but maintainer approve authoritative.

Open threads: (1) rescan-vs-index pick — CLOSED, jkwak rejected rescan (tail parked), (2) #12088 APPROVED, awaiting maintainer merge press (bot won't merge), (3) #11987 matrix rework (jkwak coarse-flag-vs-shallow-scan pick), (4) #11961 merged.

**jkwak HANDED OFF TO pdeayton + floated close 2026-07-14 (issuecomment-4975021040):** "Assigning to @pdeayton-nv to driver the rest. I think we can close this but there might be missing one." = maintainer handoff to pdeayton (issue author, human) to DRIVE THE REST + tentative close with a hedge. BOT STANDS DOWN autonomous drive — pdeayton owns the remaining epic now (per [[feedback_deadpromise_check_assignee_before_rewake]] assignee-owns→stand-down). Do NOT auto-close (close is jkwak's/pdeayton's action per [[feedback_github_writes_operator_authorized]]). **"The missing one" — bot's ledger answer: `checkForOptionalNoneUsage` is the ONE item from pdeayton's 12-list with NO gating determination (deliberately held — it's a diagnostic CHECK not a lowering pass; skipping risks dropping a DIAGNOSTIC, different risk class).** Everything else accounted: 5 gated (2 merged + 3 in approved #12088), 8 B/C-risky parked, lowerReinterpretOptional already-gated (broad). CAVEAT: if jkwak means a pass OUTSIDE the listed set, the broader ~55-pass universe was never exhaustively enumerated (that's part of what "pdeayton drives the rest" scopes). Triager to post this coverage map + name checkForOptionalNoneUsage, confirm handoff, NOT close. Bot's remaining involvement = #12088 merge + #11987 continue on own threads; otherwise pdeayton-driven. Fresh-sub-thread + sequential-family plan worked end-to-end, ZERO re-thrash — validated recovery from the compaction incident.

**PROCESS SIGNAL 2026-07-14 (reinforces existing learning):** fixer's producer-trace SUBAGENT initially falsely reported family (b) all-A-safe (claimed any-value marshalling "creates size≥2 only" — missed the unguarded numUints==1 path). Fixer caught it by verifying every producer claim IN SOURCE, not trusting the subagent summary — that discipline prevented a miscompile-class false gate. Same lesson as the invisible-edge incident: verify against ground truth, don't trust a summary. [[feedback_route_authorizations_through_dispatch_owner]]

**SAFETY TEST — the "position trap" (triager refinement 2026-07-11, load-bearing):** the stale-FALSE hazard test is "is the trigger IR SYNTHESIZED between the last calcRequiredLoweringPassSet scan and the gate," NOT "does the pass RUN after the last scan." Positional lateness ≠ risk. E.g. `processLateRequireCapabilityInsts` runs very late (:2310) but its trigger is front-end-only → A-SAFE. Conversely a pass right after the scan whose trigger IS synthesized in-window is B/C-risky. Predicate-tightening (pdeayton's mechanism) must stay a SAFE SUPERSET of everything the pass lowers, and does NOT cure stale-FALSE for post-scan-synthesized triggers. Do NOT mis-apply the positional heuristic.

**Slice #1 — SHIPPED.** Draft PR #11920 gated `lowerAppendConsumeStructuredBuffers` behind new `RequiredLoweringPassSet::appendConsumeStructuredBuffer` (front-end-only opcode, never synthesized late → stale-FALSE structurally impossible). MERGED to master by @jkwak-work 2026-07-03T02:27:27Z (mergeCommit `96003b0d85`, `Addresses` = non-closing → **#11917 stays OPEN** as the epic tracker). Guardrail clean end-to-end: bot never readied/merged — jkwak-work did both himself. Tests: 2 SIMPLE regressions × glsl+spirv-asm (present+absent); revert-drill byte-identical. Triage verdict comment 4870469839 kept current in place.

**Maintainer close-question (2026-07-06).** jkwak-work (assignee) commented on #11917 asking the bot "what remains / can we close this now?" (issuecomment-4895902849, genuine @nv-slang-bot mention). Triager answered on GitHub (issuecomment-4895933715, re-verified at HEAD b1ab2c585: flag in master slang-code-gen.h:82 / slang-emit.cpp:528 gate :1636; #11917 still OPEN): gave the honest 3-way remaining split — cheap global walks (low standalone value), costlier passes (real wins, per-PR Approach-A), and **staleness-prone passes whose trigger IR is synthesized after the last scan (:1393) — NOT safe under simple per-pass gating, need Approach B/C (design call)**. Recommended **yes, safe to close the umbrella** (mechanism+pattern in-tree, rest is opportunistic/per-PR, focused issues drive it better) while noting keep-open-as-catch-all is fine. Did NOT self-close (standing policy — close is jkwak's). **Awaiting jkwak's call**: if he explicitly asks, triager can close on his ask; else his click.

**Author reopened with 9-pass scope (2026-07-06).** pdeayton-nv (issue author) said "too early to close", named 9 passes to gate (issuecomment-4896152495) — superseding the close rec; umbrella stays OPEN. Triager classified each at HEAD b1ab2c585 (scans slang-emit.cpp:983 & :1421), acked on GitHub (issuecomment-4896227330):
- **Already gated (2, no work):** `specializeHigherOrderParameters` (:1353, `higherOrderFunc`), `lowerReinterpretOptional` (:1372, `optionalType`).
- **A-safe, slice-#1 pattern (3):** `lowerLValueCast` (:1263, InOut/OutImplicitCast), `processLateRequireCapabilityInsts` (:2275, LateRequireCapability), `validateAndRemoveAssumeAddress` (:973, AssumeAddress — CAVEAT: runs BEFORE scan#1 :983, needs earlier scan point or pass moved after scan; op itself safe).
- **Needs B/C design call (2):** `legalizeResourceTypes` (:1776, IR synth after last scan via lowerExistentials/lowerTuples/anyvalue-marshalling), `legalizeEmptyTypes` (:1784/:1794/:2401, empty types made post-scan by DCE/struct/existential/tuple legalization). Simple gating = miscompile risk; need rescan(B) or shared index(C). Triager asked pdeayton his B-vs-C preference on GitHub.
- **Uncertain, need producer trace (2):** `lowerTaggedUnionTypes` (:1492), `legalizeMatrixTypes` (:1814).
Fixer NOT dispatched initially — impl awaited operator go-ahead.

**jkwak directed PRs for the 2 uncertain passes (2026-07-06, issuecomment-4897510535):** "Please make a PR for them and see if CI tests can pass" — named `lowerTaggedUnionTypes` + `legalizeMatrixTypes`. Main authorized fixer dispatch on JKWAK's authority (not self-scope-expansion) within drafts-only guardrail, with hard trace-first safety condition. Result = **draft PR #11961** (`fix/issue-11917-pass2 @ 970736222b`, non-closing, draft, verified by Main):
- `lowerTaggedUnionTypes` → **A-SAFE, GATED.** 6 tagged-union opcodes produced only by typeflow specialization (specializeDynamicInsts←specializeModule), which runs BEFORE last scan; no post-scan producer; upcastSet only recombines existing. New `taggedUnion` flag, exact-6-opcode detection, preserves `reinterpret` side-effect. Regressions glsl+spirv-asm, revert-drill byte-identical ×4, -dump-ir confirms materialization. codex approve.
- `legalizeMatrixTypes` → **NOT gated (correctly).** Lowerable trigger (bool/int/uint-element matrices) CAN be synthesized post-scan via IRBuilder::emitCast/emitConstexprCast (in-window: anyvalue-marshalling, simplifyIR peephole). Naive gate = stale-FALSE miscompile green CI wouldn't catch; only provably-safe superset ("any matrix type") is valueless (float matrices ubiquitous). Fixer surfaced **B(rescan) vs C(shared-index)** design question to jkwak in PR body instead of shipping unsafe skip. **jkwak's B/C steer is the open design item gating any matrix work.**

TRACE-FIRST SAFETY CONDITION WORKED AS DESIGNED — caught 1 safe / 1 unsafe, refused the unsafe gate. Reusable pattern for the remaining passes.

**#11961 status (2026-07-07):** jkwak-work flipped it READY himself @ 2026-07-06T23:15:53Z (timeline-verified by Main; bot did NOT self-flip, guardrail intact). CI fully green — all 43 real jobs pass (earlier segfault = known-flaky CPU test-server shard that also flakes on master, cleared on rerun; jkwak confirmed "CIs all passing"). BUT jkwak (comment 4899404777): *"still not sure if it is safe. I will discuss with Yong later this week before merge."* → **HANDED OFF, awaiting maintainer (jkwak+Yong) safety sign-off**; resumption = their post-Yong decision. Fixer replied closest-to-the-state (4899410021) restating tagged-union producer-timing proof + revert-drill. mergeable_state=blocked (review-required + safety hold, NOT CI). Bot-merge operator/maintainer-gated — do NOT merge.

**#11961 FULLY APPROVED 2026-07-09 — hold condition MET favorably.** csyonghe (Yong) APPROVED @ 18:32:24Z (Main-verified: real maintainer approval, reviewDecision=APPROVED, bot did NOT merge/self-ready — merged=false, draft flipped by maintainer earlier, label Office-Yong, mergeable_state=blocked=awaiting maintainer merge press). jkwak COMMENTED LGTM→Yong approved = dual review complete. **No GENERAL concern raised about the flag-gate mechanism — it's BLESSED by both maintainers.** (jkwak's value-skepticism was on #11987's SCAN, a different mechanism, not the flag-gate.) Merge still maintainer-gated; bot will NOT merge/rebase (rebase risks dismissing Yong's fresh approval).

**#11961 MERGED 2026-07-10T00:37:55Z** by jkwak-work (Main-verified: merged=true, mergeCommit `cffff65f97a9880c52dc28fc8faaaca727f810a5`, base master, closed). Just before merge jkwak formalized LGTM→APPROVED so it landed on dual formal approval (jkwak+Yong). Guardrail held end-to-end: bot never merged/readied — maintainer readied for Yong, jkwak pressed merge. Final tests = single runtime COMPARE_COMPUTE_EX (both SIMPLE emission-guards deleted per jkwak). Worktree wt-slang-11917-pass2 reaped. **EPIC TALLY: slice #1 (#11920) + pass #2 (#11961) BOTH MERGED.** The flag-gate mechanism is now upstream + maintainer-validated.

**3 A-safe passes — HOLD RELEASED 2026-07-09, dispatched PROFILE-FIRST through triager.** `lowerLValueCast`, `processLateRequireCapabilityInsts`, `validateAndRemoveAssumeAddress`. Flag-gate mechanism now blessed (#11961 verdict), so the hold reason is gone. BUT jkwak's "#11987 not useful" = he rejects gates that don't save real work → do NOT batch 3 speculative gate PRs. Dispatched (via triager, single-sourced): fixer PROFILES the 3 passes' actual compile-time cost on representative modules first, then opens gate PRs ONLY for the ones that pull their weight, highest-value first, individual drafts. Value-disciplined release, not blanket. Was NOT dispatched before this.

**A-SAFE PROFILING RESULT 2026-07-09: NONE worth gating → SKIP-ALL, no PRs opened.** Fixer profiled (`-report-detailed-perf-benchmark`, 600-fn/10.8k-line zero-trigger module, spirv -O0, 7-run median, share of linkAndOptimizeIR); triager confirmed no new fix/issue-11917 A-safe branch on origin:
- `validateAndRemoveAssumeAddress` — 1.49ms / **0.011%** → SKIP (negligible + runs before scan#1 :983 → needs extra scan/pass-move = most cost least win).
- `processLateRequireCapabilityInsts` — 3.30ms / **0.023%** → SKIP (negligible even w/ its extra buildEntryPointReferenceGraph walk).
- `lowerLValueCast` — 134.94ms / **0.880%** → SKIP, BORDERLINE (only one >0.1%; but 134ms is pathological-module, ~O(module size) → few ms real shaders, <1%). Cleanest to gate if wanted (trigger kIROp_InOut/OutImplicitCast, free flag, no pass-move).
All 3 structurally gateable (single trigger op, detection piggybacks existing scan at zero added walk — #11961 template); blocker is purely VALUE = sub-1%/near-zero = exactly jkwak's "#11987 not useful" bar. **Main DECISION: accept skip-all; do NOT gate lowerLValueCast on Main authority** (data doesn't justify, invites another "not useful"). pdeayton (issue author, requested these on his 9-pass list) can object — profiling numbers PATCHed transparently to issue comment 4897530784; if he wants lowerLValueCast gated despite <1% it comes as a webhook → dispatch THROUGH triager. **This EXHAUSTS the cheaply-gateable tail** — remaining epic work = 2 B/C design-call passes (pdeayton mechanism) + #11987 matrix decision (jkwak/pdeayton), both maintainer-owned. Decision (Main + fixer concur): #11961 IS the reference impl of the flag-gate (Approach A) all 3 would copy; jkwak+Yong's safety discussion could raise a GENERAL concern about gating on RequiredLoweringPassSet (staleness worry / preference for rescan/shared-index over flags) that reshapes all 3 → building+reviewing 3 flag-gate PRs now risks rework on one objection. No correctness pressure (pure perf), no cost to wait. Resume: if jkwak+Yong bless the flag approach → batch the 3 (low-risk tail); if they steer to rescans → redo A-safe via pass #3's scan template instead. Operator may override for momentum. Do NOT re-ask operator each turn — this is the decided safe default.

**Pass #3 (matrix early-out scan) — proceeding, NOT gated by the A-safe hold** (scan approach, not flag approach, so the flag-mechanism question doesn't block it). See below.

**The 2 B/C passes** (`legalizeResourceTypes`, `legalizeEmptyTypes`) await pdeayton's rescan-vs-producer preference. NOT dispatched to fixer. Do NOT PR — a scan-only PR would unilaterally pick Approach B and pre-empt the pending maintainer design call.

**AUTHORIZATION-BOUNDARY DRIFT INCIDENT (2026-07-07):** After an 868k-token compaction, slang-fixer began prepping a PR and CONFLATED two distinct authorizations. Ground truth (Main holds the authorization edge):
- **AUTHORIZED = `legalizeMatrixTypes` (pass #3), scan approach**, per Main msg **id=45** (Main→fixer direct, in reply to fixer #38; on pdeayton PR comment 4909445741 requesting it). The "pdeayton #1 worklist / #2 scan" framing is REAL and belongs to THIS pass. Triager never saw id=45 (Main↔fixer edge, not routed through triager) → triager correctly "never dispatched it" but wrongly read it as hallucination.
- **NOT authorized = `legalizeResourceTypes` + `legalizeEmptyTypes` (the B/C pair)** — awaiting pdeayton's mechanism pref; no PR.
Triager caught it at "about to open" (NO artifact created — Main verified only #11961 exists on fix/issue-11917* branches) and sent a hard STOP challenging the fixer to quote an exact msg-id. HAZARD: fixer citing the REAL id=45 would look like more hallucination to the triager (invisible-authorization problem) → Main must inject ground truth. Disambiguation rule: reference passes by NAME (legalizeMatrixTypes vs legalizeResource/EmptyTypes), never the ambiguous "#1/#2" framing. See [[feedback_no_double_dispatch_peer_wired]].

**pdeayton steered pass #3 (matrix) 2026-07-07 (PR comment 4909445741):** asked to (a) trim #11961 to just the TU gate [fixer DONE: retitled "Gate lowerTaggedUnionTypes (#11917)", removed matrix analysis, scope note, reply 4909511644, left draft/ready to jkwak], and (b) open a NEW PR optimizing legalizeMatrixTypes — two approaches: (1) worklist-reduction in-pass speedup, (2) early linear scan detecting lowerable matrices present. **His (2) = the correct-by-construction Approach B** the fixer flagged: a fresh scan immediately before the pass reflects current module state → stale-false-negative hazard DISAPPEARS (vs a stale flag). Main authorized a fresh DRAFT PR on pdeayton's (issue-author) authority + drafts-only guardrail. SEPARATE PR from #11961, no interaction with jkwak+Yong hold.

**Matrix PR = #11987 (OPENED + Main-verified 2026-07-07).** `fix/issue-11917-matrix @ ab3bfde176`, draft=true, base master, `Addresses #11917` non-closing (closingIssuesReferences empty — Main confirmed via GraphQL), `pr: non-breaking`, report_pr_created done. Title "Skip legalizeMatrixTypes when no matrix needs legalizing (#11917)". Change = in-pass early-out at pass entry (NOT slang-emit.cpp, NOT a flag): Tier-1 O(1) `targetLegalizesMatrixTypes` (extracted, shared with shouldLowerMatrixType as single source of truth) + Tier-2 read-only presence scan mirroring exact lowering conditions, bails when no lowerable matrix. = pdeayton's early-linear-scan (Approach B). **Determination: safe-by-design (scan observes exact current module state at pass entry → stale-false-negative structurally absent); NOT an A-safe/B-C flag classification — it IS the scan mechanism.** revert-drill 6/6 byte-identical (present/absent × spirv-asm/glsl/hlsl); matrix suite 10/10 + 2 new guards 4/4; codex PLAN+CODE+OUTPUT approve. CI red = benign draft priority-yield. Draft-held, awaiting review. Worklist-reduction (pdeayton approach #1) offered as optional follow-up, not built.

**REVIEW VERDICTS 2026-07-08 (jkwak, triager-verified live):**
- **#11961 (TU gate) → LGTM, merge held for Yong.** jkwak review 4649891899 (COMMENTED): "It looks good to me. But I like to discuss with Yong before merging it." Per his inline asks fixer REMOVED the 2 SIMPLE(filecheck) emission-guard tests (runtime COMPARE_COMPUTE test covers dispatch behaviorally) — head now `bd1ccc08d1`, PR carries only tagged-union-lowering-runtime.slang. Non-draft/REVIEW_REQUIRED. Safety hold = positive LGTM + Yong-review gate, NOT a doubt. Bot holds, no ready-flip/merge. macos CI flake ("slang-test failed after 3 attempts" exit255 — harness startup flake, same red on recent master, TU tests passed) reran.
- **#11987 (matrix early-out) → jkwak "I don't think this PR is useful"** (review 4649853542): (1) target-family check true for almost all targets; (2) hasAnyMatrixToLegalize() walks the full inst tree like the pass itself → no-match saves only marginal mutation overhead, NOT a real walk. **Fixer CONCEDED, did not defend** (correct). Genuine salvage offered: IRMatrixType is hoistable → a SHALLOW global-insts-only scan (O(#globals)) would actually skip the walk. Fixer DEFERRED to jkwak/pdeayton: push shallow version or let it close. PR OPEN/draft, no unsafe change shipped. **Main's earlier "clean win / safe-by-design" framing was right on SAFETY but too optimistic on VALUE — the early-out as first written doesn't pull its weight; corrected to operator.** Do NOT auto-close #11987 — maintainer's keep-vs-close call; if shallow-scan rework is wanted it comes as a webhook → dispatch THROUGH triager.

**WORKTREE GC 2026-07-13 (disk-critical tick) — both #11917 trees reaped, unique work PRESERVED.** Under a 1GB-free ENOSPC-imminent sweep the supervisor dispatched reap on wt-slang-11917-asafe + -p3 framing them as "merged Phase-1 slice." **Fixer corrected the premise** (save-then-remove caught it): neither was disposable. (1) wt-slang-11917-asafe held UNCOMMITTED Phase-2 A-safe work — 3 backend-pass gates (`lowerLValueCast`/`lowerSumVectorMatrixInsts`/`processLateRequireCapabilityInsts`) + lvalue-cast gated+skip test pair, from a maintainer-authorized 13-pass re-dispatch → committed `9ce7f263ab`, pushed to **`origin/wip/reap/fix/issue-11917-asafe`**. (2) wt-slang-11917-p3 tip `ab3bfde176` (legalizeMatrixTypes skip / #11987-adjacent, out-of-scope per triager) was on no remote → snapshotted to **`origin/wip/reap/fix/issue-11917-matrix`**. Both refs `ls-remote`-verified. Resume Phase-2 A-safe from `wip/reap/fix/issue-11917-asafe` when the epic Phase-2 hold releases (remaining: 2 missing test pairs, revert-drill, codex gate, one family-grouped draft PR). Lesson: a GC "merged-slice" framing can be wrong; the mandatory save-then-remove is exactly why nothing was lost.

**#11987 (matrix early-out) REWORKED + READY + WOULD_APPROVE 2026-07-14.** The shallow-global-scan salvage jkwak/pdeayton steered toward is now the shipped form: fixer replaced the full-tree walk with a global-only presence scan (`hasAnyMatrixToLegalize`) + added an invariant assert, addressing jkwak's "not useful" VALUE objection (marginal mutation-overhead → real skipped walk). Bot flipped it **ready_for_review** (webhook `github.pr_ready_for_review`, author nv-slang-bot[bot]). Main routed to **slang-pr-approver** (shadow mode) on canonical thread → verdict **WOULD_APPROVE / CLEAN** @ `25974dfe6cea` (mode `live_late`, policy `v0-shadow-relaxed`, ledger row recorded, NO GitHub write). All 6 eligibility clauses pass; harvest exit 20 → Devin-only tier (production claude-code-action skips bot-authored PRs), Devin head-current = 0 bugs/0 flags/informational. Challenger CLEARED the #11917 stale-FALSE miscompile risk: both early-outs verified safe supersets of the pass's mutation set — (1) every mutation gates on `shouldLowerMatrixType` (always false on non-lowering targets → early-out #1 skips a provable no-op; extracted switch byte-identical refactor); (2) `hasAnyMatrixToLegalize` global-scan blind spot (generic-nested types) exactly matched by the pass's own `addToWorkList` IRGeneric bail, operand-vs-result cast subtlety pinned by new `matrix-legalize-early-out-mixed-cast.slang` guard. Human reviews so far = COMMENTED design feedback, all addressed in later commits (full-tree→global-only scan, invariant assert). **Next = human merge/review verdict join → `record_human_verdict`.** Merge stays maintainer-gated; bot will NOT ready/merge (guardrail). Terminal for Main's side — no reviewer/fixer dispatch (approver-only per webhook scope).

**#11987 MAINTAINER-APPROVED 2026-07-14 16:07:52Z (Main-verified at HEAD).** pdeayton-nv — the maintainer who PROPOSED the shallow-global-scan rework — **APPROVED** #11987; jkwak-work's earlier review stays COMMENTED (skeptical-of-perf-value), so latest word = APPROVE. reviewDecision=APPROVED, non-draft, MERGEABLE, CI rollup SUCCESS @ `25974dfe`. **This is the human verdict AGREEING with the approver's earlier WOULD_APPROVE/CLEAN — a positive agreement signal** (the approver's shadow call was right). `record_human_verdict` stamps at the pr_merged webhook (approver-owned), NOT now (still OPEN/unmerged). Fixer holds correctly (no flip/merge; 2 human-initiated threads left for maintainer per LLM-only-resolve). Awaiting human MERGE → worktree reap. No Main/fixer action; approver-owned for the ledger stamp at merge.

**#11987 COSMETIC RE-VERDICT + SECOND MAINTAINER APPROVAL 2026-07-14 17:17:54Z.** A `synchronize` webhook moved head `25974dfe6cea` → `5b16405c3279`. Main verified the delta itself = exactly 1 commit "Remove verbose function comments per maintainer review", **comment-only (14 `//` deletions, 0 additions, code byte-identical)** in slang-ir-legalize-matrix-types.cpp — deliberate maintainer-requested cleanup ~15h later, NOT churn/force-push loop → warranted re-decision (human merges the new SHA). Re-dispatched approver → **WOULD_APPROVE / CLEAN @ `5b16405c3279`** (new ledger row supersedes the `25974dfe6cea` row; safe-superset stale-FALSE reasoning holds unchanged since logic byte-identical). **jkwak-work APPROVED this exact SHA @ 17:17:54Z** (raced in during the approver's Devin window, caught by OUTPUT_REVIEW gate; approver stamped it via `record_human_verdict` = AGREEMENT). So BOTH maintainers now approve: pdeayton on `25974dfe`, jkwak on `5b16405`. Merge still human-gated; re-record terminal join at pr_merged. Approver process note (already captured as shared learning): `devin-fetch.sh` missing exec bit false-skips Devin — re-invoke via `bash`.

**#11987 MERGED 2026-07-14T21:14:05Z by jkwak-work (merge commit `6d299e4`).** Merged head = approver R2 decision head `5b16405c3279` EXACTLY (zero follow-up commits) → merged code byte-identical to the WOULD_APPROVE'd SHA, no read-vs-shipped drift. Approver stamped `record_human_verdict(APPROVED)` on both ledger rows (R1 `25974dfe`, R2 `5b16405`) = clean agreement both revisions, no false-safe. `Addresses #11917` non-closing → **#11917 stays OPEN** as epic tracker. **EPIC TALLY now: 3 slices MERGED — #11920 (append/consume) + #11961 (tagged-union) + #11987 (matrix early-out scan).** The matrix pass is now the shipped reference for the in-pass-scan mechanism (vs flag-gate) — relevant to the converged rescan-vs-in-pass-scan maintainer design ask for the B/C-risky tail. Chain TERMINAL. Fixer to reap the matrix worktree on next idle (owning-fixer reaps, Main never removes) — [[feedback_always_reap_merged_worktrees]].

**✅ #11987 MERGED & TERMINAL 2026-07-14 21:14:05Z (Main-verified via `gh pr view`).** jkwak-work merged #11987 (merge commit `6d299e4e11`, `state=MERGED`, mergedBy=jkwak-work). The matrix early-out sub-chain (legalizeMatrixTypes: O(1) target-family check + global-scan presence check + maintainer-requested verbose-comment removal) is CLOSED-success. Fixer reaped worktree `wt-slang-11917-matrix` + cleared the active-work sentinel. Durable learning saved (`feedback_function_comment_intent_not_description` — comments carry intention not name-restatement, per jkwak's "remember and apply to other fixes"). **This is one slice of the #11917 epic; the epic itself stays open** — family-(c) #12088 remains APPROVED+green awaiting its own maintainer merge (jkwak→pdeayton handoff), and the Pass-#2-profiled + converged-rescan-design decisions remain the open epic gates below. Only #11987 (matrix) is terminal.

**#11987 COMMENT-CLEANUP + RE-APPROVED AT NEW HEAD 2026-07-14 17:17:54Z (Main-verified independently at HEAD, not relayed).** jkwak-work left 2 inline `@nv-slang-bot` directives to remove 2 verbose function comments (the doc block over `targetLegalizesMatrixTypes()` + the "Invariant:" block in `shouldLowerMatrixType`) and "remember + apply to other fixes." Fixer removed both (comment-only, 14 deletions / 0 logic, clang-format clean), pushed `5b16405c32`, replied on both threads (did NOT resolve — jkwak resolved himself), saved durable learning `feedback_function_comment_intent_not_description.md`. **Verified at HEAD:** `gh pr view` + `pulls/11987/reviews` → reviewDecision=**APPROVED**, latest = jkwak-work APPROVED @17:17:54 on the CURRENT head `5b16405c32`; the earlier pdeayton (16:07:52) + jkwak (17:10:17) reviews on `25974dfe` are both DISMISSED by the comment-cleanup push (expected). MERGEABLE, OPEN, non-draft, `mergeStateStatus=BEHIND` (branch behind base — merge-mechanics only, resolved by maintainer merge/update-branch, NOT a bot push). **Genuinely approved-at-HEAD** (this cycle's verify-not-infer discipline). Terminal = human MERGE (operator/maintainer-gated; bot flips/merges nothing). Fixer holding, reaps worktree on MERGED/CLOSED webhook. `record_human_verdict` stamps at the pr_merged webhook (approver-owned). Chain terminal on Main's side.

**Families (a)+(b)+(c) of the 13-pass batch — draft PR #12088 shipped 2026-07-14 (Main-verified at HEAD).** `fix/issue-11917-asafe-c` (nv-slang-bot, OPEN/**draft**, `Addresses #11917` non-closing, `pr: non-breaking`, `report_pr_created` done). Family (c) = **3 A-safe passes gated** on `RequiredLoweringPassSet` (`lowerLValueCast`, `lowerSumVectorMatrixInsts`, `processLateRequireCapabilityInsts` — trigger IR proven front-end-only or pre-scan-autodiff-only; byte-identical revert-drill green; codex CODE_REVIEW approve). **2 B/C-risky EXCLUDED** (`removeRawDefaultConstructors`, `lowerReinterpretOptional` narrow-tighten — in-window synthesis confirmed in source). Batch tally: **3 shipped A-safe, 8 traced B/C-risky (no-PR, need rescan/design)**, + `legalizeUniformBufferLoad` flagged as the one potentially-promotable candidate the triager is bundling into the maintainer design ask. This is authorized-batch execution (the 13-pass re-dispatch), shipped draft-only — NOT the separate "Pass #2 profiled" gate below. Peer review dispatched to slang-reviewer, determination to slang-triager. Draft-held; merge/ready-flip gated; bot flips nothing. Webhook-driven for reviewer verdict / real CI. Do NOT double-dispatch (triager owns the epic + fixer peer-wire).

**#12088 MERGED 2026-07-15T00:41Z by csyonghe** — merge head = approver decision commit `34f450bea1d4` EXACTLY (single commit, zero follow-up delta). Clean two-maintainer agreement: jkwak-work APPROVED @ review + csyonghe merged unchanged → approver's WOULD_APPROVE/CLEAN held end-to-end, NOT a false-safe. Terminal human verdict APPROVED recorded on the ledger row; approver filed confirmed-safe calibration learning corroborating the producer-vs-governing-scan recipe for the RequiredLoweringPassSet additive-gate family. **EPIC TALLY NOW: genuinely-gateable set FULLY SHIPPED — #11920 + #11961 + #12088 (3 family-c A-safe gates) all MERGED, + #11987 matrix early-out shipped-as-decided.** B/C tail remains PARKED (jkwak rejected rescan; tail's fate tracks #11987 in-pass-scan pattern). Guardrail held: bot never merged/readied — maintainers did both. Reap worktree fix/issue-11917-asafe-c when convenient (owning fixer).

**#12088 APPROVED + flipped ready 2026-07-14 04:45Z (Main-verified at HEAD — NO breach).** jkwak-work APPROVED (reviewDecision=APPROVED) AND flipped #12088 ready-for-review himself at 04:45:40Z (timeline actor=jkwak-work); non-draft, MERGEABLE, `pull_request` CI queued. Maintainer flipped, fixer did NOT (👀'd inline comments + posted summary, correctly did not flip/dispatch-CI on the now-non-draft) — 6th clean instance of the maintainer-approve+flip pattern this session (#12055/#12053/#11984/#12081/#6557-#12068/now #12088). **Approver ledger row recorded 2026-07-14 05:14Z on the ready_for_review webhook: WOULD_APPROVE / CLEAN @ 34f450bea1d4 (Devin-only tier — production review skips bot-authored PRs; 0 bugs/0 flags; challenger traced every producer of the 3 gated trigger opcodes at pinned head, no in-window synthesis → no stale-FALSE hazard; both critique gates approve). Human jkwak APPROVE @ 04:45Z joined as agreement (mode live_late). NO GitHub write (shadow mode). Will re-record on merge/close join.** So the family-(c) 3-A-safe-gates slice is maintainer-accepted; merge is jkwak's to take (operator/maintainer-gated; bot merges nothing). slang-reviewer peer pass still running (moot given maintainer APPROVE). #11987 matrix rerun (infra flake) awaited separately. Next terminal = jkwak merges → reap. The CONVERGED MAINTAINER RESCAN DESIGN ASK (below) remains the real open epic decision — unaffected by this slice merging.

**Pass #2 — PENDING operator authorization.** jkwak signaled this first pass had modest *standalone* perf value (cheap global-insts scan); prioritize the next pass by **profiled compile-time cost**. Do NOT auto-dispatch the fixer — needs a fresh task/operator go-ahead. Fixer's plan persists at `/workspace/agent/reports/slang-11917.md` (fixer's own FS, opaque to Main). Approach A is the validated template. Reopen chain only on substantive human comment on #11917 or operator go-ahead for pass #2. See [[feedback_let_fixer_own_single_session]], [[feedback_drafts_only_guardrail]], [[feedback_github_writes_operator_authorized]] (never auto-close — surface to maintainer).
