---
name: project_12046_modulus_remainder_audit
description: "#12046 modulus/remainder audit — PR #12130 BLOCK (RED_BUG): F3 broke stale Metal filecheck; fixer fixing"
metadata: 
  node_type: memory
  type: project
  originSessionId: b3dee100-6647-4f34-a90c-7f197c687c31
---

shader-slang/slang#12046 — "Scrutinize the modulus/remainder operators and functions and the emitted instructions on different targets." Opened by **skiminki-nv** (maintainer, "Dev Opened"; Type=Performance; labels Metal/GLSL). A maintainer-opened **scrutiny/audit umbrella**, not a single handed-off bug.

**Semantic ground truth (triager-confirmed via source + DeepWiki):** Slang `%`(float)/`fmod()` = truncation remainder, sign follows DIVIDEND = SPIR-V `OpFRem` (both lower to IR `kIROp_FRem`; no `kIROp_FMod` exists). GLSL `mod()` = floor modulus `x-y*floor(x/y)`, sign follows DIVISOR = `OpFMod`. Differ for negatives: `-1.5 % 2.0`=-1.5 (rem) vs `mod(-1.5,2.0)`=0.5.

**Audit surface (file:line):**
- **F1 [REAL CORRECTNESS BUG]** slang-emit-glsl.cpp:2601-2626 — GLSL text emitter maps raw `FRem` → GLSL `mod()` (floor modulus). WRONG for negative operands; TODO in code already acknowledges. Reachable via `float%float` → GLSL text + `-emit-spirv-via-glsl`. SPIR-V *direct* target UNAFFECTED (OpFRem at :859). Stdlib `fmod()` GLSL case (hlsl.meta.slang:11288) is CORRECT (uses sign-flip workaround); bug is only the raw `%`/FRem path that bypasses stdlib. Fix = mirror stdlib sign-flip in emitter. **Static analysis, NOT runtime-reproduced.**
- **F2 [OPTIMALITY, design-gated → maintainer pushback]** glsl.meta.slang:494-533 — triager: stdlib `mod()` on SPIR-V falls to `default: x-y*floor(x/y)` (4 instrs) instead of single `OpFMod`; answered skiminki's "shouldn't SPIR-V emit OpFMod?" lead as YES. **jkwak-work 2026-07-15 (comment 4984943265):** quotes a `case spirv:` returning **`OpFRem`** and says "the Slang function `fmod()` is remainder, so it is correct to use OpFRem." **POSSIBLE mod()-vs-fmod() cross-talk:** jkwak defends `fmod()`(remainder)→OpFRem (correct); triager's F2 was about `mod()`(floor-modulus)→should be OpFMod. Either they're discussing different functions OR dispute about what glsl.meta.slang:503 actually contains. Triager must disambiguate which function jkwak is quoting vs which the issue's L503 lead points at, with receipts. Do NOT act on F2 until resolved.
- **F3 [DISPUTED — maintainer refutes]** hlsl.meta.slang:11292 — triager claimed Metal stdlib `fmod()` sign-flip is needless (Metal `fmod` = trunc-remainder, bit-identical, comment at :11248 "factually wrong"). **jkwak-work (maintainer) REFUTED 2026-07-15 (comment 4984926619):** Metal `fmod` is Modulus (per the :11238-11280 in-tree comment), so emitting plain `fmod()` would be INCORRECT — the sign-flip is load-bearing, comment stands. If jkwak is right, F3 "cleanup" would be a correctness regression AND skiminki's Metal lead answer flips to NO. **UNRESOLVED — triager re-verifying with receipts (Metal spec + actual codegen/exec, not hand-check). Do NOT act on F3 either way until resolved.** Also: jkwak asks all discussion use "Modulus"/"Remainder" terminology, not "fmod" (each language defines fmod differently). **4th comment 4984993088 (2026-07-15): jkwak "not convinced" F3 correction is right, posts screenshot from MSL Spec PDF (https://developer.apple.com/metal/Metal-Shading-Language-Specification.pdf) as authoritative evidence Metal fmod=Modulus. Triager must fetch + read the actual MSL fmod definition, not hand-check. Triager evidently already POSTED the F3 audit to GitHub (jkwak quotes their ":11248 inaccurate" language) → if wrong, needs a clean public recant.**
- **F4 [SCRUTINY]** slang-emit-glsl.cpp:2627 — GLSL int `%`/IRem via default c-like `%`; negative-operand behavior underspecified in older GLSL. Flag, don't fix blind.
- **F5 [DOCS — maintainer dispute on the formula itself]** docs/language-reference/expressions-operators.md:65-68 — skiminki (issue author) proposed adding `a%b == a-trunc(a/b)*b` as the REMAINDER definition. **jkwak-work 2026-07-15 (comment 4984965984):** "The calculation of remainder is NOT `a-trunc(a/b)*b`; this is Modulus" — cites in-tree comment hlsl.meta.slang:11254-55: `Remainder | Modulus` / `x=i*y+f | x-y*floor(x/y)`. **APPARENT CONTRADICTION in jkwak's own citation:** Modulus column uses **floor** (`x-y*floor(x/y)`); skiminki's formula uses **trunc** (`a-trunc(a/b)*b`). trunc(round-to-zero)→remainder(sign of dividend); floor→modulus(sign of divisor). skiminki's `x=i*y+f` with i=trunc(x/y) rearranges to `f=x-trunc(x/y)*y` = his proposed formula = the Remainder column, NOT Modulus. So jkwak may have conflated trunc/floor. **BUT: two maintainers (skiminki author vs jkwak) now disagree on a semantic def — do NOT adjudicate for them. Triager verifies ground truth with a 4-quadrant truth table + spec, states it precisely; if skiminki/jkwak genuinely disagree, SURFACE it, don't pick a winner.**

**RE-VERIFICATION RESULT (2026-07-15, comment 4985129490):** jkwak posted 4 comments disputing F3/F2/F5. Triager re-verified vs PRIMARY SOURCES (not hand-check) and posted ONE consolidated neutral reply. Root of all 4 disputes: Remainder/Modulus discriminator is **trunc vs floor** — `a−b·trunc(a/b)`=Rem (sign of dividend); `a−b·floor(a/b)`=Mod (sign of divisor). jkwak was reading the trunc-formula as Modulus; it's Remainder. Receipts: **MSL Spec §6.6 Table 6.4 p207** `fmod(x,y)="x – y * trunc(x/y)"` (p208 trunc=round-to-zero) → Metal fmod = **Remainder**. **SPIR-V core spec:** OpFMod sign=Operand 2 (divisor→Mod); OpFRem sign=Operand 1 (dividend→Rem). Verdicts: **F3 DEFENDED** (spec-vindicated; Metal fmod IS Remainder; sign-flip wrapper redundant; :11248 "Modulus" label wrong — NO recant needed). **F2 = talking past each other** (jkwak defends `fmod()`→OpFRem, correct+undisputed; triager's F2 was the SEPARATE `mod()` floor-fn needing OpFMod — disambiguated). **F5 neutral** (skiminki's `a%b==a−trunc(a/b)*b` IS a correct Remainder closed-form; posted truth table as ground truth, left wording to skiminki/jkwak — did NOT adjudicate maintainer-vs-maintainer). **F1 untouched** (bug stands). Method note: WebFetch 403'd by Khronos + rejected 14MB MSL PDF → used curl+pypdf. **jkwak CONVERGED 2026-07-15 (comment 4985193294): "I was wrong ... trunc is Remainder and floor is Modulus."** Dispute fully settled in triager's favor — F3 spec-vindicated, F2 cross-talk resolved, F5 formula confirmed correct Remainder closed-form (jkwak withdrew objection). All 4 disputes closed. BUT: jkwak conceding SEMANTICS ≠ skiminki authorizing a FIX dispatch. Still skiminki's maintainer-opened scrutiny umbrella → routing STAYS HELD for explicit maintainer go on the F1+F5 slice (re-open ≠ release discipline). Triager closes the dispute loop; findings now uncontested. **THEN jkwak comment 4985202385: "So the Metal emit is currently incorrect!!!"** — NEW claim, CONTRADICTS triager's F3 (which said Metal emit is correct-but-redundant: sign-flip wrapper bit-identical to plain fmod in all 4 quadrants). jkwak just over-corrected in the new direction. Forwarded to triager to verify WHICH Metal path he means (stdlib fmod wrapper hlsl.meta.slang:11292 vs raw FRem→Metal slang-emit-metal.cpp:796 — BOTH judged correct by triager) against code+truth-table. Triager must NOT reflexively concede to maintainer; F3 says Metal emit is CORRECT. Only real incorrectness on Metal is if audit missed a 3rd path OR jkwak conflates F1 (GLSL bug, real) with Metal. Verify, then re-ground or own. **RESOLVED (comment 4985238732): triager re-verified ALL THREE Metal paths at HEAD, did NOT concede — Metal emit is CORRECT.** (1) raw FRem→Metal slang-emit-metal.cpp:806 = plain fmod ✔; (2) matrix FRem→`_slang_matrixFmod` slang-emit-metal-prelude.cpp:47 = per-component fmod ✔ [3rd path, not in orig audit]; (3) stdlib fmod wrapper hlsl.meta.slang:11292 = sign-flip that is algebraic no-op ✔ (`fmod(x,y)=fmod(x,|y|)` and `-fmod(-x,|y|)=fmod(x,|y|)`), all 4 quadrants incl signed-zero. **Why jkwak over-corrected:** wrapper was written ASSUMING Metal fmod=Modulus (the wrong :11248 comment) to convert Mod→Rem; since Metal fmod is actually Rem, it's a Rem→Rem no-op — correct EITHER way you read semantics. The ONE genuine bug is the `:11248` COMMENT LABEL (comment bug, not emit bug); the real EMIT correctness bug remains **F1 on GLSL** (`-7%3`=2 wrong-Mod vs -1 correct-Rem). No 3rd broken Metal path exists. Chain fully diagnosed + uncontested; routing HELD for maintainer go on F1+F5.

**FIXED 2026-07-16: DRAFT PR #12130** (https://github.com/shader-slang/slang/pull/12130, `Fixes #12046`, base master, head `fix/issue-12046`, `closingIssuesReferences=[12046]`, isDraft:true). 8 files: glsl emitter + both meta files + docs + 2 updated intrinsic tests + 2 NEW tests. All 5 items landed. **F1 construction: fixer used `sign(x)*mod(abs(x),abs(y))` (branchless/component-wise, VALID for vector `%` where a bool-vector ternary would be invalid GLSL) — triager verified equivalent+correct, BETTER than specced ternary. Minor non-blocking nit: 2nd spelling of GLSL remainder vs stdlib fmod() ternary at hlsl.meta.slang:11288 — reviewer might raise, not correctness.** Tests (both GPU-free): F1 `tests/glsl/frem-glsl-sign.slang` (`//TEST:SIMPLE -target glsl`, scalar+vector, FileCheck `sign(...)*mod(abs(...),abs(...))` — closes #3470 blind spot); F2 `tests/spirv/mod-spirv-opfmod.slang` (FileCheck `OpFMod` +`-NOT OpFloor`, all 3 mod() overloads). Issue verdict refreshed GitHub cmt 4986682369 ("fix in draft PR #12130, held pending review"). Peer review in flight (fixer session owns; reviewer-reply + real-CI-after-priority-yield webhooks route to FIXER not triager → evidence report_pr_created fired). **report_pr_created CONFIRMED FIRED** (fixer explicit yes: called at draft-open, returned "PR shader-slang/slang#12130 mapped to this session"; corroborated — a github.ci_failed webhook for #12130 already routed to fixer session, classified benign draft priority-yield). All Main verification closed. **2026-07-16 00:45: #12130 APPROVED by jkwak-work + flipped READY-FOR-REVIEW; approval commit == HEAD; awaiting maintainer merge.** (Approval = the sign-off the draft-guardrail exists to require; verifying WHO flipped ready — maintainer-driven vs fixer self-flip — for guardrail closure; jkwak approval makes it moot either way but capture the lesson if fixer self-flipped.) Non-draft PR carries own `Closes #12046` trail → no verdict-refresh churn now. Webhooks (reviewer-reply, real-CI) route to FIXER session. **Close-out trigger: #12130 MERGES → triager refreshes verdict to "fixed, merged in #12130" + final [Triage Resolution]. Re-open on fresh substantive human comment. Silent hold.**

**2026-07-16 ~01:19 APPROVER BLOCK (RED_BUG) on #12130 @head `a891de261b27`:** ready_for_review webhook → slang-pr-approver → verdict **BLOCK**, ledger row written (both critique gates approved), shadow-only (no GitHub writes). The three fixes are each individually CORRECT (F1 `sign(x)*mod(abs(x),abs(y))` verified across all 36 sign quadrants; F2 OpFMod wired only to floor-mod `mod()`, FRem stays OpFRem; F3 Metal ternary confirmed no-op) and scope matched. **BUT PR incomplete + CI RED:** F3 changed Metal `fmod` emission from a two-`fmod(`-token ternary to a single token and did NOT update a pre-existing/untouched Metal filecheck test still asserting `METAL-COUNT-2: fmod(`. **Three `test-slang` jobs FAILED at pinned head — `tests/metal/math-vector.slang:189` on all three runners.** Near-false-safe: Devin reported 0 bugs (doesn't run tests); all 6 eligibility clauses passed because `ci_green_on_sha` reads combined-status API (blind to check-runs, returned success) + policy `require_ci_green=false`; human APPROVE landed ~8min before test jobs finished. Only Step-3 challenger's direct check-run inspection caught it (#12122 class). **DISPATCHED slang-fixer to fix stale Metal filecheck (`tests/metal/math-vector.slang:189`, update `METAL-COUNT-2: fmod(`→match new single-token emission) + push. Fixer owns #12130 session (report_pr_created fired).**

**2026-07-16 02:07 FIX PUSHED — commit `200d6f5f39` on `fix/issue-12046` (non-draft head).** Test-only, +3/−3, emitter logic untouched. Fixer verified actual codegen first (NOT blind-edit): slangc→Metal emits exactly 1 `fmod(` scoped to `Test_Vector_0`, 1 in `Test_Scalar_0`. Fixed the RED job AND two latent siblings (all share F3's 2→1 fmod-token root cause): (1) `tests/metal/math-vector.slang:189` `METAL-COUNT-2: fmod(`→`METAL: fmod(` [the RED job, scoped via METAL-LABEL]; (2) `tests/metal/math-scalar.slang:181` `METAL-COUNT-2`→`METAL` [latent, unscoped — codex-critique must-fix, fixer's first pass wrongly passed it]; (3) `tests/metal/math-scalar.slang:182` `METALLIB-COUNT-2: fmod.f32`→`METALLIB: fmod.f32` [latent, would've failed macOS metallib job]. Local `slang-test` 100% (18/18, 4 ignored) w/ negative-control proving FileCheck runs. codex-critique PLAN(must-fix addressed)+CODE(approve)+OUTPUT(approve). **jkwak's prior approval auto-dismissed by push — `reviewDecision: REVIEW_REQUIRED` now.** CI auto-triggered on `200d6f5f39`, PENDING. Two `synchronize` webhooks arrived (churn) — **approver dispatch DEBOUNCED (not re-run on churn; round-1 BLOCK happened 8min before tests finished).** **Close-out trigger REVISED: fixer reports GREEN CI on `200d6f5f39` → dispatch slang-pr-approver ONCE on settled head for clean terminal decision → on WOULD_APPROVE, human re-approves (needed post-dismissal) → merge.** Fixer monitoring CI + will post brief PR note when green.

**MAINTAINER GO 2026-07-15 (jkwak comment 4985255788): "Please make a PR that addresses three points we discussed; I agree to them all."** — (1) "fix the incorrect Metal emit"; (2) "emit OpFMod for spirv target when mod from glsl is called"; (3) change comment table hlsl.meta.slang:11254 `x= i*y +f` → `x -y*trunc(x/y)`. **ROUTING RELEASED to slang-fixer VIA triager (triager owns fixer edge).** SCOPE RECONCILIATION (critical): jkwak's point 1 says "Metal" but Metal emit is VERIFIED CORRECT — point 1 = F3 (simplify redundant wrapper to plain fmod, OUTPUT-UNCHANGED, NOT a correctness fix). Point 2 = F2 (add spirv case→OpFMod to `mod()`). Point 3 = comment-table fix. jkwak's 3 points OMIT F1 (the genuine GLSL FRem→mod() correctness bug — the ACTUAL "incorrect emit") + F5 (docs closed-form, skiminki's ask). Triager instructed to post ONE scope-confirm to jkwak (map 3 points + ask to include F1+F5 in same PR) BEFORE dispatching fixer. Draft-only PR (drafts guardrail); regression tests for F1(GLSL neg-operand FileCheck)+F2(OpFMod FileCheck); fixer calls report_pr_created.

**FULL SCOPE CONVERGED 2026-07-15 (jkwak comment 4985309632): "I see what you meant. Let's address five of them together."** — jkwak accepted triager's F1-vs-#3470 reconciliation (F1 = raw `%`/FRem→GLSL emitter path, NOT covered by his #3470 which fixed the stdlib fmod() path; so F1 is a separate uncovered gap, NOT a regression of his fix). All FIVE findings authorized in ONE PR: **F1** (GLSL raw-% FRem→mod() correctness, slang-emit-glsl.cpp:2601), **F2** (OpFMod for mod()→spirv), **F3** (Metal wrapper simplify, output-unchanged), **F5** (docs closed-form), **comment-table fix** (hlsl.meta.slang:11254 `x=i*y+f`→`x-y*trunc(x/y)`). FIXER DISPATCHED via triager on canonical thread. Requirements: DRAFT-only (guardrail), regression tests for F1 (neg-operand GLSL FileCheck) + F2 (OpFMod FileCheck), fixer calls report_pr_created. Triager's F1 #3470-diff finding + HEAD repro is the fixer's spec. **F1 RECEIPTS (verify eventual PR against these):** PR #3470 (jkwak's #1059 fix, MERGED 2024-01-24, SHA 4d21790766) changed ONLY slang-emit-spirv.cpp (kIROp_FRem: SpvOpFMod→SpvOpFRem) + tests/compute/frem.slang — fixed SPIR-V DIRECT emitter (intact at HEAD, `a%b`→OpFRem ✔). #3470 NEVER touched slang-emit-glsl.cpp. HEAD repro `slangc frem.slang -target glsl`: `a%b`→`mod(a,b)` (floor-Mod); `-7%3`→2 must be -1; TODO still present → **F1 LIVE**. Gap survived because frem.slang tests default+cpu+vk-spirv-direct, NEVER GLSL text target — so F1's regression test MUST add a GLSL-text-target case. **STATUS: fixer building draft PR; awaiting [Fix Report]+PR#. On PR: verify report_pr_created fired + draft state + F1 regression test targets GLSL-text.** F1 CONSTRUCTION NOTE: fixer chose `sign(x)*mod(abs(x),abs(y))` (NOT the audit's stdlib sign-flip `((x<0)?-mod(-x,abs(y)):mod(x,abs(y)))`) — triager verified correct+equivalent, fixer's prerogative. Long build (dispatched ~9:13, still building 11:20 — multi-path GLSL+SPIR-V+Metal change w/ 2 regression tests + SPIR-V validation; fixer actively progressing, updates landing regularly, NOT stalled).

**Routing verdict (prior): PARKED at triaged, HOLD fixer routing for skiminki/maintainer.** ← now RELEASED, all 5 findings, one PR. Per established pattern for maintainer self-filed scrutiny/design issues (#12042/#12040/#12038/#12035 all parked). NO auto-dispatch. Approach A (F1+F5) is the one unambiguous non-design slice, fixer-ready **on an explicit go**; F2/F4 are maintainer calls. Triager owns posting the audit as a GitHub comment (hedged: static, not reproduced) + offering slice A back to skiminki.

**Dup history:** #1059 (CLOSED via #3470, established current semantics), #5026 (CLOSED), #10071 (OPEN, autodiff fmod grad — distinct). No open dup of this umbrella. Related to [[user_interests]] N/A. See sibling parked maintainer scrutiny: [[project_12035_overload_diag_reasons]], [[project_12023_compileperf_sweep_abstain_policy]].
