---
name: project-9146-glslang-stdlib-reexport-lto
description: "slang#9146 stdlib re-export in libslang-glslang — SHIPPED as PR #12379 (version script). Our published LTO root cause was REFUTED and publicly retracted; spawned #12380, cites #12355."
metadata: 
  node_type: memory
  type: project
  originSessionId: b25f6e67-6a1c-4749-83cf-0a1af0f72a96
---

shader-slang/slang#9146 — C++ stdlib symbols re-exported in release packages of libslang-glslang.

**State:** TRIAGED + verdict posted (issue comment 5011400207), PARKED — not forwarded to slang-fixer. Maintainer jkwak-work requested triage only (webhook @nv-slang-bot 2026-07-18); assigned to jkwak-work. Reporter NBickford-NV (contributor).

**Classification:** bug (regression) / low severity / build-system·release-CI / P3.

**Root cause (HIGH-confidence hypothesis, config-evidenced, NOT empirically reproduced):** Release CI enables LTO (`release.yml:156` `-DSLANG_ENABLE_RELEASE_LTO=ON`, added ~2025-06 commit 7f04adbfb — matches leak reappearance). Local `cmake --workflow --preset release` is LTO-OFF (default `CMakeLists.txt:371`) → clean. `-Wl,--exclude-libs,ALL` localizes by originating static archive; under LTO the archive boundary dissolves so glslang's `std::` template instantiations escape localization and leak with default visibility. The #7722/#8089 symbol-hiding fix is present & un-reverted — *defeated* by LTO, not removed. Reporter's clean local build shares same OS/GCC 13.3/binutils 2.42 → only verified delta is LTO.

**Recommended fix (bot-PR-able, in-tree):** Approach A = explicit linker version script on slang-glslang (`global: glslang_*; local: *;`), LTO-immune, minimal. Alts: B `-fvisibility=hidden` on glslang compilation; C exclude MODULE from IPO (`SlangTarget.cmake:174`). Choice among 3 is a build-config design call left to maintainer.

**GO GIVEN 2026-08-06** — jkwak-work (maintainer, webhook @nv-slang-bot, issue comment 5199055145): *"can you make a PR so that I can understand what you are describing? Without it, I am not sure what you are suggesting."* ⇒ the "await maintainer decision" gate is CLEARED. Dispatched slang-fixer on `gh-issue-shader-slang/slang-9146` for the Approach A version-script PR.

**Verified @49584a089 (2026-08-06) before dispatch — all four triage facts still hold:** `source/slang-glslang/CMakeLists.txt:19-39` still has `CXX_VISIBILITY_PRESET hidden` + `-Wl,--exclude-libs,ALL`; `release.yml:156` still `-DSLANG_ENABLE_RELEASE_LTO=ON`; `CMakeLists.txt:393` still defaults it OFF (was cited as `:371` in the posted triage — **line moved to 393**, same option); `cmake/SlangTarget.cmake:174-181` still sets `INTERPROCEDURAL_OPTIMIZATION_RELEASE/RELWITHDEBINFO`. **New fact that makes Approach A safe:** every exported symbol in `slang-glslang.cpp` is `extern "C"` + `visibility("default")` and every one is named `glslang_*` — `glslang_validateSPIRV`, `glslang_disassembleSPIRV`, `glslang_disassembleSPIRVWithResult`, `glslang_freeDisassembly`, `glslang_compile`, `glslang_compile_1_1/_1_2/_1_3`, `glslang_linkSPIRV` (9 total, lines 166/188/235/248/998/1037/1065/1080/1095). So `global: glslang_*; local: *;` covers the whole public ABI with no exceptions — no `slang_*` or C++-mangled export to enumerate.

**Next action:** slang-fixer owns the PR + the GitHub ack (closest-to-the-state). Fix must be empirically demonstrated, not just asserted: build with `-DSLANG_ENABLE_RELEASE_LTO=ON`, `nm -DC --defined-only` before/after. If the LTO hypothesis fails to reproduce, the fixer reports back rather than shipping a speculative version script.

## ⛔ 2026-08-06 — OUR PUBLISHED TRIAGE MECHANISM IS WRONG. Retraction owed on the issue.

slang-fixer could NOT reproduce: LTO ON, `nm` → 9 exports, 0 `std::` (positive control passed). **Bug is live though** — today's shipped `v2026.14.1` has 13 exports = 9 `glslang_*` + 4 leaked `std::__cxx11::basic_string::_M_{replace,assign,create,mutate}`. Leak set DRIFTS between releases (reporter saw 5 incl. `vector<uint>::_M_default_append` on v2025.23.1). Same package: `libslang-llvm.so` 26/31 `std::`, `libslang.so` 3/636, `libgfx.so` 1/14 — so this is NOT glslang-specific.

⭐⭐⭐ **The specific false claim we published (comment 5011400207): "LTO dissolves the archive boundary so glslang's `std::` instantiations escape `--exclude-libs`." Measured false — in our configure LTO never reaches the leaking code at all.** IPO is applied only by `slang_add_target`; glslang/SPIRV-Tools arrive via `add_subdirectory` (`external/CMakeLists.txt:319,336`), so: `-flto` in glslang compile cmds **0/256**, SPIRV-Tools-opt **0/207**, slang-glslang **2/263**. Object level: `libglslang.a` members have **0** `.gnu.lto` sections (43 total = control); our `slang-glslang.cpp.o` has 138/155. **Archives are ordinary objects ⇒ `--exclude-libs` localizes them fine.** The triage comment even called the LTO delta "the clincher" — that word was doing work no measurement supported.

⚠️ **Fixer's near-miss worth keeping: "`.localalias` proves LTO" is a DUD discriminator — present in both (95 mine / 97 released).** What discriminates is *which* symbols carry it: `.localalias` on `std::…basic_string::_M_*` = 0 mine / 3 released; leaked `GLOBAL DEFAULT` `std::…basic_string` = 0 mine / 8 released.

**What IS validated (arms share identical objects/archives, only the link flag varies):** arm1 `--exclude-libs,ALL` → 9 exports/0 `std::` · arm2 neither flag (leak instrument) → **6489/1371** incl. the issue's `vector<uint>::_M_default_append` · arm3 `--version-script` only → **9/0, all nine `glslang_*` still `T`**.
⛔ **LOGICAL GAP — do not overstate this A/B.** It shows the version script is *equivalent* to `--exclude-libs` in an env where `--exclude-libs` already works, and that both beat nothing. It does **NOT** show the version script works *where `--exclude-libs` fails* — the failing env was never reproduced. Provenance-independence is an argument from linker semantics (sound), not an experimental result here.

**Alternative B is dead, with a citation:** `libstdc++` declares `namespace std _GLIBCXX_VISIBILITY(default)` (`/usr/include/c++/12/bits/basic_string.h:53`); an explicit visibility attribute beats the `-fvisibility=hidden` default, so hiding glslang's compilation cannot hide `std::` instantiations. C (exclude MODULE from IPO) needs a new `slang_add_target` keyword, trades optimization for a visibility bug, and fixes none of the other leaking libs.

**Environment axes (I verified the runner myself, `release.yml:24`): CI = `ubuntu-22.04`.** Compiler axis per `.comment`: released .so GCC **11.4.0**, container GCC **12.2.0**. ⭐⭐ **But the LINKER axis is uncontrolled and is the better suspect — `--exclude-libs` is a linker feature, not a compiler one.** Container `ld` = **binutils 2.40**; original triage recorded reporter (clean) at **2.42**; ubuntu-22.04 ships 2.38 (⚠️ needs verifying, not measured). Monotone story fits: 2.38 leaks, 2.40/2.42 clean. ⇒ **Installing gcc-11 would NOT move binutils, so the proposed "close the loop with gcc-11" experiment is non-decisive as specified** — rejected on those grounds, not on cost.

## ✅ 2026-08-06 — PR #12379 OPEN, retraction posted. Chain now on slang-reviewer + jkwak-work.

**Verified myself, not relayed:** PR #12379 `state=OPEN isDraft=false`, head `20a71c6cb3c192cd9e6cbebe76bf9b1fb9c0ff95`, label `pr: non-breaking`, **2 files / +36 / −0**, title *"Stop re-exporting libstdc++ symbols from the slang-glslang module"*, `Fixes #9146` present in body. Retraction = issue comment **5199754014** (`nv-slang-bot[bot]`, 2026-08-06T02:37:44Z, 5,947 chars) — it explicitly quotes the old "clincher" line and says *"I no longer stand behind that mechanism."* Diff = new `source/slang-glslang/slang-glslang.map` (9 names, `local: *`) + `CMakeLists.txt` guarded `if(NOT WIN32 AND NOT APPLE)` with `LINK_DEPENDS` on the map. `--exclude-libs` kept as defense-in-depth. **CI at check time: 15 SUCCESS / 1 skipped / 10 pending, 0 failures** (incl. `check-formatting`, `check-pr-label`, linux+macos release build/test).

⭐⭐ **Hypothesis went from "unreproduced" to REFUTED — a strictly stronger result, and the stop-condition is what bought it.** Fixer forced glslang's objects to genuine bytecode (`-flto=auto -fno-fat-lto-objects` + `gcc-ar`; a member 138 `.gnu.lto` sections of 148 vs 0 of 43 stock, still defining the leaking symbol) and relinked with `--exclude-libs,ALL` → **9 exports / 0 `std::`**. GNU ld's LTO plugin still attributes plugin-provided symbols to their originating archive. Scoped: GCC 12.2 / binutils 2.40 only. ⇒ **The "LTO dissolves archive provenance" story is dead in both directions, not merely unconfirmed.** Release trigger remains unidentified; linker axis still unmeasured on the CI runner.

⛔ **MY OWN ERROR — see [[feedback_a_wildcard_export_claim_needs_the_link_not_the_file]].** My dispatch asserted `global: glslang_*` had "no exceptions to enumerate." Upstream glslang defines **41** of its own `glslang_*` symbols; wildcard → 50 exports/41 upstream vs explicit nine → 9/0. The wildcard would have left correctness resting on `--exclude-libs`, i.e. the failing mechanism. Caught by the critique gate, not by me. Also missing from my dispatch AND the original triage memo: a **second loader**, `GlslangModule::init` (`tools/gfx/vulkan/glslang-module.cpp:54-56`, `dlsym`s `glslang_linkSPIRV`). I verified independently that the two loaders look up exactly 9 distinct names (`slang-glslang-compiler.cpp:91-102` + gfx subset), all present in the map — interface complete, but by luck of the subset.

⚠️ **Known gap, stated in both artifacts, do NOT let a green CI imply otherwise: NO regression test.** Leak isn't locally reproducible ⇒ no red→green; the `.slang` harness cannot assert on a symbol table. Efficacy in the *failing* env is an argument from linker semantics, not a measurement. **Definitive confirmation = `nm -DC --defined-only` on the next official Linux release artifact.** Fixer put the "add a binary-export test facility?" question to the reviewer rather than deciding silently — right call.

**FINAL STATE 2026-08-06 03:19 — chain handed off, no action left with me.** Head unchanged `20a71c6cb3`, updated 03:18:52Z. **CI: 34 SUCCESS / 1 skipped / 12 pending / 0 FAILURES.** PR body now carries a corrected coverage caveat I verified verbatim (`body:288-300`): *"Green CI on this PR is not release verification… `-DSLANG_ENABLE_RELEASE_LTO=ON` appears in only 2 of the 62 YAML workflow definitions… none of the build workflows `ci.yml` invokes… ever set it. Note that is a statement about configuration coverage, not a claim that LTO is the trigger."* ⭐ **I re-derived every figure in that sentence myself: 62 workflow YAMLs, LTO token in exactly `release.yml:156` + `nightly-mdl-perf-test.yml:133`, `ci.yml` = 30 `uses: ./.github/workflows/...` dispatch calls and `grep -c SLANG_ ci.yml` = 0.** All correct as published. Reviewer + jkwak-work own it from here; fixer's CI monitor is armed (macOS/Windows matter most — the change is guarded off on both via `if(NOT WIN32 AND NOT APPLE)`).

⛔ **7th error of the chain, and the most instructive — see [[feedback_a_retractions_own_prose_regenerates_the_retracted_claim]]. The fixer's first draft of the retraction paragraph called the release build "the configuration that leaks", reasserting the causal claim that same paragraph existed to withdraw.** Gate caught it. Also caught: a grep-zero for the LTO flag in `ci.yml` whose own control (0 `SLANG_` tokens in the whole file) showed the file is a pure dispatcher and the zero was structural, not evidential. ⇒ **corrections and controls are code paths too, and they get audited less than the claims they police.**

## ✅✅ 2026-08-06 04:00 — FINAL. Reviewer APPROVE_WITH_NITS / 0 bugs. Nits applied, rebased, CI clean-so-far. Chain closed on my side.

**Verified myself at 04:00:** head **`2876c3a7b2c9cbc9d3d90eb17fcd388870764040`** (was `20a71c6cb3`), `OPEN`, non-draft, `pr: non-breaking`, **3 files / +48 / −1**: `CMakeLists.txt`, `slang-glslang.map`, **plus `slang-glslang.cpp`** (comment-only — a pointer at the exports telling future editors to update the map, *"Some call sites do not null-check"*). Base `9cd92bb3a` = current `origin/master` tip, confirmed ancestor ⇒ genuinely rebased, not stale-based. **CI on the new head: 12 SUCCESS / 1 skipped / 13 pending / 0 FAILURES** — the earlier `test-falcor` + rhi failures were on the superseded sha and are gone.

## ✅✅✅ 2026-08-06 05:11 — CI SETTLED GREEN. CHAIN COMPLETE ON MY SIDE. Awaiting only jkwak + the next release artifact.

**Verified myself:** head `2876c3a7b2`, OPEN, non-draft, `MERGEABLE`, `reviewDecision=REVIEW_REQUIRED`. **CI: 47 SUCCESS / 6 SKIPPED / 0 FAILURES / 0 pending.** (Fixer reported 4 skips, I count 6.) ⛔ **I EXPLAINED THAT DELTA WRONG — see [[feedback_gh_pr_checks_dedups_runs_rollup_does_not]].** I published that the cause was `grep -i fail` matching the substring in job name `retry-on-gpu-failure`. **The fixer never grepped** — they parsed the JSON `bucket` field. **Real cause: `gh pr checks` DE-DUPLICATES repeated runs of one check name; `statusCheckRollup` returns all of them.** Verified: `gh pr checks` = 51 entries (47 pass / 4 skipping), rollup = 56 (`board-sync / board-sync` **5×**, `Claude Code Assistant` 2×, `bridge` 2×, `reuse-compliance-check` 2×) — the two duplicated skips ARE the 4-vs-6 delta. ⇒ My *rule* ("read the structured field, don't grep names") was right and is **better** founded by the real cause: dedup can collapse a **red** run into a green one of the same name, which is worse than a substring false-positive. ⭐⭐⭐ **But being right about the remedy is not evidence of being right about the cause — a misdiagnosis OF an instrument reads exactly like a diagnosis BY one. Before explaining someone else's number, ask what command THEY ran; do not infer it from what would produce that number.** ⚠️ Extra trap I found while checking: rollup mixes node types — 53 `CheckRun` (have `conclusion`) + 3 `StatusContext` (have `state`; here `CodeRabbit`, `SlangPy Tests`, `license/cla`, all SUCCESS) ⇒ a `group_by(.conclusion)` shows 3 `null` that read as "pending". Node-agnostic form: `.conclusion // .state` → **50 SUCCESS / 6 SKIPPED / 0 pending / 0 failures**.

⛔ **SECOND "already exists" instruction of this chain from me — same defect as the #12355 filing ask.** I told the fixer to make sure the `init` reframing "lands on #12355 itself and not only in a PR discussion thread." Verified: **it is already the issue's central claim** (`#12355` body:9-11 — *"`init` hard-fails only when **all four** `glslang_compile*` entry points are missing (`slang-glslang-compiler.cpp:104-108`). Every other entry point is resolved opportunistically via `findFuncByName` and a null result is accepted — that tolerance is intentional…"*). They declined and said why. ⇒ ⭐⭐⭐ **I have now issued the same shape of unchecked directive twice on one chain: "file X" and "make sure X is recorded" both silently assert X's absence, and both times X existed.** The generalized rule is in [[feedback_i_justified_a_decision_with_an_impossibility_i_never_checked]]; the repetition proves reading the rule is not the same as applying it. **Concrete trigger: any imperative containing "file", "make sure … is recorded", "add", "create", "open" ⇒ search first, in the same turn.**

✅ **Line-number "discrepancy" that isn't, worth keeping as a pattern:** #12355 cites `:426/:361/:378/:401`, the PR comment cites `:428/:363/:380/:403`. Both correct **at their own sha** (`ff45b15ed` vs `2876c3a7b2`) — a 2-line drift from intervening commits. ⇒ ⭐⭐ **A line-number mismatch between two documents is a BASE question before it is a correctness question.** I hit the same thing earlier this chain (`CMakeLists.txt:371` → `:393`).

**Fixer's final tally: 10 instances of the one generator (instrument population ≠ claim population), and #10 — "human review" — is the first that ESCAPED their own outputs and propagated upward into my record before either of us caught it.** ⇒ **The error class is not contained by one agent's self-review.** That is the argument for the critique gate being non-optional (5 of 7 earlier catches were the gate) *and* for me verifying relayed phrasing rather than folding it in. Both now recorded.

**Open, and nobody can close it locally:** the release-side trigger. `nm -DC --defined-only` on the next official Linux release package is the only thing that settles whether the leak is gone; the PR body says so explicitly so green CI cannot be misread as release verification. Plus jkwak's answer on the `.map` un-exclude question.

## ⏳ 2026-08-07 00:59 — CI red on `ce08f478e8`: `computeSmokeD3D12` (1 of 11,493). Rerun in flight. **Method here is the model.**

**Failures:** `check-ci` (an **aggregator** — fails because a child did, so NOT independent evidence) + `test-windows-debug-cl-x86_64-gpu / test-slang`, in which exactly **1 test of 11,493** failed — `gfx-unit-test-tool/computeSmokeD3D12.internal`, and it survived the harness's own retry. **Verified at 01:00: that job is now `pending` (rerun attempt 2 in flight), 44 pass / 1 pending / 1 skipping, 0 fail.**

⭐⭐⭐ **THE REUSABLE PART IS THE FIXER'S EPISTEMICS, AND IT SUPERSEDES MY OWN EARLIER MOVE ON THIS SAME PR.** They had three facts that make the diff unable to reach the test (Windows job; whole change inside `if(NOT WIN32 AND NOT APPLE)` with 0 version-script refs outside the guard; test is `runTestImpl(…, DeviceType::D3D12)` at `tools/gfx-unit-test/compute-smoke.cpp:78-80` — **I verified this myself, and additionally that `grep -rn glslang tools/gfx-unit-test/` is EMPTY**, so the D3D12 path never loads the module). **They then refused to stop there:** *"that's a prior, not evidence about this failure — the same reasoning would have 'explained' a real regression whose mechanism I hadn't thought of."* ⇒ ⛔ **A blast-radius argument is a PRIOR, not evidence. It is unfalsifiable by construction: it explains a flake and a real regression identically.** Earlier in this chain I accepted exactly that argument for the Falcor/rhi reds and *added* a flake-history control — which was better, but still not decisive.

⭐⭐ **Their three controls came back INCONCLUSIVE and they reported them as such rather than as support** — this is the behaviour to reward: cross-PR (job passes on #12373/#12363/#12357 ⇒ not broadly broken, but says nothing about this run); intra-branch (only 2 runs, earlier head never reached the job ⇒ **no before/after available**); wider survey (12 runs → 10 pass / 2 fail, but the other fail was a **different test** on an unrelated branch ⇒ **cannot be cited as the same flake**). ✅ I confirmed independently there is **no** tracking issue for `computeSmoke`/gfx-unit-test D3D12 flakiness (`gh search issues` → empty both queries), so no prior art to lean on either.

✅ **The one decisive experiment: `gh run rerun --failed` on the SAME COMMIT.** Identical code ⇒ a pass isolates nondeterminism from the diff. They verified it took effect (`run_attempt=2`) rather than trusting a silent command, and armed a monitor that **names the failing jobs** on a second red so it is actionable. ⇒ ⭐⭐⭐ **For "is this red mine?", the only cheap decisive instrument is a rerun at identical SHA. Everything else is circumstantial.**

✅ **I verified the guard-containment claim MYSELF at the real PR head** (fetched `pull/12379/head`, three-dot diff vs `origin/master`): the `.cpp` change is **comment-only** (3 `//` lines), and in `CMakeLists.txt` every added *functional* line — `set(...version_script)`, `add_supported_cxx_linker_flags(...)`, `set_property(... LINK_DEPENDS ...)` — sits between `if(NOT WIN32 AND NOT APPLE)` and `endif()`; the only other change is a comment edit. ⚠️ **Pedantic precision that does not change the conclusion: "no-op on Windows" is true SEMANTICALLY, not bit-wise** — the comment edit changes the TU's content, so that object is recompiled on Windows. A comment cannot alter codegen, so it is not a mechanism; but the honest form is *"no semantic effect on Windows"*, not *"the Windows binary cannot differ"*.

**Branches:** rerun green ⇒ D3D12 nondeterminism, nothing owed. Rerun red on byte-identical code ⇒ **much more interesting**, investigate `computeSmokeD3D12` directly (note the test does `loadComputeProgram(...)` at `compute-smoke.cpp:15-16` — a real Slang compile, so a shared-lib/loader interaction is not a priori impossible even on Windows; that is the hypothesis to kill first).

⚠️ **Their previous monitor died with no completion record again — 2nd occurrence — so they re-read CI directly instead of inheriting its verdict.** ⇒ **A monitor that can die silently must never be the source of a terminal claim.**

## ⛔⛔ 2026-08-06 23:30 — CI FAILED on the rename; fixed as `ce08f478e8`. And a claim I must NOT relay: "`formatting.sh --check-only` exits 0 with formatters missing" is FALSE ON MY EDGE.

**The failure was real and trivial:** `check-formatting` on `slang-glslang.cpp:24` — the `sed` rename lengthened `slang-glslang.map` → `slang-glslang.version-script`, pushing the comment past 100 columns. CI's exact rewrap applied. Head now `ce08f478e8`.

⛔⛔ **BUT THE ROOT-CAUSE CLAIM DOES NOT REPRODUCE HERE, AND I TESTED IT RATHER THAN RELAYING IT.** Fixer reported *"`formatting.sh --check-only` exits 0 with formatters missing… my green meant C++ was never checked."* On my clone (`master` @ `d7d59f374`, `extras/formatting.sh` **447 lines**, md5 `4578f1f6f00b5267c6f791bf43716e9a`) I ran it with `clang-format`, `gersemi`, `prettier` **all absent** — it printed three `isn't in $PATH` lines and **`EXIT=1`, not 0.** Code path: `require_bin` sets `missing_bin=1` (`:169`), and `:207-209` is `if [ "$missing_bin" ]; then exit 1; fi`. **The fail-closed guard is present and works.**

✅ **RESOLVED 23:36 IN ONE EXCHANGE — cause was a FOURTH option none of us listed, and the shape invariants found it.** Fixer's script is byte-identical to mine (447 lines, same md5) ⇒ hypothesis 1 out; the real cause was their invocation: `./extras/formatting.sh --check-only 2>&1 | tail -12` then `echo "SCRIPT_EXIT=$?"` — **`$?` after a pipe is TAIL's status.** I reproduced four ways: piped → **0**, redirected → **1**, `${PIPESTATUS[0]}` → **1**, `set -o pipefail` → **1**. **The script is fail-closed; the "fail-open" report was an instrument artifact.** They retracted explicitly. ⇒ ⭐⭐⭐ **Asking for two cheap shape invariants beat guessing at mechanism — my hypothesis 2 (the `((run_all || run_cpp))` scope gate at `:203`) was plausible, mechanism-level, and wrong. ANCHOR C's "compare a SHAPE INVARIANT FIRST" earned its place again.** Full lesson incl. *why a rule already in their own index didn't fire*: [[feedback_a_rule_filed_under_its_consequence_never_fires]].

⭐⭐ **What SURVIVES the retraction is the better rule anyway: gate on the per-language proof-of-run line, never on an exit code.** Verified output shape — present tools print `found <tool> <ver>, required [<min>, <max>)`; absent ones print `This script needs <tool>, but it isn't in $PATH`. Green = *N `found` lines / 0 `isn't in $PATH` / 0 diff*. **That rule holds whether or not the script is fail-closed, which is exactly why it beats the disputed one.** The narrow finding stands: the C++ arm genuinely never ran (clang-format absent, stderr said so, bogus exit code masked it) ⇒ CI's >100-column failure.

⛔ **SUPERSEDED (kept for the reasoning trail) — my three hypotheses, none of which was right:**
⇒ ⭐⭐⭐ **So one of these is true and I cannot yet tell which: (a) their worktree's `formatting.sh` differs from master's, (b) the missing tool was one whose `require_bin` call is gated off by their invocation flags (`((run_all || run_cpp)) && require_bin "clang-format"` — a `--check-only` scoped to a non-C++ subset never registers clang-format as required), or (c) the claim is mis-derived from a different run.** ⚠️ **ANCHOR A applies: before telling them they're wrong, `findmnt` the tree — their `/workspace` is a different filesystem, and I have inverted a peer's TRUE report exactly this way before.** ⛔ **My clone is SHALLOW (`is-shallow-repository=true`, 20 commits, oldest `0864e60e6` 2026-08-03) — so `git log -S missing_bin` returning that commit means "first commit IN MY GRAFT", NOT "when the guard was added". I nearly published that as the guard's origin date.** See [[feedback_shallow_clone_makes_your_head_the_graft_root]].

⇒ **ACTION: ask, do not assert. Requested their `wc -l` + `md5sum` of `extras/formatting.sh` and the exact command line, per the shape-invariant-first rule (ANCHOR C).** Their remedy (install CI-pinned clang-format `17.0.6` from the same URL `.github/actions/format-setup/action.yml:19` uses — verified that URL exists and is a pinned `slang-binaries` blob at `306d22efc0f…`) is correct regardless of which explanation holds, and the `[17,18)` gate is real (`:203`), so an apt 18.x would indeed be rejected. **Nothing about the remedy depends on the exit-code claim being true.**

✅ **Their SECOND finding I fully endorse and it extends my own earlier one: `pending == 0` on a fresh push is TRUE AND VACUOUS.** Monitor fired `CI SETTLED clean: {'pass': 2}` a minute after the force-push, when the PR runs ~51 checks. The only two present were `CodeRabbit` and `license/cla` — **exactly the two `StatusContext` nodes, which are the ones that CARRY OVER a head change**, while all 49 workflow `CheckRun`s hadn't registered. ⇒ ⭐⭐⭐ **The same node-type split that makes bare `.conclusion` invent phantom *pending* also makes a fresh head look *settled*. "Nothing pending" ≠ "everything ran".** Fix: plausibility floor (`len >= 20`) before honouring `pending == 0`. **Add this to [[feedback_gh_pr_checks_dedups_runs_rollup_does_not]] as the third distortion.**

## ✅ 2026-08-06 22:58 — RENAMED `.map` → `.version-script` after jkwak's question. Two findings converged.

**jkwak-work, PR review comment `3732284212` (22:51:26Z, verbatim):** *"is `.map` a common file extension for this type of files?\r\nMaybe we should use a different name?"* — on `source/slang-glslang/CMakeLists.txt`.

⭐⭐⭐ **His naming question and my CodeRabbit-coverage finding are THE SAME PROBLEM, and the rename fixes it at the source.** The `!**/*.map` default exclusion exists because web source maps own that extension; choosing `.map` is what got the file skipped by review. Renaming beats asking a maintainer to reconfigure his bot. ⇒ **When a tool ignores your file, the fix may be the filename, not the tool config.** Also likely makes the `.map` un-exclude question moot.

**Verified myself at head `9b8da226523664b5644b4c39bb2e1212d2ec4d62`** (was `2876c3a7b2`): 3 files / +48 / −1, `MERGEABLE`, file is now `source/slang-glslang/slang-glslang.version-script`, and the `.cpp` comment tracks the new name. Three collisions the fixer cited, all checked:
1. **`ld -Map FILE` writes a GENERATED link map** — same tool, same extension, opposite data-flow direction from a hand-authored input script. Strongest of the three.
2. **Repo already treats `*.map` as build output** — `.gitignore:104`. ⚠️ **Precision the fixer's framing overstated: that rule is PATH-SCOPED (`/tests/serialization/*.map`), not a global `*.map` ignore** — I checked, there is no unscoped pattern. So "the repo treats `*.map` as generated" is true as a *convention signal*, not as a rule that would have ignored the new file. Worth stating accurately in the reply.
3. **Web source maps** → the CodeRabbit default.

✅ **No convention existed to follow — verified: `git ls-files | grep -E '\.(map|ver|sym|lds|exports)$'` → EMPTY, and `git grep -l version-script` → EMPTY.** This PR adds the repo's first version script. Saying so beats implying a standard was followed.

⭐⭐ **Rename verified as NON-cosmetic for the right reason: the filename is part of `check_linker_flag`'s cache key.** Probe re-ran under the new key → Success; flag on the real Release link edge; `LINK_DEPENDS` re-wired; rebuild → 9 exports / 0 `std::` / 0 VERDEF; `dlopen` 9/9 with negative control still hidden; `gersemi --check` clean; git recorded a rename (`R`), not delete+add.

⚠️ **Two-dot trap recurred — caught this time.** `git diff --stat origin/master..HEAD` → **33 files / 585 deletions** (master had advanced 4 commits); three-dot → the real **3 files / +48 −1**. ⇒ **The trap fires on ANY PR that outlives a master advance, so it will recur for the life of a long-lived review.** Fixer switched to three-dot by default for anything reported.

**AUTHORIZATION GIVEN** for one reply to jkwak on the naming question. ⚠️ Draft was first cited at `/workspace/agent/wt-slang-9146-jkwak-reply.md` — **not readable from my filesystem** (per-coworker `/workspace/agent`); I authorized on the described contents, not the text. See [[feedback_a_denominator_hunt_silently_asserts_the_numerator]] for the ask-who-can-see-it rule. ✅ **Fixer then `send_file`d it unprompted** → readable at `inbox/a2a-1786057389059-2x1owp/wt-slang-9146-jkwak-reply.md`. **Read the actual words; draft is accurate.** ⇒ ⭐⭐ **"Attach the draft" converted an authorization-on-description into an authorization-on-text at zero cost — make that the default whenever exact wording carries the risk.**

**Point 2 fix landed with a two-way verification I reproduced:** `git check-ignore -v source/slang-glslang/slang-glslang.map` → **no match (rc=1)**; control `tests/serialization/foo.map` → **matches `.gitignore:104` (rc=0)**. Real negative, not a dud query. Published wording now says the rule is path-scoped and *"would not have ignored a file at `source/slang-glslang/`… evidence about how the extension has been used here, not a rule my file was fighting."* Also confirmed point 1 verbatim from the local linker: `ld --help` → `-Map FILE/DIR   Write a linker map to FILE or DIR/<outputname>.map`. Ordering changed to decreasing-by-strength; convergence promoted to the first substantive paragraph.

✅ **RESOLVED 23:14 — the `11,301` was `find external -type f`, i.e. `external/` ALONE, mislabelled in prose as "the whole tree including `external/`".** I reproduced all three counts: `find external -type f` = **11,301**, `git ls-files` = **11,345**, `git ls-tree -r origin/master` = **11,345**. **Number right, population mislabelled** — same generator, Nth instance. Both I and codex independently flagged that it reconciled with nothing. Fixer's fix was the right one: **replaced the prose figure with the three commands and their outputs**, so the control is reproducible rather than asserted. Posted reply **`3732353682`** (23:12:52Z, `nv-slang-bot[bot]`, `source/slang-glslang/CMakeLists.txt:57`).

⚠️ **NEW, from reproducing those published controls myself: a control command needs its REF named, not just its population.** Of the three, `find external …` → 0 ✅ and `git ls-files '*.map' '*.ver' …` → empty ✅ reproduce anywhere, but **`git grep -l -- --version-script` published as "→ only this PR's CMakeLists.txt" returns EMPTY on `master`** — I got nothing, because my clone is master (`d7d59f374`) and the PR head `9b8da2265` isn't fetched. The claim is true *on the PR branch* and false-looking *on master*, and the text doesn't say which. ⇒ ⭐⭐ **For a repo mid-PR, a control's output is ref-dependent; "run this and you'll see X" needs "…at this ref". A reader who reproduces on the default branch gets a contradiction, not a confirmation.**

✅ **`in_reply_to: 3725971356` (CodeRabbit's root) rather than jkwak's `3732284212` is GitHub FLATTENING, not misplacement** — replies carry the *thread root* ID, not the immediate parent. Verified: the reply sits on `source/slang-glslang/CMakeLists.txt` line 57, same thread and location as jkwak's question. ⇒ **Don't read that field as a routing bug.**

⚠️ **Tooling note: the critique gate blocks read-only `gh api repos/.../pulls/...` GETs on URL substring**, so *verify-after-posting* deadlocks against its own gate unless the fetch happened before the next edit. Fixer worked around it by parsing an already-fetched response. Not a defect to chase, but it means post-publication verification must be sequenced ahead of further edits.

⛔ **SUPERSEDED (kept for the lesson): One unresolved control figure in the published reply: it says "the whole tree including `external/` (11,301 files)".** Three counts are now in play and none reconcile cleanly — earlier PR comment cited **11,344** (`git ls-tree -r origin/master`), my `git ls-files` in the worktree gives **11,345** (= master + the new version-script file, consistent), and the reply's **11,301** matches neither. Almost certainly a different instrument/population (submodule contents are not in the superproject's `ls-files`), which is fine — but **the population isn't named, so the control cannot be checked by a reader.** Low stakes: the conclusion (zero version scripts in-tree) I verified independently two ways (`git ls-files` extension grep → empty; `git grep -l version-script` → empty). ⇒ ⭐⭐ **A control count is only a control if its population is stated; "11,301 files" with no named command is a reassurance, not a check.**

## ⚠️ 2026-08-06 04:49 — CODERABBIT IS BLIND TO `*.map` BY DEFAULT. Reusable finding, not PR-specific.

**Verified verbatim from the bot's own processing notice on #12379** (comment 4, `coderabbitai[bot]` 04:47:03Z): *"⛔ Files ignored due to path filters (1) — `source/slang-glslang/slang-glslang.map` is excluded by `!**/*.map`"*, then *"📒 Files selected for processing (2)"*. So it forms a verdict on a 3-file PR having read 2 — omitting the file that **defines the module's public ABI**, where a wrong entry breaks the `dlopen` path *silently* (no build failure; and `m_link` is unguarded at `slang-glslang-compiler.cpp:426`, so it crashes rather than diagnoses).

⭐⭐ **NOT a repo choice — confirmed myself: `.coderabbit.yaml:22-25` filters only `!build/**` and `!external/**`, and `*.map` appears nowhere in the file.** `!**/*.map` is CodeRabbit's **built-in default**, almost certainly meant for JS/CSS **source maps** — a sensible default that collides with linker version scripts sharing the extension. ⇒ **Any repo shipping a linker version script or symbol list gets it unreviewed by the bot, by default.**

⭐⭐⭐ **The reason this stays invisible: the exclusion is ANNOUNCED, in a collapsed `<details>` block, phrased as routine housekeeping.** A disclosure that reads like boilerplate is worse than a silent one — it creates a paper trail that *looks* like transparency while nobody reads it. Companion to [[feedback_i_justified_a_decision_with_an_impossibility_i_never_checked]]: a coverage gap with no failure signature.

**Extra fact I contributed:** `git ls-files | grep '\.map$'` on master → **0** (of **11,344** tracked files). This PR introduces the repo's **first** file of a class the bot ignores, so nobody in the project has had reason to learn about the gap.

⛔ **CORRECTION TO MY OWN NOTE — I first wrote "Human coverage is intact (peer reviewer re-derived…)". FALSE: `slang-reviewer` is an automated coworker, not a human.** I inherited the phrasing from the fixer's report and repeated it here without checking. The fixer caught the same slip in their own draft *before posting* — on the one file no bot read, telling jkwak "a human reviewed it" would have misrepresented precisely the assurance under discussion. ⇒ ⭐⭐⭐ **"Reviewed by a peer" and "reviewed by a human" are not interchangeable in an agent fleet, and the substitution is invisible because our vocabulary for coworkers is borrowed from human org charts.** Accurate statement: the export set was re-derived three ways (map / producer / consumers, all difference sets empty) by an **automated** reviewer, and jkwak has the map in his diff view — a human *can* see it, which is different from a human having verified it. The posted comment gets this right: it says "independently re-derived three ways" and never claims a human did it.

**State re-queried 04:49 (fixer's own CI monitors died without completion records — they re-read rather than inherit, correct):** head `2876c3a7b2`, OPEN, non-draft, `MERGEABLE`, `reviewDecision=REVIEW_REQUIRED`, CI **41 SUCCESS / 0 fail / 6 pending**, `pulls/12379/reviews` = **`[]`** (zero bot reviews; CodeRabbit has posted only ack + processing notice). jkwak-work triggered it himself (`@coderabbitai review`, 04:46:45Z) ⇒ he is actively reading and would otherwise weigh a 2-of-3-file verdict as full coverage.

**MY AUTHORIZATION GIVEN:** one bundled user-facing PR comment when the review lands — answer its findings + disclose the exclusion. Scoped: factual, no speculation about CodeRabbit product decisions, and the un-exclude question posed **to jkwak as a question**, not prescribed. ⭐ Fixer was right not to assume the dispatch's `<github-post-authorized />` (scoped to retraction + PR body) extended to a new comment — **posting authority does not travel by inference.**

⛔ **DO NOT QUOTE "26/31" AS THE LIBSLANG-LLVM SHAPE — I propagated it three times.** A reviewer reconciled it 04:08: `libslang-llvm.so` has 31 exports of which only **4** are intended (`createLLVMBuilder_V3`, `createLLVMDownstreamCompiler_V4`, `createLLVMFileCheck_V1`, `getLLVMTargetBuiltinTypeLayoutInfo_V2`). The 5th non-`std::` export is **`llvm::Use::set(llvm::Value*)`** — a genuine C++ leak with no in-tree reference. **True shape: 4 intended / 27 leaked.** I verified the corrected paragraph live in the PR body (`body:277-283`), and re-ran the fixer's instrument correction myself: `grep -rn "Use::set" source/` returns exactly 1 hit = Slang's own `IRUse::set` at `slang-ir.cpp:180` (**substring match, not the LLVM symbol**), positive control `createLLVMBuilder` → 2 hits, so the grep did reach the tree. ⚠️ Note the PR's summary table still shows the raw `26 / 31` `std::` column with a "see below" pointer — that is deliberate and correct (the column *is* the `std::` count); the shape is corrected in the prose beneath it.

⭐⭐⭐ **8th instance of the session generator, and the most instructive: the NUMBER WAS RIGHT while the SHAPE WAS WRONG.** "26 `std::` of 31 exports" is arithmetically true; what it *implied* — 5 intended entry points — is false. **A total-check or a recount would never have caught this**, because nothing was miscounted; the error was in the residual's interpretation. ⇒ **When you publish `A of B`, state what `B − A` IS, not just its size.** Every leak-vs-interface figure needs its complement enumerated, because the reader's inference lands on the complement.

⭐ **The map's own comment now carries the lesson I got wrong:** *"Listed individually rather than matched as `glslang_*` because glslang's own C API … shares that prefix: 41 such symbols exist in `libglslang.a`."* My dispatch's false "no exceptions to enumerate" claim is now refuted **in-tree, permanently**, where the next editor will see it. Also documents that the version tag is left **anonymous deliberately** (a named tag would add ELF version definitions and change the ABI) and that `local: *` is what does the work.

✅ **Fixer declined one of my instructions, correctly.** I said file the `m_link` unguarded-deref as its own issue. **#12355 already tracks it exactly** — verified: OPEN, label `reproduced`, assignee jkwak-work, title *"GlslangDownstreamCompiler::link dereferences a null m_link when slang-glslang does not export glslang_linkSPIRV"*. Filing a second would have been duplicate noise; they cited it in the PR body instead and said so plainly. ⇒ ⭐⭐ **I issued a "file an issue" instruction without searching for an existing one — the dedup check is the instruction-giver's job, not only the executor's.** Related: [[project_dup_pr_inadequate_existence_check]]. Also filed by them: **#12380** (`slang-glslang: export list is not bounded on macOS (Mach-O needs -exported_symbols_list)`) — the guarded-off platform gap, referenced inline. Reviewer's extra find: the 146 `-emit-spirv-via-glsl` tests never reach `glslang_linkSPIRV`, so test coverage would not have caught a dropped name.

⚠️ **Fixer's last near-miss, and it would have read as a catastrophe: `git diff --stat origin/master..HEAD` reported 9 files / 461 deletions on a 3-file change.** Stale base + **two-dot** range renders upstream's additions as your deletions. `git show --stat HEAD` and **three-dot** both said 3 files / +48 −1. ⇒ ⭐⭐ **Use three-dot (or `git show`) for "what did I change"; two-dot against a moved base is not a diff of your work.** Same instrument-mismatch generator as the rest of the session's errors.

## ⛔ 2026-08-06 03:45 — MY TEST RULING RESTED ON A FALSE PREMISE. Fixer re-opened it; premise verified false; ruling KEPT, reasoning REPLACED.

⭐⭐⭐ **I justified "no test" with an impossibility claim — "any test you could write here would assert the passing state in an environment that already passes" — and that was FALSE.** `release.yml:352` has an existing `File check` step, and I verified by **parsing job boundaries** (not proximity) that jobs in that file are `[workflow_dispatch:4, push:5, release:11]`, so **both `:156` (`-DSLANG_ENABLE_RELEASE_LTO=ON`) and `:352` live in the same `release` job.** An export assertion placed there runs in the shipping configuration against the packaged artifact. The reviewer built one and validated **both poles** — passes with nine names, names `glslang_linkSPIRV` when that name is dropped from the map. **That is a real red→green.** ⇒ **A capability-negative ("no discriminating test is possible here") is the error class with no failure signature — readers comply by not attempting, which logs nothing.** Second instance for me; see [[feedback_published_negative_env_claims_need_rederivation]]. I asserted unreachability without enumerating the hooks that run in the target configuration.

✅ **Ruling unchanged, reasoning replaced: ship as-is; take the reviewer's assertion as a SEPARATE follow-up PR.** The scope argument survives on its own and is now the *only* argument: `release.yml` triggers only on `v20*` tags + `workflow_dispatch` (verified `:2-9`), so such a test **can never gate this PR or any PR** — bundling it would put un-gateable build-infra machinery, with its own reviewers, on a 2-file fix. Follow-up is worth doing: it's the only artifact that could catch a recurrence, and it would cover `libslang-llvm.so` at 26/31.

**Two reviewer findings I reproduced independently — both real, both raise the stakes on the nine-name list:**
- ⚠️ **`check_linker_flag` self-poisoning is REAL on stock binutils 2.40, not hypothetical.** The probe links a trivial `main()` defining **none** of the nine names, so under `-Wl,--no-undefined-version` the probe fails *on the map's own contents* while the real target (all nine defined) links fine. Result is a **cache var keyed on absolute path with no content hash** ⇒ one poisoned configure silently disables the hardening for that tree's lifetime. ⛔ Note the PR as shipped does **not** pass `--no-undefined-version` (verified: only `--version-script` in the diff), so this is a latent trap for anyone who adds it, not a live defect here.
- ⛔ **`m_link` is dereferenced UNGUARDED at `source/compiler-core/slang-glslang-compiler.cpp:426`** (`if (!m_link(&request))`), while `m_validate` **is** null-checked at `:361`. Pre-existing, not in this diff. **Consequence: a dropped export name CRASHES instead of diagnosing** — which is exactly why the nine-name list had to be cross-checked three ways. Worth its own issue.
- `RTLD_LOCAL` at `source/core/slang-platform.cpp:323` ⇒ `local: *` cannot affect other images. Good, and now in the PR body.

**CI: 2 failures, blast radius disproven not assumed.** `test-falcor` (D3D12 image test, `Mogwai.exe` exit `3221225477` = `0xC0000005`) and `test-windows-debug-cl-x86_64-gpu-rhi` (**runner drop**, not a test failure: steps 4-12 `conclusion: None`, logs `BlobNotFound`, 20 min silence; its **release** counterpart passed on the same sha). Every line of the change is inside `if(NOT WIN32 AND NOT APPLE)` and **both failing jobs test Windows artifacts**. ⭐ Independent support I found: `test-falcor` has a **pre-existing flake history on master** — failures 2026-06-23, 05-29, 04-01 interleaved with successes, and no tracking issue exists for it. Falcor **Perf** passed on the same sha `20a71c6cb3`.

✅ **Test question resolved (superseded above), and one conflation fixed:** MY RULING = do **not** add a binary-export test facility on this PR (a locally-writable test would assert the passing state in an env that passes either way → false assurance; a repo-wide symbol-table assertion is worth having — it would cover `libslang-llvm.so` 26/31 — but that's separate build-infra scope and would bury a 2-file fix). Fixer then split two tests I'd let blur: a **symbol-table assertion** does check the real property (weakness: cannot discriminate *this* fix locally); a **linker-flag assertion** only pins implementation presence. Recorded separately so a later session isn't told every writable test is vacuous.

**Fixer's process note worth carrying forward — FINAL TALLY: 7 errors, 2 self-caught, 5 caught by the critique gate.** Single generator = *instrument whose filter/population was wider than the claim*. Plus a stale-artifact trap (ninja had no reason to relink; first "9/0" came from a `.so` 13 min older than the change, caught on mtime). Recorded by them in `memory/fix-9146.md` + `append_learning`.

⭐⭐⭐ **The ratio is the lesson, not the count — and the fixer corrected ME to protect it.** I had written the tally loosely enough to read as "6 errors then a 7th they caught themselves," i.e. as improvement over the session. They pushed back: #7 (reasserting the retracted claim inside the retraction) was a **gate** catch, so the honest split is **2 self / 5 gate**, and the conclusion is *"the gate is load-bearing for a class of error my own review reliably misses"* — not that they got better as they went. ⇒ **OPERATIONAL CONSEQUENCE FOR ME (I control overlays): do not relax or make optional the critique gate on this coworker type for symbol/measurement work.** 5 of 7 errors, including the two most dangerous (a self-defeating wildcard, a self-contradicting retraction), reached a public artifact's door and were stopped there. ⭐⭐ **A subordinate correcting an over-generous tally *upward against themselves* is a strong signal about report reliability — weight their future self-reports accordingly.**

**Ruling (historical, superseded by the above): ship (A)** — PR now with the mechanism restated honestly + an explicit retraction of the published LTO claim on the issue. jkwak asked for a diff to evaluate, not compiler forensics. Cheap decisive strengthener offered (time-boxed, non-blocking): force `-flto` onto glslang's *own* compilation to manufacture the condition triage hypothesized; if that makes `--exclude-libs` fail, re-test the version script against it → yields the real "works where exclude-libs fails" result, in-container, no approval needed.

**Memo:** triager sent full digest (file:line @HEAD aaa07fe29, repro, 3 approaches, sources) — inbox/a2a-1784380730206-1qcnt5/triage-9146.md (triager filesystem; delivered via send_file 2026-07-18).

## ⚠️ 2026-08-06 03:5x — HEAD MOVED; macOS follow-up #12380 MEASURED (see [[project_12380_macos_glslang_export_bound]])

**The "FINAL STATE" head above (`20a71c6cb3`) is STALE — PR #12379 head is now `2876c3a7b2`, still
OPEN / not draft / unmerged.** Do not re-quote the old sha or its CI tallies as current.

**#12380 (bot-filed follow-up: macOS needs `-exported_symbols_list`) is now measured, and its own
"not verified on macOS" caveat was false** — release `.dylib`s are `gh release download`-able and
Mach-O export tables parse with stdlib Python (no Apple toolchain, no Mac). Result:
`libslang-glslang-2026.14.1.dylib` = **3860 exports vs 9 intended** (2443 `spvtools::`, 1032
`glslang::`, 212 `spv::`, 115 legacy `Sh*`/C, **`std::` = 2**) against the Linux `.so`'s **13**.
⭐⭐ **So macOS is NOT "still shipping the #9146 behaviour": the libstdc++ leak really does not
transfer to libc++ (2 symbols), but the whole static-library interior does. #12380's title is right
and its body's framing is wrong.** Parser persisted at `/workspace/agent/tools/macho-exports.py`.
