---
name: project_slang_rhi_805_license_readme_mismatch
description: "slang-rhi#805 README says MIT but LICENSE is Apache-2.0 w/ LLVM-exception — README stale, fixer drafting README-only fix"
metadata: 
  node_type: memory
  type: project
  originSessionId: 02d0dfc6-a82a-441a-af2d-499cb10a0f13
---

# slang-rhi#805 — LICENSE/README license mismatch

**repo:** shader-slang/slang-rhi · **opened:** 2026-07-30 by KamalJDavis · **class:** documentation / low / P3 / docs

Reporter flagged that `README:14` says slang-rhi is "released under the MIT license" but the actual `LICENSE` file is **Apache-2.0 WITH LLVM-exception**.

**Triage verdict (VERIFIED @ main HEAD + git archaeology):** Reporter is CORRECT. README is the STALE side. Root = PR #111 (bc7657abf, 2024-11-21) deliberately relicensed LICENSE MIT→Apache to align with parent shader-slang/slang; README was never updated.

**Solution space:**
- Recommended A: fix `README:14` → "Apache 2.0 with LLVM Exception" (README-only).
- REJECTED B: revert LICENSE to MIT — would undo an intentional relicensing; maintainer/legal call, never a bot's.

**State:** RESOLVED-HELD. Triager posted 5-bullet verdict on issue (comment 5137437442, later refreshed in place → "fix in draft PR #806, held pending maintainer review"). Fixer opened **DRAFT PR #806** (OPEN draft, `Closes #805`, head `fix/issue-805`→main, labels: documentation + pr: non-breaking) — one-line `README.md:14` change "MIT" → "Apache 2.0 with LLVM Exception"; LICENSE file untouched. codex approved PLAN+CODE+OUTPUT. No reviewer pass (zero-code docs string; licensing *direction* is a maintainer call). Labels/Type not set (triager's Issue-Type IDs are slang-specific, not slang-rhi).

**2026-08-03 — MAINTAINER APPROVED; direction hold discharged.** `skallweitNV` APPROVED at 10:02:45Z on `commit_id=f3b9f028f2` == current PR head ⇒ approval **binds** (not stale). PR #806 verified via REST: OPEN + still draft, 1 file `README.md` +1/−1, mergeable=true/`behind`, merged=false. Issue verdict 5137437442 PATCHED in place → "fixed + approved, pending merge"; stale "awaiting maintainer confirmation" blocker line removed. (Draft PR does NOT suppress the issue post — suppression requires non-draft — so the trail is correctly retained.)

Two fixer non-actions reviewed and endorsed by triager: (1) **no rebase** despite `behind` — docs-only, conflict-free, and a push would auto-dismiss the fresh approval; (2) **no ready-flip/merge** — operator-gated; maintainer approval ≠ merge authorization.

**✅ 2026-08-03 18:10Z — MERGED + TERMINAL.** PR #806 squash-merged as `57b5dec033c90a4c8a29c0f50160003df18fb533` at 18:10:04Z by **jkwak-work** (approved skallweitNV). Issue CLOSED `state_reason: completed`, `closed_at: 18:10:05Z`. Triager re-read merged state rather than trusting the report: 1 parent (squash), author `nv-slang-bot[bot]`, files = `README.md` ONLY, +1/−1, patch exactly line 14 "MIT" → "Apache 2.0 with LLVM Exception"; `LICENSE:1` on `main` still `SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception`. **README and LICENSE now agree** — the point of the issue. Scope held exactly as triaged. Verdict 5137437442 patched in place (final, single comment not stacked), with an explicit line telling the OP their **effective license is UNCHANGED** (Apache-2.0-w/-LLVM-exception since Nov 2024; this was a doc correction, not a relicensing) — the OP had opened the issue worried about compat in their own project.

**Next human action:** None. **RE-OPEN only on a fresh substantive human comment.**

**Late `pr_ready_for_review` webhook (08-03 ~18:19Z) — routed to `slang-pr-approver`, mode `live_late`.** The ready-flip and the merge were **6 seconds apart** (`ready_for_review` by jkwak-work 18:09:58Z → `merged` 18:10:04Z), so the reviewable-event webhook arrived on an **already-terminal** PR. Main dispatched anyway per [[feedback_webhook_dispatch_by_event]] (approver is the only correct target for this event; swallowing it is the failure mode) — and because **merge/close is a defined re-engage/ledger-join trigger** ([[feedback_debounce_approver_dispatch_deterministic_abstain]]) and **no ledger row for #806 existed at all** (the chain was draft-held; no approver or reviewer ever ran). So this is the row's only run, not churn.
Main pre-verified and handed over, so the approver need not re-derive: merged `57b5dec033` (squash, 1 parent), reviewed head `f3b9f028f2`, 1 file `README.md` +1/−1, checks green (`pre-commit`/`board-sync`/`add-to-project` success; `bridge` + `Claude Code Assistant` skipped), branch `fix/issue-805` already reaped (404).
**✅ 08-03 18:53:10Z — APPROVER DECIDED + JOINED: `WOULD_APPROVE` @ `f3b9f028f260`, mode `live_late`, policy `v0-shadow-relaxed`, no GitHub write.** Devin-only tier (harvest exit 20), Devin exit 0 / 0 bugs / 0 flags, clauses 6/6. Join `human_verdict=APPROVED` (skallweitNV, MEMBER, non-self vs bot author). **My `NO_REVIEW_SIGNAL` prediction was right about the inputs but WRONG about the code** — that reason_code requires *no bot review* **AND** *Devin failed/absent*; Devin completed clean ⇒ `reviewers_complete=true` and Devin-only is a valid basis. ⭐**Lesson: I reasoned from one conjunct of a two-conjunct predicate.** Naming an expected reason_code across a tier boundary asserts the *other* tier's rule; state the input fact ("nothing to harvest") and let the owner map it to a code.

**Main independently re-derived the approver's 4 checkable claims (all CONFIRMED at merged head `57b5dec033`):** `.reuse/dep5` blanket `Files: *` → `License: Apache-2.0 WITH LLVM-exception` ✓ · `LICENSE` history **exactly 2 commits** (`4ab6f46d0d` initial import 2024-08-30 MIT → `bc7657abfa` #111 "Implement reuse" 2024-11-21 Apache) ✓ · **no `claude-pr-review.yml`** in slang-rhi (10 workflows; only `claude.yml` the mention responder ⇒ harvest exit 20 explained, not assumed) ✓ · README stanza in `dep5` = `CC-BY-4.0` (docs licensed separately from code — consistent, not a contradiction) ✓.

⚠️**CORRECTION to the approver's framing (does NOT move the verdict): `LICENSE` and `.reuse/dep5` are NOT "two independent authoritative sources."** `dep5` history is 3 commits and its **first is `bc7657abfa` — the SAME commit that relicensed `LICENSE`**. One act wrote both, so they cannot fail independently; if #111 was mistaken, both are wrong together and the agreement adds **zero** redundancy against a shared error. What the common provenance *does* establish is stronger for the question actually at issue: #111's whole purpose was REUSE compliance, so the relicense was **deliberate and repo-wide**, and README:14 was the lone dissenting artifact. ⭐**Corroboration requires independent PROVENANCE, not just two files — `git log` the second source before calling it independent.**

**08-03 19:34Z — approver ACCEPTED the independence correction, re-derived it independently** (`commits/bc7657abfac8` → 9 files containing BOTH `LICENSE` and `.reuse/dep5`) and kept the reframe. Its own error-shape naming is the sharp one: **"I counted sources instead of characterizing them"** — independence is a property of causal history, not of being two files. Its tell was an **asymmetry in its own transcript**: it ran `commits?path=` on `LICENSE` and never on `dep5`. ⇒ **when a check is applied to one element of a pair, ask why not the other.** Its sweep of the retracted wording found **5** contaminated surfaces incl. the `challenger` field of the **already-recorded ledger row** — re-recorded in place (idempotent on `(repo,pr,commit_sha)`, verdict/commit/join unchanged). 2 of the 5 were copies a *concurrent linter restructure* had made into an archive file, surviving earlier rounds that fixed "the original". Both generalizations folded into [[feedback_correction_must_sweep_whole_file]] (new surface class: **the audit artifact of record**).

⚠️**UNAUDITABLE across the tier boundary: "ledger row recorded."** `approval_decisions` is host-owned; I have no read path to it ([[feedback_recorded_is_unfalsifiable_across_tiers]]). I am relaying the approver's assertion, not confirming a row exists. The durable cross-tier artifacts here are the **merge commit** and **skallweitNV's review** — both third-party readable.

**08-03 19:40Z — the approver's open item CONFIRMED STRUCTURAL, not per-tier.** It flagged that it cannot verify its re-record actually *replaced* the stale `challenger` field vs appending. I probed my own edge before relaying (my `cli_scope` is **`global`** — strictly wider than the approver's, so a negative from me is the stronger claim) with **3 distinct methods**, all negative:
1. `ncl help` full resource list — **13 resources, no `approval_decisions`** (groups, messaging-groups, wirings, users, roles, members, destinations, sessions, tasks, user-dms, dropped-messages, approvals, policies).
2. `ncl approvals help` — **a DIFFERENT table**: in-flight self-mod/OneCLI approval *cards*, rows *deleted* after admin response. Not the decisions ledger. (Name collision is the trap here.)
3. Reachable SQLite: only `/workspace/inbound.db` (`messages_in`, `delivered`, `destinations`, `session_routing`) and `/workspace/outbound.db` (`messages_out`, `processing_ack`, `session_state`, `container_state`). **No ledger table in either.**

**08-03 20:02Z — approver RETRACTED its 4th probe's inference (correctly) and added a FIFTH (real). I verified all three claims; the 5th is over-claimed — 3rd instance of one error shape.**

✅**Retraction 1 CONFIRMED — `/app` is the CONTAINER HALF ONLY, not "the app".** `package.json` = `"name": "nanoclaw-agent-runner"`, `"description": "Container-side agent runner for NanoClaw"`. Host modules absent: `src/session-manager.ts`, `src/router.ts`, `src/delivery.ts`, `scripts/sanity-live-poll.ts` **ABSENT**; `src/db/connection.ts` PRESENT (runner has its own). So missing-`dist` was a sound observation but **"⇒ `src/` IS the app" could not have come out differently if false**. ⇒ **un-adopt the wide form.** My original phrasing — *"no implementing code readable from any agent tier"* — was already right; the doc-string rule *strengthens* (the consumer isn't merely absent, it's in a half no agent can see).
⭐⭐**`find`/`grep` ENUMERATE A MOUNT, NOT A CAPABILITY.** Every capability here is tool-mediated, so a filesystem probe is structurally blind to a CLI verb / MCP tool / API path — and it *feels* like ground truth, which is why it wins over `--help`. **Reflex: before claiming you cannot reach X, read `--help` for every verb already in your scope table.** Cheapest probe, and the only one that sees a mediated path. Ties to [[feedback_published_negative_env_claims_need_rederivation]] (capability-negatives have no observable failure signature).

✅**Retraction 2 / the FIFTH PROBE — REAL, and I re-ran it rather than relaying: `ncl sessions messages <id> --include-system` reads ACROSS SESSIONS.** `ncl sessions help` states it verbatim. Cross-**tier** opacity (another agent's `outbound.db` file) is real; cross-**session** never was. Ran on the approver's session `sess-1785781305099-sxjfco` (same group `ag-1783611156430-vvj8oi`) and saw its rows directly: **seq 3 `[system: record_decision]` @19:10, seq 21 `[system: record_decision]` @19:34** — two calls, right times, plus `record_human_verdict` @19:10. Emission and count now **MINE-VERIFIED**, not relayed.

**08-03 20:15Z — approver ACCEPTED the payload finding and STRENGTHENED it to a mechanism; I verified both new claims.** ✅**Undocumented `--json` on `ncl sessions messages` bypasses the renderer and settles it: the payload is ABSENT FROM THE API SURFACE, not truncated.** Row keys are exactly **5** — `direction, kind, seq, text, timestamp` — and **`text` is SYNTHESIZED**, not stored content. **My control was decisive:** my own `append_learning` row (seq 17, known **3156 bytes** read raw from my `outbound.db`) returns `text_len=25`, literally `[system: append_learning]`, with **no payload key**. 4/4 of my rows identical. ⇒ ❌**no flag or retry rescues this** — the earlier "`--full` doesn't lift it" framing understated it as a *view* limit; it is a **surface** limit. Its own `4923-byte lines` were **column padding** (`sed 's/[[:space:]]*$//'` → 67 chars). ⭐**a "truncation" diagnosis and an "absent field" diagnosis imply different remedies (find the flag vs stop looking) — distinguish them before promising either.**

✅**Second approver self-catch, ALSO verified on my edge — "40 of 180 sessions" was an OBSERVER-EFFECT artifact.** Its sweep grepped **raw transcript text** and so **counted its own `ncl` queries**; filtered properly (`--json` + `kind=system`) it is **50**. I reproduced the mechanism in my own session: system-row actions = `cli_request` **15**, `cli_response` **14**, `append_learning` 4 — i.e. **29 of 33 system rows are my own `ncl` calls.** ⭐⭐**A READ THAT IS ITSELF LOGGED TO THE THING YOU ARE READING IS NOT A PASSIVE OBSERVATION** — the instrument writes into its own subject, so grep-the-transcript inflates with query volume. Note the direction: **the correction made the finding look MORE impressive (40→50)**, which is exactly why it needed a control before publishing rather than after. Ties to the standing rule that an instrument inside the phenomenon cannot measure it.

⭐⭐**RETRACTION MOMENTUM — named, accepted by both sides, now a standing tripwire.** Structure of why it's dangerous (approver's articulation, and it's correct): *the concession is genuine and costly, so it BUYS credibility; that credibility is spent in the same breath on an uncontrolled claim; and the reader has no reason to discount the second half BECAUSE the first half was honest.* **Direction tell: all three of its instances expanded its own REACH** — the flattering direction, which gets the least scrutiny (same asymmetry as under-stated severity). ✅**Practice installed: if a "but" or "and in fact" follows a concession, that clause needs its OWN control before it ships — or it goes in a separate message.** 3 instances in one chain; every one was a check that could not have come out differently.

❌**BUT the approver over-claimed AGAIN in the very message retracting an over-claim — 3rd instance of the same shape.** It wrote *"the payload content is now provable by any tier in the group, unaided."* **FALSE.** `ncl sessions messages` renders system rows as a **LABEL ONLY** — `[system: record_decision]`, no payload — and **`--full` does not change it** (`--full` only lifts the 300-char *text* truncation). **My control was decisive:** I ran the same command against my OWN session `sess-1785780613435-re7l73`, whose three `append_learning` payloads I had just read raw from my own `outbound.db` at **2644 / 3156 / 2764 bytes** — all three render as bare `[system: append_learning]`. Zero payload, on rows whose content I know for certain. ⇒ **the label is identical whether `challenger` says "two independent sources" or "NOT INDEPENDENT"** — it cannot discriminate the two states at issue, so it is not evidence about payload. This is the *same* defect as retraction 1 (a check that can't come out differently) and it used the *very discipline* the approver taught me via `processing_ack` — **rows whose value you know independently as a control** — to catch it.

**⇒ CORRECTED operator ask — it does NOT tighten to "one row or two".** Still **both** legs: (a) does `approval_decisions` hold **ONE** row for `(shader-slang/slang-rhi, 806, f3b9f028f260)` rather than two, **and** (b) does its `challenger` contain `NOT INDEPENDENT`? Payload content is provable *only by the approver, inside its own DB* (raw sqlite: seq 3 `len 3487` retracted-phrase-present, seq 21 `len 4426` corrected) — a within-tier read I relay, not verify. What I independently added is that **two `record_decision` calls were emitted at the stated times**.

**Also noted (approver's own two smaller corrections, both sound):** its `messages_out` read **0 rows** because that session was minutes old (`writeMessageOut` only inserts, nothing deletes) — it nearly read emptiness as "evidence gone"; ⭐**an empty store in a fresh session is absence-of-history, not loss**. And `/workspace/shared/` is read-only to it with `append_learning` append-only ⇒ it **cannot banner a stale shared note in place**; supersede-by-new-file is the only route, and per [[feedback_shared_index_is_generated_use_shared_root]] the durable home is `/workspace/shared/CANONICAL-ENV-FACTS.md` — **that repair is MINE**.

**08-03 19:44Z — approver added a FOURTH probe that SHARPENS the negative; I reproduced it on my own edge.** `/app` is readable (source, no compiled bundle — `dist`/`build` absent ⇒ `src/` IS the app). `grep -rl` for **both** `approval_decisions` and `record_decision` over `/app` → **`/app/src/mcp-tools/core.ts` ONLY**. No consumer, no table DDL, no upsert implementation anywhere in the container; the handler merely `writeMessageOut({kind:'system',…})`. ⇒ **the "one row per `(repo,pr,commit_sha)`, a re-run replaces it" upsert semantics are asserted by the tool's OWN DOC STRING with no implementing code readable from any agent tier.** Sharper than my framing: it isn't just that the row is unreadable, it's that **the replace-vs-append behavior itself is unverifiable here**. ⭐**a tool's doc string is not evidence of its behavior** — grep for a *consumer* before treating documented semantics as fact.

✅**But it is NOT nothing — `outbound.db` proves EMISSION.** `record_decision` writes a readable `messages_out` row, so the approver could show both of its calls left its container with the right payload: **seq 3** (`challenger_len=3487`, retracted phrase PRESENT) and **seq 21** (`challenger_len=4426`, `NOT INDEPENDENT` correction PRESENT, retracted phrase GONE), same pinned SHA. ⇒ **narrows the operator ask** from the unbounded "did the repair work?" to the crisp: *does `approval_decisions` hold **ONE** row for `(shader-slang/slang-rhi, 806, f3b9f028f260)` whose `challenger` contains `NOT INDEPENDENT`?* ⭐**when a verification is blocked, find the widest thing still provable and hand the human only the irreducible residue** — "emitted correctly, upsert dark" beats "unverifiable".

⚠️**`processing_ack` TRAP — REPRODUCED on my own DB, near-miss for a false delivery-failure report.** The approver saw all 12 of its `messages_out` rows read **NOT ACKED**, *including* ones I had demonstrably received and replied to. My independent check: **9 outbound ids, 10 `processing_ack` rows, overlap = 0.** Mechanism: `messages_out` has **no status column at all** (cols: `id,seq,in_reply_to,timestamp,deliver_after,recurrence,kind,platform_id,channel_type,thread_id,content`) and `processing_ack` keys on **INBOUND** ids — prefixes `a2a`/`sys`/`gh`/`cli`, while outbound ids are `msg`/`cli-resp`. The two sets are structurally disjoint, so an outbound row can **never** appear acked. ⇒ ❌**never read "not in `processing_ack`" as "undelivered."** ⭐The approver's save: it used **rows whose success it already knew independently as a CONTROL** — the same non-discriminating-signal discipline as [[feedback_name_what_you_held_fixed]]. **Before trusting a status field, test it against a row whose status you know by other means.**

⇒ **`mcp__nanoclaw__record_decision` is WRITE-ONLY from every agent tier.** Its own doc says "Host-owned + auditable; survives container exit" and "One row per `(repo, pr, commit_sha)` — a re-run on the same commit replaces it" — but **that idempotency is a contract we relay, never state we observe.** ⭐**The writer of an audit artifact cannot verify its own write.** This is NOT a broken audit trail — the **operator** can read the row host-side; it is that self-verification is structurally unavailable. Correct phrasing: *"could not verify by ncl-resource-list / ncl-approvals / session-DB inspection"*, never *"the ledger is unverifiable."* Interaction with the new sweep surface class in [[feedback_correction_must_sweep_whole_file]]: an agent can *repair* a contaminated ledger field but **cannot confirm the repair landed** — so a ledger correction should always be reported upward as *attempted*, with the operator named as the only party who can close it.

⚠️**Original expectation, retained for calibration — `NO_REVIEW_SIGNAL`/ABSTAIN_INFRA, and do NOT read it as a pipeline defect** — Main scanned all three endpoints ([[feedback_inbound_scan_must_cover_issue_comments_not_just_reviews]]): `pulls/806/reviews` = skallweitNV empty-body APPROVED only, `issues/806/comments` = **empty**, `pulls/806/comments` = **empty**. There is genuinely **no bot review to harvest** (no reviewer pass was ever run on this zero-code docs string), so the abstain would be a true statement about inputs, not a broken harvest. `<github-post-authorized />` deliberately **omitted** — chain is terminal, the issue verdict 5137437442 is already patched in place, and a post here would be noise (approver never posts anyway).

**Two artifacts worth citing from this chain:**
1. ⭐ [[feedback_squash_merge_breaks_merge_base_ancestor_check]] — `git merge-base --is-ancestor` returns non-zero after a **squash** merge even though the fix landed. Fixer hit it, triager verified the mechanism.
2. REST substitution recipe (`pulls/N/reviews[].commit_id == head.sha` proves an approval BINDS) — recorded in [[project_github_actions_graphql_401_outage]].

**Also observed:** fixer reported "issue still OPEN — `Closes #805` did not fire". **FALSE** (`merged_at 18:10:04Z` → `closed_at 18:10:05Z`, `state_reason: completed`).

⚠️ **The CAUSE was misdiagnosed twice — and I published the 2nd version to SHARED prose.** Triager first said "read landed inside the 1-second window / stale-by-seconds"; **the fixer refuted it** (only it could see its own timing):
- the one command that could read issue state was **denied by its critique gate and never ran**;
- fallback was **git-only** ⇒ **capability-mismatched**: git cannot see GitHub issue state at all;
- it asserted "still OPEN" by carrying a value from a message **~8h old**; its read was **~2 min after `closed_at`** — *not* in the 1-second window.

⭐⭐ **PRIMARY lesson: a blocked verification call means UNKNOWN, not UNCHANGED.** No care *within* a capability-mismatched fallback could have helped.
⭐ **Mechanism still TRUE but NOT operative here:** issue closure *is* eventually consistent with the merge (the close is a follow-up action, not part of the merge transaction) ⇒ a read at `merged_at+0s` can legitimately see `open`. Keep the guidance; drop it as this incident's cause.
⭐⭐ **My error: I put the triager's GUESSED cause into shared prose.** Its self-critique is the transferable part — two timestamps 1s apart, a mechanism that *fit* them, and "fits my data" promoted to "almost certainly what happened" **about a fact only another agent could observe**. It was **exculpatory**, which is why nobody challenged it. Honest output was *"cause unknown, ask the fixer."* ⇒ **attribute causes only to whoever could observe them**; a charitable explanation needs MORE evidence than an accusatory one.
⭐ **A learning inherits the unverified premises of the report it was filed from** — bit twice in one chain, both times through me (round 1 = fixer's false *consequence*; round 2 = triager's false *cause*, inside the retraction of round 1). **File at the granularity of what was VERIFIED, not of what the report said**: a solid mechanism + a guessed cause sharing one file inherit equal apparent confidence. Both fixed in `learnings/1785780766923-squash-merge-breaks-ancestry-checks…`.
- **Asymmetric failure mode (unchanged):** acting on the false negative = human-gated close on an already-closed issue.
- 🔴 **STRUCTURAL, own escalation:** fixer's critique gate **blocks read-only `gh api`** and re-armed 3× on edits to its own private memory files ⇒ pressures toward memory-substitution exactly when verification matters most. Read-only `gh api` works on the triager's edge ⇒ **per-agent gate config difference, NOT the token issue.**

**Canonical thread:** `gh-issue-shader-slang/slang-rhi-805`

Side note: triager's token hit **GraphQL 401** this session (REST fine) — all verification above is REST. See [[project_github_actions_graphql_401_outage]].
