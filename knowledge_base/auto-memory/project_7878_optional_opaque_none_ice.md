---
name: project_7878_optional_opaque_none_ice
description: "#7878 Optional<opaque> none ICE — non-draft PR #12005 (maintainer-flipped+APPROVED); IR diag E41037; head 05b45592 (golden-fix self-corrected the 04:09Z CI red); merge-gated"
metadata: 
  node_type: memory
  type: project
  originSessionId: b8bdda04-ca21-4f66-a138-a5f247fc238c
---

**#7878** — returning `none` from a generic *member* accessor whose `Optional<T>` payload binds to an opaque/resource type (e.g. `Optional<Texture2D>`) ICEs at SPIR-V emit (`Unhandled local inst … defaultConstruct`). Labeled *Missing Diagnostic* — maintainers want a clean error, not codegen.

**Root cause (verified @ `bfe6a7f14`):** E30902 (`OptionalCannotWrapResourceType`) exists but the three front-end guards gate on `typeTransitivelyContainsOpaqueHandle` at AST-check while `T` is abstract; concrete `Optional<Texture2D>` only appears post-specialization and is never re-checked. Discriminator proven: generic free-fn + non-generic both emit clean E30902; only the generic *member* ICEs.

**Fix (Approach A):** draft PR **#12005** (branch `fix/issue-7878`, base master, `pr: non-breaking`, `Closes #7878`, assignee expipiplus1). Extends IR pass `checkForOptionalNoneUsage` (runs post-specialization, pre-`lowerOptionalType`, already has a sink) to emit a **new IR-layer diagnostic E41037** (`optional-cannot-wrap-resource-type-ir`) for `MakeOptionalNone` of an opaque payload. New code (not reused E30902) because E30902's struct is AST-typed and the untyped emit renders classic-style; E41037 mirrors sibling **E41027** so it renders rich. **Maintainer-flagged point:** code-number choice (E41037 vs reuse 30902) — two-line revert if they prefer single code.

**DescriptorHandle path (#9932 / PR #10064) unaffected** — lowers to integer handle, `isOpaqueType` skips it, so Approach A excludes it for free (sidesteps the Approach-B caveat).

**✅ APPROVED + non-draft (07-10 02:43Z, VERIFIED via reviews API + timeline):** pdeayton-nv APPROVED (review `4668168674`, empty body, 02:43:51Z), then **maintainer flipped ready_for_review at 02:43:57Z (actor=pdeayton-nv, NOT the bot** — clears drafts-only guardrail, verified who via timeline), re-requested review from expipiplus1 02:43:58Z. PR now `draft:false`, 0 unresolved threads, 0 changes-requested. **Head `cd34c26b9e`. NOT merged** (`merged_at:null`, state open, mergeable but `behind` master → rebase needed pre-merge) — merge is the ONLY remaining step, operator/maintainer-gated (bot will NOT merge).

**🔁 ready_for_review webhook (07-10, actor pdeayton-nv) → routed to slang-reviewer** (thread `gh-issue-shader-slang/slang-12005`, head `cd34c26b9e`) for a fresh independent COMMENT-state pass. Verified BEFORE routing: maintainer flip (not bot self-ready), so no drafts-only breach. PR already maintainer-APPROVED, so reviewer output is confirmatory. Await [Review Report]; relay to operator only if it surfaces a real bug. Do NOT re-dispatch reviewer on duplicate ready_for_review echoes for this same head.

**↻ synchronize webhook (07-10 04:09Z): head `cd34c26b9e` → `05b4559240`.** Single commit, benign: golden-file update (2 lines) in `tests/language-feature/dynamic-dispatch/diagnose-ref-interface-in-compound.slang` ("Update golden for now-informative Optional<T> type name in ref-param diagnostic") — expected fallout of the in-scope `getTypeNameHint` `Optional<T>` fix; `ahead_by:1 behind_by:0` (new commit, not rebase; no source-logic change). Did NOT re-dispatch reviewer — sent a head-bump note on the same thread to target `05b4559240` in its final report. **Current head = `05b4559240`.** ✅ **APPROVAL-VALIDITY RESOLVED (VERIFIED via reviews API 07-10 ~04:24Z):** round-7 synchronize dismissed the 02:43Z approval; **pdeayton-nv RE-APPROVED @ 04:18:48Z on head `05b4559240`**. Round-7 diff Main-verified: 1 file `diagnose-ref-interface-in-compound.slang` **+2/−2**, line REPLACEMENT (`''`→`'Optional<IFoo>'`, MORE informative), not silencing.

**🛑 SUPERSEDED by merged maintainer PR #11971 (VERIFIED 07-10 ~20:00Z — this is the current terminal-direction state):** maintainer **pdeayton-nv converted #12005 BACK TO DRAFT @19:45:50Z** (verified actor via timeline — maintainer action, NOT bot; no guardrail issue) then asked bot @19:46Z "check this PR against ToT — interaction with recent changes." Bot ToT-check @20:01Z (posted to PR, issue comment 4939058356 — closest-to-state): **#11971 (expipiplus1, "Fix #7878: diagnose default-construct of opaque handle instead of crashing", MERGED `95315356a5` @08:17Z) already fixes #7878 on master.** Design conflict CONFIRMED against #11971's own body: #11971 diagnoses via existing **E56004** in `checkUnsupportedInst` using `findUnstorableOpaqueHandleType` — *"deliberately narrower than `isOpaqueType` (buffer-backed resources and pointers allowed)"* — gated `isKhronosTarget||isWGPUTarget` (HLSL/Metal keep compiling by design, polarity test guards it). #12005's **universal E41037 + broad `isOpaqueType`** would DOUBLE-DIAGNOSE + newly REJECT HLSL/Metal + buffer-backed optionals #11971 deliberately allows → no residual value that doesn't contradict merged design. **#12005 now `draft:true`, `mergeable_state:"blocked"`, NOT merged. #7878 still OPEN** (#11971 used non-closing ref). **Recommended: close #12005 as superseded** — maintainer may instead ask for fresh ToT-based PR extending diagnostic to HLSL/Metal (#11971 scoped out).

**✅ TERMINAL — CHAIN CLOSED (VERIFIED 07-10 ~20:47Z):** pdeayton-nv **CLOSED #12005 UNMERGED @20:42:52Z** — comment "Closing as superseded by #11971" (4939384321). `state:closed, merged:false`. Bot's close-as-superseded recommendation enacted by maintainer. Fixer cleaned up (removed worktree `wt-slang-7878` + `active-work/slang-7878` sentinel — consistent w/ always-reap standing rule; no Main action). Treated close-comment as confirming ack (no counter-proposal) → no GitHub reply — correct. **⚠️ #7878 ITSELF STILL OPEN** (`closed_at:null` — #11971 non-closing ref); bug IS fixed on master by #11971, so issue is a stale-open a maintainer should close, but issue-close is maintainer's call (bot/Main do NOT auto-close issues). No further action anywhere; chain terminal. Substantive new human comment on #7878/#12005 re-opens per standing rule. Prior state was DRAFT-held, maintainer-gated (review → ready → merge; bot drafts-only, ready/merge operator-gated). Issue carries nv-slang-bot resolution comment ([issue #7878](https://github.com/shader-slang/slang/issues/7878), [PR #12005](https://github.com/shader-slang/slang/pull/12005)).

**Maintainer review (pdeayton-nv, ~6 rounds 07-08→07-10) — ACTIVE + POSITIVE-TRENDING, re-push CONFIRMED landed (VERIFIED on GitHub 07-10 02:04Z, PR `updated_at`, 14 comments):** Substantive fix ACCEPTED. Applied across rounds: strip lua comment, inline helper, **rename** pass→`checkForInvalidOptionalUsage` + file→`slang-ir-check-optional-usage`; **item = REAL bug fixed** (Optional-in-struct-field slipped linear peel → added `IROptionalType` arm to shared `isOpaqueTypeImpl`, opacity composes through Optional at any depth, deleted peel loop); **pass made unconditional** (bool gates only the always-none `.value` check + error-return before `lowerOptionalType`, per `diagnoseCircularConformances`); guarded on `requiredLoweringPassSet.optionalType`; E41037 reports **payload value type** (matches E30902) not opaque leaf + `getTypeNameHint` `IROptionalType` case; `CHECK_GEN-NOT: error[E99997]` regression assert; nested `Optional<Struct{Optional<Texture2D>}>` test. **Round 6 DONE — head `cd34c26b9e` (07-10 02:39Z [Fix Report]):** diag-style test conversion complete (position-based `diag=` annotations, test+comment-only, +40/−56, no behavior change; GENERIC unit emits 4× E41037, 0× E99997; 662/662 diagnostics pass; codex CODE+OUTPUT approve). Last maintainer-requested item now closed — ball fully in pdeayton-nv's court. Still DRAFT, no formal review objects (all feedback via issue_comments), **no verdict/ready/merge yet.** (Round 5 was `732c806723`.) Awaiting maintainer response to round 6 → then ready-flip/merge (operator-gated). NOTE: fixer reports directly to Main (tier-skip past triager); do NOT message fixer to correct (would mint competing parent edge) — triager owns the peer-wire edge and rolls up. Not relaying per-round beats; relay only ready-flip / merge / blocker / stall. Compaction notices from triager = no state change, hold in true silence. Review/CI follow-up on #12005 is webhook-driven → routes to slang-fixer session (**`report_pr_created` CONFIRMED** 07-08 — mapping in place, no orphaning risk). Chain owned by slang-triager (peer-wired to fixer); fully closed at triager tier. Substantive new human comment re-opens per standing rule.
