---
name: project_nanoclaw_1086_sync_sendmessage
description: "slang-coworkers/nanoclaw#1086 nv-main←upstream sync disallowing built-in SendMessage; merge resolution correct, but 4 of 5 listed checks structurally cannot cover container/agent-runner; 10th inline-routing instance"
metadata:
  node_type: memory
  type: project
  originSessionId: pr1086-sync-sendmessage
---

**slang-coworkers/nanoclaw#1086** — `Sync nv-main with upstream/main`, author **nv-slang-bot[bot]**, branch `sync/upstream-nv-main` → `nv-main`. `pr_ready_for_review` (reason `opened`, 2026-08-05T14:21Z). **3 files, +46/−4.** Head `4c41224f` (merge commit; parents `c0bc5a03` = nv-main, `358f1a81` = upstream/main). Brings upstream `81b18a9f` (Daisuke Tsuji): move built-in `SendMessage` from `TOOL_ALLOWLIST` into `SDK_DISALLOWED_TOOLS`, + new `claude.tool-collisions.test.ts`, + CHANGELOG line.

**ROUTING: handled INLINE by Main — 10th instance of the rule.** NanoClaw platform-infra fork; the webhook task string ("route to the project's `*-pr-approver`") targets PRODUCT (slang/slangpy) PRs. No `nanoclaw-pr-approver` exists. See [[project_nanoclaw_1085_ncl_unit_hint]] (9th), [[project_nanoclaw_1083_drift_check_empty_allowlist]] (8th), [[project_nanoclaw_pr874_webhook_route_approver]].

**Merge resolution VERIFIED CORRECT, in the claimed direction.** `BASE_TOOL_ALLOWLIST` is fork-local: `git log -S "BASE_TOOL_ALLOWLIST" upstream/main` → **empty**; upstream has only ever had `TOOL_ALLOWLIST` (`5a0098ed`). The `BASE_` prefix arrived via fork #1064 (`fcb39e4f`, which added `extraAllowedTools`/`computeBlockedTools`). Taking upstream's exported name is **required** — head test imports `{SDK_DISALLOWED_TOOLS, TOOL_ALLOWLIST}`; running it against base `claude.ts` gives `SyntaxError: Export named 'TOOL_ALLOWLIST' not found`. Rename complete (`grep BASE_TOOL_ALLOWLIST` head → 0). Sync complete: `rev-list c0bc5a03..upstream/main` = exactly the 2 commits. New test file + CHANGELOG line **byte-identical** to upstream's (`diff` vs `81b18a9f` empty).

**🟡 THE STRUCTURAL FINDING — 4 of the 5 checks in the PR body cannot exercise the change.** `pnpm run build`, `validate:templates`, `format:check`, host `vitest` **all exclude `container/agent-runner/`**: `vitest.config.ts` include has `container/*.test.ts` (top level only — deliberate, agent-runner needs `bun:sqlite`); agent-runner tsconfig `exclude: ["src/**/*.test.ts"]`; `format:check` globs `"src/**/*.ts"`. Base-vs-head measured: **delta zero on all four**, and `grep -c tool-collisions` over the vitest log AND the agent-runner tsc log = **0 / 0**. Only `bun test` in `container/agent-runner` covers it (base 342/3 → head 345/3, same 3 pre-existing `dispatchResultText — critique-gate` failures). ⭐⭐**A green suite is not evidence when the suite's include-globs structurally exclude the changed tree — check the config, don't infer coverage from the pass count.**

**🟡 INHERITED DESIGN QUESTION — `SendMessage` has a live non-collision use.** Upstream frames it as pure namespace collision. It is ALSO the documented resume path for a backgrounded `Agent` subagent: pinned CLI binary (`claude-agent-sdk` 0.3.197 / CLI manifest 2.1.197) emits `Use SendMessage with to: '<agentId>', summary: '…' to continue this agent.` **DEMONSTRATED, not theorized** — I backgrounded a subagent and resumed it via `SendMessage` mid-review (worked: "resumed from transcript in the background"), because THIS container still runs the pre-fix allowlist (`/app/src/providers/claude.ts:40-52` has no `SendMessage` in the disallow list and `:74` has it in `BASE_TOOL_ALLOWLIST`). `mcp__nanoclaw__send_message` is NOT a substitute (resolves agent-group destinations in `inbound.db`; MCP inventory = create_agent/send_message/wire_agents/send_file/add_reaction/report_pr_created/record_decision/record_human_verdict — no subagent-resume). Hook text is generic and names nothing: `Tool 'SendMessage' is not available in this environment — use the nanoclaw equivalent.` **Scoped before flagging as blocker:** grepped `container/skills/` — nothing instructs background-and-resume (`base-nanoclaw` says the opposite for builds) ⇒ no in-repo dependency today. Not a blocker; belongs upstream (`nanocoai/nanoclaw#3187`). `toolAliases` exists in this SDK (unused by fork) as a redirect-instead-of-block option.

**ℹ️ "Create a merge commit" ask is MEASURABLY correct — simulated both, don't squash.** Repo allows all 3 methods (`allow_squash_merge: true`) ⇒ one wrong click. Scratch-branch sims: **merge commit** → `358f1a81` ancestor ✅, 0 commits replayed next sync; **squash** → link LOST, **2** commits re-replayed. Trees identical either way (`git diff` empty) ⇒ **squash loses ancestry only, not content.** ⭐⭐**A merge-method request is a testable claim — simulate it in a scratch branch rather than repeating the body's reasoning.**

**Test is a VALIDATED detector, not a tautology.** Mutation control: at head, re-added `'SendMessage'` to `TOOL_ALLOWLIST` keeping the export → **1 pass / 2 fail** (both the SendMessage case and no-overlap). ⚠️Naive negative control (head test + base claude.ts) was **inconclusive** — module-load `SyntaxError`, establishes nothing about assertions. ⭐⭐**A control that fails at import proves the import, not the assertion — mutate the value, don't swap the file.**

**jq claim does NOT reproduce; PR's conclusion still right.** `jq` IS present here (`/usr/bin/jq`), all 150 jq-referencing tests pass, `grep -ic jq` over both logs = **0**. My 19 failing files are a different pre-existing set (module-resolution: `js-yaml`, `@chat-adapter/telegram`, missing `src/channels/dashboard.ts`) — **identical at base**. Three environments, three results, CI green. ⭐**The author's METHOD (compare against un-merged baseline) was right; only the named cause was container-specific — credit the method, correct the attribution.**

**CI on head `4c41224f`** (run 31014823401, headSha confirmed = PR head): `check`✓ `ci`✓ (was `pending` on first read — re-read before quoting) `label`✓. Host 2047 passed / 151 files; container **349 pass / 0 fail / 33 files**, and the CI log shows the `claude.tool-collisions.test.ts` group — so CI DOES cover it via `.github/workflows/ci.yml:163-165` (`bun test`, working-directory `container/agent-runner`). `mergeStateStatus` UNSTABLE→ resolved once ci finished.

**Posted:** one comment, [issuecomment-5193517132](https://github.com/slang-coworkers/nanoclaw/pull/1086#issuecomment-5193517132). **NOT merged** — `nv-main` is maintainer-owned, outside the `nv-coworkers` grant ([[feedback_nv_coworkers_automerge]]).

**On redelivery:** routing unchanged (inline, no dispatch). Do NOT re-review. Check only: (a) did the SendMessage capability question get answered upstream, (b) was it merged with a merge commit (not squash) — verify via `git merge-base --is-ancestor 358f1a81 origin/nv-main`.
