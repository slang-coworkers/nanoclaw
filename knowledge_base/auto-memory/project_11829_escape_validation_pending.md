---
name: PENDING maintainer scope call — #11829 backslash-#include escape regression
description: CLOSED on our end — skiminki-nv closed bot draft PR #11834 unmerged and is fixing #11829 manually ("the bot PR is going in the wrong direction"); bot stood down gracefully (comment posted, worktree cleaned); issue left open for his fix; re-engage only on a fresh substantive inbound
type: project
originSessionId: 5c954e23-910d-4673-9d6d-a57e442fb38e
---
shader-slang/slang#11829 — backslash `#include` paths rejected with error 10008, regression from #11714 (skiminki-nv's "Improve character and string literals", commit c21ead269, merged 2026-06-29). Draft PR **#11834** filed 2026-06-30 (squashed commit 12d00dffe0, `pr: non-breaking`, report_pr_created done). Original approach = Approach A (relocate escape VALIDATION from the lex scanner `_lexStringLiteralBody` into the decode path). A codex gate caught a real bug mid-build: char-literal decode runs at parse time and Slang skips parsing once lexing reports any error, so deferred char-escape diagnostics never fire in `char-literal-errors.slang` — fixer corrected by validating char escapes at LEX time while keeping string-literal validation deferred.

**Maintainer objection (skiminki-nv, author of #11714):** the fix is the wrong shape — escape PROCESSING should be DISABLED entirely for path-bearing directives (#include / #import / #line / similar), not have its VALIDATION relocated. Cited `#include "utils\neural.slang"` (\n→newline) as already-broken.

**Fixer's code-level verification (not memory):** skiminki PARTLY right. `#include` quote-form reads RAW via `getFileNameTokenValue` (lexer:1780; preprocessor:3613/3627) — \n stays literal, so the PR fixes #include correctly and skiminki's \n-for-#include example does NOT hold. BUT `#line "file"` (preprocessor:4211) and `import` (parser) decode via `getStringLiteralTokenValue` — those genuinely process escapes and the PR does nothing for them. Broader point lands.

**Resolution so far (2026-06-30):** skiminki replied "Do it" → fixer reworked (within Main's pre-authorization: named directives, moderate scope, not a token-model balloon). PR #11834 now at commit `516cc5f479` (8 files): all path-bearing constructs read RAW via `getFileNameTokenValue` — `import`/`__include`/`implementing` + `module "..."` (slang-parser.cpp:1323/1391), `#line "file"` (slang-preprocessor.cpp:4211); `#include` was already raw; the original lexer change deferring string-escape VALIDATION to the decode pass kept (chars still validated at lex). Regression tests: `tests/preprocessor/include-backslash-path.slang` + `tests/diagnostics/import-backslash-path.slang`. Tests green (`string-literal-errors*`/`char-literal-errors*` included). **slang-reviewer = APPROVE_WITH_NITS (0 bugs)** (folded FG003/C001/C003/FG004/FG002/FG005; declined FG001/C002/C004 w/ justification); codex CODE+OUTPUT = approve. CI red = cosmetic priority-yield (run 28453179918), not real.

**Update (2026-06-30 15:23):** skiminki escalated from "Do it" to driving an **incremental step-by-step redo** — *"Redo the PR. Start by updating parseFileReferenceDeclBase() to not process string escapes for quoted strings."* Fixer grounded it against the #11714 diff and surfaced a key finding on the PR (issuecomment-4845171168): #11714 added escape *reporting* to the readers (parseFileReferenceDeclBase = import/__include/implementing; parseModuleDeclarationDecl = module; HandleLineDirective = #line) AND a lexer-scan rewrite reporting for every string-literal token. So the **headline #include 10008 originates in the LEXER SCAN, not a reader** — reader-only changes fix import/module/#line but NOT #include. Fixer asked skiminki how to handle #include (defer string-escape validation to decode vs. narrower), did NOT force-push, holding for his end-state direction.

**Update (2026-06-30, takeover):** skiminki commented on the ISSUE (issuecomment-4846339690): *"Note. I'll fix this manually. The bot PR is going in the wrong direction."* He's taking over the fix himself and rejected the bot PR's direction. Fixer dispatched to stand down gracefully — post a thank-you/stand-down comment on PR #11834, push nothing further, and NOT unilaterally close the PR or issue (per the never-auto-close rule; let skiminki close the draft).

**Closed (2026-06-30):** skiminki closed draft PR #11834 unmerged himself (we did NOT auto-close — he did). Fixer posted the gracious stand-down comment (PR#11834 issuecomment-4846357766: confirms standing down, defers to his manual fix, offers the diagnosis trace) and cleaned up: worktree `wt-slang-11829` + active-work sentinel removed; `fix/issue-11829` branch left on origin (not deleted). Issue #11829 left OPEN for skiminki's manual fix.

**How to apply:** CHAIN CLOSED on our end. Bot has fully stood down — no further commits/pushes/rework. Re-engage ONLY on a fresh substantive inbound: skiminki pings the bot for something, or his manual fix stalls and a human invites us back. Don't reopen/push proactively. The original analysis (commit 516cc5f479: raw path-token reads for path-bearing directives; the two-faced regression — reader-sink for import/module/#line + lexer-scan for #include) stays on the closed PR as a reference if he wants it.
