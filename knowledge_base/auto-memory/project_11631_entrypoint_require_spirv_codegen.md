---
name: 11631-entry-point-require-spir-v-codegen-caps
description: "shader-slang/slang#11631 — entry-point [require] doesn't update SPIR-V codegen caps; long maintainer design saga → csyonghe resolution; draft PR"
metadata: 
  node_type: memory
  type: project
  originSessionId: 01981e60-dc59-4b11-95f3-bc73ad5021b7
---

**shader-slang/slang#11631** (reporter pdeayton-nv): entry-point `[require(...)]` capabilities are validated but never merged into the SPIR-V codegen capability set (`getTargetCaps()` = target format + `-profile` + `-capability` only), so source-level `[require]` diverges from command-line `-capability`. Two repros: (1) `[require(spvBindlessTextureNV)]` emits descriptor-heap path not BindlessTextureNV; (2) `[require(spirv_1_5)]` stays SPIR-V 1.0. Verified compile-only.

**Draft PR #11633** (`fix/issue-11631`, branch author nv-slang-bot[bot]) — started as the version-half fix (stamp require atom on codegen module + `determineSpirvVersion` unify). Kept DRAFT throughout.

**Maintainer design saga (all public on the issue):**
- jkwak-work first approved the version PR, then (with csyonghe) ruled `[require]` is **validation-only**, NOT a `-capability` replacement — capability must be a global per-compilation setting (multi-entry consistency). That direction → close #11633 as by-design.
- pdeayton-nv (reporter) pushed a diagnostic scheme (E41012 un-suppress + new error for unsatisfiable `[require]`).
- **tangent-vector** (capability-system architect) then read the **original issue as VALID**: his "compromise flow" step 2 says codegen SHOULD inflate caps to include the entry point's requirements; opposes a distinct `[require]`-specific diagnostic (wants explicit-OR-inferred treated **uniformly**). Contradicted jkwak's validation-only stance at the codegen layer.
- **csyonghe** (the authority both deferred to) RESOLVED it (~07-09, cmt 4928176359): encode user-specified caps as decoration on each entry point; **SPIR-V backend passes scan all entry points & honor them alongside global options; legalize bindless accordingly; conflicts (cross-entry OR source-vs-`-capability`) → error.** Sides with tangent-vector (codegen honors `[require]`), reverses jkwak, and answers jkwak's consistency concern via conflict→error.

**OPEN residual divergence (surfaced, NOT resolved):** csyonghe scopes to **user-specified (explicit `[require]`) only**; tangent-vector wanted **uniform explicit-OR-inferred**. Fixer found explicit-only is BETTER — it avoids the E50011 CI-red that stalled #11633 (ray-tracing test `explicit-shader-stage-7.slang` has zero `[require]`; body-inferred version → explicit-only stamps nothing → codegen unchanged → E50011 green).

**IMPLEMENTATION (2026-07-23, ~02:00-04:20Z) — root cause reshaped scope; VERSION-HALF ships, BINDLESS-HALF deferred to specialization-layer follow-up.** pdeayton greenlit "expand 11633"; Main green-lit; fixer implemented then, across ~4 evidence-backed root-cause corrections, discovered the bindless half is NOT implementable in the SPIR-V backend IR passes:
- **The bindless accessor is chosen in `specializeTargetSwitch` (slang-ir-specialize-target-switch.cpp:41/49/52), resolving the core-module `getDescriptorFromHandle` `__target_switch`, from GLOBAL `getTargetCaps()` only.** Entry-point `[require]` is invisible to it. The fixer's 5 IR-side edits (emit-spirv ×2, peephole, byte-address ×3, spirv-legalize) only changed the capability *declaration* + handle *type*, NOT accessor selection → produced (local repro) a cosmetic UNUSED `OpCapability BindlessTextureNV` = regression vs master, and (buffer repro) forced field uint64 while accessor stayed uint2 → `CompositeExtract` on scalar → `vector_dce.cpp:306` assert. Verified by SPIR-V diff/op-counts: on master BOTH repros compile coherently (heap path, exit 0, `[require]` silently ignored); the crash was 100% self-introduced.
- **Making bindless truly work = propagate `[require]` into the specialization capability layer = Option A = module-global = the cross-EP LEAK jkwak raised (epBindless's `[require]` would flip epPlain's shared `getDescriptorFromHandle` to bindless).** csyonghe's "conflict→error" is the design answer, but it lives at the specialization/capability layer, NOT the fixer's IR passes. Out of green-lit scope; csyonghe/tangent-vector's design call.
- **DECISIONS (Main):** (1) Option B "fold at AST type-layout" REJECTED — reintroduces cross-EP ABI leak. (2) A warning-when-`[require]`-unhonored REJECTED (chose (A) NO diagnostic, match master) — a `[require]`-conditional diagnostic is exactly tangent-vector's "bad mental model" caution + pre-empts csyonghe's make-it-work direction + unclear blast radius (would fire on version case + every validation-only `[require]`). (3) Ship VERSION HALF only (repro 2: `[require(spirv_1_5)]`→Version 1.5; independent of specializeTargetSwitch, flows through `determineSpirvVersion` spirv-legalize.cpp:2482/2538; this IS csyonghe's honor-in-codegen model applied to version — not a cop-out) + explicit-only stamping + E50011 correction (latent MASTER bug: ray-tracing shader emitted wrong Version 1.0; namespace-unification → correct 1.4 → spurious E50011 stops; NOT June scope-broadening — verified `-dump-ir` shows zero decorations on that no-`[require]` test). Revert ALL bindless IR edits (diff vs master = zero bindless change); pin buffer repro as COMPILES-COHERENTLY regression test (guards the self-introduced crash). NO auto-close #11631.
- **Follow-up issue** = the authorized csyonghe design (propagate `[require]` bindless into `specializeTargetSwitch` cap set + cross-EP conflict→error), PLUS the open warn-vs-work UX question, tagged csyonghe/tangent-vector/jkwak/pdeayton. **NOT a passive "gap" doc** — pdeayton's greenlight predates the mechanism discovery, so the finding (bindless = specialization-layer, deeper than csyonghe's "backend IR passes" wording) must go BACK to the maintainers to re-confirm approach/scope: land version-now + bindless-focused-follow-up-PR, or hold. Their call.
- **slang-triager re-triage (msg 132) adjudicated:** correct that a *warning* contradicts csyonghe (agreed — chose no-warning); WRONG that bindless is a backend-IR-pass generalization (its cited gates spirv-legalize:1755/emit-spirv:1692/2717 are NOT where the accessor is chosen — fixer proved this; that edit = the cosmetic-cap+crash). Triager's prescription = csyonghe's wording, not the verified mechanism. Told to stand down again; fixer owns live thread.
**SHIPPED (2026-07-23 ~04:47Z) — version half in DRAFT PR #11633; bindless half = follow-up #12196.** Both artifacts VERIFIED by Main via API:
- **PR #11633** DRAFT, head e82bc51aa5 (rebased onto master), assignee jkwak-work, `pr: non-breaking`, plain `#11631` ref (no auto-close). Version half only: `[require(spirv_1_x)]` raises emitted SPIR-V version (explicit-`[require]`-sourced via `ExplicitlyDeclaredCapabilityModifier` + `determineSpirvVersion` namespace-unified to internal `_spirv_1_x` via file-scope `raiseSpirvVersionForAtom`) + E50011 latent-bug correction + buffer-resident coherence-pin test (`tests/spirv/entrypoint-require-bindless-texture-in-buffer.slang`, guards the self-introduced crash). ZERO bindless codegen change (all 5 IR-side files reverted). Full-suite: spirv 529/529, capability 94/94, reflection 41/41, glsl 337/337. codex PLAN/CODE/OUTPUT APPROVE (verbatim canonical block; comment-hygiene must-fixes only, all fixed). 3 maintainer-facing flags in body (explicit-only scoping · E50011-reframe-for-jkwak · bindless-deferred-to-#12196).
- **Follow-up #12196** filed (self-authored): (A) real fix = propagate `[require]` into `specializeTargetSwitch` cap set resolving `getDescriptorFromHandle`'s `__target_switch` (lives in `defaultGetDescriptorFromHandle`), per-entry-point reachability specialization + cross-EP conflict→error per csyonghe; (B) open warn-vs-work UX question. Tagged csyonghe/tangent-vector/jkwak/pdeayton. Triager triaged it design-gated/P2 (cmt 5054501023, legit — new issue ≠ #11631 stand-down).
- **CI red on #11633 = benign priority-yield** (draft `workflow_dispatch`: only `wait-for-human-priority`+`check-ci` "fail", all build/test jobs skipped; `pull_request` CI draft-skipped). Not real. See [[project_bot_pr_priority_yield_red_run]].
**FORWARD:** webhook-driven. #11633 stays DRAFT — ready-flip+merge OPERATOR-gated (surface for authorization when review green); do NOT auto-close #11631. #12196 awaits maintainer design decision. Chain at healthy rest.
**Gates:** pushes-to-draft NOT gated; ready-flip+merge operator-gated; no auto-close.
**Gates:** code pushes to draft NOT gated ([[feedback_pushes_not_gated]]); ready-flip + merge operator-gated ([[feedback_github_writes_operator_authorized]]); do NOT auto-close the issue — post `[Resolution]`, leave close to maintainers. slang-fixer owns the live technical thread; slang-triager echo-reports (redundant, harmless).
