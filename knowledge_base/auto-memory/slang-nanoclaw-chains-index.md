---
name: slang-nanoclaw-chains-index
description: "slang-coworkers/nanoclaw (platform-infra repo) live chain rows, spilled from MEMORY.md at the index floor 2026-08-04. Open before any nanoclaw PR/review work. The ROUTING rule and verb-split write path stay in MEMORY.md's routing section (inside the load cut)."
metadata:
  node_type: memory
  type: project
  originSessionId: 44a66732-cecb-47d6-bb2a-658e5e0ab91c
---

# `slang-coworkers/nanoclaw` — live chain rows

⛔**Spilled from `MEMORY.md` 2026-08-04 at the index floor** (rows were already past every
candidate bound ⇒ dark either way; spillover is the lever, not deletion). **Named
`slang-nanoclaw-chains-index.md` deliberately so the documented tail-cut recovery glob
`ls .../slang-*-index.md` finds it.**

⚠️**The two standing rules for this repo stay in `MEMORY.md`'s ROUTING section, which is inside
the load cut — do not move them here:** (1) nanoclaw PRs are handled **INLINE by Main**, never
routed to a `*-pr-approver`; (2) the write path is **verb-split** — both `gh pr review` and
`gh pr comment` are denied, `gh api .../issues/N/comments -X POST` works. See
[[project_nanoclaw_pr874_webhook_route_approver]].

## Review-pipeline infra defects

- ⛔⭐⭐⭐[**Shared `tmp/pr-diff.patch` clobbered by concurrent review runs**](project_review_pipeline_shared_tmp_diff_clobber.md) — **5th+ occurrence, 08-04 on slang#12269.** ONE root cause, TWO shapes: the LOUD `INTEGRITY-FAIL` false positive **and** a reviewer subagent *silently reviewing the wrong PR* (#12271) while believing it read the correct diff. ⭐⭐⭐**The recurring false positive trains you to dismiss the guard exactly when it is also producing wrong output.** Discriminator: ignore the shared file — compare the run's own `pr-diff.reference` + footer-pinned head/`diff sha256` against live head. **Fix = worktree-per-run isolation (already in operator queue; this is more evidence).** Interim: scope the diff filename by PR + head sha.
- ⛔⭐⭐⭐[**A CI retry that goes green on an IDENTICAL head may mean the WORLD changed, not that it flaked**](feedback_a_retry_that_passes_may_mean_the_world_changed.md) — measured 08-05 on #1090: attempt 1 red (`SyntaxError: Export named 'TOOL_ALLOWLIST' not found`), attempt 2 green, same SHA, **because sibling PR #1086 merged into `nv-main` 4 min later.** `ci.yml`'s composed-state merge resolves owned-path conflicts to `origin/nv-main`, so it overwrote `claude.ts` with a pre-sync copy while keeping the new test that imports from it. ⭐⭐⭐**Structural to the `sync-upstream.sh` fan-out: every leaf sync PR touching an nv-main-owned path is red until the nv-main PR merges, and nothing enforces the order.** Discriminator lives ONLY in per-attempt logs (`runs/{id}/attempts/{n}/logs`); `check-runs`/`runs/{id}/jobs` show the latest attempt only. ⚠️ONE case.
- 🔴[**`persistent: true` does NOT rescue an in-session monitor**](feedback_in_session_monitors_dont_survive_teardown.md) — measured 08-04: armed persistent, still died silently ⇒ necessary-but-insufficient; use a host `schedule_task` cron or run foreground in-turn. ⭐⭐⭐**And teardown kills the DELIVERY mechanism, not necessarily the WORK — a 5-day-silent review pass had COMPLETED with output intact on disk; check artifacts before writing a run off** (I diagnosed it as terminated and was wrong).

⭐⭐⭐**Lesson spun out of #1082 — [[feedback_a_correct_rule_with_an_unvisited_boundary]]**: a finding correct
everywhere you checked is indistinguishable from a complete one, and RE-READING CANNOT FIND THE GAP (it
operates inside the range that produced it) ⇒ once you've NAMED the axes, enumerate the PRODUCT and mark
measured cells. **Also holds the INVERSE case: an adversarial pass that confirmed me 8/8 then proposed ONE
weakening whose premise I checked and found FALSE — maximum borrowed credibility aimed at the one place you
want to hedge; refused, nothing retracted.** ⇒ **the same late input carried one finding worth ADOPTING and
one worth REFUSING, so neither "trust the probe" nor "distrust it" works — only per-claim verification.**
Linked here directly so it sits at depth 2 from `MEMORY.md`'s readable prefix.

## The `SendMessage`-collision sync fan-out (#1089 `nv-slang` + #1091 `nv-nanoclaw`), author nv-slang-bot

⛔⭐⭐⭐**ONE upstream commit (`81b18a9f`, upstream `nanocoai/nanoclaw#3187`) landed on TWO overlay sync
PRs and TWO sessions reviewed it independently, hours apart, with zero visibility into each other.**
Both claimed the SAME ordinals ("10th routing instance", "8th merge race") ⇒ **ordinals in this series
are concurrently incremented and stale on landing; state the INVARIANT (the rule fired again), never
the tally.** Divergent handling on identical content: #1089 posted a comment, #1091 did not.
⇒ **Before reviewing an `nv-*` sync PR, check for a sibling overlay PR carrying the same upstream
commit** (`gh pr list --repo … --search "<upstream title>"`) so the second pass adds something or defers.

🔴⭐⭐⭐**AND THE TWO ROWS DISAGREE ON THE ONE OPERATIONAL CLAIM — believe #1089's, it has the
measurements.** #1091's "stale container image ⇒ operator rebuild" is NOT established: `/proc/mounts`
shows **`/app/src` is its own `rw` mount** (siblings `/app/hooks`, `/app/scripts` are `ro`), matching
this repo's CLAUDE.md (*"agent-runner source is a shared read-only mount, not copied per group"*) ⇒ a
rebuild need not touch it; and the live blob (`d573a411`, mtime Jul 17, has `BASE_TOOL_ALLOWLIST` +
`extraAllowedTools`) matches **0 of 7 fork branches** (positive control: `TOOL_ALLOWLIST`=2 on every
branch) ⇒ **the running agent-runner corresponds to no branch of this fork, so this merge could not
have changed it.** ⭐⭐⭐*"The fix isn't live"* (measured) and *"the image is stale"* (inferred from an
assumed deployment model) are DIFFERENT CLAIMS — **check HOW the code gets in before naming what to
rebuild.**

- ✅🔴 **[#1089 → `nv-slang` — CLEAN/green, merged `dca75e66` +8min (self-merged by `nv-slang-bot`, a NEW actor — every prior sibling was `szihs`); reviewed INLINE, comment `5193525897`; 3 non-blocking notes](project_nanoclaw_1089_sendmessage_collision.md)** — ✅Right layer per the **pinned SDK 0.3.197's own types**: `disallowedTools` = *"removed from the model's context"* (`sdk.d.ts:1325`) vs `allowedTools` = *"auto-allowed without prompting… to restrict availability use `tools`"* (`:1302`). ✅Test non-vacuous, **2 negative controls failing from OPPOSITE directions**. ✅Baseline both sides: head 159/1/0 vs base 156/1/0, delta exactly +3 tests/+1 file/+5 expects. ✅Merged tree == reviewed head **BY TREE HASH** (`78d848da`), 3/3 blobs match; merge parents = (base, head) ⇒ true merge. 🟡**For the SDK-subagent case there IS no nanoclaw equivalent, so the hook's *"use the nanoclaw equivalent"* points nowhere** — `AgentInput.name` is documented *"addressable via `SendMessage({to: name})`"* (`sdk-tools.d.ts:435`) and `mcp__nanoclaw__send_message` returns `Unknown destination` for a subagent id (`core.ts:53` walks the destination map only); in-tree cost ≈ 0 (census: only CHANGELOG + the 2 touched files + unrelated Python `SendMessageArgs`). 🟡`TeamCreate`/`TeamDelete` still allowlisted = sibling half of the same feature (0 hits in `sdk-tools.d.ts`, control `AgentInput` present; **explicitly did NOT claim they don't exist** — both are in the CLI binary). 🟡Doc drift `docs/agent-runner-details.md:197` (pre-existing: already omitted `DesignSync`/`ReportFindings` at base). ⛔⭐⭐⭐**I measured the live-fleet gap and SPENT IT AS A SCOPE CAVEAT instead of reporting it** — *a fact recruited as a caveat gets spent, not examined*. ⛔⭐⭐**Bash went fully DEAD mid-review (`echo alive` → EMPTY, not an error); a must-hit positive control is the only thing that separated "not found" from "instrument down".** ⛔**FALSE ZERO caught in-flight: an upstream `gh api` returned 0/0/0 and `lines=0` — all four were `401 Bad credentials` (token reads the fork, not upstream) ⇒ no claim published about upstream's file.** RESUME = substantive human reply, or upstream action on the 3 notes; no regression live on `nv-slang`.

### #1091 → `nv-nanoclaw` (same upstream commit)

- ✅🔴 **[#1091 disallow built-in `SendMessage` — clean/green/faithful, MERGED mid-review (8th race, +7m37s); reviewed INLINE (10th instance of the routing rule); the real finding is that the fix is NOT LIVE in this container](project_nanoclaw_1091_sendmessage_collision.md)** — upstream `81b18a9f` moves `SendMessage` from `TOOL_ALLOWLIST` → `SDK_DISALLOWED_TOOLS` (the built-in addresses the SDK's *in-session subagents*; its name lures an agent off `mcp__nanoclaw__send_message`, and the refusal reads as "group never provisioned"). ✅Body's negative claim CHECKED: removal-from-allowlist-alone would be inert because `permissionMode: 'bypassPermissions'` (`claude.ts:677`) ⇒ the allowlist is auto-approve, not a gate. ✅New test **VERIFIED NON-INERT with 2 negative controls** (drop from disallowed → 1 fail; re-add to allowlist → 2 fail incl. the overlap invariant). ✅Merged blobs == reviewed head **BY HASH** (3/3); sync **faithful to upstream by blob hash** (test + CHANGELOG byte-identical; the lone `claude.ts` delta is a PRE-EXISTING fork `fs.rmSync` hunk — `diff base…head` on it = 0 lines). ✅`ci` all 17 steps green incl. `Container tests`. 🔴**LIVE-MEASURED: `/app/src/providers/claude.ts` in THIS container still has `SendMessage` in `BASE_TOOL_ALLOWLIST` and NOT in `SDK_DISALLOWED_TOOLS`** ⇒ the merge fixed the branch, not the running fleet. ⛔⭐⭐⭐**My bare `grep "'SendMessage'"` returned a HIT and that hit meant the OPPOSITE of "fix is live" — line 74 was the ALLOWLIST. For a fix that MOVES a string between two lists, the token's presence answers nothing; only the ENCLOSING BLOCK does** (re-ran scoped to the `const` block to disambiguate). ⚠️**`check` (nv-path-guard) NEVER RUNS on overlay branches** — the workflow + all 6 configs exist ONLY on `nv-main`, and a `pull_request` workflow runs from the PR HEAD; check-run census confirms (`nv-nanoclaw` PRs → `ci,label`; `nv-main` PRs → `check,ci,label` as the positive control) ⇒ **a branch listed in a workflow's `branches:` trigger is NOT evidence it runs there.** NOT routed, NOT commented, NOT escalated. RESUME = none; terminal. 🔴**Its closing line "only live item = stale container image (operator rebuild)" is RETRACTED — see the fan-out banner above: `/app/src` is a mount and the live blob matches no fork branch, so "stale image" was inferred, not measured.**

- ✅⭐⭐⭐ **[#1088 — the `nv-dashboard` twin of #1091, reviewed INDEPENDENTLY by a CONCURRENT session; CLEAN; premise reproduced LIVE with both controls](project_nanoclaw_1088_sendmessage_collision.md)** — same upstream `81b18a9f`, same 3 files +42/−3; merged 14:31:26 (**race #8**, +7.5min), 3/3 blobs == `nv-dashboard` tip **BY HASH**. ⭐⭐⭐**Pre-fix runner in this container = a live positive control: built-in `SendMessage` → coworker name returned the PR body's string VERBATIM (`No agent named 'x' is currently addressable`); → an SDK subagent id DELIVERED (`CONTROL-REACHED`)** ⇒ the two namespaces are genuinely disjoint, measured not read. Cost of the removal ENUMERATED: 0 fork files use `SendMessage`/`subagent_type` (control `mcp__nanoclaw__send_message`→6); `slang-pr-review/WORKFLOW.md:63,81` spawns background agents **without `name:`** and reads run-dir files ⇒ unaffected. 🔴**MY OWN ERROR, caught pre-post ⇒ [[feedback_a_branch_ref_is_not_a_commit_ref_after_merge]]: a 7-branch census BY BRANCH NAME ran AFTER the batch merged and showed the fix already everywhere ⇒ "no-op, nothing to review"; `baseRefOid` `2e0041fb`/blob `f00ed766` proved otherwise. 7 agreeing fetches on ONE defective key = one measurement repeated, failing in the direction that licenses STOPPING.** 🟡Posted note: `TeamCreate`/`TeamDelete` allowlisted but **0** refs in installed SDK 0.3.197 (controls 2/4). Comment `5193340245`, verb-split REST, one-per-batch (0 comments verified on all of #1086–#1091 first). ⭐⭐**Two sessions reviewed this batch in parallel and AGREED on the verdict from different evidence — the sibling's `bypassPermissions` point (allowlist is auto-approve, so removal-alone would be inert) I re-derived independently from the SDK's own `disallowedTools` doc ("removed from the model's context … even if they would otherwise be allowed") ⇒ it STRENGTHENS the finding: the disallow-list move is the load-bearing leg, not the allowlist edit.** ⚠️**Duplicate effort is the real cost — check `slang-*-index` for a sibling row on a same-batch PR BEFORE reviewing, not just before commenting.**

## The derived-image series (#1084), author szihs, base `nv-main`

- 🔴⭐⭐⭐ **[#1084 derived agent image (hardened base + fork layers) — OPEN/`CLEAN`/CI-green at post time, a REAL pre-merge gate; reviewed INLINE (9th instance of the routing rule); 2🔴 4🟡](project_nanoclaw_1084_derived_hardened_image.md)** — premise is sound (`pull.sh`'s lock-drift refusal is real; reinstall+re-stamp is the right answer). 🔴(a) **`COPY claude-trace-wrap` source is absent from ALL 14 remote branches** (enumerated, per-branch positive control `install-cli-tools.sh`→1 everywhere; not `.dockerignore`d, not gitignored; every other COPY source resolves 1/1/1/1/89) ⇒ build dies at line 59, **before the line-71 lock label that is the image's whole purpose**; ⭐⭐⭐**the cause IS the finding — the file exists UNTRACKED on the build machine, which is why the PR's "Verified on lego" block lists the layer as confirmed: a local success cannot distinguish "committed" from "in my working tree."** Same-feature half also missing (`@mariozechner/claude-trace` in a comment only; `cli-tools.json` has 2 entries, neither it; live container: `claude-trace` ABSENT) ⇒ stated fork delta is just codex. 🔴(b) **`IMAGE_NAME` never resolves to this install's tag** — ran the node snippet verbatim → always empty (`IMAGE_BASE` has **exactly ONE occurrence in the repo, that line**; control `CONTAINER_IMAGE_BASE`→5); falls through to an **order-dependent `grep '^nanoclaw-agent' | head -1`** ⇒ ⛔**on a multi-install host it builds over ANOTHER install's slug tag and stamps THIS checkout's lock sha, so that install's `pull.sh:276` check PASSES on foreign source — the guard is FORGED, not satisfied**; else falls back to `nanoclaw-agent`, a tag the host never spawns ⇒ **"built …" + exit 0 while the un-derived image keeps running.** ⭐⭐**Single-install works BY ACCIDENT (`pull.sh:294` already made the tag) — which is why local verification passes and is not evidence.** FIX = `source setup/lib/install-slug.sh` + `container_image_base()`, as `build.sh:26`/`pull.sh:38` do. 🟡`WORKDIR /workspace/group` restores a **v1** path absent in v2 (mostly inert: `CWD` hard-coded `index.ts:48`, no `-w`) · `chown -R node:node /app` unneeded (local Dockerfile never chowns `/app`; measured root:root 755 + files 666 is readable — **the agent runs from it right now**) and rewrites **495 MB** into a new layer · `IMAGE_SOURCE=derived` **collides** with the per-group label (`container-runner.ts:1796`) so `--status` shows this identically to a group with random apt/npm ⇒ vocabulary genuinely lacks a value · no test though `vitest.config.ts` already includes `container/*.test.ts` (sibling `cli-tools.test.ts`). ⭐⭐⭐**2 webhooks; the `synchronize` landed MID-REVIEW — re-fetched and re-diffed instead of carrying the verdict: script blobs BYTE-IDENTICAL, only the allowlist changed (+2), so measurements carried; and the path-guard fix verified WITH BOTH CONTROLS.** ⭐⭐⭐**My first guard run failed identically on BOTH heads with `ModuleNotFoundError: pathspec` — two exit-1s that meant NOTHING (false-zero shape); a venv + both controls turned it into a real result.** ⚠️No docker daemon here — disclosed, and both 🔴s are STATIC tree facts a build would confirm not decide. Comment `5192090652`. RESUME = szihs replies ⇒ follow-up offered (unmerged, nothing live to chase).

## The ncl-surface series (#1082), author szihs, base `nv-main`

- 🔴⭐⭐⭐ **[#1082 `ncl groups mcp-tools get/set` — MERGED 5.5min after the sync push (7th race); reviewed POST-merge, 2/2 blobs == `nv-main` BY HASH; 2🔴 4🟡, both 🔴 in the OPERATOR SURFACE](project_nanoclaw_1082_ncl_mcp_tools_verbs.md)** — (a) **`get` calls a restricted group UNRESTRICTED with `blocked: []`**: null `allowed_mcp_tools` falls through to `resolveTypeManifest(...).tools` (`container-runner.ts:356`), a real narrow list enforced on BOTH legs (proxy 403 + SDK `disallowedTools`), measured **17 tools** for `coworker_type` null/`default` ⇒ not vacuous — and ⭐⭐⭐**the repo already asserts the OPPOSITE (`runtime-guardrails.test.ts` fixture `allowed_mcp_tools: null` → 2-3 tools at :125/:173); NO test asserts "null = unrestricted"** ⇒ contradiction INSIDE the tree; (b) **`set` on an `is_admin` group never survives respawn yet returns "also persisted for future spawns"** — `is_admin` is tested FIRST (:330) and returns the full inventory at :340, so :343 is unreachable for admin; **reachable because both new guards constrain the CALLER (`groups.ts:151,158`), never the TARGET.** 🟡Legacy comma form diverges between the two readers (`get`→unrestricted, resolver→real restriction; latent, no writer emits it) · **an agent self-targeted `set` mints an approval card GUARANTEED to fail** (HOLD returns at `dispatch.ts:170` BEFORE the handler where the throw lives) · `--tools null` asymmetry (disclosed in its own `note` ⇒ design choice) · **no tests**, baseline byte-identical head vs base. ✅"No new MCP handshake" accurate; in-place token mutation correct AND right-reasoned (iterates ALL folder tokens; revoke wired on exit). ⭐**Two webhooks — the `synchronize` was PRETTIER-ONLY, so diff the heads before re-reviewing; the in-flight analysis carried over unchanged.** ⛔⭐⭐⭐**A PROBE RETURNED ~4 MIN AFTER I PUBLISHED (I had posted with an explicit "unchallenged" caveat): different harness (real `dispatch()`, real proxy 403 + 200 control) CORROBORATED all 4 findings — nothing retracted — AND found a boundary I had not visited: the `is_admin` divergence hits `get` TOO, in the OPPOSITE direction** (`get` has ZERO `is_admin` refs ⇒ for an admin group it names a tool `blocked` that is CALLABLE; null non-admin UNDER-reports, admin OVER-reports; one root cause, one fix). Follow-up comment `5190846434`. ⭐⭐⭐**A correct rule with an UNVISITED BOUNDARY is indistinguishable from a complete one — and re-reading my own review could never surface it, because my verification range never included that cell (I tested admin-`set` and non-admin-`get`; the gap was admin-`get`)** ⇒ **enumerate the CASE MATRIX over axes already identified (caller/target × admin/non-admin × read/write).** ⭐⭐**Labelling it "unchallenged" cost nothing and turned the late result into a strengthening — publish the confidence BASIS, not just the finding.** ⭐⭐**My PROMPT to the probe held a framing error ("proper subset") I never published — check what you SHIPPED before retracting; a flaw in working notes ≠ a flaw in the artifact.** Comments `5190812383` + `5190846434`. RESUME = szihs replies ⇒ ONE follow-up closes both 🔴 incl. the admin read path (offered; regressions LIVE on `nv-main`).

## approval-ledger join series (#1069 → #1075 → #1079), author szihs, base `nv-main`

- 🔴⭐⭐⭐ **[#1079 host-side verdict stamp — MERGED 11min after opening; reviewed POST-merge, 3/3 blobs == `nv-main` BY HASH](project_nanoclaw_1079_deterministic_verdict_join.md)** — moves the join off an LLM turn (right call; `head_sha` in the terminal payload is a REAL fix — without it `join_mode` always reads `exact` and #1075's tripwire is dead on arrival). 🔴**But the PR body's "the agent's own later call becomes a harmless no-op" is FALSE on any PR with ≥2 decision rows** — `res.changes===0` is STILL overloaded, so #1075's RELOCATION defect returns through a NEW DOOR: **the host stamp guarantees a first write, so a second event ALWAYS lands in the fall-through** and credits a superseded `WOULD_APPROVE` with `MERGED`. **THREE second-event triggers, none exotic:** the woken agent (SKILL.md still instructs the call), webhook redelivery (**the `rowId` guard dedups the WAKE; the stamp runs BEFORE it, unguarded**), and close→reopen→merge (`reopened` hits the `action!=='closed'` skip). ⛔**Green because every `store.test.ts` fixture holds ONE row (same too-small-fixture shape as #1075), AND the 2 new webhook tests MOCK `recordHumanVerdict` ⇒ they pin the call ARGUMENTS, not the join.** ⭐⭐**Both narrower one-line guards leave a hole — only a CASE MATRIX × 3 triggers found it**; the fix that closes all three drops `AND human_verdict IS NULL` from the picker and returns false when the last call is already stamped (verified: full suite `1339 passed`; unpatched, a 2-row fixture fails against the REAL store, unmocked). 🟡Verdict vocabulary: host writes `MERGED`/`CLOSED_UNMERGED` but 4 places document `APPROVED|CHANGES_REQUESTED`; **positive-control-verified that NO production path wrote `MERGED` at the base — only test fixtures did.** Comment `5189680092`. RESUME = szihs replies ⇒ ONE follow-up closes both this and #1075's still-live relocation.

- 🔴⭐⭐⭐ **[#1075 MERGED 8.5min after opening; reviewed POST-merge, 7/7 blobs == `nv-main` BY HASH](project_nanoclaw_1075_ledger_join_hardening.md)** — the new exact-path `human_verdict IS NULL` guard **overloads `res.changes===0`** ("no such commit" OR "already stamped") ⇒ a 2nd verdict event **RELOCATES** the write onto an older/superseded `WOULD_APPROVE`, crediting it `MERGED` = agreement. ⭐⭐⭐**Pre-PR bug was an OVERWRITE; post-PR bug is a RELOCATION — the write moved, it didn't stop — and it MANUFACTURES the exact over-credit bias that `join_mode` was added IN THIS PR to detect.** ⛔**Green because every exact-path fixture holds ONE decision: the new test's assertions stay TRUE under the bug (damage is on the OTHER row)** ⇒ ⭐⭐⭐**right assertion, too-small FIXTURE.** 🟡PR-body ordering case 2 overstated (`datetime()` truncates sub-seconds ⇒ `rowid` decides by INSERTION ORDER); cases 1+3 hold in both orders. Notes: `funnel.ts:370` = 4th "latest" spelling, unaligned · 931 auto-discovered (no registry edit — by design) but wants `dependsOn` · ~90/101 funnel lines = prettier churn. RESUME = szihs replies ⇒ follow-up PR (regression LIVE on `nv-main`).

## ci-gate → OneCLI vault series (#1071 merged-defective → #1072 parked), author szihs, base `nv-main`

- 🔴⭐⭐⭐ **[#1071 MERGED WITH A LIVE REGRESSION + #1072 parked](project_nanoclaw_1071_1072_ci_gate_onecli.md)** — `nv-main` @ `0bef0a22` passes `--paginate --slurp --jq` together, which **`gh` rejects client-side** ⇒ empty stdout ⇒ `try/catch → false` ⇒ `probe failed — not releasing`: ⭐⭐⭐**byte-identical to the 401 it was written to fix, so it reads as fixed while failing 100%** (was 98.3%). FIX = drop `--slurp` only; the multi-line parser it shipped already handles per-page `--jq`. Its test mocks `child_process` and asserts `toContain('--slurp')` ⇒ **pins the invalid flag as correct, green**. **#1071 merged MID-SESSION** (3rd race: #1066 −26s, #1068 +104s) ⇒ verified merged blob BY HASH. #1072: **reuse `mcp-registry.ts:53 getOneCLIProxyEnv()`, do NOT add config keys**; step 1 is really "connect GitHub for the host agent" (`app_not_connected` 401). RESUME = szihs's word (`"not fixing it right now"`).

## The metrics-instrument series (#1078), author szihs, base `nv-main`

- 🔴⭐⭐⭐ **[#1078 regression-quality — MERGED 2.8min after opening (5th race); reviewed POST-merge, blob == `nv-main` BY HASH; 3🔴 2🟡, NONE findable by reading](project_nanoclaw_1078_regression_quality.md)** — quality axis (regressions per 100 merged PRs, bot vs human). 🔴(a) **denominator silently empty ⇒ every `per100` prints `-` at EXIT 0** — my real run reproduced the body's numerator EXACTLY (84/12/14%/4-vs-4) but **the headline `4.0 vs 3.2` did NOT reproduce**; (b) **a per-PR fetch failure is byte-identical to "not a culprit"** — stub failing 1 of 3: coverage 100%→75%, bot 3→2, exit 0, silent, and at n=4 that's −25% on the published number; (c) **the fix that flipped the headline introduced over-attribution** — 7/10 probes mismatch, ALL false positives, bare `since` (temporal in English) fires on 8 of the 12 real attributions. 🟡mixed bot+human ⇒ counted BOT *and* human dropped, and **#11859 — the body's own motivating example — is the only multi-culprit issue in real data and it IS mixed** · numerator/denominator are different cohorts (7 of 13 culprits merged in a different month). ⭐⭐⭐**Two of MY OWN reads were wrong and corrected pre-post: a `}{` I called a page seam was the OneCLI 401 body, and the `][` fallback is DEAD CODE not broken code (settled with a local 3-page TLS API — gh 2.96 emits ONE merged array).** ⭐⭐**A finding contradicting the PR's own working numbers is probably about MY environment — build an auth-free probe, don't publish.** Comment `5189325006`. RESUME = szihs replies ⇒ follow-up offered (regressions LIVE on `nv-main`).

## The silent-turn series (#1081), author szihs, base `nv-main`

- 🔴⭐⭐⭐ **[#1081 — the guard CANNOT FIRE for the bug it documents, and its status has NO CONSUMER; commit 2 is the real fix. OPEN at post time (2nd no-race), reviewed INLINE (6th instance of the routing rule)](project_nanoclaw_1081_silent_turn_undelivered.md)** — (a) `deliveredNothing`'s `!event.text?.trim()` sits inside `} else if (event.text) {` ⇒ the documented `text: null` payload (`codex.ts:465` `resultText || null`) is falsy, branch skipped, **guard never evaluated**; measured truth table: `null`→*no exchange*, `''`→*no exchange*, `'  \n '`→`undelivered` (the ONLY shape it catches — the one Codex never emits), `'OK x'`→`completed` (control fired). ⭐⭐⭐**The optional chain is the CAMOUFLAGE — `?.` advertises null-safety exactly where null is unreachable.** (b) status → `onExchangeComplete`, an ARCHIVING hook **0 of 4 registered providers implement** (enumerated at `registerProvider` sites, not declaration syntax) ⇒ `if (!hook) return`, discarded; and redrive keys on `processing_ack` `bounced-*` which this never writes ⇒ **even when the guard fires, ack is still `completed`, trigger consumed, no re-arm.** ✅Commit 2 (`maybeRotateContinuation`) IS the real fix — motivating fixture → `"69.7d old > 14d cap"`, file renamed aside, fresh/unknown → `null`, wired at `poll-loop.ts:308`. ✅**No regressions, baseline MEASURED: 275 pass/1 skip/12 fail/9 errors byte-identical on head AND base `a5e5b487`** (the 12 are pre-existing critique-gate). 🟡No test on either commit (one `text: null` case would have failed instantly). ⭐⭐**Finding ONE reason a fix fails is not finding the ONLY one** — stopping at (a) would have made "hoist the guard" read as complete. Comment `5189917785`. RESUME = szihs replies ⇒ follow-up offered (hoist + `markBounced` + 2 tests). **Lesson spun out: [[feedback_a_null_guard_inside_a_truthiness_branch_is_dead]]** — read a guard's ENCLOSING BRANCH before believing its condition; a 4-row truth table over the input space beats branch-tracing prose. 🔴🔴**3rd webhook @ `e5ee5d6e`: rotation REVERTED, replaced by a runner-side idle cap that CALLS `continuationMaxIdleMs()` — DEFINED NOWHERE ⇒ `TS2304`, branch does not compile, CI red. Call is UNCONDITIONAL ⇒ `ReferenceError` on ANY continuation (fresh AND stale) for `codex`/`opencode`/`mock`; `claude` short-circuits. Verdict BLOCK, comment `5190482627`.** ⭐⭐⭐**My fresh-continuation CONTROL failed unexpectedly — that failure WAS the finding.** ✅Revert rationale beat my own review: `codex.ts` is skill-installed payload (`/add-codex` overwrites it ⇒ my validated fix was NOT DURABLE) and rotation **governed nothing** (opaque server-side threadId; only `clearContinuation` gates resume) ⇒ **my "✅ rotation works" was right about the OBSERVABLE, wrong about the EFFECT.** ⚠️**2 of my own errors: published test counts from a symlinked-`node_modules` tree AFTER correctly withholding its typecheck error, and misread `FETCH_HEAD` as `nv-main` (nearly a false "live on main" alarm) ⇒ [[feedback_quarantine_the_tree_not_the_one_symptom]].** ✅**4th webhook @ `7ae6477d`: helper restored, BLOCKER CLEARED — verified independently of the green CI (clean install, typecheck exit 0, suite back to baseline 342/3 from 340/5; the control that failed now passes; cap boundary 13d-kept/15d-dropped, disable knob works, no double-rotation).** Comment `5190558156`. 🔴Both original findings byte-identical across all 5 commits ⇒ **cause fixed, DETECTION half still inert.** 🟡Zero tests across 5 commits (3rd review, same missing test). ⭐⭐⭐**4 webhooks/4 heads, shape changed TWICE mid-review (a commit I validated got reverted; a compile break appeared then was fixed) ⇒ NEVER carry a verdict across a `synchronize` — re-fetch, re-diff, re-measure.**

## The KB dashboard-surface series (#1080), author szihs, base `nv-dashboard`

- 🔴⭐⭐⭐ **[#1080 `GET /api/kb-health` — OPEN at post time (FIRST no-race in this series), reviewed INLINE (5th instance of the routing rule); 2 🔴, and the PR'S OWN STATED VERIFICATION IS BLIND TO BOTH](project_nanoclaw_1080_kb_health_route.md)** — (a) **`server.ts:5445` splits on `'\\n'`, a 2-CHAR LITERAL** (verified by `cat -A` on the decoded blob, since a patch view / jq / a quoted review could each be blamed for the extra backslash; same-file census **1 double vs 14 correct**, line 1577 is right) ⇒ whole file collapses to one element, so `driftCount` is **1 for any `--quiet` output regardless of 1 or 30 findings, and 0 with any header line** — measured against `kb-doctor.py`'s REAL format `f"{state:<6} {name:<14} {msg}"`, NOT the `DRIFT:` shape the filter implies; (b) **`.kb-doctor.txt` has NO WRITER ANYWHERE** — `kb-doctor.py` is print-only (0 write-opens vs `kb-health.py`'s 2), exhaustive `git grep` on both branches finds nothing, and on live prod `/workspace/shared` (mounted — READ IT) `.kb-health.json`+`KB-HEALTH.md` are stamped **05:45 today** while `.kb-doctor.txt` is **absent** ⇒ `driftCount:0` means *unmeasured*, indistinguishable from clean. **Two INDEPENDENT branches — fixing the split still yields 0.** 🟡`available:true` on a zero-transcript run (#1068's live defect propagating into a MORE VISIBLE surface) · `trend` mixes `window_days` (3× phantom step at identical rate) · `?? 0` maps an absent sub-object to a real 0 · no test (vs 3,460-line `server.test.ts`/118 `/api/` assertions). ✅Field contract exact · real-prod end-to-end (4,999 B, `topPages:15`) · path agreement · degradation holds · targeting exact (`dashboard/**` EMPTY on `nv-main`). ⭐⭐⭐**Body verifies `{"available":false}` on lego — I REPRODUCED IT, and it is exactly the case that cannot see either 🔴: correct for the WRONG REASON, because the env has no data.** ⇒ **Ask what input would have made the author's own verification red; "data they didn't have" ⇒ it measured nothing.** Comment `5189544063`. RESUME = szihs replies ⇒ follow-up. **Unmerged ⇒ the only real pre-merge gate in this series.**

## The KB drift-tooling series (#1074 → #1076), author szihs, base `nv-main`

- 🔴⭐⭐⭐ **[#1076 kb-doctor — OPEN, CI all-green, reviewed INLINE (4th instance of the routing rule); 2 🔴 both in the INSTRUMENT](project_nanoclaw_1076_kb_doctor.md)** — (a) an **`ncl` transport failure is reported as `DRIFT` byte-identical to real deletion**: `returncode != 0 → missing`, so a down host socket / missing `tsx` yields *"13 committed task(s) not live"* + exit 1 on a tree where NOTHING was deleted (fires on a healthy repo — reproduced in-repo); (b) **an all-`SKIP` run is indistinguishable from clean and exits 0** — under `--quiet` (the intended cron mode) a bad `--repo` emits **ZERO bytes, exit 0**, and the default `~/slang-coworkers-prod/nanoclaw` is absent on this host ⇒ a green dashboard pill because nothing was measured. 2 checks `return` with **no finding at all** (row vanishes, not even SKIP). 🟡`recurrence`+`script` snapshotted but never diffed ⇒ **the PR's own motivating weekly-vs-daily failure passes as `OK`** · `glob`+`break` picks an arbitrary instance snapshot · `len()` labelled `B` (3rd instance). ✅Builder extraction sound (exactly 1 python block; `container-runner.ts:848` mounts `data/shared`→`/workspace/shared`) · glob depth matches `group-init.ts:177` · KB path list resolves 4/5 + self-reference. ⭐⭐⭐**A drift reporter NEEDS A THIRD STATE — `OK`/`DRIFT` cannot express *unmeasured*, and both 🔴s are that missing state leaking into a confident one.** ⭐⭐**Reading the file finds NEITHER — both came from RUNNING it with a stub `ncl` + fake trees, each probe paired with a control that could return the other answer.** Comment `5188841798`. RESUME = szihs replies ⇒ follow-up on 1+2 (unmerged, so nothing live to chase).

## The learnings-KB builder series (#1066 → #1067 → #1068), author szihs, base `nv-main`

- 🔴 **[#1068 kb-health telemetry — MERGED 104s after opening; reviewed POST-merge (`5181115163`), merged blob == reviewed head BY HASH](project_nanoclaw_1068_kb_health_telemetry.md)** — ⛔**2 instrument defects LIVE on `nv-main`: (a) the cheap-line gate skips prose citations before `cite_re` runs ⇒ `sessions_citing` 10 vs 14 true (29% under) AND it is the DIVISOR of tokens/citing-session, so both errors argue for CUTTING the KB; (b) a zero-transcript run is byte-indistinguishable from a healthy one in `KB-HEALTH.md` (warning stderr-only, `rc=0`) ⇒ under the planned 05:45 cron a broken glob publishes "nobody uses the KB, cost 0" — the script's own documented landmine, in its own output surface.** ✅Offline claim exact, glob landmine real+handled, no-leak holds, ALL shape baselines reproduce (23 over-cap / 47 no-TL;DR). 🟡atoms/day divides by days-WITH-atoms (7× on a sparse fixture) · deltas ignore `window_days` (+200% at identical rate) · `other` in headline not breakdown · corrupt history silently drops 90 runs (non-atomic write). RESUME = szihs replies ⇒ follow-up PR (offered).
- ✅ **[#1067 footer normalizer — reviewed INLINE 08-04, every claim verified, the `URL`-vs-`LINK` claim STRENGTHENED (1,572 links validation was blind to on the live corpus); prod==repo BY HASH; normalizer differentially tested with BOTH controls (fires on drift, byte-identical no-op on pass 2)](project_nanoclaw_1067_footer_normalizer.md)** — 3 non-blocking notes posted (`5180310143`): dead `LINK`, a `len()`-not-bytes figure, 2 unasserted invariants holding 0/47. CI green @`066859d`. **Merge is szihs's.**
- 🔴 **[#1066 kb-fold bounded](project_nanoclaw_1066_kb_fold_bounded.md)** — MERGED `c2a7639`; **the `superseded_by` persistence defect is STILL LIVE on `nv-main`** (`finalize()` reads a mark from a tree `build()` regenerates ⇒ retirement never persists, and it fails GREEN-LOOKING: the operator cannot distinguish "nothing retired" from "retirement isn't persisting"). Follow-up PR **OWED**. RESUME = szihs answers the persistence question, or land it with #1068's fix set.

## ⭐⭐ The lesson this series keeps teaching

**Merge races are the NORM here, not the exception:** #1066 merged **26s** before my review posted,
#1068 merged **104s** after opening. ⇒ **For szihs + `nv-main`, post-merge review is the DEFAULT
posture: recheck `merged`/`state` BEFORE drafting a verdict, and verify the merged blob equals the
reviewed head BY HASH** (an identity claim wants a hash, not a diff-read). A finding filed under a
merged banner is easy to miss — say plainly that it is a review of the merged tree, not a gate.

**MERGE-RACE COUNT IS NOW SEVEN** (#1066 −26s, #1068 +104s, #1071 mid-session, #1075 +8.5min,
#1078 +2.8min, #1079 +11min, **#1082 +5.5min after the sync push**). ⇒ **post-merge review is the
DEFAULT posture on szihs/`nv-main`, not the exception — plan for it rather than being surprised.**
⭐⭐**#1082 adds a second clock: the race can start from a mid-review `synchronize`, not just from
opening — so "it was open when the latest push landed" is no safer than "it was open when I started."**
⭐**Checking merge state at the START is useless — #1075 was `open`/`mergeable` when I began and
the race closed DURING review ⇒ the recheck must be IMMEDIATELY BEFORE POSTING.**

**And both #1068 🔴s were in the INSTRUMENT, not the reasoning** — the design calls were right;
the telemetry that measures the KB could publish a confident zero about it. **#1076 and #1078
repeat it: three consecutive PRs whose every defect is in the instrument, and in all three
READING THE FILE FINDS NOTHING — they only surface from RUNNING it with stub fixtures + a real
run, each probe paired with a control that could return the other answer.** Direct instance of
[[feedback_a_guard_can_be_inert_and_read_as_passing]] and
[[feedback_control_the_instrument_not_the_reasoning]].

- ✅ **[#1087 nv-coworkers upstream sync — built-in `SendMessage` disallowed; INLINE (~21st instance of the routing rule); clean, MERGED mid-review](project_nanoclaw_1087_sendmessage_collision.md)** — upstream `81b18a9f` moves `'SendMessage'` allowlist→disallowed + exports both lists + adds `claude.tool-collisions.test.ts`; the built-in addresses the SDK's *in-session subagents*, so a post-`create_agent` agent got `No agent named 'x' is currently addressable` and read it as "group never provisioned" while `mcp__nanoclaw__send_message` was never called. **Guard verified LIVE, not just "3 pass": the NEGATIVE CONTROL (revert ⇒ 4 fail) proves it is not inert, and consumption confirmed IN THE PR-HEAD ARTIFACT** — `disallowedTools:` at the SDK call site **and** a PreToolUse `decision:'block'` hook. Suite 345 pass/3 fail = the known pre-existing `dispatchResultText — critique-gate` trio, **proven pre-existing by measuring BOTH states (reverted = 4 fail incl. the guard), not assumed**. ⚠️First run's "1 fail/1 error" was `Cannot find module '@anthropic-ai/claude-agent-sdk'` — **missing deps, NOT a red suite**; `bun install --frozen-lockfile` first. ⚠️**SCOPE: I ran the suite in a clone at `4c41224f` = `nv-main`, NOT this PR's branch** (it carries fork-only code the PR head lacks: `detectIssueClose`, `parseAllowedMcpTools`, usage telemetry, `[1m]` window) — the **test file is byte-identical** and the `SendMessage` hunks match in substance, so the guard result transfers but a whole-suite count does not. 🔴**MERGED 14:28:29Z by `nv-slang-bot`** (nightly REST auto-merge), commit `7568f80a`. ⭐⭐**A 404 on a head ref mid-review is a MERGE signal, not a fetch error** — `?ref=<branch>` succeeded, then 404'd minutes later because the repo auto-deletes merged head branches ⇒ **fetch by COMMIT SHA in an auto-merging repo.** ⚠️`gh pr view --json merged` is not a valid field (use `state`+`mergedAt`). No comment posted (clean self-verified bot sync). RESUME = none; on redelivery state is MERGED, do not re-review.
- 🔴 **[#1092 critique-gate self-heal / close the fail-open — 2 reproduced findings; INLINE (~22nd instance); author `szihs` is HUMAN not a bot](project_nanoclaw_1092_critique_gate_resolved_wedge.md)** — ⭐⭐⭐**A `synchronize` mid-review EVAPORATED my headline finding**: first head `b3e544ec` failed CI on `Duplicate migration versions: 931`; author renumbered 931→932 and CI went green at `85994cd1` ⇒ **re-fetch head + re-measure everything on any `synchronize` before publishing.** **(1)** Removing the 30-min timeout wedges a session forever when a `resolved` escalation file is left behind — **nothing in the tree deletes it**; host `index.ts:286` skips any `resolved` file while hook `:288` tests **existence only** ⇒ denies forever, never opens a fresh request; **reachable on the NORMAL path** (host itself writes `resolved:'self-healed'` at `:305`). Base-vs-head A/B isolates it to this PR. **(2)** Consumed one-shot bypass stamps `failed_open_at` onto a file already `resolved:'approved'`, and the `resolved` guard at `:286` precedes ingestion at `:293` ⇒ **the admin-authorized release is the ONE release class never recorded**, contradicting the PR's own "durable trace" contract. One root cause: `resolved` doing double duty, read oppositely by hook and host. ⛔**MY HARNESS FAILED 3× — every catch came from a control, never from reading code:** `CRITIQUE_GATE_ACTIVE=0` **in my own env** silently short-circuited the hook so every run exited 0 having never reached the gate (caught only because a **positive control that should have DENIED didn't**); `EXIT=$?` captured a preceding `echo`; `| head -3` SIGPIPE'd the status. ⇒ **run the positive control FIRST — an unvalidated "clean" result reads exactly like a finding.** ⚠️`search/code` for `last_critique_at` returned **total_count 0 = FALSE ZERO** (local grep: 6 hits, so auto-retraction is NOT inert). ⚠️Whole-suite "19 failed files / 0 failed tests" was **MY env** (borrowed `node_modules` missing `js-yaml`; branch-installed `dashboard.js` absent) — **that signature means IMPORT errors**; CI on `85994cd1` green is the authority; disclosed in the posted comment. PR's own 3 suites 78/78 ⇒ **untested gaps, not broken tests**; wrote failing tests for both in the PR's own harness. Comment `5193317699`. RESUME = maintainer owns merge; if head still `85994cd1`, do not re-review.
