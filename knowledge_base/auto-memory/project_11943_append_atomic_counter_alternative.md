---
name: project_11943_append_atomic_counter_alternative
description: PR
metadata: 
  node_type: memory
  type: project
  originSessionId: 933df625-cd2e-460d-b5dc-099e7cefd3f7
---

**#11943** (human PR by `ramang-unity`, "[Metal][WGSL] Enable AppendStructuredBuffer"; `jkwak-work` assignee) drew four `@nv-slang-bot` analysis questions from jkwak — all answered by slang-fixer with receipts, zero code changes:
1. r3530768046 → r3532981911: the `isSPIRV()` gate in `declareVars` (slang-ir-legalize-types.cpp) is target-SPECIFIC legalization, runs on the per-target linked module inside `linkAndOptimizeIR`, never on serialized front-end IR → target-agnostic invariant holds.
2. r3532955506 → r3533007063: WGSL `Atomic<>` promotion wraps the whole `getElementType()`, but only the `int` append counter is atomic-reached, so the data struct is untouched.
3. r3533046956 → r3533062482: counter is a separate synthesized `int` buffer (internal but a real bound resource); keying legalization on `IRHLSLStructuredBufferTypeBase` (not `AppendStructuredBuffer`) is intentional-and-correct — append lowering runs BEFORE WGSL legalization, and WGSL correctly needs `atomic<T>` for ANY atomically-accessed structured buffer.
4. r3533141932 → r3533168188: jkwak's "synthesize the counter atomic in the first place" is a legit cleanliness refactor, not a correctness/target-agnostic fix (both passes already run post-target).

jkwak then directed (r3535180452) a **new draft PR** of that alternative, assigned to him, to compare before landing #11943.

**Outcome:** slang-fixer opened alternative **draft #11972** (branch `alt/append-atomic-counter`): counter element `int`→`Atomic<int>` at lowering time. WGSL byte-identical, SPIR-V `OpAtomicIIncrement` validates, HLSL untouched (gated off), Metal = intended diff (cast-free `atomic<int> device*`). `report_pr_created` done; reply on r3535180452 → r3536809940 asks jkwak to **self-assign** (did NOT `--add-assignee` — see [[feedback_admin_standing_rules_precedence]], my id=12 veto of my own auto-assign directive).

**CORRECTION (07-07, supersedes earlier "unreachable" claim):** The WGSL `legalizeAtomicCounterBuffersForWGSL` promotion pass is **load-bearing, NOT theoretically-unreachable.** Earlier reasoning (in analysis reply #3 and #11972 body) claimed a user `RWStructuredBuffer<int>`+atomic can't reach WGSL via the E36107 capability gate — that gate only applies to `InterlockedAdd`. **`__atomic_add` has NO capability gate and DOES reach WGSL:** a module combining `AppendStructuredBuffer` + a user `RWStructuredBuffer<int>`+`__atomic_add` gets the USER buffer promoted to `array<atomic<i32>>`; deleting the pass emits invalid WGSL (`atomicAdd` on non-atomic `array<i32>`). codex independently reproduced. So atomic-at-birth handles the append counter, but the pass is still needed for user buffers under `__atomic_add`.

**State:** BOTH draft-held; jkwak owns the compare-and-decide between #11943 and #11972. No ready-flip / merge without operator gate ([[feedback_github_writes_operator_authorized]]). Recovered from a OneCLI blip mid-build (interrupted at 828/1169; edits intact; rebuilt synchronously). #11972 CI-red is the benign draft priority-yield ([[project_bot_pr_priority_yield_red_run]]). Reopens on a substantive human comment on either PR.

**07-07 rebase:** ramang-unity flagged #11972 was behind; fixer rebased onto #11943's current head `4df19fa2` (force-pushed head `379518603b`), keeping ALL his new COMPARE_COMPUTE+METALLIB coverage and flipping only the one Metal FileCheck line to the cast-free `atomic<int> device*`. His new `requiredLoweringPassSet.appendConsumeStructuredBuffer` gate around `legalizeAtomicCounterBuffersForWGSL` COMPOSES cleanly (pass now skips the already-atomic counter → no-op; double-narrowed). All 4 targets re-verified, codex re-approved. Reply issuecomment-4904740756. #11972 is current vs #11943, not stale.

**07-07 review round (RESOLVED — jkwak decided full removal):** jkwak left two #11972 directives. (a) inline "remove this legalization" (r3537743476) — fixer did NOT blindly remove; investigated, found the pass load-bearing (the `__atomic_add`-reaches-WGSL correction above), posted repro + asked keep-for-user-buffers vs remove-and-diagnose (reply r3537878111). (b) "add a WGPU test" — added `-wgpu` COMPARE_COMPUTE_EX to `tests/hlsl/append-structured-buffer.slang`. Good instance of surface-don't-guess discipline: refused to execute a maintainer "remove it" on evidence it would break user-atomic WGSL, THEN executed once jkwak decided with rationale.

**jkwak's design call (r3537916280):** silent user-`RWStructuredBuffer` atomic promotion is **"not supported"** — users must declare `Atomic<int>` explicitly and own the perf tradeoff — and `requiredLoweringPassSet` "shouldn't be needed either." So: full removal.

**SHIPPED (commit `524bbcdb11`, +25/−108, reply r3538033338):** removed the entire WGSL atomic-counter promotion (`legalizeAtomicCounterBuffersForWGSL` + `isAtomicOp`/`collectAtomicStructuredBuffers`/`promoteAtomicStructuredBuffer` + gated call, −88 lines), reverted `legalizeIRForWGSL` to 3-arg signature, reverted the `slang-emit.cpp` call — back to pre-#11943-gate form. Append/consume *lowering* gate (emit.cpp:1636) untouched. Verified pass-gone: WGSL append STILL emits `array<atomic<i32>>`+`atomicAdd` (atomic-at-birth alone produces it); user `__atomic_add` on plain `RWStructuredBuffer` now emits `array<i32>` (no silent upgrade = jkwak's intent); SPIR-V/Metal/HLSL unchanged. codex approved. **Net: #11972 is now the strongest version of jkwak's original target-agnostic concern — NO target-specific atomic-counter pass left; atomicity lives solely in target-agnostic lowering.**

**07-07 review round 3 (answered; PR now NON-DRAFT):** jkwak left 3 more inline comments; fixer answered all (replies r3538255437 / r3538255552 / r3538255645), HEAD `38eb7ce7c9`, real `pull_request` CI GREEN. (1) dropped `-slang` from both `-wgpu` test directives (no-op under explicit `-wgpu`). (2) On jkwak's "drop the isSPIRV guard / decorate all targets": that guard is **ramang-unity's code** (commit `53fbad6009` = #11943 base; the fixer's commit never touched `slang-ir-legalize-types.cpp` — GitHub renders it `+` only vs master). Counterfactual (unconditional decoration) **hard-aborts Metal** ("no outer func at use site for global") + churns WGSL global order; only SPIR-V consumes `CounterBufferDecoration` (slang-emit-spirv.cpp:6682 → OpDecorateCounterBuffer). Guard is load-bearing → fixer deferred the drop-decision to #11943 with jkwak+ramang-unity, offering the evidence.
- **#11972 flipped NON-DRAFT by maintainer jkwak-work at 15:33** (timeline `ready_for_review`, actor jkwak-work, no revert — VERIFIED by Main). NOT a bot breach ([[feedback_drafts_only_guardrail]]); fixer did not flip. Bot-merge still operator-gated ([[feedback_github_writes_operator_authorized]]).
- Fixer self-flagged: it `gh workflow run`-dispatched CI on the now-non-draft PR → one benign cosmetic-red priority-yield run (28883599218); real CI SUCCESS ([[project_bot_pr_priority_yield_red_run]]). Recorded the lesson itself.

**07-07 round 4 — jkwak directed the isSPIRV-guard removal (SHIPPED):** jkwak escalated from questioning the guard to supplying a verbatim 2-file patch (issuecomment-4908468587) — the exact root-cause fix the fixer's counterfactual pointed at. So the deferred "guard drop belongs on #11943" resolved: jkwak (maintainer) took ownership and directed it onto #11972. Fixer applied verbatim, pushed HEAD `b64653eb8a`, replied issuecomment-4908738203:
- (1) `slang-ir-legalize-types.cpp`: `CounterBufferDecoration` now UNCONDITIONAL (isSPIRV guard gone).
- (2) `slang-ir-explicit-global-context.cpp`: `IntroduceExplicitGlobalContextPass` rewrites the decoration's counter operand to the struct field key instead of `findOrCreateContextPtrForInst` on a global→global use — fixes the exact `no outer func at use site` Metal abort the counterfactual measured.
- Re-verified fresh binary: Metal no-abort + cast-free `atomic<int> device*`; WGSL `array<atomic<i32>>`; SPIR-V `OpAtomicIIncrement`+validates; CUDA `atomicAdd`; slang-test append 5/5. codex approved. Real `pull_request` CI auto-fired (fixer did NOT manually dispatch — drafts-only rule applied). Fixer surfaced codex's one advisory (broad `as<IRDecoration>` vs `as<IRCounterBufferDecoration>`) to jkwak as FYI, kept patch verbatim — **jkwak later (issuecomment-4909813969) confirmed he's happy keeping the fix on ALL `IRDecoration`, closing the advisory with no code change.** jkwak also resolved the isSPIRV-guard review thread (r3538082688) after the applied fix.

**State:** #11972 non-draft, atomicity now 100% target-agnostic with ZERO special-case (maximally clean version of jkwak's original concern). jkwak has been directing fixes onto #11972 (not #11943) — signals he leans toward #11972 as the PR to land, but the compare-and-decide vs #11943 is still his to formalize. Bot-merge operator-gated. Reopens on substantive comment on either PR via webhook.
