# Runtime deps for slang-mcp. Derived from pyproject.toml [project]
# dependencies — keep in lockstep (RC-L3). Dev deps (pyright, pytest*,
# ruff) belong in pyproject's `dev-dependencies`, not here.
aiohttp>=3.9.0
aiosqlite>=0.19.0
anyio>=4.5
certifi>=2025.11.12
click>=8.1.0
discord.py>=2.3.0
httpx>=0.27.0
mcp
pydantic>=2.0.0
python-dotenv>=1.0.0
rich>=13.7.0
