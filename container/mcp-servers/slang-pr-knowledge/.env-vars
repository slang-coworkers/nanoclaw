# Runtime env vars for slang-pr-knowledge. This server is a read-only
# local SQLite index over merged Slang PR data — it needs no credentials,
# so this declaration stays empty (RC-M4). Previously it listed
# ANTHROPIC_MODEL, which the server does not actually read, and that
# marked the server as auth-required in the OneCLI gateway and caused
# placeholder injection on startup.
