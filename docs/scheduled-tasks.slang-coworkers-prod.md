# Scheduled tasks — `slang-coworkers-prod`

Generated by `scripts/dump-scheduled-tasks.py`. Definitions only — runtime
state is excluded so a diff here means a task's instructions changed.

Snapshot id: `7a342cf84fc82eb526723884cc85c4d47a1e4d43c68335cbf6e1ae5b1d581025`

That id must equal `snapshot_id` in the JSON beside this file. If it does not,
the two were published by different runs and one of them is stale — run
`scripts/dump-scheduled-tasks.py --check` to confirm, then re-dump.

26 task(s), complete — the generator exits non-zero rather than
writing a partial snapshot, so a shorter file here means a task was deleted.

## `ci30098-blocker-clear-5975`

- schedule: `*/20 * * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/.ci_falcor_gate.sh
```

Prompt:

```
FALCOR-CI APPROVAL DEADLOCK SENTINEL (blocker-agnostic, ROTATION-keyed).

WHAT IS BEING WAITED ON: the PROPERTY "no shader-slang/slang ci.yml run is parked in status=waiting on the falcor-ci / ci-approvers approval gate" — NOT any single run id. Do not re-pin this to a run id (v1 did; when #30098 left `waiting` its `status != waiting` test became permanently true and it fired BLOCKER_CLEARED forever, including while a DIFFERENT run had re-formed the identical block).

SCRIPT REASONS ARE NOW (v3, 2026-08-09T11:5xZ): REPO_QUIET and BLOCKER_ROTATED. `BLOCKER_CHANGED` (any set inequality) is GONE — see the v2 defect below. `wakeAgent:false` on additions and on removals that leave a prior blocker parked.

WHY v2's set-inequality wake was WRONG (measured, do not re-derive): the waiting set is a SHARED resource — every human PR whose run reaches test-falcor joins it, and the author's next push concurrency-cancels that run out again. Each such PR therefore produced TWO wakes with nothing actionable in either. Measured 2026-08-09: #30171 (31307271324, fknfilewalker, fix-assoc-default-init-and-matrix-layout) joined the set 10:30:47Z (wake: count 1->2) and was cancelled out 11:22:07Z (wake: 2->1) — while the real blocker #30154 never moved. I reported "BLOCKER_CHANGED (added)" at 10:41Z with a figure (2 waiting) that was false 41 minutes later. LESSON: key the wake on the property you would ACT on (the previously-reported blocker is gone => rotation, or the set is empty => quiet), never on "the set differs".
v3 rotation test = new set shares NO id with prev. Overlap of even one id => the blocker already reported is still parked => silent. Armed with 13 controls (same-set / addition / removal-with-overlap / true-rotation / empty / first-fire / two substring-collision traps with real 11-digit ids / gh-failure / state-preserved-on-failure / three real-id genuine-overlap cases). A gh failure is NEVER a clear. Test seam: FALCOR_TEST_JSON + FALCOR_TEST_STATE.

MEASURED, still true as of 2026-08-09T11:5xZ:
- CURRENT SOLE BLOCKER: #30154 (31258367401, fix/issue-11981, 72a3b5025d, nv-slang-bot workflow_dispatch attempt 3). Reached test-falcor 01:23:05Z, falcor-ci deployment 5814389468, statuses = [waiting] only (no error, no approval), reviewers=[ci-approvers], wait_timer 0, current_user_can_approve=false, /approvals empty. All 30 other jobs on the run are conclusion=success — the run is 100% green except the gate. Parked 10.6h and counting.
- #30098 (31179559787, fix/issue-12383) was NEVER approved: cancelled 00:59:24Z after 36.4h by ci.yml's own concurrency group `${{ github.workflow }}-${{ github.ref }}` + cancel-in-progress, because a new bot workflow_dispatch on the SAME branch was created 00:59:22Z. (A pull_request run would not collide: its github.ref is refs/pull/N/merge.) Deployment 5805649902 went waiting -> error. SO: "left waiting" != "approved" — REPO_QUIET means the yield-lock is momentarily open, NOT that the gate was fixed.
- Anti-starvation works but only evaluates on rerun: #30154 aged 12.0h and its 00:57:24Z gate printed "Waited 12.0h (>= 12.0h ceiling); escalating priority and proceeding". ci-retry-yielded-bot refuses to rerun while anything is `waiting` (30 consecutive "CI is still active" fires 13:17Z->00:59Z), so reruns only arrive from an nv-slang-bot[bot]-token caller. Once escalated a run proceeds, re-parks on falcor-ci, and the blocker ROTATES to a newer run. That rotation is the standing condition.
- A fresh dispatch stays counterproductive: wait-for-priority classify_blockers puts bot-authored #30154 in older_bot (30154 < any new run number) -> a new dispatch yields to it AND resets its own age to ~0.
- Human refs DO get approved (gh-9182, gh-4422, gh-7262, gh-readonly-queue/pr-12373 all waiting->queued->in_progress->success on 2026-08-08); one bot ref did too (fix/issue-12396). Bot refs are slow, not categorically refused.
- Maintainer fixes still OPEN, both untouched since 2026-08-07: PR #12425 (jkiviluoto-nv, "ci: give test-falcor room for the approval gate, and fix its comment", non-draft, upd 08-07T19:48Z) and PR #12424 ("[STANDBY] Revert the Falcor bridge switch (#11915)", draft, upd 08-07T13:38Z). The gate landed as #11915 / eea5b2753 on 2026-08-07T10:04Z.
- Affected drafts, all bot-authored, all still blocked at their current heads: PR#12434/fix-issue-12386 (bdb52eb6ab), PR#12014/fix-issue-11981 (72a3b5025d — holds the gate now), PR#12429/test-property-accessor-coverage-12231 (240984c9ce), PR#12382/fix-issue-12371 (115185a041), PR#12378/fix-issue-12367 (9a24322dd3), PR#12310/fix-issue-12307 (79297fa854), PR#12408/fix-issue-12383 (49dbe8c165), PR#12423/fix-issue-12397 (6c63972f2a).

ON reason=REPO_QUIET (the only resume signal): re-verify with `gh api "repos/shader-slang/slang/actions/workflows/ci.yml/runs?status=waiting&per_page=100"` before acting — a stale read must not authorize work. If genuinely quiet, tell slang-fixer on thread_id=gh-issue-shader-slang/slang-12386 that the repo-wide falcor block is clear and it may make EXACTLY ONE dispatch on #12434. The other 7 branches belong to other sessions: route each through its owning coworker on that branch's canonical gh-issue thread, never past a tier. Then cancel this task.
ON reason=BLOCKER_ROTATED: every run I last reported is gone and different runs hold the gate. Re-measure (run id, branch, deployment id, deployment statuses, /approvals, current_user_can_approve), report the new blocker to orchestrator-dashboard, do NOT authorize any dispatch, keep this task armed.
Silent otherwise. If a wake brings nothing that changes what someone would DO or BELIEVE, emit NOTHING — bare prose outside <message> STILL DELIVERS; only <internal></internal> is silent. A correction to a figure I already published DOES ship.

NUDGE BUDGET: at most TWO nudges to orchestrator-dashboard; say which nudge it is. NUDGE 1 SPENT 2026-08-09T11:5xZ (#30154 / deployment 5814389468, 10.6h parked). One left. The ask is: approve the currently-parked falcor-ci deployment, or land #12425 / #12424. NEVER approve or cancel a deployment yourself and never ask a maintainer on GitHub — operator-gated human action.
```

## `esc6319-maintainer-gate-2719`

- schedule: `0 */6 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/.esc6319_gate.sh
```

Prompt:

```
MAINTAINER-REPLY FALLBACK for shader-slang/slang#6319 (PR #11885, branch fix/issue-6319 @ f7971ca067).

WHY THIS EXISTS: on 2026-08-07 I authorized an escalation of a language-semantics question to a maintainer (jkwak-work, assigned #6319) as PR comment #issuecomment-5213366249. That chain's resumption is webhook-driven with NO polling armed, so if the maintainer never replies, NOTHING resumes and it sits blocked indefinitely. A gate on an external party's reply has no resume trigger I control — this is that trigger.

⛔ OWNERSHIP — MEASURED 2026-08-07, DO NOT RE-DERIVE FROM RECALL: the owner is agent group ag-1780667166439-vmjrwe = slang-fixer, session sess-1782900646868-gz88if, on thread gh-issue-shader-slang/slang-6319. slang-fixer runs ELEVEN concurrent sessions, one per issue thread; the session working #12397 has NO knowledge of #6319 and correctly cannot answer for it. Address slang-fixer with thread_id=gh-issue-shader-slang/slang-6319 VERBATIM — that is what routes to the owning session. A thread-less send, or one on another issue's thread, mints/reaches the wrong session and produces a confident 'not my work' reply. (That exact misroute happened: I sent a #6319 question on thread -12397.)

THE BLOCKED STATE (do not re-litigate): for SV_Barycentrics with an inner `linear` under an outer `noperspective`, SPIR-V emits two builtins and GLSL emits one. The fixer chose the conservative reading (one binding => reject) because under-reporting ships the invalid output #6319 exists to prevent. Codex wants slang-ir-glsl-legalize.cpp:934-945 fixed first so innermost-wins holds on both. Two facts decide it: the divergence REPRODUCES ON UNMODIFIED MASTER (pre-existing, not introduced), and NO TEST IN THE REPO exercises the inner-override pattern — so codex's remedy means changing GLSL backend semantics for unspecified, untested behaviour inside a front-end bugfix. OUTPUT_REVIEW stays must-fix; reviewer dispatch and [Fix Report] stay gated. Sitting blocked was CORRECT — do not pressure the fixer to reword and clear its own control.

OPEN SUB-QUESTION for the owning session (asked 2026-08-07, not yet answered because it was misrouted): a sibling chain (#12397) dissolved an apparent semantics escalation by finding the behaviour already had a SECOND SPELLING (`__require*` intrinsics lowering to requireSPIRVExecutionModeOnReferencingEntryPoints, vs the attribute path writing dstID). Ask the -6319 session whether a second spelling exists there too. If yes, the escalation should be WITHDRAWN before the maintainer spends time on it. If no, it stands as filed. This is cheap and worth doing on the first wake regardless of maintainer silence.

DATA you wake with: data.reason is human_reply (data.replies lists login@ts#id), pr_closed, or an alert that the escalation comment is missing.

ON reason=human_reply: read it on merits. If it answers the semantics question, forward the ANSWER to slang-fixer on thread_id=gh-issue-shader-slang/slang-6319 — QUOTE the maintainer, never paraphrase a semantics ruling. If it asks the fixer something, forward the question. Then cancel this task.
ON reason=pr_closed: establish whether the fix landed or was dismissed, report to orchestrator-dashboard, cancel this task.
ON alert (comment missing): the escalation is not public — tell orchestrator-dashboard immediately; it may need reposting.

NUDGE BUDGET: at most TWO nudges to orchestrator-dashboard across this task's life, and only after 4+ consecutive silent wakes (~24h of maintainer silence). Say which nudge it is. NEVER nudge the maintainer directly on GitHub — operator-gated write into a human's inbox.
NEVER flip the draft ready, approve, or merge. All three are human acts.
If a wake brings nothing new, emit NOTHING — bare prose still delivers a message; only <internal></internal> is silent.
```

## `i11963-draft-gate-ecaa`

- schedule: `*/30 * * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/guard-11965.sh
```

Prompt:

```
SLANG #11963 / PR #11965 DRAFT-GATE TRANSITION — the gate this chain has been stuck on for 30 days just moved. Context: module-scope lambda name collision (_slang_Lambda_ bare name, no counter); root-caused + patched + regression-tested in PR #11965, which has been held as a DRAFT with 0 reviews since 2026-07-07. That draft-invisibility caused maintainer jkwak-work to RE-TRIAGE the issue on 2026-08-06 (nothing was in any review queue: timeline has 2 ReviewRequestedEvent, ZERO ReadyForReviewEvent). slang-triager answered with cmt 5199246567 on 08-06 01:10Z and re-verified the repro still fails on current master with a two-state differential on identically-built binaries (pristine => exit 255/E30200 on all 3 shapes; +patch => exit 0; one-lambda control => 0 both). Patch applies clean (behind 193/ahead 1, diverged, NOT conflicting).

The attached data.why names which transition fired. Act on it:
- ready-flipped / reviews>0 / pr-comment => a human engaged. Route to slang-triager on thread gh-issue-shader-slang/slang-11963 (triager owns this chain's peer-wire to slang-fixer — do NOT dispatch slang-fixer directly). Recommend a rebase onto master BEFORE merge so CI runs against current master, not a 193-commit-stale base.
- MERGED / state=closed => confirm issue #11963 actually closed via the Closes link; if it did not auto-close, say so. Then cancel this task (ncl tasks cancel i11963-draft-gate) and update the memory leaf project_11963_module_scope_lambda_collision.
- issue-comment-from:<human> => a substantive human comment RE-OPENS the chain (per the [MUST] rule); read it on its merits and route. A thanks/ack closes again explicitly.
NEVER flip the draft ready or merge autonomously — both stay operator-gated. Report to orchestrator-dashboard with the 5-bullet.
```

## `i12092-scope-guard-a7e2`

- schedule: `0 */6 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
#!/usr/bin/env bash
# Guard for shader-slang/slang#12092 — held on scope pending csyonghe/jkwak-work.
# Wakes the agent ONLY on: a new non-bot comment, a state/assignee change,
# or the silence deadline passing (escalate to operator).
set -uo pipefail

BASELINE_HUMAN_CMT=5221466013   # tangent-vector scope answer (handled via webhook 08-07; was csyonghe 5212939173)
BASELINE_COUNT=4
BASELINE_ASSIGNEE="jkwak-work"
# Sorted, comma-joined. Office-Tess arrived with tangent-vector's comment (08-07);
# a label change is a maintainer re-routing signal worth waking for.
BASELINE_LABELS="Office-Tess,Office-Yong,reproduced"
DEADLINE="2026-08-14T00:00:00Z"  # 7 days of silence -> escalate to operator

J=$(gh api repos/shader-slang/slang/issues/12092 2>/dev/null) || J=""
C=$(gh api repos/shader-slang/slang/issues/12092/comments --paginate 2>/dev/null) || C=""

if [ -z "$J" ] || [ -z "$C" ]; then
  echo '{"wakeAgent": false, "data": {"note": "gh unavailable this fire; will retry"}}'
  exit 0
fi

state=$(jq -r '.state' <<<"$J")
assignee=$(jq -r '.assignee.login // "none"' <<<"$J")
count=$(jq -r '.comments' <<<"$J")
labels=$(jq -r '[.labels[].name] | sort | join(",")' <<<"$J")

# newest comment from a non-bot author (exclude our own bot identity, both forms)
newest_human=$(jq -r '[.[] | select((.user.login | startswith("nv-slang-bot")) | not)
                        | select(.user.type != "Bot")] | last | .id // 0' <<<"$C")
newest_human_login=$(jq -r '[.[] | select((.user.login | startswith("nv-slang-bot")) | not)
                        | select(.user.type != "Bot")] | last | .user.login // "none"' <<<"$C")

now=$(date -u +%Y-%m-%dT%H:%M:%SZ)
reasons=()
if [ "$newest_human" -gt "$BASELINE_HUMAN_CMT" ] 2>/dev/null; then
  reasons+=("NEW_HUMAN_COMMENT:${newest_human}:${newest_human_login}")
fi
if [ "$state" != "open" ]; then
  reasons+=("STATE_CHANGED:${state}")
fi
if [ "$assignee" != "$BASELINE_ASSIGNEE" ]; then
  reasons+=("ASSIGNEE_CHANGED:${assignee}")
fi
if [ "$labels" != "$BASELINE_LABELS" ]; then
  reasons+=("LABELS_CHANGED:${labels}")
fi
if [[ "$now" > "$DEADLINE" ]]; then
  reasons+=("SILENCE_DEADLINE_PASSED:escalate-to-operator")
fi

if [ ${#reasons[@]} -eq 0 ]; then
  echo "{\"wakeAgent\": false, \"data\": {\"quiet\": true, \"comments\": $count, \"state\": \"$state\"}}"
  exit 0
fi

printf '{"wakeAgent": true, "data": {"reasons": "%s", "state": "%s", "assignee": "%s", "comments": %s, "baseline_comments": %s, "labels": "%s", "newest_human_comment": %s}}\n' \
  "$(IFS=';'; echo "${reasons[*]}")" "$state" "$assignee" "$count" "$BASELINE_COUNT" "$labels" "$newest_human"
```

Prompt:

```
ISSUE #12092 SCOPE GATE FIRED — shader-slang/slang#12092 was HELD on scope (maintainer csyonghe said a reflection-side fix is impossible by design; the post-emit-metadata path is a POSSIBILITY he described, not an authorized work order). slang-triager correctly held and asked the scope question on the issue; nobody authorized the plumbing. The guard data names the reason.

ACT ON THE REASON:
- NEW_HUMAN_COMMENT: read it. If csyonghe/jkwak-work answered the scope question (yes -> want a PR / no -> roadmap item), relay that decision to slang-triager on thread gh-issue-shader-slang/slang-12092 with in_reply_to set, quoting verbatim. Do NOT authorize feature-sized work yourself; the triager owns the fixer wire (no double-dispatch). If the comment is substantive but not a scope answer, still forward it — a substantive human comment re-opens a holding chain.
- STATE_CHANGED / ASSIGNEE_CHANGED: a maintainer moved it. Report to slang-triager on the canonical thread; if closed, confirm whether the hold should be released.
- SILENCE_DEADLINE_PASSED: no human answer in 7 days. Escalate the scope question to the OPERATOR (ask_user_question, timeout 0): does NanoClaw build the post-emit-metadata capability (feature-sized; changes a public consumer contract — a host must compile-to-target before it can size an existential buffer), or does #12092 stay a documented limitation with the [anyValueSize(64)] workaround? Then extend or retire this guard — do not let it fire forever.

Context: reflection=32 vs emitted ArrayStride=80, measured by the triager's libslang.so probe (control StructuredBuffer<Plain>{float4x4}=64 proves the probe reads real sizes). Real pass name is inferAnyValueSizeWhereNecessary — csyonghe's 'inferExistentialTypeSize' does not exist in tree. Precedent for the additive route: IBindlessResourceMetadata. Memory leaf: project_12092_reflection_anyvaluesize_stride_mismatch.
```

## `i12371-pr-guard-0175`

- schedule: `*/20 * * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/i12371-pr-guard.sh
```

Prompt:

```
ISSUE #12371 POST-DISPATCH GUARD — shader-slang/slang#12371 (SPIR-V validation reads the pre-link buffer). Read /home/node/.claude/projects/-workspace-agent/memory/project_12371_spirv_prelink_validation_buffer.md FIRST — its top section carries the verified PR state.

TWO PRs CARRY THIS FIX. #12382 (branch fix/issue-12371, head f93eb4f7, A1, 4 files +204/-7, closes 12371) is CONTAINED WHOLE by #12408 (branch fix/issue-12383, head 76281671, A2, 6 files +869/-36, closes 12371+12383) — compare f93eb4f7...76281671 = ahead 15 / behind 0. Both draft, both mergeable, both pr: non-breaking, both assignee+requested-reviewer jkwak-work, author id 274397474 type Bot (us). Q1 (A1-only vs A1+A2) is ANSWERED IN BUILT CODE by #12408's A2; the operator decision that remains is WHICH PR CARRIES #12371. Verify the ORDER TEST on any NEW head by fetching the file (compiler->compile position relative to the needsValidation block), never from the PR body.

⛔ CI IS NO LONGER INFRA-ONLY — THIS SUPERSEDES THE PRIOR "0 real failures" PARAGRAPH, WHICH IS NOW FALSE.
Measured 2026-08-08 09:2xZ at #12408 head 76281671: the priority gate RELEASED (wait-for-human-priority = success; skipped 74 -> 41, success 4 -> 35) and TWO REAL failures appeared:
  test-windows-debug-cl-x86_64-gpu / test-slang   (job 93061368941)
  test-windows-release-cl-x86_64-gpu / test-slang (job 93060460195)
Both fail only at step 8 "Test Slang", and in both the sole failing test is slang-unit-test-tool/spirvValidationAcceptsDownstreamLinkedModule.internal — the test THIS CHAIN'S PR adds (tools/slang-unit-test/unit-test-spirv-link-validation.cpp:168; absent from master). Deterministic, not a flake: it logged failed(pending retry) then FAILED after the suite's retry, while 4 other failures in the same jobs PASSED on retry. Windows-only: PASSES on 7 linux/macos configs at the same head. The file is md5-IDENTICAL at both PR heads (d2849dc4188d2cb53b2be5f9ccedf219), and #12382's head has NO test-windows*test-slang check-run at all (the gate skipped them there) — so #12382 is UNMEASURED, not clean, and this lands on BOTH PRs. Dispatched to slang-fixer 09:3xZ on thread_id=gh-issue-shader-slang/slang-12371 to triage infra-vs-code. NOT classified by me: captured stdout AND stderr were both EMPTY with result code = 1, so WHICH of the three SLANG_CHECKs fires (:179 codeResult, :180 producedCode, :184 generatorMagic == kSpvGeneratorKhronosLinker) is UNKNOWN. Do not repeat any hypothesis as a finding.

DISPATCH RULE (unchanged in form, corrected in scope): dispatch slang-fixer ONLY for a failing check name NOT in {check-ci, wait-for-human-priority} — those two remain infra-by-design (the priority gate yields bot CI to human CI; ::error::priority-gate-yielded). Classify by reading the failing job's OWN log, never the rollup color. ⚠️ The failing-name set is now carried for BOTH PRs (fingerprint cells :N:names on every xst= row, added by the 8th fix); failing_headsha alone covers only #12382 and would have missed the above.

The guard is STATE-LATCHED: it wakes only when the fingerprint changes, or on a 4-hour heartbeat floor. data.wake_reason is "changed" or "heartbeat", and prior_fingerprint vs fingerprint shows exactly what moved. ON A "heartbeat" WAKE WITH AN UNCHANGED FINGERPRINT AND NOTHING NEW: emit NOTHING — bare prose outside a <message> block STILL DELIVERS a row; only <internal></internal> is silent. Do not re-report state you already reported.

⛔ YOU NEVER FLIP THE DRAFT READY, NEVER APPROVE, NEVER MERGE — on any trigger, any answer, or silence. All three are human acts; repo precedent over the 12 most-recently-updated merged bot PRs is 12/12 human for ready_for_review, merged_by, and the sole APPROVED review (bot-as-merger 0/12). Only ever post COMMENT-state. IDENTITY TRAP: login=nv-slang-bot type=User id=286953280 is NOT us; match on id/type, never login alone. Do not patch another tier's artifact — slang-triager owns issue comment 5197829621; slang-fixer owns both PR bodies and the GitHub trail.

DO:
1. wake_reason=changed: diff prior_fingerprint against fingerprint and act ONLY on what moved. New head on either PR => re-run the ORDER TEST on the fetched file and re-check the diff shape (a silent A2 would wear A1 diff-size). New failing name outside the gate pair => dispatch slang-fixer on thread_id=gh-issue-shader-slang/slang-12371; do NOT reach past it. isDraft flipped to false, or mergedAt set, on EITHER PR => a human acted: report to orchestrator-dashboard and go to step 6.
2. trigger=human_comment: read it and route on merits — substantive input (counter-proposal, A1/A2 preference, scope question) goes to slang-triager on the canonical thread; a thanks/ack closes explicitly with a 5-bullet. A substantive human comment RE-OPENS this chain even though it looks settled. Do NOT no-op because it looks done.
3. trigger=issue_closed: confirm whether a fix landed or it was dismissed; report to orchestrator-dashboard and cancel this guard.
4. trigger=branch_pushed: read the pushed diff directly (gh api repos/shader-slang/slang/compare/master...<head>) — a PR body is a DESCRIPTION of a diff, not the diff. Same order test. Never open, ready, approve or merge.
5. NUDGE BUDGET IS EXHAUSTED (#1 06:30Z 08-06, #2 17:2xZ 08-07). No further Q1 nudge on any wake. A NEW FACT that changes what someone would do or believe (a real CI failure, a head move, a human act, a review landing) is a REPORT, not a nudge, and still ships. ask_user_question is SWALLOWED from guard sessions (messaging_group_id null) — use send_message(to:"orchestrator-dashboard") ONLY.
6. EVIDENCE HYGIENE — slang-fixer retracted three void measurements on this chain (05:04Z 08-06): a suite green that measured UNPATCHED head via a stale git stat cache, and a unit test that was a void control (precompileForTarget sets EmbedDownstreamIR => isPrecompilation true => needsLink FALSE, so it never reached the link path). Do NOT treat any single green as verification; require the two-directional result (FAILS on reverted source, PASSES with the fix) with the binary relink timeline checked, and prefer `git show HEAD:<file> | diff -q - <file>` over `git status`. I hold NO view of the fixer's tree: its test results are ITS measurement on ITS tree — say so when relaying. Likewise a check-run census is a reading at a TIME, not a property of a sha (measured: the same master sha went 80 -> 383 -> 677 rows in 12h), so never compare this wake's count against a stored one.
7. Cancel this guard (ncl tasks cancel i12371-pr-guard-0175) once a PR carrying the fix is MERGED, or both are closed, or the operator names the carrying PR AND the fixer confirms the built scope matches.
```

## `i12428-fork-gate-52aa`

- schedule: `0 */6 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/.i12428_fork_gate.sh
```

Prompt:

```
MAINTAINER-REPLY FALLBACK for the shader-slang/slang#12428 + #12433 design fork.

WHY THIS EXISTS: on 2026-08-08 slang-triager asked @jkwak-work (issue cmt 5226554214, 14:32Z) to decide TWO forks that gate a fix. A gate on an external human reply has NO resume trigger I control — the only one that existed was the triager saying it would re-ping on its next sweep. This is my own trigger. Verified ARMED before scheduling: pointed at #12367 as a positive control it fired with reason=human_reply and named commenter/ts/id.

THE TWO FORKS (do not re-litigate, do not decide for him):
1. WARNING vs ERROR for a bare unapplied function reference in statement position. Never meaningful => error defensible, BUT it is a source-compat change and the adjacent precedent E30058 is a warning. #12433 (the ICE) removes THIS fork only — a crash must become a diagnostic.
2. FIX LOCATION, A vs B — shared by both issues and NOT removed by the ICE:
   A = diagnose in visitExpressionStmt (slang-check-stmt.cpp:684-704), beside two real precedents (dangling == -> E30058 at :700-701 from PR #11493 2026-06-12; [NoDiscard] at :703 from PR #11520 2026-06-16 — both dated by REMOTE GraphQL blame, not git log -S, which lies on a shallow clone).
   B = implement the TODO at slang-check-expr.cpp:3849, the principled root; fixes #12428 AND #12433 and the let-f=addOne/E33070 asymmetry, but CheckExpr is hot-path and IFunc/lambda/__fwd_diff/functype params must be threaded as allowed contexts.
   IF HE PICKS B: one PR closes both issues; brief the fixer with BOTH issues constraints.

ON reason=human_reply: read it on merits, QUOTE him verbatim on any semantics/severity ruling (never paraphrase). Then dispatch slang-fixer THROUGH slang-triager (it owns the chain and holds the 317-line memo) on thread_id=gh-issue-shader-slang/slang-12428 VERBATIM — parallel dispatch to the same peer mints duplicate sessions. For anything about #12433 use thread_id=gh-issue-shader-slang/slang-12433 VERBATIM (a triager session exists there: sess-1786200351605-ecf22e as of 16:0xZ — RE-RUN ncl sessions list at dispatch time, a session census expires in minutes). Then cancel this task.

FIXER BRIEF, already derived — do NOT re-derive:
- Cover ALL FOUR silent shapes, not just a bare name: (addOne); paren-wrapped, s.m; bare member method, a; bare variable, (int x) => x+1; lambda-as-statement. A check keyed on "VarExpr naming a CallableDecl" leaves two silent.
- Key on discarded expression-STATEMENT position, never on "has FuncType", or it breaks two cells measured working today: functype bare name as an ARGUMENT (covered in-tree at docs/generated/tests/design/syntax-reference/grammar/type-functype-keyword.slang:37) and __fwd_diff(sq)(...).
- TEST TEMPLATE EXISTS: tests/diagnostics/dangling-comparison.slang — a == 2; diagnosing cell + (a == 2); // ok. at :13 as the boundary. Do NOT let anyone conclude E30058 is untested from grep -rl E30058 tests/ = 0; 659 of 826 DIAGNOSTIC_TEST files carry no E-code at all. Grep the MESSAGE TEXT.
- ASSERT ON THE E99997 MARKER OR MESSAGE TEXT, NEVER ON EXIT CODE. slangc collapses every failed compile to one value (source/slangc/main.cpp:46, res = SLANG_FAILED(res) ? SLANG_E_INTERNAL_FAIL : res), so undefined identifier / syntax error / ICE all exit 255 — a boundary cell written against exit codes asserts nothing.
- extras/formatting.sh could not run on the triager edge (clang-format/gersemi/prettier/shfmt absent) => the PR author must run it.

ON reason=issue_closed: establish whether a fix landed or it was dismissed, report to orchestrator-dashboard, cancel this task.
ON alert (ask comment missing): the ask is not public — tell orchestrator-dashboard immediately, it may need reposting.

NUDGE BUDGET: at most TWO nudges to orchestrator-dashboard across this task life, and only after 4+ consecutive silent wakes (~24h). Say which nudge it is. NEVER nudge the maintainer directly on GitHub — that is an operator-gated write into a human inbox. Queue depth was 63 open bot PRs on 08-08, so do NOT dispatch a speculative fix to look busy.
If a wake brings nothing new, emit NOTHING — bare prose outside <message> still delivers; only <internal></internal> is silent.

FIXER-BRIEF ADDITIONS (2026-08-08 16:2xZ, from sibling cmt 5226660337 on #12433 — I VERIFIED BOTH ON MY OWN CLONE, do not re-derive):
- THE TWO CANDIDATE INSERTION SITES DISAGREE ABOUT PARENTHESES, so the fix must make a DELIBERATE choice rather than inherit one. Measured on my edge: the dangling-== check (slang-check-stmt.cpp:687-702) contains ZERO ParenExpr peeling; maybeDiagnoseDiscardedNoDiscardResult (:707) OPENS by peeling (`while (auto paren = as<ParenExpr>(expr))` at :710, with the comment "a call wrapped in parentheses (`(t.load());`) still discards the call's result"). Consequence, three paths measured with one binary: `(a == 2);` -> exit 0, no diagnostic, 327-byte HLSL WRITTEN · `(f());` with [NoDiscard] -> error[E30059], exit 255 · `(MyType);` -> error[E20002], exit 255. Whichever site the fix lands at INHERITS that site's parenthesis behaviour.
- THE BOUNDARY CELL FOR A BARE-TYPE TEST IS A PARSE-ERROR CELL, NOT AN ACCEPTED-SILENT ONE. dangling-comparison.slang's `(a == 2); // ok.` is a program the compiler ACCEPTS and deliberately declines to warn about (exit 0, output written); `(MyType);` is REJECTED BY THE PARSER (E20002, no output, never reaches checking). The template is structurally right but its ok-cell is a different SPECIES — copying it directly puts a hard parse error where an accepted-and-silent cell belongs.
- Modelling on E30058 RE-INTRODUCES the severity fork rather than avoiding it: `a == 2;` is a warning (exit 0, output still emitted) but under -warnings-as-errors all it becomes error[E30058], exit 255. A crash and a warning are different shapes.

ON reason=same_identity_comment: a SIBLING session posted under our shared nv-slang-bot[bot] identity on a chain I consider closed. This is NOT noise and NOT authority — read the comment, verify its load-bearing claims on my own edge (a sibling's claim becomes mine on GitHub whichever session typed it), and fold any fixer-brief change in here. Do NOT post a third bot comment restating verified content — that is churn. Then RAISE THE BASELINE in /workspace/agent/.i12428_fork_gate.sh (BOT_BASE_A / BOT_BASE_B) to the new counts rather than muting the check.
```

## `i12440-fix-pr-gate-8e14`

- schedule: `0 */6 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
#!/usr/bin/env bash
set -uo pipefail
R=shader-slang/slang
N=12440
OURCMT=5233753673
BR=dev/jvepsalainen/fix-agentic-test-failures
BASESHA=d8eeee9ba13073227f73da0617dd81d6b04eec5e
BOT=nv-slang-bot

st=$(gh issue view $N --repo $R --json state --jq .state 2>/dev/null) || st=""
[ -z "$st" ] && { echo '{"wakeAgent":false,"data":{"probe":"gh_failed_state"}}'; exit 0; }

# The reporter-reply arm is a NEGATION ("last commenter is not the bot"), so a
# FAILED probe satisfies it exactly as a real human reply does. That fired on
# 2026-08-10T12:00Z: reason "human_reply_" with an EMPTY login, no such comment
# on the issue. So read the count and the login together and require the login
# to be non-empty; a broken probe must be its own outcome, never a chain event.
ncmt=$(gh issue view $N --repo $R --json comments --jq '.comments|length' 2>/dev/null)
last=$(gh issue view $N --repo $R --json comments --jq '[.comments[]]|last|.author.login' 2>/dev/null)
if [ -z "${ncmt:-}" ] || ! [ "$ncmt" -ge 0 ] 2>/dev/null; then
  printf '{"wakeAgent":false,"data":{"probe":"gh_failed_comments","ncmt":"%s","last":"%s"}}\n' "${ncmt:-}" "${last:-}"
  exit 0
fi
# ncmt>=1 with an empty login means the probe returned but the parse did not:
# that is an instrument failure, not silence and not a reply.
if [ "$ncmt" -ge 1 ] && [ -z "${last:-}" ]; then
  printf '{"wakeAgent":false,"data":{"probe":"empty_login_on_nonzero_comments","ncmt":"%s"}}\n' "$ncmt"
  exit 0
fi
humanreply=false
[ "$ncmt" -ge 1 ] && [ -n "${last:-}" ] && [ "$last" != "$BOT" ] && humanreply=true

# ARM A: branch tip moved off the SHA that carries the UNSCOPED guard.
# Movement = he is revising; possibly adding the reachability scoping.
tip=$(gh api repos/$R/git/refs/heads/$BR --jq .object.sha 2>/dev/null) || tip=""
tipmoved=false
[ -n "$tip" ] && [ "$tip" != "$BASESHA" ] && tipmoved=true
branchgone=false
[ -z "$tip" ] && branchgone=true

# ARM B: any PR whose HEAD REF is that branch. Catches drafts and PRs whose
# body never cites 12440 -- #12438 did neither (created 90min before the issue
# existed, so its body could not cite it). Head-ref is identity, not prose.
headprs=$(gh pr list --repo $R --state open --head "$BR" --json number --jq '[.[].number]|join(",")' 2>/dev/null)

# CONTROLS (executable, not prose). The recall control MUST run the same query
# ARM B runs, byte-for-byte including --state open. Seed is a branch with a LIVE
# open PR -- NOT the closed fix branch: #12438 is closed, so seeding on it makes
# this control structurally incapable of failing. Do NOT "fix" this to --state all.
SEEDBR=compile-perf-corpus-step
SEEDPR=12439
recall=$(gh pr list --repo $R --state open --head "$SEEDBR" --json number --jq "[.[].number]|any(.==$SEEDPR)" 2>/dev/null)
mustmiss=$(gh pr list --repo $R --state open --head "dev/__no_such_branch__" --json number --jq 'length' 2>/dev/null)
if [ "$recall" != "true" ] || [ "${mustmiss:-x}" != "0" ]; then
  printf '{"wakeAgent":true,"data":{"reason":"CONTROL_VOID","recall":"%s","mustmiss":"%s","note":"ARM B aperture unverified - do not trust a false reading"}}\n' "$recall" "$mustmiss"
  exit 0
fi
# Same shape of control for the comments arm: the bot comment we posted must be
# visible to the SAME query the arm reads. If this seed cannot be recalled, the
# arm's negative ("still just the bot") is unfounded and must not be trusted.
seedcmt=$(gh issue view $N --repo $R --json comments --jq "[.comments[].author.login]|any(.==\"$BOT\")" 2>/dev/null)
if [ "$seedcmt" != "true" ]; then
  printf '{"wakeAgent":true,"data":{"reason":"CONTROL_VOID_COMMENTS","seedcmt":"%s","ncmt":"%s","note":"comments aperture cannot see our own bot comment - reply arm unfounded"}}\n' "${seedcmt:-}" "$ncmt"
  exit 0
fi

# ARM C: content search, broadened from the issue number to the symbol, and
# still gated on >=1 file under source/ (a skip-list edit is not a fix).
raw=$(gh pr list --repo $R --state open --search "getStringHash in:body" --json number --jq '.[].number' 2>/dev/null)
raw2=$(gh pr list --repo $R --state open --search "12440 in:body" --json number --jq '.[].number' 2>/dev/null)
cand=$(printf '%s\n%s\n' "$raw" "$raw2" | sort -u | tr -d ' ')
fixprs=""
for p in $cand; do
  [ -z "$p" ] && continue
  nsrc=$(gh pr view "$p" --repo $R --json files --jq '[.files[].path|select(startswith("source/"))]|length' 2>/dev/null)
  [ "${nsrc:-0}" -gt 0 ] && fixprs="$fixprs $p"
done
fixprs=$(echo $fixprs)

ourts=$(gh api repos/$R/issues/comments/$OURCMT --jq .created_at 2>/dev/null)
hrs=0
[ -n "$ourts" ] && hrs=$(( ( $(date -u +%s) - $(date -u -d "$ourts" +%s) ) / 3600 ))

wake=false; reason=none
if [ "$st" != "OPEN" ]; then wake=true; reason="issue_${st}"
elif [ -n "$headprs" ]; then wake=true; reason="pr_from_branch:${headprs}"
elif [ -n "$fixprs" ]; then wake=true; reason="fix_pr:${fixprs}"
elif [ "$tipmoved" = true ]; then wake=true; reason="branch_tip_moved:${tip:0:9}"
elif [ "$branchgone" = true ]; then wake=true; reason="branch_deleted"
elif [ "$humanreply" = true ]; then wake=true; reason="human_reply_${last}"
elif [ "$hrs" -ge 72 ]; then wake=true; reason="silent_${hrs}h"
fi

printf '{"wakeAgent":%s,"data":{"reason":"%s","issue_state":"%s","comments":%s,"last_commenter":"%s","hours_since_verdict":%s,"branch_tip":"%s","tip_is_unscoped_base":%s,"open_prs_from_branch":"%s","fix_prs":"%s"}}\n' \
  "$wake" "$reason" "$st" "$ncmt" "$last" "$hrs" "${tip:0:9}" "$([ "$tipmoved" = false ] && [ "$branchgone" = false ] && echo true || echo false)" "$headprs" "$fixprs"
```

Prompt:

```
REPORTER-REPLY / FIX-PR GATE for shader-slang/slang#12440 (getStringHash on a non-literal SIGSEGVs instead of E41023).

STATE at arming (2026-08-09 ~20:50Z, all re-measured this turn, not carried):
- Issue 12440 OPEN, labels reproduced+Diagnostics+Missing Diagnostic+bug, exactly 1 comment: nv-slang-bot cmt 5233753673 created 2026-08-09T20:43:58Z (6575 chars). slang-triager is last commenter, so it can refresh in place.
- slang-triager REPRODUCED at master 716ec597f and BUILT the reporter's suggested fix: it regresses valid code. uint h(String s){return getStringHash(s);} called as h("aaa") compiles on master (uint(807729185)) but is rejected E41023 by the both-sites patch, because in AFTER performTypeInlining the dead helper body still holds the unfolded getStringHash(%s) and the check scans it; eliminateDeadCode on the next line (slang-emit.cpp:1653) removes it. Emitter-only variant fixes all 3 crash shapes and keeps the helper working, but yields E99999 not E41023.
- Reporter's own draft PR 12438 CLOSED UNMERGED 20:18:33Z from dev/jvepsalainen/fix-agentic-test-failures; verified it carried 3 files under source/. Replacement PR 12444 is OPEN with 0 files under source/ and only rewrites the expected-failures.txt comment to point at 12440 — so it does NOT fix the crash and the test stays skipped. Verified by diff this turn.
- slang-triager invited the reporter to open the focused PR; no fixer dispatched (would duplicate his work and risk re-shipping the regression).

WHY YOU ARE AWAKE: the gate script fired. Read data.reason:
- fix_pr:<N> => a PR referencing 12440 now touches source/. Dispatch slang-triager on thread gh-issue-shader-slang/slang-12440 to re-read that diff and check (a) the guard is SCOPED to surviving/reachable insts (else the h("aaa") regression ships), (b) a static-String test case is covered, (c) the 3 sibling emitter sites (slang-emit-spirv.cpp:5975, slang-emit-wgsl.cpp:1711, slang-emit-llvm.cpp:2178) are considered — noting the LLVM one was never tested by us, so it is unverified, not latent. Then refresh cmt 5233753673 in place and forward the [Triage Resolution].
- human_reply_<login> => a non-bot comment landed. A substantive reply RE-OPENS the chain: forward it to slang-triager on the canonical thread. A thanks/ack closes explicitly with a 5-bullet.
- issue_CLOSED => re-read who closed it and why; if closed by a merge, verify the merged diff scoped the guard before recording resolution.
- silent_<N>h => 72h+ with no reporter movement. Decide: nudge the reporter once on the issue, or release slang-fixer with the constraint EMITTER CAST IS SAFE AS-IS, GUARD NEEDS REACHABILITY SCOPING, DO NOT SHIP THE BOTH-SITES PATCH UNCHANGED. Do not send a second nudge if one is already on the issue.

ALWAYS: re-measure issue state and the comment trail before acting; the figures above are this-turn measurements and go stale. Cancel this series once 12440 is closed with a verified scoped fix.
```

## `i12442-prelude-watch-c3e5`

- schedule: `0 */12 * * *`
- agent group: `ag-1780667166418-apezq5`

Pre-task gate:

```bash
/workspace/agent/bin/watch-12442.sh
```

Prompt:

```
Watch fire for shader-slang/slang#12442 (render-test blanks the shared HLSL prelude). REPORT TO parent ON THREAD gh-issue-shader-slang/slang-12442 EVERY TIME, including when nothing changed — a silent watch is indistinguishable from a dead one. VOID-READING RULE: if recall_control != PASS, or mustmiss_control != 0, or any operand is PROBE_FAILED, treat the WHOLE reading as VOID and re-measure by hand before reporting any number. The recall control runs the armed query BYTE-FOR-BYTE including its --state open limb, seeded on a branch with a live open PR (compile-perf-corpus-step / #12439) — do NOT 'fix' it to --state all, that is the defect it was written to catch. TWO OPERANDS, TWO QUESTIONS: open_prs_from_fix_branch answers 'is the fix reachable to a reviewer NOW' (a closed PR is not); fix_branch_tip answers 'does the fix still EXIST' — report both, never one alone. If condition_fired=true (#12444 merged AND zero OPEN PRs from the fix branch), the three nvapi tests are suppressed with no fix reachable: re-surface on issue #12442 and flag to parent as the pressure-removed case. If fix_branch_tip is 'moved:<sha>' the author revised the fix — read the new tip's diff before reporting. If 'branch_deleted', the fix artifact is GONE: say so explicitly, do not report it as 'no fix in flight'. Keyed on BRANCH IDENTITY (dev/jvepsalainen/fix-agentic-test-failures) because commit d8eeee9ba predates issue #12442 and its PR body cites none of the four issues it fixes. Do NOT dispatch a fixer and do NOT open a competing PR — the author has the fix and parked it deliberately.
```

## `i12443-design-gate-71e8`

- schedule: `0 */6 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/.i12443_design_gate.sh
```

Prompt:

```
MAINTAINER/REPORTER-REPLY FALLBACK for shader-slang/slang#12443 (enum construction E(N) rejected in a header-phase array bound).

WHY THIS EXISTS: slang-triager triaged #12443 on 2026-08-09 (cmt 5233704622) and split it into two deliverables, then held deliverable 1 behind a maintainer design call. Its stated resume condition was "jvepsalainen-nv or a maintainer answers the by-design question or says make a PR" — a gate on an external human with NO trigger anyone here controls. Worse, I MEASURED that the triage comment @-mentions NOBODY (grep for @handle on cmt 5233704622 => 0 hits; must-hit control on cmt 5226554214 => @jkwak-work), so the design question is PUBLISHED but not ADDRESSED. This task is the trigger.

ARMED BEFORE SCHEDULING (all controls run 2026-08-09, per-field comparison, not whole-string):
- positive human_reply: pointed at #12367 with base=5193130734 => fired, NAMED 3 humans; second run silent.
- must-not-fire: #12367 with base=5212908232 (the last human) => human_reply correctly silent.
- BASE_MISSING: fabricated base 999999999 => ALERT, not a silent pass.
- same_identity_comment: real target with BOT_BASE=0 => fired and names the baseline.
- linked_pr fires once then stays silent; a simulated assignee/meta edit does NOT replay it.
Baseline consumed at arming time: PR 12444 (the reporter own agentic-nightly-green PR) is ALREADY REPORTED, so a linked_pr wake means a NEW PR references #12443 — most likely the actual fix.

THE ONE DESIGN QUESTION (do NOT decide it, do NOT re-litigate):
Is header-phase enum construction intended to work? Header-phase array bounds (function parameter, global, struct field) reject `Size(6)`; the IDENTICAL expression in a LOCAL array bound folds and emits `int a_0[int(3)]`. Enum CASES (`Size.Large`) work everywhere. slang-check-overload.cpp:3432-3436 explicitly special-cases isDeclRefTypeOf<EnumDecl> for single-arg construction via _coerce(CoercionSite::ExplicitCoercion,…), so the by-design reading looks weak — but that is a maintainer call.

ON reason=human_reply: read it on merits and QUOTE the human VERBATIM on any semantics/by-design/severity ruling (never paraphrase). Then dispatch slang-fixer THROUGH slang-triager (it owns the chain and holds the 134-line memo at /workspace/inbox/a2a-1786307632529-c242dz/triage-12443.md on MY edge — the triager has its own copy) on thread_id=gh-issue-shader-slang/slang-12443 VERBATIM. Re-run `ncl sessions list` at dispatch time; a session census expires in minutes. Then cancel this task.

FIXER BRIEF, already derived — do NOT re-derive:
- Approach A ONLY: let the header-phase const-fold path (CheckIntegerConstantExpression, slang-check-type.cpp:181 -> tryConstantFoldExpr, slang-check-expr.cpp:2898) reach the same explicit-coercion route as slang-check-overload.cpp:3432, so an InvokeExpr whose callee is a TypeType over an EnumDecl folds. Must NOT make a runtime value fold — keep ConstantFoldingKind honoured.
- DO NOT BUNDLE the diagnostic fix (Approach C: suppress the E33070/E39999 cascade when the callee is a TypeType over an EnumDecl and the real failure was the coercion). Separate PR. C is ready-for-fix NOW and needs no design call — if the human answer is slow, C can ship on its own.
- DO NOT special-case the ternary or the comparison (Approach D, rejected): `int pick(int a[int(Size(6))])` fails with no generic param, no compare, no ternary. Genericness is NOT load-bearing either. The issue TITLE is wrong on both counts.
- Add an in-tree test under tests/language-feature/enums/ — 24 tests there, NONE puts enum construction in an array bound.
- Three diagnostics fire, not two: E30019 FIRST (the failed coercion, flushed from the temporary sink at :3427-3428), then E33070 (fall-through to AddOverloadCandidates at :3465), then E39999. The issue records only the first two.
- ASSERT ON MESSAGE TEXT OR THE E-CODE MARKER, NEVER ON EXIT CODE. slangc collapses every failed compile to one value (source/slangc/main.cpp:46), so undefined identifier / syntax error / ICE all exit 255.
- BEWARE VACUOUS PASSES: a local array that is DCE-d or constant-propagated never folds its bound, so exit 0 carries NO information. Make the array escape (runtime-indexed / written to a buffer). The triager caught 4 of these.
- The emitted HLSL spells the bound `int(3)`, NOT `[3]` — a `[3]`/`[6]` grep returns 0 on a SUCCESSFUL fold. Wrong vocabulary, not an absence.
- extras/formatting.sh could not run on the triager edge (clang-format/gersemi/prettier/shfmt absent, measured 2026-08-09) => the PR author must run it.

ON reason=linked_pr: a NEW PR references #12443. Establish whether it is the fix (Approach A), the diagnostic (C), or another test-suppression PR. If it is a fix from a human, the chain is HANDED OFF — report to orchestrator-dashboard and cancel; do NOT dispatch a duplicate fix. 64 open bot-authored PRs on 2026-08-09 (uncapped search count; `gh pr list --limit 200` returns exactly 200 and is CAPPED — do not use it), so never dispatch a speculative fix to look busy.

ON reason=issue_closed: establish whether a fix landed or it was dismissed, report to orchestrator-dashboard, cancel this task.
ON alert (ask comment missing): the triage comment is not public — tell orchestrator-dashboard immediately, it may need reposting.
ON reason=same_identity_comment: a SIBLING session posted under our shared nv-slang-bot[bot] identity. Read it, verify its load-bearing claims on your own edge (a sibling claim becomes ours on GitHub whichever session typed it), fold any brief change in, do NOT post a third bot comment restating verified content, then RAISE BOT_BASE in /workspace/agent/.i12443_design_gate.sh rather than muting the check.

NUDGE BUDGET: at most TWO nudges to orchestrator-dashboard across this task life, and only after 4+ consecutive silent wakes (~24h). Say which nudge it is. NEVER nudge a maintainer directly on GitHub — that is an operator-gated write into a human inbox.
If a wake brings nothing new, emit NOTHING — bare prose outside a <message> block STILL DELIVERS; only <internal></internal> is silent.

⚠️ NIGHTLY SIGNAL GOES GREEN WHILE THE BUG IS LIVE — READ THIS BEFORE ANYTHING ELSE.
PR #12444 adds docs/generated/tests/design/ast-reference/values/builtinoperationintval-enum-operands-fold.slang to docs/generated/tests/_meta/expected-failures.txt. Once it merges, the nightly advisory suite is GREEN with the defect unfixed, so NIGHTLY REDNESS WILL NEVER RESURFACE THIS. The only in-repo tripwire is that entry's own removal comment naming #12443. Do not treat a green nightly as evidence the bug is gone; do not close on it.
=> ON reason=linked_pr YOU MUST DISTINGUISH THREE CASES, not two. CLASSIFY ON ONE TEST ONLY, IN THIS ORDER:
   TEST: `gh pr diff <n> --name-only`. Is EVERY file under docs/generated/** ?
     YES => (b) ANOTHER SUPPRESSION PR. NOT a fix. Do NOT cancel. Stay armed and say so.
     NO (any single file outside docs/generated/**) => (a) CANDIDATE FIX. Read it; if it is a real fix the chain is handed off — report and cancel. If it turns out to be an unrelated PR that merely mentions #12443, that is (c): stay armed.
   ⛔ DO NOT CLASSIFY ON "touches expected-failures.txt" — THAT TEST IS USELESS HERE. A real fix touches that file too, because it REMOVES the entry #12444 added. Both a suppression and a fix touch it; only the file-set test separates them.
   ⛔ DO NOT CLASSIFY ON "touches docs/generated/" WITHOUT THE WORD ONLY. Verified counterexample, and it is exactly our fix's shape: commit 8ac9e49a5 "Make vertex-shader-missing-sv-position pedantic and off by default" (#11893) touches source/slang/slang-diagnostics.lua + source/slang/slang-options.cpp + source/compiler-core/slang-diagnostic-sink.h + tests/diagnostics/** AND 1 file under docs/generated/ — a genuine diagnostic-behaviour change that regenerates docs alongside the code. Approach C edits slang-diagnostics.lua, so the real fix WILL look like this. A gate keyed on "touches docs/generated/" classifies the fix as a suppression and stays armed forever.
   Baseline for the test: #12444 is 11/11 files under docs/generated/, 0 under source/ => correctly (b).
   ⛔ THE FILE-SET TEST ASSUMES THE PR IS OPEN — CHECK STATE FIRST. Verified trap on this very issue: PR #12438 ("Fix getStringHash crash and render-test's HLSL prelude leak") is cross-referenced from #12443, touches source/slang/slang-emit-c-like.cpp + slang-emit-spirv.cpp + slang-ir-string-hash.cpp + tools/render-test/**, AND touches docs/generated/tests/_meta/expected-failures.txt and the enum-cast findings YAML. By file-set alone it reads as a CANDIDATE FIX. It is CLOSED AND UNMERGED (superseded by #12444) => neither fix nor suppression. The gate already filters to open PRs, so it will not wake you on #12438; if you ever inspect linked PRs by hand, apply the state check yourself.
   ⚠️ A "DOES IT TOUCH THE FIX SURFACE?" FILTER IS ADVISORY ONLY — IT MUST NEVER VETO, even though the specific counterexample I first gave for it was WRONG (retracted below). Verified: #12438 touches 0 slang-check-* and 0 slang-diagnostics.lua. A file-name glob is not a statement about where a fix can live, so use it to RAISE suspicion, never to reject. The only classifier that decides is the file-set test above.
   ⛔ RETRACTED (mine, 2026-08-09): I claimed an Approach-A fix could land ENTIRELY in source/slang/slang-ast-val.cpp (TypeCastIntVal::tryFoldImpl at :2106) and so escape a slang-check-* glob. TRACED AND REFUTED: for int(Size(6)) the outer TypeCastExpr branch (slang-check-expr.cpp:3221) calls tryConstantFoldExpr on the operand Size(6); that dispatches at :3260 to the InvokeExpr overload at :2432, which at :2443-2456 requires the callee to be a DeclRefExpr carrying an IntrinsicOpModifier or ImplicitConversionModifier. A TypeType-over-EnumDecl callee has neither => returns nullptr => val is null => the `if (val)` guard is false => TypeCastIntVal::tryFoldImpl at :3249 IS NEVER REACHED. tryFoldImpl is downstream of a fold that never starts, so a fix confined to slang-ast-val.cpp CANNOT work.
   ⛔ ALSO RETRACTED (mine, same evening): I then named slang-check-expr.cpp:2456 (the "no IntrinsicOpModifier / ImplicitConversionModifier => return nullptr" gate in the InvokeExpr fold overload) as "THE Approach-A edit point", and slang-triager banked that too. REFUTED BY A DISCRIMINATOR: that gate cannot be what distinguishes the failing from the working case, because it is phase-agnostic — it would reject an enum-construction InvokeExpr in a LOCAL bound identically. Measured: `int arr[int(Size(6))]` in a local bound, array escaping to a buffer, exits 0 and emits `arr_0[int(6)]`. Same expression, same CheckIntegerConstantExpression fold entry, opposite outcome. So :2456 is a REAL gate that the failing case does hit, but it is NOT THE CAUSE OF THE DIVERGENCE.
   ⭐ WHAT THE DISCRIMINATOR ACTUALLY SHOWS, and the honest state of the fix location: in the WORKING case ResolveInvoke's enum coercion SUCCEEDS, so slang-check-overload.cpp:3457 (`if (coerceResult) return resultExpr;`) hands back the COERCED expression and the fold never sees an enum-construction InvokeExpr at all. In the FAILING case the same coercion fails, and only then does an unconverted InvokeExpr reach the fold and hit :2456. => THE QUESTION FOR APPROACH A IS WHY THE SAME COERCION SUCCEEDS IN BODY PHASE AND FAILS IN HEADER PHASE — that is upstream of both :2456 and :3432. DO NOT let the brief assert an edit point; the fixer must derive it from that question. Both candidate files are inside a slang-check-* glob either way, so this does not change the advisory-never-veto rule.
   ⚠️ SCOPE NOTE on a structural argument slang-triager offered for this (verified, but narrower than stated): ConstantFoldingKind is only {CompileTime, LinkTime, SpecializationConstant} (slang-check-impl.h:1859-1864) and tryConstantFoldExpr takes only (expr, kind, circularityInfo), so THE FOLD PATH carries no header-vs-body distinction. TRUE, and it does close the fold path. It does NOT close the COERCION path, which is where the divergence actually lives: the coercion runs under a SemanticsVisitor/SemanticsContext whose state DOES differ by phase (m_parentFunc, m_outerScope, m_declToExcludeFromLookup — slang-check-impl.h:1242-1340 and :1470-1509). "No phase parameter in the fold signature" is a reason to look in the visitor state, NOT a proof that nothing can differ.
   TWO VISITOR-STATE HYPOTHESES ALREADY TESTED AND REFUTED on my edge — do not re-derive: (1) withDeclToExcludeFromLookup(paramDecl) at slang-check-decl.cpp:13918 — refuted because a GLOBAL array bound with no ParamDecl at all fails identically (4 diagnostics); (2) the :2456 modifier gate — refuted because it is phase-agnostic while the local bound folds. Whoever takes A starts from m_parentFunc/m_outerScope, or from what CheckUsableType does differently in header phase, not from these two.
   ⛔ DISCOVERY NOTE — DO NOT "SIMPLIFY" THE GATE'S PR QUERY BACK TO A SEARCH. It now reads the ISSUE TIMELINE (cross-referenced events), because a search on "12443 in:body" returned ONLY #12444 while the timeline carried TWO PRs: #12438's reference to this issue lives in a PR COMMENT, invisible to any body-text query (a commit-message or branch-name link is invisible the same way). Controls run 2026-08-09: #12367 -> 12378 open; #12371 -> 12382,12408 open; #12092 -> NONE (clean empty, not an error).

CORRECTED MECHANISM (round-2, verified at source by BOTH me and slang-triager on 2026-08-09 — supersedes the plumbing sentence still live in cmt 5233704622; the triager MEMO is the authority for that paragraph, not the GitHub comment):
coercion fails inside the enum branch -> its diagnostics are DISCARDED with the temp sink (collectedErrorsSink is declared slang-check-overload.cpp:3428, written by the failed _coerce at :3441, then never drained) -> typeOverloadChecked=true at :3459 SUPPRESSES overload resolution, so the :3465 AddOverloadCandidates call NEVER RUNS -> the initializer-list fallback fails on the REAL sink, emitting E30019 via _failedCoercion (slang-check-conversion.cpp:1508, reached from :1440 where canCoerce rejects) -> the terminal diagnose at :3808 fires E33070 -> the bound never folds (E39999).
Two claims in the live GitHub comment are WRONG and must not be inherited: "falls through to AddOverloadCandidates (:3465)" (that call is disabled by :3459) and "held in the temporary sink ... drained at :3778-3785" (every exit from that block RETURNS, so a drain would make :3808 unreachable and there would be no E33070 — we observe both).
Discriminators, reproducible on the triager binary: AddOverloadCandidates' signature is "no overload for 'X' applicable to arguments of type (...)" + note[E40011] candidate: — the 1-arg repro shows 0 of those and 1 E33070; Size(1.5, 2) shows 2 and 0. And _coerce:2821 always pairs TypeMismatch with NoteExplicitConversionPossible (:2825): `Size s = { 6 };` shows that note, our repro shows none, which is how :2821 was ruled out.
=> FOR APPROACH A THIS IS THE ACTIONABLE PART: a correct diagnostic for the coercion failure is ALREADY PRODUCED and thrown away. Recovering it is cheaper than authoring a new message, and it strengthens C independently of A.
DO NOT use -enable-machine-readable-diagnostics to tell a drained (diagnoseRaw) diagnostic from a structured one — the probe is BLIND. Positive control: a cell forced through forwardDiagnostics()->diagnoseRaw (slang-check-conversion.cpp:798-805) also renders as TSV, because the temp sink copies the parent's flags (slang-diagnostic-sink.h:427) and formats machine-readably internally.
```

## `infra-worktree-escalatio-8baa`

- schedule: `0 9 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Prompt:

```
INFRA ESCALATION RESUME CHECK — shared-clone / worktree isolation.

Read /home/node/.claude/projects/-workspace-agent/memory/project_shared_clone_worktree_isolation_infra.md first (measured 4-9% range, the sccache ask, both leak rules, and the per-fire State log at the bottom — read the LAST State paragraph before measuring anything).

The escalation was delivered to the operator on the dashboard 2026-08-06 ~19:4xZ, asking green/red on: (a) per-chain worktree default, shape "source + submodules, no build/", with "compiles" counting as write-capable; (b) builds opt-in, reclaim by pruning build/ subdirs; (c) install_packages for sccache (supported at CMakeLists.txt:476-519, absent on every edge checked). A FOURTH item was added 08-07: fix the guard-beside-the-action default in the coworker spines (a one-line spine edit; incident #5 was caused by that, not by absent isolation).

WHAT TO DO THIS FIRE:
1. Has the operator answered? If yes, act on it and cancel this task.
   Check: `ncl sessions list --limit 6000 --json`, filter messaging_group_id == mg-1776713211742-om8syu, then `ncl sessions messages <id> --limit 5000 --full` on each and look for an inbound `chat` row dated after 2026-08-06 19:40Z. Report the count of inbound chat rows EXAMINED as a control — a 0 with no control is not evidence.
   Do NOT cite mg-1781699149434-jdo1uq (dashboard-admin) as a second negative: it has ZERO sessions, so it can only ever return 0. It is a null instrument, not a negative result.
   Secondary attendance signal: `ncl approvals list --json` — pending critique_gate_bypass cards accumulating means operator-away rather than ask-specific rejection.
   ⛔ BUT A FALLING PENDING COUNT IS NOT AN ANSWER (measured 08-10, 5 -> 2, and it was NOT the operator). `ncl approvals` holds PENDING ROWS ONLY; help says rows are DELETED on approve/reject/expire, and --status approved|rejected|expired all return [] (control: bare list returns the live rows). Approved / rejected / expired / vanished are the SAME observation: absence. Key on the OLDEST SURVIVING row's age (monotone, cannot be faked by deletion) and on expires_at being NULL — never on the count.
2. Has the risk RECURRED since the last fire? Two measures, report BOTH:
   a. Instantaneous writers: container_status == 'running' grouped by agent_group_id.
   b. THE ONE THAT MATCHES THE FAILURE MODE — distinct sessions per agent_group_id with last_active inside a rolling 24h and 72h window. A STOPPED session still holds uncommitted edits in the shared clone, so 'running' undercounts who can be robbed. Baselines: 08-09 running 13 total (max 10), 24h Orchestrator 22 / slang-fixer 20 / slang-triager 9, 72h 107 / 67 / 44. 08-10 running 5 total, 24h Orchestrator 57 / slang-fixer 12 / slang-pr-approver 8 / slang-triager 8, 72h 83 / 33 / 26 / 13.
   ⛔ DO NOT "verify" a prior baseline by recomputing its window — `last_active` IS MUTABLE, so a backward replay is a LOWER BOUND, never the past measurement. Measured 08-10: replaying the 08-09 24h window gave Orchestrator 10 against the 22 recorded that day, because 4 of the 12 in-window sessions had since been touched and left the bucket. BOTH figures were right. Compare only same-anchor-to-now measurements, and state the anchor with every figure. For a reproducible historic count use the immutable created_at and say so — "created in the window" is a DIFFERENT quantity from "active in the window".
   INSTRUMENT TRAPS, both re-measured twice — do not re-derive them:
   - `--limit 2000` silently caps at 2000 rows (store holds ~2500). Use `--limit 6000`; confirm by checking two limits return the same count.
   - Parse `--json`, never column-position awk/cut on the text output: an empty thread_id shifts every later field and silently yields a histogram of the wrong column.
   - `ncl sessions messages` truncates to 300 chars without `--full`, which makes an incident grep return a false 0.
   ALSO sweep your own `ncl tasks` PROMPTS for destructive ops in routine recipes — that layer EXECUTES and the 08-07 five-layer sweep did not read it. Use `ncl tasks get <series-id> --json` (NOT `list`, which truncates prompt to 120 chars and manufactures false write-failures); pattern `reset --hard|clean -fdx?|checkout -f|rm -rf|git gc|worktree prune|stash (pop|drop)|push --force`, then check whether any control flow CONSUMES the guard (`-eq 0 ||` / ABORT / exit 1 / merge --ff-only). 08-10 result: 3 of 20 series matched, 17 clean (so the pattern discriminates); the real one was the nightly KB sync task-1781522302095-mjy6s1 (56 runs, unguarded `checkout -f` + `reset --hard` in a clone hosting live co-tenant worktrees) — GATE NOW ADDED, verify it is still present. Expect 2 benign matches: `rm -rf` on a task's own mirror dests, and the literal string `git gc` inside THIS task's own prohibition text (a pattern match on a rule FORBIDDING the op).
   Then sweep sessions active since the last fire for a NEW co-tenant-destruction instance. ARM the pattern first on a known-positive session (sess-1786070331908-rjavdk = 16 hits, sess-1786047482505-5nim5r = 21) so a zero is meaningful. Timestamp-key each hit against the message header row and discard pre-window ones. Distinguish: actual destruction (the thing that raises priority) vs sibling-ATTRIBUTION errors vs near-misses that failed safely (a loud Edit refusal, a control catching a stale ref). Only real destruction counts.
3. Only re-ping the operator if (2) shows a NEW destruction incident since the last fire, or the count has grown materially. Do NOT re-send the same escalation unprompted — one delivered ask does not need repeating; a new data point does. Note from the 08-07 fire: a `<message>` block in the final response was silently UNDELIVERED and needed an explicit send_message(to=...) — confirm the returned id.
4. If nothing changed, do nothing and emit no message (a content-free row is itself a cost). Still append a dated State paragraph to the memo with the figures and any new finding, so the next fire does not re-measure from scratch.

DO NOT run git gc, prune, or reset anywhere. Any git experiment goes in a throwaway /tmp clone, never a real one. Do not implement the spine default yourself — it is an operator call.
```

## `memory-integrity-scan-958b`

- schedule: `0 5 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
python3 /workspace/agent/tools/memcheck.py 2>/dev/null || echo {\"wakeAgent\":true,\"data\":{\"SCANNER_FAILED\":\"memcheck.py did not run - investigate, do NOT assume clean\"}}
```

Prompt:

```
MEMORY INTEGRITY — `python3 /workspace/agent/tools/memcheck.py` found candidates; the payload lists them by class.

TRIAGE BEFORE ACTING. A hit count is a claim about the pattern, not about the store. Verified false-positive classes (2026-08-04): [[nodiscard]] / [[LOAD]] / [[depth]] / [[required_threads_per_threadgroup]] are C++/HLSL/Metal attributes, not wikilinks; [[project_8125...]] style are truncation ellipses; hyphen-vs-underscore variants resolve after normalising; a tally inside quotes or backticks is a QUOTED EXAMPLE of drift, not live drift; feedback_audit_grep_false_negatives_asymmetric legitimately says "the four mechanisms" (4 items) and "the eight mechanisms above" next to a MECHANISM 9 framed as separate.

ACT only on: split_fm (a literal newline-dash-dash-dash inside a description splits the frontmatter block — repair to ONE clean block AND rewrite the description so it no longer embeds the sequence), desc_delim (same payload, pre-corruption), missing_field (add name/description/type, writing the description from the BODY — never paraphrase the filename), and dangling links whose intended target you can VERIFY. Never invent a target; leave it flagged.

Reply one line: files scanned, candidates, real defects fixed, false positives by class. If it all triages clean, say so and stop.
```

## `pr12200-verdict-guard-d673`

- schedule: `*/30 * * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/pr12200-guard.sh
```

Prompt:

```
REAL CI VERDICT ON c1bb185a0f (#12200) — a substantive build/test/sanitizer job has finally concluded, or the probe broke. Gate data shows which.

WHY THIS EXISTS: #12200 (fix/issue-12197, head c1bb185a0f) has had NO behavioural CI verification for hours — Actions was in major_outage since 16:33Z and every ci_failed webhook resolved to infra (cancelled filter/board-sync, stuck queued reuse-compliance, priority-yield, 0-step check-formatting). The owning session audited six webhooks, none attributable to its diff. NOBODY was watching this axis: my own guards cover #12375/#12371/#11963, and the owning session confirmed it has ZERO Monitor calls — its follow-up is webhook-only and lossy at ~15%. So silence from it means nothing.

DO:
1. Re-read /home/node/.claude/projects/-workspace-agent/memory/feedback_a_field_named_like_a_state_is_not_a_test_for_that_state.md for the CI-reading rules this chain produced.
2. If event=REAL_VERDICT with substantive_failures=0: behavioural verification finally landed clean. Tell the owning session on thread gh-issue-shader-slang/slang-12197 and cancel this guard.
3. If substantive_failures>0: a genuine code-assessing job failed. Do NOT call it real until you check per-run vs per-commit granularity — cite the RUN id, since the same head reads differently at commit level (c1bb185a0f: 122 rows, 3 failures, all board-sync/check-ci/wait-for-human-priority = infra trio). Check the step count: 0 steps means the job never executed and produced no verdict.
4. Attribution: address the OWNING SESSION by thread, and cite the inbound row id when crediting anything — eight sessions answer to the slang-fixer name, and I misattributed six findings in one evening by using the name.
5. Never re-run or re-dispatch: at ~65% job success a rerun manufactures an unvalidatable artifact, and the PR is non-draft.
6. After acting, cancel this guard and update the memo.
```

## `slang-pr-report-committe-83e0`

- schedule: `0 5 * * 1`
- agent group: `ag-1776713258088-r8pp2t`

Prompt:

```
Daily Slang Community/Bot PR report → post into #slang-committers Discord (fires weekday 05:00 UTC). You ARE authorized to write to #slang-committers for THIS maintainer-reporting job specifically (dashboard-admin set this up 2026-07-21); this does not lift the general no-self-post rule for other committer/discussion posts.

STEP 1 — Run the report (read-only), capturing STDOUT ONLY (stderr is per-repo progress noise — discard it):
  REPORT=$(python3 /home/node/.claude/skills/slang-pr-report/scripts/pr_report.py --recipient-map /workspace/agent/memory/github-to-discord.json 2>/dev/null); ec=$?
  Interpret ec: 10 = a report is due → continue to STEP 2. 0 = nothing needs attention → do NOT post; stop (this is a normal quiet day). Any other exit (e.g. rate-limited) = transient failure → do NOT post; stop, the next weekday fire retries. Never post a partial/empty report.

STEP 2 — Extract just the report body: everything AFTER the "--- report ---" marker line in $REPORT (drop the "[REPORT] @ ..." header and the per-repo "open" counts). The body starts at "## Slang PR Escalation Report". This is the ONLY content to post — exclude all stderr and the header/counts.

STEP 3 — Post to #slang-committers (channel id 1352357976878481468):
  - TODAY=$(date -u +%F). Intended thread title: "Slang Community/Bot PR Report - $TODAY".
  - IDEAL: create a thread with that title and put the report as a reply inside it — call discord_send_message with thread_name set to that title. IMPORTANT: thread_name only creates a thread on FORUM channels; #slang-committers is a text channel, so thread_name will be ignored and the message posts inline. If it posts inline, make the FIRST line of the first message a bold title: **Slang Community/Bot PR Report - $TODAY** so it is clearly labeled and dated.
  - LENGTH: Discord caps a message at ~2000 chars and the report is larger. Split it into <=1900-char chunks at assignee-bullet boundaries (never split mid-line). Post chunks in order (into the thread if one was created; otherwise sequentially in the channel). Prefix each chunk of a multi-part post with "(part k/N)".
  - Append this disclaimer as the final line of the LAST message only:
    <sub>🤖 Generated by an automated Slang coworker — may be inaccurate. A human maintainer should verify.</sub>

CONSTRAINTS: Post ONLY to #slang-committers. Do not add @-mentions beyond what the recipient-map already renders in the report body. No GitHub writes. If the Discord post fails, report the failure to parent (dashboard) rather than silently dropping it.
```

## `sweep12375-strand-guard-5ca1`

- schedule: `*/30 * * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/sweep12375-guard.sh
```

Prompt:

```
SWEEP #12375 STRANDED — state change detected. shader-slang/slang PR #12375 (the sanctioned sibling sweep for #12342) was MERGED at 2026-08-06T04:44:35Z into its base branch fix/issue-12342 as ba224921a1, NOT into master. The gate data shows which event fired.

WHY THIS EXISTS: the sweep is NOT on master. (Do NOT trust any master sha quoted here: it was 9cd92bb3a1 at arming, 9eb90c50a0 by 07:12Z. RE-MEASURE master yourself; a sha in a scheduled prompt is a state claim that decays and its reader cannot notice. Verify shipped-ness ONLY via the sentinel-file probe with a firing control, never via a remembered sha.) The sweep (SLANG_E_NOT_AVAILABLE for getVersionString/disassemble/disassembleWithResult + the slang-ir.cpp consumer) is NOT on master. Nobody is watching this axis: the #12353 merge guard was cancelled when it fired, and slang-fixer has gone quiet pending a maintainer answer.

DO:
1. Re-read /home/node/.claude/projects/-workspace-agent/memory/project_12375_downstream_sibling_sweep_followup.md for full state, the provenance split, and the recovery measurement.
2. If event=SWEEP_LANDED: the sweep is on master. Re-verify with a firing control (sentinel present AND a known-present control file present), then close the chain and cancel this guard.
3. If event=HUMAN_REPLIED: a real human answered on #12375 after 04:46:52Z. Read the comment. If jkwak-work picked a recovery path (branch PR fix/issue-12342 -> master, or cherry-pick ba224921a1), dispatch slang-fixer on thread_id=gh-issue-shader-slang/slang-12342 to execute exactly what he chose. Recovery is measured clean: merge-tree --write-tree origin/master origin/fix/issue-12342 -> exit 0, 7 files +493/-16, #12353 content NOT re-applied.
4. DO NOT open the recovery PR unasked. Two explanations fit the evidence identically (mis-click vs a deliberate stacked-branch flow he intends to land himself); acting presumes the mistake.
5. NEVER rebase/force-push de705e926d and never touch #12353.
6. After acting, cancel this guard and update the memo.
```

## `t-141e2a`

- schedule: `*/30 * * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
bash /workspace/agent/guard-12304-merge.sh
```

Prompt:

```
CO-TRIGGER FIRED — PR #12304 changed terminal state, and slang#12386 depends on it.

⛔ READ THIS FIRST: the root cause this task's earlier prompt described (createLegalPtrType's address-space allow-list) was RETRACTED 2026-08-08 after slang-fixer implemented it and instrumented the arm — it executes, returns Ptr<void>, and the abort still happens. Do NOT act on it, and do NOT revive the allow-list-vs-deny-list ruling; slang-triager struck all of it from issue comment 5201487089.

CORRECTED MECHANISM (verified at master 716ec597f): the failing operand is the pointer VALUE, not its type. legalizeLocalVar legalizes the pointee (:2216) -> none, misses the `simple` fast path (:2233), falls to default: (:2247) -> declareVars, whose `case LegalType::Flavor::none:` (:3377) returns a bare LegalVal(). So the var legalizes to nothing and fixing the pointer TYPE cannot help. As of 2026-08-08 the fixer was still working the value path; check the issue comment for its current state before assuming this is final.

WHAT SURVIVES THE RETRACTION AND IS STILL WHY THIS GUARD EXISTS: `public struct Empty` + a pointer comparison COMPILES on master (exit 0) and ICEs with #12304's delta applied. #12304 merging therefore converts a silently-working program into a compiler abort in the wild. No test covers that cell, so CI will not report it. That measurement is from 2026-08-06 and master has moved — re-measure, do not trust it.

⚠ ALSO RETRACTED: any advice to defer or hold #12304 on account of #12386. #12304 was APPROVED by csyonghe 2026-08-07. Do NOT suggest holding it.

DO NOW:
1. Read the `data` payload for #12304's actual state (MERGED vs CLOSED).
2. If MERGED: re-measure the `public struct Empty` + pointer-compare cell at the NEW master (it should now ICE where it previously exited 0). Then dispatch slang-triager on thread gh-issue-shader-slang/slang-12386 with the measured result. It owns comment 5201487089 (patched 8x in place, count still 1) and owns the fixer edge.
3. If CLOSED unmerged: co-trigger void. Tell slang-triager on the same thread, then cancel this task.
4. Either way: update /home/node/.claude/projects/-workspace-agent/memory/project_12386_ptr_empty_struct_nonsimple_ice.md, then cancel this series — single-shot by intent.

Do NOT merge or flip anything on GitHub. Do NOT post on #12386 yourself — slang-triager is closest-to-the-state and owns that comment.
```

## `t-d6220f`

- schedule: `one-shot`
- agent group: `ag-1780667166439-vmjrwe`

Prompt:

```
HORIZON 2 (one-shot, #12401 / PR 12410 — SHEPHERD SILENCE bound, set 2026-08-06T23:00Z). Draft PR shader-slang/slang#12410 @ fe09ce469f had kaizhangNV auto-assigned as shepherd by a board sync (jhelferty-nv, comment 5209720279) — that is an ADDRESS, not a commitment: nobody chose to pick it up. This horizon exists so a week of silence produces a REPORT rather than an assumption.
CHECK (read-only):
(1) gh pr view 12410 -R shader-slang/slang --json state,isDraft,assignees,reviewRequests,reviewDecision,comments,updatedAt — has ANY human (non-bot, .user.type==User) commented or reviewed since 2026-08-06T23:00Z? Enumerate comments; do not infer from updatedAt alone.
(2) Did the CI workflow steps land? grep the repo at master for 'cuda-prelude-vector-binary-ops' in .github/workflows/ci-slang-test.yml AND ci-slang-test-container.yml. If present, the top blocker is CLOSED and the fixture is finally compiled by CI.
(3) CI: gh run list -R shader-slang/slang --branch fix/issue-12401 --limit 3 --json databaseId,headSha,status,conclusion — compare headSha to the branch tip; an orphan describes dead code. Report coverage as 'N non-skipped of M', count BEFORE any verdict.
REPORT to parent either way, on thread gh-issue-shader-slang/slang-12401: if untouched, report 'shepherd assigned but silent since 2026-08-06, PR still draft, CI steps not landed' as a FACT. If a human engaged, summarise what they asked.
⛔ DO NOT: ping or @-mention kaizhangNV or any maintainer (reviewer-spam is forbidden by standing orders); mark the PR ready (operator-gated); re-dispatch CI (it just yields); post a duplicate comment — the PR body already carries the blocking [!IMPORTANT] patch block and the yield-red explanation (issuecomment-5209526858).
⭐ If a maintainer asks to WIDEN SCOPE (e.g. add the unary operators, which still carry the same longlong2 pun): that is their call to make. Answer is cheap — one SLANG_CUDA_VECTOR_UNARY_BODY_{2,3,4} family mirroring the binary one — but it was left out because the reporter measured NO compile-time gain there, not because it is structurally hard. Do not absorb a scope widening silently; confirm it as a decision.
```

## `task-1776150548767-euafnj`

- schedule: `7 8 * * *`
- agent group: `ag-1776713258088-r8pp2t`

Prompt:

```
/slang-maintain-release-report daily-report
```

## `task-1776715487702-ftr4s6`

- schedule: `0 */2 * * *`
- agent group: `ag-1776713259045-nax3cr`

Pre-task gate:

```bash
node --input-type=module -e "
const REPO = 'shader-slang/slang';
const H = {
  Authorization: 'token ' + process.env.GH_TOKEN,
  Accept: 'application/vnd.github+json',
  'User-Agent': 'slang-ci-babysitter',
};
const api = async (p) => {
  const r = await fetch('https://api.github.com/repos/' + REPO + p, { headers: H });
  if (!r.ok) throw new Error('GitHub API ' + r.status + ' ' + r.statusText + ' for ' + p);
  return r.json();
};

try {
  const prs = await api('/pulls?state=open&per_page=50');
  if (!Array.isArray(prs)) {
    console.log(JSON.stringify({ wakeAgent: true, data: { error: 'unexpected /pulls response' } }));
    process.exit(0);
  }
  const nonDraft = prs.filter((p) => !p.draft);
  const openNums = new Set(nonDraft.map((p) => p.number));

  const sinceMs = Date.now() - 24 * 3600 * 1000;
  const FAIL = new Set(['failure', 'cancelled', 'timed_out', 'startup_failure']);
  const evicted = [];
  try {
    const runs = await api('/actions/runs?event=merge_group&per_page=50');
    const seen = new Set();
    for (const r of runs.workflow_runs || []) {
      if (!FAIL.has(r.conclusion)) continue;
      if (new Date(r.run_started_at || r.created_at).getTime() < sinceMs) continue;
      const m = /pr-(\d+)-/.exec(r.head_branch || '');
      if (!m) continue;
      const num = Number(m[1]);
      if (!openNums.has(num)) continue;
      if (seen.has(num)) continue;
      seen.add(num);
      evicted.push({
        number: num,
        mergeGroupRunId: r.id,
        workflow: r.name,
        headBranch: r.head_branch,
        conclusion: r.conclusion,
        runStartedAt: r.run_started_at,
        runUrl: r.html_url,
      });
    }
  } catch (e) {
    evicted.push({ error: e.message });
  }

  console.log(JSON.stringify({
    wakeAgent: nonDraft.length > 0,
    data: {
      prCount: nonDraft.length,
      prs: nonDraft.map((p) => ({ number: p.number, title: p.title })).slice(0, 20),
      evicted,
    },
  }));
} catch (e) {
  console.log(JSON.stringify({ wakeAgent: true, data: { error: e.message } }));
}
"
```

Prompt:

```
Run the Slang CI babysitter sweep. Check all non-draft open PRs in shader-slang/slang for failing CI. For each PR with failures:

1. Run `gh pr checks <number> --repo shader-slang/slang` to get check details
2. For failed checks, get the run ID and run `gh run view <run-id> --repo shader-slang/slang --log-failed` to examine logs
3. Classify failures as intermittent (GPU device lost, runner shutdown, process killed, network errors, single-runner failures) vs legitimate (compilation errors, consistent multi-platform failures, assertion failures)
4. Rerun intermittent failures with `gh run rerun <run-id> --repo shader-slang/slang --failed`
5. If a PR was bounced from merge queue due to intermittent failure, requeue with `gh pr merge <number> --repo shader-slang/slang --merge-queue`
6. Track reruns in memory/rerun-tracker.json (max 3 per PR per day, reset daily)
7. Send a brief summary of actions taken. If no actions needed, wrap output in <internal> tags.
```

## `task-1777308843998-o6r5su`

- schedule: `0 0 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
#!/bin/bash
# Nightly release CI trigger with dedupe against concurrent human dispatches.
# Skips dispatch if an in-progress/queued run exists on master, or a run
# succeeded on master within the last 60 minutes.
#
# ⛔ SHAPE-VALIDATE EVERY $(gh ...) CAPTURE THAT FEEDS A TYPED FIELD.
# `gh api` writes its error BODY to STDOUT (only the human "gh: Not Found (HTTP 404)"
# goes to stderr), so `$(gh ... || echo 0)` does NOT replace the body -- it APPENDS,
# yielding e.g. `{"message":"Not Found",...}0`. Measured 2026-08-08, both captures below:
#   EXISTING -> `[ "$EXISTING" -gt 0 ]` rc=2 "integer expression expected" => FELL THROUGH
#               TO DISPATCH. The dedupe failed OPEN: an API hiccup at 00:00 dispatches a
#               duplicate run alongside a human's in-flight one.
#   RUN_ID   -> `[ -n "$RUN_ID" ]` TRUE, so the state file was written with run_id as a
#               JSON OBJECT. It still PARSES, so nothing failed loudly; the 01:30 checker
#               then fetched runs/[object Object], got status=unknown, took the mid-flight
#               branch and emitted wakeAgent:false => TOTAL SILENCE. An error here became
#               silent NON-VERIFICATION downstream -- the 2026-07-20 hole re-entering
#               through the TRIGGER instead of the checker.
# Testing for EMPTY assumes a failing command stays quiet on stdout. Many do not.
# Validate the SHAPE and route everything else to an explicit third state.
WF_ID=106587263
REPO=shader-slang/slang
STATE=/workspace/agent/release-ci-state.json

is_uint() { printf '%s' "$1" | grep -qE '^[0-9]+$'; }

EXISTING_RAW=$(gh api "repos/$REPO/actions/workflows/$WF_ID/runs?branch=master&per_page=10" \
  --jq '[.workflow_runs[] | select(
            .status=="in_progress" or .status=="queued"
            or (.conclusion=="success" and ((now - (.created_at | fromdateiso8601)) < 3600))
        )] | length' 2>/dev/null)

if ! is_uint "$EXISTING_RAW"; then
  # Cannot determine whether a run already exists. Do NOT fail open into a dispatch
  # (duplicate CI) and do NOT fail silently (unverified day). Record it so the 01:30
  # checker REPORTS the instrument failure instead of finding nothing and going quiet.
  cat > "$STATE" <<EOF
{"run_id": null, "status": "pending", "action": "trigger_failed", "conclusion": "null", "stage": "dedupe_query", "detail": "the dedupe query did not return a bare integer, so it is unknown whether a run already exists on master; no dispatch was attempted"}
EOF
  echo '{"wakeAgent": false, "data": {"trigger_failed": true, "stage": "dedupe_query"}}'
  exit 0
fi

if [ "$EXISTING_RAW" -gt 0 ]; then
  # Clear any stale state so the 01:30 check task does nothing
  echo '{"run_id": null, "status": "reported", "action": null, "conclusion": "skipped", "reason": "existing run on master"}' > "$STATE"
  echo '{"wakeAgent": false, "data": {"skipped": true, "reason": "existing or recent run on master"}}'
  exit 0
fi

# No existing run — dispatch our nightly.
# ⛔ The dispatch result was previously DISCARDED (`gh api ... 2>/dev/null` with no rc
# check). A rejected dispatch then slept 12s and pinned whatever run happened to be
# newest -- possibly YESTERDAY's, i.e. manufacturing the stale-pin condition rather
# than merely failing to avoid it.
DISPATCH_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)
if ! gh api "repos/$REPO/actions/workflows/$WF_ID/dispatches" -f ref=master >/dev/null 2>&1; then
  cat > "$STATE" <<EOF
{"run_id": null, "status": "pending", "action": "trigger_failed", "conclusion": "null", "stage": "dispatch", "detail": "the workflow_dispatch call itself failed (non-zero exit); no run was started by this task, so today has no nightly run unless a human or another trigger started one"}
EOF
  echo '{"wakeAgent": false, "data": {"trigger_failed": true, "stage": "dispatch"}}'
  exit 0
fi

# ⛔ Pin the run WE dispatched, not merely the newest run on master. Filtering by
# event=workflow_dispatch and requiring created_at >= our dispatch timestamp stops us
# pinning somebody's push-triggered run (or an older dispatch) as "today's nightly".
#
# ⛔ `--arg` IS A jq(1) FLAG, NOT A `gh api` FLAG. Measured 2026-08-09: this query was
# written `--jq --arg since "$DISPATCH_AT" '<filter>'`, so `--jq` swallowed the literal
# string `--arg` as its filter and `gh api` then saw FOUR positional args:
#   $ gh api URL --jq --arg since "2026-08-09T00:00:06Z" '<filter>'
#   accepts 1 arg(s), received 4    (rc=1, NO HTTP REQUEST EVER MADE)
# The same filter with the timestamp INLINED returns the id instantly. So the failure was
# argument parsing, not GitHub: it could never succeed on any night, at any budget, and
# `sleep 12` + eventual-consistency were both red herrings. INLINE the value via a jq
# string interpolation of a shell variable; do not reach for `--arg` here.
#
# ⛔ And `2>/dev/null` HID THE ONLY SYMPTOM. `accepts 1 arg(s), received 4` went to stderr;
# stdout was empty, `is_uint ""` was correctly false, so this landed in the pin_resolve
# branch and reported "no run resolved within 12s" — a TRUE sentence describing the wrong
# cause, which sent the diagnosis to timing for a day. Keep stderr and put it in `detail`.
PIN_ERR=/tmp/pin-resolve-stderr.$$
RUN_ID_RAW=""
PIN_RC=0
for attempt in 1 2 3; do
  sleep 12
  RUN_ID_RAW=$(gh api "repos/$REPO/actions/workflows/$WF_ID/runs?branch=master&event=workflow_dispatch&per_page=10" \
    --jq "[.workflow_runs[] | select(.created_at >= \"$DISPATCH_AT\")] | sort_by(.created_at) | .[0].id // empty" 2>"$PIN_ERR")
  PIN_RC=$?
  is_uint "$RUN_ID_RAW" && break
done
PIN_STDERR=$(tr -d '"\n\r\\' < "$PIN_ERR" | cut -c1-300)
rm -f "$PIN_ERR"

if is_uint "$RUN_ID_RAW"; then
  echo "{\"run_id\": $RUN_ID_RAW, \"status\": \"pending\", \"action\": \"check_run\", \"conclusion\": \"null\", \"dispatched_at\": \"$DISPATCH_AT\"}" > "$STATE"
  echo "{\"wakeAgent\": false, \"data\": {\"triggered\": true, \"run_id\": $RUN_ID_RAW}}"
else
  # Dispatch succeeded but we could not resolve WHICH run is ours. Never write a
  # poisoned pin: an unparseable/object run_id used to sail through `-n` and turn into
  # silence at 01:30. Say so instead, so the checker reports it.
  cat > "$STATE" <<EOF
{"run_id": null, "status": "pending", "action": "trigger_failed", "conclusion": "null", "stage": "pin_resolve", "detail": "the dispatch succeeded but no workflow_dispatch run created at or after $DISPATCH_AT resolved to a numeric id across 3 attempts over ~36s; gh exit=$PIN_RC and its stderr was: [$PIN_STDERR] -- a NON-EMPTY stderr here means the query itself never reached GitHub (e.g. an argument-parsing error), which is an instrument bug and NOT a slow or missing run; an EMPTY stderr with exit 0 means the query ran and genuinely matched nothing. A run may well exist either way, so check master for today's run rather than assuming none exists", "dispatched_at": "$DISPATCH_AT"}
EOF
  echo '{"wakeAgent": false, "data": {"trigger_failed": true, "stage": "pin_resolve"}}'
fi
```

Prompt:

```
Nightly CI trigger — no action needed from agent.
```

## `task-1777346910467-p0dxfu`

- schedule: `30 1 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
#!/bin/bash
STATE=$(cat /workspace/agent/release-ci-state.json 2>/dev/null || echo '{}')
ACTION=$(echo "$STATE" | node -e "try{const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.action||'')}catch{console.log('')}")
STATUS=$(echo "$STATE" | node -e "try{const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.status||'')}catch{console.log('')}")

WF_ID=106587263
REPO=shader-slang/slang

# ⛔ TRIGGER-FAILED PASSTHROUGH. Must be evaluated BEFORE the "nothing pinned" branch
# below, which would otherwise count today's runs and go QUIET if any run happened to
# exist -- leaving the day unverified with no report. The 00:00 trigger writes this shape
# when it could not (a) determine whether a run already existed, (b) dispatch at all, or
# (c) resolve WHICH run is ours. Before 2026-08-08 those three cases wrote a poisoned or
# missing pin and this checker went silent: measured, a 404 body captured into RUN_ID
# sailed past `[ -n ]`, landed in the state file as a JSON OBJECT (which still parses),
# and the pin fetch then produced runs/[object Object] -> status=unknown -> the mid-flight
# branch -> wakeAgent:false. An upstream instrument error became silent NON-VERIFICATION.
if [ "$ACTION" = "trigger_failed" ]; then
  STAGE=$(echo "$STATE" | node -e "try{const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.stage||'unknown')}catch{console.log('unknown')}")
  DETAIL=$(echo "$STATE" | node -e "try{const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log((d.detail||'').replace(/\"/g,''))}catch{console.log('')}")
  TODAY=$(date -u +%Y-%m-%d)
  # Say whether a run exists today regardless, so the report can distinguish
  # "trigger broke AND nothing ran" from "trigger broke but something ran unpinned".
  TODAY_N=$(gh api "repos/$REPO/actions/workflows/$WF_ID/runs?branch=master&per_page=20" --jq "[.workflow_runs[] | select(.created_at | startswith(\"$TODAY\"))] | length" 2>/dev/null)
  case "$TODAY_N" in ''|*[!0-9]*) TODAY_N="unknown" ;; esac
  echo "{\"wakeAgent\": true, \"data\": {\"liveness\": \"trigger_failed\", \"date\": \"$TODAY\", \"stage\": \"$STAGE\", \"detail\": \"$DETAIL\", \"runs_on_master_today\": \"$TODAY_N\", \"reason\": \"the 00:00 trigger task reported its own failure at stage=$STAGE, so no pinned run exists for today. This is NOT a green and NOT a CI failure - it is an instrument failure upstream of this check. Report the stage and detail verbatim, state that today release health is UNVERIFIED, and use runs_on_master_today to say whether anything ran unpinned (a non-zero count means a run exists that this checker never verified). Do NOT write status reported.\"}}"
  exit 0
fi

if [ "$STATUS" = "reported" ] || [ -z "$ACTION" ] || [ "$ACTION" != "check_run" ]; then
  # Nothing pinned to check. Three very different worlds land here:
  #   (a) today's run was already dispatched+reported  -> genuinely nothing to do
  #   (b) the 00:00 dispatcher never fired at all      -> release health UNVERIFIED
  #   (c) the GitHub query did not work                -> instrument failure, infer nothing
  # Before this branch existed both (a) and (b) were silent, so (b) was undetectable.
  #
  # NOTE FOR FUTURE EDITORS: the "today" filter is deliberately DAY-GRANULAR
  # (startswith on the UTC date), NOT a narrow window around 00:00. Measured
  # 2026-08-07: that day's dispatch fired at 00:28:33Z while the 14 preceding
  # nightly dispatches all fired 00:00:01-00:00:57Z. A tight window would have
  # turned that jitter into a false no_dispatch alarm. Do not narrow this.
  TODAY=$(date -u +%Y-%m-%d)
  RUNS_JSON=$(gh api "repos/$REPO/actions/workflows/$WF_ID/runs?branch=master&per_page=20" 2>/dev/null || echo '')
  SUMMARY=$(echo "$RUNS_JSON" | TODAY="$TODAY" node -e "
    try {
      const d = JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));
      const runs = d.workflow_runs;
      if (!Array.isArray(runs)) { console.log('BAD'); process.exit(0); }
      const today = runs.filter(r => String(r.created_at||'').startsWith(process.env.TODAY));
      const stamps = runs.map(r => r.created_at).filter(Boolean).sort();
      const newest = stamps.length ? stamps[stamps.length-1] : '';
      const ageH = newest ? ((Date.now() - Date.parse(newest)) / 3600000).toFixed(1) : '';
      console.log(JSON.stringify({ n: today.length, newest, ageH }));
    } catch { console.log('BAD'); }
  " 2>/dev/null || echo BAD)

  # A non-numeric / unparseable result must NEVER fall through to the quiet
  # "nothing to do" branch -- that is the same silent-skip hole this whole
  # branch exists to close, one level down. Route it to query_failed.
  if [ "$SUMMARY" = "BAD" ] || [ -z "$SUMMARY" ]; then
    echo '{"wakeAgent": true, "data": {"liveness": "query_failed", "reason": "could not reach GitHub or could not parse the runs list; inferring neither a green nor a missed dispatch"}}'
    exit 0
  fi

  TODAY_RUNS=$(echo "$SUMMARY" | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.n)")
  NEWEST=$(echo "$SUMMARY" | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.newest)")
  AGE_H=$(echo "$SUMMARY" | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.ageH)")

  if [ "$TODAY_RUNS" = "0" ]; then
    # ⛔ STALE-WORKFLOW-PIN CROSS-CHECK. Runs BEFORE any no_dispatch alarm, because a
    # renamed/moved workflow file produces a PERFECT no_dispatch impostor: GitHub keys
    # workflow identity to the PATH, so after a rename the OLD id still resolves and
    # still returns its OLD runs -- today's count goes to 0 and this branch would report
    # "the dispatcher is broken" while release CI is healthy under a NEW id.
    #
    # The path endpoint is the right instrument because, unlike the id lookup, it CANNOT
    # quietly return a stale answer -- it 404s on a path that does not exist. Measured
    # 2026-08-08 by slang-release-regression-check:
    #   workflows/.github%2Fworkflows%2Frelease.yml -> id 106587263, active
    #   workflows/.github%2Fworkflows%2Fnope.yml     -> 404  (control)
    # Bounded, not hypothetical: release.yml has never been renamed (last 3 touches
    # 07-18 #12149, 07-13 #12085, 07-01 #11868 all edits in place), but
    # .github/workflows/ took 10 commits 07-31 -> 08-07 including #12422, the
    # parallelism change that produced the 37->41 job-census move. Low probability,
    # non-zero, actively churning, one API call.
    #
    # This is deliberately IN THE MECHANISM rather than an instruction in the prompt:
    # a check left to the agent's discretion is a control that fires by luck, which is
    # the exact defect the census patch above was written to remove.
    WF_PATH=".github/workflows/release.yml"
    # ⛔ On a 404 this `gh` writes the error BODY to STDOUT and exits non-zero, so a naive
    # `$(gh ... || echo '')` captures {"message":"Not Found",...} -- a NON-EMPTY value that
    # sails past `-z`, gets spliced into a numeric JSON field, and emits UNPARSEABLE JSON.
    # Caught 2026-08-08 by CONSTRUCTING the missing-path case (control 5); control 4, the
    # rename case, passed and gave no hint. Consequence had it shipped: the branch added to
    # raise this alarm would have emitted garbage instead -- silence on the alarm path, the
    # same silent-skip hole one level down. So: require a BARE INTEGER, and treat anything
    # else (404 body, empty, error text) as "cross-check unavailable".
    PATH_ID_RAW=$(gh api "repos/$REPO/actions/workflows/.github%2Fworkflows%2Frelease.yml" --jq '.id' 2>/dev/null)
    case "$PATH_ID_RAW" in
      ''|*[!0-9]*) PATH_ID='' ;;
      *)           PATH_ID="$PATH_ID_RAW" ;;
    esac

    if [ -z "$PATH_ID" ]; then
      # Cross-check unavailable (404 => file really is gone/moved, or the call failed).
      # Do NOT silently fall through to a confident no_dispatch: say it is unvalidated.
      echo "{\"wakeAgent\": true, \"data\": {\"liveness\": \"no_dispatch\", \"date\": \"$TODAY\", \"newest_run_created_at\": \"$NEWEST\", \"newest_run_age_hours\": \"$AGE_H\", \"pin_crosscheck\": \"unavailable\", \"reason\": \"no release run on master today for pinned workflow $WF_ID, BUT the path cross-check on $WF_PATH returned nothing (404 or call failed). A renamed/moved workflow file is indistinguishable from a dead dispatcher at the id endpoint. Report this as UNVALIDATED: state that no run was found for the pinned id AND that you could not confirm the pin still points at a live file. Do not assert the dispatcher is broken.\"}}"
      exit 0
    fi

    if [ "$PATH_ID" != "$WF_ID" ]; then
      # The file moved / was replaced: a NEW workflow id owns release.yml. This is NOT a
      # missed dispatch. Release CI may be perfectly green under $PATH_ID.
      echo "{\"wakeAgent\": true, \"data\": {\"liveness\": \"stale_workflow_pin\", \"date\": \"$TODAY\", \"pinned_wf_id\": $WF_ID, \"path_wf_id\": $PATH_ID, \"wf_path\": \"$WF_PATH\", \"reason\": \"$WF_PATH is now workflow id $PATH_ID but this guard pins $WF_ID - the workflow file was renamed, moved or replaced, so the pinned id returns only its OLD runs and today's count of 0 is an ARTIFACT. This is NOT a missed dispatch and NOT a failure. Release CI may be green under $PATH_ID. Report the id change, check runs under $PATH_ID for today, and tell the operator the guard's WF_ID needs updating.\"}}"
      exit 0
    fi
    # Carry newest/age so the reader can tell "nothing since 25h ago" (plausibly a
    # late dispatch that will still fire) from "nothing for days" (dispatcher broken).
    # A single daily check CANNOT distinguish late-from-never on its own; reporting
    # the age is what makes that call possible instead of conflated.
    echo "{\"wakeAgent\": true, \"data\": {\"liveness\": \"no_dispatch\", \"date\": \"$TODAY\", \"newest_run_created_at\": \"$NEWEST\", \"newest_run_age_hours\": \"$AGE_H\", \"reason\": \"no release run on master today - 00:00 dispatcher appears not to have fired; newest run on master is ${AGE_H}h old ($NEWEST). A late-but-present dispatch and a never-fired dispatch look identical at this check time - use the age to judge.\"}}"
  else
    echo "{\"wakeAgent\": false, \"data\": {\"reason\": \"no pending run to check; $TODAY_RUNS run(s) on master today; newest ${AGE_H}h old\"}}"
  fi
  exit 0
fi

RUN_ID=$(echo "$STATE" | node -e "try{const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.run_id||'')}catch{console.log('')}")
RUN_JSON=$(gh api "repos/$REPO/actions/runs/$RUN_ID" --jq '{status,conclusion,html_url,head_sha,created_at,updated_at}' 2>/dev/null || echo '{}')
RUN_STATUS=$(echo "$RUN_JSON" | node -e "try{const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.status||'unknown')}catch{console.log('unknown')}")
RUN_CONCLUSION=$(echo "$RUN_JSON" | node -e "try{const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.conclusion||'null')}catch{console.log('null')}")

RUN_CREATED=$(echo "$RUN_JSON" | node -e "try{const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(d.created_at||'')}catch{console.log('')}")
TODAY=$(date -u +%Y-%m-%d)

if [ "$RUN_STATUS" != "completed" ]; then
  # Mid-flight. Do NOT wake: conclusion=null must never be read as a failure.
  # NOTE: this leaves the pin UNRESOLVED, which is why the staleness check below
  # exists -- see it before assuming a re-check happens.
  echo "{\"wakeAgent\": false, \"data\": {\"reason\": \"run still in progress (status=$RUN_STATUS, conclusion=null) - not a failure, awaiting completion\"}}"
  exit 0
fi

# ⛔ STALE-PIN GUARD. The checker fires once a day (30 1 * * *). If a pinned run is
# still in flight at 01:30 the pin stays unresolved, and 24h later this branch would
# find it completed and emit a YESTERDAY run as today's result -- structurally the
# 2026-07-20 stale-re-emit bug, re-entered through the pin rather than through
# "latest run on master". Measured 2026-08-07: a pin on the 08-06 run returned
# {conclusion: success} with nothing marking it as a day old.
# The dispatch->01:30 slack is only ~90 min (36-48 min normal; 14 min on 08-07),
# so this is reachable, not theoretical.
if [ -n "$RUN_CREATED" ] && [ "${RUN_CREATED:0:10}" != "$TODAY" ]; then
  echo "{\"wakeAgent\": true, \"data\": {\"liveness\": \"stale_pin\", \"date\": \"$TODAY\", \"run_id\": $RUN_ID, \"run_created_at\": \"$RUN_CREATED\", \"conclusion\": \"$RUN_CONCLUSION\", \"run_json\": $RUN_JSON, \"reason\": \"the pinned run was created ${RUN_CREATED} which is NOT today ($TODAY) - it was most likely still in flight at a previous check and never resolved. Do NOT report it as today's result. Today release health is UNVERIFIED unless a run created today is found.\"}}"
  exit 0
fi

# ⛔ JOB CENSUS. The run-level conclusion is a ROLL-UP: a run whose jobs were mostly
# never scheduled can still report conclusion=success, so "success" alone does not mean
# "the matrix ran". Until 2026-08-08 this script read ONLY the roll-up and the census was
# hand-run by the agent when it happened to think of it -- a control that fires by luck.
# Measure it here so it cannot be skipped.
#
# ⛔ DO NOT compare JOB_TOTAL against a remembered constant. Measured 2026-08-08 by
# slang-release-regression-check: merge-queue CI's census moved 37 -> 41 when two
# windows-gpu test-slang jobs split into three per-API jobs each (-2 +6). That is a
# restructure, not new territory -- but any check keyed to a stored total would have
# read it as an anomaly. Carry the census; let the reader state what was measured.
# The ONLY census check that is safe here is INTERNAL CONSISTENCY: a run cannot be
# "success" with zero jobs, and a non-success job contradicts a success roll-up.
JOBS_JSON=$(gh api "repos/$REPO/actions/runs/$RUN_ID/jobs?per_page=100" --paginate 2>/dev/null || echo '')
CENSUS=$(echo "$JOBS_JSON" | node -e "
  try {
    const txt = require('fs').readFileSync('/dev/stdin','utf8').trim();
    if (!txt) { console.log('BAD'); process.exit(0); }
    // --paginate concatenates one JSON object per page
    const jobs = [];
    for (const chunk of txt.split(/(?<=\})\s*(?=\{)/)) {
      let p; try { p = JSON.parse(chunk); } catch { continue; }
      if (Array.isArray(p.jobs)) jobs.push(...p.jobs);
    }
    if (!jobs.length) { console.log('BAD'); process.exit(0); }
    const by = {};
    for (const j of jobs) by[j.conclusion || 'null'] = (by[j.conclusion || 'null'] || 0) + 1;
    const notSuccess = jobs.filter(j => j.conclusion !== 'success').map(j => j.name + '=' + j.conclusion);
    console.log(JSON.stringify({ total: jobs.length, by, notSuccess }));
  } catch { console.log('BAD'); }
" 2>/dev/null || echo BAD)

if [ "$CENSUS" = "BAD" ] || [ -z "$CENSUS" ]; then
  # The census instrument failed. Do NOT silently degrade to a roll-up-only green --
  # that is the hole this block exists to close. Say the census is missing.
  echo "{\"wakeAgent\": true, \"data\": {\"run_id\": $RUN_ID, \"conclusion\": \"$RUN_CONCLUSION\", \"run_json\": $RUN_JSON, \"job_census\": \"unavailable\", \"census_note\": \"could not list or parse jobs for this run; the roll-up conclusion is UNCORROBORATED. Report the conclusion AND that the per-job census could not be measured - do not present it as a verified matrix pass.\"}}"
  exit 0
fi

echo "{\"wakeAgent\": true, \"data\": {\"run_id\": $RUN_ID, \"conclusion\": \"$RUN_CONCLUSION\", \"run_json\": $RUN_JSON, \"job_census\": $CENSUS, \"census_note\": \"job_census is MEASURED for this run. Report total and the per-conclusion breakdown as measured. Do NOT compare total against any remembered number - the census is not a constant (merge-queue CI moved 37->41 on 2026-08-08 via a windows-gpu per-API split). Treat only INTERNAL contradictions as findings: success roll-up with a non-success job, or zero jobs.\"}}"
```

Prompt:

```
Check release CI result. Send report to slang-release-regression-check only. No output to main channel. All reasoning in <internal> tags.

ROUTING [MUST]: send with thread_id="release-ci-nightly" on every send_message to slang-release-regression-check. Do NOT send thread-less — a thread-less send lands on their legacy session sess-1780395853618-jx2g7i (thread_id=worktree-cleanup-2026-06-02), which is misnamed for this work. thread_id is fixed at session birth, so this key mints/reuses a correctly-named session; the legacy one is retained, not GC-d.

The guard script hands you one of SIX shapes in data:
1. {run_id, conclusion, run_json, job_census, census_note} — a pinned run completed
   AND was created today.
   - Success: send OK message with run link.
   - Failure: get commits since last success and failed jobs, send full report.
   Then update /workspace/agent/release-ci-state.json with status "reported".

   ⛔ ALWAYS REPORT job_census — it is measured by the guard, not by you, and reporting
   it is not optional. The run-level conclusion is a ROLL-UP: a run whose jobs were
   mostly never scheduled still reports conclusion=success, so "success" alone does not
   mean "the matrix ran". State total and the per-conclusion breakdown as measured.
   ⛔ DO NOT compare the total against any remembered number. The census is NOT a
   constant: on 2026-08-08 merge-queue CI moved 37 -> 41 when two windows-gpu
   test-slang jobs each split into three per-API jobs (-2 +6) — a restructure, not new
   coverage, and any check keyed to a stored total would have called it an anomaly.
   The only census findings that are real are INTERNAL CONTRADICTIONS: a success
   roll-up alongside a non-success job, or zero jobs.
   If job_census == "unavailable", the census instrument failed: report the conclusion
   AND that the per-job census could not be measured. Do not present that as a
   verified matrix pass.
2. {liveness: "no_dispatch", date, newest_run_created_at, newest_run_age_hours} — the
   00:00 dispatcher never fired, so today release health is UNVERIFIED. This is NOT a
   green. Report it as a liveness gap: no run on master for that date, release health
   unknown, nothing to regress-check. USE newest_run_age_hours to judge late-vs-never:
   ~25h means the dispatch is probably just late (the check fires at 01:30 and normal
   slack is only 36-48 min, measured 14 min on 2026-08-07); ~48h+ means the dispatcher
   is broken and the operator should know. Report the age, do not hide it behind a
   verdict. Do not re-emit any prior run as today result. Do not touch the state file.
   (History: on 2026-07-20 a stale run was re-emitted as a fresh green;
   silence-on-missed-dispatch was the remaining half of that hole.)
   ⚠️ This shape is now CROSS-CHECKED IN THE MECHANISM, so you no longer have to
   remember to validate the pin by hand (a check left to your discretion is a control
   that fires by luck). Before emitting no_dispatch the guard resolves
   .github/workflows/release.yml via the PATH endpoint — which 404s rather than quietly
   returning stale data the way the id endpoint does — and compares it to WF_ID.
   Therefore: a no_dispatch WITHOUT pin_crosscheck means the pin was validated and the
   missed dispatch is real. If data carries pin_crosscheck: "unavailable", the path
   lookup did not return a bare id: report no_dispatch as UNVALIDATED and do NOT assert
   the dispatcher is broken.
3. {liveness: "query_failed"} — could not reach GitHub, OR reached it and could not
   parse the runs list (a 200 with an error body exits 0 and used to fall through to
   silence). Report the instrument failure as such; do not infer a green, a missed
   dispatch, or a failure.
4. {liveness: "stale_pin", run_id, run_created_at, conclusion, run_json} — the pinned
   run COMPLETED but was created on an EARLIER DAY. This happens when a run was still
   in flight at a previous 01:30 check: the pin never resolved, and 24h later it looks
   like a fresh completed run. This is NOT today's result and NOT a green, whatever its
   conclusion says. Report: today release health UNVERIFIED, the pin is stale, name
   run_created_at and the conclusion but explicitly label it as the earlier day's run.
   Then check whether a run created today exists on master and say so. Do NOT write
   status "reported" for today on the strength of a stale pin.
   (Found 2026-08-07 by measurement: a pin on the 08-06 run returned {conclusion:
   success} with nothing marking it a day old — structurally the 07-20 re-emit bug
   re-entered through the pin instead of through "latest run on master".)
5. {liveness: "stale_workflow_pin", pinned_wf_id, path_wf_id, wf_path} — the guard pins
   WF_ID=106587263 but .github/workflows/release.yml now resolves to a DIFFERENT id, so
   the workflow file was renamed, moved or replaced. GitHub keys workflow identity to the
   FILE PATH: after a rename the old id still resolves and still returns its OLD runs, so
   "0 runs today" is an ARTIFACT of the stale pin. This is NOT a missed dispatch and NOT
   a failure — release CI may be perfectly green under path_wf_id. Report the id change,
   check today's runs under path_wf_id, and tell the operator WF_ID needs updating.
   Do NOT write status "reported" and do NOT report a dispatcher as broken.
   (Bounded 2026-08-08 by slang-release-regression-check: release.yml has never been
   renamed — last 3 touches 07-18 #12149, 07-13 #12085, 07-01 #11868 are edits in place —
   but .github/workflows/ took 10 commits 07-31→08-07 including #12422, the parallelism
   change behind the 37→41 census move. Low probability, non-zero, churning, one call.)

6. {liveness: "trigger_failed", stage, detail, runs_on_master_today} — the 00:00 TRIGGER
   task reported its own failure, so no pinned run exists for today. stage is one of
   dedupe_query (could not tell whether a run already existed; no dispatch attempted),
   dispatch (the workflow_dispatch call failed), or pin_resolve (dispatch succeeded but
   no workflow_dispatch run created after the dispatch resolved to a numeric id).
   This is NOT a green and NOT a CI failure — it is an instrument failure UPSTREAM of
   this check. Report stage + detail verbatim, say today release health is UNVERIFIED,
   and use runs_on_master_today: a non-zero count means a run exists that this checker
   never verified (say so explicitly); "unknown" means even that query failed.
   Do NOT write status "reported".
   (Found 2026-08-08 by reading the TRIGGER after hardening this checker: `gh api` writes
   its error BODY to STDOUT, so the trigger's `RUN_ID=$(gh ... || echo '')` captured a 404
   object, passed `[ -n ]`, and wrote run_id as a JSON OBJECT — which still parses. This
   checker then fetched runs/[object Object], got status=unknown, took the mid-flight
   branch and emitted wakeAgent:false. TOTAL SILENCE from an upstream error: the 07-20
   hole re-entering through the trigger. The trigger now shape-validates every capture and
   never writes a poisoned pin; this shape is how its failures reach you.)

A mid-flight run (status != completed) does NOT wake you: the guard returns
wakeAgent:false. conclusion=null is never a failure.
```

## `task-1780670816061-rgq8eo`

- schedule: `0 */12 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Prompt:

```
/supervise-issues

DISCOVER FIRST (every tick, before anything else): run `ncl sessions list --limit 10000 --json` and filter `thread_id` to the `gh-issue-` prefix client-side (there is NO `--thread-prefix` flag — `ncl` silently ignores it and returns ALL sessions) to enumerate ALL live chains, then UNION with memory/supervisor-state.json keys. Any gh-issue session not already journaled (by KEY, not substring) is a NEW chain — add and journal it this tick. The tracker + state JSON are the PRIOR snapshot for deltas ONLY, never the chain universe. Follow the /supervise-issues skill (SKILL.md + reference.md) for all rules: board spec (11 columns incl. CI), classification, CI/rebase-master nudge, artifact enforcement, ball-in-our-court, thread hygiene, postmortem, worktree GC.

DELIVERY (cron session has no default reply target): post the board with send_message(to="orchestrator-dashboard", text=<board>) — or an inline <message to="orchestrator-dashboard">…</message> block — in the FINAL response. NEVER omit to=. NO send_file unless the inline board posted first. Retry to="orchestrator-dashboard" on "multiple destinations".

APPEND: After delivering the board, append this tick's full 11-column tracker table to reports/issue-chain-tracker.md with a `## Tick <N> — <ISO timestamp>` header. This builds a persistent history so trends (chains going silent, recurring blockers, GC patterns) are visible across ticks.

THREAD KEYS ARE COPIED, NEVER COMPOSED (measured defect 2026-08-07): every `thread_id` you dispatch on MUST be copied byte-for-byte from the `thread_id` column of `ncl sessions list`. NEVER build one from a repo name. A supervisor tick composed `gh-issue-slang/slang-12371` (owner `shader-slang` dropped), which minted a SECOND slang-triager session for issue 12371 — invisible to every other tier's dispatch on the canonical key, and it published a 4,271-char public comment on #12391 under bot identity that the real session has no record of. Two of the notices riding the malformed key were themselves complaints about malformed thread keys. Before sending, diff your key against the sessions-list value character by character; if the chain has no session yet, the canonical form is `gh-issue-<owner>/<repo>-<num>` with the FULL owner/repo.
```

## `task-1781522302095-mjy6s1`

- schedule: `0 3 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Prompt:

```
TASK: Nightly knowledge_base sync for the slang-coworkers/nanoclaw fork → nv-coworkers branch. You have standing authority (dashboard-admin, 2026-06-12/15) to push, open PRs, and merge into nv-coworkers on THIS fork ONLY. Act autonomously. Stay completely SILENT on no-op nights (no message). Only message the dashboard (orchestrator-dashboard) if something was merged or something failed.

CRITICAL TOOLING NOTES (verified 2026-06-15):
- The nv-slang-bot GitHub App CANNOT use GraphQL PR mutations. `gh pr create` / `gh pr merge` fail with "Resource not accessible by integration". You MUST use the REST API via `gh api` for PR create and merge (shown below). This is a hard requirement.
- `rsync` is NOT installed — mirror sources with rm -rf + cp.
- nv-coworkers is protected by ruleset "Protect nv-* branches": requires a PR, but 0 approvals / 0 checks. Direct push to nv-coworkers is BLOCKED — you must go through a PR.

STEP 1 — prepare clone:
  cd /workspace/agent/nanoclaw-kb  (if missing: gh repo clone slang-coworkers/nanoclaw /workspace/agent/nanoclaw-kb && cd /workspace/agent/nanoclaw-kb)
  git fetch origin nv-coworkers -q
  ⛔ CO-TENANT GATE (added 2026-08-10 — this clone is SHARED; it currently carries live worktrees
     /workspace/agent/wt-1171 and wt-1171-base, and `checkout -f`/`reset --hard` here silently
     discard any sibling session's uncommitted work. A guard whose output nothing BRANCHES on is a
     log line, not a guard — so this one exits.)
     MODS=$(git status --porcelain | grep -v '^??' | wc -l)
     test "$MODS" -eq 0 || { echo "ABORT: $MODS tracked modification(s) in the shared clone are not mine to discard"; git status --porcelain | grep -v '^??'; exit 1; }
     Control (must print ABORT, then clean up): echo x >> README.md; re-run the two lines above; git checkout -- README.md
     A zero from this gate means nothing until the control has printed ABORT on the same run.
  git checkout -f nv-coworkers -q
  git reset --hard origin/nv-coworkers -q   # safe ONLY because the gate above exited on any tracked mod
  git config user.name "nv-slang-bot[bot]"
  git config user.email "274397474+nv-slang-bot[bot]@users.noreply.github.com"

STEP 2 — mirror the four live sources into knowledge_base/ (clear+copy so deletions are reflected). For each of the three directory sources: rm -rf <dest>; mkdir -p <dest>; cp -rL <src>. <dest>/
  /workspace/shared/learnings/                          -> knowledge_base/shared/learnings
  /workspace/agent/memory/                              -> knowledge_base/agent/memory
  /home/node/.claude/projects/-workspace-agent/memory/  -> knowledge_base/auto-memory
  Then docs: rm -rf knowledge_base/agent/docs; mkdir -p knowledge_base/agent/docs; for f in /workspace/agent/*.md; do cp -L "$f" knowledge_base/agent/docs/; done

STEP 3 — SCRUB clear PII (redacts real emails + any secrets/tokens; intentionally LEAVES GitHub @handles and internal URLs per admin):
  python3 /workspace/agent/scrub_kb_pii.py --apply knowledge_base
  Then sanity-check no raw emails remain: grep -rhoE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' knowledge_base | grep -viE 'noreply|users\.noreply|@example\.|@anthropic\.com|placeholder@' | sort -u  (should be empty)

STEP 4 — stage: git add -A knowledge_base ; git add -f knowledge_base/agent/docs/CLAUDE.local.md  (CLAUDE.local.md is gitignored — force-add).

STEP 4b — DATA-ONLY GATE (added 2026-08-10; the tree already carries 6 executables under knowledge_base/):
  Nothing under knowledge_base/ may be executable, and no gitlink may ever be staged.
    git -C . diff --cached --name-only -z | xargs -0r -I{} sh -c 'test -e "{}" || exit 0; case $(git ls-files -s "{}" | cut -d" " -f1) in 100755) echo "EXEC: {}";; 160000) echo "GITLINK: {}";; esac'
  Any EXEC line: `git update-index --chmod=-x <path>` (publish it as data, not as a program).
  Any GITLINK line: STOP and report it -- never merge a gitlink.
  Control (must print one EXEC line, then clean up): touch knowledge_base/.gate-probe && chmod +x knowledge_base/.gate-probe && git add -f knowledge_base/.gate-probe  -> re-run the check -> git rm -f --cached knowledge_base/.gate-probe && rm -f knowledge_base/.gate-probe
  A zero from this check means nothing until the control has printed on the same run.

STEP 5 — if `git diff --cached --quiet` exits 0: NOTHING changed. Exit. Send NO message.

STEP 6 — commit + push branch:
  D=$(date -u +%Y%m%d); BR="kb-sync-$D"
  git checkout -b "$BR"
  git commit -m "knowledge_base sync $(date -u +%Y-%m-%d) (PII scrub: emails redacted)"
  (if the pre-commit hook fails due to missing prettier/node_modules: run `pnpm install --frozen-lockfile` once then retry the commit — NEVER use --no-verify)
  git push -u origin "$BR"

STEP 6b — BODY DISCIPLINE (added 2026-08-10 after the SAME fabricated rationale shipped in #1148 and #1156):
  The PR body must stay the fixed boilerplate in STEP 7. Do NOT add a narrative explanation of
  WHY an exclusion exists, and do NOT describe any condition as new/changed ("became a git repo
  on <date>", "since the last sync", "would have collapsed into a gitlink"). The exclusions
  (.git/, __pycache__, *.pyc) are belt-and-braces and need no rationale in the artifact.
  If you believe you have found a genuinely NEW hazard, you must first run BOTH checks below and
  paste their real output into your dashboard report (STEP 9) — never into the PR body:
    # a directory mtime is the LAST WRITE, never a creation time -- ask git, not ls
    git -C <src> log --reverse --format='%h %ad' --date=iso | head -1
    # has the "hazard" already coexisted with prior successful syncs?
    for S in $(git log --format=%H -6 origin/nv-coworkers); do \
      echo -n "$S gitlink="; git ls-tree -r $S knowledge_base/auto-memory | awk '$1=="160000"' | wc -l; done
  A hazard you removed is a hazard you never observed. If those checks show it never fired,
  it is not new and does not go in the body.

STEP 7 — open PR via REST (NOT gh pr create):
  PR_JSON=$(gh api -X POST repos/slang-coworkers/nanoclaw/pulls -f title="knowledge_base sync $(date -u +%Y-%m-%d)" -f head="$BR" -f base="nv-coworkers" -f body="Automated nightly knowledge_base snapshot of coworker memory + learnings. PII scrub applied (emails redacted; @handles and internal URLs intentionally retained per admin).")
  PR_NUM=$(echo "$PR_JSON" | python3 -c "import sys,json;print(json.load(sys.stdin)['number'])")

STEP 8 — merge via REST (NOT gh pr merge), retry up to 6x with ~8s sleeps while mergeability computes:
  gh api -X PUT repos/slang-coworkers/nanoclaw/pulls/$PR_NUM/merge -f merge_method=squash
  Success when response contains "\"merged\":true". Then delete the branch:
  gh api -X DELETE repos/slang-coworkers/nanoclaw/git/refs/heads/$BR

STEP 9 — report ONE line to the dashboard (orchestrator-dashboard): merged PR URL (https://github.com/slang-coworkers/nanoclaw/pull/$PR_NUM) + file-change count. If any step failed, report the failing step + error in one message and stop (do not loop).

Baseline ~520+ files under knowledge_base/. The scrub touches only the published copy; live memory under /workspace is never modified.
```

## `task-1781573293422-oivg33`

- schedule: `0 12 * * 1`
- agent group: `ag-1776713258088-r8pp2t`

Prompt:

```
Bi-weekly Slang Maintainer rotation ask. This fires every Monday 12:00 UTC (= 5am Pacific during PDT), but you must only ACT on rotation Mondays.

STEP 1 — Determine today. Run `date -u +%Y-%m-%d` to get today's date (call it TODAY).

STEP 2 — Bi-weekly guard. The anchor is 2026-06-22. Compute whole days between 2026-06-22 and TODAY. If `(days) mod 14 != 0`, this is an OFF week → do STEP 3 (reconcile) only, then stop without posting. If `mod 14 == 0`, this is a rotation Monday → do all steps.

STEP 3 — Reconcile prior answer. Read recent messages from #slang-committers (channel_id `1352357976878481468`) with `mcp__slang-mcp__discord_read_messages`. Look for a human reply to the previous "who is the next maintainer" question. If someone named the next maintainer, update `/home/node/.claude/projects/-workspace-agent/memory/project_maintainer_rotation.md` — fill the maintainer name into the matching term row and add new rows as needed. This file is the source of truth other prompts rely on, so keep it accurate.

STEP 4 — Post the question (rotation Mondays only). The new maintainer named today starts TOMORROW (Tuesday) and serves 2 weeks. Post to #slang-committers (channel_id `1352357976878481468`) via `mcp__slang-mcp__discord_send_message`, something like: "Slang Maintainer rotation: who will be the next Slang Maintainer? The term starts tomorrow (Tuesday) and runs 2 weeks. (Automated reminder.)" Append the bot disclaimer subscript per CLAUDE.md.

  IF the post is rejected (channel not in the allowlist / read-only / any error): DO NOT retry-spam. Instead notify dashboard-admin via `mcp__nanoclaw__send_message(to="slang-maintainer-dashboard")` with the question text so a human can post it, and note the failure.

STEP 5 — Record the pending term. In the rotation memory file, add/update the row for the term starting tomorrow with maintainer = "pending (asked TODAY)".

STEP 6 — Brief status to dashboard-admin (inline markdown): whether you posted, what you reconciled, and the current known maintainer.
```

## `task-1782828347850-4m9u23`

- schedule: `0 6 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Pre-task gate:

```bash
if find /workspace/shared/learnings -name '*.md' -newer /workspace/shared/wiki/index.md -print -quit 2>/dev/null | grep -q .; then echo '{"wakeAgent": true}'; else echo '{"wakeAgent": false}'; fi
```

Prompt:

```
Daily incremental learnings-wiki synth, then sync the result to the public knowledge_base and land it on nv-coworkers.

OBJECTIVE: a BOUNDED encyclopedia, not full coverage. Chasing "every atom cited" alone forces append-only growth — pages only ever get longer, and the largest pages are the most-read ones, so their truncated tails hurt most where it matters most. Retiring an atom is a SUCCESS, not a regression.

PART A — synth (follow the /learnings-wiki skill's INCREMENTAL path):
1. cd /workspace/shared && python3 .learnings_wiki.py build
2. python3 .learnings_wiki.py finalize — note UNCOVERED, OVERSIZE and NO-TLDR. (finalize prints only the first 40 uncovered; compute the true set with: for each wiki/learnings/*.md, it is uncovered unless some wiki/concepts/*.md links to it via [[wiki/learnings/<file>.md]] or [text](wiki/learnings/<file>.md).)
3. Fold each uncovered learning into the most relevant existing wiki/concepts/*.md page — RECONCILE, don't append. Add or extend a synthesis paragraph with an inline [[wiki/learnings/<file>.md]] link and add a "- [[…]] — <desc>" row to that page's "**Source learnings (N):**" footer. Do NOT hand-maintain N — _normalize_concept_footers() recomputes it and drops duplicate rows.
   - SUPERSESSION: when a new learning replaces an older one, rewrite the paragraph to state the current truth, add `superseded_by: <new-stem>` to the OLD atom's wiki/learnings/<old>.md frontmatter, and REMOVE the old row from the footer. finalize excludes superseded atoms from coverage, so this is how the wiki stays bounded.
   - Near-duplicates collapse into ONE paragraph citing both.
   - PAGE CAP 40 KB. Create a new concept page if a learning opens a genuinely new topic, OR if the target page is at the cap — then SPLIT by subtopic (<group>-<subtopic>-2.md). Never let a page exceed 40 KB: past ~56 KB the Read tool silently truncates it and the tail never reaches the reader. Growth belongs in page COUNT, never page SIZE.
   - Every concept page must open with a "## TL;DR" of <=40 lines (durable rules, no citations). Backfill it on any page you touch.
4. SPLIT THE LARGEST OVER-CAP PAGES FIRST — independent of where this run's learnings landed.
   Run: ls -S wiki/concepts/*.md | head -5. Split the TWO LARGEST that exceed 40 KB even if
   today's atoms did not touch them. Splitting only the page you folded into leaves the biggest
   and most-read pages over cap indefinitely.

5. BOUNDED WORK PER RUN — do not attempt the whole backlog in one night. Cap this run at ~40 uncovered folded, at most 2 page splits, and at most 5 TL;DR backfills. Report what remains.
6. Re-run python3 .learnings_wiki.py finalize — confirm 0 dangling, uncovered decreasing, and OVERSIZE / NO-TLDR counts not increasing.

PART B — git sync to knowledge_base + land on nv-coworkers (repo: /workspace/agent/nanoclaw-kb, fork slang-coworkers/nanoclaw).
NOTE: do NOT use the naive `cp -r src/ dest/` — when the dest dir already exists it nests into dest/<name>/<name>. And do NOT `git push origin nv-coworkers` directly: the day's base sync is usually already merge-committed, so a direct push is a non-fast-forward (and force-push to a shared branch is forbidden). Use branch → PR → REST-merge (the bot 403s on GraphQL `gh pr create`/`gh pr merge`).
5. cd /workspace/agent/nanoclaw-kb && git fetch origin nv-coworkers && git checkout -B kb-wiki-fold-$(date -u +%Y%m%d) origin/nv-coworkers
6. Replace-copy (avoids nesting):
   rm -rf knowledge_base/wiki && cp -r /workspace/shared/wiki knowledge_base/wiki
   rm -rf knowledge_base/sources && cp -r /workspace/shared/sources knowledge_base/sources
   rm -rf knowledge_base/shared/learnings && cp -r /workspace/shared/learnings knowledge_base/shared/learnings
6b. Link fixup — the generator writes `](wiki/…)` links that only resolve on GitHub for files AT the wiki root. GitHub resolves relative links per the linking FILE's directory, so this must be DEPTH-AWARE (a plain `wiki/→` strip breaks the 800+ subdir pages). Run BOTH:
   # subdir pages (wiki/*/*.md → learnings/, topics/, concepts/): prefix with ../
   find knowledge_base/wiki -mindepth 2 -name "*.md" -exec sed -i "s|](wiki/concepts/|](../concepts/|g; s|](wiki/topics/|](../topics/|g; s|](wiki/learnings/|](../learnings/|g; s|](wiki/index.md)|](../index.md)|g" {} +
   # root pages (wiki/*.md → index.md, glossary.md): strip wiki/
   find knowledge_base/wiki -maxdepth 1 -name "*.md" -exec sed -i "s|](wiki/concepts/|](concepts/|g; s|](wiki/topics/|](topics/|g; s|](wiki/learnings/|](learnings/|g; s|](wiki/index.md)|](index.md)|g" {} +
   # verify none remain: grep -r "](wiki/" knowledge_base/wiki --include=*.md  → should be empty
   (KNOWN GAP: the 42 concept pages use [[wiki/…]] wiki-link syntax, which GitHub renders as literal text, not links. Fixing that is a separate decision — either convert to markdown links in the generator or leave as-is.)
7. PII scrub (email-redaction invariant — every prior sync did this):
   python3 scripts/scrub_kb_pii.py --apply knowledge_base
   python3 scripts/scrub_kb_pii.py --audit knowledge_base  # confirm 0 "email redact" matches (github_handle/internal_url are review-only, kept)
8. git add knowledge_base/ && git diff --cached --stat | tail -5
9. If there is a staged diff: git commit -m "knowledge_base sync $(date -u +%Y-%m-%d): wiki-synth fold + link fixup (PII scrub: emails redacted)" && git push -u origin HEAD
10. Open + self-merge the PR via REST (NOT gh pr create/merge):
   PR=$(gh api repos/slang-coworkers/nanoclaw/pulls -X POST -f title="knowledge_base sync $(date -u +%Y-%m-%d): wiki-synth fold" -f head="$(git rev-parse --abbrev-ref HEAD)" -f base="nv-coworkers" -f body="Daily /learnings-wiki synth + fold + link fixup. Coverage <N>/<N>." --jq .number)
   gh api repos/slang-coworkers/nanoclaw/pulls/$PR/merge -X PUT -f merge_method="merge" --jq '{merged,sha}'
11. Report: learnings count, concepts count, uncovered folded, PR number, merge sha/status.
```

## `task-1783328238990-qikxwn`

- schedule: `0 */6 * * *`
- agent group: `ag-1776713211742-1w6l4e`

Prompt:

```
Scheduler watchdog — re-arm any genuinely stalled recurring tasks.

Call `ncl tasks list`. "NEXT RUN" is each task's next-fire time.

⛔ SELF-EXCLUSION: this watchdog is series `task-1783328238990-qikxwn`. Never re-arm it. (The pre-08-07 prompt excluded "any id containing scheduler-watchdog" — that string does not appear in the real id, so the guard was dead for 126 runs.) Match the id above literally.

⛔ NEVER RE-ARM A ONE-SHOT (`recurrence: null`). No exceptions, no session check needed to skip. Report it and stop.

Why this rule is absolute, measured 2026-08-07 on slang-fixer's `t-aa7516`:
  - A spent one-shot stays `status: pending` with a PAST `process_after` and `completed_runs: 0` FOREVER — `runs` increments on COMPLETION, so a fired one-shot is BYTE-IDENTICAL to an orphaned one in `ncl tasks list`.
  - `cancel` retires the TRIGGER but NOT the PAYLOAD. The prompt survives cancellation and is IMMUTABLE (`ncl tasks update --prompt` on a spent row → "no live task matched"; the same verb+flag succeeds on a live row, so the refusal is state-based, not a flag error). `t-aa7516`'s prompt still reads "post ONE brief nudge" at a human maintainer on PR #12186.
  - ⇒ A cancelled one-shot is a disarmed trigger wrapped around a live instruction, and re-arming supplies the missing trigger. THIS WATCHDOG IS THE ONLY REAL GUARD — nothing on the spent row can stop it.
  - Non-idempotent side effects include: posting a comment, nudging a human, opening a PR, sending a message. Assume every one-shot has one.

FOR RECURRING SERIES ONLY (`recurrence` non-null), a past NEXT RUN is NECESSARY BUT NOT SUFFICIENT. `process_after` is when an occurrence became ELIGIBLE, not proof it is waiting; a fired-but-in-flight or blocked run keeps its past due time and `runs: 0`. The task row cannot answer "did I already fire?" — open the session (`session_id` comes from `ncl tasks get --id <series>`):
  ncl sessions get <session_id>        # container_status running + last_active AFTER process_after ⇒ FIRED, do not touch
  ncl sessions messages <session_id>   # any outbound row after process_after ⇒ FIRED, do not touch
Re-arm only when BOTH say nothing happened after the due time. In practice a recurring series that fired keeps a FUTURE next-run and a recent last-run, so it should rarely trip this at all — if a recurring row looks overdue, suspect your reading before the scheduler.

DO NOT change any task's recurrence or prompt — only processAfter (now + 2 minutes, naive local ISO).

Note on attribution: `ncl tasks list` has no owner column and its `--group`/`--agent-group-id` filters are inert. Every row you see is almost certainly your own group's, but confirm with `ncl tasks get --id <series>` (returns `agent_group_id`) before acting on anything unfamiliar. Never re-arm a row that resolves to another group.

If nothing qualifies, do nothing and send NO message (bare prose still delivers a message; only <internal></internal> is silent). If you re-armed one or more, send ONE message to orchestrator-dashboard listing each: id, old next-run, new processAfter, and the session evidence proving it had NOT fired.

Why this exists: a host event can orphan overdue pending occurrences (recurrence advances only on completion), freezing daily tasks until a human notices. Memory: [[feedback_a_spent_one_shot_stays_pending_and_invites_a_rerun]], [[feedback_watchdog_ncl_tasks_list_empty_not_a_freeze]].
```

## `task-1783463591538-d3s5gm`

- schedule: `*/5 * * * *`
- agent group: `ag-1777389337838-f54d9l`

Pre-task gate:

```bash
#!/bin/bash
# Heartbeat pre-check: Discord new messages + CI health + summon requests
#
# Credentials come from the OneCLI gateway: we deliberately KEEP the proxy env
# and send NO Authorization header — the gateway injects the vault "Discord Bot"
# secret for discord.com (and the App token for api.github.com). The previous
# version unset the proxy and read a raw token from memory/.discord-token, which
# silently rotted on 2026-07-11 and 401'd for weeks.

MEM=/workspace/agent/memory
SUMMON_FILE="$MEM/feedback/summon_requests.jsonl"
SUMMON_HANDLED="$MEM/feedback/summon_handled.jsonl"
LAST_TS_FILE="$MEM/.heartbeat-last-ts"
GUILD_ID=1303735196696445038

LAST_TS=$(cat "$LAST_TS_FILE" 2>/dev/null || echo "1970-01-01T00:00:00Z")
UA="User-Agent: curl/8.0"

wake=false

# 1. Pending summon requests
#
# TWO counts, deliberately. The Discord wiring (session_mode=per-thread) already
# wakes a per-thread session on every summon AND every follow-up in the forums it
# reaches -- so for those threads this heartbeat is a DUPLICATE answerer, not a
# safety net. On 2026-08-08 both paths answered thread 1535675765155303506 and the
# user saw two overlapping answers to one question; the race window is
# research->send (~22 min: summon 15:47:13, first sends 16:09), not send->ledger,
# which is why appending to summon_handled.jsonl cannot fix it.
#
# So only STALE summons vote to wake: if a summon is older than the grace period
# and still unhandled, the adapter demonstrably did not take it (dead wake, a forum
# the wiring doesn't cover, or a crash mid-research) and we are the fallback.
# Fresh ones are reported but never wake -- they belong to the adapter.
#
# Grace = 20 min: comfortably longer than an observed adapter answer (~22 min
# summon->send was research time, and its FIRST message landed at 16:09 = 22 min;
# a wake that is going to happen happens within ~1 min of the summon row).
# NOTE: not narrowed to "only forums the wiring misses" -- I have positive evidence
# path A covers parent 1313936640661524601 (76/89 requests, and every request since
# June) but NO evidence either way for 1494023079666647200 / 1337094433816051813,
# and no recent traffic there to test with. An age gate is safe for all three
# without needing that unknown resolved: it cannot race the adapter, and it still
# catches a forum the adapter silently ignores (just 20 min later).
SUMMON_GRACE_MIN="${SUMMON_GRACE_MIN:-20}"
pending=0        # all unhandled (reporting only)
pending_stale=0  # unhandled AND older than the grace period (wake-worthy)
now_epoch=$(date -u +%s)
if [ -f "$SUMMON_FILE" ]; then
  handled_ids=""
  [ -f "$SUMMON_HANDLED" ] && handled_ids=$(jq -r '.thread_id // empty' "$SUMMON_HANDLED" 2>/dev/null | sort -u)
  while IFS= read -r line; do
    [ -z "$line" ] && continue
    tid=$(echo "$line" | jq -r '.thread_id // empty' 2>/dev/null)
    [ -z "$tid" ] && continue
    echo "$handled_ids" | grep -qx "$tid" && continue
    pending=$((pending + 1))
    # Age from the request row's own timestamp. Unparseable/missing => treat as
    # stale, so a schema change fails toward answering rather than toward silence.
    sts=$(printf '%s' "$line" | jq -r '.timestamp // empty' 2>/dev/null)
    s_epoch=""
    [ -n "$sts" ] && s_epoch=$(date -u -d "$sts" +%s 2>/dev/null || echo "")
    if [ -z "$s_epoch" ]; then
      pending_stale=$((pending_stale + 1))
    elif [ $(( (now_epoch - s_epoch) / 60 )) -ge "$SUMMON_GRACE_MIN" ]; then
      pending_stale=$((pending_stale + 1))
    fi
  done < "$SUMMON_FILE"
fi
[ "$pending_stale" -gt 0 ] && wake=true

# 2. Discord new messages (auth injected by OneCLI — no Authorization header)
new_count=0
discord_auth_ok=1
# Retry the auth probe before declaring an auth break. A SINGLE un-retried probe
# turned a transient proxy 503 into discord_auth_ok=0, which skipped the whole
# Discord scan and reported new_discord_messages=0 -- a NULL reading that reads
# as "quiet". Combined with the quiet-path watermark stamp that marked those
# unread messages as read, it cost a user 2h10m on 2026-08-07. Re-probed 6/6 OK
# minutes later, so the credential was never bad.
discord_auth_ok=0
for _try in 1 2 3; do
  if curl -sf --max-time 8 -H "$UA" "https://discord.com/api/v10/users/@me" >/dev/null 2>&1; then
    discord_auth_ok=1; break
  fi
  [ "$_try" -lt 3 ] && sleep 2
done
if [ "$discord_auth_ok" = "1" ]; then
  for ch in 1305995870046650368 1303735244108595330; do
    n=$(curl -sf --max-time 8 -H "$UA" "https://discord.com/api/v10/channels/$ch/messages?limit=5" \
      | jq --arg ts "$LAST_TS" '[.[] | select(.timestamp > $ts and (.author.bot | not))] | length' 2>/dev/null) || n=0
    new_count=$((new_count + n))
  done
  threads=$(curl -sf --max-time 8 -H "$UA" "https://discord.com/api/v10/guilds/$GUILD_ID/threads/active" \
    | jq -r '.threads[] | select(.parent_id == "1313936640661524601" or .parent_id == "1494023079666647200" or .parent_id == "1337094433816051813") | .id' 2>/dev/null) || threads=""
  for tid in $threads; do
    n=$(curl -sf --max-time 8 -H "$UA" "https://discord.com/api/v10/channels/$tid/messages?limit=3" \
      | jq --arg ts "$LAST_TS" '[.[] | select(.timestamp > $ts and (.author.bot | not))] | length' 2>/dev/null) || n=0
    new_count=$((new_count + n))
    [ "$new_count" -gt 0 ] && break
  done
fi
[ "$new_count" -gt 0 ] && wake=true
# Surface an auth break instead of silently reporting "no new messages".
[ "$discord_auth_ok" = "0" ] && wake=true


# 3. CI health
#
# Read the newest snapshot as the last COMPLETE line of a byte-range tail.
#
# A fixed byte range ignores line boundaries: the slice starts mid-line, so if the
# newest record is longer than the range, that record is truncated at its head and
# `grep '^{'` matches nothing -- the pipeline emits empty, which reads identically
# to "CI is quiet". Measured 2026-08-06: 291/5463 lines were >=2048B, and the
# failure is biased 34x TOWARD the wake condition (16% oversized when
# jobs_queued>30 vs 0.5% when quiet), because record width grows with the number
# of runner/queue/quota groups -- i.e. with the activity this alarm watches. So the
# alarm was most likely to be suppressed exactly when it should fire.
#
# Widening the constant alone only defers the bug, so correctness here must not
# depend on it: take the last line that both starts with `{` AND parses as JSON.
# `jq -e .` is the boundary test -- a head-truncated fragment fails it, a complete
# record passes. The range is now only a throughput knob, not a correctness one.
CI_RANGE="${CI_RANGE:-32768}"
ci_snap_raw=$(curl -sf --max-time 8 -r "-${CI_RANGE}" -H "$UA" "https://raw.githubusercontent.com/shader-slang/slang-ci-analytics/main/health_snapshots.jsonl" 2>/dev/null)
ci_curl_rc=$?
ci=""
if [ "$ci_curl_rc" -eq 0 ] && [ -n "$ci_snap_raw" ]; then
  # Walk candidate lines newest-first; first one that parses wins.
  while IFS= read -r _line; do
    case "$_line" in
      \{*)
        if printf '%s' "$_line" | jq -e . >/dev/null 2>&1; then ci="$_line"; break; fi
        ;;
    esac
  done <<< "$(printf '%s\n' "$ci_snap_raw" | tac)"
fi

# Distinguish the failure modes instead of collapsing them to silence: a truncated
# slice is a CONFIG bug (raise CI_RANGE) and must not look like a fetch failure.
if [ -z "$ci" ]; then
  if [ "$ci_curl_rc" -ne 0 ]; then
    ci='{"error":"fetch_failed"}'
  elif [ -z "$ci_snap_raw" ]; then
    ci='{"error":"empty_response"}'
  else
    ci='{"error":"no_complete_line_in_range","ci_range":'"$CI_RANGE"'}'
  fi
  # An unreadable snapshot is itself worth waking for -- previously this path went
  # silent and was indistinguishable from a healthy quiet CI.
  wake=true
fi

# Validate before use: `jq --argjson` aborts the whole script on invalid input,
# which previously turned an empty read into a run that emitted NO JSON at all
# (the `|| ci=...` guard could not fire because `tail` succeeds on empty input).
printf '%s' "$ci" | jq -e . >/dev/null 2>&1 || { ci='{"error":"unparseable_snapshot"}'; wake=true; }

queued=$(printf '%s' "$ci" | jq -r '.jobs_queued // 0' 2>/dev/null)
case "$queued" in ''|*[!0-9-]*) queued=0 ;; esac

# Gate the depth vote on FRAME AGE. A stale snapshot can carry a large jobs_queued
# from a window that has since drained (observed 2026-08-06: 3 byte-identical frames
# claiming queued=70 while the live API said 2). Voting off it wakes the agent to
# investigate a queue that no longer exists; worse, the writer runs on the same
# contended pool it measures, so frames go stale exactly when CI is busy.
ci_ts=$(printf '%s' "$ci" | jq -r '.timestamp // empty' 2>/dev/null)
ci_age_min=-1
if [ -n "$ci_ts" ]; then
  ci_epoch=$(date -u -d "$ci_ts" +%s 2>/dev/null || echo "")
  [ -n "$ci_epoch" ] && ci_age_min=$(( ( $(date -u +%s) - ci_epoch ) / 60 ))
fi
# Depth vote only when the frame is fresh enough to describe NOW (<=45 min; writer
# cadence is ~32 min, so 45 admits one missed beat without admitting a stale window).
if [ "$ci_age_min" -ge 0 ] && [ "$ci_age_min" -le 45 ]; then
  [ "$queued" -gt 30 ] 2>/dev/null && wake=true
fi

# Advance the Discord read-watermark ONLY on the quiet path -- and only AFTER every
# wake vote has been cast. This used to sit above the CI-health block, so a CI-only
# wake (queued>30 / unparseable snapshot) stamped the watermark while wake was still
# false, marking unread Discord messages as read. Latent but real; moved 2026-08-07.
# On a wake the agent stamps it itself, after the report is written, so a wake that
# dies before reporting is retried rather than silently dropped.
if [ "$wake" = "false" ]; then
  date -u +%Y-%m-%dT%H:%M:%SZ > "$LAST_TS_FILE" 2>/dev/null || true
fi

# 4. Workflow failures
wf="{}"
for repo in shader-slang/slang shader-slang/slangpy shader-slang/slang-rhi; do
  short=$(echo "$repo" | cut -d/ -f2)
  runs=$(curl -sf --max-time 8 -H "$UA" "https://api.github.com/repos/$repo/actions/runs?status=failure&per_page=3" \
    | jq -c '{total: .total_count, runs: [.workflow_runs[:3][] | {name,conclusion,created_at,html_url}]}' 2>/dev/null) || runs='{"error":"fetch_failed"}'
  # Same unreachable-guard shape as the CI block: on empty curl output `jq` exits 0
  # and `runs` is set to the empty string, so the `||` guard never fires and the
  # accumulator below dies on invalid --argjson. Validate before use.
  printf '%s' "$runs" | jq -e . >/dev/null 2>&1 || runs='{"error":"fetch_failed"}'
  wf=$(printf '%s' "$wf" | jq --argjson r "$runs" --arg k "$short" '. + {($k): $r}') || wf='{"error":"accumulator_failed"}'
done

# Output
# Final assembly. Every --argjson input above is now validated, but keep a
# last-resort guard so a malformed value degrades to a diagnosable payload
# instead of emitting nothing (a run that prints nothing is a silent dropped
# heartbeat, which is the failure mode this whole patch exists to remove).
printf '%s' "$wf" | jq -e . >/dev/null 2>&1 || wf='{"error":"wf_unparseable"}'
case "$new_count" in ''|*[!0-9]*) new_count=0 ;; esac
case "$pending" in ''|*[!0-9]*) pending=0 ;; esac
case "$pending_stale" in ''|*[!0-9]*) pending_stale=0 ;; esac

jq -nc \
  --argjson ci_age_min "$ci_age_min" \
  --argjson wake $wake \
  --argjson ci "$ci" \
  --argjson wf "$wf" \
  --argjson new "$new_count" \
  --argjson pending "$pending" \
  --argjson pending_stale "$pending_stale" \
  --argjson grace "$SUMMON_GRACE_MIN" \
  --argjson dauth "$discord_auth_ok" \
  '{wakeAgent: $wake, data: {ci_health: $ci, ci_frame_age_min: $ci_age_min, workflow_failures: $wf, new_discord_messages: $new, pending_summons: $pending, pending_summons_stale: $pending_stale, summon_grace_min: $grace, discord_auth_ok: ($dauth == 1)}}' \
  || printf '{"wakeAgent":true,"data":{"ci_health":{"error":"output_assembly_failed"}}}\n'
```

Prompt:

```
Heartbeat wake. A pre-check script has already run and its JSON output (wakeAgent + data{ci_health, workflow_failures, new_discord_messages, pending_summons}) is available to you. Follow the "Scheduled Heartbeat" workflow in your CLAUDE.md:

1. Analyze the CI data from the script output against the documented thresholds (jobs_queued, runs_queued, runner saturation, workflow failures).
2. Check Discord: read recent messages from the primary source channels and any active support-forum threads; identify unanswered user questions and bug reports (save drafts to memory/pending-questions.md — do NOT post to source channels).
3. Summons: answer ONLY the summons counted in `data.pending_summons_stale` (older than `data.summon_grace_min`). `pending_summons` alone is reporting-only — a FRESH summon belongs to the Discord per-thread wiring, which wakes its own session for the summon and every follow-up. Answering a fresh one duplicates that session's work: on 2026-08-08 both paths answered thread 1535675765155303506 and the user saw two overlapping answers to one question. If `pending_summons_stale` is 0, do not answer any summon, even if `pending_summons` is non-zero — just report the count.
   For each stale summon, CLAIM IT FIRST (before any research), because the race window is research→send (~22 min), not send→ledger:
     `mkdir /workspace/agent/memory/feedback/claims/<thread_id>` — this fails atomically if another wake already holds it, so on failure SKIP that thread. Do NOT use a jsonl append as a lock: two writers both append and both proceed.
     A claim older than 30 min is expired: `rm -rf` it and re-claim, so a wake that dies mid-research cannot silently sink the summon forever (an abandoned claim must not be indistinguishable from a finished one).
     On confirmed send success, append summon_handled.jsonl (real message_id) and then `rm -rf` your claim dir.
   Then: read thread → research → reply with add_feedback_buttons:true → ledger → release claim.
4. Before writing, read memory/heartbeat-log.md to compare with prior runs and note trends.
5. Produce a combined report. Save it to memory/latest-report.md (overwrite) AND append to memory/heartbeat-log.md under a "## YYYY-MM-DD HH:MM UTC" header. If the log exceeds ~200 lines, summarize older entries into memory/heartbeat-archive-YYYY-MM.md and trim.
6. If everything is healthy, Discord quiet, and no trends emerging, say so briefly wrapped in <internal> tags.
7. LAST STEP, only after memory/heartbeat-log.md has actually been appended: advance the Discord read-watermark with
   `date -u +%Y-%m-%dT%H:%M:%SZ > /workspace/agent/memory/.heartbeat-last-ts`
   The precheck no longer stamps this on a wake (only on the quiet path), so the messages that woke you stay "unread" until you do this. That is deliberate: a wake that dies before writing a report will be retried instead of silently dropped. But it also means SKIPPING this step makes you wake on the same messages every 5 minutes — so do not skip it, and do not do it early.

Run `date -u` before claiming any current time.
```
