<!-- provenance: added 2026-09-07, see reports/hermes-drift-2026-09-07.md -->
> ## Provenance & citation convention
>
> - **Hermes tree we build against (PINNED):** tag `v2026.8.31` @ `29112bef` — local checkout `/Users/haaggarwal/brev/hermes-agent-release` (`pyproject version = "0.21.0"`). Policy decision: **stay pinned, move on the next release day.**
> - **Drift analysis:** `/Users/haaggarwal/brev/nanoclaw/reports/hermes-drift-2026-09-07.md` — pinned tag vs upstream `main@08b140d14`, dated **2026-09-07**. Verdicts across the 61 rows: **37 STILL_VALID / 24 REWORD / 0 OBSOLETE / 0 BLOCKED / 0 UNKNOWN**. Upstream main is ~95% refactor (the Sept-2026 decomposition, PR #102117): the plugin-author contract (`plugin.yaml` schema, the 37 `VALID_HOOKS`, all 43 `PluginContext` methods, `register(ctx)`, capabilities, plugin-data dirs) is byte-for-byte identical; what moved is where the code lives, i.e. the line numbers.
> - **Dual-cite convention:** every citation carries **both** trees — `tag: <path>:<line>` (v2026.8.31) and `main: <path>:<line>` (08b140d14). Where a symbol exists in only one tree, the entry says which tree and why. No line number here is inferred: each was resolved by opening the file in the named tree (compat-manifest / symbol / literal-text resolution, recorded per cite).
> - **Compat cliff — 2026-09-14:** from that date the loader **refuses to load** any external plugin that imports a pre-decomposition module path (sets `loaded.error`, returns before `register()`): main `hermes_cli/plugin_compat.py:31` (`COMPAT_REMOVAL_DATE`), main `hermes_cli/plugins_loader.py:283-289`; escape hatch `plugins.allow_deprecated_imports` defaults False (main `hermes_cli/config_defaults.py:1565`). **Rule: all plugin code and all tests target MAIN module paths from day one.** The shim is a one-week bridge, not an API, and private `_`-prefixed names and test monkeypatch seams are explicitly never restored (main `COMPAT_MANIFEST.md:33-36`). Gate a plugin with `hermes plugins compat <dir> --json` (main `hermes_cli/plugins_cmd.py:2000`).
> - **Scope of the 2026-09-07 edit:** for the 37 STILL_VALID rows nothing but citations changed — row text, status, feasibility and design notes are untouched, existing `tag:` cites were re-verified against the pinned tree, and the `main:` side was added beside them. The 24 REWORD rows carry their own drift-report rationale.
>
> - **Where the dual cites live:** the `design_note` column below is a truncated rendering of the full design text; the authoritative, per-row **dual (`tag:` / `main:`) citation set** — 455 cites across the 37 STILL_VALID rows, each with the anchoring symbol — is in `gap-matrix-evidence.md`, in the `cites (dual …)` block under each row. Cites appearing inline in this table are TAG-relative unless labelled.
> - **Row id shape:** `<FAMILY>-F<NN>` (RT / ISO / A2A / GOV / CRED / COST / SCHED / LOOP / MEM / OBS / CH / SELF / OPS), 61 rows.
>
> Re-baselined 2026-09-08 under the fleet topology (container/spines/hermes/context/topology.md): dispositions ADOPT/CONFIGURE/BUILD/MERGE/DEFER judged against outcomes O1-O8; design notes rewritten in Hermes's shape.

| id | name | status | feas | esc | disposition | outcomes | design_note |
|---|---|---|---|---|---|---|---|
| RT-F01 | Entity model with user-level privilege (users -> messaging_groups -> wirings -> | PARTIAL | PLUGIN |  | CONFIGURE | O2,O3,O7 | The fleet's entity model is Hermes's own. Coworkers are profiles under `$HERMES_HOME/profiles/<name>/` served by ONE gateway with `gateway.multiplex_profiles: true` and `multiplex_profile_allowlist` naming the roster; messaging groups are Bot Chats, rooms and webhook routes. What the compose plugin OWNS is: profile dirs, per-profile config.yaml, the default profile's multiplex keys, per-platform allowlists, and every webhook route with its own secret and `profile:` binding rendered into the DEFAULT profile's config — no coworker profile ever enables webhook, api_server or any other port-binding platform. Room creation and membership are NOT rendered: they are desktop-app state (`ui_meta['hermes-bots']` in profile.yaml, which distributions do not carry) over gateway-owned room rows in state.db, with no CLI verb and only cross-gateway grant routes on the API — so rooms are a documented one-time operator bring-up step in the desktop app (or via `hermes onboard coworker`'s `groups.create` calls), re-checked by a runbook item. Human privilege is the pairing store plus the per-platform allowlist resolved by `_is_user_authorized`, with `gateway/slash_access.py` giving the admin/user command tier; agent-side privilege is the elevated-orchestrator predicate (GOV-F26 on LOOP-F37), not a role table. No plugin owns identity and nothing authorizes a sender from a hook. The doc page maps each NanoClaw table onto its Hermes object and records two thin spots: operator roles scope per profile, not per (human, project); and there is no CLI or render surface for room administration. |
| RT-F02 | Router fan-out with engage modes (pattern|mention|mention-sticky), sender_scope, | PARTIAL | PLUGIN_PLUS_HOOK |  | CONFIGURE | O3,O7 | Engagement is per-platform config on each profile, not a routing plugin: `require_mention` and `mention_patterns` give mention and pattern engagement, `_register_mentioned_thread` gives mention-sticky, `free_response_channels` gives always-on channels, and `allowed_channels` bounds sender/channel scope; one chat resolves to one profile through `profile_routes` (`match_profile_route`) or a webhook route's `profile:` binding (rendered on the default profile, RT-F01). Where several coworkers must deliberate on the same message, the surface is a Bot-Mode room — @mentions scope the round, un-mentioned members pass, and the hard caps bound it — not gateway fan-out; room creation and membership are provisioned through the gateway's `groups.*` JSON-RPC by `hermes onboard coworker` and the desktop app, and this row keeps that as a criterion in A2A-F18 rather than rendering it. Adapters keep their own gating (never blanket-forward), no hook becomes the router, and the compose plugin renders these engage keys per profile with the doc mapping each NanoClaw engage mode to its key. |
| RT-F03 | Session modes shared|per-thread|agent-shared under creation lock + canonical ^gh | PARTIAL | PLUGIN_PLUS_HOOK |  | CONFIGURE | O1,O3 | Session modes are per-platform config on each profile, not per chat: `group_sessions_per_user: false` gives one shared session for a group chat, per-thread continuity comes free whenever the adapter supplies a `thread_id` (`build_session_key`, where `thread_sessions_per_user` only controls whether the participant id is appended INSIDE a thread), and the canonical Bot Chat is the agent-shared 'one conversation per coworker' mode. The accepted difference from NanoClaw is granularity: one mode per (profile, platform), so two chats of the same platform served by one coworker share a mode; if a per-chat mode is ever needed the answer is a second profile, not a router plugin. `build_session_key` stays the only key authority — no `event.source` rewriting from `pre_gateway_dispatch` — and `_SessionFlight` is the creation lock the row asked for. The `^gh-issue-N` / `^gh-pr-N` chain becomes a kanban card per artifact: the GitHub route (CH-F52) creates or joins `--idempotency-key gh-pr-<n>`, so every later event for that PR lands on the one card with its one claimed worker (GOV-F25), while each webhook delivery keeps its disposable one-shot session. The doc records that per-artifact continuity lives on the board, not in a rewritten session key. |
| RT-F04 | Adapter-declared ChannelDefaults (dm/group: engageMode, engagePattern, threads, | PARTIAL | PLUGIN |  | DEFER | none | Deferred: no O1–O8 criterion depends on adapters declaring wiring-time defaults. With a handful of platforms the compose plugin writes the same few keys (`require_mention`, reply/thread mode, `unauthorized_dm_behavior`) straight into each profile's config, and Hermes's `register_platform` refuses extra declaration fields, so a registry buys uniformity we do not need at the price of a core ask. Revive only if the fleet grows past a handful of adapters or third parties start creating their own wirings; the criterion is recorded for that case. |
| RT-F05 | Admission flows: unknown_sender_policy strict|request_approval|public with Appro | PARTIAL | PLUGIN |  | ADOPT | O3,O7 | Admission is Hermes pairing, adopted as-is. `unauthorized_dm_behavior: ignore` plus an allowlist is strict; `pair` is request_approval — the unknown sender receives a code, the operator approves with `hermes pairing approve` or the dashboard button, and the pairing grant is unioned into the allowlist for that profile; allow-all is the public setting and stays off for the fleet. The doc page maps NanoClaw's three policies onto those keys, shows where approvals surface in the panel, and records the two accepted differences (the triggering message is not replayed after approval; unauthorized group messages are silently ignored). |
| RT-F06 | Dropped-messages ledger (`unregistered_senders`: reason no_agent_wired|no_agent_ | MISSING | PLUGIN |  | DEFER | none | Deferred: nothing in O1–O8 reads a per-sender drop ledger. Unauthorized and skipped inbound is logged by the gateway and pending pairing requests are enumerable from `hermes pairing` and the dashboard, which answers 'who tried and was refused' for a fleet whose channels are operator-controlled. If a public channel is ever wired, revive this as counters inside the `nv-artifact` plugin (GOV-F25, which already holds the fleet's one `pre_gateway_dispatch` inbound observer) keyed (platform, chat, user, reason) with first/last-seen, not as a subsystem of its own — the reason and the revival shape are recorded with the row. |
| RT-F07 | Cold-DM cache (user_dms) via ensureUserDm two-class resolution (direct-addressab | PARTIAL | PLUGIN |  | ADOPT | O5,O7 | Adopt Hermes's outbound addressing, and name the callable surface. Target resolution (`platform:user:<id>`, `user_name:<handle>`, Slack `conversations.open` per call), the channel-directory cache and the per-platform HomeChannel fallback are used by cron delivery, the kanban notifier, `hermes send` and by our own plugins (the COST-F30 decision, GOV-F25's fan-in) — all of which run host-side in the gateway process. An AGENT never fires a cross-platform send on its own: core deliberately does not expose `send_message` as a model tool, so a coworker reaches a human through one of three declared surfaces — the opt-in Hermes MCP server enabled per profile under GOV-F27's `mcp_servers` block, `hermes send` run inside the profile's own sandbox, or a plugin-registered tool on the orchestrator only (gated by the veto). Human decision cards do not use this path at all: approvals ride the native approval transport (GOV-F23) and the cost decision rides the approval gate (COST-F30); `send_message` is for nudges and notices. No `user_dms` table, no `ensureUserDm` resolver, no direct-addressable/resolution-required split. The doc records where escalations go (the approver's DM, else the HomeChannel, always mirrored in the panel) and that a cache would be re-added only if resolution latency ever showed up in traces. |
| RT-F08 | Cross-session context (session-echo fan-out and pull) | PARTIAL | PLUGIN |  | ADOPT | O3,O8 | Cross-coworker context is the room plus the board, not an echo. Members of a room see each other's turns through the shared room transcript while keeping their own `Group: <name>` session; durable hand-off text lives on the kanban card (summary, metadata, comments); anything older is pulled with `session_search`; and `mirror_to_session` already annotates a message delivered into another session. Nothing persists trigger-free rows, nothing fans a message across sibling sessions, and the loop guard, prune policy and `<cross-session-context>` render are dropped. The doc maps each NanoClaw echo case (same-chat siblings, delivered replies, historical pull) to room, card or `session_search`. |
| RT-F09 | Pluggable router seams (interceptors, gates, session-created hooks) | PARTIAL | PLUGIN_PLUS_HOOK |  | ADOPT | O3,O4 | Adopt the plugin seam as it stands: `pre_gateway_dispatch` for inbound observation and drops (first-claim-wins already, and the fleet's only per-webhook-delivery observation point), `pre_tool_call` for the fleet's single veto, `on_session_start` / `on_session_end` / `on_session_reset` — the VALID_HOOKS names — for session bookkeeping and the welcome path, and `register_command` / `register_cli_command` for coworker commands and fleet verbs. The doc page maps each NanoClaw router seam (interceptor, access gate, sender resolver, session-created, welcome) onto its Hermes hook, records the withdrawal of the `authorize_source` upstream ask, and carries one warning for every row that wants process- or session-lifecycle events: the colon-named `gateway:startup` / `session:start` / `session:compress` events are the separate filesystem gateway-event mechanism, not VALID_HOOKS; `register_hook` accepts an unknown name with only a warning, so a typo fails silently; the hooks dir is bound at import to the launch profile and is not distribution-owned. Plugins needing startup work use `register(ctx)` plus `ctx.spawn_task` from a first hook fire. |
| ISO-F10 | One container per session, two-DB mailbox, seq parity, heartbeat | PARTIAL | PLUGIN | Y | MERGE→LOOP-F37 | O4,O8 | MERGE→LOOP-F37. This id survives as the SANDBOX predicate of the single `nv-fleet-gates` `pre_tool_call` callback and as the criterion 'one sandbox per principal, provably'. The predicate is rendered inert (`enforce_sandbox: false`) until P4-sandbox, when every profile renders a container backend; from then on, for every file, shell, `execute_code` and `mcp_*` call it reads the resolved backend from the profile's OWN `terminal.*` config plus, on trees that ship it, the active per-turn terminal scope, blocks unless that backend is the profile's container backend (naming the profile and the resolved backend), calls `ensure_task_env()` so a file tool cannot run before the profile's podman container exists, denies stdio `mcp_*`, and re-asserts the dangerous-command floor that `_should_skip_container_guards` waives. Two limits stated in the doc rather than papered over: (a) while the pin lacks `tools/terminal_scope.py`, `TERMINAL_*` is process-global, so the predicate hard-fails a gateway-process tool call whose profile is not the launch profile and tool-bearing work runs on the board (ISO-F15); (b) MCP is an explicit, documented exception to rule 3 — the MCP client stays in the gateway process, so the stdio deny is justified as 'no un-sandboxed process spawning', NOT as 'MCP goes over the hop', and per-profile attribution for remote MCP rides the context-local secret scope carrying that profile's OneCLI token (GOV-F27, ISO-F14). The enumerated tool set also covers the HOST-side writers `skill_manage` and the memory tool, which write in the agent process and can only be constrained by this predicate's path check (MEM-F43), never by a mount. Per-profile container identity comes from the profile's `terminal.*` config, with `register_task_env_overrides` when a card needs its own workspace. No per-bot gateway, no mailbox, no runner, no scheduler. |
| ISO-F11 | Idempotent claim/ack lifecycle with host-side retry backoff | PARTIAL | PLUGIN_PLUS_HOOK | Y | ADOPT | O1,O8 | Adopt Hermes's lifecycle end to end: inbound idempotency is the webhook platform's delivery-id cache plus `hermes kanban create --idempotency-key`, ownership is `claim_task` with `heartbeat_claim` while `release_stale_claims` and `detect_crashed_workers` reclaim dead workers behind `check_respawn_guard`, restart safety is the per-session turn lease plus `resume_pending`, and outbound at-least-once is the delivery ledger with its recovered marker; API-driven runs get durable `run_idempotency` reservations. Bot-to-bot delivery retries once, only for reasons a retry can fix, with a machine-readable code. Nothing ports the two-DB claim/ack or the fixed 5s-doubling redrive; the doc maps each NanoClaw state (claimed / processing / completed / tries) to its Hermes counterpart and names the one accepted difference — at-least-once with an honest marker rather than exactly-once. The GitHub-PR index that sits on top of this (which card owns which PR) is GOV-F25, not a second lifecycle. |
| ISO-F12 | on_wake messages, race-free restart, paused kill switch | PARTIAL | PLUGIN |  | ADOPT | O6,O8 | Adopt ESTOP and the gateway's own restart path. `hermes -p <bot> pause` writes the profile-scoped sentinel that blocks new turns, cron fires and kanban spawns for that coworker while in-flight work finishes; `resume` clears it; the fleet-root sentinel stops everything. Restart is `restart.py`'s drain-then-exit-75 under the supervisor, with `resume_pending` re-arming interrupted sessions and the terminal backend recreating the sandbox on the next tool call. The on_wake message becomes the worker re-reading its kanban card after respawn, so no plugin spools inbound and no restart command is built. The doc records the one difference: a paused inbound message is answered with a notice rather than queued for replay. |
| ISO-F13 | Mount composition policy, mount allowlist, inbox containment | PARTIAL | PLUGIN |  | CONFIGURE | O4,O5 | Mount composition is rendered, not policed. The workspace root of every coworker is rendered OUTSIDE `$HERMES_HOME` (e.g. `/data/coworkers/<profile>/workspace`); the compose plugin writes each profile's `terminal.docker_volumes` as an explicit set — that workspace path rw, shared install surfaces ro, nothing else, and no path under `$HERMES_HOME`, the profile dir or their parents — PLUS `docker_mount_cwd_to_workspace: true` so a kanban worker launched with `cwd=<task workspace>` gets that card's checkout at `/workspace`, and `container_persistent` rendered explicitly per role (false where the sandbox must be a boundary between concurrent cards, true for a long-lived shared sandbox). This invariant is load-bearing: it is what makes the per-profile policy (role map, toolsets, mcp includes, plugin settings, .env, state.db) unreachable to the agent, since managed scope cannot pin per-profile values. Hermes already pins its own credential, skills and CA mounts `:ro`, stages skills symlink-free, and hardens the container. The workspace bind mount is deliberate: it makes `_docker_has_host_access` true so the dangerous-command approval chain stays ON instead of being waived for containers — which is exactly why GOV-F23 must render a per-role `command_allowlist` for kanban workers — with the ISO-F10 predicate re-asserting that floor. Attachments follow Hermes's model (host-owned dir mounted read-only), so NanoClaw's inbox-containment class never arises; no allowlist file, no mtime hot reload, no mount-class taxonomy is ported. |
| ISO-F14 | Egress lockdown through the credential gateway | PARTIAL | PLUGIN |  | CONFIGURE | O4 | Configure rather than build. Each profile runs the built-in docker backend with `HERMES_DOCKER_BINARY` pointing at the podman wrapper and the egress hop enabled with `enforce_on_docker: true`, so every sandbox receives the CA read-only, the proxy environment and `HERMES_PROXY_TOKEN_*` swap tokens instead of credentials, the collision guards refuse any config that would override them, and the egress fingerprint label prevents reusing a pre-egress container. OneCLI is reached by pointing the HOP'S UPSTREAM at the profile's OneCLI gateway, never by a second proxy env inside the container (CRED-F28); exactly one egress topology per profile is asserted in the render test. Because enforcement rejects an operator-supplied `--network`, the no-other-route half is the deployment netns (P6/P7 OpenShell/APF), which the doc states explicitly alongside the fail-closed spawn behaviour and the exact error the operator sees when the hop is down. Scope statement, also in the doc: this row guarantees the SANDBOX holds no credential and has no second route; it does NOT cover host-side egress. MCP clients run in the gateway/agent process and the hop is Docker-backend-specific, so MCP traffic leaves the gateway with the routed profile's secret-scope credentials — hence ISO-F10's stdio-MCP deny is justified as 'no un-sandboxed process spawning', not as 'MCP goes over the hop', and per-profile MCP credential attribution is a named residual on O4 that GOV-F27 resolves one of two ways (per-profile keys in the secret scope with `trust: untrusted` gating writes, or remote MCP base URLs pointed at the OneCLI gateway host-side). The choice is the operator's and is stated in the doc page. |
| ISO-F15 | Session driver seam (docker | local no-Docker | vm tier) and gateway-provider se | BETTER_IN_HERMES | PLUGIN | Y | CONFIGURE | O4 | CONFIGURE, split by execution surface. Every coworker's config.yaml sets `terminal.backend: docker` with `HERMES_DOCKER_BINARY` pointing at the podman wrapper, so each profile gets its own labelled podman container without inventing a backend name; `local` is banned by rendering the key for every profile and asserting it in the compose test. Kanban workers (`hermes chat -q` spawned with `HERMES_HOME=<profile>`) and cron jobs (chat subprocesses inheriting HERMES_HOME) are separate processes and DO resolve their own profile's backend, so per-profile podman sandboxes are configuration for ALL durable work — which is where the fleet's tool-bearing work lives. Turns served inside the shared gateway process (Bot Chat, rooms, platform DMs/channels, webhook sessions) do NOT, at the pin: `TERMINAL_*` is process-global and `tools/terminal_scope.py` exists only on main. Three consequences recorded as a CORE-CHANGE for the operator: (a) tool-bearing work runs on the board, not in Bot Chat, until the pin moves; (b) moving the pin to a post-decomposition tree containing `tools/terminal_scope.py` is the prerequisite for gateway-served tool calls and is this row's one upstream/pin ask; (c) setting `TERMINAL_ENV=docker` in the gateway unit is NOT a substitute — every `TERMINAL_DOCKER_*` is equally process-global, so all profiles would share one container identity. ISO-F10's predicate therefore reads the resolved backend from the profile's own config plus (on main) the active terminal scope, and hard-fails a gateway-process tool call whose profile is not the launch profile while the pin lacks terminal_scope. A custom provider plugin is written only if the wrapper cannot express a required flag (slirp/vfs/no-pivot), and that is a separate operator-approved decision (CRED-F28 ships none). NanoClaw's driver seam and its vm tier are retired. |
| ISO-F16 | Per-group container config and multi-provider runner | PARTIAL | CONFIG_ONLY |  | CONFIGURE | O2,O4,O6 | Per-coworker runtime config is the profile's own `config.yaml`, rendered by the compose plugin and shipped as a profile distribution: model, provider, effort and fallback providers under `model`/`providers`; MCP servers under `mcp_servers` (scoped per role, GOV-F27); mounts, image, backend and `docker_mount_cwd_to_workspace` under `terminal.*` (ISO-F13/F15); plus timezone and the profile's toolset list. Package lists stop being a mutable per-group array and become the pinned `terminal.docker_image` the fleet builds and versions. Provider or model changes need no runner switch because session storage is provider-neutral and `/model` hot-swaps a live session, and kanban can override model/provider per card for the frontier-orchestrator / cheap-worker pattern. Config changes ship through `hermes profile install/update` against the versioned distribution — with `--force-config` in the update step, since update otherwise preserves config.yaml — so every change is reviewable and revertible, pinned by the immutability of the source branch the profile records. |
| ISO-F17 | Host sweep and restart recovery (circuit breaker, lease, coordination tables) | PARTIAL | PLUGIN_PLUS_HOOK |  | ADOPT | O8 | Adopt Hermes's supervision stack whole, around ONE gateway: the fleet runs the default profile's gateway with `gateway.multiplex_profiles: true` and `multiplex_profile_allowlist` naming the roster, and because `container_boot.py` reconciles per-profile s6 slots the fleet image leaves every coworker profile at `desired_state != running` so only the default slot starts — the runbook records that `hermes -p <bot> gateway start` is a hard error while the multiplexer runs. The service unit restarts that one gateway under a systemd watchdog (launchd KeepAlive on macOS), `shutdown_watchdog` distinguishes alive from frozen loop, `restart.py` drains with exit 75 and stops the restart loop with exit 78 on fatal config, `resume_pending` re-arms interrupted sessions at startup, turn leases release on dead PIDs, and the kanban dispatcher reclaims stale claims behind its rate-limit / auth-blocker / protocol-violation guards. Sandbox orphans are the terminal backend's reaper; anything further is a cron job owned by the orchestrator, not a daemon. The doc maps each NanoClaw sweep item — processing_ack sync, stale detection, due-message wake, recurrence, crash-loop backoff — to its Hermes owner and records that the fixed 0/0/10/30/120/300/900 schedule is replaced by supervisor restart plus the respawn guard. |
| A2A-F18 | Wired-only agent messaging (destinations = routing map + ACL) | PARTIAL | PLUGIN |  | MERGE→LOOP-F37 | O3 | MERGE→LOOP-F37. This id survives as the WIRING predicate of the single `nv-fleet-gates` `pre_tool_call` callback plus the plugin-owned `edges(from_profile, to_profile, gated)` table in `plugin_db` and one `hermes wire add\|remove\|list` CLI command for the operator. The predicate denies any `message_agent(target=)` or `kanban_create(assignee=)` — and any kanban reassignment tool naming another profile — whose (calling profile from HERMES_HOME, target) pair has no row, returning `action: block` with the wired teammates listed so the model can recover; a `gated` row returns `action: approve` with a stable `rule_key` (A2A-F19). `delegate_task` is deliberately NOT gated: it spawns subagents inside the caller's own profile and crosses no edge; its fan-out is bounded by COST-F29's cap. `a2a_call` is not registered in this fleet (CH-F53 deferred); if peering is ever revived the same table gains a `peer:` edge kind and the predicate extends to it. Fail-closed is the plugin's own try/except (core swallows a raising callback), not `resolve_pre_tool_block`. The collaboration graph is not re-implemented and is NOT rendered: rooms and their membership are created through the gateway's `groups.*` JSON-RPC — by `hermes onboard coworker` for the fleet's standing rooms, by the operator in the desktop app afterwards — and `ui_meta` is only ever written through the profile-configure RPC with `ui_meta_expected_revisions`; the edge table is only the DM/assignment projection of that same wiring, and it is what the hermetic test exercises. Tool names are canonicalised through a vendored alias map before matching so an upstream rename cannot make the gate fail open (GOV-F22's surviving criterion). |
| A2A-F19 | a2a.send guard (self / lineage / destination) plus per-edge human approval polic | PARTIAL | PLUGIN_PLUS_HOOK |  | MERGE→LOOP-F37 | O3,O5 | Folded into LOOP-F37's ADR: the edge row carries `gated`, and when it is set the same `pre_tool_call` callback returns `action: approve` with `rule_key: wire:<from>:<to>` instead of allowing, so Hermes's own approval gate holds the send in the sender's live Bot Chat and the operator answers from the desktop app; no plugin approval transport, no per-edge approver table, no pending-approval store of our own. Retained as criteria: a gated edge holds rather than blocks in an INTERACTIVE session; the rule_key is stable per edge so an 'always' answer grants that edge and not every send; a denied or timed-out hold fails closed. And the honest limit, which the doc must state: a gated edge exercised from an unattended session (kanban worker under `single_query_mode`, cron fire under `cron_mode`, webhook route under `unattended_mode`) is DENIED, not held — so gated edges are wired only between interactive Bot Chat principals, and durable cross-bot work uses ungated edges plus the board. |
| A2A-F20 | Reply-home lineage (a2a_session_sources, source_session_id, in_reply_to) | PARTIAL | PLUGIN |  | ADOPT | O3,O8 | Adopt: a doc page maps the nanoclaw triple (a2a_session_sources, source_session_id, in_reply_to) onto the three native correlations — Bot Chat reply-home for conversational hand-offs, the kanban task plus its notify subscription for durable hand-offs, and the a2a task/contextId for cross-gateway peering (documented but not enabled, CH-F53) — and states the one operational rule that follows: reply-home is per invocation and the target always answers in its canonical Bot Chat, so a coworker that needs a threaded conversation opens a room or a kanban card instead of expecting per-message threading. Reply-home also depends on the canonical `Bot Chat` session existing per profile, which `hermes onboard coworker` creates (LOOP-F35). No plugin, no table; the only code obligation is that the `nv-fleet-gates` wiring predicate must not strip the completion notification path. |
| A2A-F21 | Runaway protection: echo-drop, bounced-a2a redrive, runaway card | PARTIAL | PLUGIN |  | CONFIGURE | O6,O8 | Configure, not build: the compose plugin renders per profile `tool_loop_guardrails.hard_stop_enabled: true` (mandatory at the pin, redundant only for gateway/cron platforms on main), the warn/hard-stop counters and `kanban.failure_limit`; because the guardrail block is the same value for every coworker, it is ALSO pinned in managed scope (the one `/etc/hermes/config.yaml` deep-merged onto every profile) so a self-modifying bot cannot widen it — this is a legitimate managed-scope pin precisely because it is fleet-uniform, unlike the per-profile role map, toolsets and mcp includes, whose immutability comes from the sandbox boundary instead. `A2A_MAX_PINGPONG_TURNS` is NOT rendered: peering guardrails land with CH-F53 if it is ever revived. The doc page states where each nanoclaw behaviour landed (echo-drop → room round caps plus the identical-call breaker, bounced-a2a redrive → cross-gateway only, deferred, runaway card → COST-F30's ceiling decision, which is the one actionable human alert NF-6 asks for). No plugin is added for this row. |
| GOV-F22 | Guard catalog (allow | hold | deny) with grant replay | PARTIAL | PLUGIN |  | MERGE→LOOP-F37 | O3,O5 | Folded into LOOP-F37's ADR as three criteria on the single veto callback rather than a catalog: (1) the set of tool names the veto restricts is enumerable and asserted by a test against the running registry at plugin load, so a typo, an upstream rename or a new shell-capable tool fails the plugin's load instead of failing open; (2) every incoming tool_name is canonicalised through a plugin-vendored alias map (todo→todo_list, cronjob→cronjob_manage, process→process_manage, tour→gui_tour, tip→show_tip) before matching, and every outbound ctx.dispatch_tool resolves the spelling that exists in the running tree, since registry.dispatch returns 'Unknown tool:' rather than raising; (3) hold is expressed as `action: approve` with a rule_key into Hermes's own gate and deny as `action: block`, and FAIL-CLOSED IS THE PLUGIN'S OWN OBLIGATION — the callback body is wrapped in try/except BaseException returning a block, because core swallows a raising `pre_tool_call` callback and only converts a hang into a block. Grant replay is dropped with a stated reason. |
| GOV-F23 | Approvals primitive (approver picking, cards, reject-with-reason, ghost finalize | PARTIAL | PLUGIN |  | CONFIGURE | O5,O7 | Configure. The compose plugin renders each profile's approvals block — `mode: smart`, `timeout: 300`, and all THREE unattended resolvers set to deny: `cron_mode` (cron fires), `unattended_mode` (webhook / api_server), and `single_query_mode`, which is the one that actually governs kanban workers because the dispatcher spawns them as `hermes chat -q`. Because durable fleet work runs on the board, deny alone is not a policy: the same render emits, per role, a narrow `command_allowlist` (checked BEFORE detection in `check_all_command_guards`) covering exactly the verbs that role legitimately needs — the fixer's `git push` to its own branch, `gh pr create`, `gh pr comment`, its build and test commands — and keeps `approvals.deny[]` as the never-bypassable floor for verbs that must never run unattended (`git push --force*`, release and tag verbs), with `denial_breaker_threshold` set. Nothing is set to `approve`. A worker that hits a genuinely new dangerous verb is refused instantly rather than held, comments on its card and hands the decision to the orchestrator's Bot Chat, where the native gate has a human. Immutability is split: only the fleet-uniform keys (`approvals.cron_mode` / `single_query_mode` / `unattended_mode`, `security.approval.*`) go into managed scope, which deep-merges one machine-wide file onto every profile; the per-role `command_allowlist` and `deny[]` live in the rendered profile config and are unreachable to the agent because no profile mounts `$HERMES_HOME` (ISO-F13). The doc page maps nanoclaw's approvals primitive term by term onto the native one; `register_approval_transport` stays documented as the escape hatch if a later release wants unattended holds DM'd, but no transport plugin ships for O1-O8. |
| GOV-F24 | Immutable approval-decision ledger (approval_decisions) | MISSING | PLUGIN |  | BUILD | O5,O8 | BUILD an `nv-approval-ledger` plugin, `kind: standalone`, kept deliberately separate from `nv-artifact` for the review-surface reason above and using the SAME single `pre_gateway_dispatch` webhook observation seam (no second per-delivery subscription; there is no webhook-delivery hook to subscribe to). plugin_db table `approval_decisions` with UNIQUE(repo, pr, commit_sha) and INSERT OR IGNORE so an identical repeat is a no-op and a conflicting verdict on the same key is refused and logged; a `provenance` column separating agent_verified from human rows, with `list_trusted_decisions` as the only read path metric consumers may use. The write gate uses the mechanisms that exist: `record_decision` / `record_human_verdict` are registered through `ctx.register_tool(..., check_fn=...)` so they are ABSENT from the schema of any profile that is not the reviewer or the orchestrator; the writer set is the plugin's own setting `plugins.entries.nv-approval-ledger.writers`, re-read fail-closed on every call (absent or unreadable = no writer) — and because that list is FLEET-UNIFORM it may legitimately be pinned in managed scope, unlike per-profile policy; the single `nv-fleet-gates` `pre_tool_call` predicate blocks the write tools for any other profile as belt-and-braces; and no profile can edit its own config from inside its sandbox (ISO-F13). Plugin capabilities are NOT used — that registry is closed and an unknown id is dropped. Human verdicts enter through exactly one door: the webhook route for pull_request_review, keyed on the X-GitHub-Delivery id for idempotency and validated by the route's HMAC — an agent can never mint a human verdict, and the ledger has no runtime of its own and no second approval mechanism. |
| GOV-F25 | PR-to-session mappings, first-claim-wins | PARTIAL | PLUGIN |  | BUILD | O1,O3 | BUILD `nv-artifact` — ONE plugin, `kind: standalone`, kanban-anchored, owning one plugin_db with two tables and absorbing OBS-F48: `ownership(repo, pr, task_id, profile)` written INSERT OR IGNORE so the first claimant wins and a second profile's claim is refused with the current holder named while the same holder may refresh, and `outcomes(artifact, terminal_outcome, cost_usd, profile)` appended from the kanban observers plus `on_session_end` with the session cost rolled up from `session_model_usage` and attributed through the ownership row. Feeds: ONE `post_tool_call` observer watching `terminal` AND `execute_code` for a successful `gh pr create` URL (a PR created from `execute_code` must not go unclaimed), ONE `pre_gateway_dispatch` subscription that sees every webhook delivery once and returns allow/None so the route's own dispatch is untouched (there is no per-delivery hook), and the kanban observers named above. The card itself remains the ownership record, so the claim is stamped onto the kanban task and the owner is whoever holds its claim lock; the plugin resolves (repo, pr) to the owning task and delivers later events through that task's notify+wake subscription, falling back to ctx.inject_message into the owner's session, rather than minting a session per delivery. CLI verbs via ctx.register_cli_command, registered orchestrator-only: `hermes pr remap` and `hermes outcomes funnel\|winrate\|cost-per-merge`. No routing table of our own beyond that index, no peer forwarding, no second dispatcher, and no OTLP publication (see OBS-F48). |
| GOV-F26 | cli_scope enforcement and host command gate | PARTIAL | PLUGIN |  | MERGE→LOOP-F37 | O3,O4,O7 | MERGE→LOOP-F37. This id survives as the FLEET-ADMIN SCOPE predicate of the single `nv-fleet-gates` `pre_tool_call` callback plus the profile→role map in that plugin's settings: admin-shaped tools are denied by TOOL NAME to any profile that is not the orchestrator, returning `action: block` with a scope message. The fleet-admin tools themselves (onboard/render a profile, board and room administration, cron for another profile, cost-cap control, ledger writes) are registered by `nv-coworker-compose` / `nv-fleet-gates` / `nv-approval-ledger` with `check_fn` and enabled only in the orchestrator's profile config, so at 'disabled' the model never sees them and at 'group' the handler auto-fills the caller's own profile id and refuses cross-profile arguments. Per-profile restriction of the built-in surface uses `platform_toolsets` and `agent.disabled_toolsets` rendered by the compose plugin. Immutability is SPLIT, because managed scope applies one machine-wide file identically to every profile: managed scope carries only FLEET-UNIFORM pins — `plugins.enabled` (so the veto plugin is enabled for every coworker and no rendered profile can omit it), `terminal.backend`, `proxy.*`, `tool_loop_guardrails.hard_stop_enabled: true`, `sessions.auto_prune: false`, the three `approvals.*` unattended modes — while PER-PROFILE policy (the role map, per-profile toolsets, per-profile `mcp_servers.tools.include`, the wiring edges) lives in the rendered profile config and the plugin's own settings, and its immutability comes from the sandbox boundary (no profile mounts `$HERMES_HOME`, so an agent has no path to edit its own config — ISO-F13) plus the veto itself. Any managed-scope test must set `HERMES_MANAGED_DIR`. This is the fleet's cli_scope: one predicate on one hook, one settings map, no action catalog and no second runtime. |
| GOV-F27 | MCP tool allow-list, authenticated MCP proxy, server registry | PARTIAL | CONFIG_ONLY | Y | CONFIGURE | O4 | Configure: each coworker profile's config.yaml declares only the MCP servers its role needs, with `tools.include` naming the exact tool set (a reviewer gets read verbs, a fixer gets write verbs) and `trust: untrusted` on any third-party server so write-capable tools escalate to the approval gate, all rendered by the compose plugin. Immutability is split, because managed scope is machine-wide and cannot hold a per-profile value: only fleet-uniform keys go there, while per-role MCP scoping is made non-bypassable by the veto — the single `nv-fleet-gates` `pre_tool_call` predicate denies `mcp_*` tools whose server is not in the calling profile's rendered allow-list, so widening the include list in its own config.yaml buys a bot nothing (and it cannot reach that file from its sandbox anyway; note also that the profile config write guard is not profile-aware under multiplex, a second reason not to rely on config immutability alone). Per-profile managed scope is a P8 upstream ask. The doc page maps ncl's mcp-tools policy states onto include/exclude plus trust, and states the O4 residual honestly: MCP clients run in the gateway process (stdio servers as its subprocesses, remote servers as its own HTTP calls), so their egress does not traverse the profile's sandbox hop — attribution rides the context-local secret scope (fail-closed under multiplex) carrying that profile's OneCLI agent token, and the operator chooses either to keep per-profile MCP keys in the profile secret scope with `trust: untrusted` gating writes, or to point remote MCP base URLs at the OneCLI gateway host-side so the proxy still swaps the token. No plugin, no separate mcp_* gate of its own. |
| CRED-F28 | OneCLI credential gateway (per-agent identity, request-time injection, credentia | PARTIAL | PLUGIN |  | BUILD | O4 | BUILD `podman-onecli`, `kind: standalone`, credential half only — NO TerminalEnvironmentProvider subclass ships. Every profile runs the built-in docker backend with `HERMES_DOCKER_BINARY` pointing at the podman wrapper (ISO-F13/F14/F15 unchanged), so the fleet keeps the fail-closed egress spawn, the read-only CA mount, the collision guards and the orphan reaper for free. The plugin (a) registers an `onecli` SecretSource via ctx.register_secret_source that resolves ONLY proxy coordinates — the per-profile OneCLI gateway base URL, the CA path and that profile's agent token, never a provider credential, because the source is startup-time, synchronous and refresh-free (one fetch() per process per HERMES_HOME); (b) calls OneCLI `ensureAgent(name=identifier=<profile>)` from `on_session_start` so every coworker has its own vault identity and every egress call is attributable to it; and (c) supplies that profile's `strip_env_keys` list so no real credential exists inside the sandbox. Exactly one egress topology per profile, asserted in the render test: the DEFAULT is Hermes's own hop (`proxy.enabled: true`, `enforce_on_docker: true`) with the hop's UPSTREAM pointed at that profile's OneCLI gateway — never a second proxy env inside the container, since `check_docker_env_collisions` refuses `docker_env` entries overriding proxy-control vars under enforcement; the documented alternative is OneCLI proxy env rendered into `terminal.docker_env` with `proxy.enabled: false`, which forfeits the fail-closed spawn and the collision guards. Every real credential is injected by the OneCLI gateway at request time, host-side traffic included. If the podman wrapper cannot express a required flag (slirp/vfs/no-pivot), that is a separate operator-approved core change, not a provider subclass. Egress lockdown (the no-route internal network) belongs to ISO-F14; credential-use approvals ride Hermes's existing gate. |
| COST-F29 | Two-tier per-session cost cap with immortal sessions and runtime policy | PARTIAL | PLUGIN_PLUS_HOOK |  | BUILD | O6 | BUILD `nv-cost-cap`, `kind: standalone`, hooks plus middleware only, holding {capUsd, ceilingUsd, spentUsd, status, immortal, window, episodeId, budgetGen} per session in plugin_db — policy, window and episode state ONLY. The authoritative accrual read is Hermes's own accounting: `sessions.estimated_cost_usd` plus the session's `session_model_usage` rows (already per-call, subagent- and aux-inclusive per COST-F31), reconciled on `post_tool_call` and `on_session_end` so codex-backed profiles — which price and persist usage themselves without firing `post_api_request` or entering the middleware chain — are accounted at the same grain; the `post_api_request` observer (keyed by api_request_id, INSERT OR IGNORE against double-logged responses) is a FAST PATH for the current window, never the sole source, because core fires it inside `try/except: pass`. Enforcement never raises: the `llm_execution` middleware refuses by RETURNING a synthetic terminal response of the same shape `_perform_api_call` yields — the plugin contains no `raise` on the enforcement path, since a raising callback is swallowed and the provider call proceeds — and the authoritative stop is the profile-scoped ESTOP sentinel plus a `pre_tool_call` block, both of which genuinely gate new turns, cron fires and kanban spawns. For codex profiles the `pre_tool_call` belt plus ESTOP is the ONLY enforcement today; that limitation is stated in the doc page and is this row's P8 upstream ask (fire `post_api_request` / run llm_execution middleware on the codex app-server path). Two windows as in the validated nanoclaw model: non-immortal sessions cap per fire and stop; immortal sessions cap per day, never hard-stop, and only record the crossing for the card. Runtime policy (fleet ceiling plus per-profile overrides) lives in the plugin's own settings with the fleet ceiling pinnable in managed scope (a fleet-uniform value) and is exposed by an orchestrator-only CLI command; no env var is the source of truth, and the plugin owns no runtime beyond its hooks. Deleted, not ported: the transcript scan, the vendored flat rate table and cost-thresholds.json. |
| COST-F30 | Cost escalation approval card and exact-value set-ceiling | MISSING | PLUGIN_PLUS_HOOK |  | BUILD | O6,O7 | The same `nv-cost-cap` plugin adds the decision surface, routed through the surface the panel already renders: on a Tier-1/Tier-2 crossing it raises the decision through Hermes's own approval gate — `request_tool_approval(..., rule_key='cost:<episode>:<budgetGen>')`, which lands in the once/session/always/deny ladder and is presented on every surface that already shows approvals, the desktop panel included — with `/cost continue\|stop\|ceiling <usd>` (ctx.register_command) as the text path and native buttons via `register_slack_action_handler` / `register_telegram_handler` / `register_platform_handler` as an enhancement only on adapters that implement them. Immortal sessions get Continue-only. Resolution is a compare-and-set on (episodeId, budgetGen) in plugin_db regardless of surface, so a second click OR a second `/cost continue` grants exactly one increment and reports 'already resolved'; set-ceiling takes an exact USD value, never a multiplier. Click/command authorisation is checked against the plugin's OWN `operators` setting (rendered in P2, pinned in managed scope as a fleet-uniform value) before any grant, so this row does not depend on a later phase's role map. A reconciler task, started with ctx.spawn_task from the plugin's first hook fire — not from a `gateway:startup` event, which is the separate filesystem hook mechanism and not a VALID_HOOKS name — finalises episodes whose session died mid-decision. Both P0 defects found in the nanoclaw v2 card (pill double-grant, unauthenticated click) are the two criteria this row must prove. |
| COST-F31 | Durable cost ledger, pricing parity, cost observability | PARTIAL | PLUGIN | Y | ADOPT | O6,O8 | Adopt: a doc page states that Hermes's native accounting and pricing are the base of record — per-call accrual including aux, MoA and subagent rollup, tokens persisted per (session, model, provider, mode) so a pricing correction is a reprice rather than a re-scan, and cost_status actual\|estimated\|included\|unknown carried through — and that nanoclaw's transcript scan, its verbatim LiteLLM table and cost-thresholds.json are deleted rather than ported; per-coworker cost is read from the profile-scoped analytics API and /usage in the one panel. The row's escalate=Y stays as a P8 upstream ask (date-effective pricing and a fleet-wide aggregate endpoint), and the only local obligation is the acceptance test that pins the invariant future cost work must not break — the same invariant COST-F29's enforcement read depends on: the session total equals the sum of its priced usage rows with subagent and aux included. |
| SCHED-F32 | Durable scheduled tasks (ncl tasks) with cron recurrence and run logs | BETTER_IN_HERMES | PLUGIN | Y | ADOPT | O1,O8 | ADOPT Hermes cron as-is. Each coworker profile owns its jobs in `$HERMES_HOME/profiles/<name>/cron/jobs.json`; the compose plugin ships them as the `cron/` directory of that profile's distribution, so `hermes profile install/update` is the git-versioned deployment path that `ncl tasks create` used to be. Run-log parity is `cron/output/<job>/*.md` (retention `cron.output_retention`) plus the `cron/executions.py` ledger; operability parity is `hermes cron runs\|incidents\|notepad\|doctor`. Do NOT build a cron plugin and do NOT build a name-keyed cron guard of its own: the two NanoClaw behaviours Hermes lacks are (a) a minimum-interval recurrence guard and (b) cron-for-others as an admin-only action, and both are predicates on the fleet's single `nv-fleet-gates` pre_tool_call callback (LOOP-F37), matched against the tool name the running tree registers and resolved at runtime via `registry.get_entry` with the vendored alias map. Deliverable: a doc page mapping `ncl tasks list/create/update/pause/resume/run/append-log` onto the `hermes cron` verbs and the executions ledger, plus one hermetic acceptance test. |
| SCHED-F33 | Pre-task script gates with failure backoff and auto-pause | PARTIAL | PLUGIN_PLUS_HOOK |  | CONFIGURE | O1,O8 | CONFIGURE. The compose plugin renders `cron.script_timeout_seconds: 30` (nanoclaw parity with Hermes's 3600s default), `cron.failure_nudge_threshold: 3` — which must be emitted BY VALUE because it has no entry in `config_defaults.py` and cannot be inferred from defaults — and per-job `script` / `monitor` fields into each profile's `cron/` distribution directory; the wake gate needs no code because `_parse_wake_gate` already implements the exact `wakeAgent: false` contract. Operability is native: `hermes cron incidents` + `ack` for the known-failure case, `hermes cron doctor` as a scheduled fleet health check on the orchestrator (exit 1 = actionable), `_block_and_pause_job` for unrunnable jobs. NO plugin and no `executions.db` reconciler. The residual — automatic next-run backoff and auto-pause after N consecutive failures — is a P8 upstream ask for two config keys (`cron.failure_backoff_minutes`, `cron.auto_pause_after_failures`) so the scheduler owns it; until then the streak nudge plus doctor is the fleet's answer and nothing is silently disabled. |
| SCHED-F34 | Per-group timezone override | EXISTS | CONFIG_ONLY |  | CONFIGURE | O1,O7 | CONFIGURE. The compose plugin writes `timezone: <IANA>` into each coworker profile's `config.yaml`; `hermes_time.py` resolves it (env override → profile config → host local, invalid values warn and fall back) and it grounds that profile's cron parsing, run-log stamps and system-prompt clock automatically. The sandbox side is one line of the P4 terminal block: the profile's podman sandbox is launched with `TZ` set from the same key, so shell timestamps inside the sandbox match. Operator-facing `hermes` output stays in the gateway host zone, matching nanoclaw's split. Approval-gating an agent-initiated change is native too — `config.yaml` writes are dangerous commands requiring human approval — and in any case no profile can reach its own config from inside its sandbox (ISO-F13). No plugin, no fallback chain to build. |
| LOOP-F35 | Lego coworker composition (spines, types, traits, composed CLAUDE.md) | PARTIAL | PLUGIN |  | BUILD | O2,O3,O4 | BUILD `nv-coworker-compose` — ONE plugin, `kind: standalone` in plugin.yaml, one manifest, one golden-render test, one settings block. It registers three CLI verbs via `ctx.register_cli_command` (`hermes coworker compose`, `hermes onboard project <url\|path>`, `hermes onboard coworker <coworker-types.yaml>`), the two onboarding verbs also as `/onboard-project` and `/onboard-coworker` via `ctx.register_command` and as gateway-side tools via `ctx.register_tool(check_fn=...)` so only the orchestrator's schema carries them, with `nv-fleet-gates` denying them by tool name to every other profile. It reads `container/spines/<project>/coworker-types.yaml`, resolves the extends chain (identity leaf-wins; invariants/context/skills/workflows/overlays append+dedup; trait-domain bindings) and RENDERS, per coworker type, a complete Hermes profile distribution: `distribution.yaml`, `SOUL.md`, `config.yaml`, `skills/` (LOOP-F36), `cron/` (SCHED-F32/F33), `mcp.json`, `.env.EXAMPLE`. Beyond the distribution it writes the Bot-Mode identity that distributions cannot carry: `ui_meta['hermes-bots']` (title, role description, avatar slot) into each profile via the profile-configure RPC with `ui_meta_expected_revisions` (per-key CAS, never a raw profile.yaml overwrite), and `hermes onboard coworker` creates each profile's canonical `Bot Chat` session (title exactly `BOT_CHAT_TITLE`) and the fleet's standing rooms through `groups.create` / `groups.state`. Rooms and room membership are NOT rendered into any file. Nothing is injected at runtime and no prompt is assembled in-process — `hermes profile install\|update` is the deployment path (pin by source-branch immutability, `--force-config` for config keys) and `git diff` is the review surface. The rendered `config.yaml` is the fleet's single source of the invariant keys every CONFIGURE row depends on: `timezone`, `sessions.auto_prune: false` (on the DEFAULT profile as well as every coworker profile), `checkpoints.auto_prune` explicit, `compression.in_place: true`, `memory.*` caps, `skills.external_dirs` / `create_dir`, `terminal.*` (backend, volumes, `docker_mount_cwd_to_workspace`, image), per-profile toolsets, `approvals.*` plus per-role `command_allowlist`, `plugins.entries.nv-fleet-gates` policy, and the cost-cap keys — plus, on the DEFAULT profile only, `gateway.multiplex_profiles`, `multiplex_profile_allowlist` and every webhook route with its own secret and `profile:` binding. A missing key is a render bug caught by a golden test, never a silent default. Explicitly out of scope: no gateway of its own, no a2a, no guard catalog, no per-bot process. |
| LOOP-F36 | Workflows and overlays (durable multi-step loops) | PARTIAL | PLUGIN |  | MERGE→LOOP-F35 | O1,O2 | MERGE→LOOP-F35. Keep LOOP-F36 as criteria inside the compose ADR: each workflow body renders to `skills/<workflow>/SKILL.md` in the profile distribution, each coworker type gets a `skill-bundles/<type>.yaml` alias whose `instruction:` prepend is the anchored workflow entry, and overlays are spliced by `applies-to` against the type at render time so the shipped SKILL.md is already the composed text — a `git diff` shows exactly what each bot will run. Project-specific workflows go in the repo's `<root>/.hermes/skills/` tier, which outranks profile and bundled skills on its own turf. Durability is adopted, not built: `/goal` for objective-driven multi-round work, `/loop` for timer-driven polling with `loops.max_ticks` as the runaway backstop, and kanban cards for hand-offs that must survive a session. No `pre_gateway_dispatch` rewrite, no `pre_llm_call` splicing, no overlay resolution at runtime. |
| LOOP-F37 | Codex critique gate and chain-routing gate (delivery/egress chokepoints) | PARTIAL | PLUGIN_PLUS_HOOK | Y | BUILD | O3,O5 | BUILD `nv-fleet-gates` — ONE plugin, `kind: standalone` in plugin.yaml (there is no `hooks` kind; an unknown kind logs a warning and is treated as standalone), declaring its `hooks:` list and registering EXACTLY ONE `pre_tool_call` callback plus its CLI verbs, with per-profile policy in `plugins.entries.nv-fleet-gates` rendered by the compose plugin and per-session state in `plugin_db`. The callback runs an ordered predicate chain and returns the FIRST match, blocks evaluated before approves, so no second plugin can pre-empt a block with an approve: (1) SANDBOX (ISO-F10, ISO-F13/F14/F15 — inert behind `enforce_sandbox: false` until P4): the resolved backend must be the profile's container backend read from that profile's own config plus, on trees that have it, the active terminal scope; `ensure_task_env()` must have run; stdio `mcp_*` is denied as 'no un-sandboxed process spawning'; the dangerous-command floor waived by `_should_skip_container_guards` is re-asserted; and while the pin lacks `tools/terminal_scope.py` a gateway-process tool call whose profile is not the launch profile is hard-failed (ISO-F15). (2) FLEET-ADMIN SCOPE (GOV-F26, GOV-F22, OPS-F58): admin-shaped tools — onboard/render, board and room administration, cron for another profile, cost-cap control, the approval-ledger write tools — are denied by TOOL NAME to any profile that is not the orchestrator. (3) WIRING (A2A-F18/F19, CH-F51, P5): `message_agent(target=)` / `kanban_create(assignee=)` and any kanban reassignment naming another profile against an unwired edge is blocked; a `gated` edge returns `action: approve` with `rule_key: wire:<from>:<to>`. (4) GATES (this row, LOOP-F38, LOOP-F40, MEM-F43): a delivery carrying a stage marker (`[Resolution]`, `[handoff]`, `[Fix Report]`, `[Review Verdict]`) on `message_agent`, on `terminal` OR `execute_code` matching `gh pr create\|review\|comment`, or on the kanban terminating tools, when `plugin_db` has no critique row for this session's required stages; `write_file`/`patch`/mutating `terminal` before a plan exists under `.hermes/plans/` (per-profile toggle); `gh pr comment` on a PR with no human invitation token in the route payload; and `skill_manage` create/patch/delete whose resolved target path is inside the shared learnings clone (or outside the caller's own skills tree) for any profile that is not the orchestrator, since that tool writes host-side and no mount can stop it. FAIL-CLOSED IS THE PLUGIN'S OWN OBLIGATION, not the core's: the callback body is wrapped in `try/except BaseException` and returns `action: block` with 'nv-fleet-gates internal error — refusing' on any error, because core swallows a raising `pre_tool_call` callback and lets the call proceed (only a HANG is turned into a block); as a belt the compose plugin also renders a `pre_tool_call` shell hook with `matcher` + `fail_closed: true` for the sandbox predicate. Every incoming tool_name is canonicalised through a vendored alias map before matching, and the restricted set is asserted against the running registry at plugin load so a rename or a new shell-capable tool cannot make the gate fail open. `/codex-critique` is `ctx.register_command` → `ctx.dispatch_tool("delegate_task", {background: true})` writing the critique row on completion. In-fleet reply anchoring needs no `in_reply_to`: the room plus each member's persistent `Group: <name>` session is the anchor. One conformance test asserts exactly ONE plugin registers `pre_tool_call` in a rendered fleet. One upstream ask survives to P8: a turn-end hold that fires when the turn edited no files (`pre_verify` is gated on `_turn_file_mutation_paths`); the `in_reply_to`-on-a2a ask is parked with CH-F53. |
| LOOP-F38 | Plan gate, intent router, workflow-state reset, buddy monitor | PARTIAL | PLUGIN |  | MERGE→LOOP-F37 | O5 | MERGE→LOOP-F37. The plan gate is a criterion on the single `nv-fleet-gates` `pre_tool_call` callback: a per-profile-toggled predicate that refuses `write_file` / `patch` / mutating `terminal` until the session has produced a plan under `.hermes/plans/*.md`, closing the gap that the built-in `/plan` only forbids edits for its own turn. Everything else in the row is adopted rather than built and is recorded in the same ADR as 'covered natively': workflow-state reset uses `on_session_reset` / `pre_llm_call(is_first_turn)` and the todo tool's compaction-surviving checklist; intent routing is the skills index plus `/goal` plus kanban `decompose` (configured through `auxiliary.kanban_decomposer` and `kanban.orchestrator_profile`), so no per-turn `ctx.llm.complete_structured` classifier is added; the buddy monitor is `/review` for in-session second opinions, `background_review` for post-turn memory/skill saves, and the kanban dispatcher's real ladder — TTL reclaim with live-PID extension, `kanban.dispatch_stale_timeout_seconds` for heartbeat-dead running cards, `detect_crashed_workers`, the per-task `max_runtime_seconds` kill, `kanban.failure_limit` and `kanban.reconcile_orphans` — for the fleet-level watch. |
| LOOP-F39 | supervise-issues nudger and self-healing worktree reaping | PARTIAL | PLUGIN | Y | CONFIGURE | O1,O3,O8 | CONFIGURE. Reaping is adopted whole: `hermes worktree list\|prune` for accumulated trees (archived, never deleted, unpushed work kept), `/worktree new` / `hermes -w` per task so each run starts on the freshly-fetched remote tip, and the kanban dispatcher's own ladder for worker liveness — TTL reclaim with live-PID extension (`release_stale_claims`), `kanban.dispatch_stale_timeout_seconds` for heartbeat-dead running cards, `detect_crashed_workers`, the per-task `max_runtime_seconds` kill, `kanban.failure_limit`'s consecutive-failure breaker and `kanban.reconcile_orphans` for broken claim bookkeeping. The nudger becomes a skill plus a 12h cron job rendered into the ORCHESTRATOR profile's distribution: the skill's universe is `hermes kanban diagnostics` + `kanban_list` for in-fleet work and `gh` (run inside the orchestrator's own sandbox) for GitHub issues with no card yet; a nudge is a `kanban_comment` on the card plus a `message_agent` to the assignee bot, which lands in that bot's canonical Bot Chat; repeated non-response escalates by messaging the human in the orchestrator's room. No plugin, no reaper of our own, no polling loop outside cron. The escalate=Y flag drops: nothing here needs a core change. |
| LOOP-F40 | PR review/approval coworkers with human-invited post-back and CI-gated release | PARTIAL | PLUGIN |  | CONFIGURE | O1,O5 | CONFIGURE. The compose plugin renders two GitHub webhook routes into the DEFAULT (multiplexer) profile's `config.yaml` — never into a coworker profile — each with its own `secret` and an explicit `profile:` binding: a `pull_request` route (`opened`/`synchronize`, `profile: reviewer`) whose script filter upserts a kanban card keyed by repo+PR with the latest head SHA and returns `[SILENT]` while CI is not green, and a `check_suite`/`check_run` route that promotes the matching card to `ready` so the kanban dispatcher spawns the reviewer worker in its own sandbox. Traffic arrives at `/p/reviewer/webhooks/<route>`. Review lifecycle is native kanban: `kanban_request_review` → the bundled `sdlc-review` skill → `kanban_complete` or `kanban_request_changes` back to the implementer, with the whole thread on the card. Human-invited post-back is a route `filters` clause on `comment.body` containing the configured bot mention, and the post-back is the route's own `deliver: github_comment`. The per-route `toolsets` grant is coarse — `terminal` is one toolset with no read/write split, so it can only withhold `terminal` entirely, viable for a review route that works through file tools and MCP but not for one that shells `gh` — therefore the load-bearing gate is LOOP-F37's veto: `terminal` (or `execute_code`) matching `gh pr comment\|review` on a PR with no invitation token in the session's route payload is blocked, fail-closed; where the reviewer genuinely needs no shell, the route also drops `terminal` as defence in depth. The append-only approval-decision ledger and its `agent_verified` provenance rule remain GOV-F24. Signature and replay safety are adopted as-is; no plugin. |
| MEM-F41 | OKF file memory per agent group plus persona prepend | PARTIAL | PLUGIN |  | CONFIGURE | O2,O8 | CONFIGURE. Each coworker gets its identity from the rendered `SOUL.md` (slot #1, per profile, security-scanned) and its role-scoped standing instructions from the workspace `AGENTS.md` chain — that is where the OKF dossiers land at onboarding, git-versioned in the repo rather than in a private memory tree. Curated volatile memory stays Hermes-native `~/.hermes/memories/MEMORY.md` + `USER.md`, per profile, with `memory.memory_char_limit` / `memory.user_char_limit` raised by the compose plugin to whatever the coworker types actually need and `memory.write_approval` left off for bots that must not stall on a human. Note that the memory tool writes host-side, so a cross-profile memory write is prevented by per-profile HERMES_HOME resolution plus the veto's path predicate (MEM-F43), not by a container mount. Cross-coworker knowledge is MEM-F43, not this row. Deliverable: a doc page mapping OKF (index.md, system/definition.md, dossiers, persona prepend) onto SOUL.md / AGENTS.md / MEMORY.md, the one-time migration runbook, and a hermetic test proving per-profile isolation. Reopen this row as BUILD only if the raised caps demonstrably fail in a measured run — the escape hatch is then a `MemoryProvider` subclass activated by `memory.provider`, which costs the fleet its single external-provider slot. |
| MEM-F42 | OKF synthesis (bounded always-loaded memory) | PARTIAL | PLUGIN |  | CONFIGURE | O8 | CONFIGURE. The compose plugin renders the `curator` block per coworker profile: `enabled: true`, `interval_hours` and `min_idle_hours` tuned per role, `consolidate: true` for the roles whose skill set actually overlaps (it costs an aux-model pass), `prune_builtins` per role, and `backup.enabled: true` with `keep` so every pass is rollback-able via `hermes curator rollback`; `hermes curator status` is the operator's view. Bounded always-loaded memory is enforced by the caps from MEM-F41 plus the memory tool's own in-turn overflow consolidation (the model is handed the current entries and told to consolidate, bounded per turn) — there is no unbounded growth path left to gate. Post-turn extraction stays `background_review`. No `okf-gate.py`, no `context_from` chain, no second synthesis loop; if a specific fold is genuinely needed it is a skill plus a cron job in the profile distribution (SCHED-F32), not a plugin. |
| MEM-F43 | Shared learnings and learnings wiki | PARTIAL | PLUGIN |  | CONFIGURE | O1,O8 | CONFIGURE. One git repo is the fleet's learnings store. The compose plugin renders `skills.external_dirs: [<shared-clone>/skills]` into every coworker profile so the shared learnings appear in each bot's skill index, slash commands and `skill_view` with no injection. Write access is NOT the sandbox mount: two gates instead — (1) non-promoting profiles leave `skills.create_dir` unset, so agent-authored skills land in the profile-local tree and never in the shared clone; (2) the single `nv-fleet-gates` `pre_tool_call` predicate (LOOP-F37) blocks `skill_manage` create/patch/delete whose resolved target path is inside the shared clone for any profile that is not the orchestrator, fail-closed, because that tool writes host-side via `atomic_write_text` and no mount sees it. The read-only container mount of the clone stays, but only as defence in depth for `terminal` / file-tool writes from inside a sandbox. Promotion is the orchestrator committing and pushing the clone, reviewed as a PR — that is the gate FR-8 asks for. The wiki layer is the bundled `llm-wiki` skill with `WIKI_PATH` pointed at the same clone, folded on a cron job in the orchestrator's distribution; cross-project isolation is a different `external_dirs` entry per fleet. The durable cross-bot working state stays `~/.hermes/kanban.db`, and attribution rides `skill_provenance` / `SkillNode.created_by` plus the per-profile subdirectory convention. No plugin, no new tool, no shared system-prompt section. |
| MEM-F44 | Transcript retention, archiving, compaction guidance and fresh project memory | PARTIAL | PLUGIN | Y | CONFIGURE | O8 | CONFIGURE, and it is the highest-value config row for O8. The compose plugin writes, explicitly and never by assumption, into the DEFAULT (multiplexer) profile's `config.yaml` AND every coworker profile's `config.yaml`: `sessions.auto_prune: false` (upstream default is now true and the sweep deletes the on-disk transcript and `request_dump_*` files, not just DB rows), `sessions.auto_archive: true` as the safe soft-hide alternative if state.db growth becomes a problem, `compression.in_place: true` so compaction soft-archives pre-compaction turns and everything stays FTS-searchable, and an explicit decision on `checkpoints.auto_prune` rather than its True default. A regression test asserts these keys on the default profile as well as the coworkers, after render AND after `hermes config migrate`, and the pin-move checklist re-asserts them — treat config `_config_version` 39→41 as irreversible in the runbook because migration 41 rewrites SOUL.md in every profile. Fresh project memory needs no hook: `hermes -w` / `/worktree new` bases each task's tree on the freshly-fetched remote tip, so the refresh-clones `on_session_start` plugin is dropped. If compaction summaries ever need steering, the seam is a memory-provider `on_pre_compress` or a `pre_llm_call` shell hook — not a PreCompact copy. No plugin; the escalate=Y flag drops to a P8 note asking upstream to make transcript-file deletion opt-in separately from row pruning. |
| OBS-F45 | Dashboard (Pixel Office / Coworkers / Timeline / Admin) with hook-event telemetr | PARTIAL | PLUGIN | Y | ADOPT | O7,O8 | ADOPT: the fleet panel is the one desktop app plus `hermes dashboard` on the one gateway. Deliverable is a doc page mapping Pixel Office / Coworkers / Timeline / Admin onto the native surfaces — Bots pane (one row per profile, Active-now presence strip, canonical Bot Chat, Routines tile, group rooms with @mentions and a needs-you badge) and the machine-level dashboard with the profile switcher (Sessions list/rename/pin/archive/export, Analytics tokens+cost by day and model, Cron, Logs, Skills, Profiles, Kanban board) — plus one acceptance test. The doc must state that the Bots list is gated on `ui_meta['hermes-bots']` being rendered into each profile (LOOP-F35) and that rooms are NOT a rendered surface: they are created and their membership set through the gateway's `groups.*` JSON-RPC / the desktop app as a one-time operator bring-up step (A2A-F18's criterion, RT-F01's recorded thin spot). Explicitly not ported: POST /api/hook-event, the NanoClaw dashboard app, dashboard-as-a-channel (browser and desktop chat already exist), and the codex hook bridge. The hook-event ledger the NanoClaw timeline needed is the outcomes table in GOV-F25, and if a Pixel-Office-style visual is ever wanted it is a drop-in dashboard UI plugin (manifest.json + JS bundle + plugin_api.py router) or a desktop plugin — never a service of its own. |
| OBS-F46 | claude-trace raw request/response capture | BETTER_IN_HERMES | PLUGIN | Y | CONFIGURE | O8 | CONFIGURE: set `HERMES_DUMP_REQUESTS=true` in the GATEWAY PROCESS environment — the service unit installed by `hermes gateway install` (systemd `Environment=` / launchd `EnvironmentVariables`), or managed `/etc/hermes/.env` — because the toggle is read with `os.getenv` (utils.py:33-35 via agent/turn_api_request.py:172) and a profile .env reaches only the secret scope, never `os.environ` (gateway/run.py `_profile_runtime_scope`). Capture is then fleet-wide for gateway-served turns; the compose plugin ALSO renders the key into each coworker profile's .env so the separately-spawned surfaces (kanban workers via `hermes chat -q`, cron chat subprocesses) are covered, since those load the invoked profile's .env at startup. Dumps land as `request_dump_<session_id>_*.json` beside the session files, secret-redacted, with API errors dumped unconditionally with status and body — which is exactly why `sessions.auto_prune: false` must be pinned on the DEFAULT (multiplexer) profile as well as every coworker profile (MEM-F44, OPS-F58.a): the startup sweep reads the launch profile's `sessions.*` and removes those files with the session. Deliverable is the service-unit setting plus the rendered keys, a doc page and a hermetic test; if a per-session JSONL keyed by api_request_id is later wanted for a viewer, it is two register_hook subscriptions on pre/post_api_request inside GOV-F25's nv-artifact plugin, not a new capture path. |
| OBS-F47 | Transcript viewing: show-transcript archive, ncl sessions messages, /upload-trac | EXISTS | PLUGIN |  | ADOPT | O8 | ADOPT: transcript viewing is native and complete — a self-contained HTML archive with collapsible tool output and --redact, md/qmd/jsonl per session, in-agent session_search over FTS5, `/save` to drop a transcript into the chat, the dashboard Sessions page for browsing, and `--format trace --upload` for the HF Agent Trace Viewer with redaction forced (upload raises rather than leak). Deliverable is a doc page mapping show-transcript, `ncl sessions messages` and /upload-trace onto those verbs, plus one acceptance test; fleet-wide viewing on the one gateway is a loop over `hermes -p <bot> sessions export`, kept in the runbook rather than shipped as a plugin CLI. |
| OBS-F48 | Fleet metrics and outcome analytics | PARTIAL | PLUGIN | Y | MERGE→GOV-F25 | O8,O1,O6 | MERGE→GOV-F25. This id survives as the `outcomes(artifact, terminal_outcome, cost_usd, profile)` table of the single `nv-artifact` plugin (kind: standalone) and its CLI verbs: the kanban observers named above plus `on_session_end` append one row per artifact (`<repo>#<issue\|pr>`) with its terminal outcome and the session cost rolled up from `session_model_usage`, joined to the same plugin_db's `ownership` table for profile attribution; `hermes outcomes funnel\|winrate\|cost-per-merge` ship through ctx.register_cli_command, orchestrator-only. Webhook-delivered events are observed with `pre_gateway_dispatch` returning allow/None so the route's own dispatch is untouched — there is NO per-delivery plugin hook, and `gateway_platform_event` fires only for the specific Telegram/Discord event types with real fire-sites. Publication is honest: the plugin's outputs are the ledger, the CLI verbs and a drop-in dashboard extension page reading the same ledger; Hermes's OTLP exporter carries a closed event set with an attribute allow-list, so fleet outcome gauges are NOT published through it — Grafana parity, if wanted, is a script-only cron job writing a Prometheus textfile from `hermes outcomes --json`, and a plugin-emitted monitoring event type is a P8 upstream ask. Adopt and do not rebuild: gateway/platform/cron health gauges, lifecycle traces and diagnostics logs and the per-profile Analytics tab. |
| CH-F49 | Channel adapter registry, Chat SDK bridge, built-in cli/dashboard channels | PARTIAL | PLUGIN |  | ADOPT | O1,O7 | ADOPT: a new channel in Hermes is a `kind: platform` plugin subclassing BasePlatformAdapter and self-registering through ctx.register_platform with zero core edits, and the base class already supplies the text fallbacks NanoClaw's capability flags existed to negotiate. Deliverable is a doc page mapping the NanoClaw adapter registry, the `instance` field and the built-in cli/dashboard pseudo-channels onto Hermes — instance ⇒ one bot = one profile, cli ⇒ `hermes chat` and the desktop app, dashboard ⇒ the dashboard Chat tab — plus one acceptance test. The doc must repeat the port-binding rule from CH-F52/LOOP-F40: `webhook`, `api_server` and any other port-binding platform are configured ONLY on the default (multiplexer) profile, because a secondary profile enabling one makes the gateway skip that whole profile at startup. Not ported: the Chat SDK bridge (a Node artifact of the NanoClaw host with no counterpart here) and the Linear/Webex/Resend/Dial/DeltaChat/Emacs adapters, which no outcome asks for; humans are already covered by the bundled Slack/Discord/Telegram adapters and GitHub ingress is CH-F52. |
| CH-F50 | Interactive operations (cards, questions, reactions, files) | PARTIAL | PLUGIN_PLUS_HOOK |  | ADOPT | O5,O7 | ADOPT: questions with up to four choices and an 'Other' text-capture row, approval cards with Allow once/session/always/Deny, document sends and reactions all exist, with a documented numbered-text fallback for any adapter that cannot render buttons, so the NanoClaw card/question/reaction/file surface is documented onto Hermes rather than rebuilt as register_tool wrappers over the send engine. The doc must state the panel consequence that COST-F30 depends on: the desktop app's transport (APIServerAdapter) has no `send_exec_approval`, so in the one panel every card is text plus a command and the button variants are an adapter bonus. Deliverable is that doc page plus one acceptance test across a button adapter and a fallback adapter. The single honest residue — clarify entries live in `tools/clarify_gateway._entries`, in memory, so a restart loses a parked prompt and there is no pending_questions table — is filed as this row's one P8 upstream ask (persist the entry and re-arm on start, with the click written back as a typed response); until it lands the doc states plainly that a parked question is re-asked on the next turn. |
| CH-F51 | Outbound destination ACL and delivery retries | PARTIAL | PLUGIN | Y | MERGE→LOOP-F37 | O3,O4 | MERGE→LOOP-F37: the destination ACL is room membership plus the wiring-edge predicate of the single `nv-fleet-gates` pre_tool_call callback, and this id survives there as the criterion 'a bot's attempt to reach a peer it is not wired to is refused, not merely logged' (P5 criterion). Room membership itself is NOT rendered by the compose plugin: rooms are created and members set through the gateway's `groups.*` JSON-RPC (invoked by `hermes onboard coworker` for the fleet's standing rooms, by the operator in the desktop app afterwards), and profile `ui_meta` is only ever written through the profile-configure RPC with `ui_meta_expected_revisions`. The retry half needs no work at all — the delivery-obligation ledger (pending/attempting/delivered/failed, capped attempts, explicit may-be-duplicate marker, startup and adapter-reconnect sweeps) and the persistent dead-target registry already exceed NanoClaw's delivery_attempts counter, and are recorded as an adopt inside that ADR rather than as a row of their own. |
| CH-F52 | GitHub webhook receiver and issue/PR routing | PARTIAL | PLUGIN |  | CONFIGURE | O1 | CONFIGURE: O1's ingress is a set of webhook routes rendered by the compose plugin into the DEFAULT (multiplexer) profile's `config.yaml` — never into a coworker profile, because enabling a port-binding platform on a secondary profile makes the gateway skip that whole profile at startup. Each route carries its own `secret` and an explicit `profile:` binding (`orchestrator` for issues.opened, `reviewer` and `fixer` for PR/review/check events), its X-GitHub-Event filter, its injected review skills and a prompt template over the payload; traffic arrives at `/p/<profile>/webhooks/<route>`, installed with `hermes webhook subscribe` and hot-reloaded. Durable ownership is a kanban card keyed `<repo>#<pr>` whose single-writer claim is first-claim-wins (GOV-F25), so no per-PR session mapping and no owner table are built; the mention gate and ownership exemption are declarative route filters or, at worst, one route script under ~/.hermes/scripts. One caveat the doc page must state: `deliver: github_comment` shells `gh` on the gateway host, which is the wrong principal under rule 3 and O4 — attributable post-backs are the owning bot's own `gh` call inside its podman sandbox with its OneCLI identity, and `github_comment` is reserved for unattributed notices. |
| CH-F53 | Cross-instance routing (canonical router plus peer forwarding) | PARTIAL | PLUGIN | Y | DEFER | none | DEFER, reason recorded: one gateway per fleet means there is no second instance to route to, so no owner_instance column, no re-signing of raw webhook bodies with a plugin-owned internal secret, and no X-Webhook-Trust: pre-validated header get built. Because this row is deferred, no other row ships peering config or a peering gate: `a2a_call` is not registered in this fleet (A2A-F18) and `A2A_MAX_PINGPONG_TURNS` is not rendered (A2A-F21). The adopt path is named for the day a real tenant boundary appears (another team or another machine): register the peer with `hermes peer` against its api_server, let a2a's per-peer bearer identity and trusted-peer allow-list carry trust in place of one shared secret, cross-gateway room work goes through hosted-room grants, and any cross-gateway claim table uses the shared fenced compare-and-set helper rather than a hand-rolled CAS. Revisit only on that trigger. |
| SELF-F54 | Self-modification tier 1 and agent-spawned agents | PARTIAL | PLUGIN_PLUS_HOOK |  | MERGE→LOOP-F35 | O2,O4 | MERGE→LOOP-F35, keeping this id there as the criterion 'a non-orchestrator profile's onboard/create-agent call is refused by the nv-fleet-gates veto'. Agent-spawned agents are the orchestrator-only gateway-side tool over create_profile that `nv-coworker-compose` registers with `ctx.register_tool(check_fn=...)` so a worker never sees it in its schema; package installation is contained by the per-profile podman sandbox (P4, ISO-F13/F14) rather than by an approval-gated install_packages tool with an image rebuild and a restart dance, and the same veto predicate can carry a pip/apt command rule if one is wanted; adding an MCP server is a config write picked up by the hot-reload watcher, i.e. configuration under GOV-F27 — but note GOV-F27's rule that a bot editing its own `mcp_servers` block buys nothing, because the veto denies `mcp_*` tools outside the rendered allow-list, and no profile can reach its own config from inside its sandbox. Also note in that ADR that Hermes's self-repo git guard, protected-instruction-file write approval and staged write-approval are stronger than the read-only /app/src mount they replace, so tier-1 self-modification needs no new safety surface. |
| SELF-F55 | Agent templates (Agent Plugins 1.0.0) and plugin MCP servers | BETTER_IN_HERMES | CONFIG_ONLY | Y | CONFIGURE | O2 | CONFIGURE: each coworker type is authored once as a profile distribution repo (distribution.yaml, SOUL.md, config.yaml, skills/, cron/ with jobs authored `enabled: false` to mirror NanoClaw's paused recurring tasks, mcp.json) and installed with `hermes profile install <source>`, with per-profile credentials filled from .env.EXAMPLE and never carried in the repo. Ref pinning is NOT available — install shallow-clones the default branch and upstream records ref pinning as planned-not-shipped — so the fleet pins by immutability of the SOURCE: the compose plugin renders into a per-release branch (`rel/<date>`) or a release-tag-only mirror that each profile's recorded source points at, and the runbook records the commit SHA from `hermes profile info <name>` after every install. `hermes profile update` overwrites only distribution-owned files (SOUL.md, config.yaml, mcp.json, skills/, cron/, distribution.yaml) and preserves config.yaml unless `--force-config`, so any re-rendered config key requires `--force-config` in the update step — the runbook says so explicitly. This row's deliverable is the distribution layout the compose plugin (LOOP-F35) must emit, a doc page mapping NanoClaw's agent templates and container.json onto distribution.yaml + config.yaml, and one acceptance test; ref pinning on `profile install` is this row's single P8 upstream ask. The second lane is Agent Plugins v1: a package installed with `--no-enable` and enabled per profile, its skills namespaced and its portable mcp.json merged into MCP discovery with PLUGIN_ROOT/PLUGIN_DATA expansion, so a coworker's MCP servers travel with its type. The only NanoClaw property the loader lacks is stricter symlink/byte-cap hardening — record it as an optional pre-install check, not a blocker. |
| SELF-F56 | Project and coworker onboarding | PARTIAL | PLUGIN |  | MERGE→LOOP-F35 | O2,O1 | MERGE→LOOP-F35. The onboarding entry points are three CLI verbs on the one `nv-coworker-compose` plugin — `hermes coworker compose`, `hermes onboard project <github-url\|path>`, `hermes onboard coworker <coworker-types.yaml>` — with the two onboarding verbs also exposed as `/onboard-project` / `/onboard-coworker` via ctx.register_command and as gateway-side tools via `ctx.register_tool(check_fn=...)` so only the orchestrator's schema carries them, and the single nv-fleet-gates veto denying them by tool name to every other profile (rule 4). `compose` renders; `onboard coworker` renders, calls create_profile per role with the description the kanban decomposer routes on, installs the rendered distribution, writes each profile's `ui_meta['hermes-bots']` through the profile-configure RPC (per-key CAS, never a raw profile.yaml overwrite) so the bots actually appear in the Bots list and `message_agent` is injected, creates each profile's canonical `Bot Chat` session (title must equal BOT_CHAT_TITLE) so headless bot-to-bot DMs work via `hermes -p <bot> chat -c "Bot Chat"`, and creates the fleet's standing rooms through `groups.create` / `groups.state`; `onboard project` runs the aux-LLM repo scan reusing the /init prompt and emits a project distribution (SOUL.md spine, skills/<capability>/SKILL.md bodies, workflow skills, cron/, mcp.json with DeepWiki for OSS repos). State is profiles, distributions, room state and kanban assignees only — no entity tables, no per-bot gateway, no runtime of its own, no a2a inside the fleet. This id survives in LOOP-F35's ADR as the criterion 'a worker profile's onboard call is refused by the veto'. |
| SELF-F57 | Skill engine, versioned updates, zero-downtime skill refresh, supply chain (comp | BETTER_IN_HERMES | CONFIG_ONLY | Y | ADOPT | O2,O8 | ADOPT: coworker skills ship as the distribution's `skills/` (or a skills tap); a bump is `hermes profile update <bot>` or `hermes skills update` and touches only that profile, replacing distribution-owned files and leaving memories, sessions, auth and .env untouched — with `--force-config` required for a re-rendered config.yaml key to land. Version identity is the source branch plus the SHA reported by `hermes profile info`, not a ref flag: ref pinning is not shipped and is carried as SELF-F55's upstream ask. The skill index is a 30 s mtime cache, so an on-disk change is live in a running gateway with no restart and no other bot disturbed. The update pipeline is adopted as-is — read-only plan phase, machine-readable receipts, a single cross-process update marker shared by CLI, dashboard and desktop updaters, a per-profile current\|stale\|unknown fleet version matrix, refusal to self-update an image-managed install, and restart recovery from a clean interpreter — together with skill provenance (quarantine, scanner, content-hash lock, per-mutation ledger). Deliverable is a doc page mapping NanoClaw's skill engine, /update-nanoclaw and pnpm minimumReleaseAge policy onto those surfaces, plus one acceptance test; the shared-directory fan-out half is SELF-F57.b and the nc: fence engine is SELF-F57.a (deferred). |
| SELF-F57.a | Sub-feature: deterministic, journaled, idempotent SKILL.md `nc:` directive engin | MISSING | N/A |  | DEFER | none | DEFER, reason recorded: the property the nc: engine bought — 'setup and the agent install identically, deterministically, once' — comes for free from `hermes plugins install <repo> --ref <sha>` and `hermes profile install`, one path for both callers, with the manifest declaring env, dependencies and load order, `.example` files never overwritten, Python dependencies printed rather than silently installed, and `hermes plugins doctor` able to load the plugin in a throwaway HERMES_HOME with the network blocked. If a deterministic post-install step is ever genuinely needed it is a single `hermes <plugin> apply` CLI command registered by that plugin and logged, not a fence grammar, a parser, a linter and a journal. Nothing is dispatched for this row. |
| SELF-F57.b | Sub-feature: hourly external-skill fetch mirrored into every bot with no contain | PARTIAL | CONFIG_ONLY |  | CONFIGURE | O2,O8 | CONFIGURE: every coworker profile's config.yaml gets `skills.external_dirs: [/data/shared-skills]`, baked into the distribution's config.yaml by the compose plugin, so one directory feeds every bot on the one gateway and a change there is picked up within the 30 s mtime cache with no gateway restart and no session interruption — the zero-downtime property NanoClaw achieved with a per-container copy plus a refresh sweep. The hourly fetch is a script-only cron job (`hermes cron create --schedule "every 1h" --script refresh-skills.sh --no-agent`, script under ~/.hermes/scripts, no provider credentials inherited) doing the git pull of the external skills repo, or a loop of `hermes -p <bot> skills update` for hub-managed taps. Write access to that directory is governed by MEM-F43 (an external dir is explicitly not a write boundary), not by this row. The doc page states the limit plainly — no automatic hub update, no built-in cross-profile fan-out — so the cron job is understood as the mechanism, not a workaround. No plugin, no hook, no core change. |
| OPS-F58 | ncl admin CLI (socket + in-container transport, per-command access), migrations, | PARTIAL | PLUGIN | Y | ADOPT | O7,O3 | ADOPT the native admin surface and build nothing called ncl: `hermes profile` is the agent-group analog, `hermes cron` the tasks analog, `hermes sessions` the sessions analog, `hermes kanban` the board, plus plugins/skills/config/gateway/webhook/secrets/egress/monitoring/insights/pairing — all reachable from the one desktop app and dashboard, which is what O7 asks for. That verb surface is the OPERATOR's, run on the gateway host or from the desktop app; an agent never invokes it, because its shell is sandboxed with no `$HERMES_HOME` mount and those verbs are inert inside a coworker. The fleet-admin actions a coworker legitimately needs are gateway-side plugin TOOLS registered by `nv-coworker-compose` / `nv-fleet-gates` with `check_fn` so only the orchestrator's schema carries them, and the single veto callback denies them BY TOOL NAME to every other profile — that is the refusal, and this id survives as a criterion there (GOV-F26's surviving criteria). Deliverable is a doc page mapping every ncl resource and verb onto a native one (and stating which have no analog because the baseline removed the concept), plus one acceptance test. Three residues, each owned elsewhere and none of them an ncl clone: cost-cap verbs (COST-F29/F30), PR ownership (GOV-F25), and room administration — no `hermes` CLI verb exists and the only HTTP surface is the cross-gateway membership-grant set, so creation and local membership stay desktop-only over `groups.*` RPC, which is A2A-F18's problem to name and RT-F01's recorded thin spot. |
| OPS-F58.a | Sub-feature: ops tooling — migrations runner, setup wizard + service install, un | BETTER_IN_HERMES | CONFIG_ONLY | Y | CONFIGURE | O8,O7 | CONFIGURE: adopt the ops stack — declarative state-DB column reconciliation on every start in place of a hand-written migrations runner, table-driven `hermes config migrate`, rotating redacted logs, `hermes doctor` and `hermes debug share` for triage, `hermes uninstall --dry-run` — and carry over only documentation: one slang-coworkers on-call runbook page. The fleet container runs ONE gateway: the default profile's, with `gateway.multiplex_profiles: true` and `multiplex_profile_allowlist` naming the roster, supervised by systemd `Type=notify` + WatchdogSec (launchd KeepAlive on macOS) plus gateway/shutdown_watchdog.py. Because `container_boot.py` reconciles per-profile s6 slots (one process per profile), the fleet image leaves every coworker profile at `desired_state != running` so only the default slot starts, and the runbook states that `hermes -p <bot> gateway start` is a hard error while the multiplexer runs. Configuration deliverables, rendered by the compose plugin: `sessions.auto_prune: false` on the DEFAULT (multiplexer) profile AND on every coworker profile — the startup sweep reads the launch profile's `sessions.*` while `hermes -p <bot>` CLI, cron and kanban-worker starts read the invoked profile's, and the sweep deletes pruned sessions' transcripts and `request_dump_*` files (MEM-F44, OBS-F46) — plus an explicit decision on `checkpoints.auto_prune` rather than its True default, with pruning done deliberately from the runbook; and a runbook step treating config migrations 39→41 as irreversible, taking a backup first and re-rendering SOUL.md from the compose plugin afterwards. `hermes gateway install` itself lands with the fleet phase (P6). |
