- RT-F01 Entity model with user-level privilege: Users (`<channel>:<handle>`) -> messaging_groups (one chat per platform, per adapter `instance`) -> many-to-many wirings `messaging_group_agents` -> agent_groups -> sessions keyed (agent_group_id, messaging_group_id, thr [FR-2, SR-4] (src/db/schema.ts:11-112; src/modules/permissions/access.ts:16-23)
- RT-F02 Router fan-out with engage modes and sender scope: `routeInbound` evaluates every wired agent independently: `engage_mode` pattern|mention|mention-sticky (with `engage_pattern` regex and `{name}` substitution), `sender_scope` gate, access gate, `ignored_message_policy` a [FR-2] (src/router.ts:348-450; src/router.ts:493-530)
- RT-F03 Session modes and canonical GitHub-chain collapse: `session_mode` shared | per-thread | agent-shared (the three isolation levels) is resolved in `resolveSession` under a per-key creation lock, and `^gh-(issue|pr)-` thread ids collapse every a2a sender plus the webhook se [FR-2, SR-5] (src/session-manager.ts:118-175; docs/isolation-model.md:7-50; src/reconcile-gh-sessions.ts:1-18)
- RT-F04 Adapter-declared channel defaults and live thread policy: Each adapter declares `ChannelDefaults` (dm/group contexts: engageMode, engagePattern, threads, unknownSenderPolicy; mentions platform|dm-only|never) with a behavior-faithful fallback for undeclared adapters, and `messag [FR-2] (src/channels/channel-defaults.ts:72-135; src/router.ts:224-231; docs/api-details.md:62-108)
- RT-F05 Admission flows: unknown_sender_policy and channel-registration approval: `messaging_groups.unknown_sender_policy` strict|request_approval|public gates non-members (Approve/Deny card DM'd to an owner/admin, `pending_sender_approvals` holds the original event, on approve add `agent_group_member [FR-2, SR-4, FR-4] (src/modules/permissions/sender-approval.ts:1-18; src/modules/permissions/channel-approval.ts:1-18; s)
- RT-F06 Dropped-messages ledger: Every silently unrouted message is recorded in `unregistered_senders` with reason `no_agent_wired` | `no_agent_engaged`, per (channel, chat, sender) counts and first/last-seen timestamps, readable via `ncl dropped-messag [FR-2] (src/router.ts:297-306; src/router.ts:452-461; src/cli/resources/dropped-messages.ts:4-26)
- RT-F07 Cold-DM cache (user_dms) via ensureUserDm: `ensureUserDm(userId)` lazily mints or reuses the messaging_group the host should DM a user on, using two-class resolution (direct-addressable handles vs Chat-SDK `openDM`) cached in `user_dms`; every approval card, pair [FR-4] (src/modules/permissions/user-dm.ts:1-18; src/cli/resources/user-dms.ts:4-19)
- RT-F08 Cross-session context (session-echo fan-out and pull): Triggering user messages and delivered agent replies fan into sibling sessions of the SAME messaging group as trigger=0 `session-echo` rows that never wake, never route, are loop-guarded and pruned by the sweep, and rend [FR-2] (src/modules/cross-session-context/fan.ts:1-25; src/cli/resources/sessions.ts:83-92; container/agent-)
- RT-F09 Pluggable router seams (interceptors, gates, session-created hooks): Modules register message interceptors (first claim wins, e.g. reject-reason capture), a sender resolver, access gate, sender-scope gate, channel-request gate and session-created hooks (e.g. the `welcome` skill greeting a [FR-2] (src/router.ts:52-191; container/skills/welcome/SKILL.md:2-4)
- ISO-F10 One container per session, two-DB mailbox, seq parity, heartbeat: Each session owns `inbound.db` (host writes: messages_in, destinations, session_routing) and `outbound.db` (container writes: messages_out, processing_ack, session_state, container_state) with exactly one writer per file [SR-1, PR-1] (docs/db-session.md:114-121; container/agent-runner/src/heartbeat.ts:11-27; src/session-manager.ts:53)
- ISO-F11 Idempotent claim/ack lifecycle with host-side retry backoff: The container claims a batch by upserting `processing_ack` (processing -> completed, acked even on provider error), the host mirrors it onto `messages_in.status`, stale or crashed claims are reset with `tries`++ and `pro [PR-1, PR-3, NF-5] (docs/architecture.md:492-528; src/host-sweep.ts:76-80; src/host-sweep.ts:711)
- ISO-F12 on_wake messages, race-free restart, paused kill switch: `messages_in.on_wake=1` rows are visible only to a fresh container's first poll; `restartAgentGroupContainers` writes on_wake, kills, then respawns via `onExit` (`ncl groups restart --id [--rebuild] [--message]`, auto-sc [NF-5, FR-3] (src/container-restart.ts:1-25; container/agent-runner/src/mailbox/sqlite/operations.ts:25-33; src/db)
- ISO-F13 Mount composition policy, mount allowlist, inbox containment: Mounts carry classes and scopes (`install-surface` pinned `ro`, `/app/src` runner source read-only for every group, group-state RW), `additionalMounts` are validated against `~/.config/nanoclaw/mount-allowlist.json` (all [SR-1, SR-5] (src/container-runner.ts:2191-2213; src/modules/mount-security/index.ts:1-8; src/inbox-safety.ts:1-15)
- ISO-F14 Egress lockdown through the credential gateway: `NANOCLAW_EGRESS_LOCKDOWN=true` places agents on a Docker `--internal` network with the OneCLI gateway as the only reachable hop (non-root, no NET_ADMIN, agent cannot undo it) and fails the spawn rather than ever running [SR-2, SR-5] (src/egress-lockdown.ts:1-30)
- ISO-F15 Session driver seam (docker | local no-Docker | vm tier) and gateway-provider seam: `NANOCLAW_RUNTIME_DRIVER` selects a self-registered SessionDriver -- `docker` (default) or `local` (runner spawned as a host `bun` child with container paths remapped, no isolation, managed Claude auth via host HOME) --  [SR-1, NF-2] (src/drivers/index.ts:1-14; src/drivers/local-driver.ts:1-26; src/gateway-providers/none.ts:1-11; src)
- ISO-F16 Per-group container config and multi-provider runner: A `container_configs` row per agent group (provider claude|codex|opencode|pi, model, effort, fastMode, apt/npm packages, mcpServers, additionalMounts, imageTag, skills, timezone, maxMessagesPerPrompt, assistantName) is m [FR-10, NF-2] (src/container-config.ts:240-281; src/cli/resources/groups.ts:611-789; container/agent-runner/src/db/)
- ISO-F17 Host sweep and restart recovery (circuit breaker, lease, coordination tables): A 60s per-session sweep does ack sync, stale detection via heartbeat mtime widened by declared Bash timeouts and capped by `CONTAINER_TIMEOUT` (30m), due-message wake, CLAUDE.md-staleness respawn, ghost-approval finaliza [NF-5, PR-2] (src/host-sweep.ts:68-80; src/host-sweep.ts:183-350; src/circuit-breaker.ts:7-11; src/db/coordination)
- A2A-F18 Wired-only agent messaging (destinations = routing map + ACL): `send_message(to=<name>)` with `channel_type='agent'` is delivered only if an `agent_destinations` row (per-source local name -> target) exists -- no row = unauthorized, fail-closed -- created by admin `wire_agents` (bid [SR-3, FR-2] (src/db/migrations/module-agent-to-agent-destinations.ts:5-16; src/modules/agent-to-agent/wire-agents)
- A2A-F19 a2a.send guard (self / lineage / destination) plus per-edge human approval policies: The guard allows self-send, allows a reply upward to a genuine ancestor via `a2a_session_sources` lineage, otherwise requires a destination row, denies unknown targets, and HOLDs even an authorized send when an `agent_me [SR-3, SR-4, FR-4] (src/modules/agent-to-agent/guard.ts:61-107; src/cli/resources/policies.ts:6-20; src/modules/agent-to)
- A2A-F20 Reply-home lineage (a2a_session_sources, source_session_id, in_reply_to): Every a2a inbound row records `source_session_id`, and `a2a_session_sources` maps each recipient session to the delegating session/thread so replies route back to the exact originating session rather than minting a new o [FR-2] (src/db/migrations/920-a2a-session-sources.ts:3-14; src/modules/agent-to-agent/agent-route.ts:273-299)
- A2A-F21 Runaway protection: echo-drop, bounced-a2a redrive, runaway card: Host-local echo-drop skips the container wake (row persisted as trigger=0) for known no-op acks or >=3 identical texts within 120s from one source; failed a2a turns are classified permanent|transient|unknown and redriven [NF-6, PR-3] (src/modules/runaway/echo-drop.ts:1-27; container/agent-runner/src/transient-error.ts:1-16; src/host-)
- GOV-F22 Guard catalog (allow | hold | deny) with grant replay: `defineGuardedAction` mints nominal, enumerable actions (approval_ledger.record_decision, approval_ledger.record_human_verdict, self_mod.install_packages, self_mod.add_mcp_server, senders.admit, channels.register, agents [SR-4, NF-2] (src/guard/guard-actions.ts:1-61; src/delivery.ts:728-730; src/guard/conformance.test.ts:2-9)
- GOV-F23 Approvals primitive (approver picking, cards, reject-with-reason, ghost finalizer): `requestApproval` persists a `pending_approvals` row and delivers a card via `pickApprover` (scoped admins -> global admins -> owners) and `pickApprovalDelivery` (`ensureUserDm` with same-channel tie-break) so it renders [FR-4, SR-4] (src/modules/approvals/primitive.ts:1-14; src/modules/approvals/response-handler.ts:1-14; src/modules)
- GOV-F24 Immutable approval-decision ledger (approval_decisions): PR-approver coworkers call `record_decision` (WOULD_APPROVE | ABSTAIN_POLICY | BLOCK) and the host appends to `approval_decisions` keyed (repo, pr, commit_sha), append-only first-write-wins (identical repeat = no-op, dif [SR-7] (src/modules/approval-ledger/store.ts:1-18; container/agent-runner/src/mcp-tools/core.ts:550-558; src)
- GOV-F25 PR-to-session mappings, first-claim-wins: `report_pr_created` (auto-prompted by the `pr-auto-map.sh` PostToolUse hook when `gh pr create` succeeds) writes `pr_session_mappings` so that PR's webhooks route to the creating session; a claim binds on first write (th [FR-2, SR-7, PR-3] (src/modules/pr-mapping/store.ts:1-27; container/agent-runner/src/mcp-tools/core.ts:515-522; containe)
- GOV-F26 cli_scope enforcement and host command gate: `container_configs.cli_scope` disabled|group|global: disabled strips ncl instructions from the spine and rejects any `cli_request`; group auto-fills `--id`, allows only `GROUP_SCOPE_RESOURCES` {groups, sessions, destinat [SR-4] (src/cli/dispatch.ts:78-80; src/cli/registry.ts:20; src/cli/guard.ts:72; src/command-gate.ts:1-15)
- GOV-F27 MCP tool allow-list, authenticated MCP proxy, server registry: `agent_groups.allowed_mcp_tools` (single resolver, `ncl groups mcp-tools get/set` restarts the group) governs EXTERNAL MCP servers only while each built-in tool answers to its own named gate; external servers run behind  [SR-4, SR-2] (src/mcp-allowlist.ts:1-14; src/mcp-auth-proxy.ts:1-10; src/mcp-registry.ts:1-9; docs/mcp-allowlist.m)
- CRED-F28 OneCLI credential gateway (per-agent identity, request-time injection, credential approvals): At spawn the gateway provider calls `ensureAgent({name, identifier=agent_group_id})` and contributes typed proxy env plus CA mounts; credentials are injected into outbound HTTPS at request time (never env or chat; new ag [SR-2, SR-6, SR-4] (src/gateway-providers/onecli.ts:1-12; src/gateway-providers/onecli.ts:129; src/modules/approvals/one)
- COST-F29 Two-tier per-session cost cap with immortal sessions and runtime policy: Tier-1 cap = the group's own 7-day p90 (`data/cost-thresholds.json`, escalate to a human with Continue/Stop) and Tier-2 ceiling (typ. $150) hard-stops non-immortal sessions at turn granularity, while immortal groups (`is [NF-6, FR-4] (docs/cost-cap-model.md:19-80; container/agent-runner/src/poll-loop.ts:87-160; container/agent-runner)
- COST-F30 Cost escalation approval card and exact-value set-ceiling: The runner emits a `cost_escalation` system row and the host DMs the approver through `requestApproval` (Continue/Stop for lifetime windows, Continue-only for immortal), persisting `cost_escalation_episodes` with a compa [FR-4, NF-6] (src/delivery.ts:826-846; src/modules/cost-approval/index.ts:1-14; src/db/cost-escalation-episodes.ts)
- COST-F31 Durable cost ledger, pricing parity, cost observability: An append-only `cost_events` table (tokens not dollars, identity PK with INSERT OR IGNORE so codex forks no-op) and a versioned LiteLLM rate table copied verbatim runner<->dashboard are checked by a standing parity harne [NF-1] (container/agent-runner/src/cost-events.ts:1-16; container/agent-runner/src/pricing.ts:1-16; scripts/)
- SCHED-F32 Durable scheduled tasks (ncl tasks) with cron recurrence and run logs: Tasks are `messages_in` rows (prompt, optional script, `process_after`, `recurrence` cron, `series_id`) in an isolated `system:tasks:<series>` session per series, managed by `ncl tasks create/update/cancel/pause/resume/r [FR-9] (src/cli/resources/tasks.ts:426-611; src/modules/scheduling/recurrence.ts:1-15; src/modules/schedulin)
- SCHED-F33 Pre-task script gates with failure backoff and auto-pause: A task's bash `script` runs in-container first (30s timeout, 1MB output) and its last stdout line `{wakeAgent:false}` completes the run with no model call while `{wakeAgent:true,data}` wakes the agent with data injected; [FR-9, NF-6] (container/agent-runner/src/scheduling/task-script.ts:7-32; src/modules/scheduling/recurrence.ts:24-3)
- SCHED-F34 Per-group timezone override: `ncl groups config update --timezone <IANA>` (approval-gated for agent callers; `""` clears) grounds that group's cron interpretation, `--process-after`, and run-log stamps immediately and the container `TZ` on respawn,  [FR-9] (src/cli/resources/groups.ts:611-633; src/modules/scheduling/run-log.ts:28; src/timezone.ts:1-15)
- LOOP-F35 Lego coworker composition (spines, types, traits, composed CLAUDE.md): Typed coworkers are composed at every wake from `container/spines/<project>/coworker-types.yaml` (extends chain base-common -> project-common -> leaf; identity leaf-wins, invariants/context/skills/workflows/overlays appe [FR-11, NF-3, FR-10] (docs/lego-coworker-workflows.md:14-75; container/spines/base/coworker-types.yaml:5-88; container/spi)
- LOOP-F36 Workflows and overlays (durable multi-step loops): Compose-time workflows with `{#step-id}` anchors (`/plan` modes plan|investigate|review|research, `/implement`, project specializations slang-triage-issue, slang-fix-issue, slang-pr-review, slang-pr-approve, slang-mainta [FR-3] (docs/lego-coworker-workflows.md:32-75; container/spines/slang/coworker-types.yaml:43-106; src/db/mig)
- LOOP-F37 Codex critique gate and chain-routing gate (delivery/egress chokepoints): Coworkers record independent read-only codex reviews (`/codex-critique`, `STAGE: DIAGNOSIS_REVIEW|PLAN_REVIEW|CODE_REVIEW|DECISION_REVIEW|OUTPUT_REVIEW`, tracked by `track-critique.sh`); `gate-critique-on-deliver.sh` (Pr [SR-4, FR-4, FR-3] (container/skills/codex-critique/SKILL.md:11-32; container/hooks/gate-critique-on-deliver.sh:2-8; con)
- LOOP-F38 Plan gate, intent router, workflow-state reset, buddy monitor: A hook chain around each turn: `gate-plan.sh` blocks source edits until a plan file exists (opt-in overlay), `plan-tracker`/`track-edits` maintain `workflow-state.json`, `workflow-state-reset.sh` resets plan/critique sta [FR-3] (container/hooks/gate-plan.sh:2-8; container/hooks/workflow-state-reset.sh:2-8; container/hooks/inten)
- LOOP-F39 supervise-issues nudger and self-healing worktree reaping: An orchestrator skill on a 12h cron: `pull-universe.sh` lists active `gh-issue-*` sessions (stamped with `ncl cost-cap status`), `scan.py` computes stuck-time and `awaiting_us` states to decide nudges (a message into the [FR-3, FR-4, NF-6] (container/skills/supervise-issues/SKILL.md:4; container/skills/supervise-issues/SKILL.md:62-76; cont)
- LOOP-F40 PR review/approval coworkers with human-invited post-back and CI-gated release: `slang-reviewer` runs the production claude-code-action protocol locally in parallel with Devin (`nanoclaw-pr-review-runner`) and a clarity pass, posting COMMENT-only reviews only when the dispatch carries `<github-post- [SR-7, SR-4, FR-4] (container/spines/slang/coworker-types.yaml:108-162; container/skills/slang-pr-approver/SKILL.md:3; c)
- MEM-F41 OKF file memory per agent group plus persona prepend: `groups/<folder>/memory/` (mounted `/workspace/agent/memory/`) is an OKF v0.1 bundle -- `index.md` (Core Memory + map) and `system/definition.md` always loaded via Claude's SessionStart hook or the system-prompt door for [FR-8, NF-2] (docs/memory.md:11-80; container/agent-runner/src/memory/context.ts:4-21; container/agent-runner/src/)
- MEM-F42 OKF synthesis (bounded always-loaded memory): The `okf-synthesis` skill on a daily cron gate wakes only when `index.md`/`definition.md` approach the 16k budget or unsynthesized dossiers and oversize catch-all concepts accumulate, folds them into one-concept-per-file [FR-8] (container/skills/okf-synthesis/SKILL.md:2-4)
- MEM-F43 Shared learnings and learnings wiki: The `append_learning` MCP tool (unguarded but attributed) writes `data/shared/learnings/<author-group>/*.md` with author group and session front-matter and rebuilds `INDEX.md`, mounted into every container at `/workspace [FR-8] (src/modules/agent-to-agent/append-learning.ts:1-23; container/agent-runner/src/mcp-tools/learnings.t)
- MEM-F44 Transcript retention, archiving, compaction guidance and fresh project memory: Claude Code `cleanupPeriodDays` is pinned to 3650 so cost-source transcripts are never swept, the PreCompact hook archives and rotates transcripts, every provider writes a `conversations/` exchange archive, PreCompact cu [NF-1, FR-8] (src/group-init.ts:13-26; container/agent-runner/src/providers/claude.ts:294-353; container/agent-run)
- OBS-F45 Dashboard (Pixel Office / Coworkers / Timeline / Admin) with hook-event telemetry: A separate dashboard process reads central and session DBs and ingests real-time hook events (`POST /api/hook-event` -> `hook_events`; Claude hooks and a Codex lifecycle bridge feed the same endpoint; `sdk_session_routes [NF-1, NF-2] (dashboard/server.ts:2-13; dashboard/server.ts:6884; dashboard/server.ts:13486-14010; src/dashboard-i)
- OBS-F46 claude-trace raw request/response capture: A patched claude-trace build is mounted read-only at `/opt/claude-trace` and `CLAUDE_CODE_EXECUTABLE` points the SDK at its wrapper so every session dumps raw API request/response `.jsonl` plus rendered `.html` into `gro [NF-1] (src/container-runner.ts:2213-2233; src/container-runner.ts:2577-2586; scripts/claude-trace-gc.py:2-1)
- OBS-F47 Transcript viewing: show-transcript archive, ncl sessions messages, /upload-trace: `/show-transcript` renders every group's Claude Code and Codex JSONLs into a hierarchical HTML archive (groups -> sessions -> transcripts) served on :8080 via `build-transcripts-archive.ts`, `ncl sessions messages <id>`  [NF-1] (.claude/skills/show-transcript/SKILL.md:3-8; scripts/build-transcripts-archive.ts:1-8; src/cli/resou)
- OBS-F48 Fleet metrics and outcome analytics: `nanoclaw-metrics.py` (systemd oneshot every 60s) emits InfluxDB line protocol for Grafana, and cron-refreshed dashboard panels cover the issue funnel (Routed -> Triaged -> Fix-attempted -> PR-opened -> PR-ready -> CI-gr [NF-1] (ops/README.md:1-25; scripts/funnel.ts:1-10; scripts/regression-quality.py:2-15; scripts/review-round)
- CH-F49 Channel adapter registry, Chat SDK bridge, built-in cli/dashboard channels: Adapters register by name with `supportsThreads`, `defaults`, `subscribe`, `openDM`; the Chat SDK bridge wraps Discord, Slack, Telegram, WhatsApp(+Cloud), Teams, Linear, GitHub, iMessage/Photon, Webex, Resend, Matrix, Go [FR-2] (src/channels/chat-sdk-bridge.ts:1-6; src/channels/cli.ts:1-14; src/channels/dashboard.ts:1-8; src/ch)
- CH-F50 Interactive operations (cards, questions, reactions, files): `ask_user_question` writes a card row and holds the tool call while the host delivers platform buttons and writes the click back as a `question_response` (`pending_questions`), alongside `send_card`, `add_reaction(seq)`, [FR-4] (container/agent-runner/src/mcp-tools/interactive.ts:39-178; src/modules/interactive/index.ts:1-12; s)
- CH-F51 Outbound destination ACL and delivery retries: `deliverMessage` lets an agent reply to its origin chat implicitly but otherwise requires an `agent_destinations` channel row (`ncl destinations add/remove/list`), prefers the sender's own destination-mapped adapter inst [FR-2, PR-3] (src/delivery.ts:405-480; src/delivery.ts:317; src/delivery.ts:522-560)
- CH-F52 GitHub webhook receiver and issue/PR routing: A dedicated HMAC-validated server on `GITHUB_WEBHOOK_PORT` routes PR comments by `pr_session_mappings` (local session / peer forward / orchestrator fallback), sends `issues.opened` always to the orchestrator, sends revie [FR-2] (src/github-webhook-server.ts:1-7; src/github-webhook-server.ts:593-690; src/webhook-github.ts:1-15; )
- CH-F53 Cross-instance routing (canonical router plus peer forwarding): One canonical instance receives every GitHub webhook; `pr_session_mappings.owner_instance` decides local delivery vs forwarding to a peer (`INSTANCE_FORWARD_TARGETS` slug=URL) with the raw body re-signed by `INTERNAL_REG [FR-2, PR-3] (src/modules/pr-mapping/forward.ts:1-15; src/modules/pr-mapping/register-endpoint.ts:1-9; docs/cross-)
- SELF-F54 Self-modification tier 1 and agent-spawned agents: `install_packages` / `add_mcp_server` MCP tools are guard-held for admin approval (validated container- and host-side); on approve the handler updates `container_configs`, rebuilds the image when needed, kills the contai [FR-11, SR-4] (src/modules/self-mod/apply.ts:1-13; src/modules/self-mod/request.ts:1-14; container/agent-runner/src)
- SELF-F55 Agent templates (Agent Plugins 1.0.0) and plugin MCP servers: `ncl groups create --template` and the setup wizard stamp a validated `plugin.json` plugin (hardened copy: no symlinks, special files or path escapes, size caps) into a group-private skills store (`.claude-shared/skills` [FR-11, NF-3] (src/templates/manifest.ts:1-12; src/templates/plugin-dir.ts:1-12; src/templates/tasks.ts:1-18; src/g)
- SELF-F56 Project and coworker onboarding: `/onboard-project <github-url|path>` generates a project spine, 5 capability skills, 2 workflow extensions ({project}-plan/-implement), common/reader/writer coworker types and all 15 trait bindings (DeepWiki for OSS repo [FR-11] (.claude/skills/onboard-project/SKILL.md:3; .claude/skills/onboard-coworker/SKILL.md:3; scripts/init-)
- SELF-F57 Skill engine, versioned updates, zero-downtime skill refresh, supply chain: SKILL.md `nc:` directive fences are applied deterministically and idempotently by `skill-apply.ts` (journaled) so setup and agents perform identical installs; `/update-nanoclaw` is a transactional upstream update with sn [NF-3] (scripts/skill-apply.ts:1-8; src/upgrade-state.ts:1-12; scripts/refresh-skills-cron.sh:2-11; .claude/)
- OPS-F58 ncl admin CLI, migrations, setup/service and runbook: `ncl <resource> <verb>` runs over a 0600 Unix socket (`data/ncl.sock`) on the host or the session-DB `cli_request` transport in-container, covering groups, messaging-groups, wirings, users, roles, members, destinations,  [NF-3, NF-5] (src/cli/socket-server.ts:1-8; container/agent-runner/src/cli/ncl.ts:2-9; src/cli/resources/index.ts:)
